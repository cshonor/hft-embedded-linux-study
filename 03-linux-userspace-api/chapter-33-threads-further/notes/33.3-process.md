# 33.3 进程控制

> 本章：[TLPI 第 33 章 — Threads: Further Details](./README.md)
> - 上一节：[33.2 线程与信号（重难点）](./33.2-thread-signal.md)
> - 下一节：[33.4 实现模型](./33.4-model.md)

---

## 本节讲什么

pthread_atfork(prepare, parent, child); ---

---

## 要点

| 调用 | 多线程行为 |
|------|------------|
| **fork** | 子进程只留调用线程；锁状态危险 → **立刻 exec**；`pthread_atfork` 仅缓解 |
| **exec** | 只留调用线程；handler→DFL |
| **exit** / main return | **整进程**全线程死 |
| 只退主线程 | 主线程 `pthread_exit` |

```c
pthread_atfork(prepare, parent, child);
```

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 进程控制 在嵌入式系统的应用场景 |
| HFT | 进程控制 对低延迟和性能的影响 |

## 代码示例

```c
#include <pthread.h>
#include <unistd.h>
#include <stdio.h>
void *worker(void *arg) {
    printf("线程 PID=%d\n", getpid());
    /* fork 在多线程中只复制调用线程 */
    pid_t pid = fork();
    if (pid == 0) { printf("子进程: 只有 fork 的线程\n"); _exit(0); }
    wait(NULL);
    return NULL;
}
int main(void) {
    pthread_t tid;
    pthread_create(&tid, NULL, worker, NULL);
    pthread_join(tid, NULL);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[33.2 线程与信号（重难点）](./33.2-thread-signal.md)
- 下一节：[33.4 实现模型](./33.4-model.md)
- 前置：**前置**：[Ch29](../chapter-29-threads-intro/notes.md)–[Ch32](../chapter-32-thread-cancellation/notes.md) · [Ch22 sigwait](../chapter-22-signals-advanced/notes.md) · [Ch28 fork](../chapter-28-process-creation-exec-detail/notes.md)  
- 后置：**后置**：[Ch34 进程组/会话](../chapter-34-process-groups-sessions/notes.md)（线程模块收束；daemon 见 [Ch37](../chapter-37-daemons/notes.md)）
