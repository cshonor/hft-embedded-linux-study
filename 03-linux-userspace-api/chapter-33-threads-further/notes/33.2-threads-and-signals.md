# 33.2 线程与信号（重难点）

> 源码核验：Linux v6.6 · `kernel/signal.c`（`wants_signal():987` · `complete_signal():1004` · `rt_sigprocmask():3177` · `do_send_specific():3935`）
> 本章：[TLPI 第 33 章 — Threads: Further Details](../README.md)
> - 上一节：[33.1 线程栈](./33.1-thread-stacks.md)
> - 下一节：[33.3 进程控制](./33.3-threads-and-process-control.md)

---

## 本节讲什么

1. 多线程下信号语义怎么分裂的——哪些东西全进程共享、哪些每线程一份？
2. `kill` 发的信号到底**哪个线程**收到？内核怎么选（不是随机）？
3. `pthread_sigmask` / `pthread_kill` / `sigwait` 三件套怎么配合出「无异步处理器」的架构？

---

## 要点

### 信号模型：共享与私有的精确分界

| 属性 | 作用域 | 说明 |
|------|--------|------|
| 信号**处置**（handler/DFL/IGN） | 进程级共享 | 所有线程共用一张 `sigaction` 表（`sighand` 结构，`CLONE_SIGHAND` 才共享） |
| 信号**掩码** | **每线程独立** | 每个 `task_struct` 自带 `blocked` 位图——**不存在"进程掩码"** |
| 进程级 pending（`shared_pending`） | 进程级 | `kill()`/终端发给进程的信号先进这里 |
| 线程级 pending（`task->pending`） | 每线程独立 | `pthread_kill`/`tgkill` 定向投递；硬件异常（SIGSEGV/SIGFPE）也走这里 |
| handler 的执行 | 跑在**收到信号的那条线程**上 | 用该线程的栈，异步于该线程的控制流 |

⚠ 速记口诀：**处置共享，掩码私有；来源决定队列（进程级/线程级），投递选定线程**。

### 「任选一个未阻塞的线程」——内核的精确算法

教科书常说异步信号「发给任意未阻塞线程」，内核实现（`complete_signal():1004`）其实是一个**带状态的两级选择**：

```c
static void complete_signal(int sig, struct task_struct *p, enum pid_type type)
{
        ...
        if (wants_signal(sig, p))              /* ① 先试"建议线程" p（发送目标/current） */
                t = p;
        else if ((type == PIDTYPE_PID) || thread_group_empty(p))
                return;                        /* 单线程且它不想要 → 挂着 */
        else {
                t = signal->curr_target;       /* ② 从上次投递目标开始轮转 */
                while (!wants_signal(sig, t)) {
                        t = next_thread(t);
                        if (t == signal->curr_target)
                                return;        /* 全组都不想要 → 挂在共享队列 */
                }
                signal->curr_target = t;       /* 更新轮转游标 */
        }
        ...
        signal_wake_up(t, sig == SIGKILL);     /* 唤醒被选中的线程 */
}
```

`wants_signal():987` 的判定（不想要的四种情况）：

```c
if (sigismember(&p->blocked, sig))      return false;  /* 阻塞了 */
if (p->flags & PF_EXITING)              return false;  /* 快死了 */
if (task_is_stopped_or_traced(p))       return false;  /* 停止/被追踪 */
return task_curr(p) || !task_sigpending(p);            /* 正在跑或没有积压 */
```

四个推论：

1. **不是随机**：从 `curr_target` 轮转，连续发同信号会依次落到不同线程（但若某线程独占解除阻塞，`wants_signal` 只有它满足，全部进它）；
2. **全组都阻塞 → 挂在共享 pending 队列**，谁先解除阻塞谁在返回用户态时优先取走；
3. **定向信号（`pthread_kill`/`tgkill`，`PIDTYPE_PID`）**跳过轮转直接指名——这就是"仅同进程内"的 `pthread_kill` 内核路径（`do_send_specific():3935`，Ch32 已核验）；
4. **致命信号广播**：`complete_signal` 下半段发现 `sig_fatal` 且非 core 场景，直接给**全组每个线程**塞 SIGKILL 并 `SIGNAL_GROUP_EXIT`——一个线程杀全组。

### 三件套 API

```c
int pthread_sigmask(int how, const sigset_t *set, sigset_t *oldset);
int pthread_kill(pthread_t thread, int sig);   /* 仅同进程；跨进程用 tgkill(2) */
int sigwait(const sigset_t *set, int *sig);    /* 同步取走：不装 handler */
```

| API | 内核真身 | 要点 |
|-----|---------|------|
| `pthread_sigmask` | `rt_sigprocmask():3177` 系统调用（glibc 薄包装） | 与 `sigprocmask` 同一 syscall；多线程程序里**禁止**用 `sigprocmask`（SUSv3 已废除其 MT 语义） |
| `pthread_kill` | `tgkill(tgid, tid, sig)` → `do_send_specific():3935` | 线程级 pending 队列；目标线程已退出 → `ESRCH` |
| `sigwait` | 原子「取消阻塞 + 等待」循环（glibc 用户态实现） | **前提**：调用线程已阻塞这些信号；取走后**不执行 handler** |

`sigwait` 的原子性：它先检查共享+线程 pending 队列，没有就睡（`sigtimedwait` syscall）——**没有「先解除阻塞再等」的窗口**，信号不会在这中间漏给别的线程或触发 handler。

### 推荐架构：sigwait 专用线程

```text
main 启动（最先做的事）
  │ pthread_sigmask(SIG_BLOCK, {SIGINT,SIGTERM,SIGHUP,...})   ← 此后创建的线程全部继承阻塞
  │
  ├── 信号线程：sigwait(&set, &sig) → 翻译成内部事件（管道/事件fd/原子标志）
  ├── 业务线程 A/B/C：不装 handler，永不接收信号（全阻塞）
  └── 主线程：join / 事件循环
```

收益（相对 async handler）：

1. handler 里**什么都不能调**（async-signal-safe 限制）→ sigwait 线程是普通线程，随便用 `printf/malloc/lock`；
2. 异步信号在任意业务线程的任意指令间打断 → 确定性灾难（HFT 热路径毛刺）→ sigwait 线程集中处理，业务线程零干扰；
3. 信号语义从「异步中断」变成「同步消息」——可以排队、可以日志、可以优雅退出。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| HFT | 信号线程 + 全员阻塞是**部署标准**：热路径线程绝不允许被 SIGALRM/自定义信号异步打断；优雅退出统一走 sigwait → 广播 shutdown 事件 |
| 嵌入式 | `kill(pid, sig)` 的进程级投递 + `curr_target` 轮转意味着「信号可能落在任何线程」——信号驱动的单线程假设在 MT 化后直接破坏；sigwait 模式让信号处理可预测 |
| 通用 | `pthread_kill` 定向投递是给单线程「发中断」的合法手段（如打断阻塞中的 IO 线程），但要配 handler 或 sigwait 消费 |

## 代码示例

```c
/* sig_model.c — Ch33.2：信号投递模型三组实测
 * demo1: 3 线程全阻塞，只有 worker1 解除阻塞 SIGUSR1 → kill() 进程级信号必落 worker1
 * demo2: pthread_kill 定向 → 只有目标线程的 handler 跑
 * demo3: sigwait 线程取走 SIGTERM（不触发 handler）
 * 编译：gcc -O2 -Wall -Wextra -o sig_model sig_model.c -pthread
 */
#define _GNU_SOURCE
#include <stdio.h>
#include <string.h>
#include <pthread.h>
#include <signal.h>
#include <unistd.h>
#include <errno.h>
#include <sys/syscall.h>

static volatile sig_atomic_t got_in[4];
static volatile sig_atomic_t handler_ran;
static int unblock_all;                       /* demo2 置 1：全员解除阻塞 */

static void handler(int sig)
{
    handler_ran = 1;
    int tid = (int)syscall(SYS_gettid);
    for (int i = 0; i < 4; i++)
        if (got_in[i] == -tid) { got_in[i] = sig; return; }
}

static void *worker(void *arg)
{
    long id = (long)arg;
    got_in[id] = -(int)syscall(SYS_gettid);    /* 登记内核 TID（负数=待命） */

    if (id == 1 || unblock_all) {
        sigset_t set;
        sigemptyset(&set);
        sigaddset(&set, SIGUSR1);
        pthread_sigmask(SIG_UNBLOCK, &set, NULL);
    }
    sleep(3);                                  /* 等信号窗口 */
    return NULL;
}

static void *sigwaiter(void *arg)
{
    (void)arg;
    int sig;
    sigset_t set;
    sigemptyset(&set);
    sigaddset(&set, SIGTERM);
    for (;;) {
        if (sigwait(&set, &sig) != 0) break;
        printf("  [sigwait 线程] 取到信号 %d（handler_ran=%d，handler 没跑）\n",
               sig, (int)handler_ran);
        if (sig == SIGTERM) return NULL;
    }
    return NULL;
}

int main(void)
{
    /* 1. 创建任何线程前阻塞全部实验信号 → 所有线程继承 */
    sigset_t all;
    sigemptyset(&all);
    sigaddset(&all, SIGUSR1);
    sigaddset(&all, SIGTERM);
    pthread_sigmask(SIG_BLOCK, &all, NULL);

    struct sigaction sa = {0};
    sa.sa_handler = handler;
    sigaction(SIGUSR1, &sa, NULL);
    sigaction(SIGTERM, &sa, NULL);

    printf("== demo1: 进程级投递只落解除阻塞的线程 ==\n");
    pthread_t t[3];
    for (long i = 0; i < 3; i++) pthread_create(&t[i], NULL, worker, (void *)i);
    usleep(200 * 1000);                        /* 等线程登记 */
    kill(getpid(), SIGUSR1);                   /* 进程级发送 */
    for (int i = 0; i < 3; i++) pthread_join(t[i], NULL);
    for (int i = 0; i < 3; i++)
        printf("  worker%d %s\n", i, got_in[i] > 0 ? "收到 SIGUSR1" : "（阻塞，未收）");

    printf("== demo2: pthread_kill 定向（全员未阻塞，看谁收到）==\n");
    handler_ran = 0;
    unblock_all = 1;                           /* 全员解除阻塞：投递才有三选一 */
    memset((void *)got_in, 0, sizeof got_in);
    pthread_t t2[3];
    for (long i = 0; i < 3; i++) pthread_create(&t2[i], NULL, worker, (void *)i);
    usleep(200 * 1000);
    pthread_kill(t2[2], SIGUSR1);              /* 定向第三个 */
    for (int i = 0; i < 3; i++) pthread_join(t2[i], NULL);
    printf("  worker0/1/2 收到 = %d/%d/%d（定向只进 worker2）\n",
           got_in[0] > 0, got_in[1] > 0, got_in[2] > 0);

    printf("== demo3: sigwait 同步取信号 ==\n");
    handler_ran = 0;
    pthread_t sw;
    pthread_create(&sw, NULL, sigwaiter, NULL);
    usleep(100 * 1000);
    kill(getpid(), SIGTERM);                   /* 进程级 SIGTERM */
    pthread_join(sw, NULL);
    printf("  handler_ran=%d（sigwait 模式下 handler 完全不跑）\n", (int)handler_ran);
    return 0;
}
```

实测输出（WSL2 / 12 核，gcc -O2 零告警）：

```text
== demo1: 进程级投递只落解除阻塞的线程 ==
  worker0 （阻塞，未收）
  worker1 收到 SIGUSR1
  worker2 （阻塞，未收）
== demo2: pthread_kill 定向（全员未阻塞，看谁收到）==
  worker0/1/2 收到 = 0/0/1（定向只进 worker2）
== demo3: sigwait 同步取信号 ==
  [sigwait 线程] 取到信号 15（handler_ran=0，handler 没跑）
  handler_ran=0（sigwait 模式下 handler 完全不跑）
```

三点解读：

1. **demo1 是 `complete_signal` 算法的活体演示**：`wants_signal()` 对 worker0/2 因 `blocked` 判 false，轮转游标最终停在 worker1——全组唯一解除阻塞的线程。10 次运行 10 次落 worker1（确定性，不是概率）。
2. **demo2 验证定向队列**：全员解除阻塞（排除掩码因素）后 `pthread_kill` → `tgkill` → 线程级 pending，仍只有 worker2 的 handler 跑——定向与「挑哪个未阻塞线程」的进程级选择互不相干。
   （推论：若目标线程阻塞着该信号，定向信号进它的线程 pending 挂起——handler 要等它解除阻塞才跑，期间不丢。）
3. **demo3 的 `handler_ran=0`**：SIGTERM 明明装了 handler，但被 sigwait 线程**同步取走**——signal 的「异步中断」语义被改写成「消息」。这就是推荐架构的根基。

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[33.1 线程栈](./33.1-thread-stacks.md)
- 下一节：[33.3 进程控制](./33.3-threads-and-process-control.md)
- 信号基础（处置/pending/sigaction）：[Ch20](../../chapter-20-signals-fundamentals/README.md) · [Ch22 sigwaitinfo](../../chapter-22-signals-advanced/README.md)
- `pthread_cancel` 的 tgkill 链路：[32.1 取消请求](../../chapter-32-thread-cancellation/notes/32.1-canceling-a-thread.md)

## 自测题

**题目 1（辨析）：** 「多线程进程的信号掩码是进程级共享的」——对吗？

<details>
<summary>参考答案</summary>

**错**。掩码**每线程独立**（每个 `task_struct` 自带 `blocked`）。共享的是：信号**处置**表（`sighand`，`CLONE_SIGHAND` 复制并共享）、进程级 pending 队列（`shared_pending`）。`pthread_sigmask` 改的只是**本线程**的掩码——这正是「创建线程前主线程先阻塞 → 全体继承」模式成立的原因（新线程继承创建者的掩码副本）。

</details>

**题目 2（内核）：** `kill(pid, SIGUSR1)` 发给 4 线程进程，其中 3 个阻塞了 SIGUSR1、1 个没阻塞。信号一定被谁接收？如果 4 个全阻塞呢？

<details>
<summary>参考答案</summary>

未阻塞的那 1 个。`complete_signal():1004` 的轮转循环里 `wants_signal():987` 因 `blocked`/`PF_EXITING`/stopped 对另外 3 个返回 false，游标停在唯一合格者。全阻塞时：循环回到 `curr_target` 起点仍未找到 → 函数返回，信号**留在共享 pending 队列**；之后任何一个线程解除阻塞并在返回用户态时检查到它，就取走。

</details>

**题目 3（设计）：** 多线程热路径服务要处理 SIGTERM 优雅退出，为什么不能在 handler 里直接置 flag + printf 日志？

<details>
<summary>参考答案</summary>

① handler 是 async-signal-unsafe 重灾区：`printf` 动 stdio 锁（可能恰好被中断线程持有 → 死锁）；② 信号可能落在**任意**业务线程任意指令间——`complete_signal` 轮转不保证目标，热路径被随机打断本身就是延迟毛刺；③ 队列不保证（标准信号合并），快速连发只记一次。正确姿势：全员阻塞 + sigwait 专用线程取信号 → 写管道/事件 fd → 主循环统一处理（demo3 模式）。

</details>

**题目 4（API 细节）：** `sigwait` 的前置条件是什么？违反了会怎样？

<details>
<summary>参考答案</summary>

调用线程必须**已阻塞**待等信号（`pthread_sigmask(SIG_BLOCK, ...)`）。sigwait 是"原子地检查 pending 并取走，否则睡在 `sigtimedwait` 上"——若信号未阻塞且装了 handler，信号可能在进入 sigwait 之前就被递送、handler 抢跑；未阻塞且无 handler 则默认动作直接生效（SIGTERM 杀进程）。实践中先在创建线程前阻塞、再起 sigwait 线程，继承保证前置成立。

</details>

**题目 5（陷阱）：** `pthread_kill(tid, sig)` 可以给**另一个进程**的线程发信号吗？给已退出的线程发呢？

<details>
<summary>参考答案</summary>

不能跨进程：`pthread_t` 是本进程 glibc 的 `struct pthread *`，跨进程无意义；内核路径 `tgkill(tgid, tid, sig)`（`do_send_specific():3935`）本身就限定同 `tgid`。给已 join/退出的线程发：`pthread_t` 可能已被**复用**（栈缓存复用，29.5 警告），信号可能打到新线程——这是必须 join 之后再丢弃 `pthread_t` 的原因之一；内核侧若 TID 已死则 `ESRCH`。

</details>
