# 33.2 线程与信号（重难点）

> 本章：[TLPI 第 33 章 — Threads: Further Details](./README.md)
> - 上一节：[33.1 线程栈](./33.1-thread-stack.md)
> - 下一节：[33.3 进程控制](./33.3-process.md)

---

## 本节讲什么

int pthread_sigmask(int how, const sigset_t *set, sigset_t *oldset); int pthread_kill(pthread_t thread, int sig);   /* 仅同进程内 */ int sigwait(const sigset_t *set, int *sig);    /* 同步取信号 */

---

## 要点

| 进程级（共享） | 线程级（私有） |
|----------------|----------------|
| `sigaction` 处置 / 默认动作 | **信号掩码**（`pthread_sigmask`） |
| 进程 pending（标准信号不排队） | 线程 pending；递送目标线程 |

```c
int pthread_sigmask(int how, const sigset_t *set, sigset_t *oldset);
int pthread_kill(pthread_t thread, int sig);   /* 仅同进程内 */
int sigwait(const sigset_t *set, int *sig);    /* 同步取信号 */
```

| 投递 | |
|------|--|
| 异步信号（SIGINT 等） | 任选**未阻塞该信号**的线程 |
| `pthread_kill` | 指定线程 |
| handler | 跑在收到信号的那条线程上 |

**推荐：** 创建业务线程前主线程阻塞关心信号 → 专用线程 `sigwait` → 业务线程不装异步 handler。

Demo：[`code/thread_sigwait.c`](./code/thread_sigwait.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 线程与信号（重难点） 在嵌入式系统的应用场景 |
| HFT | 线程与信号（重难点） 对低延迟和性能的影响 |

## 代码示例

```c
#include <pthread.h>
#include <signal.h>
#include <stdio.h>
void handler(int sig) { printf("线程 %lu 收到信号 %d\n", (unsigned long)pthread_self(), sig); }
void *worker(void *arg) {
    signal(SIGUSR1, handler);
    pause();
    return NULL;
}
int main(void) {
    pthread_t tid;
    pthread_create(&tid, NULL, worker, NULL);
    sleep(1);
    /* pthread_kill: 给特定线程发信号 */
    pthread_kill(tid, SIGUSR1);
    pthread_join(tid, NULL);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[33.1 线程栈](./33.1-thread-stack.md)
- 下一节：[33.3 进程控制](./33.3-process.md)
- 前置：**前置**：[Ch29](../chapter-29-threads-intro/notes.md)–[Ch32](../chapter-32-thread-cancellation/notes.md) · [Ch22 sigwait](../chapter-22-signals-advanced/notes.md) · [Ch28 fork](../chapter-28-process-creation-exec-detail/notes.md)  
- 后置：**后置**：[Ch34 进程组/会话](../chapter-34-process-groups-sessions/notes.md)（线程模块收束；daemon 见 [Ch37](../chapter-37-daemons/notes.md)）
