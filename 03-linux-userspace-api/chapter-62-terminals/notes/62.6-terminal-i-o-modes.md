# 62.6 Terminal I/O Modes

> 本章：[TLPI 第 62 章](./README.md)

## 本节讲什么

终端的两种主要 I/O 模式：规范模式（canonical, 行缓冲）和原始模式（raw, 非规范），以及 VMIN/VTIME 超时控制。

## 要点

### 规范模式 vs 原始模式

| 特性 | 规范模式 (ICANON) | 原始模式 (~ICANON) |
|------|-------------------|---------------------|
| 缓冲 | 行缓冲（按 NL/CR 提交） | 无缓冲（按字符/超时） |
| 行编辑 | 支持（ERASE/KILL） | 不支持 |
| 信号 | INTR/QUIT/SUSP 产生信号 | 不产生 |
| 回显 | 可配置 | 可配置 |
| 触发读取 | 整行或 EOF | VMIN/VTIME 控制 |
| 适用 | shell, 交互命令 | 串口, TUI, 协议解析 |

### VMIN 和 VTIME

| 组合 | 行为 | 典型用途 |
|------|------|---------|
| VMIN=1, VTIME=0 | 阻塞等1个字符 | 交互式，按键响应 |
| VMIN=0, VTIME=0 | 非阻塞轮询 | 检测输入但不阻塞 |
| VMIN=0, VTIME=N | 超时 N*100ms 无输入返回 | 超时读取 |
| VMIN=1, VTIME=N | 等到1字符或超时 | 协议帧解析 |

### 设置原始模式

```c
struct termios raw, orig;
tcgetattr(fd, &orig);
raw = orig;

/* 关闭 ICANON, ECHO, ISIG, IEXTEN */
raw.c_lflag &= ~(ICANON | ECHO | ISIG | IEXTEN);
/* 关闭输入处理 */
raw.c_iflag &= ~(ICRNL | INLCR | IGNCR | IXON | IXOFF);
/* 关闭输出处理 */
raw.c_oflag &= ~OPOST;
/* 8 位字符 */
raw.c_cflag |= CS8;

/* 非规范参数 */
raw.c_cc[VMIN] = 1;   /* 至少1字符 */
raw.c_cc[VTIME] = 0;  /* 无超时 */

tcsetattr(fd, TCSAFLUSH, &raw);
/* ... 操作终端 ... */
/* 恢复 */
tcsetattr(fd, TCSAFLUSH, &orig);
```

### cfmakeraw() 便捷函数

```c
#include <termios.h>
struct termios t;
tcgetattr(fd, &t);
cfmakeraw(&t);  /* 设置为原始模式 */
tcsetattr(fd, TCSANOW, &t);
```

### tcsetattr 的 when 参数

| 值 | 说明 |
|----|------|
| `TCSANOW` | 立即生效 |
| `TCSADRAIN` | 等待输出完成后生效 |
| `TCSAFLUSH` | 刷新输入输出队列后生效 |

## HFT / 嵌入式关联

- 嵌入式串口通信：原始模式 + VMIN/VTIME 解析协议帧
- 交易终端 TUI：原始模式捕获按键，关闭回显
- 串口调试：原始模式透传二进制数据

## 代码示例

```c
#include <termios.h>
#include <unistd.h>
#include <stdio.h>
#include <string.h>

/* 原始模式读取串口数据（带超时） */
int read_serial_raw(int fd, void *buf, size_t len, int timeout_ms) {
    struct termios orig, raw;

    tcgetattr(fd, &orig);
    raw = orig;
    cfmakeraw(&raw);

    /* 设置超时 */
    raw.c_cc[VMIN] = 0;
    raw.c_cc[VTIME] = (timeout_ms + 50) / 100;  /* 100ms 单位 */

    tcsetattr(fd, TCSANOW, &raw);

    int n = read(fd, buf, len);

    /* 恢复 */
    tcsetattr(fd, TCSANOW, &orig);
    return n;
}

/* 交互式 TUI：捕获单键 */
int kbhit(void) {
    struct termios orig, raw;
    tcgetattr(STDIN_FILENO, &orig);
    raw = orig;
    raw.c_lflag &= ~(ICANON | ECHO);
    raw.c_cc[VMIN] = 0;
    raw.c_cc[VTIME] = 0;
    tcsetattr(STDIN_FILENO, TCSANOW, &raw);

    char c;
    int n = read(STDIN_FILENO, &c, 1);

    tcsetattr(STDIN_FILENO, TCSANOW, &orig);
    return (n == 1) ? (int)c : 0;
}
```

## 衔接

- 上一节 [62.5 终端标志](./62.5-terminal-flags.md)
- 下一节 [62.7 修改特殊字符](./62.7-changing-special-characters.md)

## 代码自测

<details>
<summary>VMIN=0, VTIME=0 和 VMIN=0, VTIME=1 有什么区别？</summary>

- `VMIN=0, VTIME=0`：完全非阻塞，`read` 立即返回 0（无数据）或已有数据
- `VMIN=0, VTIME=1`：如果在 100ms 内无输入则返回 0，有输入则立即返回。这是一个"超时轮询"模式，适合检测是否有数据到达而不长时间阻塞

</details>
