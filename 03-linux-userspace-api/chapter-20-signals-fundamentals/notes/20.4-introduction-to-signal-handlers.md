# 20.4 信号掩码与未决集

> 本章：[TLPI 第 20 章 — Signals: Fundamental Concepts](../README.md)
> - 上一节：[20.3 生命周期](20.3-changing-signal-dispositions-signal.md)
> - 下一节：[20.5 `signal()` 缺陷（警示）](20.10-the-signal-mask-blocking-signal-delivery.md)

---

## 本节讲什么

掩码中的信号 = **阻塞** → 抵达后进 pending，解除后才递送。 **掩码是线程属性**；多线程下 `sigprocmask` 影响**当前线程**。 int sigemptyset(sigset_t *set);

---

## 要点

掩码中的信号 = **阻塞** → 抵达后进 pending，解除后才递送。  
**掩码是线程属性**；多线程下 `sigprocmask` 影响**当前线程**。

```c
int sigemptyset(sigset_t *set);
int sigfillset(sigset_t *set);
int sigaddset(sigset_t *set, int sig);
int sigdelset(sigset_t *set, int sig);
int sigismember(const sigset_t *set, int sig);

int sigprocmask(int how, const sigset_t *set, sigset_t *oldset);
/* SIG_BLOCK | SIG_UNBLOCK | SIG_SETMASK */

int sigpending(sigset_t *set);   /* 当前线程未决集 */
```

### `sigprocmask` how

| how | 行为 |
|-----|------|
| `SIG_BLOCK` | 新掩码 = 旧 ∪ set |
| `SIG_UNBLOCK` | 新掩码 = 旧 − set |
| `SIG_SETMASK` | 新掩码 = set |

Demo：[`code/block_pending.c`](../code/block_pending.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 信号掩码与未决集 在嵌入式系统的应用场景 |
| HFT | 信号掩码与未决集 对低延迟和性能的影响 |

## 代码示例

```c
#include <stdio.h>
#include <signal.h>
#include <unistd.h>
/* 信号掩码: 阻塞信号使其变为"待决" */
void handler(int sig) {
    const char *m = "caught!\n";
    write(1, m, 8);
}

int main(void) {
    signal(SIGINT, handler);

    /* 阻塞 SIGINT */
    sigset_t mask, old;
    sigemptyset(&mask);
    sigaddset(&mask, SIGINT);
    sigprocmask(SIG_BLOCK, &mask, &old);
    printf("SIGINT blocked for 3s, try Ctrl+C...\n");

    sleep(3);

    /* 检查待决信号 */
    sigset_t pending;
    sigpending(&pending);
    printf("SIGINT pending: %s\n",
           sigismember(&pending, SIGINT) ? "yes" : "no");

    /* 解除阻塞: 待决信号立即递送 */
    printf("Unblocking...\n");
    sigprocmask(SIG_SETMASK, &old, NULL);
    sleep(1);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[20.3 生命周期](20.3-changing-signal-dispositions-signal.md)
- 下一节：[20.5 `signal()` 缺陷（警示）](20.10-the-signal-mask-blocking-signal-delivery.md)
- 前置：**前置**：[Ch19 inotify](../../chapter-19-monitoring-file-events/notes)（同属异步事件，机制不同）  
- 后置：**后置**：[Ch21 Signal Handlers](../../chapter-21-signal-handlers/notes) · [Ch22 高级信号](../../chapter-22-signals-advanced/notes)
