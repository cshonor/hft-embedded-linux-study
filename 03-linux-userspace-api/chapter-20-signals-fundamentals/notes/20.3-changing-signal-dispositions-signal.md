# 20.3 生命周期

> 本章：[TLPI 第 20 章 — Signals: Fundamental Concepts](../README.md)
> - 上一节：[20.2 编号与分类](20.2-signal-types-and-default-actions.md)
> - 下一节：[20.4 信号掩码与未决集](20.10-the-signal-mask-blocking-signal-delivery.md)

---

## 本节讲什么

1. **产生** — 内核标记目标 2. **未决（pending）** — 已产生但被阻塞，暂不递送 3. **递送** — 中断用户流，执行默认动作或 handler

---

## 要点

1. **产生** — 内核标记目标  
2. **未决（pending）** — 已产生但被阻塞，暂不递送  
3. **递送** — 中断用户流，执行默认动作或 handler  

> 递送点：进程从**内核态返回用户态前**（典型）。不是任意用户指令中间插入。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 生命周期 在嵌入式系统的应用场景 |
| HFT | 生命周期 对低延迟和性能的影响 |

## 代码示例

```c
#include <signal.h>
#include <unistd.h>
#include <stdio.h>
/* 信号生命周期: 产生 → 未决(pending) → 传递(delivery) → 处理 */
void handler(int sig) { printf("处理信号 %d\n", sig); }
int main(void) {
    sigset_t mask;
    sigemptyset(&mask);
    sigaddset(&mask, SIGINT);
    sigprocmask(SIG_BLOCK, &mask, NULL);  /* 阻塞 SIGINT */
    printf("SIGINT 已阻塞, 发送后处于 pending 状态\n");
    raise(SIGINT);  /* 产生但未传递 (被阻塞) */
    /* 检查是否 pending */
    sigset_t pending;
    sigpending(&pending);
    printf("SIGINT pending: %s\n", sigismember(&pending, SIGINT) ? "YES" : "NO");
    signal(SIGINT, handler);
    printf("解除阻塞 → 信号传递\n");
    sigprocmask(SIG_UNBLOCK, &mask, NULL);  /* 传递 → handler 执行 */
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[20.2 编号与分类](20.2-signal-types-and-default-actions.md)
- 下一节：[20.4 信号掩码与未决集](20.10-the-signal-mask-blocking-signal-delivery.md)
- 前置：**前置**：[Ch19 inotify](../../chapter-19-monitoring-file-events/notes)（同属异步事件，机制不同）  
- 后置：**后置**：[Ch21 Signal Handlers](../../chapter-21-signal-handlers/notes) · [Ch22 高级信号](../../chapter-22-signals-advanced/notes)
