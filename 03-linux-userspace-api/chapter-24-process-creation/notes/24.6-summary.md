# 24.6 多线程 + `fork`（重难点）

> 本章：[TLPI 第 24 章 — Process Creation](../README.md)
> - 上一节：[24.5 stdio 缓冲陷阱](24.5-avoiding-race-conditions-by-synchronizin.md)
> - 下一节：[24.7 典型范式](24.7-exercises.md)

---

## 本节讲什么

子进程只保留 **fork 那条线程**；其它线程不跑清理 → 锁可能永久锁死、堆状态撕裂。 ---

---

## 要点

子进程只保留 **fork 那条线程**；其它线程不跑清理 → 锁可能永久锁死、堆状态撕裂。

| 做法 | |
|------|--|
| ✅ | 多线程里尽量只 `fork` 后立刻 `exec` |
| ⚠️ | `pthread_atfork` 只能缓解 |
| ❌ | fork 后子进程继续跑复杂多线程逻辑 |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 多线程进程 fork 只保留调用线程，其它线程锁可能永久锁死 |
| HFT | 多线程热路径进程慎 fork；要 fork 就立刻 exec（pthread_atfork 只缓解） |

## 代码示例

```c
#include <unistd.h>
#include <stdio.h>
#include <sys/wait.h>
int main(void) {
    /* fork 常见用法: 父进程继续服务, 子进程处理请求 */
    for (int i = 0; i < 3; i++) {
        pid_t pid = fork();
        if (pid == 0) {
            printf("子进程 %d (PID=%d)\n", i, getpid());
            _exit(i);
        }
    }
    /* 父进程等所有子进程 */
    int status;
    while (wait(&status) > 0) {
        printf("子进程退出: code=%d\n", WEXITSTATUS(status));
    }
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[24.5 stdio 缓冲陷阱](24.5-avoiding-race-conditions-by-synchronizin.md)
- 下一节：[24.7 典型范式](24.7-exercises.md)
- 前置：**前置**：[Ch23 Timers](../../chapter-23-timers-sleeping/notes) · [Ch20–22 信号](../../chapter-20-signals-fundamentals/notes)  
- 后置：**后置**：[Ch25 进程终止](../../chapter-25-process-termination/notes) · [Ch26 wait](../../chapter-26-monitoring-child-processes/notes) · [Ch27 exec](../../chapter-27-program-execution/notes) · [Ch28 fork/exec 深潜](../../chapter-28-process-creation-exec-detail/notes)
