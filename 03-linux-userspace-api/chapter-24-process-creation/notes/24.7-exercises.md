# 24.7 典型范式

> 本章：[TLPI 第 24 章 — Process Creation](../README.md)
> - 上一节：[24.6 多线程 + `fork`（重难点）](24.1-overview-of-fork-exit-wait-and-execve.md)
> - 下一节：[24.8 `vfork`（了解即可）](24.3-the-vfork-system-call.md)

---

## 本节讲什么

**fork + exec**（shell / 起外部程序）：子进程关多余 fd、重定向，再 `exec*`；失败 `_exit(127)`。 **fork 后分叉业务**（多进程服务）：父子各跑逻辑；父须 `wait*` 防僵尸（Ch26）。 ---

---

## 要点

**fork + exec**（shell / 起外部程序）：子进程关多余 fd、重定向，再 `exec*`；失败 `_exit(127)`。  
**fork 后分叉业务**（多进程服务）：父子各跑逻辑；父须 `wait*` 防僵尸（Ch26）。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 典型范式 在嵌入式系统的应用场景 |
| HFT | 典型范式 对低延迟和性能的影响 |

## 代码示例

```c
#include <unistd.h>
#include <stdio.h>
/* fork 速查:
 * - 返回: 父=子PID, 子=0, 错=-1
 * - 继承: fd/内存/信号掩码 (COW)
 * - 不继承: 锁/定时器值(只继承剩余时间)
 * - stdio 缓冲区: fork 前 fflush!
 */
int main(void) {
    fflush(stdout);
    pid_t pid = fork();
    if (pid == 0) { printf("子 PID=%d\n", getpid()); _exit(0); }
    wait(NULL);
    printf("父 PID=%d\n", getpid());
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[24.6 多线程 + `fork`（重难点）](24.1-overview-of-fork-exit-wait-and-execve.md)
- 下一节：[24.8 `vfork`（了解即可）](24.3-the-vfork-system-call.md)
- 前置：**前置**：[Ch23 Timers](../../chapter-23-timers-sleeping/notes) · [Ch20–22 信号](../../chapter-20-signals-fundamentals/notes)  
- 后置：**后置**：[Ch25 进程终止](../../chapter-25-process-termination/notes) · [Ch26 wait](../../chapter-26-monitoring-child-processes/notes) · [Ch27 exec](../../chapter-27-program-execution/notes) · [Ch28 fork/exec 深潜](../../chapter-28-process-creation-exec-detail/notes)
