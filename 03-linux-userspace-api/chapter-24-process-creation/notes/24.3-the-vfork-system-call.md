# 24.3 The vfork() System Call

> 本章：[TLPI 第 24 章 — Process Creation](./README.md)

## 本节讲什么

介绍 `vfork()` 系统调用：一种优化版 `fork()`，不复制父进程地址空间，子进程直接共享父进程内存，直到调用 `exec()` 或 `_exit()`。

## 要点

### vfork vs fork vs clone

| 特性 | `fork()` | `vfork()` | `clone()` |
|------|---------|----------|-----------|
| 地址空间 | 复制（COW） | 不复制，共享 | 可选 |
| 父子关系 | 独立运行 | 父阻塞等待 | 独立 |
| 子进程限制 | 无 | 只能 `_exit()` 或 `exec()` | 无 |
| 性能 | 中等（COW） | 最快（无复制） | 最灵活 |
| POSIX | 标准 | 已废弃 | Linux 特有 |
| 现代推荐 | 是 | 否 | 是（用于线程） |

### vfork 语义

```
vfork() 调用后：
1. 子进程在父进程的地址空间中运行
2. 父进程被挂起（阻塞），直到子进程：
   a) 调用 _exit()  → 父进程恢复
   b) 调用 exec()   → 父进程恢复
3. 子进程绝对不能：
   - 修改任何变量（共享父进程内存！）
   - 使用 return（而不是 _exit）
   - 调用其他修改内存的函数
```

### vfork 的限制

| 禁止操作 | 后果 |
|---------|------|
| `return` | 栈帧问题，破坏父进程栈 |
| `exit()` | 调用 atexit 处理器和 stdio flush，破坏父进程数据 |
| 修改变量 | 直接修改父进程变量 |
| `malloc` | 修改堆状态，破坏父进程 |

### vfork 的设计意图

- **BSD 时代**：`fork` 需要复制全部页面（无 COW），开销大
- `vfork` 专为"fork+exec"模式优化：子进程立即 exec，无需复制地址空间
- **现代系统**：`fork` 已使用 COW（Copy-On-Write），开销很小
- `vfork` 的性能优势微乎其微，且语义危险

### 现代 Linux 的 fork 性能

```
fork+exec 性能（典型 x86_64）：
  fork():  ~50-100μs（COW，仅复制页表）
  vfork(): ~20-30μs（无页表复制）
  clone(): ~20-30μs（可控制共享项）
```

差异在大多数应用中可忽略，但 HFT 高频创建子进程场景可能敏感。

## HFT / 嵌入式关联

- **HFT 不使用 vfork**：语义危险，收益微小
- **现代替代**：用 `posix_spawn()` 或 `clone()` + `exec()`
- **嵌入式**：uClinux（无 MMU）不支持 COW fork，旧版可能需 vfork；现代嵌入式 Linux 有 MMU，用标准 fork
- **性能分析**：如果 fork+exec 是瓶颈，优化方式：(1) 减少子进程创建频率；(2) 用线程替代进程

## 代码示例

```c
/* vfork 的正确（但已不推荐）用法 */
#include "tlpi_hdr.h"
#include <unistd.h>

int main(void) {
    pid_t pid = vfork();    /* 不推荐使用！ */
    if (pid == -1) errExit("vfork");

    if (pid == 0) {        /* 子进程 */
        /* 只能做两件事：exec 或 _exit */
        execlp("ls", "ls", "-l", NULL);
        /* 如果 exec 失败 */
        _exit(EXIT_FAILURE);  /* 必须 _exit，不能 exit/return */
    }
    /* 父进程在此恢复，子进程已 exec 或 _exit */
    printf("child finished\n");
    return 0;
}

/* 现代替代方案：posix_spawn */
#include <spawn.h>

int modern_fork_exec(void) {
    pid_t pid;
    char *argv[] = {"ls", "-l", NULL};
    char *envp[] = {NULL};
    /* posix_spawn 内部优化了 fork+exec */
    int ret = posix_spawnp(&pid, "ls", NULL, NULL, argv, envp);
    return ret;
}
```

## 衔接

- [24.2 The fork() System Call](./24.2-the-fork-system-call.md) — 标准 fork
- [24.4 fork() Differences](./24.4-fork-differences.md) — fork 的语义细节
- **Ch25** Process Termination — `_exit` vs `exit`
- **Ch27** Program Execution — `execve`/`posix_spawn`

## 代码自测

<details>
<summary>1. vfork 后子进程为什么不能用 return？</summary>

`vfork` 不创建新的地址空间和栈，子进程与父进程共享同一个栈。如果子进程 `return`，函数返回会修改共享栈的返回地址和局部变量，父进程恢复执行时栈状态已被破坏，可能导致崩溃或未定义行为。必须用 `_exit()` 正确退出子进程。
</details>

<details>
<summary>2. 现代 Linux 还需要 vfork 吗？</summary>

几乎不需要。现代 `fork()` 使用 COW（Copy-On-Write），只复制页表（不复制物理页），开销已经很低。`vfork` 的性能优势很小，但语义危险（子进程不能修改内存、不能用 exit/return）。

替代方案：
1. `posix_spawn()` — POSIX 标准的 fork+exec 替代
2. `clone(CLONE_VM | CLONE_VFORK, ...)` — Linux 特有，更灵活
3. 直接 `fork()` + `exec()` — COW 已经足够快
</details>

<details>
<summary>3. vfork 和 clone 的关系？</summary>

Linux 中 `vfork()` 内部实现为 `clone(CLONE_VM | CLONE_VFORK | SIGCHLD, ...)`：
- `CLONE_VM`：共享地址空间
- `CLONE_VFORK`：父进程挂起直到子进程 exec/exit
- `SIGCHLD`：子进程退出时发 SIGCHLD

`clone()` 是更通用的原语，`fork()`/`vfork()`/`pthread_create()` 都基于 `clone()` 实现。
</details>
