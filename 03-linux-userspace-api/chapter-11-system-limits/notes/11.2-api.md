# 11.2 三大查询 API

> 本章：[TLPI 第 11 章 — System Limits and Options](./README.md)
> - 上一节：[11.1 基础概念](./11.1-concepts.md)
> - 下一节：[11.3 编译期：`<limits.h>` / `<unistd.h>`](./11.3-limits-unistd.md)

---

## 本节讲什么

#include <unistd.h> long sysconf(int name);                         /* 系统级，不依赖路径 */ long pathconf(const char *pathname, int name);  /* 按路径 */

---

## 要点

```c
#include <unistd.h>

long sysconf(int name);                         /* 系统级，不依赖路径 */
long pathconf(const char *pathname, int name);  /* 按路径 */
long fpathconf(int fd, int name);               /* 按 fd，优先 */
```

### 返回值陷阱（必记）

`返回 -1` 有两种含义：

| 条件 | 含义 |
|------|------|
| `errno != 0` | **真正出错**（如 `EINVAL`：name 非法） |
| `errno == 0` | 限制 **indeterminate（不确定）**，不是错误 |

```c
errno = 0;
long v = sysconf(name);
if (v == -1) {
    if (errno == 0)
        /* indeterminate */;
    else
        /* error */;
}
```

### 常量命名

| API | 前缀 | 例 |
|-----|------|-----|
| `sysconf` | `_SC_*` | `_SC_ARG_MAX`、`_SC_OPEN_MAX`、`_SC_PAGESIZE`、`_SC_CLK_TCK` |
| `pathconf`/`fpathconf` | `_PC_*` | `_PC_NAME_MAX`、`_PC_PATH_MAX`、`_PC_PIPE_BUF` |

Shell：`getconf`（封装同类查询）

```bash
getconf ARG_MAX
getconf NAME_MAX /home
```

Demo：[`code/print_limits.c`](./code/print_limits.c)

---

---

## 与 C 语言学习的关系

系统限制和信息 API 涉及 C 语言的可移植性编程。`sysconf`/`pathconf` 返回 `long`，需要检查返回值和 errno。这部分内容训练 C 程序员不硬编码系统参数（如 `OPEN_MAX`、`PAGE_SIZE`），而是运行时查询——这是可移植 C 编程的基本素养。
---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 三大查询 API 在嵌入式系统的应用场景 |
| HFT | 三大查询 API 对低延迟和性能的影响 |

---

## 常见陷阱

1. **硬编码系统限制**

```c
#define MAX_FD 256  /* 错误假设 */
long max_fd = sysconf(_SC_OPEN_MAX);  /* 运行时查询 */
```

系统限制值因平台和配置而异。硬编码会导致程序在新环境上失败。应用 sysconf/pathconf 运行时查询。

2. **sysconf 返回值未检查**

```c
long val = sysconf(_SC_OPEN_MAX);
/* val 可能是 -1（无限制或出错） */
```

sysconf 返回 -1 可能表示"无限制"或"出错"。需要检查 errno 来区分：先清除 errno，调用后检查。

3. **uname 结构体字段假设**

```c
struct utsname buf;
uname(&buf);
printf("%s", buf.release);  /* 字段长度有限! */
```

utsname 各字段长度有限（通常 65 字节）。过长的字段会被截断。不要假设完整信息。

---

## 自测题

**题目 1（选择）：** 获取系统最大打开文件数，应该用？
A. 宏 OPEN_MAX
B. sysconf(_SC_OPEN_MAX)
C. getrlimit(RLIMIT_NOFILE)
D. B 或 C

<details>
<summary>参考答案</summary>

D。sysconf 返回系统默认值，getrlimit 返回当前进程的限制。两者都可，但 getrlimit 更精确。硬编码 OPEN_MAX 宏不可移植。

</details>

**题目 2（判断）：** `sysconf(_SC_OPEN_MAX)` 返回 -1 一定表示出错。

<details>
<summary>参考答案</summary>

错误。返回 -1 可能表示"无限制"或"出错"。需要先清除 errno，调用后检查 errno 是否被设置来区分。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
long page_size = 4096;  /* 硬编码 */
void *p = mmap(NULL, page_size, ...);
```

<details>
<summary>参考答案</summary>

页面大小因平台而异（x86=4KB, ARM64 可能=16KB/64KB）。应运行时查询：`long page_size = sysconf(_SC_PAGESIZE);`

</details>

**题目 4（应用）：** 写代码获取当前进程的文件描述符限制并打印。

<details>
<summary>参考答案</summary>

```c
struct rlimit rl;
if (getrlimit(RLIMIT_NOFILE, &rl) == 0) {
    printf("soft: %lu, hard: %lu
",
           (unsigned long)rl.rlim_cur,
           (unsigned long)rl.rlim_max);
}
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[11.1 基础概念](./11.1-concepts.md)
- 下一节：[11.3 编译期：`<limits.h>` / `<unistd.h>`](./11.3-limits-unistd.md)
- 前置：**前置**：[Ch10 Times and Dates](../chapter-10-time/notes.md)（`_SC_CLK_TCK` ↔ `times()`）  
- 后置：**后置**：[Ch12 System and Process Information](../chapter-12-system-process-info/notes.md) · [Ch15 文件属性](../chapter-15-file-attributes/notes.md) · [Ch44 管道](../chapter-44-pipes-fifos/notes.md) · [Ch36 资源限制](../chapter-36-process-resources/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
