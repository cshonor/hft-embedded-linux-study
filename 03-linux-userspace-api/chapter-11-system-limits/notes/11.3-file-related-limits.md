# 11.3 编译期：`<limits.h>` / `<unistd.h>`

> 本章：[TLPI 第 11 章 — System Limits and Options](../README.md)
> - 上一节：[11.2 三大查询 API](11.2-runtime-limits.md)
> - 下一节：[11.4 常用限制速览](11.4-indeterminate-limits.md)

---

## 本节讲什么

---

---

## 要点

| 头文件 | 内容 |
|--------|------|
| `<limits.h>` | `_POSIX_*_MAX` 标准最低下限；部分系统还有实现定义的 `NAME_MAX` 等 |
| `<unistd.h>` | 特性宏：`_POSIX_THREADS`、`_POSIX_REALTIME_SIGNALS` 等 |

> 宏只代表**编译期声明**；裁剪内核 / 容器可能关掉功能 → **可靠做法仍是 `sysconf` 运行时校验**。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 编译期：`<limits.h>` / `<unistd.h>` 在嵌入式系统的应用场景 |
| HFT | 编译期：`<limits.h>` / `<unistd.h>` 对低延迟和性能的影响 |

## 代码示例

```c
#include <unistd.h>
#include <stdio.h>

int main(void) {
    /* <unistd.h> 中的 _POSIX_* 编译时常量 */
    printf("_POSIX_OPEN_MAX  = %d\n", _POSIX_OPEN_MAX);
    printf("_POSIX_CHILD_MAX = %d\n", _POSIX_CHILD_MAX);
    printf("_POSIX_PATH_MAX  = %d\n", _POSIX_PATH_MAX);
    printf("_POSIX_NAME_MAX  = %d\n", _POSIX_NAME_MAX);

    /* 编译时常量是最小保证值, 运行时实际可能更大 */
    long actual = sysconf(_SC_OPEN_MAX);
    printf("\n运行时实际 max fd = %ld (>= %d)\n", actual, _POSIX_OPEN_MAX);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[11.2 三大查询 API](11.2-runtime-limits.md)
- 下一节：[11.4 常用限制速览](11.4-indeterminate-limits.md)
- 前置：**前置**：[Ch10 Times and Dates](../../chapter-10-time/notes)（`_SC_CLK_TCK` ↔ `times()`）  
- 后置：**后置**：[Ch12 System and Process Information](../../chapter-12-system-process-info/notes) · [Ch15 文件属性](../../chapter-15-file-attributes/notes) · [Ch44 管道](../../chapter-44-pipes-fifos/notes) · [Ch36 资源限制](../../chapter-36-process-resources/notes)
