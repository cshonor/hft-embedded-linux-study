# 15.1 `stat` / `lstat` / `fstat`

> 本章：[TLPI 第 15 章 — File Attributes](../README.md)
> - 上一节：本章首页
> - 下一节：[15.2 `st_mode`：类型与权限](15.2-file-timestamps.md)

---

## 本节讲什么

#include <sys/stat.h> int  stat(const char *pathname, struct stat *buf);   /* 跟随符号链接 */ int lstat(const char *pathname, struct stat *buf);   /* 链接自身 */

---

## 要点

```c
#include <sys/stat.h>
int  stat(const char *pathname, struct stat *buf);   /* 跟随符号链接 */
int lstat(const char *pathname, struct stat *buf);   /* 链接自身 */
int fstat(int fd, struct stat *buf);                 /* 已打开 fd；减 TOCTOU */
```

| 调用 | 符号链接 |
|------|----------|
| `stat` | 跟到目标 |
| `lstat` | 得到链接 inode（`S_ISLNK`） |
| `fstat` | 看 fd 所指对象（打开时已解析） |

### `struct stat` 核心字段

| 字段 | 含义 |
|------|------|
| `st_dev` / `st_ino` | 设备 + inode（同 FS 内唯一） |
| `st_mode` | 类型 + 权限 + 特殊位 |
| `st_nlink` | 硬链接计数 |
| `st_uid` / `st_gid` | 属主 / 属组 |
| `st_rdev` | 设备文件：主/次设备号 |
| `st_size` | 字节大小 |
| `st_blksize` / `st_blocks` | 优选 IO 块；占用块数（常 512B 单位） |
| `st_atim` / `st_mtim` / `st_ctim` | 访问 / 内容修改 / inode 变更（现代为 `timespec`） |

Demo：[`code/print_stat.c`](../code/print_stat.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `fstat` 对已打开 fd 避免路径重解析的 TOCTOU；`lstat` 不跟随符号链接，取链接自身信息 |
| HFT | 热路径已持有 fd 用 `fstat` 而非 `stat(路径)`，省一次 dentry 查找与权限检查，降低延迟 |

## 代码示例

```c
#include <stdio.h>
#include <sys/stat.h>
#include <unistd.h>
/* stat: 跟随符号链接; lstat: 不跟随 */
int main(void) {
    struct stat sb;

    /* stat: 如果是符号链接, 获取目标文件信息 */
    symlink("/etc/passwd", "/tmp/stat_test_link");
    stat("/tmp/stat_test_link", &sb);
    printf("stat: inode=%lu (target's inode)\n", (unsigned long)sb.st_ino);

    /* lstat: 获取链接本身的信息 */
    lstat("/tmp/stat_test_link", &sb);
    printf("lstat: inode=%lu (link's own inode)\n", (unsigned long)sb.st_ino);
    printf("is symlink: %s\n", S_ISLNK(sb.st_mode) ? "yes" : "no");

    /* fstat: 通过 fd 获取 */
    int fd = open("/etc/passwd", O_RDONLY);
    fstat(fd, &sb);
    printf("fstat: size=%ld\n", (long)sb.st_size);
    close(fd);
    remove("/tmp/stat_test_link");
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 下一节：[15.2 `st_mode`：类型与权限](15.2-file-timestamps.md)
- 前置：**前置**：[Ch14 File Systems](../../chapter-14-file-systems/notes)（inode 模型）  
- 后置：**后置**：[Ch16 Extended Attributes](../../chapter-16-extended-attributes/notes) · [Ch18 目录与链接](../../chapter-18-directories-links/notes) · [Ch38 特权与安全](../../chapter-38-secure-privileged/notes)
