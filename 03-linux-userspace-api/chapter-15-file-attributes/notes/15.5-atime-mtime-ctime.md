# 15.5 atime / mtime / ctime

> 本章：[TLPI 第 15 章 — File Attributes](./README.md)
> - 上一节：[15.4 `chown` / `fchown` / `lchown`](./15.4-chown-fchown-lchown.md)
> - 下一节：[15.6 `umask`](./15.6-umask.md)

---

## 本节讲什么

⚠️ **ctime 不是创建时间**；程序**不能**直接设定 ctime。 经典 ext 不强调 birth time；较新内核可用 **`statx`** 取 **btime**。 int utime(const char *path, const struct utimbuf *times);      /* 旧 */

---

## 要点

| 时间 | 何时更新 |
|------|----------|
| **atime** | 读数据（`read` 等）；挂载 `noatime` 可关，提性能 |
| **mtime** | 内容变（`write`/`truncate`） |
| **ctime** | inode 元数据变（`chmod`/`chown`/`link`/`unlink`/`rename` 等） |

⚠️ **ctime 不是创建时间**；程序**不能**直接设定 ctime。  
经典 ext 不强调 birth time；较新内核可用 **`statx`** 取 **btime**。

### 改 atime/mtime

```c
int utime(const char *path, const struct utimbuf *times);      /* 旧 */
int utimes(const char *path, const struct timeval times[2]);
int futimens(int fd, const struct timespec times[2]);          /* 推荐，纳秒 */
int utimensat(int dirfd, const char *pathname,
              const struct timespec times[2], int flags);
```

- `times[0]`=atime，`times[1]`=mtime  
- `times == NULL` → 都设为现在  
- `UTIME_NOW` / `UTIME_OMIT` 可选改一项  
- `AT_SYMLINK_NOFOLLOW`：不跟随链接  

Demo：[`code/futimens_demo.c`](./code/futimens_demo.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | atime / mtime / ctime 在嵌入式系统的应用场景 |
| HFT | atime / mtime / ctime 对低延迟和性能的影响 |

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[15.4 `chown` / `fchown` / `lchown`](./15.4-chown-fchown-lchown.md)
- 下一节：[15.6 `umask`](./15.6-umask.md)
- 前置：**前置**：[Ch14 File Systems](../chapter-14-file-systems/notes.md)（inode 模型）  
- 后置：**后置**：[Ch16 Extended Attributes](../chapter-16-extended-attributes/notes.md) · [Ch18 目录与链接](../chapter-18-directories-links/notes.md) · [Ch38 特权与安全](../chapter-38-secure-privileged/notes.md)

---

## man 手册参考

- `man 2 read`
- `man 2 write`
- `man 2 chmod`
- `man 2 chown`
- `man 2 link`
- `man 2 unlink`
