# 15.4 `chown` / `fchown` / `lchown`

> 本章：[TLPI 第 15 章 — File Attributes](./README.md)
> - 上一节：[15.3 `chmod` / `fchmod`](./15.3-chmod-fchmod.md)
> - 下一节：[15.5 atime / mtime / ctime](./15.5-atime-mtime-ctime.md)

---

## 本节讲什么

int  chown(const char *path, uid_t owner, gid_t group); int lchown(const char *path, uid_t owner, gid_t group);  /* 不跟随链接 */ int fchown(int fd, uid_t owner, gid_t group);

---

## 要点

```c
int  chown(const char *path, uid_t owner, gid_t group);
int lchown(const char *path, uid_t owner, gid_t group);  /* 不跟随链接 */
int fchown(int fd, uid_t owner, gid_t group);
```

Linux 常见约束：

| 谁 | 能做什么 |
|----|----------|
| root | 任意改 uid/gid |
| 普通用户 | 不能把属主改成别人；属组通常只能改成自己所属组 |

`owner`/`group` 传 `(uid_t)-1` / `(gid_t)-1`：**不改该项**。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `chown` / `fchown` / `lchown` 在嵌入式系统的应用场景 |
| HFT | `chown` / `fchown` / `lchown` 对低延迟和性能的影响 |

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[15.3 `chmod` / `fchmod`](./15.3-chmod-fchmod.md)
- 下一节：[15.5 atime / mtime / ctime](./15.5-atime-mtime-ctime.md)
- 前置：**前置**：[Ch14 File Systems](../chapter-14-file-systems/notes.md)（inode 模型）  
- 后置：**后置**：[Ch16 Extended Attributes](../chapter-16-extended-attributes/notes.md) · [Ch18 目录与链接](../chapter-18-directories-links/notes.md) · [Ch38 特权与安全](../chapter-38-secure-privileged/notes.md)
