# 15.7 `access()` — 尽量别用

> 本章：[TLPI 第 15 章 — File Attributes](./README.md)
> - 上一节：[15.6 `umask`](./15.6-umask.md)
> - 下一节：[15.9 `statx`（Linux）](./15.9-statx.md)

---

## 本节讲什么

int access(const char *pathname, int mode);  /* R_OK W_OK X_OK F_OK */ 安全做法：直接 `open`，处理 `EACCES`。 ---

---

## 要点

```c
int access(const char *pathname, int mode);  /* R_OK W_OK X_OK F_OK */
```

| 问题 | |
|------|--|
| 用 **真实** UID/GID 测 | 实际打开用 **有效** UID/GID → setuid 下易误判 |
| TOCTOU | 测完再 `open` 之间状态可变 |

安全做法：直接 `open`，处理 `EACCES`。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `access()` — 尽量别用 在嵌入式系统的应用场景 |
| HFT | `access()` — 尽量别用 对低延迟和性能的影响 |

## 代码示例

```c
#include <unistd.h>
#include <stdio.h>

int main(void) {
    /* access: 检查真实 UID/GID 的权限 (不是 effective) */
    const char *path = "/etc/passwd";

    if (access(path, R_OK) == 0)
        printf("可读 %s\n", path);
    else
        printf("不可读: %s\n", path);

    if (access(path, W_OK) == 0)
        printf("可写 %s\n", path);
    else
        printf("不可写\n");

    if (access(path, X_OK) == 0)
        printf("可执行 %s\n", path);
    else
        printf("不可执行\n");

    if (access(path, F_OK) == 0)
        printf("文件存在\n");

    /* 注意: access 有 TOCTOU 竞态!
     * access 检查和后续 open 之间文件可能被替换
     * 安全做法: 直接 open 并检查错误 */
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[15.6 `umask`](./15.6-umask.md)
- 下一节：[15.9 `statx`（Linux）](./15.9-statx.md)
- 前置：**前置**：[Ch14 File Systems](../chapter-14-file-systems/notes.md)（inode 模型）  
- 后置：**后置**：[Ch16 Extended Attributes](../chapter-16-extended-attributes/notes.md) · [Ch18 目录与链接](../chapter-18-directories-links/notes.md) · [Ch38 特权与安全](../chapter-38-secure-privileged/notes.md)
