# 47.8 `SEM_UNDO`

> 本章：[TLPI 第 47 章 — System V Semaphores](../README.md)
> - 上一节：[47.5 初始化竞态（经典坑）](47.5-semaphore-initialization.md)
> - 下一节：[47.9 二元信号量 ≈ 互斥](47.10-semaphore-limits.md)

---

## 本节讲什么

带此标志的 `semop` 记入进程 undo 表；进程退出（含 kill）内核回放反向操作。 局限：只跟**本进程**；`IPC_RMID` 后 undo 失效；多次 P 叠加 undo；**不能替代正常 V**。 ---

---

## 要点

带此标志的 `semop` 记入进程 undo 表；进程退出（含 kill）内核回放反向操作。  
局限：只跟**本进程**；`IPC_RMID` 后 undo 失效；多次 P 叠加 undo；**不能替代正常 V**。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `SEM_UNDO` 在嵌入式系统的应用场景 |
| HFT | `SEM_UNDO` 对低延迟和性能的影响 |

## 代码示例

```c
#include <stdio.h>
#include <sys/sem.h>
#include <unistd.h>
#include <sys/wait.h>
/* SEM_UNDO: 进程退出时自动撤销信号量操作 */
int main(void) {
    int semid = semget(IPC_PRIVATE, 1, IPC_CREAT | 0666);
    semctl(semid, 0, SETVAL, 1);

    pid_t pid = fork();
    if (pid == 0) {
        /* 子进程: P 操作带 SEM_UNDO 标志 */
        struct sembuf sop = {0, -1, SEM_UNDO};
        semop(semid, &sop, 1);
        printf("Child: P done, value=%d\n", semctl(semid, 0, GETVAL));

        /* 模拟异常退出 (不 V) */
        printf("Child: exiting without V (SEM_UNDO will auto-undo)\n");
        _exit(0);  /* 内核自动恢复信号量值 */
    }

    waitpid(pid, NULL, 0);
    /* 子进程退出后, SEM_UNDO 自动将值恢复 */
    printf("Parent: after child exit, value=%d (auto-restored to 1)\n",
           semctl(semid, 0, GETVAL));

    /* 不用 SEM_UNDO 的情况: 子进程 P 后崩溃, 信号量永远卡在 0 */
    semctl(semid, 0, IPC_RMID);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[47.5 初始化竞态（经典坑）](47.5-semaphore-initialization.md)
- 下一节：[47.9 二元信号量 ≈ 互斥](47.10-semaphore-limits.md)
- 前置：**前置**：[Ch45 导论](../../chapter-45-sysv-ipc-intro/notes) · [Ch46 消息队列](../../chapter-46-sysv-message-queues/notes)  
- 后置：**后置**：[Ch48 SysV 共享内存](../../chapter-48-sysv-shared-memory/notes)
