# 47.5 初始化竞态（经典坑）

> 本章：[TLPI 第 47 章 — System V Semaphores](./README.md)
> - 上一节：[47.2 –47.4 API](./47.2-api.md)
> - 下一节：[47.8 `SEM_UNDO`](./47.8-semundo.md)

---

## 本节讲什么

A `semget(CREAT)` 尚未 `SETVAL`，B 已 `semop` → 值未定义。 **解法**：仅 `IPC_CREAT|IPC_EXCL` 成功者初始化；`EEXIST` 则再 `semget` 打开。 semid = semget(key, 1, IPC_CREAT | IPC_EXCL | 0600);

---

## 要点

A `semget(CREAT)` 尚未 `SETVAL`，B 已 `semop` → 值未定义。

**解法**：仅 `IPC_CREAT|IPC_EXCL` 成功者初始化；`EEXIST` 则再 `semget` 打开。

```c
semid = semget(key, 1, IPC_CREAT | IPC_EXCL | 0600);
if (semid >= 0) {
    union semun su = { .val = 1 };
    semctl(semid, 0, SETVAL, su);
} else if (errno == EEXIST) {
    semid = semget(key, 1, 0600);
}
```

Demo：[`code/`](./code/)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 初始化竞态（经典坑） 在嵌入式系统的应用场景 |
| HFT | 初始化竞态（经典坑） 对低延迟和性能的影响 |

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[47.2 –47.4 API](./47.2-api.md)
- 下一节：[47.8 `SEM_UNDO`](./47.8-semundo.md)
- 前置：**前置**：[Ch45 导论](../chapter-45-sysv-ipc-intro/notes.md) · [Ch46 消息队列](../chapter-46-sysv-message-queues/notes.md)  
- 后置：**后置**：[Ch48 SysV 共享内存](../chapter-48-sysv-shared-memory/notes.md)
