# 47.8 `SEM_UNDO`

> 本章：[TLPI 第 47 章 — System V Semaphores](./README.md)
> - 上一节：[47.5 初始化竞态（经典坑）](./47.5-initialization.md)
> - 下一节：[47.9 二元信号量 ≈ 互斥](./47.9-semaphore-mutex.md)

---

## 本节讲什么

带此标志的 `semop` 记入进程 undo 表；进程退出（含 kill）内核回放反向操作。 局限：只跟**本进程**；`IPC_RMID` 后 undo 失效；多次 P 叠加 undo；**不能替代正常 V**。 ---

---

## 要点

带此标志的 `semop` 记入进程 undo 表；进程退出（含 kill）内核回放反向操作。  
局限：只跟**本进程**；`IPC_RMID` 后 undo 失效；多次 P 叠加 undo；**不能替代正常 V**。

---

---

## 与 C 语言学习的关系

`kill` 发送信号看似简单，但涉及 C 语言的进程权限模型。`kill(pid, 0)` 的探测用法需要理解 C 语言的返回值约定（0 成功，-1 失败）和 errno 机制。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `SEM_UNDO` 在嵌入式系统的应用场景 |
| HFT | `SEM_UNDO` 对低延迟和性能的影响 |

---

## 常见陷阱

1. **kill 不等于杀死**

```c
kill(pid, SIGTERM);  /* 发送 SIGTERM，不是杀死 */
```

kill 是"发送信号"，不是"杀死进程"。SIGTERM 可以被捕获和处理。只有 SIGKILL 才强制杀死。

2. **权限不足**

```c
kill(other_pid, SIGTERM);  /* EPERM if not owner */
```

只能给自己同 UID 的进程发信号（root 除外）。权限不足返回 -1，errno=EPERM。

3. **pid 参数含义**

```c
kill(0, sig);   /* 发给同进程组 */
kill(-1, sig);  /* 发给所有有权限的 */
kill(-pgid, sig); /* 发给进程组 */
```

pid 的正/负/零含义不同。用错可能误发信号给不该收的进程。

---

## 自测题

**题目 1（选择）：** `kill(pid, 0)` 的作用是？
A. 杀死进程
B. 发送信号 0
C. 检查进程是否存在/是否有权限
D. 无操作

<details>
<summary>参考答案</summary>

C。信号 0 不实际发送信号，但会进行权限检查。返回 0 表示进程存在且有权限，-1 表示不存在或无权限。

</details>

**题目 2（判断）：** `kill(getpid(), SIGKILL)` 等价于 `exit()`。

<details>
<summary>参考答案</summary>

错误。SIGKILL 不会执行 atexit 处理器、不会 flush stdio 缓冲、不会做资源清理。exit() 会执行清理。

</details>

**题目 3（踩坑）：** 以下代码有什么风险？
```c
pid_t pid = fork();
if (pid == 0) {
    /* child */
    sleep(100);
}
kill(pid, SIGTERM);  /* 父进程发信号 */
```

<details>
<summary>参考答案</summary>

如果 fork 失败（返回 -1），pid=-1，kill(-1, SIGTERM) 会给所有有权限的进程发信号！必须先检查 fork 返回值。

</details>

**题目 4（应用）：** 写代码：给指定进程发 SIGTERM，等 1 秒后如果还没退出则发 SIGKILL。

<details>
<summary>参考答案</summary>

```c
kill(pid, SIGTERM);
sleep(1);
if (waitpid(pid, NULL, WNOHANG) == 0) {
    kill(pid, SIGKILL);
    waitpid(pid, NULL, 0);
}
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[47.5 初始化竞态（经典坑）](./47.5-initialization.md)
- 下一节：[47.9 二元信号量 ≈ 互斥](./47.9-semaphore-mutex.md)
- 前置：**前置**：[Ch45 导论](../chapter-45-sysv-ipc-intro/notes.md) · [Ch46 消息队列](../chapter-46-sysv-message-queues/notes.md)  
- 后置：**后置**：[Ch48 SysV 共享内存](../chapter-48-sysv-shared-memory/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
