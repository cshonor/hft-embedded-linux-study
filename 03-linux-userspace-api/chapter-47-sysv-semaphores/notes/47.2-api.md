# 47.2 –47.4 API

> 本章：[TLPI 第 47 章 — System V Semaphores](./README.md)
> - 上一节：[47.1 概念](./47.1-concepts.md)
> - 下一节：[47.5 初始化竞态（经典坑）](./47.5-initialization.md)

---

## 本节讲什么

创建时指定 `nsems`；打开已有集时 `nsems` 不能大于集大小。 ⚠️ **只分配，不初始化数值**。 struct sembuf { unsigned short sem_num; short sem_op; short sem_flg; };

---

## 要点

### `semget(key, nsems, semflg)`

创建时指定 `nsems`；打开已有集时 `nsems` 不能大于集大小。  
⚠️ **只分配，不初始化数值**。

### `semctl` · 须自声明 `union semun`

| cmd | |
|-----|--|
| `SETVAL` / `GETVAL` | 单个 |
| `SETALL` / `GETALL` | 整集 |
| `IPC_STAT` / `IPC_SET` | 属性 |
| `IPC_RMID` | **立即删整集**；唤醒阻塞者 |

### `semop(semid, sops, nsops)`

```c
struct sembuf { unsigned short sem_num; short sem_op; short sem_flg; };
```

| `sem_op` | 行为 |
|----------|------|
| `>0` | V，不阻塞 |
| `<0` | P；值不够则阻塞（或 `IPC_NOWAIT`→`EAGAIN`） |
| `==0` | 等变为 0 |

`sem_flg`：`IPC_NOWAIT` · **`SEM_UNDO`**。  
多条 `sembuf`：**原子 all-or-nothing**（可一次拿多锁，减死锁）。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | –47.4 API 在嵌入式系统的应用场景 |
| HFT | –47.4 API 对低延迟和性能的影响 |

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[47.1 概念](./47.1-concepts.md)
- 下一节：[47.5 初始化竞态（经典坑）](./47.5-initialization.md)
- 前置：**前置**：[Ch45 导论](../chapter-45-sysv-ipc-intro/notes.md) · [Ch46 消息队列](../chapter-46-sysv-message-queues/notes.md)  
- 后置：**后置**：[Ch48 SysV 共享内存](../chapter-48-sysv-shared-memory/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
