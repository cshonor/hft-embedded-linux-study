# 29.4 线程 vs 进程

> 本章：[TLPI 第 29 章 — Threads: Introduction](../README.md)
> - 上一节：[29.2 Pthreads 规范](29.2-background-details-of-the-pthreads-api.md)
> - 下一节：下一章+1:02d}-*/notes.md)

---

## 本节讲什么

---

## 要点

| | 进程 fork | 线程 pthread |
|--|-----------|--------------|
| 地址空间 | 独立（COW） | 共享 |
| 创建开销 | 更高 | 更低 |
| 共享数据 | IPC | 直接全局/堆 |
| `exit` | 只杀本进程 | 杀**全部**线程 |
| 回收 | `waitpid` | join / detach |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 线程共享地址空间省 IPC；进程隔离更安全（一个进程崩不拖累其他）——嵌入式看实时性选线程、看可靠性选进程 |
| HFT | HFT 关键路径用线程共享内存（零拷贝共享行情/订单簿），但隔离性差——一个线程 segfault 杀**全进程**；对稳定性要求高的组件改用进程 + 共享内存隔离 |

## 代码示例

```c
#include <pthread.h>
#include <unistd.h>
#include <stdio.h>
void *worker(void *arg) {
    printf("线程 PID=%d TID=%lu\n", getpid(), (unsigned long)pthread_self());
    return NULL;
}
int main(void) {
    pthread_t t1, t2;
    pthread_create(&t1, NULL, worker, NULL);
    pthread_create(&t2, NULL, worker, NULL);
    pthread_join(t1, NULL); pthread_join(t2, NULL);
    printf("主线程 PID=%d (同进程共享 PID)\n", getpid());
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[29.2 Pthreads 规范](29.2-background-details-of-the-pthreads-api.md)
- 前置：**前置**：[Ch28 fork/exec 深潜](../../chapter-28-process-creation-exec-detail/notes)  
- 后置：**后置**：[Ch30 线程同步](../../chapter-30-thread-synchronization/notes)
