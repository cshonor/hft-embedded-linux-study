# 29.7 分离线程（pthread_detach）

> 本章：[TLPI 第 29 章 — 线程](../README.md)
> - 上一节：[29.6 加入线程](./29.6-joining-with-a-terminated-thread.md)
> - 下一节：[29.8 线程属性](./29.8-thread-attributes.md)

---

## 本节讲什么

`pthread_detach()` — 将线程标记为分离状态，终止时自动回收资源，无需 join。

---

## 要点

### API

```c
int pthread_detach(pthread_t thread);
```
- 将 `thread` 标记为 detached
- 线程终止时自动回收资源
- 之后不能 `pthread_join`（返回 EINVAL）

### 两种线程状态

| 状态 | 行为 | 资源回收 |
|------|------|----------|
| **joinable** (默认) | 终止后等待 join | 需要 `pthread_join` |
| **detached** | 终止后自动回收 | 内核自动回收 |

### 何时用 detach

- **不需要返回值** → detach
- **不想阻塞等待** → detach
- **线程生命周期不确定** → detach
- **需要返回值** → join（不 detach）

### 设置方式

```c
/* 方式1: 创建后 detach */
pthread_t tid;
pthread_create(&tid, NULL, worker, arg);
pthread_detach(tid);

/* 方式2: 线程内自己 detach */
void *worker(void *arg) {
    pthread_detach(pthread_self());
    /* ... */
    return NULL;
}

/* 方式3: 创建时通过属性 detach */
pthread_attr_t attr;
pthread_attr_init(&attr);
pthread_attr_setdetachstate(&attr, PTHREAD_CREATE_DETACHED);
pthread_create(&tid, &attr, worker, arg);
pthread_attr_destroy(&attr);
```

### 注意事项

- detach 后无法获取返回值
- detach 后无法知道线程何时终止
- **每个线程必须被 join 或 detach**，否则资源泄漏

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 短生命周期后台任务用 detach，避免泄漏 |
| HFT | 线程池中的工作线程不用 detach（需复用），独立监控线程可 detach |

## 代码示例

```c
#include <stdio.h>
#include <pthread.h>
#include <unistd.h>
/* pthread_detach: 分离线程, 自动回收 */
void *logger(void *arg) {
    /* 自己 detach */
    pthread_detach(pthread_self());
    for (int i = 0; i < 3; i++) {
        printf("logger: tick %d\n", i);
        sleep(1);
    }
    return NULL;  /* 自动回收, 无需 join */
}

int main(void) {
    pthread_t tid;
    pthread_create(&tid, NULL, logger, NULL);

    /* 主线程不等 logger, 自己做别的事 */
    sleep(2);
    printf("main: doing other work\n");

    sleep(2);  /* 等 logger 跑完 */
    printf("main: exiting\n");
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[29.6 加入线程](./29.6-joining-with-a-terminated-thread.md)
- 下一节：[29.8 线程属性](./29.8-thread-attributes.md)
