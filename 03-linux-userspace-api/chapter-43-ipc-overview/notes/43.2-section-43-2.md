# 43.2 命名与句柄（表 43-1 精简）

> 本章：[TLPI 第 43 章 — Interprocess Communication Overview](./README.md)
> - 上一节：[43.1 三大类](./43.1-section-43-1.md)
> - 下一节：[43.2 可访问范围 + 持久性（表 43-2 · 高频）](./43.2-persistence-access-scope.md)

---

## 本节讲什么

痛点：SysV 独立 key/id，无标准路径名 → POSIX 用类文件名改良。 ---

---

## 要点

| 工具 | 跨进程识别名 | 进程内句柄 |
|------|--------------|------------|
| 匿名管道 | 无 | fd |
| FIFO | 路径名 | fd |
| UNIX / Internet socket | 路径或 IP:port | fd |
| System V IPC | `key_t` | IPC id |
| POSIX mq / 有名 sem / shm | `/` 路径名 | `mqd_t` / `sem_t*` / fd |
| POSIX 无名 sem | 无（放共享内存） | `sem_t*` |
| mmap 文件映射 | 文件路径 | fd |

痛点：SysV 独立 key/id，无标准路径名 → POSIX 用类文件名改良。

---

---

## 与 C 语言学习的关系

System V IPC（消息队列、信号量、共享内存）是 Unix 进程间通信的传统机制。这些 API 使用 `key_t` 和 `int` ID，需要 C 语言的类型和 struct 知识。注意 SVIPC 资源不会随进程退出自动释放——这是和 pipe/fifo 的重要区别，需要手动清理。
---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 命名与句柄（表 43-1 精简） 在嵌入式系统的应用场景 |
| HFT | 命名与句柄（表 43-1 精简） 对低延迟和性能的影响 |

---

## 常见陷阱

1. **IPC 资源未释放**

```c
/* 创建共享内存段 */
int shmid = shmget(key, size, IPC_CREAT|0666);
/* 进程退出后段仍存在! */
```

System V IPC 资源持久存在于内核中，不会随进程退出自动释放。必须显式调用 shmctl(shmid, IPC_RMID, NULL) 删除。

2. **ftok 路径失效**

```c
key_t key = ftok("/tmp/myapp", 'A');
/* /tmp/myapp 被删或重建 → key 变化 */
```

ftok 基于文件 inode 生成 key。如果文件被删除重建，inode 变化，key 不同。应使用稳定的文件路径。

3. **信号量初始值未设置**

```c
int semid = semget(key, 1, IPC_CREAT|0666);
/* 初始值不确定（通常是0） */
/* 需要手动 SETVAL */
```

新创建的信号量初始值不确定。必须用 semctl(semid, 0, SETVAL, val) 显式设置初始值。

---

## 自测题

**题目 1（选择）：** System V IPC 的三种类型是？
A. pipe, FIFO, socket
B. 消息队列, 信号量, 共享内存
C. mutex, condvar, barrier
D. mmap, shm, sem

<details>
<summary>参考答案</summary>

B。System V IPC 包含消息队列（msgq）、信号量（semaphore）和共享内存（shared memory）三种。

</details>

**题目 2（判断）：** System V IPC 资源会随进程退出自动释放。

<details>
<summary>参考答案</summary>

错误。SVIPC 资源持久存在于内核中，不会随进程退出自动释放。必须显式删除（如 shmctl(IPC_RMID)）。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
key_t key = ftok("/tmp/app", 'A');
int shmid = shmget(key, 4096, IPC_CREAT|0666);
char *p = shmat(shmid, NULL, 0);
/* 使用 p ... */
/* 程序退出，没有 shmdt 和 shmctl(IPC_RMID) */
```

<details>
<summary>参考答案</summary>

1) 没有 shmdt(p) 解除附加；2) 没有 shmctl(shmid, IPC_RMID, NULL) 删除段。段会一直存在于内核中直到手动删除或系统重启。

</details>

**题目 4（应用）：** 写代码创建共享内存段，附加，使用后正确清理。

<details>
<summary>参考答案</summary>

```c
int shmid = shmget(IPC_PRIVATE, 4096, IPC_CREAT|0666);
char *p = shmat(shmid, NULL, 0);
/* 使用 p ... */
shmdt(p);
shmctl(shmid, IPC_RMID, NULL);
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[43.1 三大类](./43.1-section-43-1.md)
- 下一节：[43.2 可访问范围 + 持久性（表 43-2 · 高频）](./43.2-persistence-access-scope.md)
- 前置：**前置**：[Ch42 共享库高级 / dlopen](../chapter-42-shared-libraries-advanced/notes.md)  
- 后置：**后置**：[Ch44 管道与 FIFO](../chapter-44-pipes-fifos/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
