# 43.2 可访问范围 + 持久性（表 43-2 · 高频）

> 本章：[TLPI 第 43 章 — Interprocess Communication Overview](./README.md)
> - 上一节：[43.2 命名与句柄（表 43-1 精简）](./43.2-section-43-2.md)
> - 下一节：[43.3 SysV vs POSIX（铺垫）](./43.3-sysv.md)

---

## 本节讲什么

坑：内核持久对象忘记 `*_unlink` / `ipcrm` → 资源泄漏、占满限额。 ---

---

## 要点

### 可访问范围

| 范围 | 例子 |
|------|------|
| 仅相关进程 | 匿名 pipe、匿名 POSIX sem |
| 本机任意（权限掩码） | FIFO、Unix socket、SysV/POSIX IPC、文件锁 |
| 跨主机 | Internet socket |

### 持久性（生命周期）

| 级别 | 含义 | 代表 |
|------|------|------|
| **进程持久** | 最后一方关句柄即毁 | pipe、FIFO、socket |
| **内核持久** | 须显式删或重启；进程全退仍在 | SysV IPC、POSIX 有名 mq/sem/shm |
| **文件系统持久** | 落盘，重启仍在 | 基于文件的 `mmap` |

坑：内核持久对象忘记 `*_unlink` / `ipcrm` → 资源泄漏、占满限额。

---

---

## 与 C 语言学习的关系

`mmap` 是 C 语言高级内存操作。理解 `mmap` 需要 C 语言指针、内存对齐、页概念的知识。`mmap` 返回 `void*`（不是 NULL）失败是 C 语言中不同于 `malloc` 的一个细节。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 可访问范围 + 持久性（表 43-2 · 高频） 在嵌入式系统的应用场景 |
| HFT | 可访问范围 + 持久性（表 43-2 · 高频） 对低延迟和性能的影响 |

---

## 常见陷阱

1. **MAP_FAILED vs NULL**

```c
void *p = mmap(NULL, size, PROT_READ|PROT_WRITE,
              MAP_PRIVATE|MAP_ANONYMOUS, -1, 0);
if (p == NULL) ...  /* 错误! */
if (p == MAP_FAILED) ...  /* 正确 */
```

mmap 失败返回 MAP_FAILED ((void*)-1)，不是 NULL。检查方式与 malloc 不同。

2. **munmap 忘记调用**

```c
void *p = mmap(NULL, 4096, ...);
/* 使用 p ... */
/* 没有 munmap → 内存泄漏 */
```

mmap 分配的内存不会自动释放。必须 munmap(p, size) 显式释放。

3. **信号处理中的 SIGSEGV**

```c
mmap(NULL, size, PROT_READ, MAP_PRIVATE|MAP_ANONYMOUS, -1, 0);
/* 写只读映射 → SIGSEGV */
```

对 PROT_READ 的映射写入会触发 SIGSEGV。确保保护属性和访问操作匹配。

---

## 自测题

**题目 1（选择）：** `mmap` 失败时返回什么？
A. NULL
B. -1
C. MAP_FAILED
D. 0

<details>
<summary>参考答案</summary>

C。mmap 失败返回 MAP_FAILED（定义为 (void *)-1），不是 NULL。这与 malloc 不同。

</details>

**题目 2（判断）：** `mmap` 映射的内存会在进程退出时自动释放。

<details>
<summary>参考答案</summary>

正确。进程退出时所有映射自动解除。但在长时间运行的程序中仍需手动 munmap。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
void *p = mmap(NULL, 4096, PROT_READ|PROT_WRITE,
               MAP_PRIVATE|MAP_ANONYMOUS, -1, 0);
memset(p, 0, 8192);
```

<details>
<summary>参考答案</summary>

写入了 8192 字节但只映射了 4096 字节。超出映射区域会触发 SIGSEGV。应确保操作不超过映射大小。

</details>

**题目 4（应用）：** 写代码用 mmap 分配匿名内存并初始化为 0。

<details>
<summary>参考答案</summary>

```c
void *p = mmap(NULL, 4096, PROT_READ|PROT_WRITE,
               MAP_PRIVATE|MAP_ANONYMOUS, -1, 0);
if (p == MAP_FAILED) { perror("mmap"); exit(1); }
memset(p, 0, 4096);
/* ... 使用 ... */
munmap(p, 4096);
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[43.2 命名与句柄（表 43-1 精简）](./43.2-section-43-2.md)
- 下一节：[43.3 SysV vs POSIX（铺垫）](./43.3-sysv.md)
- 前置：**前置**：[Ch42 共享库高级 / dlopen](../chapter-42-shared-libraries-advanced/notes.md)  
- 后置：**后置**：[Ch44 管道与 FIFO](../chapter-44-pipes-fifos/notes.md)

---

## man 手册参考

- `man 2 mmap`
