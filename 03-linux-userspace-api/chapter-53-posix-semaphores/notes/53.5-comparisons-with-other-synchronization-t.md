# 53.5 fork / exec

> 本章：[TLPI 第 53 章 — POSIX Semaphores](../README.md)
> - 上一节：[53.4 匿名](53.4-unnamed-semaphores.md)
> - 下一节：[53.6 vs SysV · vs mutex](53.6-semaphore-limits.md)

---

## 本节讲什么

---

---

## 要点

| | 命名 | 匿名 pshared=0 | 匿名 pshared≠0 |
|--|------|----------------|----------------|
| fork | 同内核对象 | **各自副本，不同步** | 共享内存内同一 sem |
| exec | 自动 close | 映射毁 → 失效 | 同左 |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | fork / exec 在嵌入式系统的应用场景 |
| HFT | fork / exec 对低延迟和性能的影响 |

## 代码示例

```c

/* POSIX 信号量在 fork/exec 后的继承 */
#include <stdio.h>
#include <stdlib.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <semaphore.h>
#include <sys/wait.h>
#include <unistd.h>

int main(void) {
    /* 命名信号量：fork 后子进程可用 sem_open 重新打开（同名） */
    sem_t *sem = sem_open("/inherit_sem", O_CREAT, 0644, 1);
    if (sem == SEM_FAILED) { perror("sem_open"); exit(1); }

    pid_t pid = fork();
    if (pid == 0) {
        /* 子进程继承父进程的 sem_t* 指针（同一引用） */
        sem_wait(sem);
        printf("child: acquired inherited sem\n");
        sem_post(sem);
        _exit(0);
    }
    wait(NULL);

    /* exec 后：命名信号量可用 sem_open 重新获取 */
    /* 未命名信号量：如果在共享内存中，fork 后仍可用，exec 后丢失 */
    printf("fork: sem_t* inherited (same handle)\n");
    printf("exec: named sem re-openable, unnamed sem lost\n");

    sem_close(sem); sem_unlink("/inherit_sem");
    return 0;
}

```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[53.4 匿名](53.4-unnamed-semaphores.md)
- 下一节：[53.6 vs SysV · vs mutex](53.6-semaphore-limits.md)
- 前置：**前置**：[Ch52 POSIX mq](../../chapter-52-posix-message-queues/notes) · [Ch47 SysV sem](../../chapter-47-sysv-semaphores/notes)  
- 后置：**后置**：[Ch54 POSIX 共享内存](../../chapter-54-posix-shared-memory/notes)
