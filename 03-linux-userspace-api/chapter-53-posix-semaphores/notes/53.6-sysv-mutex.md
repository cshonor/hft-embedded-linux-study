# 53.6 vs SysV · vs mutex

> 本章：[TLPI 第 53 章 — POSIX Semaphores](./README.md)
> - 上一节：[53.5 fork / exec](./53.5-fork-exec.md)
> - 下一节：[下一章](../chapter-{int(ch_num)+1:02d}-*/notes.md)

---

## 本节讲什么

工程：多锁原子 / SEM_UNDO → SysV；简洁单计数器 → POSIX。 线程互斥优先 **`pthread_mutex`**（有所有权、常更快）；跨进程用信号量。 ---

---

## 要点

| | SysV | POSIX |
|--|------|-------|
| 形态 | 集、多计数器 | 单个 |
| 初始化 | get+ctl 竞态 | open/init 原子 |
| 多 op 原子 | `semop` ✅ | ❌ |
| 崩溃撤销 | `SEM_UNDO` | **无** |
| 超时 | 弱 | `sem_timedwait` |

工程：多锁原子 / SEM_UNDO → SysV；简洁单计数器 → POSIX。  
线程互斥优先 **`pthread_mutex`**（有所有权、常更快）；跨进程用信号量。

---

---

## 与 C 语言学习的关系

本节涉及的 `open()` 是 C 语言文件操作的基础系统调用。与 K&R 第 8 章的 `fopen()` 不同，`open()` 是 POSIX 系统调用而非 C 标准库函数。理解 `open()` 的 flags 参数（O_RDONLY、O_CREAT 等）是掌握 Unix 系统编程的第一步。对照 K&R 8.3 节的 `open/creat/close` 学习，注意 C 标准库 `fopen` 的 "r"/"w"/"a" 模式如何映射到 `open` 的 flags。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | vs SysV · vs mutex 在嵌入式系统的应用场景 |
| HFT | vs SysV · vs mutex 对低延迟和性能的影响 |

---

## 常见陷阱

1. **死锁**

```c
pthread_mutex_lock(&a);
pthread_mutex_lock(&b);  /* 如果另一线程先 lock b → 死锁 */
```

多个 mutex 的加锁顺序不一致会导致死锁。所有线程必须按相同顺序加锁。

2. **忘记 unlock**

```c
pthread_mutex_lock(&m);
if (error) return;  /* 没 unlock */
pthread_mutex_unlock(&m);
```

所有代码路径都必须 unlock。用 goto cleanup 或 C++ RAII。

3. **优先级反转**

```c
/* 低优先级线程持有 mutex */
/* 高优先级线程等待 mutex → 被低优先级阻塞 */
```

低优先级线程持有锁时高优先级线程被阻塞。用 PTHREAD_PRIO_INHERIT 或避免在关键路径上加锁。

---

## 自测题

**题目 1（选择）：** `pthread_mutex_trylock` 返回 EBUSY 表示什么？
A. 系统繁忙
B. mutex 已被其他线程持有
C. mutex 不存在
D. 参数无效

<details>
<summary>参考答案</summary>

B。mutex 已被其他线程持有，trylock 不会阻塞而是立即返回 EBUSY。

</details>

**题目 2（判断）：** 同一个线程可以对同一个 mutex 加锁两次。

<details>
<summary>参考答案</summary>

错误（默认情况下）。普通 mutex 不可重入，同线程第二次 lock 会死锁。需要可重入 mutex 用 PTHREAD_MUTEX_RECURSIVE。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
pthread_mutex_t m = PTHREAD_MUTEX_INITIALIZER;
pthread_mutex_lock(&m);
/* 处理数据 */
if (error)
    exit(1);
pthread_mutex_unlock(&m);
```

<details>
<summary>参考答案</summary>

error 时直接 exit(1)，没有 unlock mutex。虽然 exit 会终止进程（mutex 随之销毁），但如果改为 return 就会死锁。应养成所有路径都 unlock 的习惯。

</details>

**题目 4（应用）：** 写代码用 mutex 保护一个计数器。

<details>
<summary>参考答案</summary>

```c
pthread_mutex_t m = PTHREAD_MUTEX_INITIALIZER;
int counter = 0;
void increment(void) {
    pthread_mutex_lock(&m);
    counter++;
    pthread_mutex_unlock(&m);
}
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[53.5 fork / exec](./53.5-fork-exec.md)
- 前置：**前置**：[Ch52 POSIX mq](../chapter-52-posix-message-queues/notes.md) · [Ch47 SysV sem](../chapter-47-sysv-semaphores/notes.md)  
- 后置：**后置**：[Ch54 POSIX 共享内存](../chapter-54-posix-shared-memory/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
