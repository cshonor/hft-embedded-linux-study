# 53.4 匿名

> 本章：[TLPI 第 53 章 — POSIX Semaphores](../README.md)
> - 上一节：[53.3 操作（两形态共用）](53.3-semaphore-operations.md)
> - 下一节：[53.5 fork / exec](53.5-comparisons-with-other-synchronization-t.md)

---

## 本节讲什么

int sem_init(sem_t *sem, int pshared, unsigned value); int sem_destroy(sem_t *sem); 勿对命名调 `sem_destroy`；勿对匿名调 `sem_open/close`。

---

## 要点

```c
int sem_init(sem_t *sem, int pshared, unsigned value);
int sem_destroy(sem_t *sem);
```

| `pshared` | |
|-----------|--|
| `0` | 线程间；普通进程内存 |
| `≠0` | 进程间；`sem_t` **必须在共享内存** |

勿对命名调 `sem_destroy`；勿对匿名调 `sem_open/close`。

Demo：[`code/`](../code/)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | pshared≠0 + sem_t 放共享内存可跨进程；线程间 pshared=0 无内核对象开销 |
| HFT | 线程内互斥优先匿名 pshared=0，绕开 sem_open 命名解析路径 |

## 代码示例

```c

/* 未命名信号量 sem_init / sem_destroy */
#include <stdio.h>
#include <stdlib.h>
#include <semaphore.h>
#include <sys/mman.h>
#include <sys/wait.h>
#include <unistd.h>

int main(void) {
    /* 在匿名共享内存中创建未命名信号量 */
    sem_t *sem = mmap(NULL, sizeof(sem_t), PROT_READ | PROT_WRITE,
                      MAP_SHARED | MAP_ANONYMOUS, -1, 0);
    if (sem == MAP_FAILED) { perror("mmap"); exit(1); }

    /* pshared=1 表示可跨进程共享 */
    if (sem_init(sem, 1, 2) < 0) { perror("sem_init"); exit(1); }
    printf("unnamed sem initialized: pshared=1, value=2\n");

    /* fork 两个子进程，各消耗一个信号量 */
    for (int i = 0; i < 2; i++) {
        pid_t pid = fork();
        if (pid == 0) {
            sem_wait(sem);
            printf("child %d: acquired sem\n", i);
            sleep(1);
            sem_post(sem);
            _exit(0);
        }
    }
    for (int i = 0; i < 2; i++) wait(NULL);

    int val;
    sem_getvalue(sem, &val);
    printf("final value=%d\n", val);

    sem_destroy(sem);
    munmap(sem, sizeof(sem_t));
    return 0;
}

```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[53.3 操作（两形态共用）](53.3-semaphore-operations.md)
- 下一节：[53.5 fork / exec](53.5-comparisons-with-other-synchronization-t.md)
- 前置：**前置**：[Ch52 POSIX mq](../../chapter-52-posix-message-queues/notes) · [Ch47 SysV sem](../../chapter-47-sysv-semaphores/notes)  
- 后置：**后置**：[Ch54 POSIX 共享内存](../../chapter-54-posix-shared-memory/notes)
