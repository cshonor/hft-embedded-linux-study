# 53.3 操作（两形态共用）

> 本章：[TLPI 第 53 章 — POSIX Semaphores](./README.md)
> - 上一节：[53.2 命名](./53.2-section-53-2.md)
> - 下一节：[53.4 匿名](./53.4-section-53-4.md)

---

## 本节讲什么

`sem_wait` · `sem_trywait` · `sem_timedwait`（**绝对** `CLOCK_REALTIME`）· `sem_post` · `sem_getvalue`（瞬时快照，勿当同步条件）。 ---

---

## 要点

`sem_wait` · `sem_trywait` · `sem_timedwait`（**绝对** `CLOCK_REALTIME`）· `sem_post` · `sem_getvalue`（瞬时快照，勿当同步条件）。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 操作（两形态共用） 在嵌入式系统的应用场景 |
| HFT | 操作（两形态共用） 对低延迟和性能的影响 |

## 代码示例

```c

/* sem_wait / sem_trywait / sem_timedwait / sem_post */
#include <stdio.h>
#include <stdlib.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <semaphore.h>
#include <time.h>

int main(void) {
    sem_t *sem = sem_open("/ops_sem", O_CREAT, 0644, 1);
    if (sem == SEM_FAILED) { perror("sem_open"); exit(1); }

    /* sem_wait：阻塞 P 操作 */
    sem_wait(sem);
    printf("sem_wait: acquired (value now 0)\n");

    /* sem_trywait：非阻塞 P，返回 -1 + EAGAIN */
    if (sem_trywait(sem) < 0) {
        printf("sem_trywait: would block (EAGAIN)\n");
    }

    /* sem_timedwait：限时等待 */
    struct timespec ts = { .tv_sec = 2, .tv_nsec = 0 };
    if (sem_timedwait(sem, &ts) < 0) {
        printf("sem_timedwait: timed out after 2s\n");
    }

    /* sem_post：V 操作 */
    sem_post(sem);
    printf("sem_post: released (value now 1)\n");

    /* 现在可以再次 sem_wait */
    sem_wait(sem);
    printf("sem_wait: acquired again\n");
    sem_post(sem);

    sem_close(sem); sem_unlink("/ops_sem");
    return 0;
}

```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[53.2 命名](./53.2-section-53-2.md)
- 下一节：[53.4 匿名](./53.4-section-53-4.md)
- 前置：**前置**：[Ch52 POSIX mq](../chapter-52-posix-message-queues/notes.md) · [Ch47 SysV sem](../chapter-47-sysv-semaphores/notes.md)  
- 后置：**后置**：[Ch54 POSIX 共享内存](../chapter-54-posix-shared-memory/notes.md)
