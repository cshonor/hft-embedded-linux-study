# 41.4 构建

> 本章：[TLPI 第 41 章 — Fundamentals of Shared Libraries](./README.md)
> - 上一节：[41.1 –41.3 静态 vs 共享](./41.1-static.md)
> - 下一节：[41.5 工具](./41.5-tools.md)

---

## 本节讲什么

gcc -c -fPIC foo.c gcc -shared -Wl,-soname,libfoo.so.1 -o libfoo.so.1.0.0 foo.o ln -s libfoo.so.1.0.0 libfoo.so.1

---

## 要点

```bash
gcc -c -fPIC foo.c
gcc -shared -Wl,-soname,libfoo.so.1 -o libfoo.so.1.0.0 foo.o
ln -s libfoo.so.1.0.0 libfoo.so.1
ln -s libfoo.so.1 libfoo.so          # -lfoo
gcc -o app main.c -L. -lfoo -Wl,--enable-new-dtags,-rpath,'$ORIGIN'
```

| 名称 | 例 |
|------|-----|
| Real | `libfoo.so.1.0.0` 实体文件 |
| **Soname** | `libfoo.so.1`（写进 ELF；运行时找它） |
| Linker | `libfoo.so`（`-lfoo`） |

**PIC**：相对寻址，可映射任意基址；建 `.so` 必备 `-fPIC`。

Demo 树：[`code/`](./code/)

---

---

## 与 C 语言学习的关系

本节内容属于 Unix 系统编程，是 C 语言学习的进阶实践。掌握这些 API 需要扎实的 C 语言基础：指针操作、结构体、错误处理（errno/返回值检查）、内存管理。建议对照 K&R 和 C 专家编程的相关章节学习，将系统 API 的使用与 C 语言底层机制（如函数调用约定、栈布局、内存对齐）联系起来理解。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 构建 在嵌入式系统的应用场景 |
| HFT | 构建 对低延迟和性能的影响 |

---

## 常见陷阱

1. **返回值未检查**

```c
/* 调用后不检查返回值直接继续 */
result = some_call(...);
use(result);  /* 如果失败? */
```

系统调用和库函数的返回值必须检查。失败时通常返回 -1 或 NULL 并设置 errno。不检查会导致后续操作在错误状态下继续。

2. **资源泄漏**

```c
/* 打开资源后在错误路径未释放 */
fd = open(...);
if (error) return;  /* fd 泄漏 */
```

所有分配的资源（fd、内存、锁）在所有代码路径上都必须释放。用 goto cleanup 模式确保清理。

3. **可移植性假设**

```c
/* 假设特定平台行为 */
/* 如假设 int 是 4 字节、指针是 8 字节 */
```

不要硬编码平台相关假设。用 sizeof、stdint.h 类型（uint32_t 等）和 POSIX 宏确保可移植性。

---

## 自测题

**题目 1（选择）：** 系统调用失败时通常如何报告错误？
A. 打印错误信息
B. 返回 -1 并设置 errno
C. 调用 exit
D. 抛出异常

<details>
<summary>参考答案</summary>

B。系统调用失败时返回 -1 并设置 errno。用 perror 或 strerror(errno) 获取描述。Pthreads 例外，返回错误码。

</details>

**题目 2（判断）：** errno 在函数成功时会被清零。

<details>
<summary>参考答案</summary>

错误。errno 只在失败时被设置，成功时不会清零。不能通过 errno 判断是否出错，必须先检查返回值。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
if (some_call() < 0) {
    printf("error: %s\n", strerror(errno));
    /* 但中间可能调用了其他函数，errno 被覆盖 */
}
```

<details>
<summary>参考答案</summary>

errno 可能在 strerror 调用前被其他函数覆盖。应该先保存 errno：`int e = errno;` 然后用 strerror(e)。或在 some_call 失败后立即处理。

</details>

**题目 4（应用）：** 写一个错误处理模板：调用系统调用，失败时打印错误并退出。

<details>
<summary>参考答案</summary>

```c
if (some_syscall(args) < 0) {
    perror("some_syscall");
    exit(EXIT_FAILURE);
}
/* 成功继续 */
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[41.1 –41.3 静态 vs 共享](./41.1-static.md)
- 下一节：[41.5 工具](./41.5-tools.md)
- 前置：**前置**：[Ch40 登录记账](../chapter-40-login-accounting/notes.md)  
- 后置：**后置**：[Ch42 共享库高级 / dlopen](../chapter-42-shared-libraries-advanced/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
