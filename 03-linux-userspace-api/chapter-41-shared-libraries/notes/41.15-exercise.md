# 41.15 Exercise

> 本章：[TLPI 第 41 章](./README.md)

## 本节讲什么

第 41 章的练习题与参考答案，巩固共享库创建、安装和版本管理的核心概念。

## 要点

### 练习 1：创建共享库

**题目**：从以下源文件创建共享库 `libstr.so.1.0.0`，设置正确的 SONAME，并安装到系统目录。

```c
// str_reverse.c
void str_reverse(char *s, int len) { /* ... */ }
// str_upper.c
void str_upper(char *s, int len) { /* ... */ }
```

<details>
<summary>参考答案</summary>

```bash
# 1. 编译为 PIC 目标文件
gcc -fPIC -c str_reverse.c str_upper.c

# 2. 创建共享库（设置 SONAME）
gcc -shared -Wl,-soname,libstr.so.1 -o libstr.so.1.0.0 \
    str_reverse.o str_upper.o

# 3. 安装到系统目录
sudo install -m 755 libstr.so.1.0.0 /usr/local/lib/

# 4. 创建符号链接
sudo ln -sf libstr.so.1.0.0 /usr/local/lib/libstr.so.1
sudo ln -sf libstr.so.1     /usr/local/lib/libstr.so

# 5. 更新缓存
sudo ldconfig

# 6. 验证
ldconfig -p | grep libstr
```

</details>

### 练习 2：版本兼容性

**题目**：以下对共享库的变更哪些是 ABI 兼容的？

(a) 增加 `void new_func(void)` 导出函数
(b) 将 `int foo(int)` 改为 `int foo(int, int)`
(c) 在 struct 末尾增加字段
(d) 修复函数内部 bug
(e) 删除导出函数 `bar()`

<details>
<summary>参考答案</summary>

- (a) **兼容** — 新增导出符号不影响已有接口
- (b) **不兼容** — 改变函数参数数量，调用方传参方式改变
- (c) **可能不兼容** — 如果调用者直接分配该 struct，大小不足
- (d) **兼容** — 内部实现改变不影响 ABI
- (e) **不兼容** — 删除导出符号，使用该函数的程序无法链接

</details>

### 练习 3：运行时查找

**题目**：一个可执行文件使用 `-Wl,-rpath,/opt/app/lib` 编译，但运行时 `libfoo.so` 在 `/opt/app/lib` 中不存在。解释动态链接器的搜索顺序。

<details>
<summary>参考答案</summary>

如果使用 `--enable-new-dtags`（生成 RUNPATH 而非 RPATH）：
1. LD_LIBRARY_PATH（如果设置）
2. DT_RUNPATH (`/opt/app/lib`) — 此处找不到
3. ld.so.cache — 搜索 `/etc/ld.so.conf` 配置的路径
4. 默认路径 `/lib`, `/usr/lib`

如果使用旧 RPATH：
1. DT_RPATH (`/opt/app/lib`) — 此处找不到
2. LD_LIBRARY_PATH
3. ld.so.cache
4. 默认路径

最终结果取决于哪个路径中存在 `libfoo.so`。

</details>

## 衔接

- 上一节 [41.14 总结](./41.14-summary.md)
- 第 42 章将介绍共享库高级主题
