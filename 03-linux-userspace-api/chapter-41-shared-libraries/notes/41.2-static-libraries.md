# 41.2 Static Libraries

> 本章：[TLPI 第 41 章](../README.md)

## 本节讲什么

静态库是目标文件（`.o`）的归档集合（`.a`）。链接时，链接器从库中提取所需目标文件，将其代码直接复制到可执行文件中。运行时不依赖库文件。

## 要点

### 创建静态库流程

```
源文件 → 编译 → 目标文件 → 归档 → 静态库
.c          .o              ar      .a
```

```bash
# 1. 编译为目标文件
gcc -c foo.c bar.c baz.c    # 生成 foo.o bar.o baz.o

# 2. 归档为静态库
ar rcs libmylib.a foo.o bar.o baz.o
#    r = 替换已有成员
#    c = 创建库
#    s = 写入索引（等同于 ranlib）
```

### ar 命令

| 选项 | 功能 |
|------|------|
| `r` | 插入/替换成员 |
| `c` | 静默创建 |
| `s` | 创建索引 |
| `t` | 列出成员 |
| `x` | 提取成员 |
| `d` | 删除成员 |

### 静态库符号表

```
libmylib.a
├── 符号索引（symdef）  ← ar s 创建，加速链接
├── foo.o              ← 目标文件
├── bar.o
└── baz.o
```

链接器通过索引快速查找符号所在的目标文件，只提取引用到的 `.o`。

### 链接静态库

```bash
# 方式1：直接在命令行指定
gcc -o app app.o libmylib.a

# 方式2：使用 -l（需 -L 指定路径）
gcc -o app app.o -L. -lmylib
```

### 静态库 vs 共享库

| 特性 | 静态库 (.a) | 共享库 (.so) |
|------|------------|-------------|
| 代码复制 | 是，每个程序各一份 | 否，内存中一份 |
| 可执行文件大小 | 大 | 小 |
| 运行时依赖 | 无 | 有 |
| 升级 | 需重新链接 | 替换 .so 即可 |
| 内存占用 | 每进程一份代码 | 多进程共享 |
| 启动速度 | 快 | 稍慢（需动态链接） |

## HFT / 嵌入式关联

- HFT 核心策略库可静态链接，消除运行时依赖和启动延迟
- 嵌入式交叉编译常使用静态库减少目标系统依赖
- 安全敏感代码静态链接避免库替换攻击

## 代码示例

```bash
#!/bin/bash
# 完整静态库创建和使用示例

# 1. 源文件
cat > greet.c << 'EOF'
#include <stdio.h>
void greet(const char *name) {
    printf("Hello, %s!\n", name);
}
EOF

cat > farewell.c << 'EOF'
#include <stdio.h>
void farewell(const char *name) {
    printf("Goodbye, %s!\n", name);
}
EOF

# 2. 编译 + 归档
gcc -c greet.c farewell.c
ar rcs libgreet.a greet.o farewell.o

# 3. 查看内容
ar t libgreet.a

# 4. 链接使用
cat > main.c << 'EOF'
void greet(const char *);
void farewell(const char *);
int main() {
    greet("HFT");
    farewell("HFT");
    return 0;
}
EOF
gcc -o app main.c libgreet.a
./app
```

## 衔接

- 上一节 [41.1 目标文件库概述](41.1-object-libraries.md)
- 下一节 [41.3 共享库概述](./41.3-overview-of-shared-libraries.md)

## 代码自测

<details>
<summary>ar 命令的 s 选项为什么重要？</summary>

`s` 选项创建符号索引表（archive symbol table），让链接器快速查找符号所在的目标文件，避免线性扫描所有 `.o`。没有索引时链接器需要逐个扫描目标文件，大型库链接速度显著变慢。现代 `ar` 默认创建索引，但显式 `s` 确保兼容性。`ranlib` 命令也可单独创建索引。

</details>
