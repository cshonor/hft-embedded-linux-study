# 41.4 构建

> 本章：[TLPI 第 41 章 — Fundamentals of Shared Libraries](../README.md)
> - 上一节：[41.1 –41.3 静态 vs 共享](41.1-object-libraries.md)
> - 下一节：[41.5 工具](41.5-useful-tools-for-working-with-shared-lib.md)

---

## 本节讲什么

gcc -c -fPIC foo.c gcc -shared -Wl,-soname,libfoo.so.1 -o libfoo.so.1.0.0 foo.o ln -s libfoo.so.1.0.0 libfoo.so.1

---

## 要点

```bash
gcc -c -fPIC foo.c
gcc -shared -Wl,-soname,libfoo.so.1 -o libfoo.so.1.0.0 foo.o
ln -s libfoo.so.1.0.0 libfoo.so.1
ln -s libfoo.so.1 libfoo.so          # -lfoo
gcc -o app main.c -L. -lfoo -Wl,--enable-new-dtags,-rpath,'$ORIGIN'
```

| 名称 | 例 |
|------|-----|
| Real | `libfoo.so.1.0.0` 实体文件 |
| **Soname** | `libfoo.so.1`（写进 ELF；运行时找它） |
| Linker | `libfoo.so`（`-lfoo`） |

**PIC**：相对寻址，可映射任意基址；建 `.so` 必备 `-fPIC`。

Demo 树：[`code/`](../code/)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 构建 在嵌入式系统的应用场景 |
| HFT | 构建 对低延迟和性能的影响 |

## 代码示例

```c
#include <stdio.h>
/* 共享库优点:
 * 1. 多进程共享代码 (节省内存)
 * 2. 升级库不需要重新编译程序
 * 3. 模块化设计
 * 缺点:
 * 1. 启动时需要链接 (稍慢)
 * 2. 版本兼容性问题
 */
int main(void) {
    printf("共享库: .so 文件, 运行时动态加载\n");
    printf("查看依赖: ldd <binary>\n");
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[41.1 –41.3 静态 vs 共享](41.1-object-libraries.md)
- 下一节：[41.5 工具](41.5-useful-tools-for-working-with-shared-lib.md)
- 前置：**前置**：[Ch40 登录记账](../../chapter-40-login-accounting/notes)  
- 后置：**后置**：[Ch42 共享库高级 / dlopen](../../chapter-42-shared-libraries-advanced/notes)
