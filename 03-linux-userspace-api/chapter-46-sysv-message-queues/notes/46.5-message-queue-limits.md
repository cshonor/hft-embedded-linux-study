# 46.5 Message Queue Limits

> 本章：[TLPI 第 46 章](../README.md)

## 本节讲什么

System V 消息队列的系统级限制：队列大小、消息大小、总消息数等。这些限制影响队列容量和性能。

## 要点

### 系统限制参数

| 参数 | 文件 | 典型值 | 说明 |
|------|------|--------|------|
| `MSGMAX` | `/proc/sys/kernel/msgmax` | 8192 | 单条消息最大字节 |
| `MSGMNB` | `/proc/sys/kernel/msgmnb` | 16384 | 队列最大字节 |
| `MSGMNI` | `/proc/sys/kernel/msgmni` | 32000 | 系统最大队列数 |

### 队列级 vs 系统级限制

| 级别 | 限制 | 调整方式 |
|------|------|---------|
| 队列级 | `msg_qbytes` (默认=MSGMNB) | `msgctl(IPC_SET)` |
| 系统级 | MSGMAX/MSGMNB/MSGMNI | `/proc/sys/kernel/msg*` |
| 硬编码 | 内核编译时上限 | 重新编译内核 |

### 调整限制

```bash
# 查看当前限制
cat /proc/sys/kernel/msgmax   # 单条消息最大字节
cat /proc/sys/kernel/msgmnb   # 队列最大字节
cat /proc/sys/kernel/msgmni   # 最大队列数

# 临时调整（重启失效）
echo 65536 > /proc/sys/kernel/msgmax
echo 65536 > /proc/sys/kernel/msgmnb

# 永久调整
echo "kernel.msgmax=65536" >> /etc/sysctl.conf
echo "kernel.msgmnb=65536" >> /etc/sysctl.conf
sysctl -p
```

### 队列级限制调整

```c
struct msqid_ds ds;
msgctl(msqid, IPC_STAT, &ds);
ds.msg_qbytes = 65536;  /* 增大队列容量 */
msgctl(msqid, IPC_SET, &ds);
```

### 限制影响

| 限制 | 满时行为 |
|------|---------|
| `msg_qbytes` | `msgsnd` 阻塞或 `EAGAIN` |
| `MSGMAX` | `msgsnd` 返回 `EINVAL` |
| `MSGMNI` | `msgget` 返回 `ENOSPC` |

### 常见错误

| errno | 原因 |
|-------|------|
| `EAGAIN` | 队列满且 `IPC_NOWAIT` |
| `EINVAL` | 消息超 `MSGMAX` 或 mtype<0 |
| `ENOSPC` | 系统队列数超限 |

## HFT / 嵌入式关联

- HFT 行情消息可能超默认 MSGMAX，需调大
- 嵌入式系统资源有限，控制队列数和大小
- 监控 `msg_qbytes` 使用率，避免队列满阻塞

## 代码示例

```bash
#!/bin/bash
# HFT 系统消息队列配置

# 查看当前限制
echo "=== Current Limits ==="
echo "MSGMAX (max message):   $(cat /proc/sys/kernel/msgmax)"
echo "MSGMNB (queue size):    $(cat /proc/sys/kernel/msgmnb)"
echo "MSGMNI (max queues):    $(cat /proc/sys/kernel/msgmni)"

# HFT 推荐配置
echo "kernel.msgmax=65536" >> /etc/sysctl.conf
echo "kernel.msgmnb=1048576" >> /etc/sysctl.conf
echo "kernel.msgmni=65535" >> /etc/sysctl.conf
sysctl -p

# 验证
echo "=== Updated Limits ==="
echo "MSGMAX: $(cat /proc/sys/kernel/msgmax)"
echo "MSGMNB: $(cat /proc/sys/kernel/msgmnb)"
echo "MSGMNI: $(cat /proc/sys/kernel/msgmni)"
```

## 衔接

- 上一节 [46.4 消息队列数据结构](./46.4-message-queue-associated-data-structure.md)
- 下一节 [46.6 消息队列操作](46.3-message-queue-control-operations.md)

## 代码自测

<details>
<summary>msgsnd 返回 EAGAIN 和 EINVAL 分别表示什么？</summary>

- **EAGAIN**：队列已满（`msg_cbytes` + 新消息大小 > `msg_qbytes`），且设置了 `IPC_NOWAIT` 标志。如果不设置 `IPC_NOWAIT`，会阻塞直到队列有空间。
- **EINVAL**：消息大小超过系统限制 `MSGMAX`，或 `mtype` < 0，或 `msqid` 无效。这是参数错误，不可通过等待解决。

</details>
