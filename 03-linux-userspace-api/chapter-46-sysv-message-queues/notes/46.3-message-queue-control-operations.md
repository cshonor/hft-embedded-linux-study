# 46.3 –46.5 `msqid_ds` · 限额 · 运维

> 本章：[TLPI 第 46 章 — System V Message Queues](../README.md)
> - 上一节：[46.2 API](46.2-exchanging-messages.md)
> - 下一节：[46.6 –46.7 模型与缺陷](46.6-displaying-all-message-queues-on-the-sys.md)

---

## 本节讲什么

关注：`msg_qnum`、`msg_qbytes`（可 `IPC_SET`）、`msg_cbytes`、权限与时间戳。 超限：`msgget`→`ENOSPC` 等。 `ipcs -q` · `ipcrm -q id` / `-M key`。

---

## 要点

关注：`msg_qnum`、`msg_qbytes`（可 `IPC_SET`）、`msg_cbytes`、权限与时间戳。

| `/proc/sys/kernel/` | 含义（量级示意） |
|---------------------|------------------|
| `msgmni` | 队列个数上限 |
| `msgmax` | 单条最大字节 |
| `msgmnb` | 单队列默认总字节 |

超限：`msgget`→`ENOSPC` 等。  
`ipcs -q` · `ipcrm -q id` / `-M key`。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | msg_qbytes 可 IPC_SET 调队列容量；超限 msgget 报 ENOSPC |
| HFT | 队列容量超限丢单，启动前调大 msg_qbytes；ipcs -q 监控积压 |

## 代码示例

```c
#include <stdio.h>
#include <sys/msg.h>
#include <time.h>
/* struct msqid_ds: 消息队列属性 */
int main(void) {
    int msqid = msgget(IPC_PRIVATE, IPC_CREAT | 0666);

    /* 发送几条消息 */
    struct { long mtype; char mtext[32]; } msg = {1, "test"};
    msgsnd(msqid, &msg, 5, 0);
    msgsnd(msqid, &msg, 5, 0);

    /* IPC_STAT: 获取队列属性 */
    struct msqid_ds ds;
    msgctl(msqid, IPC_STAT, &ds);

    printf("msqid_ds fields:\n");
    printf("  msg_perm: uid=%u gid=%u mode=%04o\n",
           ds.msg_perm.uid, ds.msg_perm.gid, ds.msg_perm.mode & 0777);
    printf("  msg_stime: %s", ctime(&ds.msg_stime));  /* 最后发送时间 */
    printf("  msg_rtime: %s", ctime(&ds.msg_rtime));  /* 最后接收时间 */
    printf("  msg_ctime: %s", ctime(&ds.msg_ctime));  /* 最后修改时间 */
    printf("  msg_cbytes: %lu (current bytes)\n", (unsigned long)ds.msg_cbytes);
    printf("  msg_qnum: %lu (messages in queue)\n", (unsigned long)ds.msg_qnum);
    printf("  msg_qbytes: %lu (max bytes)\n", (unsigned long)ds.msg_qbytes);
    printf("  msg_lspid: %d (last sender)\n", ds.msg_lspid);
    printf("  msg_lrpid: %d (last receiver)\n", ds.msg_lrpid);

    /* IPC_SET: 修改队列属性 (如最大字节数) */
    ds.msg_qbytes = 2048;
    msgctl(msqid, IPC_SET, &ds);
    printf("\nAfter IPC_SET: msg_qbytes=%lu\n",
           (unsigned long)ds.msg_qbytes);

    msgctl(msqid, IPC_RMID, NULL);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[46.2 API](46.2-exchanging-messages.md)
- 下一节：[46.6 –46.7 模型与缺陷](46.6-displaying-all-message-queues-on-the-sys.md)
- 前置：**前置**：[Ch45 SysV IPC 导论](../../chapter-45-sysv-ipc-intro/notes)  
- 后置：**后置**：[Ch47 SysV 信号量](../../chapter-47-sysv-semaphores/notes)
