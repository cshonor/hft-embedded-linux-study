# 31.5 易错 / 原理

> 本章：[TLPI 第 31 章 — Thread Safety and Per-Thread Storage](./README.md)
> - 上一节：[31.4 静态 TLS：`__thread` / `_Thread_local`](./31.4-thread-threadlocal.md)
> - 下一节：[下一章](../chapter-{int(ch_num)+1:02d}-*/notes.md)

---

## 本节讲什么

1. `key_delete` 不跑析构 2. destructor 内勿再 `setspecific` 同 key（循环析构风险） 3. `errno`：每线程一份（glibc 用 TLS），不是普通全局 int

---

## 要点

1. `key_delete` 不跑析构  
2. destructor 内勿再 `setspecific` 同 key（循环析构风险）  
3. `errno`：每线程一份（glibc 用 TLS），不是普通全局 int  

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[31.4 静态 TLS：`__thread` / `_Thread_local`](./31.4-thread-threadlocal.md)
- 前置：**前置**：[Ch30 同步](../chapter-30-thread-synchronization/notes.md)  
- 后置：**后置**：[Ch32 线程取消](../chapter-32-thread-cancellation/notes.md)
