# 31.2 一次性初始化：`pthread_once`

> 本章：[TLPI 第 31 章 — Thread Safety and Per-Thread Storage](../README.md)
> - 上一节：[31.1 线程安全与可重入](31.1-thread-safety-and-reentrancy-revisited.md)
> - 下一节：[31.3 TSD：`pthread_key_*`](31.3-thread-specific-data.md)

> 源码核验：Linux v6.6 · 无专属内核路径 · glibc 侧：`pthread_once` 用原子 CAS + futex 实现（与 Ch30 mutex 同一套底座）

---

## 本节讲什么

1. **"初始化恰好一次"为什么是个难题**——`if (!inited) { init(); inited = 1; }` 在多线程下的检查-执行-标记竞态，以及"用锁包住检查"的代价（每次路过都要付锁钱）。
2. **`pthread_once` 的语义与用法**——哪个线程执行 `init_routine`（不定！）、其他线程在干什么（阻塞等待）、为什么 `once_control` 必须零初始化且不能是 malloc 出来的。
3. **它在 TSD 范式里的位置**——`pthread_once` + `pthread_key_create` 是改造老接口的标准开场（31.3 的第一块积木）。

---

## 一、"恰好一次"是个难题

### 1.1 朴素写法的竞态

```c
static int inited = 0;

void ensure_init(void) {
    if (!inited) {          /* ① 检查：两个线程同时看到 0 */
        do_init();          /* ② 执行：两个线程各跑一遍 init！ */
        inited = 1;         /* ③ 标记：来晚了 */
    }
}
```

`do_init` 常常有副作用：打开同一个文件两次、注册同一个 atexit 处理器两次、把计数器重置两次。①②③ 的窗口在多线程下必然被踩。

### 1.2 用锁修复的代价

```c
static pthread_mutex_t m = PTHREAD_MUTEX_INITIALIZER;
void ensure_init(void) {
    pthread_mutex_lock(&m);
    if (!inited) { do_init(); inited = 1; }
    pthread_mutex_unlock(&m);
}
```

正确，但**每一次调用**（包括初始化早已完成的亿万次）都要走一遍锁的快路径（~7 ns 无竞争，争抢时 70 ns+）。对高频接口这是纯浪费——初始化只发生一次，凭什么每次都查？

`pthread_once` 就是把"锁 + 检查 + 执行 + 标记"整个封装成专用原语：**已完成后的再次调用只剩一条原子读**（几乎零成本）。

---

## 二、API 与语义

```c
#include <pthread.h>

pthread_once_t once_control = PTHREAD_ONCE_INIT;      /* 必须静态/零初始化 */

int pthread_once(pthread_once_t *once_control,
                 void (*init_routine)(void));
```

四条语义（每条都有坑点）：

| # | 语义 | 坑点 |
|---|------|------|
| 1 | 多线程并发调用时，`init_routine` **恰好执行一次** | 保证的是"一次"，不是"谁执行"——**哪个线程跑 init 不确定**（通常是第一个到的）|
| 2 | 其他线程**阻塞**直到 init 完成 | init 里不能做"等所有线程都到齐"的事 → 自死锁 |
| 3 | `once_control` 必须 `PTHREAD_ONCE_INIT`（全零）初始化 | **不能 malloc**：内存未必零填充；栈上/静态区天然可行 |
| 4 | 返回值恒为 0（不设 errno）| init 本身失败如何反馈？——init_routine 无参无返回，失败信息要走全局（如 `static int init_err;`，init 里写，调用方事后查）|

关于第 2 条的深坑：`init_routine` 执行期间，其他调用 `pthread_once` 的线程睡在 futex 上（Ch30 的机制）。如果 init 内部又需要"另一个还没到达 pthread_once 的线程"的结果——死锁。init 必须自包含。

### 2.1 glibc 实现一瞥（呼应 Ch30）

glibc 的 `pthread_once`（简化）：

```c
/* 状态：0=未开始 1=进行中 2=完成 */
int pthread_once(pthread_once_t *once, void (*fn)(void)) {
    if (atomic_load_acquire(&once->__data) == 2)   /* 快路径：一条原子读 */
        return 0;
    /* 慢路径：CAS 0→1 成功者执行 fn；失败者 futex 等待 */
    ...futex(FUTEX_WAIT / FUTEX_WAKE)...           /* 与 mutex 同一套底座 */
}
```

正是 30.1 §3.1 那套"快路径用户态原子 + 慢路径 futex"哲学的又一个应用：**一次性事件的常见情形是"早已完成"**，所以快路径只做一次原子读；只有初始化窗口期的竞争者才付出睡眠成本。

### 2.2 与 C11 `call_once` 的关系

C11 线程库（`<threads.h>`）提供了语义等价的 `call_once(flag, fn)`（`once_flag` = 全零初始化）。glibc 两者都提供，实现共用。新代码可以按项目风格二选一，但**别混用两种 flag 类型**（`pthread_once_t` 和 `once_flag` 不是同一种东西）。

### 2.3 更朴素的替代：静态初始化

```c
static GType g_x = COMPILE_TIME_VALUE;    /* 编译期完成，天生无竞态 */
```

能用编译期常量初始化的全局量，根本不需要 `pthread_once`。`PTHREAD_ONCE_INIT` 本身就是全零——glibc 大量 `*_initializer` 宏（mutex/cond/key 创建的第一步）都在利用"零初始化的静态存储天然免竞态"这一 C 语言保证。**先问能不能静态初始化，再考虑 once**。

---

## 三、TSD 范式中的位置

31.3 的标准开场（先睹）：

```c
static pthread_key_t strerror_key;
static pthread_once_t once = PTHREAD_ONCE_INIT;

static void destructor(void *buf) { free(buf); }
static void create_key(void) {                 /* 只跑一次 */
    pthread_key_create(&strerror_key, destructor);
}

char *my_strerror(int errnum) {
    pthread_once(&once, create_key);           /* ⭐ 每线程都调用，key 只建一次 */
    char *buf = pthread_getspecific(strerror_key);
    if (buf == NULL) {                         /* 本线程第一次：分配 */
        buf = malloc(256);
        pthread_setspecific(strerror_key, buf);
    }
    ...
}
```

为什么 key 的创建必须恰好一次：`pthread_key_create` 每次返回**新的** key（编号递增）。两个线程各自 create，拿到两个不同的 key，各自的 `setspecific` 写进不同的格子——"每线程一份"变成"每线程两份"，析构、读取全部错位。**`pthread_once` 在这里的职责是保证全局的 key 唯一**。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| HFT | 首选静态初始化/C++ 静态对象（启动期单线程完成一切初始化是 HFT 的常规架构——开盘前 init，盘中零初始化路径）；`pthread_once` 用于无法静态化的惰性初始化，其快路径一条原子读对热路径无害 |
| HFT · 失败语义 | init_routine 无返回值 → 失败状态要落在静态变量里；热路径检查"初始化失败"应与检查"未初始化"合并为同一次原子读（状态机：0 未跑 / 1 进行 / 2 成功 / 3 失败）|
| 嵌入式 | RTOS 上 `pthread_once` 看线程库支持；裸机不存在此问题（main 单线程顺序初始化）；**静态初始化在嵌入式是默认美德**——少一条运行时分支，少一个竞态面 |
| 嵌入式 · init 里的耗时操作 | once 阻塞语义意味着 init 期间所有调用者排队——把设备探测、固件加载这类百毫秒级 init 放进 once，会让并发首调用的线程集体停顿，注意超时预算 |

---

## 代码示例

`once_demo.c`（gcc -O2 -Wall -Wextra 零告警编译通过）：

```c
/* once_demo.c — Ch31.2：init 只跑一次；朴素 if-init 的双重执行
 * 编译：gcc -O2 -Wall -Wextra -o once_demo once_demo.c -pthread
 */
#include <stdio.h>
#include <pthread.h>
#include <unistd.h>

/* ---------- 演示 1：朴素 if-init 双重执行 ---------- */
static int naive_inited = 0;
static int naive_count = 0;
static pthread_barrier_t bar;

static void naive_init(void){ naive_count++; }         /* 统计执行次数 */

static void *naive_worker(void *a){(void)a;
    pthread_barrier_wait(&bar);            /* 8 线程同时冲进来 */
    if (!naive_inited) {
        usleep(10000);                     /* 拉大检查与标记的窗口 */
        naive_init();                      /* 窗口内大家都能进来 */
        naive_inited = 1;
    }
    return NULL;}

static void demo_naive(void){
    pthread_t t[8];
    naive_inited = 0; naive_count = 0;
    pthread_barrier_init(&bar, NULL, 8);
    for (int i = 0; i < 8; i++) pthread_create(&t[i], NULL, naive_worker, NULL);
    for (int i = 0; i < 8; i++) pthread_join(t[i], NULL);
    pthread_barrier_destroy(&bar);
    printf("[demo1] 朴素 if-init：init 执行了 %d 次（期望 1 次）\n", naive_count);
}

/* ---------- 演示 2：pthread_once 恰好一次 ---------- */
static pthread_once_t once = PTHREAD_ONCE_INIT;
static int once_count = 0;
static pthread_t once_runner;

static void real_init(void){
    once_runner = pthread_self();          /* 记下是谁在执行 */
    once_count++;
    usleep(10000);                         /* 制造竞争窗口 */
}

static void *once_worker(void *a){(void)a;
    pthread_barrier_wait(&bar);
    pthread_once(&once, real_init);        /* 其余线程在此阻塞等待 */
    return NULL;}

static void demo_once(void){
    pthread_t t[8];
    pthread_barrier_init(&bar, NULL, 8);
    for (int i = 0; i < 8; i++) pthread_create(&t[i], NULL, once_worker, NULL);
    for (int i = 0; i < 8; i++) pthread_join(t[i], NULL);
    pthread_barrier_destroy(&bar);
    printf("[demo2] pthread_once：init 执行了 %d 次（期望 1），"
           "执行者是 worker 线程(%d)\n",
           once_count,
           !pthread_equal(once_runner, pthread_self()) ? 1 : 0);
}

int main(void){
    demo_naive();
    demo_once();
    return 0;
}
```

实测输出（WSL2 / 12 核）：

```text
[demo1] 朴素 if-init：init 执行了 8 次（期望 1 次）
[demo2] pthread_once：init 执行了 1 次（期望 1），执行者是 worker 线程(1)
```

解读：demo1 的 8 个线程在 10 ms 窗口内全部通过了 `!inited` 检查，init 执行了 8 次；demo2 换成 `pthread_once` 后恰好 1 次，且执行者是**抢先到的 worker 线程**而非主线程——印证语义表第 1 条："谁执行不确定，恰好一次才是不变量"。

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[31.1 线程安全与可重入](31.1-thread-safety-and-reentrancy-revisited.md)
- 下一节：[31.3 TSD](31.3-thread-specific-data.md) —— once + key_create 的完整改造实战
- futex 快路径/慢路径哲学：[30.1 互斥量](../../chapter-30-thread-synchronization/notes/30.1-protecting-accesses-to-shared-variables-.md)
