# 9.4 Saved-ID 升降权流程

> 本章：[TLPI 第 09 章 — Process Credentials](../README.md)
> - 上一节：[9.2 –9.3 Set-User-ID / Set-Group-ID](9.3-set-user-id.md)
> - 下一节：[9.5 获取凭证 API](9.5-fs-uid-gid.md)

---

## 本节讲什么

典型：普通用户跑 root 属主 setuid 程序 初始:  RUID=1000  EUID=0     SUID=0 seteuid(1000):

---

## 要点

典型：普通用户跑 root 属主 setuid 程序

```
初始:  RUID=1000  EUID=0     SUID=0
seteuid(1000):
       RUID=1000  EUID=1000  SUID=0     ← 临时降权
seteuid(0):
       RUID=1000  EUID=0     SUID=0     ← 靠 Saved-ID 再提权
```

若无 Saved-ID，降权后**无法**再回到 0。

### 永久丢弃特权（对外 SUID 程序推荐）

业务结束后不要保留可恢复 root：

```c
setuid(getuid());   /* 特权进程：R/E/S 全部钉成 RUID，无法再 seteuid(0) */
```

临时降权用 `seteuid(getuid())`（Saved 仍为 0，可再提权）——仅内部可信阶段使用。

Demo：[`code/seteuid_drop_restore.c`](../code/seteuid_drop_restore.c) · [`code/setuid_permanent_drop.c`](../code/setuid_permanent_drop.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | Saved-ID 机制让 setuid 程序可在「普通身份↔root」间切换：临时降权用 seteuid（可恢复），业务结束用 setuid 永久丢弃特权，是嵌入式服务安全范式 |
| HFT | 交易进程启动阶段需特权（绑端口/锁内存），进主循环前 setuid(getuid()) 永久丢弃 root，杜绝运行时被利用提权；临时降权用 seteuid 保留可恢复性 |

## 代码示例

```c
#include <stdio.h>
#include <unistd.h>
/* saved-set uid: 保存 effective uid 以便恢复 */
int main(void) {
    uid_t ruid, euid, suid;
    getresuid(&ruid, &euid, &suid);
    printf("real=%u effective=%u saved=%u\n",
           (unsigned)ruid, (unsigned)euid, (unsigned)suid);

    /* seteuid: 临时切换 effective uid */
    if (euid == 0) {
        seteuid(ruid);  /* 降为普通用户 */
        printf("after seteuid(ruid): euid=%u\n", (unsigned)geteuid());
        seteuid(0);     /* 恢复 root (saved-set 允许) */
        printf("after seteuid(0): euid=%u\n", (unsigned)geteuid());
    }
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[9.2 –9.3 Set-User-ID / Set-Group-ID](9.3-set-user-id.md)
- 下一节：[9.5 获取凭证 API](9.5-fs-uid-gid.md)
- 前置：**前置**：[Ch8 Users and Groups](../../chapter-08-users-and-groups/notes) · [Ch24 fork](../../chapter-24-process-creation/notes) · [Ch27 exec](../../chapter-27-program-execution/notes)  
- 后置：**后置**：[Ch10 Times and Dates](../../chapter-10-time/notes) · [Ch38 特权程序](../../chapter-38-secure-privileged/notes) · [Ch39 Capabilities](../../chapter-39-capabilities/notes)
