# 9.5 获取凭证 API

> 本章：[TLPI 第 09 章 — Process Credentials](./README.md)
> - 上一节：[9.4 Saved-ID 升降权流程](./9.4-saved-id.md)
> - 下一节：[9.6 –9.7 修改凭证（重难点）](./9.6-credentials.md)

---

## 本节讲什么

#include <unistd.h> #include <sys/types.h> uid_t getuid(void);      /* RUID */

---

## 要点

```c
#include <unistd.h>
#include <sys/types.h>

uid_t getuid(void);      /* RUID */
uid_t geteuid(void);     /* EUID */
gid_t getgid(void);      /* RGID */
gid_t getegid(void);     /* EGID */

#define _GNU_SOURCE
#include <unistd.h>
int getresuid(uid_t *ruid, uid_t *euid, uid_t *suid);
int getresgid(gid_t *rgid, gid_t *egid, gid_t *sgid);

int getgroups(int size, gid_t list[]);  /* size==0 时返回组个数 */
```

FUID 无标准 getter；现代可视为 = EUID（除非调用过 `setfsuid`）。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 获取凭证 API 在嵌入式系统的应用场景 |
| HFT | 获取凭证 API 对低延迟和性能的影响 |

## 代码示例

```c
#include <stdio.h>
#include <unistd.h>
/* setuid/seteuid/setreuid/setresuid 的区别 */
int main(void) {
    uid_t ruid = getuid();
    uid_t euid = geteuid();
    printf("before: real=%u eff=%u\n", (unsigned)ruid, (unsigned)euid);

    /* seteuid: 只改 effective (最常用, 最安全) */
    /* setuid: root 下改全部三个; 普通用户只改 effective */
    /* setreuid: 交换/设置 real 和 effective */
    /* setresuid: 分别设置三个 */

    /* 普通用户演示: seteuid 到自己的 real uid */
    seteuid(ruid);
    printf("after seteuid(ruid): eff=%u\n", (unsigned)geteuid());
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[9.4 Saved-ID 升降权流程](./9.4-saved-id.md)
- 下一节：[9.6 –9.7 修改凭证（重难点）](./9.6-credentials.md)
- 前置：**前置**：[Ch8 Users and Groups](../chapter-08-users-and-groups/notes.md) · [Ch24 fork](../chapter-24-process-creation/notes.md) · [Ch27 exec](../chapter-27-program-execution/notes.md)  
- 后置：**后置**：[Ch10 Times and Dates](../chapter-10-time/notes.md) · [Ch38 特权程序](../chapter-38-secure-privileged/notes.md) · [Ch39 Capabilities](../chapter-39-capabilities/notes.md)
