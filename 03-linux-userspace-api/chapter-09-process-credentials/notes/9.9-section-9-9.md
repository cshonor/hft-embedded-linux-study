# 9.9 易混考点

> 本章：[TLPI 第 09 章 — Process Credentials](./README.md)
> - 上一节：[9.8 fork / exec 对凭证（必考）](./9.8-fork-exec.md)
> - 下一节：[下一章](../chapter-{int(ch_num)+1:02d}-*/notes.md)

---

## 本节讲什么

1. **特权进程 ≠ RUID=0** 判据：**EUID==0**。普通用户跑 setuid-root：`RUID≠0, EUID=0` → 仍是特权进程。 2. **`setuid` vs `seteuid`**

---

## 要点

1. **特权进程 ≠ RUID=0**  
   判据：**EUID==0**。普通用户跑 setuid-root：`RUID≠0, EUID=0` → 仍是特权进程。

2. **`setuid` vs `seteuid`**  
   特权下 `setuid(uid)` 常把 R+E+S 一并钉死；`seteuid` 只动有效 ID（可配合 Saved 来回切）。

3. **Saved-ID**  
   支持 setuid 程序 **多次** 降权 / 提权；对外服务结束应用 `setuid(getuid())` 永久丢弃。

4. **环境变量**  
   SUID 程序须警惕 `LD_PRELOAD` / `LD_LIBRARY_PATH` 等劫持；见 Ch38。

5. **FUID**  
   现代默认=EUID；POSIX 无；别写进可移植逻辑。

---

---

## 与 C 语言学习的关系

权限与凭证管理是 Unix 系统编程的重要主题。C 语言中 `uid_t`/`gid_t` 是整数类型，理解 setuid 程序的 real/effective/saved UID 切换需要扎实的 C 语言基础。这部分内容对应 K&R 中文件权限的基础概念，并在 HFT 场景中涉及特权端口的绑定和权限降级。
---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 易混考点 在嵌入式系统的应用场景 |
| HFT | 易混考点 对低延迟和性能的影响 |

---

## 常见陷阱

1. **TOCTOU 竞态**

```c
if (access("f.txt", R_OK) == 0) {
    /* 攻击者在此期间替换文件 */
    fd = open("f.txt", O_RDONLY);
}
```

access() 和 open() 之间文件可能被替换（TOCTOU）。应直接 open 并检查错误，或用 fstat 验证已打开的 fd。

2. **setuid 程序未降权**

```c
/* setuid root 程序 */
fd = open("/etc/shadow", O_RDONLY);
/* ... 处理 ... */
/* 忘记降权就执行用户代码 → 提权! */
```

setuid 程序应在完成特权操作后立即降权（seteuid(getuid())），遵循最小权限原则。

3. **混淆 real UID 和 effective UID**

```c
/* real UID: 启动进程的用户 */
/* effective UID: 当前有效权限 */
/* setuid 程序中两者不同! */
```

real UID 是登录用户，effective UID 决定当前权限。setuid 程序中 effective UID 变为文件属主。

---

## 自测题

**题目 1（选择）：** setuid 程序执行时，哪个 UID 会改变？
A. real UID
B. effective UID
C. saved set-user-ID
D. 所有 UID

<details>
<summary>参考答案</summary>

B。setuid 程序执行时 effective UID 变为文件属主的 UID。real UID 不变。saved set-user-ID 也被设为 effective UID。

</details>

**题目 2（判断）：** `access()` 和 `open()` 组合使用是安全的权限检查方式。

<details>
<summary>参考答案</summary>

错误。access() 基于 real UID 检查，open() 基于 effective UID。而且两者之间有 TOCTOU 竞态。应直接 open 并检查。

</details>

**题目 3（踩坑）：** 以下 setuid root 程序有什么安全隐患？
```c
int main() {
    system("ls /root");
}
```

<details>
<summary>参考答案</summary>

system() 调用 shell，shell 会解析 PATH 环境变量。攻击者可以修改 PATH 让恶意程序替代 ls。setuid 程序应直接用 execve 指定完整路径。

</details>

**题目 4（应用）：** 写代码：setuid 程序先打开特权文件，然后降权到普通用户。

<details>
<summary>参考答案</summary>

```c
int fd = open("/etc/shadow", O_RDONLY);
if (fd < 0) { perror("open"); exit(1); }
/* 立即降权 */
seteuid(getuid());
/* 现在以普通用户身份运行 */
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[9.8 fork / exec 对凭证（必考）](./9.8-fork-exec.md)
- 前置：**前置**：[Ch8 Users and Groups](../chapter-08-users-and-groups/notes.md) · [Ch24 fork](../chapter-24-process-creation/notes.md) · [Ch27 exec](../chapter-27-program-execution/notes.md)  
- 后置：**后置**：[Ch10 Times and Dates](../chapter-10-time/notes.md) · [Ch38 特权程序](../chapter-38-secure-privileged/notes.md) · [Ch39 Capabilities](../chapter-39-capabilities/notes.md)

---

## man 手册参考

- `man 2 setuid`
