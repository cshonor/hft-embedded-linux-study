# 9.8 fork / exec 对凭证（必考）

> 本章：[TLPI 第 09 章 — Process Credentials](./README.md)
> - 上一节：[9.6 –9.7 修改凭证（重难点）](./9.6-credentials.md)
> - 下一节：[9.9 易混考点](./9.9-section-9-9.md)

---

## 本节讲什么

要点： - **只有 exec 加载带 SUID 位的程序**才会把 euid 换成文件属主；**fork 永不改 UID**。 - SUID 进程再 fork：子**继承 euid 特权**；子若再 exec **普通**程序，euid **不会自动掉回** ruid。

---

## 要点

| 行为 | ruid | euid | saved-set UID |
|------|------|------|---------------|
| **fork()** | 继承 | 继承 | 继承 |
| **exec 普通程序**（无 SUID 位） | 不变 | **不变** | ← 拷贝当前 euid |
| **exec SUID 程序** | 不变 | = 文件属主 | ← 拷贝**新** euid（exec 前旧 euid 已反映在切换逻辑中；见 man/书表） |

要点：

- **只有 exec 加载带 SUID 位的程序**才会把 euid 换成文件属主；**fork 永不改 UID**。  
- SUID 进程再 fork：子**继承 euid 特权**；子若再 exec **普通**程序，euid **不会自动掉回** ruid。  
- Linux 上 shebang 脚本的 SUID/SGID **无效**（防脚本提权）。  
- 补充组：`getgroups`/`setgroups`（仅 root 可改）；exec/SUID **不改**附属组列表。

文件访问判定（看 euid / egid+附属组，Linux 文件侧还有 fsuid≈euid）：属主位 → 组位 → other。

> 写安全代码时用 `getresuid` **打印验证**；边角见 `man 2 execve` / `man 7 credentials`。

---

---

## 与 C 语言学习的关系

`fork()` 是 C 语言系统编程的核心。C 语言没有内置的并发原语——`fork` 是 Unix 创建进程的唯一方式。理解 `fork` 的返回值（父返回子 PID，子返回 0）需要扎实的 C 语言指针和返回值概念。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | fork / exec 对凭证（必考） 在嵌入式系统的应用场景 |
| HFT | fork / exec 对凭证（必考） 对低延迟和性能的影响 |

---

## 常见陷阱

1. **fd 泄漏到 exec**

```c
int fd = open(...);
execvp(prog, argv);  /* fd 仍然打开 */
```

默认情况下 exec 后 fd 仍然打开。设 FD_CLOEXEC 或用 O_CLOEXEC 防止泄漏。

2. **exec 后代码继续执行**

```c
execvp(prog, argv);
printf("这里会执行吗?");  /* 会! exec 失败时 */
```

exec 成功时不返回，当前进程映像被替换。只有 exec 失败时才返回 -1。必须检查返回值。

3. **PATH 搜索差异**

```c
execlp("ls", "ls", NULL);   /* 搜索 PATH */
execl("/bin/ls", "ls", NULL);  /* 不搜索 PATH */
```

execlp/execvp 搜索 PATH，execl/execv 不搜索。用错可能找不到程序。

---

## 自测题

**题目 1（选择）：** `exec` 成功后会发生什么？
A. 创建新进程
B. 替换当前进程映像
C. 返回旧进程
D. 什么也不发生

<details>
<summary>参考答案</summary>

B。exec 用新程序替换当前进程映像。PID 不变，代码段/数据段/堆栈被替换。成功时不返回。

</details>

**题目 2（判断）：** `exec` 失败时返回 -1，原程序继续执行。

<details>
<summary>参考答案</summary>

正确。exec 失败时返回 -1，当前进程映像不变，代码继续执行。成功时不返回。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
execvp("ls", args);
perror("exec failed");
```

<details>
<summary>参考答案</summary>

没有检查 execvp 返回值。应该：
`if (execvp("ls", args) < 0) { perror("exec"); exit(1); }`
perror 本身没问题，但应该配合 exit 确保失败后退出。

</details>

**题目 4（应用）：** 写代码：fork + exec 执行 `ls -l`，父进程等待子进程结束。

<details>
<summary>参考答案</summary>

```c
pid_t pid = fork();
if (pid == 0) {
    execlp("ls", "ls", "-l", NULL);
    perror("exec"); exit(1);
}
wait(NULL);
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[9.6 –9.7 修改凭证（重难点）](./9.6-credentials.md)
- 下一节：[9.9 易混考点](./9.9-section-9-9.md)
- 前置：**前置**：[Ch8 Users and Groups](../chapter-08-users-and-groups/notes.md) · [Ch24 fork](../chapter-24-process-creation/notes.md) · [Ch27 exec](../chapter-27-program-execution/notes.md)  
- 后置：**后置**：[Ch10 Times and Dates](../chapter-10-time/notes.md) · [Ch38 特权程序](../chapter-38-secure-privileged/notes.md) · [Ch39 Capabilities](../chapter-39-capabilities/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
