# 37.4 `SIGHUP` 热重载

> 本章：[TLPI 第 37 章 — Daemons](../README.md)
> - 上一节：[37.3 编写规范](37.3-guidelines-for-writing-daemons.md)
> - 下一节：[37.5 syslog](37.5-logging-messages-and-errors-using-syslog.md)

---

## 本节讲什么

`kill -HUP <pid>`。handler **只设** `volatile sig_atomic_t`；主循环里重载。 Demo：[`code/daemon_sighup.c`](../code/daemon_sighup.c)（演示标志位范式；可配合 becomeDaemon） ---

---

## 要点

`kill -HUP <pid>`。handler **只设** `volatile sig_atomic_t`；主循环里重载。

Demo：[`code/daemon_sighup.c`](../code/daemon_sighup.c)（演示标志位范式；可配合 becomeDaemon）

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `kill -HUP` 触发，handler 只设 `volatile sig_atomic_t` 标志，主循环里重载 |
| HFT | 配置热重载不重启进程，避免交易中断；handler 里只置标志不做重活（async-safe） |

## 代码示例

```c
#include <signal.h>
#include <unistd.h>
#include <stdio.h>
/* 守护进程无控制终端 → SIGHUP 不会因终端断开触发
 * 惯例: 用 SIGHUP 做配置重载
 */
int reload = 0;
void handler(int sig) { reload = 1; }
int main(void) {
    signal(SIGHUP, handler);
    printf("守护进程: kill -SIGHUP %d → 重载配置\n", getpid());
    for (;;) { if (reload) { printf("重载配置\n"); reload = 0; } sleep(1); }
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[37.3 编写规范](37.3-guidelines-for-writing-daemons.md)
- 下一节：[37.5 syslog](37.5-logging-messages-and-errors-using-syslog.md)
- 前置：**前置**：[Ch34 会话/`setsid`](../../chapter-34-process-groups-sessions/notes) · [Ch36 rlimit](../../chapter-36-process-resources/notes)  
- 后置：**后置**：[Ch38 特权程序安全](../../chapter-38-secure-privileged/notes)
