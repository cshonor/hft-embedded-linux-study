# 37.3 编写规范

> 本章：[TLPI 第 37 章 — Daemons](./README.md)
> - 上一节：[37.2 标准 7 步（及原因）](./37.2-section-37-2.md)
> - 下一节：[37.4 `SIGHUP` 热重载](./37.4-sighup.md)

---

## 本节讲什么

1. **单实例**：`/run/xxx.pid` + `fcntl` 排他锁 2. 防泄漏；启动可 `setrlimit(NOFILE…)` 3. **SIGTERM**：快清理退出（systemd 随后可能 `SIGKILL`）

---

## 要点

1. **单实例**：`/run/xxx.pid` + `fcntl` 排他锁  
2. 防泄漏；启动可 `setrlimit(NOFILE…)`  
3. **SIGTERM**：快清理退出（systemd 随后可能 `SIGKILL`）  
4. **SIGHUP**：重载配置/日志（daemon 无终端，可复用此信号）  
5. 少用缓冲 stdio；优先 syslog  
6. 能降权则降权（Ch38）  

---

---

## 与 C 语言学习的关系

`kill` 发送信号看似简单，但涉及 C 语言的进程权限模型。`kill(pid, 0)` 的探测用法需要理解 C 语言的返回值约定（0 成功，-1 失败）和 errno 机制。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 编写规范 在嵌入式系统的应用场景 |
| HFT | 编写规范 对低延迟和性能的影响 |

---

## 常见陷阱

1. **fd 标志 vs 文件状态标志混淆**

```c
/* 错误：F_SETFL 改不了访问模式 */
fcntl(fd, F_SETFL, O_WRONLY);  /* 无效 */
```

F_SETFL 只能改 O_APPEND/O_NONBLOCK 等，不能改 O_RDONLY/O_WRONLY/O_RDWR。访问模式在 open 时确定。

2. **F_GETFL 解析访问模式**

```c
int flags = fcntl(fd, F_GETFL);
int acc = flags & O_ACCMODE;  /* O_RDONLY/O_WRONLY/O_RDWR */
```

不能直接用 `flags & O_WRONLY` 判断，因为 O_RDONLY 通常是 0。必须用 O_ACCMODE 掩码。

3. **FD_CLOEXEC 竞态**

```c
int fd = open(...);
fcntl(fd, F_SETFD, FD_CLOEXEC);  /* open 和 fcntl 之间有间隙 */
/* 多线程中 exec 可能在此间隙发生 */
```

open + fcntl(F_SETFD) 之间存在竞态窗口。用 O_CLOEXEC 标志一步到位。

---

## 自测题

**题目 1（选择）：** `fcntl(fd, F_SETFL, flags)` 可以改变以下哪个标志？
A. O_RDONLY
B. O_WRONLY
C. O_NONBLOCK
D. O_CREAT

<details>
<summary>参考答案</summary>

C。F_SETFL 只能改 O_APPEND、O_NONBLOCK、O_ASYNC 等状态标志，不能改访问模式（O_RDONLY 等）。

</details>

**题目 2（判断）：** `FD_CLOEXEC` 是文件状态标志，存在打开文件描述中。

<details>
<summary>参考答案</summary>

错误。FD_CLOEXEC 是 fd 标志，存在进程 fd 表中，每个 fd 私有。文件状态标志（如 O_APPEND）才在打开文件描述中。

</details>

**题目 3（踩坑）：** 以下代码试图将文件改为非阻塞，有什么问题？
```c
fcntl(fd, F_SETFL, O_NONBLOCK);
```

<details>
<summary>参考答案</summary>

问题：F_SETFL 会覆盖所有状态标志。应该先 F_GETFL 取回当前标志，再 OR 上 O_NONBLOCK：
`fcntl(fd, F_SETFL, fcntl(fd, F_GETFL) | O_NONBLOCK);`

</details>

**题目 4（应用）：** 写代码设置 fd 为非阻塞模式。

<details>
<summary>参考答案</summary>

```c
int flags = fcntl(fd, F_GETFL);
if (flags < 0) return -1;
return fcntl(fd, F_SETFL, flags | O_NONBLOCK);
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[37.2 标准 7 步（及原因）](./37.2-section-37-2.md)
- 下一节：[37.4 `SIGHUP` 热重载](./37.4-sighup.md)
- 前置：**前置**：[Ch34 会话/`setsid`](../chapter-34-process-groups-sessions/notes.md) · [Ch36 rlimit](../chapter-36-process-resources/notes.md)  
- 后置：**后置**：[Ch38 特权程序安全](../chapter-38-secure-privileged/notes.md)

---

## man 手册参考

- `man 2 fcntl`
- `man 2 setrlimit`
