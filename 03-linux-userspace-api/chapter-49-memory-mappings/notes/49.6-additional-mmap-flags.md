# 49.6 mmap() flags 参数补充细节

> 源码核验：Linux v6.6 · `uapi/asm-generic/mman-common.h`（`MAP_SHARED:0x01` · `MAP_PRIVATE:0x02` · `MAP_FIXED:0x10` · `MAP_FIXED_NOREPLACE:0x100000`:31）· `mm/mmap.c`（`do_mmap():1203` flags 校验分支）
> 本章：[TLPI 第 49 章 — Memory Mappings](../README.md)
> - 上一节：[49.5 `msync`](49.5-synchronizing-a-mapped-region-msync.md)
> - 下一节：[49.7 匿名映射](49.7-anonymous-mappings.md)

---

## 本节讲什么

flags 参数的正式语义细节：SHARED/PRIVATE 的**修改可见性矩阵**（SUSv3 规定 + 实测验证）、MAP_TYPE 位域结构、fork 的继承规则。三个灵魂问题：

1. 「PRIVATE = 私有」到底私有什么？（只私有自己的**写**——读跟随后备，实测三连见 49.4）
2. SHARED/PRIVATE 在 flags 里占什么位置？（低 4 位 MAP_TYPE 掩码，其余标志在上层位叠加）
3. fork 之后呢？（两类映射都继承，语义原样传递——这是父子 IPC 的根基）

---

## 要点

### 修改可见性矩阵（SUSv3 规定 + v6.6 实测）

| 映射类型 | 修改者 | 谁能看到修改 | 修改是否到达底层文件 |
|----------|--------|--------------|---------------------|
| `MAP_SHARED` 文件 | 本进程 | 所有映射同一文件区域的进程 + read() 读者 | ✅（异步回写 / msync 强制） |
| `MAP_PRIVATE` 文件 | 本进程 | **只有自己**（写过之后，COW 页） | ❌ 永不 |
| `MAP_SHARED` 文件 | **其他进程**（经 SHARED 映射） | 本进程**能看到**（即使本进程是 PRIVATE 映射——**但仅限未写过的页**） | — |
| `MAP_SHARED` 文件 | 其他进程（经 write()） | 同上——**未写过的 PRIVATE 页能读到** | — |

⭐ 加粗行的矩阵正是 [49.4 实测三连](49.4-file-mappings.md) 的结论：**PRIVATE 是 copy-on-write，未写过的页始终跟随 page cache**。SUSv3 只规定了行为，实测给出了机制层面的原因（PTE 仍指向共享页）。

### MAP_TYPE：flags 的位域结构

```c
/* uapi/asm-generic/mman-common.h */
#define MAP_SHARED    0x01     /* 低 4 位是「映射类型」域 */
#define MAP_PRIVATE   0x02
#define MAP_TYPE      0x0f     /* 掩码：flags & MAP_TYPE 取类型 */
/* 高层位是行为修饰（可叠加）： */
#define MAP_FIXED         0x10
#define MAP_ANONYMOUS    0x20
#define MAP_GROWSDOWN    0x0100
#define MAP_DENYWRITE    0x0800
#define MAP_EXECUTABLE  0x1000
#define MAP_LOCKED      0x2000
#define MAP_NORESERVE   0x4000
#define MAP_POPULATE   0x008000     /* :26 */
#define MAP_STACK      0x020000     /* :28 */
#define MAP_HUGETLB    0x040000     /* :29 */
#define MAP_FIXED_NOREPLACE 0x100000  /* :31 */
```

类型域与修饰域正交：`MAP_SHARED|MAP_ANONYMOUS|MAP_POPULATE` = 类型(01) + 修饰(20|8000)。**flags 校验顺序**：`do_mmap():1203` 先取 `flags & MAP_TYPE` 判断 SHARED/PRIVATE（既无 SHARED 也无 PRIVATE → `EINVAL`），再逐个处理修饰标志。

### 各修饰标志一句话

| 标志 | 语义 | 详见 |
|------|------|------|
| `MAP_FIXED` / `MAP_FIXED_NOREPLACE` | 固定地址（覆盖/拒绝覆盖） | [49.10](49.10-the-map-fixed-flag.md) |
| `MAP_ANONYMOUS` | 无后备文件，缺页走零页/COW | [49.7](49.7-anonymous-mappings.md) |
| `MAP_NORESERVE` | 不预留 swap（overcommit） | [49.9](49.9-map-noreserve-and-swap-space-overcommitt.md) |
| `MAP_LOCKED` | 映射即锁定（等价 mmap+mlock 一步） | [50.2](../../chapter-50-virtual-memory/notes/50.2-memory-locking-mlock-and-mlockall.md) |
| `MAP_POPULATE` | 映射即预缺页（读写前页表已建好） | 本节下方 |
| `MAP_STACK` | 告诉内核「这是栈」（仅影响布局启发，不做强制） | — |
| `MAP_HUGETLB` | 用预留大页池（hugetlbfs） | [06-linux-mm](../../../06-linux-mm/README.md) |

### MAP_POPULATE：预缺页的两种姿势

```c
/* 姿势 1：mmap 时带标志 */
p = mmap(NULL, len, RW, MAP_PRIVATE|MAP_ANONYMOUS|MAP_POPULATE, -1, 0);
/* 姿势 2：mmap 后 madvise（等价） */
p = mmap(NULL, len, RW, MAP_PRIVATE|MAP_ANONYMOUS, -1, 0);
madvise(p, len, MADV_POPULATE_WRITE);      /* 5.14+，读用 POPULATE_READ */
```

把「N 页 × 首触缺页（各 µs 级）」从运行时挪到启动时。**注意 POPULATE 不等于 mlock**：页填了但不锁，内存压力下照样被换出——延迟敏感场景 POPULATE+mlock 配套用。

### fork 的继承规则

| 映射 | fork 后子进程 | 写的语义 |
|------|---------------|----------|
| MAP_PRIVATE（任意） | 继承同一映射 | 父子各自 COW（同 49.4 矩阵） |
| MAP_SHARED 文件 | 继承，与父（及所有映射者）共享 page cache | 立即互相可见 |
| MAP_SHARED|MAP_ANONYMOUS | 继承，父子共享同一物理页 | 立即互相可见（**仅亲缘进程间**，无文件名无法让无关进程拿到同一映射） |

exec 则相反：**所有映射销毁**（新程序映像重建地址空间）。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `MAP_POPULATE` 消除上电后首触抖动（工控响应时间预算友好）；`MAP_STACK` 对自定义线程栈布局是「礼貌提示」——NOMMU 上才真正影响分配 |
| HFT | 启动序标配 `MAP_POPULATE`（或 `mlockall` 顺带的填充）——把 16384 页 × 1µs 的首触税从交易时段挪到 init；`MAP_LOCKED`=mmap+mlock 原子化（免两步间隙的窗口）；可见性矩阵提醒：策略读行情用 SHARED 映射（实时跟随）或**主动触碰过的** PRIVATE（确定性快照），别用「未写过的 PRIVATE」——那是最阴险的中间态：**测试环境像快照（没人并发写），生产环境像实时（行情不断改）** |

---

## 代码示例

```c

/* MAP_POPULATE vs 惰性映射：首触缺页对比
 * 实测（WSL Ubuntu，gcc -O2 -Wall -Wextra，2026-09-05）：
 *   lazy     映射后：Rss=0 kB（画区不填页）
 *   populate 映射后：Rss=16384 kB（16MB 全部预填）
 *   lazy     区 memset 首触 15 ms（含 4096 次缺页）
 *   populate 区 memset      1 ms（无缺页，纯写内存）——15 倍差
 */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mman.h>
#include <time.h>
#include <unistd.h>

static long smaps_rss(void *addr)
{
    FILE *fp = fopen("/proc/self/smaps", "r");
    if (!fp) return -1;
    char line[300];
    unsigned long lo, hi, a = (unsigned long)addr;
    long rss = -1;
    int in_target = 0;
    while (fgets(line, sizeof line, fp)) {
        if (sscanf(line, "%lx-%lx", &lo, &hi) == 2)
            in_target = (a >= lo && a < hi);
        else if (in_target && strncmp(line, "Rss:", 4) == 0) {
            sscanf(line + 4, "%ld", &rss);
            break;
        }
    }
    fclose(fp);
    return rss;
}

int main(void)
{
    size_t len = 16UL << 20;                 /* 16MB = 4096 页 */

    char *lazy = mmap(NULL, len, PROT_READ | PROT_WRITE,
                      MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
    if (lazy == MAP_FAILED) { perror("mmap lazy"); return 1; }
    printf("lazy     映射后 Rss=%ld kB（画区，0 物理页）\n", smaps_rss(lazy));

    char *pop = mmap(NULL, len, PROT_READ | PROT_WRITE,
                     MAP_PRIVATE | MAP_ANONYMOUS | MAP_POPULATE, -1, 0);
    if (pop == MAP_FAILED) { perror("mmap populate"); return 1; }
    printf("populate 映射后 Rss=%ld kB（全部预填，写脏即真页）\n",
           smaps_rss(pop));

    /* 用时间对比首触：lazy 区首次 memset 才付税 */
    struct timespec t0, t1;
    clock_gettime(CLOCK_MONOTONIC, &t0);
    memset(lazy, 1, len);
    clock_gettime(CLOCK_MONOTONIC, &t1);
    printf("lazy 区 memset 首触耗时 %ld ms（含 4096 次缺页）\n",
           (t1.tv_sec - t0.tv_sec) * 1000 + (t1.tv_nsec - t0.tv_nsec) / 1000000);

    clock_gettime(CLOCK_MONOTONIC, &t0);
    memset(pop, 1, len);
    clock_gettime(CLOCK_MONOTONIC, &t1);
    printf("populate 区 memset 耗时 %ld ms（无缺页，纯写内存）\n",
           (t1.tv_sec - t0.tv_sec) * 1000 + (t1.tv_nsec - t0.tv_nsec) / 1000000);

    munmap(lazy, len);
    munmap(pop, len);
    return 0;
}
```

（此示例完整版含计时实测记录在 [49.13 实验](49.13-exercises.md)。）

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[49.5 `msync`](49.5-synchronizing-a-mapped-region-msync.md)
- 下一节：[49.7 匿名映射](49.7-anonymous-mappings.md)
- 可见性实测三连：[49.4 文件映射](49.4-file-mappings.md)
- MAP_POPULATE 的 madvise 等价：[50.4 `madvise`](../../chapter-50-virtual-memory/notes/50.4-advising-future-memory-usage-patterns-ma.md)
- MAP_LOCKED：[50.2 `mlock`](../../chapter-50-virtual-memory/notes/50.2-memory-locking-mlock-and-mlockall.md)

---

## 自测题

**题目 1（选择）：** `flags & MAP_TYPE` 的作用？

A. 取出 SHARED/PRIVATE 类型位　B. 校验 flags 合法性　C. 清除修饰标志　D. 区分匿名/文件映射

<details>
<summary>参考答案</summary>

**A**。MAP_TYPE=0x0f 掩出低 4 位的映射类型域（SHARED=0x01/PRIVATE=0x02）；匿名与否由 MAP_ANONYMOUS（0x20）单独表达，与类型域正交——四种组合（文件/匿名 × SHARED/PRIVATE）就是 [49.1 四大组合](49.1-overview.md)。

</details>

**题目 2（判断）：** `MAP_POPULATE` 之后这些页被锁定，不会被换出。

<details>
<summary>参考答案</summary>

错误。POPULATE 只做**预缺页**（当下把页表建满、页填进来），不设 VM_LOCKED——内存压力下页照样进入回收/换出路径。要「永不换出」必须 mlock/MAP_LOCKED。两者是正交维度：POPULATE 管「现在有没有」，LOCKED 管「将来走不走」。

</details>

**题目 3（应用）：** 策略进程用 `MAP_PRIVATE` 映射行情文件，测试回测全对、实盘偶发「读到未来数据」式异常，从本节矩阵出发定位问题。

<details>
<summary>参考答案</summary>

回测时无并发写入者，PRIVATE 未写过的页读到的永远是「当前值」= 事实快照，行为正确；实盘行情进程持续改 page cache，策略的 PRIVATE 页**未写过的部分跟随实时变化**——同一个虚址两次读可能不同，回测里不存在这种竞态。修复：① 改用 SHARED 映射 + 显式双缓冲版本号（承认实时性，用序号 fence 一致性）；② 或初始化时主动触碰整段（memset 定格 COW 快照）+ 定期显式刷新重定格。根源是误把 copy-on-write 当 copy-on-access。

</details>
