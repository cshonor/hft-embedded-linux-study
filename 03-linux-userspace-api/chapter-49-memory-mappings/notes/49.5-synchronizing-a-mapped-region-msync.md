# 49.5 `msync`

> 本章：[TLPI 第 49 章 — Memory Mappings](./README.md)
> - 上一节：[49.4 文件映射](./49.4-map.md)
> - 下一节：[49.6 –49.8 Flags · `mremap`](./49.6-mremap.md)

---

## 本节讲什么

int msync(void *addr, size_t length, int flags); `MS_SYNC` 阻塞落盘 · `MS_ASYNC` 后台 · `MS_INVALIDATE` 作废缓存。 仅对 **SHARED** 有意义。

---

## 要点

```c
int msync(void *addr, size_t length, int flags);
```

`MS_SYNC` 阻塞落盘 · `MS_ASYNC` 后台 · `MS_INVALIDATE` 作废缓存。  
仅对 **SHARED** 有意义。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `msync` 在嵌入式系统的应用场景 |
| HFT | `msync` 对低延迟和性能的影响 |

## 代码示例

```c

/* msync 三种模式：MS_SYNC / MS_ASYNC / MS_INVALIDATE */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <fcntl.h>
#include <sys/mman.h>
#include <unistd.h>

int main(void) {
    int fd = open("/tmp/ch49_msync.txt", O_RDWR | O_CREAT | O_TRUNC, 0644);
    ftruncate(fd, 4096);
    char *m = mmap(NULL, 4096, PROT_READ | PROT_WRITE, MAP_SHARED, fd, 0);
    if (m == MAP_FAILED) { perror("mmap"); exit(1); }

    strcpy(m, "msync demo data");

    /* MS_SYNC：同步刷盘，阻塞直到写回完成 */
    if (msync(m, 4096, MS_SYNC) < 0) perror("msync MS_SYNC");
    printf("MS_SYNC done — data flushed to disk\n");

    /* MS_ASYNC：异步刷盘，立即返回 */
    strcpy(m, "async update");
    if (msync(m, 4096, MS_ASYNC) < 0) perror("msync MS_ASYNC");
    printf("MS_ASYNC submitted\n");

    munmap(m, 4096);
    close(fd);
    return 0;
}

```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[49.4 文件映射](./49.4-map.md)
- 下一节：[49.6 –49.8 Flags · `mremap`](./49.6-mremap.md)
- 前置：**前置**：[Ch48 SysV 共享内存](../chapter-48-sysv-shared-memory/notes.md)  
- 后置：**后置**：[Ch50 虚拟内存操作](../chapter-50-virtual-memory/notes.md) · [Ch51 POSIX IPC](../chapter-51-posix-ipc-intro/notes.md)
