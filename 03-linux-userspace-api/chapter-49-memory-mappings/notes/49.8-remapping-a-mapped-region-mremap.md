# 49.8 重新映射：mremap()

> 本章：[TLPI 第 49 章 — 内存映射](../README.md)
> - 上一节：[49.7 匿名映射](./49.7-anonymous-mappings.md)
> - 下一节：[49.9 MAP_NORESERVE 与 overcommit](./49.9-map-noreserve-and-swap-space-overcommitt.md)

---

## 本节讲什么

`mremap()` — Linux 特有，调整已映射区域的大小或位置（类似 `realloc` 对 `mmap`）。

---

## 要点

### API

```c
void *mremap(void *old_address, size_t old_size, size_t new_size, int flags, ...);
```
- 调整 `old_address` 处 `old_size` 的映射为 `new_size`
- `flags`: `MREMAP_MAYMOVE`（允许移动地址）
- 返回新地址（可能与旧地址不同）

### 核心行为

| 操作 | 行为 |
|------|------|
| 扩大 | 在原地址扩展（空间足够）或移动到新位置 |
| 缩小 | 截断，多余部分被 munmap |
| 移动 | 如果 MREMAP_MAYMOVE 且原位置空间不足 |

### 与 realloc 的关系

`glibc` 的 `realloc` 对大块内存（>128KB）内部使用 `mremap`：
- `malloc` 大块 → `mmap`
- `realloc` 大块 → `mremap`
- `free` 大块 → `munmap`

### 注意事项

- **Linux 特有**，不可移植
- 只能 remap 匿名映射（不能 remap 文件映射的大小）
- 不指定 `MREMAP_MAYMOVE` 且空间不足 → 返回 `MAP_FAILED`
- 缩小时，被截断区域的脏页（MAP_SHARED）会写回文件

### 性能优势

- 扩大映射时不需要拷贝数据（如果原地扩展）
- 比 malloc+memcpy+free 快得多

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | mremap 扩展共享内存避免拷贝，性能优于重新 mmap |
| HFT | 大块内存重分配用 mremap 避免数据拷贝 |

## 代码示例

```c
#include <stdio.h>
#include <sys/mman.h>
#include <string.h>
/* mremap: 调整映射大小 */
int main(void) {
    /* 初始映射 1 页 */
    size_t old_size = 4096;
    char *p = mmap(NULL, old_size, PROT_READ|PROT_WRITE,
                   MAP_PRIVATE|MAP_ANONYMOUS, -1, 0);
    if (p == MAP_FAILED) { perror("mmap"); return 1; }
    strcpy(p, "hello");

    /* 扩大到 4 页 */
    size_t new_size = 4096 * 4;
    char *new_p = mremap(p, old_size, new_size, MREMAP_MAYMOVE);
    if (new_p == MAP_FAILED) {
        perror("mremap");
        munmap(p, old_size);
        return 1;
    }

    printf("old: %p, new: %p\n", p, new_p);
    printf("data preserved: %s\n", new_p);  /* "hello" */

    /* 写入更大范围 */
    memset(new_p + old_size, 'x', new_size - old_size);
    printf("expanded region filled\n");

    munmap(new_p, new_size);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[49.7 匿名映射](./49.7-anonymous-mappings.md)
- 下一节：[49.9 MAP_NORESERVE](./49.9-map-noreserve-and-swap-space-overcommitt.md)
