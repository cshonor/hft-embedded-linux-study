# 56.6 –56.8 I/O 与关闭

> 本章：[TLPI 第 56 章 — Sockets: Introduction](./README.md)
> - 上一节：[56.5 面向连接 API](./56.5-api.md)
> - 下一节：[下一章](../chapter-{int(ch_num)+1:02d}-*/notes.md)

---

## 本节讲什么

`read`/`write` 可用；`send`/`recv` 带 flags（`MSG_PEEK` 等）。 STREAM 无边界 → 应用层分包。 默认阻塞；`O_NONBLOCK` → `EAGAIN`/`EWOULDBLOCK`（配 epoll 等）。

---

## 要点

`read`/`write` 可用；`send`/`recv` 带 flags（`MSG_PEEK` 等）。  
STREAM 无边界 → 应用层分包。

| | `close` | `shutdown(how)` |
|--|---------|-----------------|
| 语义 | fd 引用 −1；归零才断 | 直接关通道；`SHUT_RD/WR/RDWR` |
| 半关闭 | 难 | **`SHUT_WR` 发 FIN，仍可读** |
| fork 后 | 一方 close 未必断 TCP | 推荐显式 shutdown |

默认阻塞；`O_NONBLOCK` → `EAGAIN`/`EWOULDBLOCK`（配 epoll 等）。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | –56.8 I/O 与关闭 在嵌入式系统的应用场景 |
| HFT | –56.8 I/O 与关闭 对低延迟和性能的影响 |

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[56.5 面向连接 API](./56.5-api.md)
- 前置：**前置**：[Ch55 文件锁](../chapter-55-file-locking/notes.md) · 本地 IPC  
- 后置：**后置**：[Ch57 UNIX 域](../chapter-57-sockets-unix-domain/notes.md) → [Ch58 TCP/IP](../chapter-58-tcpip-fundamentals/notes.md) → [Ch59 Internet](../chapter-59-internet-domains/notes.md)

---

## man 手册参考

- `man 2 read`
- `man 2 write`
- `man 2 send`
- `man 2 recv`
- `man 2 close`
