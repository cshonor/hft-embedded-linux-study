# 56.2 –56.4 创建与地址

> 本章：[TLPI 第 56 章 — Sockets: Introduction](./README.md)
> - 上一节：[56.1 概念](./56.1-concepts.md)
> - 下一节：[56.5 面向连接 API](./56.5-api.md)

---

## 本节讲什么

int socket(int domain, int type, int protocol);  /* protocol 常 0 */ int bind(int sockfd, const struct sockaddr *addr, socklen_t addrlen); 统一入口 `struct sockaddr`；实际用 `sockaddr_un` / `_in` / `_in6`。

---

## 要点

```c
int socket(int domain, int type, int protocol);  /* protocol 常 0 */
int bind(int sockfd, const struct sockaddr *addr, socklen_t addrlen);
```

统一入口 `struct sockaddr`；实际用 `sockaddr_un` / `_in` / `_in6`。  
`addrlen` 用 **`socklen_t`**。代码用 `AF_*`（与 `PF_*` 同值）。

---

---

## 与 C 语言学习的关系

Socket 编程是 C 语言网络编程的核心。`socket/bind/listen/accept` 的调用顺序需要理解 C 语言的 struct（如 `sockaddr_in`）、字节序（`htons/ntohs`）和字符串处理。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | –56.4 创建与地址 在嵌入式系统的应用场景 |
| HFT | –56.4 创建与地址 对低延迟和性能的影响 |

---

## 常见陷阱

1. **SIGPIPE**

```c
send(fd, buf, n, 0);  /* 对端关闭 → SIGPIPE */
send(fd, buf, n, MSG_NOSIGNAL);  /* 安全 */
```

向已关闭连接的 socket 写数据触发 SIGPIPE。用 MSG_NOSIGNAL 或忽略 SIGPIPE。

2. **TIME_WAIT**

```c
bind(fd, (struct sockaddr*)&addr, len);
/* EADDRINUSE if port in TIME_WAIT */
```

服务端重启时端口可能还在 TIME_WAIT。用 SO_REUSEADDR 允许绑定。

3. **accept 返回值未检查**

```c
int cfd = accept(lfd, NULL, NULL);
/* cfd 可能 -1 (EMFILE/ENFILE) */
```

accept 可能因 fd 耗尽返回 -1。不检查直接用会导致后续操作失败。

---

## 自测题

**题目 1（选择）：** `listen()` 的第二个参数 backlog 表示什么？
A. 最大连接数
B. 等待 accept 的队列长度
C. 缓冲区大小
D. 超时秒数

<details>
<summary>参考答案</summary>

B。backlog 是已完成连接但尚未 accept 的队列长度。超过后新连接可能被拒绝。

</details>

**题目 2（判断）：** `bind` 可以绑定任意端口号。

<details>
<summary>参考答案</summary>

错误。1-1023 是特权端口，需要 root 权限。已占用的端口会返回 EADDRINUSE。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
int sfd = socket(AF_INET, SOCK_STREAM, 0);
connect(sfd, (struct sockaddr*)&addr, sizeof(addr));
send(sfd, "data", 4, 0);
```

<details>
<summary>参考答案</summary>

没有检查 connect 返回值。connect 可能失败（连接被拒绝、超时等），不检查直接 send 会导致错误。

</details>

**题目 4（应用）：** 写代码创建 TCP 服务端 socket 并监听。

<details>
<summary>参考答案</summary>

```c
int sfd = socket(AF_INET, SOCK_STREAM, 0);
int opt = 1;
setsockopt(sfd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));
struct sockaddr_in addr = {0};
addr.sin_family = AF_INET;
addr.sin_port = htons(8080);
addr.sin_addr.s_addr = INADDR_ANY;
bind(sfd, (struct sockaddr*)&addr, sizeof(addr));
listen(sfd, 128);
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[56.1 概念](./56.1-concepts.md)
- 下一节：[56.5 面向连接 API](./56.5-api.md)
- 前置：**前置**：[Ch55 文件锁](../chapter-55-file-locking/notes.md) · 本地 IPC  
- 后置：**后置**：[Ch57 UNIX 域](../chapter-57-sockets-unix-domain/notes.md) → [Ch58 TCP/IP](../chapter-58-tcpip-fundamentals/notes.md) → [Ch59 Internet](../chapter-59-internet-domains/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
