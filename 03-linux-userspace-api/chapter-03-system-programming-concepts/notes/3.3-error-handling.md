# 3.3 错误处理（本章重中之重）

> 本章：[TLPI 第 03 章 — System Programming Concepts](./README.md)
> - 上一节：[3.2 Library Functions（glibc）](./3.2-library-functions-glibc.md)
> - 下一节：[3.4 可移植编程（SUSv3 / POSIX）](./3.4-susv3.md)

---

## 本节讲什么

/* ❌ 错误：成功路径也可能留下旧 errno */ open(...); if (errno != 0) { ... }

---

## 要点

### `errno`（`<errno.h>`）

| 规则 | |
|------|--|
| 成功 | **不会** 把 `errno` 清零 |
| 失败 | 才被赋有意义的值 |
| 现代 glibc | **线程局部**；多线程互不干扰 |

```c
/* ❌ 错误：成功路径也可能留下旧 errno */
open(...);
if (errno != 0) { ... }

/* ✅ 先看返回值，失败再读 errno */
fd = open(path, O_RDONLY);
if (fd == -1) {
    /* 此时读 errno 才有意义 */
}
```

### 打印 / 退出工具

| 函数 | 作用 |
|------|------|
| `perror(msg)` | 打印 `msg` + errno 文本 |
| `strerror(errnum)` | 错误码 → 字符串（经典版非线程安全；有 `_r` 变体） |
| TLPI `errExit()` | 打印并 **exit** |
| TLPI `errMsg()` | 只打印不退出 |
| TLPI `fatal()` | 程序逻辑错误（非 syscall） |

### `tlpi_hdr.h`

原书示例统一引入：头文件集合、错误函数、数值解析等。  
清单概念：`lib/tlpi_hdr.h`、`error_functions.c` 等（见 man7 源码包）。

---

---

## 与 C 语言学习的关系

本节涉及的 `open()` 是 C 语言文件操作的基础系统调用。与 K&R 第 8 章的 `fopen()` 不同，`open()` 是 POSIX 系统调用而非 C 标准库函数。理解 `open()` 的 flags 参数（O_RDONLY、O_CREAT 等）是掌握 Unix 系统编程的第一步。对照 K&R 8.3 节的 `open/creat/close` 学习，注意 C 标准库 `fopen` 的 "r"/"w"/"a" 模式如何映射到 `open` 的 flags。

---

## HFT / 嵌入式关联

| 路线 | |
|------|--|
| 嵌入式 | 严格返回值+`errno`；功能测试宏保证 API 可见 |
| HFT | 少 syscall；测延迟时区分包装成本与真陷入 |

---

## 常见陷阱

1. **O_CREAT 忘传 mode**

```c
/* mode 参数丢失，权限不确定 */
fd = open("f.txt", O_WRONLY | O_CREAT);
/* 必须给 mode */
fd = open("f.txt", O_WRONLY | O_CREAT, 0644);
```

O_CREAT 时第三个参数 mode 是新文件权限，受 umask 影响。漏传会导致权限不确定（取决于栈上残留值）。

2. **返回值未检查**

```c
int fd = open(path, O_RDONLY);
read(fd, buf, n);  /* fd=-1 → EBADF */
```

open 失败返回 -1。不检查直接用 fd 会导致后续操作全部失败。必须 `if ((fd = open(...)) < 0) { perror; exit; }`。

3. **O_TRUNC 意外截断**

```c
/* 想追加写，却加了 O_TRUNC */
fd = open("log.txt", O_WRONLY | O_TRUNC);  /* 清空了! */
fd = open("log.txt", O_WRONLY | O_APPEND);  /* 正确 */
```

O_TRUNC 在文件存在时截断为 0。追加写场景误加 O_TRUNC 会丢失数据。

---

## 自测题

**题目 1（选择）：** `open("f.txt", O_CREAT)` 缺少什么参数？
A. flags 不对
B. mode 参数
C. pathname
D. 返回值

<details>
<summary>参考答案</summary>

B。O_CREAT 时必须提供 mode（如 0644），受 umask 影响。漏传会使用栈上不确定的值。

</details>

**题目 2（判断）：** `open` 失败时返回 0。

<details>
<summary>参考答案</summary>

错误。open 失败返回 -1 并设置 errno。fd=0 是 stdin，open 正常时可能返回 0（如果 stdin 被关闭）。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
int fd = open("/etc/passwd", O_WRONLY|O_CREAT|O_TRUNC, 0644);
```

<details>
<summary>参考答案</summary>

对 /etc/passwd 用 O_WRONLY|O_TRUNC 会截断系统密码文件！想读文件应用 O_RDONLY。这是非常危险的操作。

</details>

**题目 4（应用）：** 写代码：以读写方式打开文件，不存在则创建（权限 0644），存在则追加。

<details>
<summary>参考答案</summary>

```c
int fd = open("data.txt", O_RDWR|O_CREAT|O_APPEND, 0644);
if (fd < 0) { perror("open"); exit(1); }
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[3.2 Library Functions（glibc）](./3.2-library-functions-glibc.md)
- 下一节：[3.4 可移植编程（SUSv3 / POSIX）](./3.4-susv3.md)
- 前置：**前置**：[Ch2 Fundamental Concepts](../chapter-02-basic-concepts/notes.md)  
- 后置：**后置**：[Ch4 Universal I/O](../chapter-04-file-io-universal/notes.md)（第一个实战 syscall 集）  

---

## man 手册参考

- `man 3 perror`
- `man 3 strerror`
