# 3.5 Notes on the Example Programs in This Book 本书示例程序说明

> 本章：[TLPI 第 03 章 — System Programming Concepts](../README.md)
> 源码核验：man7 官方按章文件清单（`lib/tlpi_hdr.h` = Listing 3-1 … `lib/get_num.c` = Listing 3-6）
> - 上一节：[3.4 Handling Errors from System Calls and Library Functions 错误处理](3.4-error-handling.md)
> - 下一节：[3.6 Portability Issues 可移植性](3.6-portability.md)

---

## 本节讲什么

TLPI 全书有几百个示例程序，它们共享同一套「约定」——同一套命令行接口、同一批头文件、同一批自造工具函数。3.5 就是这套约定的说明书。它分两半：

| 小节 | 讲什么 |
|------|--------|
| **3.5.1** Command-Line Options and Arguments | 示例程序的命令行长什么样（`./prog [-a] [-b value] file...`），为什么统一用 `getopt(3)` |
| **3.5.2** Common Functions and Header Files | 每个示例都 `#include` 的 `tlpi_hdr.h`，以及自带的两组工具：错误处理（`error_functions`）与数值解析（`get_num`） |

三个 demo：

| demo | 对应小节 | 干什么 |
|------|----------|--------|
| `c3_6_cli_args.c` | 3.5.1 | 把 `argc`/`argv` 的**真实形状**打印出来 + 喂人工构造的 `argv` 演示 `getopt` 的三种结局 |
| `c3_7_get_num.c` | 3.5.2 | 复刻 `get_num.c` 的**三态判定**，并对照 `atoi` 为什么不够用 |
| `c3_8_error_functions.c` | 3.5.2 | 把「会自杀的」六个错误处理函数各关进一个子进程，抓回真实输出与退出码 |

> 这一节**不讲**机制。系统调用的寄存器约定、`copy_from_user`、`EFAULT` 都在 [3.1](3.1-system-calls.md) 和 [3.4](3.4-error-handling.md) 里；本节只回答「原书那套示例骨架长什么样，为什么这么设计」。

---

## 1. 原书 `lib/` 的六个 Listing（官方编号）

出处：man7 官方「List of source code files, by chapter」——第 3 章一共 6 个带 Listing 号的文件，另有两个无编号的补充文件：

| Listing | 文件 | 作用 |
|---------|------|------|
| **3-1** | `lib/tlpi_hdr.h` | 公共头文件：把常用系统头一次性拉进来 |
| **3-2** | `lib/error_functions.h` | 错误处理函数**声明** |
| **3-3** | `lib/error_functions.c` | 错误处理函数**实现** |
| **3-4** | `lib/ename.c.inc` | `errno` → 名字 的静态表 |
| **3-5** | `lib/get_num.h` | 命令行数值解析函数**声明** |
| **3-6** | `lib/get_num.c` | 命令行数值解析函数**实现** |
| — | `lib/alt_functions.c` / `.h` | libc 函数的替代版（无 Listing 号） |
| — | `progconc/syscall_speed.c` | 测系统调用开销（无 Listing 号，见 [3.7](3.7-summary.md)） |

几个容易记错的点：

| 易错 | 正确 |
|------|------|
| 3-1 是 `error_functions.h` | **3-1 是 `tlpi_hdr.h`**；`error_functions.h` 是 3-2 |
| 3-5 是 `get_num.c` | **3-5 是 `get_num.h`**，`.c` 才是 3-6 |
| `ename.c.inc` 没编号 | 它是 **Listing 3-4** |
| `.inc` 是个 C 源文件 | 它是被 `#include` 进 `error_functions.c` 的**片段**，不单独编译 |

## 2. `tlpi_hdr.h`：为什么这本书要先造一个头

TLPI 的每个示例第一行都是 `#include "tlpi_hdr.h"`。它做的事很简单——把后面几百个程序都要用的东西集中一次：

| 它做了什么 | 为什么 |
|------------|--------|
| 一次性 `#include` 常用系统头（`sys/types.h` / `stdio.h` / `stdlib.h` / `unistd.h` / `errno.h` / `string.h`） | 示例代码里就不用写十几行 `#include`，正文能专注于要讲的 syscall |
| 转手 `#include "get_num.h"` 与 `"error_functions.h"` | 任何包含 `tlpi_hdr.h` 的程序自动获得两组工具函数 |
| `typedef enum { FALSE, TRUE } Boolean;` | C99 之前没有 `stdbool.h` 的 `bool`，书里统一用这个 |
| 提供 `min()` / `max()` 宏 | 同上，随手可用 |

> **注意这不是「好实践」而是「书稿便利」**：一个头把系统的、自己的、宏的全部糊在一起，在真实项目里会造成编译依赖爆炸。它的价值是让书里的示例代码只有十几行就能跑。自己写项目别照抄这个结构。

## 3. §3.5.1 命令行接口的统一约定

原书所有示例用同一种用法串描述自己的接口，形如：

```
./prog [-a] [-b value] file...
```

三条约定：

| 约定 | 说明 |
|------|------|
| 选项 = `-` + **单个字符** | `-a` 是开关，`-b value` 带一个值。这是 POSIX `getopt(3)` 的语法 |
| `[...]` 表示可选，裸词表示必需 | `[-a]` 可省，`file...` 至少给一个 |
| 用法错就 `usageErr()` 并退出 | 打到 **stderr**、退出码 `EXIT_FAILURE`，让 shell 脚本能用 `$?` 判断 |

为什么统一用 `getopt(3)` 而不是自己解析 `argv`：它把「选项与操作数混排」「`-ab` 合并写」「`--` 结束选项」这些边角都处理好了，且是 POSIX 标准（任何合规 Unix 都有）。

### 代码示例 A：`argc` / `argv` 的真实形状 + `getopt` 的三种结局

源码：[../code/c3_6_cli_args.c](../code/c3_6_cli_args.c)

```c
/* TLPI 第 03 章 §3.5.1 —— 命令行选项与参数：argc/argv 的形状 + getopt(3)
 *
 * 原书在 §3.5.1 说明「本书示例程序统一用什么方式解析命令行」，用到的就是
 * getopt(3)。本 demo 把解析逻辑做成函数，然后**喂人工构造的 argv** ——
 * 这样实验可控（CE 上真实 argv 只有 ./output.s），也顺带演示了
 * 「argc/argv 只是一个数组，没有魔法」。
 *
 * 编译：gcc -O0 -Wall -Wextra -o c3_6 c3_6_cli_args.c
 */
#define _GNU_SOURCE
#include <getopt.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* ---------- ① argv 的形状：最后一个元素必然是 NULL ---------- */

static void show_argv(int argc, char **argv, const char *tag)
{
    printf("  [%s] argc = %d\n", tag, argc);
    for (int i = 0; i < argc; i++) {
        printf("      argv[%d] = \"%s\"\n", i, argv[i]);
    }
    printf("      argv[%d] = %s   ← 标准保证最后一个元素是 NULL\n", argc,
           argv[argc] == NULL ? "NULL" : "(非 NULL，不合法！)");
}

/* ---------- ② getopt(3)：`-a` / `-b val` / `-c` / 非法选项 ---------- */

static void parse(int argc, char **argv, const char *tag)
{
    printf("  [%s] 解析 \"%s\"（getopt 选项串 \"ab:c\"）\n", tag, argc > 1 ? argv[1] : "");
    optind = 1;          /* getopt 的状态是全局的，重入前必须复位 */
    opterr = 0;          /* 关掉 getopt 自己打的错误信息，改由我们统一报 */
    optarg = NULL;

    int ch;
    while ((ch = getopt(argc, argv, "ab:c")) != -1) {
        switch (ch) {
        case 'a':
            printf("      -a 开关，无参数\n");
            break;
        case 'b':
            printf("      -b 带参数，optarg = \"%s\"\n", optarg);
            break;
        case 'c':
            printf("      -c 开关，无参数\n");
            break;
        case '?':
            if (optopt) {
                printf("      非法选项 '-%c'（未知选项）→ 本该 usageErr 并退出\n", optopt);
            } else {
                printf("      '-%s' 缺少必需参数 → 本该 usageErr 并退出\n",
                       argv[optind - 1] ? argv[optind - 1] : "?");
            }
            break;
        default:
            printf("      getopt 返回了意外字符 '%c'\n", ch);
            break;
        }
    }
    printf("      解析结束 optind = %d\n", optind);
    for (int i = optind; i < argc; i++) {
        printf("      非选项参数（操作数）: \"%s\"\n", argv[i]);
    }
    printf("\n");
}

int main(int argc, char **argv)
{
    setvbuf(stdout, NULL, _IONBF, 0);

    printf("=== ① 本次真正的 argv（CE 上只给了程序名）===\n");
    show_argv(argc, argv, "真实 argv");
    printf("\n");

    printf("=== ② 人工构造的 argv：演示 getopt 的三种结局 ===\n");
    char *t1[] = {"prog", "-a", "-b", "42", "-c", "file1", "file2", NULL};
    char *t2[] = {"prog", "-x", NULL};
    char *t3[] = {"prog", "-b", NULL};
    char *t4[] = {"prog", "standalone.txt", NULL};
/* 用 sizeof 算 argc，别手数 —— 手数一次就会错 */
#define ARGC(a) ((int) (sizeof(a) / sizeof((a)[0]) - 1))
    parse(ARGC(t1), t1, "全部正常");
    parse(ARGC(t2), t2, "未知选项 -x");
    parse(ARGC(t3), t3, "缺参数 -b");
    parse(ARGC(t4), t4, "只有操作数");

    printf("=== ③ 为什么 GLIBC 的 getopt 支持 \"--long\"：getopt_long ===\n");
    printf("  POSIX 的 getopt 只认单字符选项；GNU 扩展 getopt_long/getopt_long_only\n");
    printf("  才认 \"--verbose\" 这种长选项（需要 <getopt.h>）\n\n");

    printf("=== ④ 原书示例程序的统一约定（§3.5.1）===\n");
    printf("  - 用法：./prog [-a] [-b value] file...\n");
    printf("  - 参数错 → usageErr(\"%%s [-a] [-b value] file...\", prog) 打到 stderr 并退出\n");
    printf("  - usageErr 的实现见 c3_8_error_functions.c\n");
    return 0;
}
```

编译与运行（CE 复核，gcc 13.3.0 / x86-64 / Ubuntu 24.04，`-O0 -Wall -Wextra`，**build code = 0，diagnostics = 0**）：

```text
=== ① 本次真正的 argv（CE 上只给了程序名）===
  [真实 argv] argc = 1
      argv[0] = "./output.s"
      argv[1] = NULL   ← 标准保证最后一个元素是 NULL

=== ② 人工构造的 argv：演示 getopt 的三种结局 ===
  [全部正常] 解析 "-a"（getopt 选项串 "ab:c"）
      -a 开关，无参数
      -b 带参数，optarg = "42"
      -c 开关，无参数
      解析结束 optind = 5
      非选项参数（操作数）: "file1"
      非选项参数（操作数）: "file2"

  [未知选项 -x] 解析 "-x"（getopt 选项串 "ab:c"）
      非法选项 '-x'（未知选项）→ 本该 usageErr 并退出
      解析结束 optind = 2

  [缺参数 -b] 解析 "-b"（getopt 选项串 "ab:c"）
      非法选项 '-b'（未知选项）→ 本该 usageErr 并退出
      解析结束 optind = 2

  [只有操作数] 解析 "standalone.txt"（getopt 选项串 "ab:c"）
      解析结束 optind = 1
      非选项参数（操作数）: "standalone.txt"
```

### 逐条解读

| 段 | 读数 | 说明 |
|----|------|------|
| ① | `argc = 1`，`argv[0] = "./output.s"`，`argv[1] = NULL` | **`argv` 就是一个以 `NULL` 结尾的字符串数组，没有魔法**。CE 只传了程序名，所以只有一个元素。`argv[argc] == NULL` 是标准保证的哨兵，所以遍历可以写 `for (char **p = argv; *p; p++)` |
| ② 全部正常 | `optind = 5`，两个操作数 `file1` / `file2` | `getopt` 每消费一个选项就推进 `optind`。选项解析完后，**`argv[optind]` 起的全是操作数**。注意 GNU 版会把选项重排到前面，所以两端的 `file1 file2` 被挪到了尾部 |
| ② 未知选项 `-x` | 走 `'?'` 分支，`optopt = 'x'` | `getopt` 返回 `'?'` 表示「这一步失败了」，具体是哪种失败看 `optopt` 或 `optarg` |
| ② **缺参数 `-b`** | 打印的却是「**未知选项** '-b'」 | ⚠️ **这是 demo 判据不够细的地方，正好当教学点**：`optstring` 写成 `"ab:c"` 时，「未知选项」和「`-b` 缺参数」**都返回 `'?'`**（只是 `optopt` 不同）。本 demo 的 `if (optopt)` 分支把两者混成了一个说法 |
| ② 只有操作数 | `optind = 1`，没有 `'?'` | 一个选项都没有时 `getopt` 立刻返回 `-1`，`optind` 停在 1，第一个参数就是操作数 |
| ③ | `getopt_long` 才支持 `--verbose` | POSIX 的 `getopt` **只认单字符选项**；长选项是 GNU 扩展，需要 `#include <getopt.h>` |

> **把「未知选项」和「缺参数」分开的正确做法**：`optstring` **开头加一个冒号** → `":ab:c"`。这时「缺参数」返回 `':'`（而不是 `'?'`），「未知选项」仍返回 `'?'`，两类错误就分开了。这是 `getopt(3)` man page 里明确写的（`man 3 getopt` 的「Return value」段）。
>
> 另外注意 demo 里那句 `optind = 1;`——`getopt` 的**解析状态是全局的**（`optind`/`optarg`/`optopt` 都是全局变量），所以同一个进程里第二次解析前**必须复位**，否则会接着上一次的位置继续。

## 4. §3.5.2 工具 A：`get_num` —— 把「0」和「出错」分开

原书 `get_num.c`（Listing 3-6）暴露两个函数，被大量示例用来解析「从命令行拿到的数字」：

```c
int getInt(const char *arg, int flags, const char *name);
long getLong(const char *arg, int flags, const char *name);
```

`flags` 是一组位标志，用来加约束：

| flag | 含义 |
|------|------|
| `GN_NONNEG` | 值必须 ≥ 0 |
| `GN_GT_0` | 值必须 > 0 |
| `GN_ANY_BASE` / `GN_BASE_8` / `GN_BASE_16` | 允许任意基数 / 八进制 / 十六进制 |
| `GN_NOFLAG` | 不加约束 |

它存在的理由只有一个：**`atoi(3)` 无法把「数字 0」和「解析失败」分开**——两者都返回 0，且溢出时静默给一个截断值。原书 `man atoi(3)` 的一句原文是「`The atoi() function ... has undefined behavior if the value cannot be represented`」。所以示例程序必须自己造一个**三态**版本：成功 / 非法 / 溢出。

### 代码示例 B：复刻 `get_num` 的判据

源码：[../code/c3_7_get_num.c](../code/c3_7_get_num.c)

```c
/* TLPI 第 03 章 §3.5.2 —— 原书 get_num.c 要解决的问题：把「字符串读数字」
 * 里「0」和「出错」这两件完全不同的事分开。
 *
 * atoi(3) 的致命缺陷（man 原文）："The atoi() function ... has undefined behavior
 * if the value cannot be represented" —— 而且它**完全没有错误返回通道**：
 *   atoi("abc") == 0   也  atoi("0") == 0
 * 你还可能是 ERANGE 溢出，它照样给你一个截断后的值。
 *
 * 原书 lib/get_num.c（Listing 3-6）的做法 = strtol + errno + endptr 三态判定。
 *
 * 编译：gcc -O0 -Wall -Wextra -o c3_7 c3_7_get_num.c
 */
#define _GNU_SOURCE
#include <errno.h>
#include <limits.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* ---------- 反面教材：atoi 说不清到底发生了什么 ---------- */
static void show_atoi(const char *s)
{
    errno = 0;
    int v = atoi(s);
    printf("  atoi(%-28s) = %-12d errno = %d\n", s, v, errno);
}

/* ---------- 正面写法：原书 get_num.c 的三态判定 ---------- */
enum parse_status { OK, BAD_TOKEN, OVERFLOW, EMPTY };

static enum parse_status get_int(const char *str, int *out)
{
    if (str == NULL || *str == '\0') {
        return EMPTY;
    }
    errno = 0;
    char *endptr = NULL;
    long v = strtol(str, &endptr, 10);

    if (errno != 0) {              /* ① 溢出：strtol 设 ERANGE */
        return OVERFLOW;
    }
    if (endptr == str || *endptr != '\0') {   /* ② 一个数字字符都没吃 / 后面还有垃圾 */
        return BAD_TOKEN;
    }
    if (v < INT_MIN || v > INT_MAX) {         /* ③ long 装得下但 int 装不下 */
        return OVERFLOW;
    }
    *out = (int) v;
    return OK;
}

static const char *status_name(enum parse_status s)
{
    switch (s) {
    case OK:        return "OK";
    case BAD_TOKEN: return "BAD_TOKEN";
    case OVERFLOW:  return "OVERFLOW";
    case EMPTY:     return "EMPTY";
    }
    return "?";
}

int main(void)
{
    printf("=== ① atoi 的问题：0 与出错无法区分 ===\n");
    show_atoi("0");
    show_atoi("abc");
    show_atoi("");
    show_atoi("123abc");                       /* 只吃前缀，后半段被静默丢掉 */
    show_atoi("99999999999999999999");         /* 远超 int 范围 */
    show_atoi("-99999999999999999999");
    printf("  → 前两行的返回值一模一样，都是 0；最后两行的溢出也不报错\n\n");

    printf("=== ② 同一个输入用 strtol + errno + endptr 判 ===\n");
    const char *cases[] = {"0", "abc", "", "123abc", "99999999999999999999",
                           "-99999999999999999999", " 42", "42 ", "+7", "0x1f", "+0", NULL};
    for (int i = 0; cases[i] != NULL; i++) {
        int v = 0;
        enum parse_status st = get_int(cases[i], &v);
        printf("  get_int(%-24s) → %-10s", cases[i], status_name(st));
        if (st == OK) {
            printf(" out = %d", v);
        }
        printf("\n");
    }
    printf("\n");

    printf("=== ③ 朴素解法绕不过去的角落 ===\n");
    printf("  \" 42\"  → strtol 跳过前导空白后成功（返回 42）—— 与 atoi 行为一致\n");
    printf("  \"42 \"  → strtol 会在空格处停，endptr 指向空格 → 判 BAD_TOKEN\n");
    printf("            想接受尾随空白，得自己跳过 endptr 处的 isspace()\n");
    printf("  \"0x1f\" → 基数为 10 时只吃 \"0\"，endptr 指向 'x' → BAD_TOKEN\n");
    printf("            真正的十六进制要用基数 0（自动判别前缀）或 16\n");
    printf("  \"+7\"、\"-0\"、\"007\" 都是合法的，strtol 照收\n\n");

    printf("=== ④ 原书 get_num.c 的返回值语义（复刻其判据）===\n");
    printf("  它返回 int 而不是状态枚举，用「返回 -1 表示出错」的风格，\n");
    printf("  并在函数里直接 errExit(\"strtol\")，所以调用者只需处理「拿到数字」这一种情况。\n");
    printf("  两种风格都行，关键是：**必须有三态**（成功 / 非法 / 溢出），不能只有两态\n");
    return 0;
}
```

编译与运行（CE 复核，`-O0 -Wall -Wextra`，**build code = 0，diagnostics = 0**）：

```text
=== ① atoi 的问题：0 与出错无法区分 ===
  atoi(0                           ) = 0            errno = 0
  atoi(abc                         ) = 0            errno = 0
  atoi(                            ) = 0            errno = 0
  atoi(123abc                      ) = 123          errno = 0
  atoi(99999999999999999999        ) = -1           errno = 34
  atoi(-99999999999999999999       ) = 0            errno = 34
  → 前两行的返回值一模一样，都是 0；最后两行的溢出也不报错

=== ② 同一个输入用 strtol + errno + endptr 判 ===
  get_int(0                       ) → OK         out = 0
  get_int(abc                     ) → BAD_TOKEN 
  get_int(                        ) → EMPTY     
  get_int(123abc                  ) → BAD_TOKEN 
  get_int(99999999999999999999    ) → OVERFLOW  
  get_int(-99999999999999999999   ) → OVERFLOW  
  get_int( 42                     ) → OK         out = 42
  get_int(42                      ) → BAD_TOKEN 
  get_int(+7                      ) → OK         out = 7
  get_int(0x1f                    ) → BAD_TOKEN 
  get_int(+0                      ) → OK         out = 0

=== ③ 朴素解法绕不过去的角落 ===
  " 42"  → strtol 跳过前导空白后成功（返回 42）—— 与 atoi 行为一致
  "42 "  → strtol 会在空格处停，endptr 指向空格 → 判 BAD_TOKEN
            想接受尾随空白，得自己跳过 endptr 处的 isspace()
  "0x1f" → 基数为 10 时只吃 "0"，endptr 指向 'x' → BAD_TOKEN
            真正的十六进制要用基数 0（自动判别前缀）或 16
  "+7"、"-0"、"007" 都是合法的，strtol 照收
```

### 逐条解读

| 输入 | `atoi` 给什么 | 三态判定给什么 | 说明 |
|------|--------------|----------------|------|
| `"0"` | `0`，`errno=0` | `OK`，`out = 0` | **`atoi("0")` 和 `atoi("abc")` 返回值完全相同**——这是本节的核心证据 |
| `"abc"` | `0`，`errno=0` | `BAD_TOKEN` | `strtol` 的 `endptr == str`（一个数字字符都没吃） |
| `""` | `0` | `EMPTY` | 空串是**第四态**：不是「解析失败」而是「参数根本没给」 |
| `"99999999999999999999"` | `-1`，`errno=34` | `OVERFLOW` | 注意：`atoi` 在 glibc 里**只是转发了 `strtol` 的 `errno`**，它自己的返回值毫无意义 |
| `"123abc"` | `123` | `BAD_TOKEN` | `atoi` 静默吃掉前缀；三态判定看 `*endptr != '\0'` 判非法 |
| `" 42"` | `42` | `OK` | `strtol` **跳过前导空白**（POSIX 规定）——与 `atoi` 一致 |
| `"42 "` | `42` | `BAD_TOKEN` | `strtol` 在空格处停，`endptr` 指向空格。想接受尾随空白要自己 `isspace()` 跳过 |
| `"0x1f"` | `0` | `BAD_TOKEN` | 基数为 10 时只吃 `"0"`。要认十六进制得传基数 **0**（自动判别 `0x` 前缀）或 16 |
| `"+7"` / `"+0"` | `7` / `0` | `OK` | 前导 `+` 合法 |

> 原书 `get_num.c` 的返回值语义与本 demo 的**状态枚举**不同：原书返回 `int`，出错时**直接调 `errExit("strtol")` 把进程结束掉**，所以调用者只需处理「拿到数字」这一种情况。两种风格都行，但**必须有三态**（成功 / 非法 / 溢出），只有两态就一定会在某个输入上把错误当成合法值。

## 5. §3.5.2 工具 B：`error_functions` 与 `ename.c.inc`

原书 `error_functions.[hc]`（Listing 3-2 / 3-3）提供六个函数，全部**往 stderr 打印并结束进程**：

| 函数 | 打印什么 | 退出方式 |
|------|----------|----------|
| `errExit(msg)` | `prog: msg: strerror(errno)` | `exit(EXIT_FAILURE)` |
| `err_exit(msg)` | 同上 | **`_exit()`**（不 flush stdio、不跑 `atexit`） |
| `errExitEN(errnum, msg)` | 用**显式给的** `errnum` 而非当前 `errno` | `exit` |
| `fatal(msg)` | `prog: msg`（**不带** errno 描述） | `exit` |
| `usageErr(fmt, ...)` | `Usage: ...` | `exit` |
| `cmdLineErr(fmt, ...)` | `Command-line usage error: ...` | `exit` |

配套的 `ename.c.inc`（Listing 3-4）是一张 `errno → 名字` 的静态数组，理由是：某些平台的 `strerror()` 只覆盖极少数 `errno`，其余返回 `"Unknown error N"`；原书要让示例在任何 Unix 上都打印出像样的名字，于是自带一张表。

### 代码示例 C：把「会自杀的函数」抓回来

难点很直白——这些函数**都会把进程结束掉**，没法「依次调用看输出」。本 demo 用 `fork` + 管道把子进程的 stdout/stderr 一起捞回来：

源码：[../code/c3_8_error_functions.c](../code/c3_8_error_functions.c)

```c
/* TLPI 第 03 章 §3.5.2 —— 原书 lib/error_functions.[hc]（Listing 3-2 / 3-3）
 * 的六个函数各自解决什么，以及它为什么要自带一张 errno 名字表（ename.c.inc）
 *
 * 六个函数的语义差别（照原书）：
 *   errExit(msg)            打印 "prog: msg: strerror(errno)" 到 stderr，exit(EXIT_FAILURE)
 *   err_exit(msg)           同上，但用 _exit()（不 flush stdio、不跑 atexit）
 *   errExitEN(errnum, msg)  用**显式**给定的 errno 号，而不是当前 errno
 *   fatal(msg)              打印 "prog: msg"（**不带** errno），exit
 *   usageErr(fmt, ...)      打印 "Usage: ..." 到 stderr，exit(EXIT_FAILURE)
 *   cmdLineErr(fmt, ...)    打印 "Command-line usage error: ..." 到 stderr，exit
 *
 * 难点：这些函数**都会把进程结束掉**，所以不能「依次调用看输出」。
 * 本 demo 用 fork + 管道把子进程的 stdout/stderr 都抓回来，于是每个函数的真实
 * 输出和真实退出码都能并排打印。
 *
 * 这里还顺带踩了两个真实的坑（都做成了独立段落）：
 *   坑 1  父进程从管道里「只 read 一次就 close」→ 子进程后续的 write 收到
 *         SIGPIPE(13) 直接被杀死，抓回来的输出全是残句。正确姿势是读到 EOF。
 *         demo 的第一版就是这么挂的：七个函数里六个变成「被信号 13 终止」，
 *         只有 fatal 侥幸活下来，整张表失去意义。
 *   坑 2  err_exit 用 _exit()，不 flush stdio → 崩溃路径上写进 stdout 缓冲的
 *         日志会**直接蒸发**。
 *
 * 编译：gcc -O0 -Wall -Wextra -o c3_8 c3_8_error_functions.c
 */
#define _GNU_SOURCE
#include <errno.h>
#include <signal.h>
#include <stdarg.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/wait.h>
#include <unistd.h>

static const char *progname = "c3_8";

/* ---------- 原书 error_functions.h 的精简复刻 ---------- */

static void fatal(const char *fmt, ...)
{
    va_list ap;
    fflush(stdout);
    fprintf(stderr, "%s: ", progname);
    va_start(ap, fmt);
    vfprintf(stderr, fmt, ap);
    va_end(ap);
    fprintf(stderr, "\n");
    exit(EXIT_FAILURE);
}

static void errExit(const char *fmt, ...)
{
    va_list ap;
    int err = errno;                 /* 先存下来：后面 fprintf 自己可能改 errno */
    fflush(stdout);
    fprintf(stderr, "%s: ", progname);
    va_start(ap, fmt);
    vfprintf(stderr, fmt, ap);
    va_end(ap);
    fprintf(stderr, ": %s\n", strerror(err));
    exit(EXIT_FAILURE);
}

/* 原书的 errExitEN：显式指定 errno 号，用于「错误来自函数返回值而非 errno」的场合 */
static void errExitEN(int errnum, const char *fmt, ...)
{
    va_list ap;
    fflush(stdout);
    fprintf(stderr, "%s: ", progname);
    va_start(ap, fmt);
    vfprintf(stderr, fmt, ap);
    va_end(ap);
    fprintf(stderr, ": %s\n", strerror(errnum));
    exit(EXIT_FAILURE);
}

static void err_exit(const char *fmt, ...)
{
    va_list ap;
    int err = errno;
    fprintf(stderr, "%s: ", progname);
    va_start(ap, fmt);
    vfprintf(stderr, fmt, ap);
    va_end(ap);
    fprintf(stderr, ": %s\n", strerror(err));
    _exit(EXIT_FAILURE);             /* 与 errExit 的唯一区别：不跑 atexit、不 flush */
}

static void usageErr(const char *fmt, ...)
{
    va_list ap;
    fflush(stdout);
    fprintf(stderr, "Usage: ");
    va_start(ap, fmt);
    vfprintf(stderr, fmt, ap);
    va_end(ap);
    fprintf(stderr, "\n");
    exit(EXIT_FAILURE);
}

static void cmdLineErr(const char *fmt, ...)
{
    va_list ap;
    fflush(stdout);
    fprintf(stderr, "Command-line usage error: ");
    va_start(ap, fmt);
    vfprintf(stderr, fmt, ap);
    va_end(ap);
    fprintf(stderr, "\n");
    exit(EXIT_FAILURE);
}

/* ---------- 把「会自杀的函数」关进子进程，抓回它的输出与退出码 ---------- */

typedef void (*errfn)(void);

static void c_msg_enoent(void) { errno = ENOENT; errExit("open"); }
static void c_msg_eacces(void) { errno = EACCES; errExit("open %s", "/etc/shadow"); }
static void c_en(void)         { errExitEN(ENOSYS, "pthread_style_call"); }
static void c_fatal(void)      { fatal("unexpected state: counter went negative"); }
static void c_usage(void)      { usageErr("%s [-a] [-b value] file...", progname); }
static void c_cmdline(void)    { cmdLineErr("option '-b' requires an argument"); }
static void c_err_exit(void)   { errno = ENOSPC; err_exit("write"); }

/* 这两个用来对比 exit() 与 _exit() 对 stdio 缓冲的不同态度：
 * 都先往 stdout 写一段标记，但子进程的 stdout 已被重定向到**管道** → 全缓冲，
 * 不 flush 就看不到。
 * 注意这里**不能**用 setvbuf 去改缓冲模式：流已经被用过，此时再 setvbuf 是 UB，
 * glibc 直接当没听见。本 demo 的正确做法是反过来 —— 全局**不**给 stdout 设
 * _IONBF（见 main 顶部注释），让它在「非 tty」的默认判定下就是全缓冲。 */
static void c_flush_via_errExit(void)
{
    fputs("MARKER-from-stdout-buffer\n", stdout);
    errno = ENOENT;
    errExit("flush-demo");           /* 里面那句 fflush(stdout) 会把它冲出来 */
}

static void c_flush_via_err_exit(void)
{
    fputs("MARKER-from-stdout-buffer\n", stdout);
    errno = ENOENT;
    err_exit("flush-demo");          /* 没有 fflush，_exit 直接丢缓冲 */
}

/* 子进程里跑 fn（它不会返回），父进程把它的 stdout+stderr 一起捞回来。
 * ⚠️ 关键：必须一直 read 到返回 0（EOF）才能 close —— EOF 意味着子进程
 *    那一端的 fd 已全部关闭。中途 close 会让还在写的子进程收到 SIGPIPE。 */
static void capture(const char *tag, void (*fn)(void))
{
    int fds[2];
    if (pipe(fds) != 0) {
        printf("  [%s] pipe 失败\n", tag);
        return;
    }
    fflush(NULL);                    /* fork 前清缓冲，否则子进程会重复输出一遍 */

    pid_t pid = fork();
    if (pid == 0) {                  /* ---- 子进程 ---- */
        close(fds[0]);
        dup2(fds[1], STDOUT_FILENO);
        dup2(fds[1], STDERR_FILENO);
        close(fds[1]);
        fn();                        /* 这里不会返回 */
        _exit(127);
    }
    /* ---- 父进程 ---- */
    close(fds[1]);
    char buf[512];
    size_t got = 0;
    for (;;) {
        char chunk[256];
        ssize_t n = read(fds[0], chunk, sizeof chunk);
        if (n <= 0) {                /* n == 0 就是 EOF：可以安全关闭了 */
            break;
        }
        size_t room = sizeof buf - 1 - got;
        size_t take = (size_t) n < room ? (size_t) n : room;
        memcpy(buf + got, chunk, take);
        got += take;
    }
    close(fds[0]);
    buf[got] = '\0';
    for (size_t i = 0; i < got; i++) {   /* 多行也排成一条记录，方便并排看 */
        if (buf[i] == '\n') {
            buf[i] = ' ';
        }
    }

    int status = 0;
    waitpid(pid, &status, 0);
    if (WIFEXITED(status)) {
        printf("  %-12s exit(%d)  输出: %s\n", tag, WEXITSTATUS(status), buf);
    } else if (WIFSIGNALED(status)) {
        printf("  %-12s 被信号 %d 终止  输出: %s\n", tag, WTERMSIG(status), buf);
    }
}

/* ---------- 附：读端到底什么时候能关 ---------- */

static void pipe_close_order(void)
{
    printf("=== 附：抓子进程输出时，「管道读端什么时候能关」===\n");
    /* 子进程故意写 25×8192 = 204800 字节，远超管道容量（64 KiB）→ 必然写到阻塞，
     * 于是「提前 close」这一方的错误必定暴露，不依赖时序运气。 */
    for (int mode = 0; mode < 2; mode++) {
        int fds[2];
        if (pipe(fds) != 0) {
            return;
        }
        fflush(NULL);
        pid_t pid = fork();
        if (pid == 0) {
            close(fds[0]);
            dup2(fds[1], STDERR_FILENO);
            close(fds[1]);
            char blk[8192];
            memset(blk, 'x', sizeof blk);
            for (int i = 0; i < 25; i++) {
                if (write(STDERR_FILENO, blk, sizeof blk) < 0) {
                    break;           /* 只有在 SIGPIPE 被忽略时才会走到这里 */
                }
            }
            _exit(1);
        }
        close(fds[1]);
        char blk[8192];
        size_t got = 0;
        if (mode == 0) {
            for (;;) {                       /* 正确：读到 EOF */
                ssize_t n = read(fds[0], blk, sizeof blk);
                if (n <= 0) {
                    break;
                }
                got += (size_t) n;
            }
        } else {
            ssize_t n = read(fds[0], blk, sizeof blk);   /* 错误：只读一次就关 */
            got = n > 0 ? (size_t) n : 0;
        }
        close(fds[0]);
        int status = 0;
        waitpid(pid, &status, 0);
        const char *name = mode == 0 ? "读到 EOF 再关" : "读一次就关";
        if (WIFEXITED(status)) {
            printf("  %-14s → 子进程 exit(%d)，抓到 %zu 字节\n", name, WEXITSTATUS(status), got);
        } else if (WIFSIGNALED(status)) {
            printf("  %-14s → 子进程被信号 %d 终止，只抓到 %zu 字节\n", name,
                   WTERMSIG(status), got);
        }
    }
    printf("    教训：只要读端一关，还在写的子进程立刻收到 SIGPIPE(13)，默认处置是\n"
           "          终止进程 —— 于是你抓回来的是半句话，而且看不出任何报错。\n"
           "          正确姿势：read() 返回 0（EOF）才说明子进程那一端已经关干净，\n"
           "          此时再 close 是安全的。\n"
           "          同一个道理在服务端更致命：对端断开后自己再 write → SIGPIPE\n"
           "          把服务进程写死，所以启动时该 signal(SIGPIPE, SIG_IGN)。\n\n");
}

/* ---------- ename.c.inc 的意义：strerror 覆盖不到时怎么办 ---------- */

static void strerror_coverage(void)
{
    int unknown = 0, total = 0;
    printf("=== 扫 errno 1..200，看 strerror 的覆盖率 ===\n");
    for (int e = 1; e <= 200; e++) {
        const char *s = strerror(e);
        total++;
        if (strncmp(s, "Unknown error", 13) == 0) {
            unknown++;
        } else if (e <= 12 || e == 38 || e == 95 || e == 110 || e == 111) {
            printf("  errno %3d → %s\n", e, s);
        }
    }
    printf("  ...（1..200 中认得 = %d/%d，返回 \"Unknown error\" 的 = %d）\n\n",
           total - unknown, total, unknown);
    printf("  原书为什么要自带 ename.c.inc：\n");
    printf("    1. 有些平台的 strerror() 只覆盖极少数 errno，其余返回 \"Unknown error N\"，\n"
           "       而原书要让示例在任何 UN*X 上都打印出像样的名字，所以自带一张表；\n"
           "    2. 那张表是按 errno 值直接建索引的静态数组，取名字比 strerror 更快\n"
           "       （没有 locale 查询、没有函数调用）—— HFT 的错误日志路径里这点很实际；\n"
           "    3. 它用 #ifdef 把本平台不存在的 errno 整块剪掉，所以同一份表能跨平台编译。\n\n");
}

int main(void)
{
    /* 这里**故意不**调用 setvbuf(stdout, NULL, _IONBF, 0)。
     * 原因有两层：
     *   1. 本容器里 stdout 是 socket，不是 tty → 默认全缓冲，正好用来演示
     *      「缓冲没冲刷就 _exit() 会丢日志」；
     *   2. 每次 fork 之前都会 fflush(NULL)，所以缓冲里不会有残留被复制成两份。
     * 在别处写 demo 时若为了行序好看而设 _IONBF，这个「丢日志」现象就消失了。 */
    strerror_coverage();

    printf("=== 六个函数的真实输出与退出码（各关在一个子进程里）===\n");
    capture("errExit", c_msg_enoent);
    capture("errExit+fmt", c_msg_eacces);
    capture("errExitEN", c_en);
    capture("fatal", c_fatal);
    capture("usageErr", c_usage);
    capture("cmdLineErr", c_cmdline);
    capture("err_exit", c_err_exit);

    printf("\n=== err_exit 的 _exit() 到底少做了什么（同一个标记，两种结局）===\n");
    capture("errExit", c_flush_via_errExit);
    capture("err_exit", c_flush_via_err_exit);
    printf("    ↑ 两行都把 \"MARKER-from-stdout-buffer\" 写进了 stdout 缓冲；\n"
           "      errExit 里有 fflush(stdout) → 标记出现；\n"
           "      err_exit 走 _exit() → 缓冲里的字节随进程一起消失。\n"
           "      实战含义：崩溃路径上想留下最后一句话，就别用 _exit()，\n"
           "      或者自己先 fflush(NULL)。\n\n");

    pipe_close_order();

    printf("=== 读表要点 ===\n");
    printf("  · errExit / err_exit 多打一段 \": 错误描述\"，fatal / usageErr / cmdLineErr 不打\n");
    printf("  · 全部用 EXIT_FAILURE(=1) 退出，不是 0 —— 脚本才能用 $? 判失败\n");
    printf("  · 全部写 **stderr** 而不是 stdout，所以重定向 stdout 也不会丢错误\n");
    printf("  · err_exit 的 _exit() 跳过 atexit 与 stdio flush —— 想「立刻死、别做收尾」时用它，\n"
           "    但代价是缓冲里的日志一起蒸发\n");
    printf("  · errExitEN 的存在理由：pthread_* 这类 API 的失败码是**返回值**，errno 根本没动，\n"
           "    拿 errExit 只会报出一个无关的错误原因\n");
    return 0;
}
```

编译与运行（CE 复核，`-O0 -Wall -Wextra`，**build code = 0，diagnostics = 0**）：

```text
=== 扫 errno 1..200，看 strerror 的覆盖率 ===
  errno   1 → Operation not permitted
  errno   2 → No such file or directory
  errno   3 → No such process
  errno   4 → Interrupted system call
  errno   5 → Input/output error
  errno   6 → No such device or address
  errno   7 → Argument list too long
  errno   8 → Exec format error
  errno   9 → Bad file descriptor
  errno  10 → No child processes
  errno  11 → Resource temporarily unavailable
  errno  12 → Cannot allocate memory
  errno  38 → Function not implemented
  errno  95 → Operation not supported
  errno 110 → Connection timed out
  errno 111 → Connection refused
  ...（1..200 中认得 = 131/200，返回 "Unknown error" 的 = 69）

  原书为什么要自带 ename.c.inc：
    1. 有些平台的 strerror() 只覆盖极少数 errno，其余返回 "Unknown error N"，
       而原书要让示例在任何 UN*X 上都打印出像样的名字，所以自带一张表；
    2. 那张表是按 errno 值直接建索引的静态数组，取名字比 strerror 更快
       （没有 locale 查询、没有函数调用）—— HFT 的错误日志路径里这点很实际；
    3. 它用 #ifdef 把本平台不存在的 errno 整块剪掉，所以同一份表能跨平台编译。
```

```text
=== 六个函数的真实输出与退出码（各关在一个子进程里）===
  errExit      exit(1)  输出: c3_8: open: No such file or directory 
  errExit+fmt  exit(1)  输出: c3_8: open /etc/shadow: Permission denied 
  errExitEN    exit(1)  输出: c3_8: pthread_style_call: Function not implemented 
  fatal        exit(1)  输出: c3_8: unexpected state: counter went negative 
  usageErr     exit(1)  输出: Usage: c3_8 [-a] [-b value] file... 
  cmdLineErr   exit(1)  输出: Command-line usage error: option '-b' requires an argument 
  err_exit     exit(1)  输出: c3_8: write: No space left on device 

=== err_exit 的 _exit() 到底少做了什么（同一个标记，两种结局）===
  errExit      exit(1)  输出: MARKER-from-stdout-buffer c3_8: flush-demo: No such file or directory 
  err_exit     exit(1)  输出: c3_8: flush-demo: No such file or directory 
```

```text
=== 附：抓子进程输出时，「管道读端什么时候能关」===
  读到 EOF 再关 → 子进程 exit(1)，抓到 204800 字节
  读一次就关 → 子进程被信号 13 终止，只抓到 8192 字节
```

### 逐条解读

| 读数 | 说明 |
|------|------|
| `errExit` / `errExit+fmt` 打的是 `prog: msg: 描述` | 三个部分用 `: ` 拼：程序名、调用点描述、`strerror(errno)`。**`errExit+fmt` 那行带上了业务上下文**（`/etc/shadow` 是哪个路径），这正是 `perror` 给不了的（见 [3.4](3.4-error-handling.md) 误用 6） |
| `errExitEN` 报的是 `Function not implemented` | 用的是**显式传入**的 `ENOSYS`，而不是当前 `errno`。用途：`pthread_*` 这类 API 把失败码当**返回值**返回、根本不碰 `errno`，拿 `errExit` 只会报出一个无关的原因 |
| `fatal` / `usageErr` / `cmdLineErr` 都**不带** `strerror` 段 | 它们描述的不是「系统调用失败」而是「程序自己发现的问题」，`errno` 没有意义 |
| 六个函数（含 `err_exit`）全部 `exit(1)` | `exit(EXIT_FAILURE)` = 1，**不是 0**——否则 shell 脚本的 `$?` 判不出失败 |
| 全部写 **stderr** | 所以 `./prog > out.txt` 不会把错误信息也重定向走 |
| **MARKER 对照实验** | 两行都先 `fputs("MARKER-from-stdout-buffer\n", stdout)`。`errExit` 里有 `fflush(stdout)` → **标记出现**；`err_exit` 走 `_exit()` → **标记消失**，缓冲里的字节随进程一起蒸发 |
| `读到 EOF 再关` → 抓到 **204800** 字节 | 子进程写 25×8192 字节，超过 64 KiB 管道容量，必然写到阻塞 |
| `读一次就关` → 子进程**被信号 13 终止**，只抓到 8192 字节 | 读端一关，还在写的子进程立刻收到 `SIGPIPE(13)`，默认处置是**终止进程**。于是你抓回来的是半句话，**而且看不出任何报错** |

> **这两个坑都是真实踩出来的**（脚本注释里有记录）：demo 第一版「只 `read` 一次就 `close`」，结果七个函数里**六个变成「被信号 13 终止」**，只有 `fatal` 侥幸活下来，整张表失去意义。正确姿势是**一直 `read` 到返回 0（EOF）**——EOF 意味着子进程那一端的 fd 已经全部关干净，此时 `close` 才安全。
>
> 同一个道理在服务端更致命：对端断开后自己再 `write` → `SIGPIPE` 把服务进程写死。所以网络服务启动时该 `signal(SIGPIPE, SIG_IGN)`（详见 [3.1](3.1-system-calls.md) ⑧ 段）。

## 6. HFT / 嵌入式关联

| 现象 | 本节内容的兑现 |
|------|----------------|
| 崩溃路径留不下日志 | `err_exit` 用 `_exit()` **不 flush stdio**——本 demo 的 MARKER 实验直接证明了缓冲里的字节会消失。要保证「最后一句一定写出去」，用 `exit()` 或自己 `fflush(NULL)` |
| 错误日志缺上下文 | `perror` 只给「哪一步 + 什么错」；`errExit("open %s", path)` 这种带 `fmt` 的写法才带上「哪个路径、哪个 fd」 |
| 错误名字路径要快 | `ename.c.inc` 是**按 `errno` 直接建索引的静态数组**，没有 locale 查询、没有函数调用。高频错误日志里 `strerror()` 的开销是真实的 |
| 解析配置/命令行要严 | 用 `getInt` 式的**三态**解析（成功/非法/溢出），别用 `atoi`——`atoi` 把「0」和「出错」混成同一个返回值 |
| 脚本化验收 | `exit(EXIT_FAILURE)`(=1) + 写 stderr，是「错误可被自动化发现」的最小要求 |
| 抓子进程输出 | 必须读到 EOF 再 `close`，否则 `SIGPIPE` 会杀掉还在写的子进程（本节实测）。嵌入式里跑交叉编译 / 上游日志收集时同样适用 |

## 7. 衔接

- 本章 README：[README](../README.md)
- 上一节：[3.4 Handling Errors from System Calls and Library Functions 错误处理](3.4-error-handling.md) —— 六条 `errno` 规则 + `EFAULT`
- 下一节：[3.6 Portability Issues 可移植性](3.6-portability.md) —— 功能测试宏与系统数据类型
- 机制回看：[3.1 System Calls 系统调用](3.1-system-calls.md) —— 寄存器约定、`copy_from_user`、`EFAULT`
- 内核视角：[05/Ch5.1 与内核通信](../../../05-linux-kernel/chapter-05-system-calls/notes/section-5.1-与内核通信.md) —— libc ≠ syscall
- 官方源码清单：[man7 — List of source code files, by chapter](https://man7.org/tlpi/code/online/all_files_by_chapter.html) —— 本节的 Listing 编号出处
- TLPI 后续：[Ch4 通用 I/O 模型](../../chapter-04-file-io-universal/README.md)（`copy.c` = Listing 4-1，用的就是本节这套骨架）

---

<details>
<summary>代码自测（Q&A）</summary>

**Q1：原书第 3 章的 6 个 Listing 分别是什么？**
A：`3-1` = `lib/tlpi_hdr.h`（公共头文件）；`3-2` = `lib/error_functions.h`；`3-3` = `lib/error_functions.c`；`3-4` = `lib/ename.c.inc`（`errno`→名字静态表）；`3-5` = `lib/get_num.h`；`3-6` = `lib/get_num.c`。另外 `lib/alt_functions.[hc]` 与 `progconc/syscall_speed.c` **没有 Listing 编号**。最容易记错的是：3-1 是 `tlpi_hdr.h` 而不是 `error_functions.h`，`get_num.h` 才是 3-5。

**Q2：`argc` / `argv` 的形状是什么？遍历为什么可以不看 `argc`？**
A：`argv` 是一个**以 `NULL` 结尾的字符串数组**，`argv[0]` 是程序名，`argv[1..argc-1]` 是参数，`argv[argc]` 被标准保证为 `NULL`。本 demo 实测 `argc = 1` 时 `argv[1] = NULL`。因为有这个哨兵，遍历可以写 `for (char **p = argv; *p; p++)` 而不需要 `argc`。

**Q3：`getopt` 怎么区分「未知选项」和「选项缺参数」？**
A：**默认（`optstring` 不以 `:` 开头）下它不区分**——两者都返回 `'?'`。本 demo 的实测就暴露了这一点：`-b` 缺参数时打印的是「非法选项 '-b'（未知选项）」，因为 demo 的 `if (optopt)` 分支把两者混成了一个说法。正确做法是 **`optstring` 开头加冒号**（`":ab:c"`）：缺参数返回 `':'`，未知选项返回 `'?'`。另外 `getopt` 的解析状态（`optind`/`optarg`/`optopt`）是**全局的**，同一进程里第二次解析前必须 `optind = 1` 复位。

**Q4：为什么原书要自己写 `get_num.c`，不用 `atoi`？**
A：因为 `atoi(3)` **没有错误返回通道**：`atoi("0")` 和 `atoi("abc")` 都返回 `0`，溢出时也只给一个截断值（本 demo 实测 `atoi("99999999999999999999") = -1`，`errno=34`，但这个 `errno` 是它内部转发的 `strtol` 留下的，`atoi` 的返回值本身毫无意义）。原书要的**三态**（成功 / 非法 / 溢出）只有 `strtol` + `errno` + `endptr` 组合才能给。

**Q5：`strtol` 处理 `" 42"`、`"42 "`、`"0x1f"` 分别是什么结果？**
A：`" 42"` → **成功**（`strtol` 跳过前导空白，POSIX 规定）；`"42 "` → **`BAD_TOKEN`**（在空格处停，`endptr` 指向空格；想接受尾随空白要自己 `isspace()` 跳过）；`"0x1f"` → **`BAD_TOKEN`**（基数为 10 时只吃 `"0"`，`endptr` 指向 `'x'`；要认十六进制得传基数 **0** 或 16）。三者都是本 demo 实测。

**Q6：`err_exit` 和 `errExit` 的区别是什么？**
A：唯一的区别是退出方式——`errExit` 用 `exit()`，`err_exit` 用 `_exit()`。`exit()` 会 flush stdio 缓冲并跑 `atexit` 处理器；`_exit()` 直接让进程消失，**缓冲里的日志一起丢掉**。本 demo 的 MARKER 实验给出了直接证据：同样先往 stdout 写一段标记，`errExit` 那行标记出现，`err_exit` 那行标记消失。崩溃路径上想留下最后一句话，就别用 `_exit()`。

**Q7：为什么 demo 里「抓子进程输出」必须先 `read` 到 EOF 再 `close`？**
A：因为读端一关，还在写的子进程立刻收到 `SIGPIPE(13)`，默认处置是**终止进程**——于是抓回来的是半句话，而且看不出任何报错。本 demo 的附段用「子进程写 204800 字节（超过 64 KiB 管道容量，必然阻塞）」把错误逼出来：读到 EOF 再关能抓满 204800 字节，读一次就关只抓到 8192 字节且子进程被信号 13 杀掉。

</details>
