# 3.2 Library Functions 库函数

> 本章：[TLPI 第 03 章 — System Programming Concepts](../README.md)
> 实测核验：Compiler Explorer gcc 13.3.0 / glibc 2.39（`/proc/self/io` 计数器）
> - 上一节：[3.1 System Calls 系统调用](3.1-system-calls.md)
> - 下一节：[3.3 The Standard C Library; The GNU C Library (glibc)](3.3-glibc.md)

---

## 本节讲什么

「库函数不是系统调用」这句话人人会说，但多数人只是记住了结论。本节把它**量出来**：用 `/proc/self/io` 的 `syscr`/`syscw` 计数器（进程累计发起的 read/write 系统调用**次数**）直接看每一次调用有没有进内核。

三个要点：

1. 库函数与系统调用的**方向**：C 代码 → glibc 库 → 系统调用 → 内核。
2. 「不进内核」在计数器上长什么样——以及这个测量方法本身的两个陷阱（底噪、看不见的 `brk/mmap`）。
3. 一个活教材：**库函数的封装会附加你没写出来的 flag**，所以同一文件 `open()` 能开、`fopen()` 不能开。

---

## 1. 三个角色，不要混在一起

| 角色 | 层面 | 代表函数 | 谁定义 |
|------|------|----------|--------|
| **ISO-C 标准库** | 语言层面 | `fopen()` / `printf()` / `malloc()` | C 语言标准（ISO/IEC 9899） |
| **POSIX 系统调用** | 操作系统接口层面 | `open` / `read` / `write` | POSIX 规范 |
| **glibc / musl** | 用户态中间层（桥梁） | 上面两者的具体实现 | 各 C 库项目 |

**核心一句话**：C 语言本身**没有**任何操作文件、进程、网络的内置语法——没有 `openfile` 关键字，没有 `fork` 运算符。全部靠库函数，库再去请求内核。

调用链条（Linux）：

```
你写的应用 C 代码
        │  调用
        ▼
【glibc】
   ├─ 情况 A：调标准 C 函数 fopen()
   │      → glibc 做用户态缓冲管理 → 内部再调 open() 系统调用
   └─ 情况 B：直接调 POSIX 接口 open()
          → glibc 薄薄一层封装 → 发 syscall 指令
                │
                ▼
        内核真正干活（打开文件）
                │
                ▼
        结果返回 glibc → 返回你的代码
```

> 方向千万别搞反：**是 C 库去调用系统调用**，不是系统调用调用 C 语言。

## 2. 库函数的三种类型

| 类型 | 例子 | 是否进内核 |
|------|------|------------|
| **纯用户态** | `strcpy` / `strlen` / `atoi` / `snprintf` | **否**（一次都不进） |
| **薄封装** | `read` / `write` / `open` | 一次 syscall |
| **多层** | `fopen` / `fprintf` / `fclose` | 内部 `open` + 用户态缓冲管理，flush 时才产生 `write` |
| **走外部数据的** | `getpwnam` / `gethostbyname` | **每次都要读文件/网络**（走 NSS），见下面的实测 |

## 3. 代码示例：把「不进内核」量出来

源码：[../code/c3_2_lib_vs_syscall.c](../code/c3_2_lib_vs_syscall.c)

```c
/* TLPI 第 03 章 §3.2 —— 「库函数 ≠ 系统调用」的可测量版
 *
 * 口头说「atoi 不进内核、getpwuid 会读文件」很容易，本 demo 用 /proc/self/io
 * 的计数器把它**量出来**：
 *   rchar / wchar = 进程累计读/写的字节数
 *   syscr / syscw = 进程累计发起的 read / write 系统调用**次数**
 *
 * 方法论（很重要，否则读数会骗你）：
 *   读 /proc/self/io 这个动作自身就要 open + read + close，所以每测一次都自带
 *   一份「底噪」。本 demo 第一件事就是把这个底噪量出来，之后所有数字都与它对比，
 *   而不是假装底噪是 0。
 *   另外 /proc/self/io 只统计 read/write 这一类 I/O 系统调用 —— brk / mmap /
 *   ioctl 它看不见。所以它证明的是「没做 I/O」，不是「没进内核」。
 *
 * 编译：gcc -O0 -Wall -Wextra -o c3_2 c3_2_lib_vs_syscall.c
 */
#define _GNU_SOURCE
#include <errno.h>
#include <fcntl.h>
#include <pwd.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

struct io_counters {
    unsigned long long rchar, wchar, syscr, syscw;
};

static struct io_counters before, after;
static unsigned long long sink;
static char userbuf[64] = "ce";
static struct passwd *pw;
static FILE *out;

static int read_io(struct io_counters *io)
{
    FILE *f = fopen("/proc/self/io", "r");
    if (!f) {
        return -1;
    }
    char line[128];
    io->rchar = io->wchar = io->syscr = io->syscw = 0;
    while (fgets(line, sizeof line, f)) {
        sscanf(line, "rchar: %llu", &io->rchar);
        sscanf(line, "wchar: %llu", &io->wchar);
        sscanf(line, "syscr: %llu", &io->syscr);
        sscanf(line, "syscw: %llu", &io->syscw);
    }
    fclose(f);
    return 0;
}

/* 先把两次快照都取完，再打印 —— 打印本身也会产生 write 系统调用，
 * 若边打边测，就把自己的输出算进被测对象里了。 */
static void measure(const char *tag, void (*work)(void))
{
    if (read_io(&before) < 0) {
        printf("  %-34s /proc/self/io 不可读（errno=%d）\n", tag, errno);
        return;
    }
    work();
    if (read_io(&after) < 0) {
        return;
    }
    printf("  %-34s Δsyscr=%-3lld Δsyscw=%-3lld Δrchar=%-9lld Δwchar=%lld\n", tag,
           (long long) (after.syscr - before.syscr),
           (long long) (after.syscw - before.syscw),
           (long long) (after.rchar - before.rchar),
           (long long) (after.wchar - before.wchar));
}

/* ---------- 被测的五种「工作负载」 ---------- */

static void w_nothing(void)                 /* 空负载 → 量出底噪 */
{
}

static void w_userlib(void)                 /* 纯用户态：解析、拷贝、求长度 */
{
    char buf[256];
    for (int i = 0; i < 100000; i++) {
        strcpy(buf, "abcdef");
        sink += strlen(buf) + (unsigned long long) atoi("12345");
    }
}

static void w_snprintf(void)                /* 同样是「格式化」，但不落到任何 fd */
{
    char buf[64];
    for (int i = 0; i < 1000; i++) {
        snprintf(buf, sizeof buf, "%d-%s", i, "x");
        sink += (unsigned long long) buf[0];
    }
}

static void w_getpwnam(void)                /* 走 NSS 的查找：真的去读文件 */
{
    pw = getpwnam(userbuf);
}

static void w_malloc(void)                  /* 堆操作：brk/mmap 不在 /proc/self/io 视野里 */
{
    for (int i = 0; i < 1000; i++) {
        void *p = malloc(64);
        sink += (unsigned long long) (p != NULL);
        free(p);
    }
}

static void w_fprintf_buffered(void)        /* 100 次 fprintf，故意不 fflush */
{
    for (int i = 0; i < 100; i++) {
        fprintf(out, "x");
    }
}

static void w_fflush(void)                  /* 把用户态缓冲真正交出去 */
{
    fflush(out);
}

int main(void)
{
    setvbuf(stdout, NULL, _IONBF, 0);

    /* 先从 /etc/passwd 里自己读一个**真实存在**的用户名。
     * 不能写死 getuid()：本容器里 uid 0 并不在 /etc/passwd 中，
     * getpwuid(0) 会返回 NULL（那是「查无此人」，不是出错）。 */
    FILE *pwf = fopen("/etc/passwd", "r");
    if (pwf) {
        char line[256];
        if (fgets(line, sizeof line, pwf)) {
            char *colon = strchr(line, ':');
            if (colon) {
                *colon = '\0';
                size_t n = strlen(line);
                if (n > sizeof userbuf - 1) {      /* 手工截断，避免 -Wformat-truncation */
                    n = sizeof userbuf - 1;
                }
                memcpy(userbuf, line, n);
                userbuf[n] = '\0';
            }
        }
        fclose(pwf);
    }
    printf("=== 0. 本环境参数 ===\n");
    printf("  euid=%d uid=%d，/etc/passwd 第一条记录的用户名 = \"%s\"\n\n",
           (int) geteuid(), (int) getuid(), userbuf);

    /* ④ 要用的可写 FILE*：/dev/null 是最好的靶子（写进去的字节数照样计入 wchar）。
     * 这里必须用 open() + fdopen()：fopen("/dev/null","w") 在本容器里会被拒
     * （它隐式带 O_CREAT|O_TRUNC），而 open(O_WRONLY) 是允许的。 */
    int dn = open("/dev/null", O_WRONLY);
    errno = 0;
    FILE *probe = fopen("/dev/null", "w");
    printf("=== 0b. 先记一个 §3.2 的活教材：库函数封装给你加了什么 ===\n");
    printf("  open(\"/dev/null\", O_WRONLY)   → %s\n", dn >= 0 ? "成功" : "失败");
    printf("  fopen(\"/dev/null\", \"w\")       → %s（errno=%d %s）\n",
           probe ? "成功" : "失败", errno, strerror(errno));
    if (probe) {
        fclose(probe);
    }
    printf("    ↑ fopen 的 \"w\" 隐含 O_WRONLY|O_CREAT|O_TRUNC，比上面多了两位 flag；\n"
           "      本环境恰好只拒绝带 O_CREAT 的打开（/dev 是受限挂载）→ 同一个文件，\n"
           "      open() 能开、fopen() 不能开。所以下面改用 fdopen 绕开这一层。\n\n");
    out = dn >= 0 ? fdopen(dn, "w") : tmpfile();
    if (!out) {
        printf("  可写 FILE* 拿不到，后面的缓冲实验跳过\n");
    }

    printf("=== 1. 底噪：读一次 /proc/self/io 自身要花几个系统调用 ===\n");
    measure("空负载（这就是底噪）", w_nothing);
    printf("\n");

    printf("=== 2. 纯用户态库函数：strcpy / strlen / atoi ×100000 ===\n");
    measure("×100000 轮", w_userlib);
    printf("    ↑ 与上一行的底噪完全相同 → 放大十万倍也没多出一次 I/O。\n"
           "      这就是「库函数不进内核」的可测量形式。\n\n");

    printf("=== 3. snprintf：同样是「格式化」，所以它当然也不进内核 ===\n");
    measure("snprintf ×1000 次到内存", w_snprintf);
    printf("    ↑ 和 fprintf 的区别不在「格式化」，只在**写给了谁**\n\n");

    printf("=== 4. getpwnam()：走 NSS 的名称解析，真的会去读 /etc/passwd ===\n");
    measure("getpwnam() 第 1 次", w_getpwnam);
    measure("getpwnam() 第 2 次", w_getpwnam);
    measure("getpwnam() 第 3 次", w_getpwnam);
    printf("  → 查到用户 = %s，uid = %d，errno = %d\n", pw ? pw->pw_name : "NULL",
           pw ? (int) pw->pw_uid : -1, errno);
    printf("    ↑ 三次的增量都一样、且都明显大于底噪 → 每次调用都重新 open+read 了\n"
           "      /etc/passwd。本环境（glibc 无 nscd/systemd-userdbd）**没有跨调用的\n"
           "      缓存**；反过来说，在 HFT 的日志热路径里调 getpwuid/getpwnam，\n"
           "      每一行日志都要多付几次 read 系统调用。\n"
           "    另注：查无此人时返回 NULL 且**不设 errno** —— 本容器 getpwuid(0) 就是\n"
           "      这种情形，别把它当成 ENOENT 那种错误。\n\n");

    printf("=== 5. stdio 的缓冲：fprintf 不立即产生 write 系统调用 ===\n");
    if (out) {
        measure("fprintf ×100 次（未 fflush）", w_fprintf_buffered);
        measure("fflush() 一次", w_fflush);
        printf("    ↑ 100 次 fprintf 的 Δsyscw 是 0，字节只进了用户态缓冲；\n"
               "      fflush 一次才把它变成 1 次 write —— 这就是「printf 慢」的真正位置。\n"
               "      注意 Δwchar：缓冲里的字节数只有真正 write 出去才计入。\n\n");
    }

    printf("=== 6. malloc/free：/proc/self/io 看不见的「内核活动」 ===\n");
    measure("malloc(64)+free ×1000", w_malloc);
    printf("    ↑ 增量与底噪相同，但这**不能**推出「malloc 没进内核」：\n"
           "      brk / mmap 不是 read/write，压根不在 /proc/self/io 的统计范围内。\n"
           "      用本文件当可移植的「库函数 vs 系统调用」证据时，只对 I/O 类调用有效。\n\n");

    printf("=== 读表要点 ===\n");
    printf("  · Δsyscr / Δsyscw 是「次数」，Δrchar / Δwchar 是「字节数」\n");
    printf("  · 每测一次自带一份底噪（读 /proc/self/io 自身），所以只能比相对差\n");
    printf("  · 同一个「格式化」动作：snprintf→内存 0 次 I/O，fprintf→FILE* 由缓冲决定\n");
    printf("  · NSS 查找一次 = 数次 read；这是 getpwuid 出现在日志热路径里的代价\n");
    if (out) {
        fclose(out);
    }
    return 0;
}
```

编译与运行（CE 复核，gcc 13.3.0 / x86-64 / Ubuntu 24.04，`-O0 -Wall -Wextra`，**build code = 0，diagnostics = 0**）：

```text
=== 0. 本环境参数 ===
  euid=0 uid=0，/etc/passwd 第一条记录的用户名 = "ce"

=== 0b. 先记一个 §3.2 的活教材：库函数封装给你加了什么 ===
  open("/dev/null", O_WRONLY)   → 成功
  fopen("/dev/null", "w")       → 失败（errno=13 Permission denied）
    ↑ fopen 的 "w" 隐含 O_WRONLY|O_CREAT|O_TRUNC，比上面多了两位 flag；
      本环境恰好只拒绝带 O_CREAT 的打开（/dev 是受限挂载）→ 同一个文件，
      open() 能开、fopen() 不能开。所以下面改用 fdopen 绕开这一层。

=== 1. 底噪：读一次 /proc/self/io 自身要花几个系统调用 ===
  空负载（这就是底噪）     Δsyscr=2   Δsyscw=0   Δrchar=97        Δwchar=0

=== 2. 纯用户态库函数：strcpy / strlen / atoi ×100000 ===
  ×100000 轮                       Δsyscr=2   Δsyscw=0   Δrchar=97        Δwchar=0
    ↑ 与上一行的底噪完全相同 → 放大十万倍也没多出一次 I/O。
      这就是「库函数不进内核」的可测量形式。

=== 3. snprintf：同样是「格式化」，所以它当然也不进内核 ===
  snprintf ×1000 次到内存       Δsyscr=2   Δsyscw=0   Δrchar=98        Δwchar=0
    ↑ 和 fprintf 的区别不在「格式化」，只在**写给了谁**

=== 4. getpwnam()：走 NSS 的名称解析，真的会去读 /etc/passwd ===
  getpwnam() 第 1 次               Δsyscr=4   Δsyscw=0   Δrchar=148       Δwchar=0
  getpwnam() 第 2 次               Δsyscr=4   Δsyscw=0   Δrchar=148       Δwchar=0
  getpwnam() 第 3 次               Δsyscr=4   Δsyscw=0   Δrchar=148       Δwchar=0
  → 查到用户 = ce，uid = 10240，errno = 0
    ↑ 三次的增量都一样、且都明显大于底噪 → 每次调用都重新 open+read 了
      /etc/passwd。本环境（glibc 无 nscd/systemd-userdbd）**没有跨调用的
      缓存**；反过来说，在 HFT 的日志热路径里调 getpwuid/getpwnam，
      每一行日志都要多付几次 read 系统调用。
    另注：查无此人时返回 NULL 且**不设 errno** —— 本容器 getpwuid(0) 就是
      这种情形，别把它当成 ENOENT 那种错误。

=== 5. stdio 的缓冲：fprintf 不立即产生 write 系统调用 ===
  fprintf ×100 次（未 fflush）  Δsyscr=2   Δsyscw=0   Δrchar=98        Δwchar=0
  fflush() 一次                    Δsyscr=2   Δsyscw=1   Δrchar=98        Δwchar=100
    ↑ 100 次 fprintf 的 Δsyscw 是 0，字节只进了用户态缓冲；
      fflush 一次才把它变成 1 次 write —— 这就是「printf 慢」的真正位置。
      注意 Δwchar：缓冲里的字节数只有真正 write 出去才计入。

=== 6. malloc/free：/proc/self/io 看不见的「内核活动」 ===
  malloc(64)+free ×1000             Δsyscr=2   Δsyscw=0   Δrchar=98        Δwchar=0
    ↑ 增量与底噪相同，但这**不能**推出「malloc 没进内核」：
      brk / mmap 不是 read/write，压根不在 /proc/self/io 的统计范围内。
      用本文件当可移植的「库函数 vs 系统调用」证据时，只对 I/O 类调用有效。

=== 读表要点 ===
  · Δsyscr / Δsyscw 是「次数」，Δrchar / Δwchar 是「字节数」
  · 每测一次自带一份底噪（读 /proc/self/io 自身），所以只能比相对差
  · 同一个「格式化」动作：snprintf→内存 0 次 I/O，fprintf→FILE* 由缓冲决定
  · NSS 查找一次 = 数次 read；这是 getpwuid 出现在日志热路径里的代价
```

### 逐条解读

| 段 | 读数 | 说明 |
|----|------|------|
| 0b | `open(O_WRONLY)` 成功、`fopen("w")` 失败 `errno=13` | **本节最值钱的一行**：差别只在 `fopen` 的 `"w"` 隐含 `O_WRONLY\|O_CREAT\|O_TRUNC`。「库函数是薄封装」不等于「库函数和系统调用一一对应」，它总会附加 flag 与语义 |
| 1 | 底噪 `Δsyscr=2` | 读 `/proc/self/io` 自身要 `open + read + close`，所以**每测一次自带一份底噪**。测出底噪再比，才叫实测 |
| 2 | `×100000` 轮，读数与底噪**完全相同** | 纯用户态函数放大十万倍也没多出一次 I/O —— 「库函数不进内核」的可测量形式 |
| 3 | `snprintf` 同样等于底噪 | 和 `fprintf` 的差别不在「格式化」，只在**写给了谁** |
| 4 | `getpwnam()` 每次 `Δsyscr=4` | 每次都重新 `open+read` `/etc/passwd`：本环境**没有跨调用的 NSS 缓存**。日志热路径里调 `getpwuid` 的代价就在这里 |
| 4 副产物 | 查到 `ce`（uid 10240），`getpwuid(0)` → `NULL` 且 `errno=0` | 「查无此人」与「出错」是两回事；`NULL` 不代表 `errno` 有意义 |
| 5 | `fprintf ×100` → `Δsyscw=0`；`fflush()` → `Δsyscw=1`、`Δwchar=100` | 100 次 `fprintf` 一次内核都没进；`fflush` 才把它变成 1 次 `write`。「printf 慢」的位置在**缓冲策略**和**字节量**，不在格式化 |
| 6 | `malloc` 等于底噪 | 但**不能**推出「malloc 没进内核」：`brk`/`mmap` 不是 read/write，不在 `/proc/self/io` 统计范围内 |

> **测量方法本身的两个陷阱**（这是本 demo 最该带走的东西）：
> ① `/proc/self/io` 只统计 read/write 这一类 I/O 系统调用，`brk`/`mmap`/`ioctl` 它看不见 → 它证明的是「没做 I/O」，不是「没进内核」；
> ② 读计数器本身要花 2 次 read → 只能比相对差，不能指望绝对值为 0。

## 4. `fopen` 模式 → `open` flags 的映射

| `fopen` 模式 | `open` 对应的 flags | 说明 |
|---|---|---|
| `"r"` | `O_RDONLY` | 只读，文件必须存在 |
| `"w"` | `O_WRONLY \| O_CREAT \| O_TRUNC` | 只写、不存在则创建、存在则**截断清空** |
| `"a"` | `O_WRONLY \| O_CREAT \| O_APPEND` | 追加写，`O_APPEND` 保证每次写在末尾 |
| `"r+"` | `O_RDWR` | 读写，文件必须存在 |
| `"w+"` | `O_RDWR \| O_CREAT \| O_TRUNC` | 读写 + 创建 + 截断 |
| `"a+"` | `O_RDWR \| O_CREAT \| O_APPEND` | 读写 + 追加 |

对照本 demo 的 0b 段：**`fopen(path,"w")` 一定会带上 `O_CREAT|O_TRUNC`**。所以在「不允许创建/截断」的挂载点（只读文件系统、受限容器、某些设备节点）上，`fopen("w")` 会失败，而 `open(path, O_WRONLY)` 可以成功。

### 谁提供 `fopen`、谁提供 `open`

| | `fopen()` | `open()` |
|---|---|---|
| 归属 | ISO-C 标准库 | POSIX 系统调用包装 |
| 返回值 | `FILE *`（带用户态缓冲 + 文件位置 + 锁） | `int fd`（内核整数句柄） |
| 缓冲 | 有（`_IOFBF`/`_IOLBF`/`_IONBF` 三档） | 无，每次 `read`/`write` 就是一次调用 |
| 可移植 | C 标准，跨平台 | POSIX；Windows 只有非标准的 `_open` |

**同一个 `fopen`，不同操作系统内部不同**：Linux 上 glibc 的 `fopen` 内部调 `open` 系统调用；Windows 上 msvcrt 的 `fopen` 内部调 `CreateFileA`。C 标准只规定「外表」，不规定内核细节。

## 5. HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| **HFT 低延迟** | 热路径别用 `fprintf` 到文件/socket：`printf` 的代价在格式化（用户态）+ 字节量（`write`）+ 缓冲策略（是否每次 flush）。要写到日志就用「用户态环形缓冲 + 独立线程批量 `write`」 |
| **HFT 日志热路径** | 别在每行日志里调 `getpwuid`/`gethostname`：本 demo 实测每次查找要多付数次 `read`。要么缓存结果，要么不带这两个字段 |
| **嵌入式（资源受限）** | 直接用 `open`/`read`/`write` 可绕开 libc 缓冲、省内存；用 `fopen` 系列则简单但要留出 `BUFSIZ` 的缓冲。严格检查返回值与 `errno` 是底线 |
| **交叉编译** | 板上跑 glibc 还是 musl 决定了一堆行为差异（NSS、locale、`strerror_r` 语义），见 [3.3](3.3-glibc.md) |

## 6. 衔接

- 本章 README：[README](../README.md)
- 上一节：[3.1 System Calls 系统调用](3.1-system-calls.md)
- 下一节：[3.3 The Standard C Library; The GNU C Library (glibc)](3.3-glibc.md)
- 实战：[4.3 `open()`](../../chapter-04-file-io-universal/notes/4.3-open.md) —— flags 的完整清单与实测

---

<details>
<summary>代码自测（Q&A）</summary>

**Q1：怎么**证明**「`strcpy`/`atoi` 不进内核」，而不是只背下这句话？**
A：用 `/proc/self/io` 的 `syscr`/`syscw`（进程累计的 read/write 系统调用**次数**）。先量一次空负载得到底噪（读计数器自身要 2 次 read），再跑 100000 轮 `strcpy/strlen/atoi`——读数与底噪**完全相同**。放大十万倍都没有多出一次 I/O，这就叫可测量。

**Q2：`/proc/self/io` 能不能证明「某个库函数没进内核」？**
A：不能。它只统计 read/write 这一类 I/O 系统调用，`brk`/`mmap`/`ioctl`/`futex` 都看不见。本 demo 里 `malloc(64)+free ×1000` 的增量等于底噪，但那是「没做 I/O」，不是「没进内核」——`malloc` 该 `brk`/`mmap` 的时候照样会进。要说「没进内核」得看 `strace`/`perf`。

**Q3：`fopen(path,"w")` 和 `open(path, O_WRONLY)` 是同一件事吗？**
A：不是。`fopen` 的 `"w"` 隐含 `O_WRONLY|O_CREAT|O_TRUNC`，比 `open(O_WRONLY)` 多了**创建**和**截断**两位。本 demo 0b 段实测：同一台机器的同一个 `/dev/null`，`open("/dev/null", O_WRONLY)` 成功、`fopen("/dev/null","w")` 失败 `errno=13`。这就是「库函数是薄封装」这句话的真实含义——薄，但**不等于没有附加行为**。

**Q4：`fprintf` 100 次为什么一次 `write` 都没有？**
A：因为字节进了用户态缓冲。stdout/stderr/FILE 的缓冲模式决定「什么时候真正发 `write`」：全缓冲（`_IOFBF`）要等缓冲满或 `fflush`；行缓冲（`_IOLBF`）遇到 `\n` 就发；无缓冲（`_IONBF`）每次立即发。本 demo 实测 `fprintf ×100` → `Δsyscw=0`，紧随其后一次 `fflush()` → `Δsyscw=1`、`Δwchar=100`。

**Q5：为什么说 `getpwnam()` 在 HFT 日志热路径里是「隐性成本」？**
A：因为它走 NSS，每次调用都会去读 `/etc/passwd`（本 demo 实测每次 `Δsyscr=4`，三次都一样 → 本环境无缓存）。每写一行日志就多几次 `read` 系统调用，在高频路径上叠加起来很可观。要么把用户信息缓存下来，要么日志里不带这些字段。

</details>
