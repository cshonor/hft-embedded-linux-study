# 3.8 Exercises 练习

> 本章：[TLPI 第 03 章 — System Programming Concepts](../README.md)
> 源码核验：Linux **v6.6** `include/uapi/linux/reboot.h`（4 个 magic 值的十进制与十六进制均逐一核对）
> - 上一节：[3.7 Summary 本章小结](3.7-summary.md)
> - 下一章：[Ch4 Universal I/O Model 通用 I/O 模型](../../chapter-04-file-io-universal/README.md)

---

## 练习索引

| # | 出处 | 考点 | 对应节 |
|---|------|------|--------|
| **3-1** | 原书（**本章唯一一道**） | `syscall()` 直发未包装的调用 + 读内核源码 | §3.1 |
| 练 1 | 自编 | `errno` 判定范式 + 短写/`EINTR` | §3.4 |
| 练 2 | 自编 | 直接 `syscall()` 的负错误码 | §3.1 / §3.3 |
| 练 3 | 自编 | `getopt` 区分「未知选项」与「缺参数」 | §3.5.1 |
| 练 4 | 自编 | `strerror_r` 两种签名与 FTM 的关系 | §3.6.1 |
| 练 5 | 自编 | `-std=c99` 为什么会减掉声明 | §3.6.1 |

---

## 原书习题 3-1

> **Exercise 3-1.** When using the Linux-specific `reboot()` system call to reboot the system, the second argument, `magic2`, must be specified as one of a set of magic numbers (e.g., `LINUX_REBOOT_MAGIC2`). What is the significance of these numbers? (Converting them to hexadecimal provides a clue).

**题面中文**：用 Linux 特有的 `reboot()` 系统调用重启系统时，第二个参数 `magic2` 必须取一组魔数之一（如 `LINUX_REBOOT_MAGIC2`）。这些数字有什么意义？（把它们转成十六进制就有线索了。）

### 参考解答

`reboot()` 是**没有 glibc 包装**的系统调用之一（`man 2 reboot` 的 NOTES 里明确说了「glibc does not provide a wrapper for this system call」），只能用 `syscall(2)` 直发。它要求四个参数：

```c
#include <linux/reboot.h>

syscall(SYS_reboot, LINUX_REBOOT_MAGIC1, LINUX_REBOOT_MAGIC2, LINUX_REBOOT_CMD_RESTART, NULL);
/*        ↑ 4 个参数怎么塞进寄存器见 §3.1 */
```

魔数值来自内核头文件 `include/uapi/linux/reboot.h`（v6.6）：

```c
#define	LINUX_REBOOT_MAGIC1	0xfee1dead
#define	LINUX_REBOOT_MAGIC2	672274793
#define	LINUX_REBOOT_MAGIC2A	85072278
#define	LINUX_REBOOT_MAGIC2B	369367448
#define	LINUX_REBOOT_MAGIC2C	537993216
```

把这四个十进制的 `magic2` 值转成十六进制（实测）：

| 常量 | 十进制 | 十六进制 | 含义（`ddmmyyyy`） |
|------|--------|----------|--------------------|
| `LINUX_REBOOT_MAGIC2` | `672274793` | `0x28121969` | **28/12/1969** — Linus Torvalds 的生日 |
| `LINUX_REBOOT_MAGIC2A` | `85072278` | `0x05121996` | **05/12/1996** — 长女 Patricia Miranda |
| `LINUX_REBOOT_MAGIC2B` | `369367448` | `0x16041998` | **16/04/1998** — 次女 Daniela Yolanda |
| `LINUX_REBOOT_MAGIC2C` | `537993216` | `0x20112000` | **20/11/2000** — 三女 Celeste Amanda |

`LINUX_REBOOT_MAGIC1` 则是 `0xfee1dead` —— leetspeak 的 **"feel dead"**。

**为什么要有魔数**：`reboot()` 是一个能瞬间关掉整台机器、且**无法撤销**的调用。加两个「必须同时猜对」的魔数（第一个是英文短语、第二个是生日），是为了让「误调用」几乎不可能发生——比如程序把随机内存当作参数传进来，恰好凑齐两组魔数的概率极低。

> 这道题的真正考点不是背生日，而是两件事：
> 1. **`reboot()` 没有 glibc 包装**，必须用 [`syscall(2)`](3.1-system-calls.md) 直发——这正是 `syscall()` 存在的理由（[3.1](3.1-system-calls.md) ⑥ 段）。
> 2. **`man 2 reboot` 已经写了答案**（原文：*"The hexadecimal values of these constants are meaningful."*，并列出了四个 hex 值）。系统编程的基本功是「先读 man；man 说不清就去读内核源码」，而不是猜。
>
> 顺带一个实用事实：`reboot()` 需要 `CAP_SYS_BOOT`。在容器里跑它只会拿到 `EPERM`——所以这道题是可以安全练习的，不会真把机器关掉。

---

## 自编练习 1：错误处理范式（`errno` + 短写 + `EINTR`）

写一个 `./copy source dest`，用 `open`/`read`/`write`/`close` 复制文件。要求：

1. **每一个**系统调用都检查返回值，失败用 `perror` 报告（不要用 `if (errno)` 判成败）
2. 处理 **`EINTR`**：被信号打断要重试，不是报错退出
3. 处理 **短写**：`write` 可能只写出去一部分，必须循环到写满
4. 用标准 typedef（`ssize_t` / `mode_t`），不写死 `int` / `long`

<details>
<summary>参考答案</summary>

```c
#include <fcntl.h>
#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>
#include <errno.h>
#include <sys/types.h>

/* 循环直到写满 n 字节；中间遇到 EINTR 重试 */
static int write_all(int fd, const char *buf, size_t n)
{
    size_t done = 0;
    while (done < n) {
        ssize_t w = write(fd, buf + done, n - done);
        if (w == -1) {
            if (errno == EINTR) continue;   /* 被信号打断 → 重试，不是错误 */
            return -1;
        }
        done += (size_t) w;                 /* w 可能 < 剩余量（短写） */
    }
    return 0;
}

int main(int argc, char *argv[])
{
    if (argc != 3) {
        fprintf(stderr, "Usage: %s source dest\n", argv[0]);
        exit(EXIT_FAILURE);
    }

    int src = open(argv[1], O_RDONLY);
    if (src == -1) { perror("open source"); exit(EXIT_FAILURE); }

    int dst = open(argv[2], O_WRONLY | O_CREAT | O_TRUNC, 0644);
    if (dst == -1) { perror("open dest"); exit(EXIT_FAILURE); }

    char buf[4096];
    for (;;) {
        ssize_t n = read(src, buf, sizeof buf);
        if (n == 0) break;                       /* EOF */
        if (n == -1) {
            if (errno == EINTR) continue;
            perror("read");
            exit(EXIT_FAILURE);
        }
        if (write_all(dst, buf, (size_t) n) == -1) { perror("write"); exit(EXIT_FAILURE); }
    }

    if (close(src) == -1) perror("close src");
    if (close(dst) == -1) perror("close dst");
    return 0;
}
```

**要点**：
- `read` 返回 `0` 才是 EOF；返回 `-1` 必须再看 `errno` 是不是 `EINTR`（见 [3.4](3.4-error-handling.md) 六条规则）。
- 短写**不是**错误：`write` 的契约是「成功时返回已写字节数，可以小于请求」，所以必须循环。POSIX 只保证「对普通文件、且 `n` 不超过管道/文件系统的实现上限时，要么写满要么失败」——**不要依赖这条**。
- `close` 也可能失败（尤其是网络 fd 会把延迟错误暴露在这里），所以它也要检查。

</details>

## 自编练习 2：直接 `syscall()` 的负错误码

不调用 `write()`，用 `syscall(SYS_write, ...)` 写 `"raw\n"`，并**故意**传一个无效 fd，观察返回值：

1. 说明为什么失败时返回值是负错误码而不是 `-1`
2. 写一段代码把这个负值还原成 `errno`

<details>
<summary>参考答案</summary>

```c
#include <stdio.h>
#include <string.h>
#include <errno.h>
#include <unistd.h>
#include <sys/syscall.h>

int main(void)
{
    /* 正常路径 */
    long r = syscall(SYS_write, STDOUT_FILENO, "raw\n", 4);
    printf("syscall 正常返回 = %ld\n", r);

    /* 失败路径：fd = -1 必然 EBADF */
    r = syscall(SYS_write, -1, "x", 1);
    printf("syscall 失败返回 = %ld\n", r);

    /* 自己还原 errno：内核约定 [-4095, -1] 表示错误 */
    if (r < 0 && r >= -4095) {
        int e = (int) -r;
        printf("还原出的 errno = %d (%s)\n", e, strerror(e));
    }
    return 0;
}
```

**要点**：
- 内核用 **`-errno`** 表示失败，区间是 `[-4095, -1]`（`include/linux/err.h:17` 的 `MAX_ERRNO = 4095`）。
- glibc 的包装函数会做**错误翻译**：落在这个区间就把 `errno = -返回值` 并返回 `-1`；否则原样返回。而 `syscall(2)` **也做了**这层翻译（它本身就在 glibc 里），所以你也拿到 `-1`+`errno`。
- 想拿到**裸负值**只有一条路：**手写 `syscall` 指令**，绕过 glibc 这一层（[3.1](3.1-system-calls.md) ③ 段实测拿到 `ret=-9`）。`syscall(2)` 这个**库函数**自己带翻译层，调它拿到的仍是 `-1`+`errno`。
- 所以上面「自己还原」那段主要是**教学用**：理解 `[-4095,-1]` 这个区间与 `MAX_ERRNO` 的约定，比记住某个 API 的返回值形态更重要。
- 判断成功失败**必须先看返回值**：`r` 也可能是 `-4095..-1` 之外的合法负值（有些调用合法范围就含负值，以各自 man 为准）。

</details>

## 自编练习 3：`getopt` 的两种失败

原书示例的 `c3_6_cli_args.c` 有一个**判据不够细**的地方：`optstring = "ab:c"` 时，「未知选项 `-x`」和「`-b` 缺参数」**都返回 `'?'`**，所以 demo 把两者都打成了「未知选项」。

1. 改 `optstring`，让两者能被区分
2. 说明 `optind` 为什么要复位

<details>
<summary>参考答案</summary>

```c
#define _GNU_SOURCE
#include <stdio.h>
#include <unistd.h>      /* optind / optarg / optopt */

int main(int argc, char **argv)
{
    optind = 1;          /* getopt 状态是全局的，重入前必须复位 */
    opterr = 0;          /* 关掉 getopt 自己的报错，改由我们统一报 */

    int ch;
    /* ★ 关键：optstring 开头的 ':' —— 缺参数时返回 ':' 而不是 '?' */
    while ((ch = getopt(argc, argv, ":ab:c")) != -1) {
        switch (ch) {
        case 'a': printf("-a\n"); break;
        case 'b': printf("-b %s\n", optarg); break;
        case 'c': printf("-c\n"); break;
        case ':':
            /* 走到这里 = 选项本身合法，但缺必需参数 */
            fprintf(stderr, "选项 -%c 缺少参数\n", optopt);
            return 1;
        case '?':
            /* 走到这里 = 选项本身不认识 */
            fprintf(stderr, "未知选项 -%c\n", optopt);
            return 1;
        }
    }
    for (int i = optind; i < argc; i++)
        printf("操作数: %s\n", argv[i]);
    return 0;
}
```

**要点**：
- `optstring` 以 `:` 开头是 POSIX 定的区分手段：**缺参数 → `':'`**，**未知选项 → `'?'`**。两种情况下 `optopt` 都是那个选项字符。
- `getopt` 的解析状态（`optind` / `optarg` / `optopt`）是**全局变量**，解析完停在 `optind`。同一个进程里第二次解析前**必须 `optind = 1`**，否则会接着上次的位置继续跑（本仓库 `c3_6_cli_args.c` 连解析四次，每次开头都复位）。
- 解析结束后，`argv[optind..argc-1]` **全是操作数**。GNU 版 `getopt` 会把选项重排到前面（实测 `t1` 的 `optind = 5`，两个操作数被挪到尾部）。

</details>

## 自编练习 4：`strerror_r` 的两种签名取决于哪个宏

下面代码能在 glibc 上编过：

```c
char buf[128];
char *p = strerror_r(ENOENT, buf, sizeof buf);   /* 按 GNU 语义用 */
```

1. 说明为什么它能编过——是「glibc 默认就是 GNU 语义」吗？
2. 严格 POSIX 下应该怎么写？

<details>
<summary>参考答案</summary>

**1. 不是默认。** 恰恰相反：glibc **默认给的是 POSIX 版**（返回 `int`），只有定义了 **`_GNU_SOURCE`**（→ `__USE_GNU`）才切到 GNU 版（返回 `char *`）。

所以在上面那段代码前后必须能找到 `#define _GNU_SOURCE`（放在所有 `#include` 之前）或编译命令里的 `-D_GNU_SOURCE`。两道源码坐标：

| 版本 | glibc 头文件位置 | 原型 |
|------|------------------|------|
| POSIX 版 | `string/string.h:428` | `int strerror_r(int errnum, char *buf, size_t buflen)` |
| GNU 版 | `string/string.h:448` | `char *strerror_r(int errnum, char *buf, size_t buflen)` |

本仓库 [3.6.1](3.6-portability.md) 的五组旗标实测正是这个结论：**只有 `-D_GNU_SOURCE` 那一组是 GNU 版，其余四组（含完全不定义 FTM 的默认组）都是 POSIX 版**。

**2. 严格 POSIX 写法**（不定义 `_GNU_SOURCE`）：

```c
#include <string.h>
#include <errno.h>
#include <stdio.h>

int main(void)
{
    char buf[128];
    int r = strerror_r(ENOENT, buf, sizeof buf);
    if (r != 0) {                   /* 返回 0 表示成功；非 0 时 buf 内容未定义 */
        fprintf(stderr, "strerror_r 本身失败: %d\n", r);
        return 1;
    }
    printf("%s\n", buf);
    return 0;
}
```

**要点**：
- 「同一个函数名，两种 FTM 下签名不同」不是笔误，是 glibc 的既定设计（XSI 版 vs GNU 版共存）。换旗标不只是「能不能用」，还可能**改变 ABI**。
- 这也是「可移植性」最典型的坑：代码在 glibc + `_GNU_SOURCE` 下编得好好的，换到 musl（只有 POSIX 语义）就类型不匹配。要跨 C 库，就按 POSIX 版写。
- `strerror` 本身还有另一个坑：它返回**全局静态缓冲**，非线程安全、且每次调用覆写同一块内存（[3.4](3.4-error-handling.md) 误用 4/5 实测了同一地址反复出现）。要留错误信息就用 `strerror_r` 或立刻 `strdup`。

</details>

## 自编练习 5：`-std=c99` 为什么让声明消失

某工程把编译命令从 `gcc -O2` 改成 `gcc -O2 -std=c99` 之后，`strdup()`、`clock_gettime()`、`strsignal()` 全部报「隐式声明」。

1. 解释原因（要提到具体宏）
2. 给出两种修法

<details>
<summary>参考答案</summary>

**1. 原因**：`-std=c99` 会置上 **`__STRICT_ANSI__`**，此时 glibc 的 `<features.h>` **不再自动补** `_DEFAULT_SOURCE` / `_POSIX_C_SOURCE`（条件在 `features.h:231-239`）。于是所有 `__USE_POSIX*` / `__USE_XOPEN*` / `__USE_MISC` / `__USE_ATFILE` 全部 OFF，那些 POSIX/BSD 声明就都不露出来了。

本仓库 [3.6.1](3.6-portability.md) 的 `stdc99` 组实测把整条链记下来了：

| 阶段 | 值 |
|------|-----|
| ① 定义了什么 | 四个 FTM **全未定义**；`__STRICT_ANSI__ = 1`；`__STDC_VERSION__ = 199901L` |
| ② 内部开关 | 除 `__USE_ISOC99` 外**全部 OFF**（含 `__USE_POSIX` / `__USE_XOPEN2K8` / `__USE_MISC`） |
| ③ 声明可见性 | `strdup()`、`struct timespec`/`clock_gettime`/`nanosleep`、`memmem()`、`strsignal()` **全部不可见** |

**关键认知：加旗标不一定只增不减。** `-std=c99` 的目的是「更严格的标准符合性」，代价就是关掉方言扩展。

**2. 两种修法**：

```bash
# 修法 A：显式把方言要回来（推荐，可移植性可控）
gcc -O2 -std=c99 -D_POSIX_C_SOURCE=200809L -o prog prog.c

# 修法 B：要 GNU 扩展（Linux 专属跑的话最省心）
gcc -O2 -std=c99 -D_GNU_SOURCE -o prog prog.c
```

或者写在源码里（**必须在所有 `#include` 之前**）：

```c
#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <string.h>
#include <unistd.h>
```

另外注意：`memmem()` 守的是 **`__USE_MISC`**（`string.h:385`），不是 `__USE_GNU`——所以**修法 A 仍然编译不过 `memmem`**，用 `memmem` 就得走修法 B（`_GNU_SOURCE`）或单独加 `-D_DEFAULT_SOURCE`。这是 [3.6.1](3.6-portability.md) 那张可见性表里最容易踩的一格。

</details>

---

## 衔接

- 上一节：[3.7 Summary 本章小结](3.7-summary.md)
- 本章 README：[README](../README.md)
- 下一章：[Ch4 Universal I/O Model 通用 I/O 模型](../../chapter-04-file-io-universal/README.md) —— 用 `open`/`read`/`write`/`lseek` 做真正的文件实战（`copy.c` = Listing 4-1，用的就是本章这套骨架）
