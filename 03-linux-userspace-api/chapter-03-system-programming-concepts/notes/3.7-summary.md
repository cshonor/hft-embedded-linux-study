# 3.7 Summary

> 本章：[TLPI 第 03 章 — System Programming Concepts](../README.md)
> - 上一节：[3.4 可移植编程（SUSv3 / POSIX）](3.6-portability.md)
> - 下一节：[3.8 Exercise](3.8-exercise.md)

---

## 本章回顾

第 3 章是「用户态 ↔ 内核」交互模型的总纲，**不含**文件读写实战（那是 Ch4）。核心是把**系统调用、库函数、错误处理、参数传递、可移植性**五个概念串成一条线。

### 核心概念速查

| 概念 | 一句话 | 关键点 | 详细 |
|------|--------|--------|------|
| 系统调用 | 用户态进内核的受控入口 | 编号 + 寄存器 + `syscall` 指令，远贵于普通函数调用 | [3.1](3.1-system-calls.md) |
| 库函数 | glibc 的高层封装 | 纯用户态函数（`strcpy`）不进内核；`fopen` 内部才调 `open` | [3.2](3.2-library-functions.md) |
| glibc | 一个库三种身份 | ISO-C 实现 + POSIX 封装 + Linux 扩展；`MAX_ERRNO` 错误翻译 | [3.3](3.3-glibc.md) |
| 错误处理 | 失败返回 -1 + 设 `errno` | 成功不清零；`EINTR` 要重试；`strerror_r` 线程安全 | [3.3](3.4-error-handling.md) |
| 参数传递 | 寄存器传参，最多 6 个 | `copy_from_user` 校验 + 拷贝；非法指针 → `EFAULT` | [3.5](3.5-example-programs.md) |
| 可移植性 | 功能测试宏 + 标准 typedef | 宏放 `#include` 前；`off_t` 别写死 `long` | [3.4](3.6-portability.md) |

### 系统调用完整往返（本章全景）

```
用户态代码
   │  调 glibc 包装（如 open/read/write/fork）
   ▼
glibc 包装：rax=调用号, rdi/rsi/rdx/r10/r8/r9=参数
   │  功能测试宏决定这个包装函数是否被声明（features.h）
   ▼
syscall 指令陷入内核（用户态 → 内核态）
   │  内核 sys_call_table[号] 分发到 sys_xxx
   │  copy_from_user 校验并拷贝用户指针（防 EFAULT / 内核被攻击）
   ▼
内核干活，返回值放进 rax：
   成功 → 合法返回值（fd≥0 / 指针 / 字节数）
   失败 → -errno（落在 [-4095, -1]，MAX_ERRNO=4095）
   ▼
回用户态，glibc 包装判断 rax：
   ∈[-4095,-1] → errno = -rax，返回 -1   （错误翻译）
   否则        → 返回 rax（合法值）
   ▼
用户代码：先看返回值，失败才读 errno（成功不清零！）
```

### 五个必须记住的坑

1. **成功不清 errno** → 判断成败只能看返回值，不能 `if (errno)`。
2. **`EINTR` 不是错误** → 被信号打断，`while (n==-1 && errno==EINTR)` 重试。
3. **`EFAULT` 是内存 bug** → 传了非法指针给内核，查野指针/越界，别查 I/O。
4. **`EAGAIN` 是暂时状态** → 非阻塞「暂无数据」，重试或进 epoll，别当失败。
5. **直接 `syscall()` 拿负错误码** → 绕过 glibc 时返回值是 `-2`（-ENOENT）不是 `-1`。

## HFT / 嵌入式收束

| 路线 | 本章的落地 |
|------|------------|
| HFT 低延迟 | 少 syscall（逐字节 read = N 次陷入）；区分 glibc 包装成本 vs 真陷入；错误处理用 `strerror_r`（线程安全）+ 完整上下文 |
| 嵌入式 | 严格返回值 + `errno` 检查；musl 静态编译摆脱 glibc 依赖；`_FILE_OFFSET_BITS=64` 处理大日志 |

## 衔接

- 下一节：[3.8 Exercise](3.8-exercise.md)
- 实战起点：[Ch4 通用 I/O 模型](../../chapter-04-file-io-universal/README.md) — 把本章的 syscall 模型用到 `open`/`read`/`write`
- 内核视角：[05/Ch5.1 与内核通信](../../../05-linux-kernel/chapter-05-system-calls/notes/section-5.1-与内核通信.md) — libc ≠ syscall

---

<details>
<summary>代码自测（Q&A）</summary>

**Q1：用一句话串起本章五个概念。**
A：系统调用是「用户态进内核的受控入口」（编号+寄存器+陷阱指令），库函数是 glibc 在其上的高层封装（纯用户态函数不进内核）；系统调用失败返回 -1 并设 `errno`（成功不清零、`EINTR` 重试）；参数通过寄存器传递且内核必须 `copy_from_user` 拷贝校验；跨平台靠功能测试宏 + 标准 typedef 保证 API 可见性与类型宽度一致。

**Q2：为什么「判断成功失败要看返回值，不能看 errno」？**
A：因为成功时内核/glibc **不会清零** `errno`，它保留的是上一次失败留下的旧值。用 `if (errno != 0)` 判断成败会误判。正确姿势：`fd = open(...); if (fd == -1) { /* 这时才读 errno */ }`。

**Q3：系统调用失败，用户看到「-1 + errno」，内核实际返回了什么？**
A：内核实际返回 `-errno`（负错误码，如 `-ENOENT`=`-2`，落在 `[-4095,-1]`，`MAX_ERRNO=4095`）。glibc 包装检测返回值在此范围，则设 `errno = -返回值` 并返回 `-1`。所以「-1 + errno」是 glibc 翻译的结果——直接 `syscall()` 拿到的是 `-2` 不是 `-1`。

**Q4：`EINTR`、`EAGAIN`、`EFAULT` 三个错误码怎么区分处理？**
A：`EINTR`（=4）被信号打断 → **重试**（`while(n==-1 && errno==EINTR)`）；`EAGAIN`（=11）非阻塞暂时无数据 → **稍后重试或进 epoll**；`EFAULT`（=14）传给内核的指针非法 → **查内存 bug**（野指针/越界），不是 I/O 错误。前两个是「可恢复的暂时状态」，最后一个是「程序 bug」。

**Q5：`fopen` 和 `open` 的本质区别是什么？**
A：`fopen` 是 ISO-C 标准库函数，返回带用户态缓冲的 `FILE*`，跨平台（Windows 用 msvcrt 实现）；`open` 是 POSIX 系统调用包装，返回内核整数 `fd`，无用户态缓冲，Unix/Linux 限定。`fopen` 内部最终会调用 `open` 系统调用（Linux 上），但多了一层用户态缓冲管理。

</details>
