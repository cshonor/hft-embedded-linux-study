# 13.3 【致命】混用 `FILE*` 与 `read`/`write`(fd)

> 本章：[TLPI 第 13 章 — File I/O Buffering](../README.md)
> - 上一节：[13.2 stdio 用户态缓冲（`FILE*`）](./13.2-stdio-file.md)
> - 下一节：[13.4 `posix_fadvise` — 访问模式提示](13.4-summary-buffering.md)

---

## 本节讲什么

两套缓冲**互不可见**： - stdio 有自己的用户缓冲 - `read`/`write` 直达内核缓存，**绕过** stdio

---

## 要点

两套缓冲**互不可见**：

- stdio 有自己的用户缓冲  
- `read`/`write` 直达内核缓存，**绕过** stdio  

→ 乱序、丢数据。

| 做法 | |
|------|--|
| ✅ | 全程只用一套接口 |
| ✅ | 必须混用：切换前对输出流 **`fflush`** |
| ❌ | `printf` 完直接对同一 fd `write`（未 fflush） |

Demo：[`code/mix_stdio_write.c`](../code/mix_stdio_write.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 同一 fd 上混用 `FILE*` 与 `read`/`write`，两层缓冲各自独立会乱序/丢数据，混用前必须 `fflush` 同步 |
| HFT | 日志线程 `printf` 完直接 `write` 同一 fd 会数据乱序丢失；混用前必须 `fflush`，热路径干脆统一走 syscall |

---

## 常见陷阱

1. **短读未处理**

```c
/* 假设一次读完 */
ssize_t n = read(fd, buf, 1024);
/* n 可能 < 1024 */
```

read 返回值可能小于请求字节数（short read）。网络和管道尤其常见，必须循环读。

2. **EINTR 未处理**

```c
/* 信号中断后 read 返回 -1, errno=EINTR */
/* 不是错误，应重试 */
```

信号中断系统调用时返回 -1 且 errno=EINTR。应该重试，或用 SA_RESTART 自动重启。

3. **EOF 判断错误**

```c
/* 用 feof 预判 EOF → 多读一次 */
while (!feof(fp)) { fgets(...); }
/* 正确：检查返回值 */
while (fgets(buf, sz, fp) != NULL) { ... }
```

read 返回 0 表示 EOF。不要用 feof 预判（feof 只在读取越过后才为真）。

---

## 自测题

**题目 1（选择）：** `read()` 返回 0 表示什么？
A. 出错
B. EOF
C. 没有数据
D. 需要重试

<details>
<summary>参考答案</summary>

B。返回 0 表示已到达文件末尾（EOF）。返回 -1 表示出错，返回 >0 表示读到的字节数。

</details>

**题目 2（判断）：** `read` 一定会读满请求的字节数。

<details>
<summary>参考答案</summary>

错误。read 可能返回少于请求的字节数（short read），尤其在管道、socket、终端上。必须循环读。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
char buf[1024];
read(fd, buf, sizeof(buf));
buf[strlen(buf)] = '\0';
```

<details>
<summary>参考答案</summary>

1) read 不保证以 null 结尾；2) 没检查返回值；3) strlen 在未终止的 buf 上越界。应检查返回值并手动加 `\0`。

</details>

**题目 4（应用）：** 写一个安全的 readall 函数，确保读满 n 字节或遇到 EOF。

<details>
<summary>参考答案</summary>

```c
ssize_t readall(int fd, void *buf, size_t n) {
    size_t total = 0;
    while (total < n) {
        ssize_t r = read(fd, (char*)buf+total, n-total);
        if (r < 0 && errno == EINTR) continue;
        if (r < 0) return -1;
        if (r == 0) break; /* EOF */
        total += r;
    }
    return total;
}
```

</details>

---

## 代码示例

```c
#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>
#include <string.h>
/* read/write vs fread/fwrite: 直接系统调用 vs stdio */
int main(void) {
    /* write: 直接进 kernel page cache */
    int fd = open("/tmp/rw_test.txt", O_WRONLY | O_CREAT | O_TRUNC, 0644);
    write(fd, "via write()\n", 12);
    close(fd);

    /* fwrite: 经 stdio 缓冲, flush 时才调 write */
    FILE *fp = fopen("/tmp/rw_test.txt", "a");
    fwrite("via fwrite()\n", 13, 1, fp);
    fclose(fp);  /* fclose 自动 flush */

    /* posix_fadvise: 给内核预读/丢弃提示 */
    fd = open("/tmp/rw_test.txt", O_RDONLY);
    posix_fadvise(fd, 0, 0, POSIX_FADV_SEQUENTIAL);  /* 顺序读 */
    char buf[64];
    read(fd, buf, sizeof(buf));
    printf("content: %s", buf);
    close(fd);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[13.2 stdio 用户态缓冲（`FILE*`）](./13.2-stdio-file.md)
- 下一节：[13.4 `posix_fadvise` — 访问模式提示](13.4-summary-buffering.md)
- 前置：**前置**：[Ch4 Universal I/O](../../chapter-04-file-io-universal/notes) · [Ch5 Further I/O](../../chapter-05-file-io-further/notes) · [Ch12 `/proc`](../../chapter-12-system-process-info/notes)  
- 后置：**后置**：[Ch14 File Systems](../../chapter-14-file-systems/notes) · [Ch49 mmap](../../chapter-49-memory-mappings/notes) · Ch63 替代 I/O
