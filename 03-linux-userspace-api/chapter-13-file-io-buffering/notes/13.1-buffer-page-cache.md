# 13.1 内核缓冲（Buffer / Page Cache）

> 本章：[TLPI 第 13 章 — File I/O Buffering](./README.md)
> - 上一节：本章首页
> - 下一节：[13.2 stdio 用户态缓冲（`FILE*`）](./13.2-stdio-file.md)

---

## 本节讲什么

`read()` / `write()` **不直接对磁盘**做同步块传输，而是在用户缓冲 ↔ **内核页缓存**之间拷贝。 后台 flusher 周期性刷脏页；参数见 `/proc/sys/vm/dirty_*`。 #include <unistd.h>

---

## 要点

`read()` / `write()` **不直接对磁盘**做同步块传输，而是在用户缓冲 ↔ **内核页缓存**之间拷贝。

### 延迟写（Delayed Write）

| | |
|--|--|
| `write()` 成功返回 | **≠ 已落盘**；数据多在脏页里 |
| 好处 | 合并小写、少寻道、吞吐高 |
| 风险 | 崩溃/断电 → 脏页丢失 |

后台 flusher 周期性刷脏页；参数见 `/proc/sys/vm/dirty_*`。

### 强制落盘

```c
#include <unistd.h>
void sync(void);          /* 全局刷脏；标准允许不等待完成；业务勿狂调 */
int fsync(int fd);        /* 该文件：数据 + 元数据；等到设备完成 */
int fdatasync(int fd);    /* 优先刷数据；元数据按需；通常比 fsync 轻 */
```

### `open` 同步标志

| 标志 | 近似语义 |
|------|----------|
| `O_SYNC` | 每次 `write` ≈ `fsync`（数据+元数据） |
| `O_DSYNC` | 每次 `write` ≈ `fdatasync` |

> 同步 IO **极慢**；事务/关键日志按需用。  
> 若前面还有 **stdio**，必须先 `fflush` 再依赖 `O_SYNC`/`write`。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 内核缓冲（Buffer / Page Cache） 在嵌入式系统的应用场景 |
| HFT | 内核缓冲（Buffer / Page Cache） 对低延迟和性能的影响 |

## 代码示例

```c
#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>
#include <string.h>
/* 三层缓冲: stdio buffer -> kernel page cache -> disk */
int main(void) {
    /* stdio buffer: 用户态, setvbuf 控制 */
    FILE *fp = fopen("/tmp/buffer_test.txt", "w");
    setvbuf(fp, NULL, _IOFBF, 4096);  /* 全缓冲 4KB */
    fprintf(fp, "in stdio buffer (not on disk yet)\n");
    /* fflush: stdio -> kernel page cache */
    fflush(fp);
    fclose(fp);

    /* kernel page cache: write() 先进 cache */
    int fd = open("/tmp/buffer_test.txt", O_WRONLY | O_APPEND);
    write(fd, "in page cache\n", 14);
    /* fsync: page cache -> disk */
    fsync(fd);
    close(fd);

    printf("Data path: stdio buffer -> page cache -> disk\n");
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 下一节：[13.2 stdio 用户态缓冲（`FILE*`）](./13.2-stdio-file.md)
- 前置：**前置**：[Ch4 Universal I/O](../chapter-04-file-io-universal/notes.md) · [Ch5 Further I/O](../chapter-05-file-io-further/notes.md) · [Ch12 `/proc`](../chapter-12-system-process-info/notes.md)  
- 后置：**后置**：[Ch14 File Systems](../chapter-14-file-systems/notes.md) · [Ch49 mmap](../chapter-49-memory-mappings/notes.md) · [Ch63 替代 I/O](../chapter-63-alternative-io/notes.md)
