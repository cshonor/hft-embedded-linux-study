# 13.5 Direct I/O（`O_DIRECT`）

> 本章：[TLPI 第 13 章 — File I/O Buffering](./README.md)
> - 上一节：[13.4 `posix_fadvise` — 访问模式提示](./13.4-posixfadvise.md)
> - 下一节：[13.6 两层数据流](./13.6-section-13-6.md)

---

## 本节讲什么

`open(path, O_RDWR | O_DIRECT)`：绕过页缓存，应用与块设备更直接传数据。 缓冲区地址、文件偏移、传输长度须为**逻辑块大小**（常见 512/4096）整数倍。 缓冲分配：`posix_memalign()`。

---

## 要点

`open(path, O_RDWR | O_DIRECT)`：绕过页缓存，应用与块设备更直接传数据。

### 对齐（硬约束）

缓冲区地址、文件偏移、传输长度须为**逻辑块大小**（常见 512/4096）整数倍。  
缓冲分配：`posix_memalign()`。

| 适用 | 自带缓存的 DB 等，不想占系统 page cache |
|------|----------------------------------------|
| 缺点 | 小 IO 很惨；**不能替代 `fsync` 持久化** |

> **`O_DIRECT` ≠ `O_SYNC`**：前者绕过页缓存；设备本身可能有缓存，落盘仍要 `fsync`/`fdatasync`。

Demo：[`code/odirect_align.c`](./code/odirect_align.c)

---

---

## 与 C 语言学习的关系

本节涉及的 `open()` 是 C 语言文件操作的基础系统调用。与 K&R 第 8 章的 `fopen()` 不同，`open()` 是 POSIX 系统调用而非 C 标准库函数。理解 `open()` 的 flags 参数（O_RDONLY、O_CREAT 等）是掌握 Unix 系统编程的第一步。对照 K&R 8.3 节的 `open/creat/close` 学习，注意 C 标准库 `fopen` 的 "r"/"w"/"a" 模式如何映射到 `open` 的 flags。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | Direct I/O（`O_DIRECT`） 在嵌入式系统的应用场景 |
| HFT | Direct I/O（`O_DIRECT`） 对低延迟和性能的影响 |

---

## 常见陷阱

1. **O_CREAT 忘传 mode**

```c
/* mode 参数丢失，权限不确定 */
fd = open("f.txt", O_WRONLY | O_CREAT);
/* 必须给 mode */
fd = open("f.txt", O_WRONLY | O_CREAT, 0644);
```

O_CREAT 时第三个参数 mode 是新文件权限，受 umask 影响。漏传会导致权限不确定（取决于栈上残留值）。

2. **返回值未检查**

```c
int fd = open(path, O_RDONLY);
read(fd, buf, n);  /* fd=-1 → EBADF */
```

open 失败返回 -1。不检查直接用 fd 会导致后续操作全部失败。必须 `if ((fd = open(...)) < 0) { perror; exit; }`。

3. **O_TRUNC 意外截断**

```c
/* 想追加写，却加了 O_TRUNC */
fd = open("log.txt", O_WRONLY | O_TRUNC);  /* 清空了! */
fd = open("log.txt", O_WRONLY | O_APPEND);  /* 正确 */
```

O_TRUNC 在文件存在时截断为 0。追加写场景误加 O_TRUNC 会丢失数据。

---

## 自测题

**题目 1（选择）：** `open("f.txt", O_CREAT)` 缺少什么参数？
A. flags 不对
B. mode 参数
C. pathname
D. 返回值

<details>
<summary>参考答案</summary>

B。O_CREAT 时必须提供 mode（如 0644），受 umask 影响。漏传会使用栈上不确定的值。

</details>

**题目 2（判断）：** `open` 失败时返回 0。

<details>
<summary>参考答案</summary>

错误。open 失败返回 -1 并设置 errno。fd=0 是 stdin，open 正常时可能返回 0（如果 stdin 被关闭）。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
int fd = open("/etc/passwd", O_WRONLY|O_CREAT|O_TRUNC, 0644);
```

<details>
<summary>参考答案</summary>

对 /etc/passwd 用 O_WRONLY|O_TRUNC 会截断系统密码文件！想读文件应用 O_RDONLY。这是非常危险的操作。

</details>

**题目 4（应用）：** 写代码：以读写方式打开文件，不存在则创建（权限 0644），存在则追加。

<details>
<summary>参考答案</summary>

```c
int fd = open("data.txt", O_RDWR|O_CREAT|O_APPEND, 0644);
if (fd < 0) { perror("open"); exit(1); }
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[13.4 `posix_fadvise` — 访问模式提示](./13.4-posixfadvise.md)
- 下一节：[13.6 两层数据流](./13.6-section-13-6.md)
- 前置：**前置**：[Ch4 Universal I/O](../chapter-04-file-io-universal/notes.md) · [Ch5 Further I/O](../chapter-05-file-io-further/notes.md) · [Ch12 `/proc`](../chapter-12-system-process-info/notes.md)  
- 后置：**后置**：[Ch14 File Systems](../chapter-14-file-systems/notes.md) · [Ch49 mmap](../chapter-49-memory-mappings/notes.md) · [Ch63 替代 I/O](../chapter-63-alternative-io/notes.md)

---

## man 手册参考

- `man 2 open`
