# 13.4 `posix_fadvise` — 访问模式提示

> 本章：[TLPI 第 13 章 — File I/O Buffering](../README.md)
> - 上一节：[13.3 【致命】混用 `FILE*` 与 `read`/`write`(fd)](13.3-controlling-kernel-buffering.md)
> - 下一节：[13.5 Direct I/O（`O_DIRECT`）](13.5-advising-kernel.md)

---

## 本节讲什么

int posix_fadvise(int fd, off_t offset, off_t len, int advice); 仅**建议**内核；常用： ---

---

## 要点

```c
int posix_fadvise(int fd, off_t offset, off_t len, int advice);
```

仅**建议**内核；常用：

| advice | 含义 |
|--------|------|
| `POSIX_FADV_NORMAL` | 默认 |
| `POSIX_FADV_SEQUENTIAL` | 顺序 → 加大预读 |
| `POSIX_FADV_RANDOM` | 随机 → 少预读 |
| `POSIX_FADV_DONTNEED` | 短期不用 → 可回收缓存 |
| `POSIX_FADV_WILLNEED` | 即将用 → 预加载 |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 闪存随机读慢，`fadvise(WILLNEED)` 预取整文件进 page cache，提升启动/加载速度，规避按需读的延迟 |
| HFT | 开盘前 `posix_fadvise(fd, WILLNEED)` 预热行情/静态数据文件，把 I/O 挪到盘前；顺序日志给 `SEQUENTIAL` 提示 |

## 代码示例

```c
#include <fcntl.h>
#include <unistd.h>
#include <stdio.h>

int main(void) {
    int fd = open("/tmp/fadvise_demo.txt", O_RDONLY);
    if (fd < 0) { perror("open"); return 1; }

    /* 告诉内核: 我要顺序读, 预读有帮助 */
    if (posix_fadvise(fd, 0, 0, POSIX_FADV_SEQUENTIAL) != 0) {
        perror("posix_fadvise SEQUENTIAL");
    }
    printf("提示: 顺序读 (内核会预读)\n");

    /* 告诉内核: 这段数据不会再用了, 可丢弃缓存 */
    if (posix_fadvise(fd, 0, 4096, POSIX_FADV_DONTNEED) != 0) {
        perror("posix_fadvise DONTNEED");
    }
    printf("提示: 丢弃前4KB缓存\n");

    /* 预取: 数据马上要用, 提前加载到缓存 */
    if (posix_fadvise(fd, 4096, 8192, POSIX_FADV_WILLNEED) != 0) {
        perror("posix_fadvise WILLNEED");
    }
    printf("提示: 预取4KB-12KB\n");

    close(fd);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[13.3 【致命】混用 `FILE*` 与 `read`/`write`(fd)](13.3-controlling-kernel-buffering.md)
- 下一节：[13.5 Direct I/O（`O_DIRECT`）](13.5-advising-kernel.md)
- 前置：**前置**：[Ch4 Universal I/O](../../chapter-04-file-io-universal/notes) · [Ch5 Further I/O](../../chapter-05-file-io-further/notes) · [Ch12 `/proc`](../../chapter-12-system-process-info/notes)  
- 后置：**后置**：[Ch14 File Systems](../../chapter-14-file-systems/notes) · [Ch49 mmap](../../chapter-49-memory-mappings/notes) · Ch63 替代 I/O
