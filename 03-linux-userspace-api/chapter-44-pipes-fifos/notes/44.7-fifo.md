# 44.7 –44.8 FIFO

> 本章：[TLPI 第 44 章 — Pipes and FIFOs](./README.md)
> - 上一节：[44.6 管道 + stdio](./44.6-stdio.md)
> - 下一节：[44.9 局限](./44.9-limitations.md)

---

## 本节讲什么

mkfifo(path, mode); open(path, O_RDONLY);   /* 阻塞到有写端 */ open(path, O_WRONLY);   /* 阻塞到有读端 */

---

## 要点

```c
mkfifo(path, mode);
open(path, O_RDONLY);   /* 阻塞到有写端 */
open(path, O_WRONLY);   /* 阻塞到有读端 */
```

`O_NONBLOCK`：`O_RDONLY` 常立刻成功；`O_WRONLY` 无读端 → **`ENXIO`**。  
勿依赖 `O_RDWR` 开 FIFO（可移植性差）。

简单 C/S：一服务端读 FIFO，多客户端写 — 并发写仍可能交织（≤`PIPE_BUF` 可原子）。

Demo：[`code/fifo_demo.c`](./code/fifo_demo.c) + README 双进程用法。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | –44.8 FIFO 在嵌入式系统的应用场景 |
| HFT | –44.8 FIFO 对低延迟和性能的影响 |

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[44.6 管道 + stdio](./44.6-stdio.md)
- 下一节：[44.9 局限](./44.9-limitations.md)
- 前置：**前置**：[Ch43 IPC 综述](../chapter-43-ipc-overview/notes.md)  
- 后置：**后置**：[Ch45 System V IPC 导论](../chapter-45-sysv-ipc-intro/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
