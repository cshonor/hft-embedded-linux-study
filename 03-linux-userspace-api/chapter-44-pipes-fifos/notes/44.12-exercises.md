# 44.12 Exercises

> 本章：[TLPI 第 44 章](../README.md)

## 本节讲什么

第 44 章的练习题与参考答案。

## 要点

### 练习 1：管道实现 popen

**题目**：使用 `pipe` + `fork` + `exec` 实现 `popen()` 功能：执行命令并获取其输出。

<details>
<summary>参考答案</summary>

```c
#include <unistd.h>
#include <stdio.h>
#include <fcntl.h>

FILE *my_popen(const char *cmd, const char *type) {
    int pfd[2];
    pipe2(pfd, O_CLOEXEC);

    pid_t pid = fork();
    if (pid == 0) {
        if (*type == 'r') {
            close(pfd[0]);
            dup2(pfd[1], STDOUT_FILENO);
        } else {
            close(pfd[1]);
            dup2(pfd[0], STDIN_FILENO);
        }
        execl("/bin/sh", "sh", "-c", cmd, NULL);
        _exit(127);
    }
    if (*type == 'r') {
        close(pfd[1]);
        return fdopen(pfd[0], "r");
    } else {
        close(pfd[0]);
        return fdopen(pfd[1], "w");
    }
}
```

</details>

### 练习 2：PIPE_BUF 原子性

**题目**：两个进程同时向同一 FIFO 写入 > PIPE_BUF 的数据，观察数据交错现象。

<details>
<summary>参考答案</summary>

当写入量 > PIPE_BUF（通常 4096）时，write 不保证原子性。两个进程的写操作可能交替执行，导致输出数据交错。解决方法：(1) 每条消息 ≤ PIPE_BUF 确保原子性；(2) 使用消息队列（有边界保证）；(3) 自定义协议（长度前缀+完整消息）；(4) 使用文件锁序列化写入。

</details>

## 衔接

- [44.11 总结](./44.11-summary.md) | 第 45 章 SysV IPC 概述
