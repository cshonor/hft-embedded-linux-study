# 22.4 实时信号与 `sigqueue`

> 本章：[TLPI 第 22 章 — Signals: Advanced Features](../README.md)
> - 上一节：[22.3 同步等待：`sigwait` 族](22.3-interruptible-and-uninterruptible-proces.md)
> - 下一节：[22.5 备用信号栈](22.5-synchronous-and-asynchronous-signal-gene.md)

---

## 本节讲什么

int sigqueue(pid_t pid, int sig, const union sigval value); 标准信号不排队；实时信号可排队，小号优先。 Demo：[`code/sigqueue_rt.c`](../code/sigqueue_rt.c)

---

## 要点

```c
int sigqueue(pid_t pid, int sig, const union sigval value);
```

| vs `kill` | |
|-----------|--|
| 可带 `sigval`（int 或 ptr） | 跨进程 **勿传指针**（地址空间不同） |
| 可排队 | 队列满则失败 |
| 接收端 | `SA_SIGINFO`，读 `si_value` |

标准信号不排队；实时信号可排队，小号优先。

Demo：[`code/sigqueue_rt.c`](../code/sigqueue_rt.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | sigqueue 带 int 值且排队，嵌入式进程间可传少量带编号控制消息（通道号/事件 ID）；跨进程勿传指针（地址空间不同） |
| HFT | 实时信号虽排队带值，但递送仍是异步中断，HFT 不用它做高频数据通路；仅作低频控制信令（切模式/降级），数据走共享内存 + eventfd |

## 代码示例

```c
#include <signal.h>
#include <unistd.h>
#include <stdio.h>
void handler(int sig, siginfo_t *si, void *ctx) {
    printf("收到 sig=%d, 值=%d\n", sig, si->si_value.sival_int);
}
int main(void) {
    struct sigaction sa = {0};
    sa.sa_sigaction = handler;
    sa.sa_flags = SA_SIGINFO;
    sigaction(SIGUSR1, &sa, NULL);

    union sigval sv;
    sv.sival_int = 42;
    sigqueue(getpid(), SIGUSR1, sv);  /* 带数据的信号 */
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[22.3 同步等待：`sigwait` 族](22.3-interruptible-and-uninterruptible-proces.md)
- 下一节：[22.5 备用信号栈](22.5-synchronous-and-asynchronous-signal-gene.md)
- 前置：**前置**：[Ch21 Signal Handlers](../../chapter-21-signal-handlers/notes)  
- 后置：**后置**：[Ch23 Timers and Sleeping](../../chapter-23-timers-sleeping/notes) · [Ch29+ 线程](../../chapter-29-threads-intro/notes) · Ch63 多路 I/O
