# 22.8 三大等待对比

> 本章：[TLPI 第 22 章 — Signals: Advanced Features](../README.md)
> - 上一节：[22.7 `EINTR` 再强调](22.7-implementation-and-portability-of-signal.md)
> - 下一节：下一章+1:02d}-*/notes.md)

---

## 本节讲什么

---

---

## 要点

| 方式 | 模型 | 评价 |
|------|------|------|
| `pause` | 异步 + 睡 | 有竞态，正式代码少用 |
| `sigsuspend` | 异步 + 原子睡 | 修竞态；仍受 async-safe 约束 |
| `sigwaitinfo` | 同步取 pending | 无 async 限制；信号须先阻塞 |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 三者决定 handler 模型——pause 有竞态、sigsuspend 修竞态但受 async-safe 约束、sigwaitinfo 转同步最省心，嵌入式按「是否多线程」选 |
| HFT | 结论明确：HFT 一律 sigwaitinfo（同步消费、零 async-safe 限制、主循环无中断）；pause/sigsuspend 的异步+休眠模型只适合非实时管理线程 |

## 代码示例

```c
#include <sys/wait.h>
#include <unistd.h>
#include <stdio.h>
int main(void) {
    pid_t pid = fork();
    if (pid == 0) { _exit(7); }

    int status;
    /* wait: 阻塞等任意子进程 */
    wait(&status);
    if (WIFEXITED(status))
        printf("wait: 子进程退出码=%d\n", WEXITSTATUS(status));

    pid = fork();
    if (pid == 0) { _exit(3); }
    /* waitpid: 可指定 PID, 可非阻塞 */
    waitpid(pid, &status, 0);
    printf("waitpid: 退出码=%d\n", WEXITSTATUS(status));

    /* waitid: 最灵活, 可指定哪些状态变化 */
    printf("\nwait: 阻塞等任意子进程\n");
    printf("waitpid: 指定 PID, WNOHANG 非阻塞\n");
    printf("waitid: 最灵活, 指定状态类型\n");
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[22.7 `EINTR` 再强调](22.7-implementation-and-portability-of-signal.md)
- 前置：**前置**：[Ch21 Signal Handlers](../../chapter-21-signal-handlers/notes)  
- 后置：**后置**：[Ch23 Timers and Sleeping](../../chapter-23-timers-sleeping/notes) · [Ch29+ 线程](../../chapter-29-threads-intro/notes) · Ch63 多路 I/O
