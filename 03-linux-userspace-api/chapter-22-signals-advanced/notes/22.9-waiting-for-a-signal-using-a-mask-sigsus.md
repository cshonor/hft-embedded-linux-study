# 22.9 使用掩码等待信号：sigsuspend()

> 本章：[TLPI 第 22 章 — 高级信号处理](../README.md)
> - 上一节：22.8 系统调用中断与 SA_RESTART
> - 下一节：[22.10 同步等待信号](./22.10-synchronously-waiting-for-a-signal.md)

---

## 本节讲什么

`sigsuspend()` — 原子地"解除信号阻塞 + 等待信号"，消除 `pause()` 的竞态。

---

## 要点

### 核心问题：pause() 的竞态

```c
/* 竞态: check 和 pause 之间信号可能到达 */
if (ready == 0)
    pause();  /* 如果信号在 if 之后、pause 之前到达 → 永远阻塞 */
```

### sigsuspend 解决方案

```c
int sigsuspend(const sigset_t *mask);
```
- **原子操作**：将进程信号掩码替换为 `mask`，然后等待信号
- 信号处理函数返回后，掩码恢复为调用前的值
- 总是返回 -1，errno = EINTR

### 正确的临界区等待模式

```c
sigset_t mask, oldmask;
sigemptyset(&mask);
sigaddset(&mask, SIGINT);

/* 1. 阻塞 SIGINT */
sigprocmask(SIG_BLOCK, &mask, &oldmask);

/* 2. 临界区 — SIGINT 被阻塞 */
while (!ready) {
    /* 3. 原子地: 解除 SIGINT 阻塞 + 等待 */
    sigsuspend(&oldmask);  /* oldmask 不含 SIGINT */
}

/* 4. 恢复原掩码 */
sigprocmask(SIG_SETMASK, &oldmask, NULL);
```

### 与 pause 的对比

| | `pause()` | `sigsuspend()` |
|--|-----------|-----------------|
| 原子性 | 无（check + wait 分离） | 有（unblock + wait 原子） |
| 竞态 | 有 | 无 |
| 生产使用 | 不推荐 | 推荐 |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | sigsuspend 是信号同步等待的标准做法，无竞态 |
| HFT | 用 sigsuspend 等待配置重载信号（SIGHUP），保证不丢失事件 |

## 代码示例

```c
#include <stdio.h>
#include <signal.h>
#include <unistd.h>
#include <string.h>
/* sigsuspend: 原子等待 SIGINT, 无竞态 */
volatile sig_atomic_t got_sigint = 0;
void handler(int sig) { got_sigint = 1; }

int main(void) {
    struct sigaction sa;
    memset(&sa, 0, sizeof(sa));
    sa.sa_handler = handler;
    sigaction(SIGINT, &sa, NULL);

    sigset_t intmask, oldmask;
    sigemptyset(&intmask);
    sigaddset(&intmask, SIGINT);

    /* 阻塞 SIGINT */
    sigprocmask(SIG_BLOCK, &intmask, &oldmask);

    printf("waiting for SIGINT (race-free)...\n");
    while (!got_sigint) {
        /* oldmask 不含 SIGINT → 原子解除阻塞 + 等待 */
        sigsuspend(&oldmask);
    }
    printf("got SIGINT\n");

    sigprocmask(SIG_SETMASK, &oldmask, NULL);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：22.8 系统调用中断
- 下一节：[22.10 同步等待信号](./22.10-synchronously-waiting-for-a-signal.md)
