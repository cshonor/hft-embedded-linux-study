# 22.15 练习题

> 本章：[TLPI 第 22 章 — 高级信号处理](../README.md)

## 本节讲什么

原书第 22 章练习题要点。

<details>
<summary>1. 用 sigsuspend 实现一个原子的 "等待 SIGUSR1 或 SIGUSR2" 逻辑</summary>

```c
sigset_t mask, oldmask;
sigemptyset(&mask);
sigaddset(&mask, SIGUSR1);
sigaddset(&mask, SIGUSR2);
sigprocmask(SIG_BLOCK, &mask, &oldmask);

/* oldmask 不含 SIGUSR1/SIGUSR2 → 原子解除 + 等待 */
sigsuspend(&oldmask);

sigprocmask(SIG_SETMASK, &oldmask, NULL);
```
</details>

<details>
<summary>2. sigwaitinfo 和 sigsuspend 有什么本质区别？</summary>

sigsuspend 是异步模式：信号到达后触发 handler，handler 返回后 sigsuspend 返回。sigwaitinfo 是同步模式：不触发 handler，信号直接被消费，sigwaitinfo 返回信号编号和 siginfo。sigwaitinfo 不需要注册 handler。
</details>

<details>
<summary>3. 为什么 signalfd 要求信号先被阻塞？</summary>

如果不阻塞，信号到达时内核会触发 handler（如果有注册）或执行默认动作（如终止进程）。阻塞后信号进入 pending 队列，signalfd 的 read 能从中取出。这保证了信号只被一种机制消费。
</details>

## 与后续衔接

- Ch23 定时器 — 定时器信号处理
- Ch26 IPC 概览

---

## 代码自测

<details>
<summary>参考答案</summary>

参见原书附录 F。
</details>
