# 22.7 `EINTR` 再强调

> 本章：[TLPI 第 22 章 — Signals: Advanced Features](../README.md)
> - 上一节：[22.6 `prctl`（Linux）](22.6-timing-and-order-of-signal-delivery.md)
> - 下一节：[22.8 三大等待对比](22.8-realtime-signals.md)

---

## 本节讲什么

不靠 `SA_RESTART` 包打天下；`connect`/`accept`/`sleep`/`epoll_wait` 等行为因调用而异 → **显式重试**。 ---

---

## 要点

不靠 `SA_RESTART` 包打天下；`connect`/`accept`/`sleep`/`epoll_wait` 等行为因调用而异 → **显式重试**。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `EINTR` 再强调 在嵌入式系统的应用场景 |
| HFT | `EINTR` 再强调 对低延迟和性能的影响 |

## 代码示例

```c
#include <signal.h>
#include <unistd.h>
#include <stdio.h>
void handler(int sig) { printf("handler\n"); }
int main(void) {
    struct sigaction sa = {0};
    sa.sa_handler = handler;
    sa.sa_flags = 0;  /* 无 SA_RESTART */
    sigaction(SIGUSR1, &sa, NULL);

    printf("sleep 5 秒...\n");
    unsigned remain = sleep(5);
    printf("sleep 剩余 %u 秒 (被信号中断)\n", remain);

    /* 处理 EINTR 的标准模式:
     * while (read(fd, buf, n) < 0 && errno == EINTR) continue;
     */
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[22.6 `prctl`（Linux）](22.6-timing-and-order-of-signal-delivery.md)
- 下一节：[22.8 三大等待对比](22.8-realtime-signals.md)
- 前置：**前置**：[Ch21 Signal Handlers](../../chapter-21-signal-handlers/notes)  
- 后置：**后置**：[Ch23 Timers and Sleeping](../../chapter-23-timers-sleeping/notes) · [Ch29+ 线程](../../chapter-29-threads-intro/notes) · Ch63 多路 I/O
