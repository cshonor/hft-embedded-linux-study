# 22.3 同步等待：`sigwait` 族

> 本章：[TLPI 第 22 章 — Signals: Advanced Features](../README.md)
> - 上一节：[22.2 `sigsuspend()`（核心）](22.2-special-cases-for-delivery-disposition-a.md)
> - 下一节：[22.4 实时信号与 `sigqueue`](22.4-hardware-generated-signals.md)

---

## 本节讲什么

**不跑 handler**，从 pending 里同步取出信号。 int sigwait(const sigset_t *set, int *sig); int sigwaitinfo(const sigset_t *set, siginfo_t *info);

---

## 要点

**不跑 handler**，从 pending 里同步取出信号。

```c
int sigwait(const sigset_t *set, int *sig);
int sigwaitinfo(const sigset_t *set, siginfo_t *info);
int sigtimedwait(const sigset_t *set, siginfo_t *info,
                 const struct timespec *timeout);
```

多线程推荐：

1. 关心的信号在所有线程上**先阻塞**  
2. 专用线程循环 `sigwaitinfo`  
3. 处理逻辑可 `malloc`/`printf`/加锁  

前提：目标信号必须已阻塞，否则可能先异步进 handler。

Demo：[`code/sigwaitinfo_loop.c`](../code/sigwaitinfo_loop.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 同步等待：`sigwait` 族 在嵌入式系统的应用场景 |
| HFT | 同步等待：`sigwait` 族 对低延迟和性能的影响 |

## 代码示例

```c
#include <stdio.h>
#include <signal.h>
#include <unistd.h>
#include <sys/wait.h>
/* sigwait: 同步等待信号 (不用信号处理器) */
int main(void) {
    sigset_t mask;
    sigemptyset(&mask);
    sigaddset(&mask, SIGUSR1);
    sigaddset(&mask, SIGUSR2);
    sigprocmask(SIG_BLOCK, &mask, NULL);  /* 必须先阻塞 */

    /* 子进程发信号 */
    pid_t pid = fork();
    if (pid == 0) {
        sleep(1);
        kill(getppid(), SIGUSR1);
        sleep(1);
        kill(getppid(), SIGUSR2);
        _exit(0);
    }

    /* sigwait: 同步等待, 不需要 handler */
    int sig;
    for (int i = 0; i < 2; i++) {
        sigwait(&mask, &sig);
        printf("sigwait returned signal %d\n", sig);
    }

    waitpid(pid, NULL, 0);
    printf("Done (no signal handler needed!)\n");
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[22.2 `sigsuspend()`（核心）](22.2-special-cases-for-delivery-disposition-a.md)
- 下一节：[22.4 实时信号与 `sigqueue`](22.4-hardware-generated-signals.md)
- 前置：**前置**：[Ch21 Signal Handlers](../../chapter-21-signal-handlers/notes)  
- 后置：**后置**：[Ch23 Timers and Sleeping](../../chapter-23-timers-sleeping/notes) · [Ch29+ 线程](../../chapter-29-threads-intro/notes) · Ch63 多路 I/O
