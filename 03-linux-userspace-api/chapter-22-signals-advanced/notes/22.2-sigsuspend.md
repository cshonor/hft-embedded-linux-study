# 22.2 `sigsuspend()`（核心）

> 本章：[TLPI 第 22 章 — Signals: Advanced Features](./README.md)
> - 上一节：[22.1 `pause()`](./22.1-pause.md)
> - 下一节：[22.3 同步等待：`sigwait` 族](./22.3-sigwait.md)

---

## 本节讲什么

int sigsuspend(const sigset_t *mask); 原子步骤： 1. 将线程掩码**换成** `mask`

---

## 要点

```c
int sigsuspend(const sigset_t *mask);
```

原子步骤：

1. 将线程掩码**换成** `mask`  
2. 休眠等信号  
3. 捕获并跑完 handler 后，**恢复调用前掩码**  
4. 返回 `-1` / `EINTR`

安全范式：

```c
/* 1) 先 SIG_BLOCK 目标信号 */
/* 2) wait_mask = 当前掩码，但去掉要等的信号 */
while (!got_signal)
    sigsuspend(&wait_mask);
```

| | `sigprocmask` + `pause` | `sigsuspend` |
|--|-------------------------|--------------|
| 改掩码+睡 | 非原子 | **原子** |

Demo：[`code/sigsuspend_wait.c`](./code/sigsuspend_wait.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `sigsuspend()`（核心） 在嵌入式系统的应用场景 |
| HFT | `sigsuspend()`（核心） 对低延迟和性能的影响 |

## 代码示例

```c
#include <stdio.h>
#include <signal.h>
#include <unistd.h>
/* sigsuspend: 原子地"解除阻塞+等待信号" */
static volatile sig_atomic_t got = 0;

void handler(int sig) { got = 1; }

int main(void) {
    signal(SIGUSR1, handler);

    sigset_t mask, old;
    sigemptyset(&mask);
    sigaddset(&mask, SIGUSR1);
    sigprocmask(SIG_BLOCK, &mask, &old);

    printf("SIGUSR1 blocked. Sending to self...\n");
    raise(SIGUSR1);  /* 变为待决 */

    /* 错误方式: 先解除阻塞再 pause 有竞态
     * sigprocmask(SIG_UNBLOCK, &mask, NULL);
     * pause();  // 信号可能在 pause 之前递送!
     */

    /* 正确方式: sigsuspend 原子操作 */
    sigset_t empty;
    sigemptyset(&empty);
    sigsuspend(&empty);  /* 原子: 解除所有阻塞 + 等待 */

    printf("got = %d\n", got);
    sigprocmask(SIG_SETMASK, &old, NULL);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[22.1 `pause()`](./22.1-pause.md)
- 下一节：[22.3 同步等待：`sigwait` 族](./22.3-sigwait.md)
- 前置：**前置**：[Ch21 Signal Handlers](../chapter-21-signal-handlers/notes.md)  
- 后置：**后置**：[Ch23 Timers and Sleeping](../chapter-23-timers-sleeping/notes.md) · [Ch29+ 线程](../chapter-29-threads-intro/notes.md) · [Ch63 多路 I/O](../chapter-63-alternative-io/notes.md)
