# 28.5 exec 资源细则（承 Ch27）

> 本章：[TLPI 第 28 章 — Process Creation and Program Execution in More Detail](../README.md)
> - 上一节：[28.4 `clone`（Linux）](28.2-the-clone-system-call.md)
> - 下一节：[28.6 `FD_CLOEXEC` / `O_CLOEXEC`](28.6-exercise.md)

---

## 本节讲什么

---

## 要点

| ✅ 保留 | ❌ 销毁/重置 |
|---------|--------------|
| PID/PGID/SID、cwd、umask | 整块用户地址空间 |
| **信号掩码** | **handler → SIG_DFL** |
| 未 CLOEXEC 的 fd | pending 清空、atexit 丢弃 |
| rlimit… | 其它线程、多数锁/定时器/AIO… |

### 超级对比表

| 操作 | 信号掩码 | handler | pending |
|------|----------|---------|---------|
| **fork** | 继承 | 继承 | **清空** |
| **exec** | **继承** | **→ SIG_DFL** | **清空** |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | exec 保留信号掩码/cwd/fd，重置 handler→SIG_DFL |
| HFT | exec 后 handler 重置、掩码保留，worker 需按需重装 handler |

## 代码示例

```c
#include <unistd.h>
#include <stdio.h>
/* exec 内核细节:
 * 1. 检查文件格式 (ELF? shebang?)
 * 2. 分配新地址空间, 丢弃旧的
 * 3. 加载代码/数据段
 * 4. PID 不变, fd 默认保留 (除 FD_CLOEXEC)
 * 5. 信号处理器重置 (忽略/默认保留, 自定义的改为默认)
 */
int main(void) {
    pid_t pid = fork();
    if (pid == 0) {
        char *args[] = {"ps", NULL};
        execvp("ps", args);
        _exit(1);
    }
    wait(NULL);
    printf("exec: PID 不变, 映像替换\n");
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[28.4 `clone`（Linux）](28.2-the-clone-system-call.md)
- 下一节：[28.6 `FD_CLOEXEC` / `O_CLOEXEC`](28.6-exercise.md)
- 前置：**前置**：[Ch24](../../chapter-24-process-creation/notes) · [Ch25](../../chapter-25-process-termination/notes) · [Ch26](../../chapter-26-monitoring-child-processes/notes) · [Ch27](../../chapter-27-program-execution/notes)  
- 后置：**后置**：[Ch29 线程导论](../../chapter-29-threads-intro/notes)（同步见 [Ch30](../../chapter-30-thread-synchronization/notes)）· 凭证见 [Ch9](../../chapter-09-process-credentials/notes)
