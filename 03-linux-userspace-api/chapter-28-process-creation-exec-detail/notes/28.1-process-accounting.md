# 28.1 fork 再深入

> 本章：[TLPI 第 28 章 — Process Creation and Program Execution in More Detail](../README.md)
> - 上一节：本章首页
> - 下一节：[28.2 多线程 + fork](28.2-the-clone-system-call.md)

---

## 本节讲什么

fork 复制页表、页标只读；写时缺页再拷贝物理页。现代 Linux fork 开销低；逻辑上地址空间仍独立。 Demo：[`code/fork_signal_state.c`](../code/fork_signal_state.c) ---

---

## 要点

### COW

fork 复制页表、页标只读；写时缺页再拷贝物理页。现代 Linux fork 开销低；逻辑上地址空间仍独立。

### 继承 vs 不继承（精要）

| ✅ 继承 | ❌ 不继承 |
|---------|-----------|
| 地址空间（COW）、fd 表（共享 `struct file`/偏移） | 新 PID/PPID |
| 信号 **handler**、信号 **掩码** | **pending 清空** |
| 凭证、cwd、umask、rlimit、PGID/SID… | 其它线程（只留调用线程） |
| | 文件锁、部分定时器/AIO/inotify 等 |

> 掩码继承；pending 清空。

Demo：[`code/fork_signal_state.c`](../code/fork_signal_state.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | fork 复制页表标只读，写时缺页再拷贝，省 RAM |
| HFT | COW 使 fork 开销低；继承/不继承清单是 fork 子进程环境控制的依据 |

## 代码示例

```c
#include <unistd.h>
#include <sys/wait.h>
#include <stdio.h>
/* fork 内核细节:
 * - 分配新 task_struct, 新 PID
 * - 复制父进程的 mm_struct (COW)
 * - 复制 fd 表 (共享打开文件描述)
 * - 复制信号处理器和掩码
 */
int main(void) {
    pid_t pid = fork();
    if (pid == 0) { printf("子: PID=%d\n", getpid()); _exit(0); }
    wait(NULL);
    printf("父: PID=%d\n", getpid());
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 下一节：[28.2 多线程 + fork](28.2-the-clone-system-call.md)
- 前置：**前置**：[Ch24](../../chapter-24-process-creation/notes) · [Ch25](../../chapter-25-process-termination/notes) · [Ch26](../../chapter-26-monitoring-child-processes/notes) · [Ch27](../../chapter-27-program-execution/notes)  
- 后置：**后置**：[Ch29 线程导论](../../chapter-29-threads-intro/notes)（同步见 [Ch30](../../chapter-30-thread-synchronization/notes)）· 凭证见 [Ch9](../../chapter-09-process-credentials/notes)
