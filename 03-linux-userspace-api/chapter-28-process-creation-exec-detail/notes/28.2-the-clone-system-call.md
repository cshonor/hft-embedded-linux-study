# 28.2 多线程 + fork

> 本章：[TLPI 第 28 章 — Process Creation and Program Execution in More Detail](../README.md)
> - 上一节：[28.1 fork 再深入](28.1-process-accounting.md)
> - 下一节：[28.3 `vfork`](28.3-speed-of-process-creation.md)

---

## 本节讲什么

POSIX：子进程**只保留发起 fork 的线程**；其它线程不跑清理 → 锁/堆状态危险。 ---

---

## 要点

POSIX：子进程**只保留发起 fork 的线程**；其它线程不跑清理 → 锁/堆状态危险。

| 方案 | |
|------|--|
| **最优** | fork 后立刻 exec |
| 缓解 | `pthread_atfork(prepare, parent, child)` 统一加解锁 |
| 避免 | fork 后子进程继续跑复杂多线程逻辑 |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 多线程 + fork 在嵌入式系统的应用场景 |
| HFT | 多线程 + fork 对低延迟和性能的影响 |

## 代码示例

```c
#include <unistd.h>
#include <stdio.h>
int main(void) {
    /* fork 后父子执行顺序不确定!
     * 不要依赖执行顺序, 用同步机制 (wait/pipe/signal) */
    pid_t pid = fork();
    if (pid == 0) { printf("子进程\n"); _exit(0); }
    /* 不保证子先还是父先执行 */
    wait(NULL);
    printf("父进程\n");
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[28.1 fork 再深入](28.1-process-accounting.md)
- 下一节：[28.3 `vfork`](28.3-speed-of-process-creation.md)
- 前置：**前置**：[Ch24](../../chapter-24-process-creation/notes) · [Ch25](../../chapter-25-process-termination/notes) · [Ch26](../../chapter-26-monitoring-child-processes/notes) · [Ch27](../../chapter-27-program-execution/notes)  
- 后置：**后置**：[Ch29 线程导论](../../chapter-29-threads-intro/notes)（同步见 [Ch30](../../chapter-30-thread-synchronization/notes)）· 凭证见 [Ch9](../../chapter-09-process-credentials/notes)
