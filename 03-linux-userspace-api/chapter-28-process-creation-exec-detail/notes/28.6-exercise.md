# 28.6 `FD_CLOEXEC` / `O_CLOEXEC`

> 本章：[TLPI 第 28 章 — Process Creation and Program Execution in More Detail](../README.md)
> - 上一节：[28.5 exec 资源细则（承 Ch27）](28.4-effect-of-exec-and-fork-on-process-attri.md)
> - 下一节：[28.7 fork + exec 规范](28.4-effect-of-exec-and-fork-on-process-attri.md)

---

## 本节讲什么

打开即 `O_CLOEXEC`，避免 fork 与 exec 之间的竞态。见 [Ch27 cloexec demo](../../chapter-27-program-execution/code/cloexec_demo.c)。 ---

---

## 要点

| | fork | exec |
|--|------|------|
| CLOEXEC fd | 不关（标记可复制） | **关掉** |

打开即 `O_CLOEXEC`，避免 fork 与 exec 之间的竞态。见 [Ch27 cloexec demo](../../chapter-27-program-execution/code/cloexec_demo.c)。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | O_CLOEXEC 原子设置，避免 fork/exec 间竞态 |
| HFT | O_CLOEXEC 是防 fd 泄漏到 worker 的最佳实践（原子无竞态） |

## 代码示例

```c
#include <fcntl.h>
#include <unistd.h>
#include <stdio.h>
int main(void) {
    /* 对比: open(O_CLOEXEC) vs fcntl(FD_CLOEXEC) */
    /* O_CLOEXEC: 原子操作, 无竞态 */
    int fd1 = open("/tmp/a.txt", O_CREAT | O_CLOEXEC, 0644);

    /* fcntl: open + fcntl 之间有竞态 (多线程 exec) */
    int fd2 = open("/tmp/b.txt", O_CREAT, 0644);
    fcntl(fd2, F_SETFD, FD_CLOEXEC);  /* 有间隙! */

    printf("O_CLOEXEC (原子) > open+fcntl (有竞态)\n");
    close(fd1); close(fd2);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[28.5 exec 资源细则（承 Ch27）](28.4-effect-of-exec-and-fork-on-process-attri.md)
- 下一节：[28.7 fork + exec 规范](28.4-effect-of-exec-and-fork-on-process-attri.md)
- 前置：**前置**：[Ch24](../../chapter-24-process-creation/notes) · [Ch25](../../chapter-25-process-termination/notes) · [Ch26](../../chapter-26-monitoring-child-processes/notes) · [Ch27](../../chapter-27-program-execution/notes)  
- 后置：**后置**：[Ch29 线程导论](../../chapter-29-threads-intro/notes)（同步见 [Ch30](../../chapter-30-thread-synchronization/notes)）· 凭证见 [Ch9](../../chapter-09-process-credentials/notes)
