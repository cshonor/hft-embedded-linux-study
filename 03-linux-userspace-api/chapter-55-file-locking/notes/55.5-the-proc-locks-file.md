# 55.5 劝告 vs 强制（Linux）

> 本章：[TLPI 第 55 章 — File Locking](../README.md)
> - 上一节：[55.3 –55.4 `fcntl` 记录锁](55.3-record-locking-with-fcntl.md)
> - 下一节：[55.6 –55.9 对比 · 运维 · 场景](55.6-running-just-one-instance-of-a-program.md)

---

## 本节讲什么

强制：挂载 `-o mand` + 文件 `g+s,g-x`。IO 被内核拦截。 TLPI：慢、有坑、不可移植 → **别用**。 ---

---

## 要点

强制：挂载 `-o mand` + 文件 `g+s,g-x`。IO 被内核拦截。  
TLPI：慢、有坑、不可移植 → **别用**。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | Linux 强制锁慢且有坑、不可移植，实际工程几乎不用 |
| HFT | 强制锁会拖慢每次读写、引入不可控延迟，HFT 千万别用，用劝告锁 + 进程协作 |

## 代码示例

```c

/* 锁的继承与释放：fork 和 close 的行为 */
#include <stdio.h>
#include <stdlib.h>
#include <fcntl.h>
#include <unistd.h>
#include <sys/file.h>
#include <sys/wait.h>

int main(void) {
    int fd = open("/tmp/ch55_inherit.txt", O_RDWR | O_CREAT, 0644);

    /* flock 锁在 fork 后：父子共享同一锁（不是各自一份） */
    flock(fd, LOCK_EX);
    printf("parent: flock LOCK_EX acquired\n");

    pid_t pid = fork();
    if (pid == 0) {
        /* 子进程继承 fd，但不继承 flock 锁的所有权 */
        /* 子进程可以解锁父进程的锁 */
        flock(fd, LOCK_UN);
        printf("child: unlocked parent's flock\n");
        _exit(0);
    }
    wait(NULL);

    /* fcntl 记录锁：fork 后子进程不继承锁 */
    struct flock fl = { .l_type = F_WRLCK, .l_whence = SEEK_SET,
                        .l_start = 0, .l_len = 0 };
    fcntl(fd, F_SETLK, &fl);
    printf("parent: fcntl F_WRLCK acquired\n");

    /* close(fd) 释放该 fd 持有的所有 fcntl 锁 */
    printf("close(fd) releases all fcntl locks on this fd\n");

    close(fd);
    return 0;
}

```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[55.3 –55.4 `fcntl` 记录锁](55.3-record-locking-with-fcntl.md)
- 下一节：[55.6 –55.9 对比 · 运维 · 场景](55.6-running-just-one-instance-of-a-program.md)
- 前置：**前置**：[Ch54 POSIX shm](../../chapter-54-posix-shared-memory/notes) · 文件 I/O  
- 后置：**后置**：[Ch56 Sockets 导论](../../chapter-56-sockets-intro/notes)
