# 55.6 –55.9 对比 · 运维 · 场景

> 本章：[TLPI 第 55 章 — File Locking](./README.md)
> - 上一节：[55.5 劝告 vs 强制（Linux）](./55.5-section-55-5.md)
> - 下一节：[下一章](../chapter-{int(ch_num)+1:02d}-*/notes.md)

---

## 本节讲什么

`/proc/locks` 查看。 **PID 文件单实例**：`F_WRLCK` 成功 → 写 pid；失败 → 已有实例；崩溃内核放锁。 ---

---

## 要点

| | flock | fcntl |
|--|-------|-------|
| 标准 | BSD | POSIX |
| 区域 | 整文件 | 任意字节 |
| 绑定 | OFD | 进程+inode |
| close | 该 OFD 的锁 | **进程全锁** |
| 强制锁 | 否 | 可（勿用） |

`/proc/locks` 查看。  
**PID 文件单实例**：`F_WRLCK` 成功 → 写 pid；失败 → 已有实例；崩溃内核放锁。

---

---

## 与 C 语言学习的关系

本节涉及 `close()`，对应 K&R 8.3 节。C 语言中资源管理靠手动——没有 C++ 的 RAII。`close` 必须在所有代码路径上调用，这是 C 语言内存/资源管理的核心训练点。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | –55.9 对比 · 运维 · 场景 在嵌入式系统的应用场景 |
| HFT | –55.9 对比 · 运维 · 场景 对低延迟和性能的影响 |

---

## 常见陷阱

1. **close 返回值未检查**

```c
close(fd);  /* NFS 上可能报延迟错误 */
```

close 可能返回错误（尤其在 NFS 上）。不检查会丢失延迟的写错误。生产代码应检查。

2. **fd 泄漏**

```c
int fd = open(...);
if (error) return;  /* fd 泄漏! */
close(fd);
```

打开 fd 后，在所有错误路径上都必须 close。用 goto cleanup 模式或 RAII。

3. **double close**

```c
close(fd);
/* ... */
close(fd);  /* 可能关闭了复用的 fd */
```

close 后 fd 被回收。如果另一个 open 复用了同一编号，double close 会关错文件。close 后应置 -1。

---

## 自测题

**题目 1（选择）：** `close(fd)` 后 fd 的值变为？
A. 0
B. -1
C. 不变但不可用
D. 自动设为 -1

<details>
<summary>参考答案</summary>

C。close 不会修改 fd 变量的值，但该 fd 编号已被回收，可被后续 open 复用。建议手动置 -1。

</details>

**题目 2（判断）：** `close` 不可能失败。

<details>
<summary>参考答案</summary>

错误。close 可能失败，尤其在 NFS 上。延迟的写错误可能在 close 时才报告。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
int fd = open("f.txt", O_RDONLY);
if (read(fd, buf, n) < 0)
    return -1;
close(fd);
```

<details>
<summary>参考答案</summary>

read 失败时直接 return，没有 close(fd)，导致 fd 泄漏。应改为 goto cleanup 或在 return 前 close。

</details>

**题目 4（应用）：** 写一个安全的文件读取函数，确保所有路径都关闭 fd。

<details>
<summary>参考答案</summary>

```c
int read_file(const char *path, char *buf, size_t n) {
    int fd = open(path, O_RDONLY);
    if (fd < 0) return -1;
    ssize_t r = read(fd, buf, n);
    close(fd);
    return (r < 0) ? -1 : r;
}
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[55.5 劝告 vs 强制（Linux）](./55.5-section-55-5.md)
- 前置：**前置**：[Ch54 POSIX shm](../chapter-54-posix-shared-memory/notes.md) · 文件 I/O  
- 后置：**后置**：[Ch56 Sockets 导论](../chapter-56-sockets-intro/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
