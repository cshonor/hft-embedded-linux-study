# 01.4 标准化：POSIX / SUS / LSB

> 本章：[TLPI 第 01 章 — History and Standards](./README.md)
> - 上一节：[01.3 Linux = 内核 + GNU 工具链（必分清）](./01.3-gnu.md)
> - 下一节：[01.5 贯穿全书：POSIX vs Linux 扩展](./01.5-section-01-5.md)

---

## 本节讲什么

**一句话（双线共用）：主力标准 POSIX；SUS 当拓展参考；LSB 工程里忘掉。** - **UNIX（商标）**：须通过 SUS 等认证。 - **Linux**：遵循 POSIX 接口语义，但**没有**官方 UNIX 认证 → 习惯称「类 UNIX」。

---

## 要点

**一句话（双线共用）：主力标准 POSIX；SUS 当拓展参考；LSB 工程里忘掉。**

| 标准 | 层级 | 作用 | 对本仓库 |
|------|------|------|----------|
| **POSIX**（IEEE 1003.1） | 源码级可移植 | 定义 syscall/文件 I/O/进程/信号/pthread/实时接口、`clock_gettime`、`mmap`… | **主力**；Linux 尽可能兼容（未花钱官方认证，日常按这套语义写） |
| **SUS**（Single UNIX Specification） | POSIX + 额外扩展 | 通过认证才能合法用 **「UNIX」商标** | 了解即可；发行版几乎不花钱认证；写代码很少刻意追 SUS |
| **LSB**（Linux Standard Base） | 曾求跨发行版**二进制**兼容 | 一份编译产物多发行版跑 | **已废**：末版约 2015，停维护；Yocto/主流发行版已撤；嵌入式/HFT **不考虑**。Primer 为何仍写：见 [08-lsb-vs-posix](../../08-embedded-boot-build/primer-system-overview/chapter-01-introduction/1.4-lsb-vs-posix.md) |

### 易混考点：Linux ≠ UNIX（商标）

- **UNIX（商标）**：须通过 SUS 等认证。  
- **Linux**：遵循 POSIX 接口语义，但**没有**官方 UNIX 认证 → 习惯称「类 UNIX」。

---

---

## 与 C 语言学习的关系

系统调用与 C 标准库的关系是 C 语言系统编程的核心概念。C 标准只定义函数接口（如 `fopen`），不规定实现；Linux 上由 glibc 实现并最终调用内核系统调用。理解这一分层对掌握 Unix C 编程至关重要。对照 K&R 第 8 章学习文件 I/O 的 C 标准库接口，与本模块的 POSIX 系统调用接口对比。
---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 标准化：POSIX / SUS / LSB 在嵌入式系统的应用场景 |
| HFT | 标准化：POSIX / SUS / LSB 对低延迟和性能的影响 |

---

## 常见陷阱

1. **混淆系统调用与库函数**

```c
/* printf 是库函数，write 是系统调用 */
printf("hello
");      /* glibc 处理缓冲+格式化 */
write(1, "hello
", 6);  /* 直接陷入内核 */
```

系统调用是内核接口（open/read/write），库函数是用户态封装（fopen/fread/fwrite）。库函数内部可能调用系统调用，但不是系统调用本身。

2. **errno 检查时机错误**

```c
int fd = open("f.txt", O_RDONLY);
if (errno) perror("open");  /* 错误! */
if (fd < 0) perror("open");  /* 正确 */
```

errno 只在系统调用失败时被设置，成功时不清零。必须先检查返回值判断是否出错，再读 errno。

3. **假设系统调用跨平台一致**

```c
/* Linux: signal() 不恢复默认handler */
/* BSD: signal() 恢复默认handler */
signal(SIGINT, handler);
```

同名的系统调用/库函数在不同 Unix 变体上行为可能不同。生产代码应查文档并用 POSIX 接口（如 sigaction 代替 signal）。

---

## 自测题

**题目 1（选择）：** 系统调用失败时通常返回什么？
A. 0
B. -1
C. NULL
D. 取决于函数

<details>
<summary>参考答案</summary>

B。大多数系统调用失败时返回 -1 并设置 errno。但 pthread 函数返回错误码，mmap 返回 MAP_FAILED。

</details>

**题目 2（判断）：** errno 在函数成功返回时会被清零。

<details>
<summary>参考答案</summary>

错误。errno 只在失败时被设置，成功时不会清零。不能通过 errno 判断是否出错，必须先检查返回值。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
FILE *fp = fopen("f.txt", "r");
if (errno) {
    printf("error: %s
", strerror(errno));
}
```

<details>
<summary>参考答案</summary>

fopen 成功时 errno 不被清零——可能保留前一次调用的错误值。应检查返回值：`if (fp == NULL)` 而非 `if (errno)`。

</details>

**题目 4（应用）：** 写一个通用的错误检查宏，检查系统调用返回值并在失败时报错退出。

<details>
<summary>参考答案</summary>

```c
#define CHECK(call) do { \
    if ((call) < 0) { \
        perror(#call); \
        exit(EXIT_FAILURE); \
    } \
} while(0)
CHECK(open("f.txt", O_RDONLY));
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[01.3 Linux = 内核 + GNU 工具链（必分清）](./01.3-gnu.md)
- 下一节：[01.5 贯穿全书：POSIX vs Linux 扩展](./01.5-section-01-5.md)

---

## man 手册参考

- `man 2 clock_gettime`
- `man 2 mmap`
