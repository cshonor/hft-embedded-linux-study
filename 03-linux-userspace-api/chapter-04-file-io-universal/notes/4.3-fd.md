# 4.3 文件描述符 FD

> 本章：[TLPI 第 04 章 — File I/O: The Universal I/O Model](./README.md)
> - 上一节：[4.2 通用 I/O 模型（核心思想）](./4.2-universal-model.md)
> - 下一节：[4.4 `open()`](./4.4-open.md)

---

## 本节讲什么

→ 内核三层（fd 表 / open file description / inode）见书内 Ch5 · [LKD §3.8](../../05-linux-kernel/00_Book_3rd_Notes/chapter-03-process-management/notes/section-3.8-身份PID与资源FD.md) ---

---

## 要点

| | |
|--|--|
| 定义 | 非负整数；进程内引用 **已打开文件** |
| 默认 | `0` `STDIN_FILENO` · `1` `STDOUT_FILENO` · `2` `STDERR_FILENO`（`<unistd.h>`） |
| 私有 | fd 是 **进程私有**；两进程同数字 fd 通常指向 **不同** 打开实例 |

→ 内核三层（fd 表 / open file description / inode）见书内 Ch5 · [LKD §3.8](../../05-linux-kernel/00_Book_3rd_Notes/chapter-03-process-management/notes/section-3.8-身份PID与资源FD.md)

## 代码示例

```c
#include <fcntl.h>
#include <unistd.h>
#include <stdio.h>

int main(void) {
    /* 0/1/2 默认打开：STDIN/STDOUT/STDERR */
    printf("STDIN_FILENO = %d\n", STDIN_FILENO);   /* 0 */
    printf("STDOUT_FILENO = %d\n", STDOUT_FILENO); /* 1 */
    printf("STDERR_FILENO = %d\n", STDERR_FILENO); /* 2 */

    /* 新 fd 从最小可用数字开始分配 */
    int fd1 = open("/tmp/a.txt", O_CREAT | O_RDWR, 0644);  /* 期望 3 */
    int fd2 = open("/tmp/b.txt", O_CREAT | O_RDWR, 0644);  /* 期望 4 */
    printf("fd1 = %d, fd2 = %d\n", fd1, fd2);

    /* 关闭 fd1 后，3 被回收，下次 open 复用 */
    close(fd1);
    int fd3 = open("/tmp/c.txt", O_CREAT | O_RDWR, 0644);  /* 期望 3 */
    printf("fd3 = %d (复用最小可用)\n", fd3);

    close(fd2);
    close(fd3);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[4.2 通用 I/O 模型（核心思想）](./4.2-universal-model.md)
- 下一节：[4.4 `open()`](./4.4-open.md)
- 前置：**前置**：Ch2 基本概念（fd）· Ch3 系统编程概念（错误处理等）  
- 后置：**后置**：书内 Ch5 Further Details → [`../chapter-05-file-io-further/`](../chapter-05-file-io-further/)
