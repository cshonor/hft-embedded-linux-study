# 4.10 Exercises

> 本章：[TLPI 第 04 章 — File I/O: The Universal I/O Model](../README.md)
> 源码核验：Linux **v6.6**（`fs/read_write.c`、`fs/open.c`、`include/linux/fs.h`）
> - 上一节：[4.9 Summary](4.9-summary.md)
> - 下一节：[Ch5 文件 I/O 深入](../../chapter-05-file-io-further/README.md)

---

## 本节讲什么

用四道练习把 4.1–4.8 的知识串起来：文件拷贝、随机定位、部分写、追加原子性。每道题都对应一个「实战必踩」的机制点。

---

## 练习 1：实现简化版 `cp`（短读 + 部分写）

用 `open/read/write/close` 实现文件拷贝，要求处理短读和部分写。

<details>
<summary>参考答案</summary>

```c
#include <fcntl.h>
#include <unistd.h>
#include <stdio.h>

int main(int argc, char *argv[]) {
    if (argc != 3) { fprintf(stderr, "usage: %s src dst\n", argv[0]); return 1; }

    int src = open(argv[1], O_RDONLY);
    if (src < 0) { perror("open src"); return 1; }

    int dst = open(argv[2], O_WRONLY|O_CREAT|O_TRUNC, 0644);
    if (dst < 0) { perror("open dst"); return 1; }

    char buf[8192];
    ssize_t n;
    while ((n = read(src, buf, sizeof(buf))) > 0) {     /* 循环读（短读） */
        ssize_t written = 0;
        while (written < n) {                            /* 循环写（部分写） */
            ssize_t w = write(dst, buf + written, n - written);
            if (w < 0) { perror("write"); return 1; }
            written += w;
        }
    }
    if (n < 0) perror("read");

    if (close(src) < 0) perror("close src");
    if (close(dst) < 0) perror("close dst");
    return 0;
}
```

</details>

## 练习 2：文件内容倒序输出（`lseek` 随机定位）

用 `lseek` 从末尾向前逐字节读，倒序输出。

<details>
<summary>参考答案</summary>

```c
#include <fcntl.h>
#include <unistd.h>
#include <stdio.h>

int main(int argc, char *argv[]) {
    int fd = open(argv[1], O_RDONLY);
    if (fd < 0) { perror("open"); return 1; }

    off_t size = lseek(fd, 0, SEEK_END);   /* 拿文件大小（顺带移到末尾） */
    for (off_t pos = size - 1; pos >= 0; pos--) {
        lseek(fd, pos, SEEK_SET);
        char c;
        if (read(fd, &c, 1) == 1)
            write(STDOUT_FILENO, &c, 1);
    }
    close(fd);
    return 0;
}
```

</details>

## 练习 3：解释 short write 的至少 3 种成因

<details>
<summary>参考答案</summary>

1. **管道/FIFO 缓冲不足**：写入超过 `PIPE_BUF` 或缓冲区剩余空间，只写满就返回。
2. **信号中断**：`EINTR`，部分数据已写入后被打断。
3. **磁盘空间不足**：`ENOSPC`，只写入剩余空间能容纳的部分。
4. **网络 socket**：TCP 发送缓冲区部分填满，只拷贝了能放下的字节。

对应内核机制：`vfs_write` 不做「保证写满」的承诺，实际写入量由 `f_op->write` 决定（`fs/read_write.c:564`）。

</details>

## 练习 4：`O_APPEND` 的原子性

多进程同时写同一文件，`O_APPEND` 会交错吗？不用 `O_APPEND` 呢？

<details>
<summary>参考答案</summary>

- **有 `O_APPEND`**：不会交错。内核在**每次 `write` 前**原子地把偏移设到文件末尾，然后写。「设偏移 + 写」是一个不可分割的整体，多个进程的追加严格串行。
- **无 `O_APPEND`**：会交错。`lseek(SEEK_END)` + `write()` 是**两步**操作，进程 A 的 `lseek` 和 `write` 之间可能被进程 B 插入，两者都从同一个「旧末尾」写，导致覆盖或交错。

这是经典的 TOCTOU（check-to-use）竞争窗口。`O_APPEND` 把「定位到末尾」这个动作下沉到内核的 `write` 里原子完成，用户态无法用 `lseek+write` 复刻同等的原子性。

</details>

## HFT / 嵌入式关联

| 练习 | 实战兑现 |
|------|---------|
| 练习 1 | HFT 日志归档、固件 OTA 烧写都要处理 short read/write，否则静默丢数据 |
| 练习 2 | 随机访问回放文件（行情 tick 文件按偏移定位）依赖 `lseek` |
| 练习 3 | 理解部分写才能正确设计「批量落盘」与「零丢失」策略 |
| 练习 4 | 多进程行情日志统一用 `O_APPEND`，避免 `lseek+write` 竞争 |

## 衔接

- 上一节：[4.9 Summary](4.9-summary.md) — 本章总结
- 下一章：[Ch5 文件 I/O 深入](../../chapter-05-file-io-further/README.md) — `pread/pwrite` 原子定位读写、`dup`、`fcntl`（替代 `lseek+read` 的竞争写法）
