# 4.7 `lseek()`

> 本章：[TLPI 第 04 章 — File I/O: The Universal I/O Model](../README.md)
> 源码核验：Linux **v6.6**（`fs/read_write.c:230` `default_llseek`、`:285` `vfs_llseek`、`:293` `ksys_lseek`、`:311` `SYSCALL_DEFINE3(lseek)`）
> - 上一节：[4.6 `close()`](4.6-close.md)
> - 下一节：[4.8 `ioctl()`](4.8-ioctl.md)

---

## 本节讲什么

`lseek()` 调整文件偏移。本节回答三个问题：

1. `lseek` 在内核里怎么改 `f_pos`？`SEEK_END` 的「末尾」从哪来？
2. 为什么管道/socket 上 `lseek` 返回 `ESPIPE`？
3. 「文件空洞」是怎么来的？读空洞区得到什么？

---

## 1. 原型与内核链路

```c
#include <unistd.h>
off_t lseek(int fd, off_t offset, int whence);
/* 成功返回新偏移；失败 -1（ESPIPE / EBADF / EINVAL） */
```

| `whence` | 基准 | 语义 |
|----------|------|------|
| `SEEK_SET` | 文件起始 | 偏移 = `offset` |
| `SEEK_CUR` | 当前偏移 | 偏移 = `f_pos + offset` |
| `SEEK_END` | 文件末尾 | 偏移 = `i_size + offset` |
| `SEEK_DATA` / `SEEK_HOLE` | — | 定位到下一个数据/空洞区（部分文件系统支持） |

```
用户态 lseek(fd, offset, whence)
      ▼
SYSCALL_DEFINE3(lseek)         fs/read_write.c:311
      ▼
ksys_lseek()                   fs/read_write.c:293
      ├─→ fdget_pos(fd)       ← 取 struct file，fd 无效 → -EBADF
      ├─→ vfs_llseek()        ← 见下
      └─→ fdput_pos(f)
```

`vfs_llseek()` 先做「可 seek 性」检查（`fs/read_write.c:285`）：

```c
loff_t vfs_llseek(struct file *file, loff_t offset, int whence)
{
	if (!(file->f_mode & FMODE_LSEEK))
		return -ESPIPE;              /* 管道/socket/终端 → 不支持 seek */
	return file->f_op->llseek(file, offset, whence);
}
```

普通文件走默认实现 `default_llseek()`（`fs/read_write.c:230`）：

```c
loff_t default_llseek(struct file *file, loff_t offset, int whence)
{
	struct inode *inode = file_inode(file);
	switch (whence) {
	case SEEK_END:
		offset += i_size_read(inode);   /* 末尾 = inode 记录的大小 */
		break;
	case SEEK_CUR:
		if (offset == 0) { retval = file->f_pos; goto out; }  /* 只查不改 */
		offset += file->f_pos;
		break;
	case SEEK_DATA: ...   case SEEK_HOLE: ...
	}
	if (offset >= 0) { file->f_pos = offset; retval = offset; }
	...
}
```

## 2. 关键语义

| 特性 | 说明 |
|------|------|
| **只改偏移，不产生 I/O** | `lseek` 改的是 `struct file::f_pos`（`:1007`），**不碰磁盘**，也不移动数据 |
| **`SEEK_END` 的末尾来自 inode** | `i_size_read(inode)`——文件大小的权威来源是 inode 的 `i_size`，不是「实际扫到 EOF」 |
| **`SEEK_CUR` + offset=0** | 返回当前 `f_pos` 而不移动，是「查当前偏移」的惯用法 |
| **越尾再写 = 空洞** | `lseek` 跳过尾部再 `write`，中间字节不占磁盘块（读得 0） |
| **不可 seek 对象** | 管道/socket/终端没设 `FMODE_LSEEK` → `-1` + `errno=ESPIPE` |

## 3. 文件空洞（sparse file）

```c
lseek(fd, 1024*1024, SEEK_SET);   /* 跳到 1MB 处 */
write(fd, "x", 1);                /* 中间 1MB 是空洞 */
```

产生一个「逻辑大小 1MB+1，物理只占几 KB」的稀疏文件：

- 空洞区**不分配磁盘块**（`ls -lh` 显示逻辑大小，`du -h` 显示真实占用）
- 读空洞区返回 **0**（内核在页缓存里「现场造零」）
- 拷贝稀疏文件若用普通 `read/write` 循环，会把空洞「读成真的 0 字节」写满目标——`cp` 默认能识别稀疏（`cp --sparse=auto`），自己写拷贝工具要注意

## 4. HFT / 嵌入式关联

| 现象 | 本节机制的兑现 |
|------|----------------|
| 日志文件大小 | `lseek(fd, 0, SEEK_END)` 是「不用 `stat` 拿文件大小」的快捷方式（但要恢复原偏移） |
| 稀疏文件省盘 | 预分配大文件（如行情回放文件）用 `lseek` + 写尾，逻辑大小大、物理占用小 |
| 管道不可 seek | 从管道/socket 读流时不能回退/跳转，只能顺序消费——行情流天然是「只前进」的 |
| 偏移是共享状态 | `f_pos` 在 `struct file` 里，`dup`/`fork` 共享；多进程随机读写要各自独立 `open` |

## 5. 代码示例

```c
#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>

int main(void) {
    int fd = open("/tmp/ch4_lseek.txt", O_RDWR|O_CREAT|O_TRUNC, 0644);
    write(fd, "ABCDEFGHIJ", 10);

    lseek(fd, 3, SEEK_SET);              /* 移到偏移 3 */
    char c; read(fd, &c, 1);
    printf("byte at offset 3: %c\n", c); /* 'D' */

    off_t pos = lseek(fd, 0, SEEK_CUR);  /* 查当前偏移 */
    printf("current offset: %ld\n", (long)pos);

    lseek(fd, -1, SEEK_END);             /* 末尾回退 1 */
    read(fd, &c, 1);
    printf("last byte: %c\n", c);        /* 'J' */
    close(fd);
    return 0;
}
```

## 6. 衔接

- 下一节：[4.8 `ioctl()`](4.8-ioctl.md) — 通用模型之外的控制
- 内核视角（对照 LKD）：
  - [`05/Ch13.3 操作对象`](../../../05-linux-kernel/chapter-13-vfs/notes/section-13.3-操作对象.md) — `struct file::f_pos`
  - [`05/Ch13.5 与文件系统相关的数据结构`](../../../05-linux-kernel/chapter-13-vfs/notes/section-13.5-与文件系统相关的数据结构.md) — inode 与 `i_size`
- TLPI 后续：[Ch5 文件 I/O 深入](../../chapter-05-file-io-further/README.md) · [Ch13 文件 I/O 缓冲](../../chapter-13-file-io-buffering/README.md)

---

<details>
<summary>代码自测（Q&A）</summary>

**Q1：`lseek(fd, 0, SEEK_CUR)` 的作用是什么？**
A：从当前位置偏移 0 字节，即「查询当前偏移而不移动」——这是不用 `stat` 拿当前位置的惯用法。`default_llseek` 里 `SEEK_CUR` 且 `offset==0` 直接返回 `f_pos`。

**Q2：所有文件类型都支持 `lseek` 吗？**
A：不。管道、socket、终端没有 `FMODE_LSEEK` 标志，`vfs_llseek` 返回 `-ESPIPE`。它们是流式对象，数据读走即消失，没有「位置」概念。

**Q3：`lseek` 越尾再写会怎样？为什么「中间不占磁盘」？**
A：产生文件空洞。`lseek` 只改 `f_pos` 不写数据，中间的偏移区间没有对应磁盘块；只有后来 `write` 的尾部字节真正分配块。读空洞区时内核返回 0（页缓存现场造零）。所以逻辑大小可能远大于物理占用。

**Q4：用「`lseek(SEEK_END)` 拿文件大小」有什么要注意的？**
A：`SEEK_END` 会**移动** `f_pos` 到末尾。若之后还要在原位置读写，必须先 `SEEK_CUR` 记住原偏移、查完再 `SEEK_SET` 恢复。更稳妥的是用 `fstat`/`stat` 直接读 `st_size`，不碰偏移。

**Q5：`lseek` 会不会触发磁盘 I/O？**
A：不会。它只改内存里的 `struct file::f_pos`（`include/linux/fs.h:1007`），不读数据、不分配块（除非后续 `write` 到空洞区才分配）。这也是「`lseek` 极快」的原因。

</details>
