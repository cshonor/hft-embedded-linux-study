# 7.1 Program Break（程序断点）

> 本章：[TLPI 第 07 章 — Memory Allocation](./README.md)
> - 上一节：本章首页
> - 下一节：[7.2 `brk` / `sbrk`](./7.2-brk-sbrk.md)

---

## 本节讲什么

堆在 BSS 上方；**program break** = 堆当前上边界。 ---

---

## 要点

堆在 BSS 上方；**program break** = 堆当前上边界。

| 动作 | 效果 |
|------|------|
| 上移断点 | 扩大堆，获得更多**虚拟**地址空间 |
| 下移断点 | 缩小堆，归还虚拟地址给内核 |

> **关键：** 抬高断点**只预留虚拟地址**；**首次访问**时才通过缺页异常分配物理页（延迟分配）。

## 代码示例

```c
#include <unistd.h>
#include <stdio.h>

int main(void) {
    /* sbrk(0) 返回当前 program break（堆上边界） */
    void *brk1 = sbrk(0);
    printf("初始 program break = %p\n", brk1);

    /* 手动抬高断点 4096 字节（分配虚拟地址） */
    void *brk2 = sbrk(4096);
    printf("sbrk(4096) 返回旧 break = %p\n", brk2);
    printf("新 break = %p (差 %ld 字节)\n", sbrk(0), (char *)sbrk(0) - (char *)brk1);

    /* 可以在抬高的区域写数据 */
    char *p = (char *)brk2;
    p[0] = 'X';
    p[4095] = 'Y';
    printf("写入成功: p[0]=%c p[4095]=%c\n", p[0], p[4095]);

    /* 下移断点，归还虚拟地址 */
    sbrk(-4096);
    printf("sbrk(-4096) 后 break = %p (回到原位)\n", sbrk(0));

    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 下一节：[7.2 `brk` / `sbrk`](./7.2-brk-sbrk.md)
- 前置：**前置**：[Ch6 Processes](../chapter-06-processes/notes.md)（堆 / BSS / 地址空间）  
- 后置：**后置**：[Ch8 用户与组](../chapter-08-users-and-groups/notes.md) · [Ch49 Memory Mappings](../chapter-49-memory-mappings/notes.md) · [Ch24 fork / COW](../chapter-24-process-creation/notes.md)


---

# 7.2 `brk` / `sbrk`

> 本章：[TLPI 第 07 章 — Memory Allocation](./README.md)
> - 上一节：[7.1 Program Break（程序断点）](./7.1-program-break.md)
> - 下一节：[7.3 `malloc` / `free`](./7.3-malloc-free.md)

---

## 本节讲什么

#include <unistd.h> int brk(void *end_data_segment);     /* 断点设到绝对地址；0 成功，-1 失败 */ void *sbrk(intptr_t increment);      /* 相对移动；成功返回调整前地址 */

---

## 要点

```c
#include <unistd.h>
int brk(void *end_data_segment);     /* 断点设到绝对地址；0 成功，-1 失败 */
void *sbrk(intptr_t increment);      /* 相对移动；成功返回调整前地址 */
```

| 规则 | |
|------|--|
| `sbrk(0)` | **不移动**，返回当前 break（查边界） |
| `sbrk` 成功 | 返回**调整前**的 break；失败 `(void *)-1` |
| Linux | `sbrk` 多是 glibc 封装，底层 `brk` |
| 业务代码 | **禁止**直接 `brk`/`sbrk`；只为理解 `malloc` |
| 下移过猛 | 勿把断点压到初始堆底以下 → 易 `SIGSEGV` |

Demo：[`code/sbrk_probe.c`](./code/sbrk_probe.c)

## 代码示例

```c
#include <unistd.h>
#include <stdio.h>

int main(void) {
    void *initial = sbrk(0);
    printf("初始 break = %p\n", initial);

    /* sbrk(n): 相对移动 n 字节，返回调整前的 break */
    void *old = sbrk(1024);
    if (old == (void *)-1) { perror("sbrk"); return 1; }
    printf("sbrk(1024): old=%p, new=%p\n", old, sbrk(0));

    /* 在新分配的区域使用 */
    char *buf = (char *)old;
    buf[0] = 'A';
    buf[1023] = 'Z';
    printf("写入: buf[0]=%c, buf[1023]=%c\n", buf[0], buf[1023]);

    /* brk(addr): 设置绝对断点地址 */
    void *target = (char *)initial + 2048;
    if (brk(target) != 0) { perror("brk"); return 1; }
    printf("brk(%p) 后 break = %p\n", target, sbrk(0));

    /* 恢复到初始位置 */
    brk(initial);
    printf("恢复 break = %p\n", sbrk(0));

    /* 注意：业务代码永远不要直接用 brk/sbrk，用 malloc/free */
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[7.1 Program Break（程序断点）](./7.1-program-break.md)
- 下一节：[7.3 `malloc` / `free`](./7.3-malloc-free.md)
- 前置：**前置**：[Ch6 Processes](../chapter-06-processes/notes.md)（堆 / BSS / 地址空间）  
- 后置：**后置**：[Ch8 用户与组](../chapter-08-users-and-groups/notes.md) · [Ch49 Memory Mappings](../chapter-49-memory-mappings/notes.md) · [Ch24 fork / COW](../chapter-24-process-creation/notes.md)


---

# 7.3 `malloc` / `free`

> 本章：[TLPI 第 07 章 — Memory Allocation](./README.md)
> - 上一节：[7.2 `brk` / `sbrk`](./7.2-brk-sbrk.md)
> - 下一节：[7.4 `calloc` / `realloc`](./7.4-calloc-realloc.md)

---

## 本节讲什么

#include <stdlib.h> void *malloc(size_t size);   /* 未初始化；失败 NULL + errno */ void free(void *ptr);

---

## 要点

```c
#include <stdlib.h>
void *malloc(size_t size);   /* 未初始化；失败 NULL + errno */
void free(void *ptr);
```

### 块布局

用户指针**不含**分配器头部；元数据（大小、链表指针等）在用户区**前方**。  
越界写 → 破坏头部 → `free` 常直接崩。

### `free` 要点

1. `ptr` 必须来自 `malloc`/`calloc`/`realloc`；否则 UB。  
2. `free(NULL)` 安全、空操作。  
3. **`free` 不一定下调 program break**  
   - 小块：进 glibc 空闲链表，供后续复用（少 syscall）  
   - 大块连续空闲：才可能 `sbrk(-n)` 还虚拟内存  
4. `free` **不清零**内容（信息残留风险）。

Demo：[`code/free_and_sbrk.c`](./code/free_and_sbrk.c)（Listing 7-1 精神）

## 代码示例

```c
#include <stdlib.h>
#include <stdio.h>
#include <errno.h>
#include <string.h>

int main(void) {
    /* malloc 基本用法 + 错误检查 */
    int *arr = malloc(10 * sizeof(int));
    if (arr == NULL) {
        fprintf(stderr, "malloc 失败: %s\n", strerror(errno));
        return 1;
    }

    /* malloc 不清零！内容不确定 */
    for (int i = 0; i < 10; i++) {
        arr[i] = i * i;
    }
    printf("arr[5] = %d\n", arr[5]);  /* 25 */

    /* realloc 改变大小 */
    int *bigger = realloc(arr, 20 * sizeof(int));
    if (bigger == NULL) {
        free(arr);  /* 原 arr 仍有效 */
        perror("realloc");
        return 1;
    }
    arr = bigger;  /* realloc 成功后旧指针已失效 */
    arr[15] = 999;
    printf("arr[15] = %d (realloc 后的新区域)\n", arr[15]);

    /* free(NULL) 安全，什么都不做 */
    free(NULL);
    printf("free(NULL) 安全\n");

    /* free 后指针变悬挂 — 置 NULL 是好习惯 */
    free(arr);
    arr = NULL;
    printf("free 完成, arr = NULL\n");

    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[7.2 `brk` / `sbrk`](./7.2-brk-sbrk.md)
- 下一节：[7.4 `calloc` / `realloc`](./7.4-calloc-realloc.md)
- 前置：**前置**：[Ch6 Processes](../chapter-06-processes/notes.md)（堆 / BSS / 地址空间）  
- 后置：**后置**：[Ch8 用户与组](../chapter-08-users-and-groups/notes.md) · [Ch49 Memory Mappings](../chapter-49-memory-mappings/notes.md) · [Ch24 fork / COW](../chapter-24-process-creation/notes.md)


---

# 7.4 `calloc` / `realloc`

> 本章：[TLPI 第 07 章 — Memory Allocation](./README.md)
> - 上一节：[7.3 `malloc` / `free`](./7.3-malloc-free.md)
> - 下一节：[7.5 `malloc(0)`](./7.5-malloc.md)

---

## 本节讲什么

void *calloc(size_t numitems, size_t size);  /* 置零 */ void *realloc(void *ptr, size_t newsize); /* ❌ ptr = realloc(ptr, n); 失败时原指针丢失 → 泄漏 */

---

## 要点

```c
void *calloc(size_t numitems, size_t size);  /* 置零 */
void *realloc(void *ptr, size_t newsize);
```

| | |
|--|--|
| `calloc(n,s)` | ≈ `malloc(n*s)` + 清零；适合数组 |
| `realloc` 扩容 | 后方够 → 原地；不够 → 新块 + 拷贝 + 释放旧块（指针可能变） |
| `realloc` 缩容 | 截断尾部；起始指针通常不变 |

```c
/* ❌ ptr = realloc(ptr, n); 失败时原指针丢失 → 泄漏 */
void *np = realloc(ptr, newsize);
if (np == NULL) { /* ptr 仍有效 */ }
else { ptr = np; }
```

另：`realloc(NULL, n)` ≈ `malloc(n)`；`realloc(ptr, 0)` 平台不统一，**尽量避免**。

## 代码示例

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/* calloc: 分配并清零; realloc: 调整大小 */
int main(void) {
    /* calloc: 分配 10 个 int, 自动清零 */
    int *arr = calloc(10, sizeof(int));
    printf("calloc[0]=%d (zeroed)\n", arr[0]);

    /* realloc: 扩大到 20 个 int */
    arr = realloc(arr, 20 * sizeof(int));
    /* realloc 不保证新增部分清零 */
    printf("realloc[15]=%d (may be garbage)\n", arr[15]);

    /* realloc(NULL, size) 等价于 malloc */
    int *p = realloc(NULL, 100);
    printf("realloc(NULL,100) = %p\n", (void*)p);

    /* realloc(ptr, 0) 等价于 free */
    realloc(p, 0);

    free(arr);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[7.3 `malloc` / `free`](./7.3-malloc-free.md)
- 下一节：[7.5 `malloc(0)`](./7.5-malloc.md)
- 前置：**前置**：[Ch6 Processes](../chapter-06-processes/notes.md)（堆 / BSS / 地址空间）  
- 后置：**后置**：[Ch8 用户与组](../chapter-08-users-and-groups/notes.md) · [Ch49 Memory Mappings](../chapter-49-memory-mappings/notes.md) · [Ch24 fork / COW](../chapter-24-process-creation/notes.md)


---

# 7.5 `malloc(0)`

> 本章：[TLPI 第 07 章 — Memory Allocation](./README.md)
> - 上一节：[7.4 `calloc` / `realloc`](./7.4-calloc-realloc.md)
> - 下一节：[7.6 glibc 双路径（拓展重点）](./7.6-glibc.md)

---

## 本节讲什么

POSIX 允许返回 `NULL` **或** 可 `free` 的非空指针。 **glibc：常返回有效指针，必须 `free`**，不能当「失败」处理。 ---

---

## 要点

POSIX 允许返回 `NULL` **或** 可 `free` 的非空指针。  
**glibc：常返回有效指针，必须 `free`**，不能当「失败」处理。

## 代码示例

```c
#include <stdlib.h>
#include <stdio.h>

int main(void) {
    /* malloc(0) 的行为: POSIX 允许返回 NULL 或可 free 的非空指针 */
    void *p = malloc(0);
    printf("malloc(0) = %p\n", p);

    if (p != NULL) {
        printf("glibc 返回非空指针 → 必须 free!\n");
        /* 不能解引用！大小为 0，访问是 UB */
        free(p);
        printf("已 free\n");
    } else {
        printf("返回 NULL → 不需要 free\n");
    }

    /* 实际场景: size 变量为 0 时 */
    size_t n = 0;  /* 可能来自计算结果 */
    char *buf = malloc(n);
    if (buf == NULL && n > 0) {
        perror("malloc");
        return 1;
    }
    /* 无论 NULL 还是非空，free 都安全 */
    free(buf);

    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[7.4 `calloc` / `realloc`](./7.4-calloc-realloc.md)
- 下一节：[7.6 glibc 双路径（拓展重点）](./7.6-glibc.md)
- 前置：**前置**：[Ch6 Processes](../chapter-06-processes/notes.md)（堆 / BSS / 地址空间）  
- 后置：**后置**：[Ch8 用户与组](../chapter-08-users-and-groups/notes.md) · [Ch49 Memory Mappings](../chapter-49-memory-mappings/notes.md) · [Ch24 fork / COW](../chapter-24-process-creation/notes.md)


---

# 7.6 glibc 双路径（拓展重点）

> 本章：[TLPI 第 07 章 — Memory Allocation](./README.md)
> - 上一节：[7.5 `malloc(0)`](./7.5-malloc.md)
> - 下一节：[7.7 典型内存错误](./7.7-memory.md)

---

## 本节讲什么

→ 另一种拿虚拟内存的主路径：书内 [Ch49 Memory Mappings](../chapter-49-memory-mappings/notes.md)（勿与 SysV SHM Ch48 混淆）。 ---

---

## 要点

| 路径 | 方式 | 特点 |
|------|------|------|
| 小块 | `sbrk` 扩主堆 | 释放后常留在空闲链表 |
| 大块（默认约 >128KB，可配） | `mmap(MAP_ANONYMOUS)` | `free` 时常**立刻还内核**；不绑在 program break 上 |

→ 另一种拿虚拟内存的主路径：书内 [Ch49 Memory Mappings](../chapter-49-memory-mappings/notes.md)（勿与 SysV SHM Ch48 混淆）。

## 代码示例

```c
#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>

int main(void) {
    void *brk_before = sbrk(0);

    /* 小块分配: 走 sbrk 扩主堆 */
    void *small = malloc(64);
    printf("小块(64B): %p, sbrk 变化: %s\n",
           small,
           sbrk(0) != brk_before ? "是(主堆扩展)" : "否");

    /* 大块分配: 走 mmap (默认 >128KB) */
    void *large = malloc(256 * 1024);  /* 256KB */
    printf("大块(256KB): %p\n", large);

    /* 检查大块是否在 mmap 区域（远离堆） */
    void *brk_after = sbrk(0);
    long heap_grew = (char *)brk_after - (char *)brk_before;
    printf("堆增长: %ld 字节 (大块不走 sbrk, 堆几乎没变)\n", heap_grew);

    /* free 大块时常立刻还内核 (mmap 的区域 munmap) */
    free(large);
    printf("free 大块后: 堆 break = %p (不变, mmap 区域已归还)\n", sbrk(0));

    /* free 小块后: 堆 break 通常不变 (留在空闲链表) */
    free(small);
    printf("free 小块后: 堆 break = %p (不变, 留在空闲链表)\n", sbrk(0));

    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[7.5 `malloc(0)`](./7.5-malloc.md)
- 下一节：[7.7 典型内存错误](./7.7-memory.md)
- 前置：**前置**：[Ch6 Processes](../chapter-06-processes/notes.md)（堆 / BSS / 地址空间）  
- 后置：**后置**：[Ch8 用户与组](../chapter-08-users-and-groups/notes.md) · [Ch49 Memory Mappings](../chapter-49-memory-mappings/notes.md) · [Ch24 fork / COW](../chapter-24-process-creation/notes.md)


---

# 7.7 典型内存错误

> 本章：[TLPI 第 07 章 — Memory Allocation](./README.md)
> - 上一节：[7.6 glibc 双路径（拓展重点）](./7.6-glibc.md)
> - 下一节：[下一章](../chapter-{int(ch_num)+1:02d}-*/notes.md)

---

## 本节讲什么

1. **泄漏**：丢指针未 `free` 2. **双重 free**：破坏空闲链表 3. **野指针 free**：栈/全局/非堆指针

---

## 要点

1. **泄漏**：丢指针未 `free`  
2. **双重 free**：破坏空闲链表  
3. **野指针 free**：栈/全局/非堆指针  
4. **越界**：覆盖块头  
5. **悬挂指针**：`free` 后继续用  

## 代码示例

```c
#include <stdlib.h>
#include <stdio.h>

int main(void) {
    /* 1. 内存泄漏: 丢指针未 free */
    {
        int *p = malloc(sizeof(int));
        *p = 42;
        /* 作用域结束, p 消失, 但堆内存没 free → 泄漏 */
    }
    printf("1. 泄漏: 块无法回收\n");

    /* 2. 正确做法: 用完就 free */
    {
        int *p = malloc(sizeof(int));
        *p = 42;
        free(p);       /* 释放内存 */
        p = NULL;      /* 置空防悬挂 */
    }
    printf("2. 正确: free + 置 NULL\n");

    /* 3. free(NULL) 安全 */
    int *p = NULL;
    free(p);
    printf("3. free(NULL) 安全\n");

    /* 4. calloc 自动清零 (对比 malloc 不清零) */
    int *arr = calloc(5, sizeof(int));
    printf("4. calloc 清零: arr[0]=%d, arr[4]=%d\n", arr[0], arr[4]);
    free(arr);

    /* 5. 越界写破坏块头 (演示概念, 不要这样做!) */
    int *bad = malloc(4);
    /* bad[10] = 0xDEAD;  // 越界! 破坏分配器元数据, free 时崩溃 */
    free(bad);
    printf("5. 越界写 → free 时可能崩溃 (未演示)\n");

    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[7.6 glibc 双路径（拓展重点）](./7.6-glibc.md)
- 前置：**前置**：[Ch6 Processes](../chapter-06-processes/notes.md)（堆 / BSS / 地址空间）  
- 后置：**后置**：[Ch8 用户与组](../chapter-08-users-and-groups/notes.md) · [Ch49 Memory Mappings](../chapter-49-memory-mappings/notes.md) · [Ch24 fork / COW](../chapter-24-process-creation/notes.md)
