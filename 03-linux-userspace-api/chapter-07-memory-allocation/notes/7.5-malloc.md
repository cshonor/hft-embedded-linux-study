# 7.5 `malloc(0)`

> 本章：[TLPI 第 07 章 — Memory Allocation](./README.md)
> - 上一节：[7.4 `calloc` / `realloc`](./7.4-calloc-realloc.md)
> - 下一节：[7.6 glibc 双路径（拓展重点）](./7.6-glibc.md)

---

## 本节讲什么

POSIX 允许返回 `NULL` **或** 可 `free` 的非空指针。 **glibc：常返回有效指针，必须 `free`**，不能当「失败」处理。 ---

---

## 要点

POSIX 允许返回 `NULL` **或** 可 `free` 的非空指针。  
**glibc：常返回有效指针，必须 `free`**，不能当「失败」处理。

---

---

## 与 C 语言学习的关系

`malloc`/`free`/`mmap` 是 C 语言动态内存管理的核心。K&R 第 6 章和第 8.7 节有 `malloc` 的实现。理解 `malloc` 返回 `void*` 需要 C 语言类型系统的知识。`mmap` 是 Linux 高级内存操作，在 HFT 中用于零拷贝和共享内存。注意 `mmap` 失败返回 `MAP_FAILED`（不是 `NULL`）。
---

## HFT / 嵌入式关联

| 路线 | |
|------|--|
| 嵌入式 | 控泄漏；大块/常驻慎用；理解为何 RSS ≠ 所有 `malloc` 之和 |
| HFT | 热路径少 `malloc`；预分配 / 池；大块与 `mmap`/大页衔接 Ch49–50 |

---

## 常见陷阱

1. **返回值未检查**

```c
int *p = malloc(sizeof(int) * 1000);
p[0] = 42;  /* malloc 失败 → NULL → 段错误 */
```

malloc 失败返回 NULL。不检查直接用会导致段错误。Linux 上可能 OOM killer 先杀进程。

2. **use-after-free**

```c
free(p);
p[0] = 42;  /* UB! */
```

free 后访问已释放内存是未定义行为。free 后应立即置 NULL：`free(p); p = NULL;`。

3. **realloc 指针失效**

```c
int *p = malloc(100);
int *q = realloc(p, 200);
/* p 可能已失效，不能再用 */
```

realloc 可能返回新地址并释放旧地址。realloc 后不能再用旧指针。应 `p = realloc(p, size);`。

---

## 自测题

**题目 1（选择）：** `malloc(0)` 的返回值是？
A. NULL
B. 非 NULL 或 NULL（实现定义）
C. 一定非 NULL
D. 未定义行为

<details>
<summary>参考答案</summary>

B。malloc(0) 的行为是实现定义的：可能返回 NULL，也可能返回一个非 NULL 但不可解引用的指针。

</details>

**题目 2（判断）：** `free(NULL)` 是未定义行为。

<details>
<summary>参考答案</summary>

错误。free(NULL) 是安全的，什么都不做。C 标准明确允许。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
char *p = malloc(10);
strcpy(p, "Hello, World!");
```

<details>
<summary>参考答案</summary>

缓冲区溢出。"Hello, World!" 是 13 字节（含 \0），但只分配了 10 字节。应该 malloc(strlen(s)+1)。

</details>

**题目 4（应用）：** 写一个安全的字符串复制函数。

<details>
<summary>参考答案</summary>

```c
char *safe_strdup(const char *s) {
    char *p = malloc(strlen(s) + 1);
    if (p) strcpy(p, s);
    return p;
}
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[7.4 `calloc` / `realloc`](./7.4-calloc-realloc.md)
- 下一节：[7.6 glibc 双路径（拓展重点）](./7.6-glibc.md)
- 前置：**前置**：[Ch6 Processes](../chapter-06-processes/notes.md)（堆 / BSS / 地址空间）  
- 后置：**后置**：[Ch8 用户与组](../chapter-08-users-and-groups/notes.md) · [Ch49 Memory Mappings](../chapter-49-memory-mappings/notes.md) · [Ch24 fork / COW](../chapter-24-process-creation/notes.md)  

---

## man 手册参考

- `man 3 malloc`
- `man 3 free`
