# 7.3 Summary

> 本章：[TLPI 第 07 章 — Memory Allocation](./README.md)

## 本章回顾

### 内存分配方式对比

| 分配方式 | API | 位置 | 释放 | 特点 |
|---------|-----|------|------|------|
| 堆分配 | `malloc`/`calloc`/`realloc` | 堆 | `free` | 最常用，需手动管理 |
| 栈分配 | `alloca()` | 栈 | 自动 | 函数返回自动释放，不可跨函数 |
| 系统调用 | `brk`/`sbrk` | 堆顶 | `sbrk(-n)` | malloc 底层，通常不直接用 |
| 内存映射 | `mmap`/`munmap` | 任意虚拟地址 | `munmap` | 大块分配，可共享 |
| 页对齐 | `posix_memalign`/`aligned_alloc` | 堆 | `free` | SIMD/DMA 需求 |

### malloc 家族

| 函数 | 原型 | 说明 |
|------|------|------|
| `malloc(size)` | `void *malloc(size_t size)` | 分配 size 字节，内容未初始化 |
| `calloc(nmemb, size)` | `void *calloc(size_t n, size_t sz)` | 分配 n×sz 字节，清零 |
| `realloc(ptr, size)` | `void *realloc(void *ptr, size_t sz)` | 调整大小，可能移动内存 |
| `free(ptr)` | `void free(void *ptr)` | 释放，ptr 可为 NULL |
| `aligned_alloc(align, size)` | C11 | 页/SIMD 对齐分配 |
| `strdup(s)` | POSIX | malloc + strcpy |

### malloc 调整规则

| 场景 | 行为 |
|------|------|
| `realloc(NULL, n)` | 等价 `malloc(n)` |
| `realloc(p, 0)` | 实现定义（C11 起未定义，glibc 行为：释放并返回 NULL） |
| `realloc(p, smaller)` | 可能原地缩减，返回 p |
| `realloc(p, bigger)` | 可能移动，原内存自动释放 |

### 分配器实现

| 分配器 | 特点 | 适用场景 |
|--------|------|---------|
| ptmalloc (glibc) | 分区管理，fastbin/smallbin/largebin | 通用 |
| tcmalloc (Google) | 线程缓存，减少锁竞争 | 多线程 |
| jemalloc (Facebook) | 分区按 size class，减少碎片 | 多线程/大内存 |
| mimalloc (Microsoft) | 分段管理，碎片少 | 通用高性能 |

### 错误与陷阱

| 问题 | 说明 | 预防 |
|------|------|------|
| 内存泄漏 | `malloc` 不 `free` | valgrind/asan |
| 重复释放 | `free` 同一指针两次 | `p = NULL` 释放后置空 |
| 越界写 | 写超出分配边界 | asan/fortify |
| use-after-free | 释放后仍访问 | 悬空指针置 NULL |
| 未检查返回值 | `malloc` 返回 NULL 被解引用 | 检查 `if (p == NULL)` |
| `realloc` 后旧指针 | 移动后访问旧地址 | 只用返回的新指针 |

## HFT / 嵌入式关联

- **低延迟分配**：HFT 热路径避免 `malloc`（不可预测延迟），预分配池/arena
- **内存池**：固定大小块分配器（`obstack`/自实现），O(1) 分配释放
- **Huge Pages**：`mmap(MAP_HUGETLB)` 减少 TLB miss，HFT 关键数据结构
- **对齐**：`posix_memalign` 用于 SIMD（AVX-512 需 64 字节对齐）
- **嵌入式**：无 MMU 系统（uClinux）不能用 `brk`，只能 `mmap`
- **mmap 替代**：大块内存用 `mmap` 更高效，可 `MAP_LOCKED` 防止 swap

## 代码示例

```c
/* 安全 realloc 模式 */
void *safe_realloc(void **ptr, size_t new_size) {
    void *new_ptr = realloc(*ptr, new_size);
    if (new_ptr == NULL) {
        free(*ptr);   /* 原内存仍需释放 */
        *ptr = NULL;
        return NULL;
    }
    *ptr = new_ptr;
    return new_ptr;
}

/* 内存池简单实现 */
#define POOL_SIZE 4096
static char pool[POOL_SIZE];
static size_t pool_off = 0;

void *pool_alloc(size_t size) {
    size = (size + 15) & ~15;  /* 16 字节对齐 */
    if (pool_off + size > POOL_SIZE) return NULL;
    void *p = pool + pool_off;
    pool_off += size;
    return p;  /* 不需要 free，整池重置 */
}
```

## 衔接

- **Ch50** 虚拟内存 — `mmap`/`mlock`/`mprotect`
- **Ch49** 内存映射 I/O — `mmap` 文件映射
- **Ch63** 替代 I/O — 零拷贝

## 代码自测

<details>
<summary>1. calloc 和 malloc + memset 的区别？</summary>

- `calloc(n, size)` 内部检测 `n × size` 整数溢出，更安全
- `calloc` 可能利用内核的页清零（`mmap` 返回的匿名页已被清零），避免实际 memset
- `malloc + memset` 需要两次操作，且不检查溢出
- 在 glibc 中，大块 calloc 使用 mmap，页可能已经被清零
</details>

<details>
<summary>2. realloc 移动内存后，原指针的状态？</summary>

`realloc` 成功且返回新地址时，原内存被自动释放。旧指针变为悬空指针，不可再使用。必须只使用返回值。如果 realloc 失败（返回 NULL），原内存仍有效，需要手动 free。
</details>

<details>
<summary>3. alloca 的优势和使用限制？</summary>

**优势**：栈上分配，函数返回自动释放，无需 free，不会内存泄漏。
**限制**：不能跨函数返回值传递；栈大小有限（默认 8MB），大块分配导致栈溢出；不可移植（非标准 C）。
</details>
