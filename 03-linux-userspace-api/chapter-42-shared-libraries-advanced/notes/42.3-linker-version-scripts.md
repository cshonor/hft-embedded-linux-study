# 42.3 Linker Version Scripts

> 本章：[TLPI 第 42 章 — Shared Libraries: Advanced Topics](../README.md)

## 本节讲什么

介绍 GNU ld 版本脚本（version script，`.map` 文件）：声明式控制符号导出和符号版本管理。

## 要点

### 版本脚本语法

```
/* libfoo.map */
LIBFOO_1.0 {
    global:
        foo_init;
        foo_destroy;
        foo_process;
    local:
        *;       /* 其他所有符号隐藏 */
};

LIBFOO_1.1 {
    global:
        foo_batch;     /* 1.1 新增 */
} LIBFOO_1.0;          /* 依赖 1.0 */
```

### 编译使用

```bash
gcc -shared -fPIC -Wl,--version-script,libfoo.map -o libfoo.so foo.o
```

### 版本脚本能力

| 能力 | 说明 |
|------|------|
| 控制符号导出 | `global:` 列出导出符号 |
| 隐藏内部符号 | `local: *;` 隐藏其他所有 |
| 符号版本管理 | 允许多版本同名符号共存 |
| ABI 兼容 | 新版本可依赖旧版本 |

### 符号版本

```c
/* foo.c */
__asm__(".symver foo_init_v1,foo_init@LIBFOO_1.0");
int foo_init_v1(void) { return 1; }

__asm__(".symver foo_init_v2,foo_init@@LIBFOO_1.1");  /* @@ 表示默认版本 */
int foo_init_v2(void) { return 2; }
```

```
/* libfoo.map */
LIBFOO_1.0 {
    global: foo_init;
    local: *;
};
LIBFOO_1.1 {
    global: foo_init;
} LIBFOO_1.0;
```

- `@@` 表示默认版本（链接器选择）
- `@` 表示非默认版本（特定版本请求时选择）
- 运行时 `dlsym` 默认返回 `@@` 版本

### 版本脚本 vs visibility 属性

| 特性 | 版本脚本 | visibility 属性 |
|------|---------|----------------|
| 位置 | 外部 .map 文件 | 源码内 |
| 粒度 | 按通配符批量 | 每符号 |
| 版本管理 | 支持 | 不支持 |
| 维护 | 集中管理 | 分散在源码 |
| 可读性 | 声明式 | 代码侵入 |

### 实际应用：glibc 符号版本

```bash
# 查看 glibc 的符号版本
readelf -s /lib/x86_64-linux-gnu/libc.so.6 | grep printf
# printf@GLIBC_2.2.5
# __printf_chk@GLIBC_2.7
```

glibc 用版本脚本保持 ABI 兼容：旧程序链接 `printf@GLIBC_2.2.5`，新程序链接 `@@GLIBC_2.2.5`（默认版本）。

## HFT / 嵌入式关联

- **ABI 管理**：HFT 策略库用版本脚本管理接口版本，支持热更新
- **符号隐藏**：`local: *;` 等价于 `-fvisibility=hidden`，集中管理
- **嵌入式**：版本脚本减少导出符号，节省空间
- **glibc 兼容**：链接特定 glibc 版本避免运行时版本不匹配

## 代码示例

```bash
# 完整示例

# libfoo.map
LIBFOO_1.0 {
    global:
        foo_init;
        foo_process;
    local:
        *;
};

LIBFOO_1.1 {
    global:
        foo_batch;
} LIBFOO_1.0;

# 编译
gcc -c -fPIC foo.c -o foo.o
gcc -shared -Wl,--version-script,libfoo.map -o libfoo.so foo.o

# 验证
nm -D libfoo.so
# 应该只有 foo_init, foo_process, foo_batch
```

## 衔接

- [42.2 Controlling Symbol Visibility](./42.2-controlling-symbol-visibility.md) — visibility 属性
- **Ch41** Shared Libraries — SONAME 和版本命名
- **Ch41.8** ABI 兼容性

## 代码自测

<details>
<summary>1. local: *; 的作用是什么？</summary>

`local: *;` 是通配符，将所有未在 `global:` 中列出的符号标记为 local（隐藏）。这是一种"白名单"模式——默认隐藏所有符号，只导出明确列出的。等价于 `-fvisibility=hidden` + 逐个标记导出，但集中在外部脚本文件中管理。
</details>

<details>
<summary>2. @@ 和 @ 的区别？</summary>

`@@` 标记默认版本：链接器在未指定版本时自动选择该版本。一个符号只能有一个 `@@` 版本。`@` 标记非默认版本：只有显式请求该版本的程序才会链接到它。运行时 `dlsym` 返回 `@@` 版本的符号。
</details>
