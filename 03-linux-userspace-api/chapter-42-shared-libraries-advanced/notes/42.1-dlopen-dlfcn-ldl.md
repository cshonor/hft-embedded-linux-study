# 42.1 –42.4 dlopen 家族（`<dlfcn.h>`，`-ldl`）

> 本章：[TLPI 第 42 章 — Advanced Features of Shared Libraries](./README.md)
> - 上一节：本章首页
> - 下一节：[42.5 dladdr](./42.5-dladdr.md)

---

## 本节讲什么

void *dlopen(const char *filename, int flags); void *dlsym(void *handle, const char *symbol); int   dlclose(void *handle);

---

## 要点

```c
void *dlopen(const char *filename, int flags);
void *dlsym(void *handle, const char *symbol);
int   dlclose(void *handle);
char *dlerror(void);   /* 读一次即清零 */
```

### Flags

| 组 | 标志 | 含义 |
|----|------|------|
| 绑定 | `RTLD_LAZY` | 首次调用再解析（默认） |
| | `RTLD_NOW` | 打开时解析完；缺符号立即失败 |
| 作用域 | `RTLD_LOCAL` | 符号不进全局空间（默认） |
| | `RTLD_GLOBAL` | 后续库可解析本库符号 |
| 附加 | `RTLD_NODELETE` | `dlclose` 后仍不卸载映射 |

```c
dlopen("./plugin.so", RTLD_NOW | RTLD_LOCAL);
```

插件实践：`RTLD_NOW | RTLD_LOCAL` + 通过 `dlsym` 取**一个** `plugin_api` 结构体指针，避免库间互相 `dlsym` 依赖。

### dlsym

返回 `void*`；转函数指针宜用 typedef + 中转赋值。C++ 需 `extern "C"` 防 name mangling。

### dlerror

每次调用会清除状态。模板：先 `dlerror()` 清空 → `dlsym` → 再 `dlerror()` 判错（区分「符号不存在」与「合法 NULL」）。

### dlclose

引用计数到 0 → 跑 destructor → 默认卸载。  
陷阱：插件线程/回调仍存活 → SIGSEGV。先注销回调/停线程，或 `RTLD_NODELETE`。

Demo：[`code/`](./code/)

---

---

## 与 C 语言学习的关系

系统限制和信息 API 涉及 C 语言的可移植性编程。`sysconf`/`pathconf` 返回 `long`，需要检查返回值和 errno。这部分内容训练 C 程序员不硬编码系统参数（如 `OPEN_MAX`、`PAGE_SIZE`），而是运行时查询——这是可移植 C 编程的基本素养。
---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | –42.4 dlopen 家族（`<dlfcn.h>`，`-ldl`） 在嵌入式系统的应用场景 |
| HFT | –42.4 dlopen 家族（`<dlfcn.h>`，`-ldl`） 对低延迟和性能的影响 |

---

## 常见陷阱

1. **硬编码系统限制**

```c
#define MAX_FD 256  /* 错误假设 */
long max_fd = sysconf(_SC_OPEN_MAX);  /* 运行时查询 */
```

系统限制值因平台和配置而异。硬编码会导致程序在新环境上失败。应用 sysconf/pathconf 运行时查询。

2. **sysconf 返回值未检查**

```c
long val = sysconf(_SC_OPEN_MAX);
/* val 可能是 -1（无限制或出错） */
```

sysconf 返回 -1 可能表示"无限制"或"出错"。需要检查 errno 来区分：先清除 errno，调用后检查。

3. **uname 结构体字段假设**

```c
struct utsname buf;
uname(&buf);
printf("%s", buf.release);  /* 字段长度有限! */
```

utsname 各字段长度有限（通常 65 字节）。过长的字段会被截断。不要假设完整信息。

---

## 自测题

**题目 1（选择）：** 获取系统最大打开文件数，应该用？
A. 宏 OPEN_MAX
B. sysconf(_SC_OPEN_MAX)
C. getrlimit(RLIMIT_NOFILE)
D. B 或 C

<details>
<summary>参考答案</summary>

D。sysconf 返回系统默认值，getrlimit 返回当前进程的限制。两者都可，但 getrlimit 更精确。硬编码 OPEN_MAX 宏不可移植。

</details>

**题目 2（判断）：** `sysconf(_SC_OPEN_MAX)` 返回 -1 一定表示出错。

<details>
<summary>参考答案</summary>

错误。返回 -1 可能表示"无限制"或"出错"。需要先清除 errno，调用后检查 errno 是否被设置来区分。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
long page_size = 4096;  /* 硬编码 */
void *p = mmap(NULL, page_size, ...);
```

<details>
<summary>参考答案</summary>

页面大小因平台而异（x86=4KB, ARM64 可能=16KB/64KB）。应运行时查询：`long page_size = sysconf(_SC_PAGESIZE);`

</details>

**题目 4（应用）：** 写代码获取当前进程的文件描述符限制并打印。

<details>
<summary>参考答案</summary>

```c
struct rlimit rl;
if (getrlimit(RLIMIT_NOFILE, &rl) == 0) {
    printf("soft: %lu, hard: %lu
",
           (unsigned long)rl.rlim_cur,
           (unsigned long)rl.rlim_max);
}
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 下一节：[42.5 dladdr](./42.5-dladdr.md)
- 前置：**前置**：[Ch41 共享库基础](../chapter-41-shared-libraries/notes.md)  
- 后置：**后置**：[Ch43 IPC 综述](../chapter-43-ipc-overview/notes.md)  

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
