# 5.7 非阻塞 `O_NONBLOCK`

> 本章：[TLPI 第 05 章 — File I/O: Further Details](./README.md)
> - 上一节：[5.6 打开标志补充（扩展 Ch4）](./5.6-ch4.md)
> - 下一节：[5.8 大文件 LFS](./5.8-lfs.md)

---

## 本节讲什么

---

## 要点

| 对象 | 行为 |
|------|------|
| **普通磁盘文件** | 通常 **无效果**（读盘一般不靠这个变「非阻塞」） |
| 管道 / FIFO / socket / 终端 | 无数据时 `read` 立即 `-1`，`errno=EAGAIN`/`EWOULDBLOCK` |

> `O_NONBLOCK` ≠ 异步 AIO；只是「不睡、立刻返回」的轮询式语义。

## 代码示例

```c
#include <fcntl.h>
#include <unistd.h>
#include <errno.h>
#include <stdio.h>
#include <string.h>

int main(void) {
    int pipefd[2];
    if (pipe(pipefd) < 0) { perror("pipe"); return 1; }

    /* 把读端设为非阻塞 */
    int flags = fcntl(pipefd[0], F_GETFL);
    fcntl(pipefd[0], F_SETFL, flags | O_NONBLOCK);

    /* 管道里没数据 → read 立即返回 -1, errno=EAGAIN */
    char buf[64];
    ssize_t n = read(pipefd[0], buf, sizeof(buf));
    if (n < 0) {
        if (errno == EAGAIN || errno == EWOULDBLOCK) {
            printf("read 返回 -1, errno=EAGAIN (无数据可读)\n");
        } else {
            perror("read");
        }
    } else if (n == 0) {
        printf("EOF\n");
    } else {
        printf("读到 %zd 字节\n", n);
    }

    /* 写入数据后非阻塞读就能拿到 */
    write(pipefd[1], "hi", 2);
    n = read(pipefd[0], buf, sizeof(buf));
    printf("第二次 read: %zd 字节: %.*s\n", n, (int)n, buf);

    close(pipefd[0]);
    close(pipefd[1]);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[5.6 打开标志补充（扩展 Ch4）](./5.6-ch4.md)
- 下一节：[5.8 大文件 LFS](./5.8-lfs.md)
- 前置：**前置**：[Ch4 Universal I/O](../chapter-04-file-io-universal/notes.md)  
- 后置：**后置**：[Ch6 Processes](../chapter-06-processes/notes.md) · [Ch13 File I/O Buffering](../chapter-13-file-io-buffering/notes.md)
