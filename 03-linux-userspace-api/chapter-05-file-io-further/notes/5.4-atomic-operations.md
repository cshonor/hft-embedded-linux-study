# 5.4 原子操作

> 本章：[TLPI 第 05 章 — File I/O: Further Details](./README.md)
> - 上一节：[5.3 `pread` / `pwrite`](./5.3-pread-pwrite.md)
> - 下一节：[5.5 `fcntl` — 文件控制](./5.5-fcntl.md)

---

## 本节讲什么

/* ❌ 非原子：两进程可同时「不存在」再各自 create */ if (open("a.txt", O_RDONLY) == -1) open("a.txt", O_WRONLY | O_CREAT, mode);

---

## 要点

### 反例：检查再创建（竞态）

```c
/* ❌ 非原子：两进程可同时「不存在」再各自 create */
if (open("a.txt", O_RDONLY) == -1)
    open("a.txt", O_WRONLY | O_CREAT, mode);
```

```c
/* ✅ 检查+创建一步完成 */
fd = open(path, O_WRONLY | O_CREAT | O_EXCL, mode);
```

`O_EXCL` + `O_CREAT`：已存在则失败。  
Demo：[`code/o_excl_create.c`](./code/o_excl_create.c)

### `O_APPEND` 原子追加

多进程同文件：带 `O_APPEND` 时每次 write 内核先到 EOF 再写。  
无则 `lseek(SEEK_END)+write` 可互相覆盖。

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[5.3 `pread` / `pwrite`](./5.3-pread-pwrite.md)
- 下一节：[5.5 `fcntl` — 文件控制](./5.5-fcntl.md)
- 前置：**前置**：[Ch4 Universal I/O](../chapter-04-file-io-universal/notes.md)  
- 后置：**后置**：[Ch6 Processes](../chapter-06-processes/notes.md) · [Ch13 File I/O Buffering](../chapter-13-file-io-buffering/notes.md)
