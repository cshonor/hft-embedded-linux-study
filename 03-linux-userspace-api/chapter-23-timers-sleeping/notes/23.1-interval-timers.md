# 23.1 休眠接口

> 本章：[TLPI 第 23 章 — Timers and Sleeping](../README.md)
> - 上一节：本章首页
> - 下一节：[23.2 `setitimer` / `alarm`](23.2-scheduling-and-accuracy-of-timers.md)

---

## 本节讲什么

int nanosleep(const struct timespec *req, struct timespec *rem); - **不占 `SIGALRM`** - 中断：`-1`/`EINTR`，`rem` 剩余时间可重试

---

## 要点

### `sleep` / `usleep`（少用）

| API | 问题 |
|-----|------|
| `sleep` | 内部常走 `alarm`+`SIGALRM`，与其它 `alarm` 冲突；打断返回剩余秒数 |
| `usleep` | 已废弃；新代码用 `nanosleep` |

### `nanosleep`（推荐短休眠）

```c
int nanosleep(const struct timespec *req, struct timespec *rem);
```

- **不占 `SIGALRM`**  
- 中断：`-1`/`EINTR`，`rem` 剩余时间可重试  
- 线程友好  

Demo：[`code/nanosleep_retry.c`](../code/nanosleep_retry.c)

### `clock_nanosleep`

```c
int clock_nanosleep(clockid_t clock_id, int flags,
                    const struct timespec *req, struct timespec *rem);
```

| flags | 含义 |
|-------|------|
| `0` | 相对休眠（≈ `nanosleep`，常等价 `CLOCK_REALTIME` 相对） |
| `TIMER_ABSTIME` | 等到绝对时刻 |

定时/超时逻辑优先 **`CLOCK_MONOTONIC`**（不受墙钟跳变影响）。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 休眠接口 在嵌入式系统的应用场景 |
| HFT | 休眠接口 对低延迟和性能的影响 |

## 代码示例

```c
#include <time.h>
#include <stdio.h>
int main(void) {
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    printf("CLOCK_REALTIME: %ld.%09ld\n", ts.tv_sec, ts.tv_nsec);

    clock_gettime(CLOCK_MONOTONIC, &ts);
    printf("CLOCK_MONOTONIC: %ld.%09ld\n", ts.tv_sec, ts.tv_nsec);

    /* nanosleep: 高精度睡眠 */
    struct timespec req = {.tv_sec = 0, .tv_nsec = 500 * 1000 * 1000};
    printf("nanosleep 500ms...\n");
    nanosleep(&req, NULL);
    printf("done\n");
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 下一节：[23.2 `setitimer` / `alarm`](23.2-scheduling-and-accuracy-of-timers.md)
- 前置：**前置**：[Ch22 信号高级](../../chapter-22-signals-advanced/notes)  
- 后置：**后置**：[Ch24 Process Creation](../../chapter-24-process-creation/notes) · Ch63 多路 I/O
