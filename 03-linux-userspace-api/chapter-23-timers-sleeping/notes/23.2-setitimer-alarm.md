# 23.2 `setitimer` / `alarm`

> 本章：[TLPI 第 23 章 — Timers and Sleeping](./README.md)
> - 上一节：[23.1 休眠接口](./23.1-section-23-1.md)
> - 下一节：[23.3 POSIX 定时器 `timer_create`](./23.3-timercreate.md)

---

## 本节讲什么

int setitimer(int which, const struct itimerval *new_val, struct itimerval *old_val); unsigned int alarm(unsigned int seconds);  /* 简化版 ITIMER_REAL */

---

## 要点

```c
int setitimer(int which, const struct itimerval *new_val,
              struct itimerval *old_val);
unsigned int alarm(unsigned int seconds);  /* 简化版 ITIMER_REAL */
```

| which | 计时 | 信号 |
|-------|------|------|
| `ITIMER_REAL` | 墙钟 | `SIGALRM` |
| `ITIMER_VIRTUAL` | 用户态 CPU | `SIGVTALRM` |
| `ITIMER_PROF` | 用户+内核 CPU | `SIGPROF` |

`it_value` 启动/停止；`it_interval` 为 0 则单次。  
每类 **which 全局一个**；依赖信号、标准信号不排队 → 新代码优先 POSIX 定时器。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `setitimer` / `alarm` 在嵌入式系统的应用场景 |
| HFT | `setitimer` / `alarm` 对低延迟和性能的影响 |

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[23.1 休眠接口](./23.1-section-23-1.md)
- 下一节：[23.3 POSIX 定时器 `timer_create`](./23.3-timercreate.md)
- 前置：**前置**：[Ch22 信号高级](../chapter-22-signals-advanced/notes.md)  
- 后置：**后置**：[Ch24 Process Creation](../chapter-24-process-creation/notes.md) · [Ch63 多路 I/O](../chapter-63-alternative-io/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
