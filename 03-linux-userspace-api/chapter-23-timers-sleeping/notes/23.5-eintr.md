# 23.5 休眠与 `EINTR`

> 本章：[TLPI 第 23 章 — Timers and Sleeping](./README.md)
> - 上一节：[23.4 时钟类型](./23.4-clock-types.md)
> - 下一节：[下一章](../chapter-{int(ch_num)+1:02d}-*/notes.md)

---

## 本节讲什么

相对休眠被信号打断 → `EINTR`；`nanosleep` 用 `rem` 重试： while (nanosleep(&req, &req) == -1 && errno == EINTR) ;

---

## 要点

相对休眠被信号打断 → `EINTR`；`nanosleep` 用 `rem` 重试：

```c
while (nanosleep(&req, &req) == -1 && errno == EINTR)
    ;
```

`TIMER_ABSTIME`：打断后通常**重算绝对时间**，不是简单用 rem。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 休眠与 `EINTR` 在嵌入式系统的应用场景 |
| HFT | 休眠与 `EINTR` 对低延迟和性能的影响 |

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[23.4 时钟类型](./23.4-clock-types.md)
- 前置：**前置**：[Ch22 信号高级](../chapter-22-signals-advanced/notes.md)  
- 后置：**后置**：[Ch24 Process Creation](../chapter-24-process-creation/notes.md) · [Ch63 多路 I/O](../chapter-63-alternative-io/notes.md)

---

## man 手册参考

- `man 2 nanosleep`
