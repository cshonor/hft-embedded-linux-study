# 23.5 休眠与 `EINTR`

> 本章：[TLPI 第 23 章 — Timers and Sleeping](../README.md)
> - 上一节：[23.4 时钟类型](23.4-suspending-execution-for-a-fixed-interva.md)
> - 下一节：下一章+1:02d}-*/notes.md)

---

## 本节讲什么

相对休眠被信号打断 → `EINTR`；`nanosleep` 用 `rem` 重试： while (nanosleep(&req, &req) == -1 && errno == EINTR) ;

---

## 要点

相对休眠被信号打断 → `EINTR`；`nanosleep` 用 `rem` 重试：

```c
while (nanosleep(&req, &req) == -1 && errno == EINTR)
    ;
```

`TIMER_ABSTIME`：打断后通常**重算绝对时间**，不是简单用 rem。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 休眠与 `EINTR` 在嵌入式系统的应用场景 |
| HFT | 休眠与 `EINTR` 对低延迟和性能的影响 |

## 代码示例

```c
#include <time.h>
#include <unistd.h>
#include <signal.h>
#include <stdio.h>
void handler(int sig) { printf("handler\n"); }
int main(void) {
    signal(SIGUSR1, handler);
    /* nanosleep 被信号中断后, remaining 返回剩余时间 */
    struct timespec req = {.tv_sec = 5, .tv_nsec = 0}, rem;
    raise(SIGUSR1);
    int ret = nanosleep(&req, &rem);
    if (ret < 0) {
        printf("nanosleep 被中断, 剩余 %ld.%09ld\n", rem.tv_sec, rem.tv_nsec);
    }
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[23.4 时钟类型](23.4-suspending-execution-for-a-fixed-interva.md)
- 前置：**前置**：[Ch22 信号高级](../../chapter-22-signals-advanced/notes)  
- 后置：**后置**：[Ch24 Process Creation](../../chapter-24-process-creation/notes) · Ch63 多路 I/O
