# 32.3 状态与类型

> 本章：[TLPI 第 32 章 — Threads: Thread Cancellation](../README.md)
> - 上一节：[32.1 –32.2 取消请求](32.1-canceling-a-thread.md)
> - 下一节：[32.4 取消点](32.4-testing-for-thread-cancellation.md)

---

## 本节讲什么

pthread_setcancelstate(PTHREAD_CANCEL_ENABLE|DISABLE, &old); pthread_setcanceltype(PTHREAD_CANCEL_DEFERRED|ASYNCHRONOUS, &old); ---

---

## 要点

```c
pthread_setcancelstate(PTHREAD_CANCEL_ENABLE|DISABLE, &old);
pthread_setcanceltype(PTHREAD_CANCEL_DEFERRED|ASYNCHRONOUS, &old);
```

| 状态 | |
|------|--|
| ENABLE（默认） | 接受取消 |
| DISABLE | 请求挂起，再 ENABLE 后处理 |

| 类型（仅 ENABLE 时） | |
|----------------------|--|
| **DEFERRED（默认）** | 到**取消点**才取消；工程首选 |
| ASYNCHRONOUS | 任意指令可被杀；持锁/半改数据 → 灾难 |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 状态与类型 在嵌入式系统的应用场景 |
| HFT | 状态与类型 对低延迟和性能的影响 |

## 代码示例

```c
#include <pthread.h>
#include <stdio.h>
void *worker(void *arg) {
    /* 默认: PTHREAD_CANCEL_ENABLE + PTHREAD_CANCEL_DEFERRED */
    pthread_setcancelstate(PTHREAD_CANCEL_DISABLE, NULL);
    printf("取消已禁用\n");
    sleep(1);
    pthread_setcancelstate(PTHREAD_CANCEL_ENABLE, NULL);
    printf("取消已启用 (此处可能被取消)\n");
    return NULL;
}
int main(void) {
    pthread_t tid;
    pthread_create(&tid, NULL, worker, NULL);
    pthread_cancel(tid);
    pthread_join(tid, NULL);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[32.1 –32.2 取消请求](32.1-canceling-a-thread.md)
- 下一节：[32.4 取消点](32.4-testing-for-thread-cancellation.md)
- 前置：**前置**：[Ch29](../../chapter-29-threads-intro/notes) · [Ch30](../../chapter-30-thread-synchronization/notes)  
- 后置：**后置**：[Ch33 线程更多细节](../../chapter-33-threads-further/notes)
