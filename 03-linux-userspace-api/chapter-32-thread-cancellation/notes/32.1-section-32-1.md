# 32.1 –32.2 取消请求

> 本章：[TLPI 第 32 章 — Threads: Thread Cancellation](./README.md)
> - 上一节：本章首页
> - 下一节：[32.3 状态与类型](./32.3-state-types.md)

---

## 本节讲什么

int pthread_cancel(pthread_t thread);  /* 异步投递请求，不 join */ 可接合线程被取消后仍须 `pthread_join`。 `join` 得 `res == PTHREAD_CANCELED` → 被取消。

---

## 要点

```c
int pthread_cancel(pthread_t thread);  /* 异步投递请求，不 join */
```

| | |
|--|--|
| `pthread_exit` | 线程主动退 |
| `pthread_cancel` | 外部请求被动终止 |

可接合线程被取消后仍须 `pthread_join`。  
`join` 得 `res == PTHREAD_CANCELED` → 被取消。

Demo：[`code/thread_cancel.c`](./code/thread_cancel.c)

---

---

## 与 C 语言学习的关系

本节的 `read()` 是 K&R 第 8 章（8.2 节）的核心系统调用。K&R 用 `read(fd, buf, n)` 演示了低级 I/O。与 C 标准库 `fread()` 不同，`read()` 无缓冲、直接系统调用。理解短读（short read）是 C 系统编程的基本功——K&R 的 copy.c 示例就利用了 `read` 返回实际字节数的特性。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | –32.2 取消请求 在嵌入式系统的应用场景 |
| HFT | –32.2 取消请求 对低延迟和性能的影响 |

---

## 常见陷阱

1. **返回值 vs errno**

```c
if (pthread_create(&t, NULL, func, arg) < 0)  /* 错误! */
    perror("pthread_create");
/* 正确：返回错误码，不是 -1 */
if ((err = pthread_create(&t, NULL, func, arg)) != 0)
    strerror(err);
```

Pthreads 函数返回错误码而不是设置 errno。不能用 perror，要用 strerror(err)。

2. **忘记 join 或 detach**

```c
pthread_create(&t, NULL, func, arg);
/* 不 join 也不 detach → 资源泄漏 */
```

线程结束后如果不 join 或 detach，资源不会回收。必须 join 或 detach。

3. **mutex 忘记 unlock**

```c
pthread_mutex_lock(&m);
if (error) return;  /* 没 unlock! */
pthread_mutex_unlock(&m);
```

mutex lock 后在所有路径都必须 unlock。用 goto cleanup 或 pthread_mutex_trylock + cleanup。

---

## 自测题

**题目 1（选择）：** `pthread_create` 失败时返回什么？
A. -1
B. 0
C. 错误码（正整数）
D. NULL

<details>
<summary>参考答案</summary>

C。Pthreads 函数返回错误码（如 EAGAIN）而不是 -1，也不设置 errno。用 strerror(err) 获取描述。

</details>

**题目 2（判断）：** 主线程退出时其他线程会继续运行。

<details>
<summary>参考答案</summary>

错误。主线程调用 exit() 或从 main() 返回时，所有线程都会被终止。只有 pthread_exit 才允许其他线程继续。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
void *thread_func(void *arg) {
    int x = *(int*)arg;
    return NULL;
}
int val = 42;
pthread_create(&t, NULL, thread_func, &val);
```

<details>
<summary>参考答案</summary>

如果 thread_func 在 val 被修改后才执行，会读到错误的值。应确保 arg 的生命周期覆盖线程执行期，或动态分配。

</details>

**题目 4（应用）：** 写代码创建线程、等待线程结束并获取返回值。

<details>
<summary>参考答案</summary>

```c
void *func(void *arg) { return (void*)42; }
pthread_t t;
pthread_create(&t, NULL, func, NULL);
void *retval;
pthread_join(t, &retval);
printf("result: %ld\n", (long)retval);
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 下一节：[32.3 状态与类型](./32.3-state-types.md)
- 前置：**前置**：[Ch29](../chapter-29-threads-intro/notes.md) · [Ch30](../chapter-30-thread-synchronization/notes.md)  
- 后置：**后置**：[Ch33 线程更多细节](../chapter-33-threads-further/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
