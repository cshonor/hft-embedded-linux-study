# 2.8 内存映射（Memory Mappings）

> 本章：[TLPI 第 02 章 — Fundamental Concepts](../README.md)
> 源码核验：Linux **v6.6**（`mm/mmap.c`、`include/linux/mm_types.h`）
> - 上一节：[2.7 进程（Process）](2.7-processes.md)
> - 下一节：[2.9 共享库（Shared Libraries）](2.9-shared-libraries.md)

---

## 本节讲什么

内存映射（mmap）：把文件或设备「映射」进进程地址空间，之后用**内存访问**代替 `read`/`write`。本节回答三个问题：

1. mmap 在内核里留下什么数据结构？（`vm_area_struct`）
2. 文件映射 vs 匿名映射有什么区别？
3. mmap 什么时候比 `read`/`write` 快？代价是什么？

这是 HFT 里「零拷贝」「共享内存」两大性能手段的共同地基（Ch49 完整展开）。

---

## 1. 内核表示：`vm_area_struct`（VMA）

`mmap()` 的实质，是在进程的地址空间里**登记一块区域**（VMA），记录「这段虚拟地址对应什么」：

```c
SYSCALL_DEFINE6(mmap_pgoff, unsigned long, addr, unsigned long, len, ...)  /* mm/mmap.c:1427 */
```

```c
struct vm_area_struct {              /* include/linux/mm_types.h:565 */
    unsigned long vm_start;          /* :522 区域起始地址（含） */
    unsigned long vm_end;            /* :523 区域结束地址（不含，[start,end)） */
    struct file  *vm_file;           /* :526 映射的文件，匿名映射为 NULL */
    unsigned long vm_pgoff;          /* :525 文件内偏移（页为单位） */
    pgprot_t      vm_page_prot;      /* :580 访问权限（读/写/执行） */
    vm_flags_t    vm_flags;          /* :521 标志：MAP_SHARED/PRIVATE/ANON... */
};
```

```
进程地址空间 = 一串 VMA 链表（task_struct->mm->mmap）
┌──────┬──────────────┬──────────┬────────┬──────┐
│ Text │ Data/Heap    │  mmap 区  │ libc.so │ Stack│   ← 每段一个 VMA
└──────┴──────────────┴──────────┴────────┴──────┘
```

`cat /proc/pid/maps` 就是**遍历这个 VMA 链表**打印出来的——每一行是一个 `vm_area_struct`。

## 2. 两种映射类型

| 类型 | 关键标志 | 后端 | 用途 |
|------|---------|------|------|
| **文件映射** | `MAP_SHARED`/`MAP_PRIVATE` + 文件 fd | 磁盘文件的页缓存 | 零拷贝读文件、多进程共享文件内容 |
| **匿名映射** | `MAP_ANONYMOUS`，无 fd | 物理页（零填充） | `malloc` 大块内存、父子进程共享内存 |

```
文件映射： 虚拟地址 ──缺页──► 页缓存(page cache) ──► 磁盘文件
匿名映射： 虚拟地址 ──缺页──► 分配零填充物理页（无文件后端）
```

**访问机制**：mmap 后访问映射区，第一次触发**缺页中断**（page fault），内核才把文件页读进页缓存、建页表映射。之后就是纯内存访问，**不再有系统调用**——这就是它比 `read`/`write` 快的本质。

## 3. mmap vs read/write

| 维度 | `read`/`write` | `mmap` |
|------|----------------|--------|
| 数据路径 | 内核页缓存 → 用户缓冲区（**一次拷贝**） | 页缓存直接映射进用户地址（**零拷贝**） |
| 系统调用 | 每次读写都 syscall | 仅 `mmap`/`munmap` 两次 |
| 页错误 | 无 | 首次访问每页一次缺页 |
| 适用 | 顺序读一次、小文件 | 随机访问、大文件、共享 |

> **不是永远快**：小文件或顺序读一次，`mmap` 的缺页 + TLB 开销可能反超 `read`。mmap 的优势在「随机访问大文件」「反复访问」「跨进程共享」场景。

## 4. HFT / 嵌入式关联

| 现象 | 本节机制的兑现 |
|------|----------------|
| 零拷贝行情分发 | 文件映射让数据从页缓存直达用户态，省一次内核拷贝 |
| 跨进程共享内存 | `MAP_SHARED` + 同一文件（或 `MAP_ANONYMOUS` + fork）实现进程间共享，无 IPC 序列化开销 |
| 缺页毛刺 | mmap 后首次 touch 每页都触发 page fault，热路径要 `madvise(MADV_WILLNEED)` 或 `MAP_POPULATE` 预取 |
| 观测映射布局 | `/proc/pid/maps` = VMA 链表 dump，排查「地址被映射到哪」的第一工具 |

## 5. 代码示例

```c
#include <stdio.h>
#include <sys/mman.h>
#include <fcntl.h>
#include <unistd.h>
#include <sys/stat.h>
/* 文件映射：读文件零拷贝 */
int main(void) {
    int fd = open("/tmp/data.bin", O_RDONLY);
    struct stat st;
    fstat(fd, &st);                       /* 拿文件大小 */

    char *p = mmap(NULL, st.st_size, PROT_READ, MAP_PRIVATE, fd, 0);
    if (p == MAP_FAILED) { perror("mmap"); return 1; }

    /* 之后直接读内存，无需 read() 系统调用 */
    for (off_t i = 0; i < st.st_size; i++)
        putchar(p[i]);

    munmap(p, st.st_size);                /* 解除映射 */
    close(fd);
    return 0;
}
```

## 6. 衔接

- 下一节：[2.9 共享库（Shared Libraries）](2.9-shared-libraries.md) — 共享库靠 mmap 加载
- TLPI 深入：[Ch49 内存映射](../../chapter-49-memory-mappings/README.md) · [Ch48 SysV 共享内存](../../chapter-48-sysv-shared-memory/README.md) · [Ch54 POSIX 共享内存](../../chapter-54-posix-shared-memory/README.md)
- 内核视角（对照 MM）：[`06/ch04 进程地址空间`](../../../06-linux-mm/chapter-04-process-address-space/README.md) — VMA 全貌

---

<details>
<summary>代码自测（Q&A）</summary>

**Q1：`mmap` 之后马上能读到文件内容吗？为什么？**
A：能读到，但不是「立刻全量读入」。首次访问某页时触发缺页，内核才把那一页从磁盘读进页缓存并映射。所以 `mmap` 是**惰性**的——只有 touch 到的页才真正 I/O。这解释了「映射大文件很快，但遍历它才慢」的现象。

**Q2：`MAP_PRIVATE` 和 `MAP_SHARED` 的区别？**
A：`MAP_SHARED` 的修改写回文件、其他映射同一文件的进程可见；`MAP_PRIVATE` 的修改**不写回**文件，写时触发 COW 复制一份私有页（类似 fork 的 COW）。调试器常用 `MAP_PRIVATE` 读文件，避免意外改文件。

**Q3：`vm_start` 和 `vm_end` 是左闭右开区间 `[start, end)`，这有什么好处？**
A：相邻 VMA 能无缝拼接——一个的 `vm_end` 就是下一个的 `vm_start`，遍历时无重叠、无空洞歧义。这也是 `mmap` 返回地址按页对齐的原因：`vm_end - vm_start` 恒为页大小整数倍。

**Q4：匿名映射的「匿名」是什么意思？它映射到哪里？**
A：「匿名」= 没有文件后端（`vm_file == NULL`）。缺页时内核直接分配**零填充物理页**，不涉及磁盘。`malloc` 分配大块（超过 `MMAP_THRESHOLD`，默认 128KB）走的就是匿名 mmap；`fork` 后的共享内存也常基于匿名映射。

**Q5：为什么说 mmap 是「零拷贝」而 read 不是？**
A：`read` 的数据路径是「磁盘 → 页缓存 → 用户缓冲区」两次拷贝（页缓存→用户态那次是 CPU 拷贝）。mmap 让用户地址**直接映射到页缓存那一页**，读取时 CPU 直接访问页缓存，省掉「页缓存→用户缓冲区」那次拷贝。注意「零拷贝」指省 CPU 拷贝，磁盘 I/O 本身省不掉。

</details>
