# 2.5 文件 I/O 模型（File I/O Model）

> 本章：[TLPI 第 02 章 — Fundamental Concepts](../README.md)
> 源码核验：Linux **v6.6**（`fs/open.c`、`fs/read_write.c`、`include/linux/fdtable.h`）
> - 上一节：[2.4 目录层次](2.4-directory-hierarchy.md)
> - 下一节：[2.6 程序（Programs）](2.6-programs.md)

---

## 本节讲什么

Linux 的「一切皆文件」模型。本节回答三个问题：

1. fd（文件描述符）到底是什么？它和内核里的「打开文件」怎么对应？
2. `open`/`read`/`write` 在内核里各走哪条链路？
3. 为什么 `fork` 后父子能共享 fd？`O_CLOEXEC` 为什么重要？

这是全书 I/O 的「纲」——后面 Ch4/Ch5 的每个 `open`/`read`/`write` 细节，都建立在本节的 fd 三层结构之上。

---

## 1. fd 的三层结构（内核视角）

用户态手里的 `fd` 只是一个**小整数**，真正的打开文件状态在内核里分三层：

```
用户态             内核态
 fd (int) ──────► files_struct（每进程一份，task_struct->files）
                     │  fdtable（fd 数组，fd 即下标）
                     ▼
                  struct file（打开文件对象：inode + 偏移量 + f_op）
                     │
                     ▼
                  struct inode（磁盘文件本体，VFS 层）
```

| 层 | 结构 | 作用 | 源码 |
|----|------|------|------|
| 进程级 | `struct files_struct` | 每进程的文件表，含 `next_fd`（下一个可分配 fd） | `fdtable.h:49-63` |
| 表级 | `struct fdtable` | fd → `struct file*` 的数组，`max_fds` 限制上限 | `fdtable.h:27-28` |
| 文件级 | `struct file` | 打开文件的**运行时状态**（当前偏移、访问模式、`f_op`） | `include/linux/fs.h` |

**关键认知**：`fd` 是「进程私有」的——两个进程的 `fd 3` 可以指向完全不同的文件。但 `fork` 后，父子进程的 `fdtable` 指向**同一批 `struct file`**（各自持引用计数），所以共享同一个文件偏移量（详见 Ch24）。

## 2. `open` / `read` / `write` 的系统调用链路

```c
SYSCALL_DEFINE3(open, const char __user *, filename, int, flags, umode_t, mode)   /* fs/open.c:1441 */
SYSCALL_DEFINE3(read, unsigned int, fd, char __user *, buf, size_t, count)        /* fs/read_write.c:621 */
SYSCALL_DEFINE3(write, unsigned int, fd, const char __user *, buf, size_t, count) /* fs/read_write.c:646 */
```

```
open(filename, flags)  → do_sys_open → 分配最小可用 fd → 填 fdtable[fd] = file
read(fd, buf, n)       → fdget_pos(fd) 拿 file → vfs_read  → file->f_op->read
write(fd, buf, n)      → fdget_pos(fd) 拿 file → vfs_write → file->f_op->write
close(fd)              → 从 fdtable 摘除 → 引用计数减一 → 归零才释放 file
```

> `read`/`write` 只认 `fd` 这个整数，不认文件名——**文件名只在 `open` 那一刻被解析成 inode**。所以「重命名/删除一个正在被写的文件」不影响已经打开的 fd（inode 还在），这也是「写临时文件 + rename 原子替换」能工作的原因（见 Ch18）。

## 3. 「一切皆文件」与默认三个 fd

Linux 用**同一套 API**（`open`/`read`/`write`/`close`）操作所有 I/O 资源：

| 资源 | 打开方式 | 说明 |
|------|---------|------|
| 普通文件 | `open("/path", ...)` | 磁盘文件 |
| 设备 | `open("/dev/sda", ...)` | 字符/块设备文件 |
| 管道 | `pipe()` | 无文件名，直接返回两个 fd |
| 套接字 | `socket()` | 网络 I/O |
| 目录 | `open("/dir", O_RDONLY)` | 读目录项 |

每个进程启动时默认打开三个 fd（由父进程/登录 shell 传入）：

| fd | 名称 | 用途 |
|----|------|------|
| 0 | stdin | 标准输入 |
| 1 | stdout | 标准输出 |
| 2 | stderr | 标准错误（**不缓冲**，错误立即可见） |

## 4. HFT / 嵌入式关联

| 现象 | 本节机制的兑现 |
|------|----------------|
| fd 泄漏导致「Too many open files」 | `fdtable->max_fds` 达上限；长期运行进程必须 `close` 成对，或用 `O_CLOEXEC` 防 exec 后泄漏 |
| `fork`+`exec` 竞态泄漏 fd | 父进程 `open` 后未关、子进程 `exec` 继承 → 用 `O_CLOEXEC` 让 exec 时**原子关闭** |
| 日志热路径延迟 | `write` 走 `file->f_op->write` 有内核态拷贝；高频日志用 `write` 批量或异步落盘 |
| 文件偏移共享 | `fork` 后父子共享 `struct file` 偏移 → 并发写同一文件会交错，需 `O_APPEND` 原子追加 |

## 5. 代码示例

```c
#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>
#include <string.h>
/* fd 是进程私有小整数；O_CLOEXEC 防 exec 泄漏 */
int main(void) {
    int fd = open("/tmp/note.txt", O_WRONLY | O_CREAT | O_CLOEXEC, 0644);
    if (fd < 0) { perror("open"); return 1; }

    const char *msg = "hello fd\n";
    write(fd, msg, strlen(msg));   /* fd 是 open 返回的整数 */
    close(fd);                     /* 释放 fd，引用计数减一 */
    return 0;
}
```

## 6. 衔接

- 下一节：[2.6 程序（Programs）](2.6-programs.md)
- TLPI 深入：[Ch4 通用 I/O](../../chapter-04-file-io-universal/README.md) · [Ch5 I/O 进阶](../../chapter-05-file-io-further/README.md)
- 内核视角（对照 LKD）：[`05/Ch13 VFS`](../../../05-linux-kernel/chapter-13-vfs/README.md) — `struct file` / inode 全貌

---

<details>
<summary>代码自测（Q&A）</summary>

**Q1：两个进程都有 `fd 3`，它们指向同一个文件吗？**
A：不一定。fd 是进程私有的下标，指向各自 `files_struct->fdtable[3]`。只有「继承」（fork 共享 `struct file`）或「显式传 fd」（`sendmsg` 的 `SCM_RIGHTS`）时，两个进程的 fd 才指向同一 `struct file`。

**Q2：`open` 返回的 fd 一定是最小可用整数吗？为什么？**
A：是。`files_struct->next_fd` 记录下一个候选，`do_sys_open` 从最小空闲位分配。这是刻意设计——`close(1)` 后下次 `open` 往往拿到 fd 1，正好接替 stdout，实现「重定向」惯用法。

**Q3：`read(fd, ...)` 为什么不需要文件名参数？**
A：因为 `open` 时已经把路径解析成 inode 并存入 `struct file`。之后 `read`/`write`/`lseek` 只操作这个已打开的文件对象，与文件名解耦。所以文件被删除/重命名后，已打开的 fd 仍可正常读写。

**Q4：`O_CLOEXEC` 解决什么问题？不用它有什么后果？**
A：多线程程序里，线程 A 在 `open` 和「设 `FD_CLOEXEC` 标志」之间，线程 B 可能 `fork`+`exec`，导致子进程继承了一个本应关闭的 fd（竞态泄漏）。`O_CLOEXEC` 让 `open` 和「exec 时关闭」**原子绑定**，无竞态窗口。长期服务进程所有 fd 都应带 `O_CLOEXEC`。

**Q5：`close(fd)` 之后文件真的立刻关闭了吗？**
A：不一定。`close` 只是把 `fdtable[fd]` 摘除、`struct file` 引用计数减一。只有引用计数归零（所有指向它的 fd 都关了、fork 的子进程也关了），内核才真正释放 `struct file`。这就是「同一文件被 open 两次，关一个 fd 另一个还能用」的原因。

</details>
