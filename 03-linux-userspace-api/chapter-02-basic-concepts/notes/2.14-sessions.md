# 2.14 会话、控制终端与控制进程（Sessions）

> 本章：[TLPI 第 02 章 — Fundamental Concepts](../README.md)
> 源码核验：Linux **v6.6**（`include/linux/sched.h`、`include/linux/sched/signal.h`）
> - 上一节：[2.13 进程组（Process Groups）](2.13-process-groups.md)
> - 下一节：[2.15 伪终端（Pseudoterminals）](2.15-pseudoterminals.md)

---

## 本节讲什么

会话（session）是「进程组之上的容器」，把一组进程组与一个**控制终端**绑定。本节回答三个问题：

1. 会话（SID）和进程组（PGID）是什么关系？`setsid()` 做了什么？
2. 「控制终端」为什么会发出 `Ctrl+C`/`SIGHUP`，信号到底发给谁？
3. 守护进程为什么要 `fork()` + `setsid()` 两步脱离终端？

完整机制在 Ch34，这里建立「会话 = 终端关联单位」的核心心智。

---

## 1. 会话的基本结构

```
会话 (SID = 会话首领 PID)
 ├── 前台进程组  ←─ 唯一能读终端输入、接收 Ctrl+C 的组
 ├── 后台进程组 1
 ├── 后台进程组 2
 └── ...
```

| 概念 | 说明 |
|------|------|
| **SID** | 会话 ID，= 会话首领进程的 PID |
| **会话首领** | 调用 `setsid()` 创建会话的进程 |
| **控制终端** | 会话可（可选）关联一个终端设备，`/dev/tty` 指向它 |
| **控制进程** | 会话首领（若它有控制终端，也叫控制进程） |

```c
pid_t setsid(void);    /* 新建会话，调用者成为会话首领 */
pid_t getsid(pid_t pid);
```

## 2. 内核里的 session：`task->signal->pids[PIDTYPE_SID]`

会话 ID 和进程组 ID 一样，存在进程的 `signal` 结构里，按 `PIDTYPE_*` 索引取：

```c
/* include/linux/sched/signal.h:697 */
static inline struct pid *task_session(struct task_struct *task)
{ return task->signal->pids[PIDTYPE_SID]; }

/* include/linux/sched/signal.h:692 */
static inline struct pid *task_pgrp(struct task_struct *task)
{ return task->signal->pids[PIDTYPE_PGID]; }
```

取数值 ID 走 `task_session_vnr`（`include/linux/sched.h:1626`，内部 `__task_pid_nr_ns(tsk, PIDTYPE_SID, NULL)`）。**要点**：`pids[]` 是个数组，`PIDTYPE_PID`/`PIDTYPE_TGID`/`PIDTYPE_PGID`/`PIDTYPE_SID` 各自存一份 `struct pid` 引用，所以一个进程同时「属于」进程组和会话——不是树形嵌套，是两套独立的归属标签。

## 3. 控制终端：信号的源头

控制终端不是「进程的属性」，而是**会话的属性**。终端驱动（tty）收到按键后，把字符转成信号发给**会话的前台进程组**：

| 按键/事件 | 产生的信号 | 发给谁 |
|-----------|-----------|--------|
| `Ctrl+C`（intr） | SIGINT | 前台进程组 |
| `Ctrl+Z`（susp） | SIGTSTP | 前台进程组 |
| `Ctrl+\`（quit） | SIGQUIT | 前台进程组 |
| 终端断开（挂机/hangup） | SIGHUP | **会话首领（控制进程）** |

> **SIGHUP 是关键**：终端关闭（ssh 断开、串口拔出、pty 关闭）时，内核向会话的控制进程发 SIGHUP。默认动作是终止进程，这就是「ssh 一断，前台任务全挂」的根因——除非用 `nohup`/`setsid`/`tmux` 把进程挪出这个会话。

## 4. 守护进程为什么要 `setsid()`（两步脱离）

守护进程要在后台长期运行、不依赖任何终端，所以必须「脱离控制终端」。标准套路是**两次 fork + setsid**：

```c
/* 第一次 fork：子进程不再是「组长」，才有资格 setsid */
if (fork() > 0) _exit(0);      /* 父进程退出 */

setsid();                       /* 新会话，子进程成为会话首领（但尚无控制终端） */

/* 第二次 fork：会话首领退出，孙进程彻底不是首领、永远拿不到控制终端 */
if (fork() > 0) _exit(0);

/* 可选：chdir("/") + umask(0) + 关闭 0/1/2 + 打开 /dev/null */
```

**为什么 `setsid` 前要先 fork？** 内核规定：**进程组组长不能调用 `setsid`**（会返回 `EPERM`）。因为组长若成为新会话首领，会造成「组 ID 和会话 ID 冲突」的歧义。第一次 `fork` 让子进程变成非组长，才有资格 `setsid`。

**为什么还要第二次 fork？** `setsid` 后子进程成为会话首领，而**会话首领可以再获取控制终端**（打开一个 tty 就自动成为它的控制终端）。第二次 fork 产生孙进程（非首领），它**永远无法**再获取控制终端，从而彻底摆脱终端。

## 5. HFT / 嵌入式关联

| 现象 | 本节机制的兑现 |
|------|----------------|
| 交易服务 daemon 化 | `fork`+`setsid` 脱离控制终端，避免运维误按 `Ctrl+C` 打断策略主循环 |
| ssh 断开导致策略进程被杀 | 终端关闭触发 SIGHUP → 控制进程默认终止；生产环境必须 `setsid` 或 `tmux` 托管 |
| 后台读 stdin 被挂起 | 后台进程组读终端触发 SIGTTIN 默认暂停；headless 交易机 stdout 非 tty 无此问题 |
| 嵌入式 init 进程 | PID 1（init/systemd）自成一会话、无控制终端，负责收养孤儿进程 |

## 6. 代码示例

```c
#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

/* 演示：setsid 后进程脱离控制终端，SID == 自己的 PID */
int main(void) {
    pid_t pid = fork();
    if (pid < 0) { perror("fork"); return 1; }
    if (pid > 0) _exit(0);          /* 父进程退出，子进程成为孤儿 */

    if (setsid() == -1) {           /* 成为新会话首领 */
        perror("setsid"); return 1;
    }

    printf("sid=%d (should == pid=%d)\n", getsid(0), getpid());
    printf("tty=%s (null = 已脱离终端)\n",
           ttyname(0) ? ttyname(0) : "none");

    /* 后台常驻，不再响应终端 Ctrl+C */
    for (;;) pause();
    return 0;
}
```

编译运行后 `ps -o pid,sid,pgid,tty,cmd` 查看：SID 等于进程自己的 PID，TTY 列为空——标志它已脱离控制终端。

## 7. 衔接

- 下一节：[2.15 伪终端（Pseudoterminals）](2.15-pseudoterminals.md)
- TLPI 深入：[Ch34 进程组与会话](../../chapter-34-process-groups-sessions/README.md) · [Ch37 守护进程](../../chapter-37-daemons/README.md)
- 内核视角：[05/Ch3 进程管理](../../../05-linux-kernel/chapter-03-process-management/README.md) — `task->signal->pids[]` 的 PID 类型体系

---

<details>
<summary>代码自测（Q&A）</summary>

**Q1：会话和进程组是什么关系？**
A：会话是「进程组的容器」，一个会话里有多个进程组，其中至多一个是**前台进程组**。进程组归属用 `pids[PIDTYPE_PGID]` 标记，会话归属用 `pids[PIDTYPE_SID]` 标记，两者是独立的两套标签，不是树形嵌套。会话 ID（SID）等于会话首领的 PID。

**Q2：`setsid()` 到底做了什么？为什么组长不能调用？**
A：`setsid` 做三件事：① 让调用进程成为新会话的**首领**；② 让调用进程成为新进程组的**组长**（新组里只有它自己）；③ 与旧控制终端**断开**。组长不能调用是因为：若允许组长成为新会话首领，就会出现「它的 PGID 同时是一个组的 ID 又是新会话的 ID」的歧义，内核干脆禁止（返回 `EPERM`）。

**Q3：控制终端什么时候会发 SIGHUP？发之前发生了什么？**
A：终端设备断开（ssh 掉线、串口拔出、pty 主端关闭）时，tty 驱动检测到挂机（hangup），向该会话的**控制进程（会话首领）**发送 SIGHUP。SIGHUP 默认动作是终止进程，所以「ssh 一断前台全挂」。之后会话若继续存在，其成员读/写已断的终端会得到 `EIO`。

**Q4：为什么守护进程要两次 fork？一次不行吗？**
A：一次 `fork` + `setsid` 后，进程已经是会话首领，而**会话首领仍可能再获取控制终端**（只要打开一个 tty 就自动绑定）。第二次 `fork` 产生孙进程（非首领、非组长），它**结构上就永远无法**再获取控制终端，从而彻底杜绝「被终端信号打断」的风险。所以两次 fork 是「double fork」守护进程标准写法。

**Q5：`nohup` 和 `setsid` 防挂断的原理一样吗？**
A：不完全一样。`nohup` 只是让进程**忽略 SIGHUP**（信号来了不终止），进程仍在原会话、仍挂控制终端，其他终端信号（SIGINT/SIGTSTP）照收。`setsid` 是**彻底脱离**：新建会话、无控制终端，从根源上就不会收到终端驱动产生的任何信号。要真正「后台常驻」应优先用 `setsid`（或 `tmux`/`screen` 这类会话管理器）。

</details>
