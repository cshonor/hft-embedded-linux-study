# 2.18 实时（Realtime）

> 本章：[TLPI 第 02 章 — Fundamental Concepts](../README.md)
> 源码核验：Linux **v6.6**（`mm/mlock.c`、`kernel/sched/core.c`）
> - 上一节：[2.17 客户端/服务器（Client-Server）](2.17-client-server.md)
> - 下一节：[2.19 /proc 文件系统（The /proc File System）](2.19-proc-filesystem.md)

---

## 本节讲什么

「实时」指系统能在**确定的时限内**响应事件。本节回答三个问题：

1. 硬实时和软实时的本质区别是什么？错过期限的代价有何不同？
2. Linux 靠哪几样机制凑出「实时能力」？`SCHED_FIFO`、`mlock`、`PREEMPT_RT` 各管什么？
3. HFT 的微秒级延迟，为什么必须 `mlockall` + CPU 亲和绑定？

完整调度机制在 Ch35（实时调度）与 Ch50（内存锁定），这里建立「实时 = 确定性」的核心心智。

---

## 1. 硬实时 vs 软实时

| 维度 | 硬实时（Hard RT） | 软实时（Soft RT） |
|------|------------------|------------------|
| 错过期限的后果 | **系统级失败**（可能造成事故/损坏） | 性能下降、服务质量降低，可容忍 |
| 典型场景 | 核反应堆控制、安全气囊、飞行控制 | HFT、音视频流、游戏 |
| 对 Linux 的要求 | 通常需 RTOS 或 `PREEMPT_RT` 补丁 | 标准内核 + 实时调度策略基本够用 |

**核心是「确定性」而非「快」**：硬实时要求的是**最坏情况延迟（worst-case latency）有上界**，不是平均响应快。标准 Linux 内核是「吞吐优先」，`CONFIG_PREEMPT_NONE` 下关中断/持自旋锁的临界区会引入不可预知的调度延迟，所以纯标准内核无法保证硬实时。

## 2. 实时调度策略：`SCHED_FIFO` / `SCHED_RR`

普通进程用 `SCHED_NORMAL`（CFS，完全公平调度，按 vruntime 分配时间片）。实时进程用：

| 策略 | 语义 | 抢占行为 |
|------|------|----------|
| `SCHED_FIFO` | 先进先出，**没有时间片**，跑到主动让出/被更高优先级抢占 | 高优先级可抢占低优先级 |
| `SCHED_RR` | 轮转，同优先级**有时间片**（`sched_rr_timeslice_ms`） | 同优先级轮流跑 |

**实时优先级范围**（内核注释，`kernel/sched/core.c:7652-7654`）：

```c
/* Valid priorities for SCHED_FIFO and SCHED_RR are
 * 1..MAX_RT_PRIO-1, valid priority for SCHED_NORMAL,
 * SCHED_BATCH and SCHED_IDLE is 0. */
```

`MAX_RT_PRIO = 100`，所以实时优先级是 **1~99**（用户态），数值越大优先级越高。**关键事实：任何实时进程（优先级 ≥1）都优先于所有 `SCHED_NORMAL` 进程（优先级 0）**——所以一个跑飞的 `SCHED_FIFO` 任务能把整台机器「卡死」，因为它永远不让出 CPU。

```c
struct sched_param sp = { .sched_priority = 50 };   /* 1..99 */
sched_setscheduler(0, SCHED_FIFO, &sp);              /* 需 CAP_SYS_NICE 权限 */
```

## 3. 内存锁定：`mlock` / `mlockall`（防换页）

实时任务的隐形杀手是**缺页（page fault）**：哪怕 CPU 上跑得好好的，一旦访问的页被换出（swap），就要等磁盘换入，延迟从微秒暴涨到毫秒级。`mlock` 把页钉在物理内存里，禁止换出：

```c
/* mm/mlock.c:622 */
SYSCALL_DEFINE2(mlock, unsigned long, start, size_t, len)

/* mm/mlock.c:705 */
SYSCALL_DEFINE1(mlockall, int, flags)
{
	...
	if (!flags || (flags & ~(MCL_CURRENT | MCL_FUTURE | MCL_ONFAULT)) ||
	    flags == MCL_ONFAULT)
		return -EINVAL;

	if (!can_do_mlock())
		return -EPERM;

	lock_limit = rlimit(RLIMIT_MEMLOCK);   /* 受 RLIMIT_MEMLOCK 限制 */
	lock_limit >>= PAGE_SHIFT;
	...
	ret = apply_mlockall_flags(flags);    /* 给 VMA 打 VM_LOCKED 标记 */
	...
	if (!ret && (flags & MCL_CURRENT))
		mm_populate(0, TASK_SIZE);        /* 立即建立页表 + 分配物理页 */
	return ret;
}
```

三个 `MCL_*` 标志：

| 标志 | 含义 |
|------|------|
| `MCL_CURRENT` | 锁定**当前**已映射的所有页 |
| `MCL_FUTURE` | 锁定**以后**新映射的页（`mmap`/`malloc` 增长的部分） |
| `MCL_ONFAULT` | 延迟锁定：等页首次被访问（缺页）时才锁，省内存 |

**HFT 用 `mlockall(MCL_CURRENT | MCL_FUTURE)`**：既锁现有页，又锁将来分配的热路径缓冲，彻底消除「策略开跑后首次访问某块内存」的缺页毛刺。代价是锁定的内存不计入 swap 候选，系统内存压力大时可能 OOM。

## 4. `PREEMPT_RT` 补丁：让内核本身可抢占

实时调度策略和内存锁定都只解决**用户态**的确定性；真正的延迟往往来自**内核态**——关中断、持自旋锁、关抢占的临界区。`PREEMPT_RT`（现已逐步合入主线，`CONFIG_PREEMPT_RT`）把内核里几乎所有自旋锁换成可睡眠的互斥锁（rt_mutex）、把中断处理线程化，使内核态也能被高优先级任务抢占，从而压低**最坏情况调度延迟**。

| 内核配置 | 抢占能力 | 适用 |
|----------|----------|------|
| `PREEMPT_NONE` | 仅在返回用户态时调度 | 服务器吞吐优先 |
| `PREEMPT_VOLUNTARY` | 自愿让出点 + 返回用户态 | 桌面 |
| `PREEMPT`（抢占式内核） | 内核态大部分可抢占 | 通用低延迟 |
| `PREEMPT_RT` | 近乎全可抢占（含中断线程化） | 硬实时 |

## 5. HFT 的实时需求兑现

HFT 是典型的**软实时 + 极致低延迟**场景，一条完整的「低延迟改造清单」都对应本节机制：

| 需求 | 手段 | 消除的延迟源 |
|------|------|--------------|
| 微秒级响应 | `SCHED_FIFO` + 高优先级 | 被 CFS 排队 / 时间片抢占 |
| 稳定的延迟上界 | CPU 亲和（`sched_setaffinity`）+ `isolcpus` 隔离核 | 跨 CPU 迁移 / 缓存失效 |
| 消除缺页毛刺 | `mlockall(MCL_CURRENT\|MCL_FUTURE)` | swap 换入 / 首次缺页 |
| 避免系统调用开销 | 批量化、`io_uring`、用户态轮询（busy-wait） | 上下文切换 / 系统调用进出 |
| 避免调度抖动 | `SCHED_FIFO` 独占核、关掉 tick（`nohz_full`） | 周期定时器中断打断 |

> **诚实评估**：单靠应用层（`SCHED_FIFO` + `mlockall` + CPU 亲和）能压掉 80% 的软实时延迟；但要逼近硬件极限（个位数微秒、抖动 <1µs），还需要 `isolcpus`/`nohz_full` 内核参数 + `PREEMPT_RT` 内核 + 网卡驱动零拷贝（`AF_XDP`/DPDK）配合，这已经是「内核 + 用户态」协同的系统工程。

## 6. 代码示例

```c
#define _GNU_SOURCE
#include <stdio.h>
#include <sched.h>
#include <sys/mman.h>
#include <unistd.h>

/* 把进程钉在 CPU 2，锁内存，升实时优先级 */
int main(void) {
    /* 1. CPU 亲和：只跑在 CPU 2 */
    cpu_set_t set;
    CPU_ZERO(&set);
    CPU_SET(2, &set);
    if (sched_setaffinity(0, sizeof(set), &set) == -1)
        perror("sched_setaffinity");

    /* 2. 锁内存：当前 + 将来，消除缺页毛刺 */
    if (mlockall(MCL_CURRENT | MCL_FUTURE) == -1)
        perror("mlockall");        /* 需 CAP_IPC_LOCK 或足够 RLIMIT_MEMLOCK */

    /* 3. 实时调度：SCHED_FIFO 优先级 80 */
    struct sched_param sp = { .sched_priority = 80 };
    if (sched_setscheduler(0, SCHED_FIFO, &sp) == -1)
        perror("sched_setscheduler");  /* 需 CAP_SYS_NICE */

    printf("pinned to CPU 2, FIFO prio 80, memory locked\n");
    for (;;) ;  /* 热循环占核（busy-wait 轮询行情） */
    return 0;
}
```

编译运行（`sudo` 获取权限后）：`gcc -o rt rt.c && sudo ./rt`。用 `top -H` 看 `PR` 列显示 `-80` 或 `rt`，确认已进实时调度；`ps -o pid,psr,ni,rtprio,cmd` 确认 PSR=2（钉在 CPU 2）。

## 7. 衔接

- 下一节：[2.19 /proc 文件系统（The /proc File System）](2.19-proc-filesystem.md)
- TLPI 深入：[Ch35 进程优先级与调度](../../chapter-35-process-priorities-scheduling/README.md) · [Ch50 虚拟内存操作（mlock）](../../chapter-50-virtual-memory/README.md)
- 内核视角：[05/Ch3 进程管理](../../../05-linux-kernel/chapter-03-process-management/README.md) — CFS 与实时调度器

---

<details>
<summary>代码自测（Q&A）</summary>

**Q1：硬实时和软实时的本质区别是什么？**
A：区别在**错过期限的后果**：硬实时错过期限 = 系统失败（可能造成物理损坏/事故），所以要求**最坏情况延迟有严格上界**；软实时错过期限只是性能/体验下降，追求的是「尽量快、延迟尽量低」。HFT 是软实时——偶尔一次延迟毛刺是亏损，不是「系统失败」，所以标准内核 + 实时策略通常够用，不必上 RTOS。

**Q2：`SCHED_FIFO` 和 `SCHED_RR` 有什么区别？**
A：都是实时策略（优先级 1~99，高于所有普通进程）。`SCHED_FIFO` **没有时间片**，任务一直跑到主动让出（sleep/阻塞）或被更高优先级抢占；`SCHED_RR` 给**同优先级**的任务分配时间片轮转，避免一个 FIFO 任务把同优先级伙伴饿死。两者都被更高优先级任务抢占。

**Q3：HFT 为什么必须 `mlockall()`？**
A：因为**缺页**是低延迟的头号杀手——访问被换出（swap）的页要等磁盘换入，延迟从微秒涨到毫秒级，且发生时机不可预测。`mlockall(MCL_CURRENT|MCL_FUTURE)` 把当前及未来映射的页全部钉在物理内存、禁止换出，消除「策略开跑后首次访问某块内存」的缺页毛刺。代价是占用物理内存、系统内存紧张时可能 OOM。

**Q4：`MCL_CURRENT` 和 `MCL_FUTURE` 分别锁什么？**
A：`MCL_CURRENT` 锁定调用时**已经映射**的所有页（并触发 `mm_populate` 立即分配物理页）；`MCL_FUTURE` 锁定**之后新映射**的页（后续 `mmap`/堆增长自动带 `VM_LOCKED`）。HFT 两个都要：既锁已有缓冲，又锁开跑后热路径动态分配的内存。

**Q5：为什么光靠 `SCHED_FIFO` + `mlockall` 还到不了「硬实时」？**
A：因为延迟大头在内核态：关中断、持自旋锁、关抢占的临界区会让用户态高优先级任务**无法被及时调度**，且这段延迟不可预测。要压缩它需要内核级配合——`PREEMPT_RT` 补丁（自旋锁换 rt_mutex、中断线程化，让内核态也可抢占）+ `isolcpus`/`nohz_full`（隔离核、关周期 tick）。所以「实时」是用户态 + 内核态协同的系统工程，不是单靠一个系统调用。

</details>
