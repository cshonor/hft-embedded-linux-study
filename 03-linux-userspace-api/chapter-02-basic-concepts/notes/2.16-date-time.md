# 2.16 日期与时间（Date and Time）

> 本章：[TLPI 第 02 章 — Fundamental Concepts](../README.md)
> 源码核验：Linux **v6.6**（`arch/x86/entry/syscalls/syscall_64.tbl`、`lib/vdso/gettimeofday.c`）
> - 上一节：[2.15 伪终端（Pseudoterminals）](2.15-pseudoterminals.md)
> - 下一节：[2.17 客户端/服务器（Client-Server）](2.17-client-server.md)

---

## 本节讲什么

Linux 有两类时间：**日历时间**（墙上的钟，可被修改）和**进程时间**（CPU 消耗）。本节回答三个问题：

1. 日历时间和进程时间分别是什么？用什么 API 拿？
2. `CLOCK_REALTIME`、`CLOCK_MONOTONIC`、`CLOCK_MONOTONIC_RAW` 有什么区别？HFT 计时该用哪个？
3. 为什么 `clock_gettime` 能快到「不进内核」（vDSO）？

完整 API 在 Ch10，这里建立「计时选对时钟 = 延迟测量正确性」的核心心智。

---

## 1. 两类时间

| 类型 | 说明 | 典型 API |
|------|------|----------|
| **日历时间（wall clock）** | 自 Epoch（1970-01-01 00:00:00 UTC）起的秒数，可被修改 | `time()`、`gettimeofday()`、`clock_gettime()` |
| **进程时间（CPU time）** | 进程消耗的 CPU 时间 | `times()`、`clock()`、`clock_gettime(CLOCK_PROCESS_CPUTIME_ID)` |

进程时间又分三段：

| 时间 | 含义 |
|------|------|
| **user time** | 用户态执行消耗的 CPU 时间 |
| **system time** | 内核态（系统调用）消耗的 CPU 时间 |
| **real time** | 实际墙上流逝时间（= user + system + 等待/被抢占） |

`time` 命令的 `real/user/sys` 三列就是这个区分——`real` 可能远大于 `user+sys`（进程在等 I/O、被调度器抢占）。

## 2. 三种时钟 API 与精度

```c
time_t t = time(NULL);                          /* 秒级，最粗 */
struct timeval tv; gettimeofday(&tv, NULL);     /* 微秒级 */
struct timespec ts;
clock_gettime(CLOCK_REALTIME, &ts);             /* 纳秒级，现代首选 */
```

`clock_gettime` 是 POSIX 标准推荐接口，用 `clockid_t` 选择「哪个钟」：

| clockid | 含义 | 会跳变吗 |
|---------|------|----------|
| `CLOCK_REALTIME` | 墙上时间（Epoch 起） | ✅ 被 NTP 步进、`settimeofday` 修改 |
| `CLOCK_MONOTONIC` | 单调递增（从某点起） | ❌ 只受 NTP 频率微调（slew），不回拨 |
| `CLOCK_MONOTONIC_RAW` | 纯硬件计数（不受 NTP 任何调整） | ❌ 完全不受 NTP 影响 |
| `CLOCK_PROCESS_CPUTIME_ID` | 本进程 CPU 时间 | ❌ |
| `CLOCK_THREAD_CPUTIME_ID` | 本线程 CPU 时间 | ❌ |

## 3. HFT 计时铁律：用 `CLOCK_MONOTONIC`

**测「时间间隔」绝不能用 `CLOCK_REALTIME`**——它会被 NTP 步进（stepping）或管理员改时间**回拨**，导致测出**负的时间差**。正确做法：

| 场景 | 该用 |
|------|------|
| 测量延迟/间隔（latency） | `CLOCK_MONOTONIC` 或 `CLOCK_MONOTONIC_RAW` |
| 记录「事件发生在墙上几点」 | `CLOCK_REALTIME`（但别用它算间隔） |
| 统计进程 CPU 占用 | `CLOCK_PROCESS_CPUTIME_ID` |

```
CLOCK_REALTIME:      ├───────────┤  NTP 回拨 → 时间倒退，间隔算出负数 ❌
CLOCK_MONOTONIC:     ├───────────→  只会单调递增，间隔永远 ≥ 0        ✅
```

`CLOCK_MONOTONIC` vs `CLOCK_MONOTONIC_RAW`：前者会被 NTP **频率微调**（slew，缓慢校正晶振漂移，不回拨），后者是**纯硬件计数**、完全不受 NTP 干扰。HFT 测微秒级抖动用 `CLOCK_MONOTONIC_RAW` 更纯粹（不受 NTP 的 slew 影响），但日常测间隔用 `CLOCK_MONOTONIC` 即可。

## 4. vDSO：`clock_gettime` 为什么能不进内核

`clock_gettime` 的系统调用号是 228（`syscall_64.tbl`），但**实际调用常常不陷入内核**——因为内核把时钟数据通过 **vDSO**（virtual dynamic shared object）映射到用户态，`clock_gettime` 直接在用户态读这块只读数据：

```c
/* lib/vdso/gettimeofday.c:110 —— 高精度时钟，用户态直接算 */
static __always_inline int do_hres(const struct vdso_data *vd, clockid_t clk,
				   struct __kernel_timespec *ts)
```

vDSO 是内核映射进每个进程地址空间的一段代码 + 数据（`vdso_data`），glibc 的 `clock_gettime`/`gettimeofday`/`time` 会优先走 vDSO 快速路径，**只有需要「精确到纳秒 + 实时校准」的罕见情况才真正陷入内核**。这是「高频计时零开销」的关键——HFT 热路径里大量 `clock_gettime` 调用不会拖慢系统。

## 5. HFT / 嵌入式关联

| 现象 | 本节机制的兑现 |
|------|----------------|
| 延迟测量正确性 | 用 `CLOCK_MONOTONIC` 测间隔，避免 NTP 回拨导致负延迟 |
| 高频时间戳零开销 | `clock_gettime` 走 vDSO 用户态直接读，不陷入内核 |
| 订单时间戳审计 | 用 `CLOCK_REALTIME` 记「墙上时间」做对账，但内部排序用 MONOTONIC |
| CPU 占用归因 | `CLOCK_PROCESS_CPUTIME_ID` 区分「跑在用户态 vs 内核态」的时间 |
| 嵌入式 RTC | 嵌入式设备无网络时靠 RTC 芯片维持 `CLOCK_REALTIME`，启动时同步 |

## 6. 代码示例

```c
#define _GNU_SOURCE
#include <stdio.h>
#include <time.h>

/* 测一段代码的耗时：必须用 CLOCK_MONOTONIC，不能 REALTIME */
static long long ns_diff(struct timespec a, struct timespec b) {
    return (b.tv_sec - a.tv_sec) * 1000000000LL
         + (b.tv_nsec - a.tv_nsec);
}

int main(void) {
    struct timespec t0, t1;

    clock_gettime(CLOCK_MONOTONIC, &t0);
    /* 模拟一段热路径（如解析行情包） */
    volatile long x = 0;
    for (long i = 0; i < 1000000; i++) x += i;
    clock_gettime(CLOCK_MONOTONIC, &t1);

    printf("elapsed = %lld ns\n", ns_diff(t0, t1));

    /* 墙上时间（用于审计，别用来算间隔） */
    struct timespec wall;
    clock_gettime(CLOCK_REALTIME, &wall);
    printf("wall clock = %lld.%09ld\n",
           (long long)wall.tv_sec, wall.tv_nsec);

    /* 进程 CPU 时间 */
    clock_gettime(CLOCK_PROCESS_CPUTIME_ID, &wall);
    printf("cpu time   = %lld.%09ld\n",
           (long long)wall.tv_sec, wall.tv_nsec);
    return 0;
}
```

编译：`gcc -o t t.c`（`clock_gettime` 在 glibc 2.17+ 不需要 `-lrt`）。`elapsed` 是单调时钟测的间隔，NTP 调整也不会变成负数。

## 7. 衔接

- 下一节：[2.17 客户端/服务器（Client-Server）](2.17-client-server.md)
- TLPI 深入：[Ch10 时间](../../chapter-10-time/README.md) · [Ch23 定时器与休眠](../../chapter-23-timers-sleeping/README.md)
- 关联：[2.18 实时（Realtime）](2.18-realtime.md) — 高精度计时与实时延迟

---

<details>
<summary>代码自测（Q&A）</summary>

**Q1：日历时间和进程时间的区别是什么？**
A：日历时间是「墙上的钟」（自 Epoch 起的秒数，可被修改），进程时间是「进程消耗的 CPU 时间」（不可回拨）。进程时间又分 user time（用户态）、system time（内核态）、real time（实际流逝 = user + system + 等待/被抢占）。

**Q2：`CLOCK_REALTIME` 和 `CLOCK_MONOTONIC` 的区别是什么？为什么测间隔不能用 REALTIME？**
A：`CLOCK_REALTIME` 是墙上时间，会被 NTP 步进或管理员改时间**回拨**，用它测间隔可能算出**负数**；`CLOCK_MONOTONIC` 从某点单调递增、只受 NTP 频率微调（slew）不回拨，间隔永远 ≥ 0。所以「测延迟/间隔」用 MONOTONIC，「记墙上几点」才用 REALTIME。

**Q3：`CLOCK_MONOTONIC` 和 `CLOCK_MONOTONIC_RAW` 有什么区别？**
A：`CLOCK_MONOTONIC` 受 NTP **频率微调**（slew——缓慢校正晶振漂移，让时间走快/走慢一点，但绝不回拨）；`CLOCK_MONOTONIC_RAW` 是**纯硬件计数**，完全不受 NTP 任何调整。HFT 测微秒级抖动用 RAW 更纯粹（连 slew 都不想要），日常测间隔用 MONOTONIC 即可。

**Q4：为什么 `clock_gettime` 能快到「不陷入内核」？**
A：因为 vDSO 机制——内核把时钟数据（`vdso_data`）和一段代码映射进每个进程的地址空间，glibc 的 `clock_gettime`/`gettimeofday`/`time` 优先走 vDSO 快速路径，**直接在用户态读只读数据算出时间**（`lib/vdso/gettimeofday.c` 的 `do_hres`/`do_coarse`），只有需要纳秒级实时校准的罕见情况才真正发系统调用（号 228）陷入内核。所以高频计时几乎零开销。

**Q5：`time` 命令输出的 `real/user/sys` 三列分别是什么？为什么 `real` 可能远大于 `user+sys`？**
A：`real` = 墙上实际流逝时间；`user` = 用户态 CPU 时间；`sys` = 内核态 CPU 时间（系统调用）。`real > user+sys` 是常态，因为进程在等 I/O、被调度器抢占、睡眠时，CPU 时间不增加但墙上时间在走。`real` 远大于 `user+sys` 说明进程大量时间在等待（I/O 密集），接近则说明是 CPU 密集。

</details>
