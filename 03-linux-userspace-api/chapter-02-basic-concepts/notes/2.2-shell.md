# 2.2 Shell

> 本章：[TLPI 第 02 章 — Fundamental Concepts](../README.md)
> - 上一节：[2.1 内核（Kernel）](2.1-kernel.md)
> - 下一节：[2.3 用户与组（Users and Groups）](2.3-users-and-groups.md)

---

## 本节讲什么

Shell 是「用户与内核之间的命令行解释器」。本节回答三个问题：

1. Shell 是内核的一部分吗？它的核心循环是什么？
2. 内置命令（builtin）和外部命令有什么区别？`system()` 为什么危险？
3. Shell 的「作业控制」和前面讲的进程组/会话有什么关系？

---

## 1. Shell 的本质：一个普通用户态进程

Shell **不是内核的一部分**，它只是一个跑在用户态的程序（`/bin/bash`、`/bin/dash` 等），做的事就是循环：

```c
/* shell 核心循环（极度简化） */
while (1) {
    printf("$ ");                        /* 打印提示符 */
    if (fgets(cmd, sizeof(cmd), stdin) == NULL) break;  /* 读一行命令 */

    pid_t pid = fork();                  /* ① fork 出子进程 */
    if (pid == 0) {
        execvp(argv[0], argv);           /* ② 子进程 exec 成目标程序 */
        perror("execvp"); _exit(127);    /* exec 失败才走到这 */
    }
    waitpid(pid, &status, 0);            /* ③ 父进程等待子进程结束 */
}
```

「读命令 → fork → exec → wait」就是 shell 的全部秘密。**每敲一条外部命令，就 fork 一个新进程 + exec 掉它**——这也是「为什么 shell 里循环启动进程有开销」的根源。

## 2. 常见 Shell 与「内置 vs 外部」

| Shell | 路径 | 特点 |
|-------|------|------|
| bash | `/bin/bash` | GNU 默认，功能最全 |
| dash | `/bin/dash` | Debian/Ubuntu 轻量，`/bin/sh` 常软链到它 |
| zsh | `/usr/bin/zsh` | 交互补全强 |
| fish | `/usr/bin/fish` | 现代，**非 POSIX 兼容**（脚本会跑挂） |

**内置命令 vs 外部命令**是关键区分：

| 类型 | 例子 | 是否 fork |
|------|------|-----------|
| **内置命令（builtin）** | `cd`、`export`、`echo`(bash)、`ulimit` | ❌ 不 fork，直接改 shell 自身状态 |
| **外部命令** | `ls`、`grep`、`cat` | ✅ fork + exec 子进程 |

`cd` 必须是内置命令——因为它要改的是 **shell 进程自己的工作目录**；若 fork 子进程去 `chdir`，改的是子进程、父 shell 的工作目录根本不变。同理 `ulimit` 改的是 shell 的 rlimit。

## 3. `system()`：隐式的 `/bin/sh -c`

C 程序里执行 shell 命令用 `system()`，但它是**隐式 fork 三层**：

```c
system("ls -l");
/* 等价于： fork → execl("/bin/sh", "sh", "-c", "ls -l", NULL) → waitpid */
```

| 隐患 | 说明 |
|------|------|
| **阻塞** | `system()` 会 `wait`，调用线程卡住直到命令结束 |
| **信号干扰** | 期间 SIGINT/SIGQUIT 被屏蔽，SIGCHLD 被忽略，容易漏处理 |
| **命令注入** | 拼接字符串传给 `sh -c`，用户输入含 `;`/`$()` 即被解析执行 |
| **依赖 `/bin/sh`** | 不同系统 `/bin/sh` 指向不同（bash/dash），行为可能不同 |

所以：**生产代码里能用 `fork`+`exec`+`wait` 就别用 `system()`**，尤其别把不可信输入拼进 `system()` 字符串。

## 4. Shell 的作业控制

Shell 之所以能实现 `Ctrl+C` 杀整条管道、`Ctrl+Z` 挂起、`fg`/`bg` 切换，靠的是它作为「会话首领 + 前台进程组管理者」的身份：

- shell 启动时是**会话首领**，拥有控制终端（见 2.14）
- 每条管道命令放进**一个进程组**（见 2.13）
- `Ctrl+C` → 终端驱动给**前台进程组**发 SIGINT
- `Ctrl+Z` → 发 SIGTSTP，shell 把该组移到后台，打印 `[1]+ Stopped`
- `fg`/`bg` → shell 调 `tcsetpgrp()` 切换前台进程组 + 发 SIGCONT

所以「作业控制」本质是 shell 对**进程组 + 会话**这两套内核机制的编排，不是 shell 自创的魔法。

## 5. HFT / 嵌入式关联

| 现象 | 本节机制的兑现 |
|------|----------------|
| 启动编排 | shell 脚本负责拉起/重启交易进程、设置环境变量、ulimit（`RLIMIT_MEMLOCK`、`NOFILE`） |
| `system()` 慎用 | 交易热路径用 `fork`+`exec`+`wait` 或直接 IPC，避免 `system()` 的阻塞 + 注入风险 |
| 循环 fork 开销 | 脚本里 `for` 循环大量外部命令 = 大量 fork/exec，编排脚本应内置化或用 `xargs` 批量 |
| `/bin/sh` 可移植性 | 部署脚本写 `#!/bin/sh` 需守 POSIX 子集，别用 bash 专属语法（`[[ ]]`、数组） |
| 监控脚本 | `ps`/`top`/`grep` 组合做进程存活监控，`kill -0 pid` 探活 |

## 6. 代码示例

```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/wait.h>

/* 手动实现 system() 的安全替代：fork + exec + wait */
int main(int argc, char **argv) {
    if (argc < 2) { fprintf(stderr, "usage: %s <cmd> [args...]\n", argv[0]); return 1; }

    pid_t pid = fork();
    if (pid < 0) { perror("fork"); return 1; }

    if (pid == 0) {
        execvp(argv[1], &argv[1]);   /* 子进程 exec 成目标程序 */
        perror("execvp");
        _exit(127);
    }

    int status;
    waitpid(pid, &status, 0);        /* 父进程等待 */
    if (WIFEXITED(status))
        printf("exit=%d\n", WEXITSTATUS(status));
    else if (WIFSIGNALED(status))
        printf("killed by signal %d\n", WTERMSIG(status));
    return 0;
}
```

编译运行：`gcc -o run run.c && ./run ls -l`。对比 `system("ls -l")` 的差异——这里你可以精确控制「要不要等」「怎么处理信号」「传参不用拼字符串」，避免注入。

## 7. 衔接

- 下一节：[2.3 用户与组（Users and Groups）](2.3-users-and-groups.md)
- 关联：[2.13 进程组（Process Groups）](2.13-process-groups.md) · [2.14 会话（Sessions）](2.14-sessions.md)
- TLPI 深入：[Ch27 程序执行（exec）](../../chapter-27-program-execution/README.md) · [Ch34 进程组与会话](../../chapter-34-process-groups-sessions/README.md)

---

<details>
<summary>代码自测（Q&A）</summary>

**Q1：Shell 是内核的一部分吗？它的核心循环是什么？**
A：不是。Shell 是**普通用户态进程**（`/bin/bash` 等），核心循环是「读命令 → `fork` → `exec` → `wait`」四步。每执行一条外部命令就 fork 一个新进程、exec 成目标程序，父 shell 等待其结束。

**Q2：为什么 `cd` 必须是内置命令，不能是外部命令？**
A：因为 `cd` 要改的是 **shell 进程自己的当前工作目录**（`chdir`）。若做成外部命令，`fork` 出的子进程执行 `chdir` 只改子进程的工作目录，父 shell 的目录不变——命令返回后「什么都没发生」。同理 `export`（改环境）、`ulimit`（改 rlimit）都必须是内置命令。

**Q3：`system("ls -l")` 背后发生了什么？为什么说它危险？**
A：它隐式执行 `fork` → `execl("/bin/sh", "sh", "-c", "ls -l", NULL)` → `waitpid` 三步。危险在于：① 阻塞当前线程直到命令结束；② 若命令字符串由用户输入拼接，会被 `sh` 解析执行（命令注入，如 `"; rm -rf /"`）；③ 期间信号处理被扰动。生产代码应改用 `fork`+`exec`+`wait`。

**Q4：`Ctrl+C` 能杀掉一条管道里的所有命令，是 shell 的魔法吗？**
A：不是魔法，是「进程组」机制。shell 把 `ls | grep | wc` 这条管道里的进程放进**同一个进程组**（前台进程组），`Ctrl+C` 时终端驱动向该组广播 SIGINT，整组全收（见 2.13）。shell 只是编排者，机制来自内核的进程组/会话。

**Q5：脚本里 `for` 循环调几百次外部命令，为什么慢？怎么优化？**
A：每次外部命令都要 `fork` + `exec` 一个新进程（创建进程 + 加载程序 + 复制页表），几百次循环就是几百次进程创建开销。优化：① 用 shell 内置命令/参数展开替代循环外部命令；② 用 `xargs` 批量；③ 把循环逻辑写进单个 `awk`/`sed`/`python` 进程一次处理；④ 对性能敏感的编排改用编译型程序。

</details>
