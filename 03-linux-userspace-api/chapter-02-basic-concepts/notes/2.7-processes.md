# 2.7 进程（Process）

> 本章：[TLPI 第 02 章 — Fundamental Concepts](../README.md)
> 源码核验：Linux **v6.6**（`kernel/fork.c`、`include/linux/sched.h`）
> - 上一节：[2.6 程序（Programs）](2.6-programs.md)
> - 下一节：[2.8 内存映射（Memory Mappings）](2.8-memory-mappings.md)

---

## 本节讲什么

进程是「程序运行的实例」。本节回答三个问题：

1. 进程的基本身份是什么？PID/PPID 从哪来？
2. 进程怎么被创建？`fork()` 在内核里走的是怎样一条链路？
3. 进程有哪些状态（内核视角的 `TASK_*`），内存布局长什么样？

原书以「概念」为主；本节补上内核侧的实现坐标，把「进程」这个抽象落到 `task_struct` 上，为后面 Ch24（`fork` 深入）、Ch6（进程详解）打底。

---

## 1. 进程的基本属性

| 属性 | 含义 | 内核来源 |
|------|------|----------|
| **PID** | 进程唯一标识 | `kernel_clone()` 里 `pid_vnr(pid)` 取回（`kernel/fork.c:2921-2922`） |
| **PPID** | 父进程 ID | 复制自父进程 `task_struct` 的 `real_parent` |
| **TGID** | 线程组 ID（主线程 PID） | 同一进程的所有线程共享 TGID |
| **状态** | `TASK_RUNNING` / `TASK_INTERRUPTIBLE` 等 | `task_struct->__state`（`sched.h:86-131`） |

每个进程（`init`/`systemd` 的 PID=1 除外）都由父进程通过 `fork()` 创建。PID 1 是系统启动后第一个用户态进程；PID 0 是内核的 idle 进程（swapper），不参与用户态调度（见 05 内核 Ch3.10）。

## 2. 进程创建：`fork()` 的完整链路

`fork()` 不是「凭空造一个进程」，而是**复制当前进程**。内核侧的真实链路是：

```
用户态 fork()                    (glibc 封装)
      │
      ▼
SYSCALL_DEFINE0(fork)            kernel/fork.c:2991
      │  args.exit_signal = SIGCHLD   (:2995)
      ▼
kernel_clone(args)               kernel/fork.c:2868
      │  copy_process(NULL, trace, NUMA_NO_NODE, args)   (:2909)
      ▼
copy_process()                   kernel/fork.c:2240
      │  复制 task_struct / 信号 / 文件表 / 内存描述符(mm)
      ├─→ dup_task_struct      复制 task_struct + 内核栈
      ├─→ copy_mm              COW 复制页表（不复制物理页）
      ├─→ copy_files           fd 表（fork 默认共享表项、各持引用计数）
      └─→ copy_sighand        信号处理表
      ▼
返回 pid：父进程拿「子进程 PID」，子进程拿 0
```

**关键源码**（`kernel/fork.c:2991`）：

```c
SYSCALL_DEFINE0(fork)
{
#ifdef CONFIG_MMU
	struct kernel_clone_args args = {
		.exit_signal = SIGCHLD,   /* 子进程退出时给父进程发 SIGCHLD */
	};
	return kernel_clone(&args);
#else
	/* can not support in nommu mode */
	return -EINVAL;
#endif
}
```

`fork` 本质是 `clone` 的特例——`fork` 只设了 `exit_signal=SIGCHLD`，共享粒度（内存/文件表）由内核默认；`clone(SIGCHLD, 0, ...)` 等价于 `fork`。`pthread_create` 则走 `clone` 传 `CLONE_VM|CLONE_FS|CLONE_FILES|CLONE_SIGHAND` 实现**线程**（共享内存/文件表，见 Ch29）。

## 3. 进程状态机（内核视角）

内核用 `task_struct->__state` 的一个**位掩码**表示状态（`include/linux/sched.h:86-131`），`ps`/`top` 看到的单字母是它的投影：

| 内核宏 | 值 | `ps` 标志 | 含义 | HFT 场景 |
|--------|-----|-----------|------|----------|
| `TASK_RUNNING` | `0x00000000` | R | 正在运行或就绪排队 | 交易主循环占满 CPU |
| `TASK_INTERRUPTIBLE` | `0x00000001` | S | 可中断睡眠，等事件 | `recv` 等行情（可被信号打断） |
| `TASK_UNINTERRUPTIBLE` | `0x00000002` | D | 不可中断睡眠（通常等 I/O） | 等磁盘/网络驱动返回，**信号无法打断** |
| `__TASK_STOPPED` | `0x00000004` | T | 被 `SIGSTOP`/`SIGTSTP` 停止 | `Ctrl+Z` 挂起调试 |
| `__TASK_TRACED` | `0x00000008` | t | 被 ptrace 跟踪（gdb） | 调试器单步 |
| `EXIT_ZOMBIE` | `0x00000020` | Z | 已终止、父进程未 `wait` 收尸 | 僵尸堆积耗尽 PID |
| `EXIT_DEAD` | `0x00000010` | — | 已 `wait`、资源释放前最后态 | — |

**两个组合态**（源码里是位运算拼出来的）：

```c
#define TASK_KILLABLE  (TASK_WAKEKILL | TASK_UNINTERRUPTIBLE)   /* :116 可被致命信号杀死 */
#define TASK_STOPPED   (TASK_WAKEKILL | __TASK_STOPPED)          /* :117 */
```

> **D 状态是 HFT 排查重点**：进程卡在 D（`TASK_UNINTERRUPTIBLE`）时，连 `kill -9` 都打不断——它在等一个**内核侧**的 I/O 完成。交易机若发现 worker 卡 D 且 `ps` 的 `WCHAN` 指向磁盘/网络驱动，通常是存储或网卡驱动卡死，而非业务逻辑死锁。

## 4. 进程内存布局

一个进程的虚拟地址空间，从低到高：

```
高地址 ┌──────────────────────┐
       │      Stack（栈）      │  局部变量、函数帧，向下增长
       │        ...           │  （运行时动态）
       ├──────────────────────┤
       │        ↕             │  ← 中间留空，可 mmap
       ├──────────────────────┤
       │      Heap（堆）       │  malloc 分配，向上增长（brk/mmap）
       ├──────────────────────┤
       │   BSS（未初始化全局）  │  零填充，不占磁盘
       ├──────────────────────┤
       │   Data（已初始化全局） │  占磁盘、加载时拷贝进内存
       ├──────────────────────┤
       │   Text（代码，只读）   │  可共享、只读
低地址 └──────────────────────┘
```

| 段 | 内容 | 来源 | 共享性 |
|----|------|------|--------|
| Text | 机器指令 | 可执行文件 `.text` | 同一程序多进程共享（只读） |
| Data | 已初始化全局/静态变量 | `.data` | 每进程私有 |
| BSS | 未初始化全局/静态变量 | `.bss`（零填充） | 每进程私有 |
| Heap | `malloc` 分配 | `brk`/`mmap` 动态 | 每进程私有 |
| Stack | 局部变量、函数帧 | 内核启动时分配 | 每进程私有（线程各自独立栈） |

`fork` 后父子进程的 Text/Data/BSS 页**暂时共享**（COW），直到某一方写入才真正复制——所以「`fork` 复制整个内存」是**误区**，实际只复制页表。

## 5. HFT / 嵌入式关联

| 现象 | 本节机制的兑现 |
|------|----------------|
| 进程创建是启动延迟预算 | `fork` 要 `dup_task_struct`+`copy_mm` 复制页表，毫秒级；热路径应线程池预创建、避免运行时 fork |
| COW 首写触发缺页毛刺 | `fork` 后第一次写共享页触发 page fault + 物理页复制，是「父子都活」场景的隐藏延迟 |
| 僵尸进程耗尽 PID | 长期运行进程不 `wait` 收尸 → 僵尸堆积 → `pid_max` 耗尽 → 无法新建进程 |
| 卡 D 状态 | worker 卡 `TASK_UNINTERRUPTIBLE` 等 I/O，`kill -9` 无效，须查 `WCHAN` 定位驱动 |
| 多线程 = 共享地址空间 | `clone(CLONE_VM)` 让线程共享页表，省内存但引入数据竞争（见 Ch29-30） |

## 6. 代码示例

```c
#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/wait.h>
/* fork: 父进程拿到子进程 PID，子进程拿到 0 */
int main(void) {
    pid_t pid = fork();

    if (pid < 0) {
        perror("fork");
        return 1;
    } else if (pid == 0) {
        /* 子进程：pid == 0 */
        printf("child : my pid=%d, my ppid=%d\n", getpid(), getppid());
        return 42;                        /* 退出码 42 */
    } else {
        /* 父进程：pid == 子进程 PID */
        int status;
        printf("parent: child pid=%d\n", pid);
        wait(&status);                    /* 收尸，否则子进程变僵尸 */
        if (WIFEXITED(status))
            printf("parent: child exited with %d\n", WEXITSTATUS(status));
    }
    return 0;
}
```

编译运行：`gcc -o demo fork.c && ./demo`。注意 `wait()` 必不可少——不 `wait` 的子进程退出后停在 `EXIT_ZOMBIE`，直到父进程退出或显式回收。

## 7. 衔接

- 下一节：[2.8 内存映射（Memory Mappings）](2.8-memory-mappings.md) — 进程地址空间里的 `mmap` 区域
- 内核视角（对照 LKD）：
  - [`05/Ch3.2 进程描述符与任务结构`](../../../05-linux-kernel/chapter-03-process-management/notes/section-3.2-进程描述符与任务结构.md) — `task_struct` 全貌
  - [`05/Ch3.3 进程状态`](../../../05-linux-kernel/chapter-03-process-management/notes/section-3.3-进程状态.md) — `TASK_*` 状态机与调度
  - [`05/Ch3.4 进程创建与写时拷贝`](../../../05-linux-kernel/chapter-03-process-management/notes/section-3.4-进程创建与写时拷贝.md) — `copy_process` + COW 深挖
- TLPI 后续：[Ch24 进程创建](../../chapter-24-process-creation/README.md) · [Ch25 进程终止](../../chapter-25-process-termination/README.md) · [Ch26 监控子进程](../../chapter-26-monitoring-child-processes/README.md)

---

<details>
<summary>代码自测（Q&A）</summary>

**Q1：`fork()` 之后，父子进程谁先执行？为什么不能假设顺序？**
A：两者先后由调度器决定，不可预测。内核在 `kernel_clone()` 里 `wake_up_new_task()` 唤醒子进程后，父子都可能先被调度。依赖「父先执行」是经典竞态 bug——需要同步时用 `wait`/管道/信号显式约定。

**Q2：`fork()` 返回两次，父进程拿到 PID、子进程拿到 0，这个「0」是怎么来的？**
A：`copy_process()` 复制出的子进程，其返回路径（`ret_from_fork`）会强制让 `fork` 在子进程里返回 0。这是内核故意设计的「同一段代码、两个返回值」——父进程走 `kernel_clone` 的 `return nr`（子 PID），子进程首次被调度时从 `ret_from_fork` 起步、`rax` 清 0。

**Q3：为什么说「`fork` 复制整个内存」是错的？**
A：`copy_mm` 只复制页表（`dup_mm`→`dup_mmap`），父子共享物理页，页表项标只读。任一进程写入时触发写保护缺页，内核才分配新物理页 + 复制内容（COW）。所以 `fork` 成本与「内存大小」无关，只与「页表项数量」相关。

**Q4：进程和线程在内核里到底有什么区别？**
A：几乎只有「共享粒度」的区别——`fork` 默认共享最少，`pthread_create` 用 `clone(CLONE_VM|CLONE_FS|CLONE_FILES|CLONE_SIGHAND|CLONE_THREAD)` 让新 task 与父共享地址空间/文件表/信号表，此时它没有独立 PID（共享 TGID），就是一个线程。内核里两者都是 `task_struct`，没有独立的「线程」结构体。

**Q5：一个进程卡在 `ps` 的 D 状态，`kill -9` 也杀不掉，为什么？**
A：D = `TASK_UNINTERRUPTIBLE`，进程在等内核态 I/O 完成（磁盘/网络驱动），信号投递被延迟到 I/O 返回之后。`kill -9` 只是置一个 pending 信号，进程从 D 转回 R 之前不会处理。排查：`ps -o pid,stat,wchan` 看 `WCHAN` 指向哪个驱动，通常是存储或网卡问题，而非进程本身。

</details>
