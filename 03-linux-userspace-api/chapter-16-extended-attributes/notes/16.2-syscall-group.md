# 16.2 系统调用组

> 本章：[TLPI 第 16 章 — Extended Attributes](./README.md)
> - 上一节：[16.1 概念](./16.1-concepts.md)
> - 下一节：[16.3 `listxattr` 格式](./16.3-listxattr.md)

---

## 本节讲什么

#include <sys/xattr.h> int setxattr(const char *path, const char *name, const void *value, size_t size, int flags);

---

## 要点

```c
#include <sys/xattr.h>

int setxattr(const char *path, const char *name,
             const void *value, size_t size, int flags);
int lsetxattr(...);   /* 不跟随链接 */
int fsetxattr(int fd, ...);

ssize_t getxattr(const char *path, const char *name, void *value, size_t size);
ssize_t lgetxattr(...);
ssize_t fgetxattr(int fd, ...);

int removexattr(const char *path, const char *name);
int lremovexattr(...);
int fremovexattr(int fd, ...);

ssize_t listxattr(const char *path, char *list, size_t size);
ssize_t llistxattr(...);
ssize_t flistxattr(int fd, ...);
```

### `setxattr` flags

| flag | 行为 |
|------|------|
| `0`（默认） | 无则建，有则换 |
| `XATTR_CREATE` | 仅新建；已存在 → `EEXIST` |
| `XATTR_REPLACE` | 仅更新；不存在 → `ENODATA`（文档/历史上也称 ENOATTR） |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 系统调用组 在嵌入式系统的应用场景 |
| HFT | 系统调用组 对低延迟和性能的影响 |

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[16.1 概念](./16.1-concepts.md)
- 下一节：[16.3 `listxattr` 格式](./16.3-listxattr.md)
- 前置：**前置**：[Ch15 File Attributes](../chapter-15-file-attributes/notes.md)  
- 后置：**后置**：[Ch17 Access Control Lists](../chapter-17-access-control-lists/notes.md) · [Ch39 Capabilities](../chapter-39-capabilities/notes.md) · [Ch38 特权与安全](../chapter-38-secure-privileged/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
