# 16.1 概念

> 本章：[TLPI 第 16 章 — Extended Attributes](../README.md)
> - 上一节：本章首页
> - 下一节：[16.2 系统调用组](16.2-extended-attribute-implementation-detail.md)

---

## 本节讲什么

标准 inode 字段固定；**扩展属性（EA）** 附加任意 `key → value`（多为小元数据，非大文件内容替代）。 - 对象：普通文件、目录、链接、设备、FIFO 等（视 FS） - 支持：ext4 / XFS / Btrfs / tmpfs 等；**不保证所有 FS** → 处理 `ENOTSUP`

---

## 要点

标准 inode 字段固定；**扩展属性（EA）** 附加任意 `key → value`（多为小元数据，非大文件内容替代）。

- 对象：普通文件、目录、链接、设备、FIFO 等（视 FS）  
- 支持：ext4 / XFS / Btrfs / tmpfs 等；**不保证所有 FS** → 处理 `ENOTSUP`  
- 符号链接：默认跟随目标；操作链接本体用 `l*` 系列（FS 对 symlink EA 支持常很弱）

### 键名：`namespace.name`

| 命名空间 | 谁用 | 权限要点 |
|----------|------|----------|
| **`user.*`** | 普通自定义元数据 | 受文件 r/w 权限约束 |
| **`trusted.*`** | 管理员私有 | 仅 root（EUID） |
| **`security.*`** | SELinux、`security.capability` 等 | 多为内核/特权 |
| **`system.*`** | 内核用途；**ACL** 在此（`system.posix_acl_*`） | 普通用户一般不能乱改 |

> POSIX ACL 实质是存在 `system.*` 里的 EA → [Ch17](../../chapter-17-access-control-lists/notes)。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | EA 附加 `key→value` 小元数据，非所有 FS 支持须处理 `ENOTSUP`；ext4/tmpfs 可用，嵌入式固件可存自定义标签 |
| HFT | 用 `user.*` 存文件元数据标签（数据版本/来源）不污染文件内容；但 EA 是额外 syscall 有开销，热路径不碰 |

## 代码示例

```c
#include <sys/xattr.h>
#include <stdio.h>

int main(void) {
    const char *path = "/tmp/xattr_test.txt";

    /* 创建测试文件 */
    FILE *fp = fopen(path, "w");
    if (fp) { fprintf(fp, "test"); fclose(fp); }

    /* 设置扩展属性 (key-value 元数据) */
    if (setxattr(path, "user.comment", "hello world", 11, 0) == 0) {
        printf("设置 user.comment = hello world\n");
    } else {
        perror("setxattr");
    }

    /* 读取扩展属性 */
    char buf[256];
    ssize_t n = getxattr(path, "user.comment", buf, sizeof(buf) - 1);
    if (n > 0) {
        buf[n] = '\0';
        printf("读取 user.comment = %s\n", buf);
    }

    /* user.* 命名空间: 普通用户可用 (需有写权限) */
    /* trusted.* 命名空间: 需 root */
    /* security.* / system.*: 内核用 */
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 下一节：[16.2 系统调用组](16.2-extended-attribute-implementation-detail.md)
- 前置：**前置**：[Ch15 File Attributes](../../chapter-15-file-attributes/notes)  
- 后置：**后置**：[Ch17 Access Control Lists](../../chapter-17-access-control-lists/notes) · [Ch39 Capabilities](../../chapter-39-capabilities/notes) · [Ch38 特权与安全](../../chapter-38-secure-privileged/notes)
