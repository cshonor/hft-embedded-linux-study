# 16.3 `listxattr` 格式

> 本章：[TLPI 第 16 章 — Extended Attributes](../README.md)
> - 上一节：[16.2 系统调用组](16.2-extended-attribute-implementation-detail.md)
> - 下一节：[16.4 权限（Linux 要点）](16.4-summary.md)

---

## 本节讲什么

缓冲区为连续 `\0` 结尾的键名： user.foo\0user.bar\0 返回值 = 总字节数。范式：

---

## 要点

缓冲区为连续 `\0` 结尾的键名：

```text
user.foo\0user.bar\0
```

返回值 = 总字节数。范式：

1. `listxattr(path, NULL, 0)`（或 `size=0`）得长度  
2. 分配缓冲再读；`getxattr(..., size=0)` 同理得 value 长度  

`listxattr` **只有键**，value 需再 `getxattr`。

Demo：[`code/xattr_demo.c`](../code/xattr_demo.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `listxattr` 返回连续 `\0` 结尾键名（先 `size=0` 取长度再分配读），只有键无值，值需再 `getxattr` |
| HFT | 解析键名列表按 `strlen+1` 步进遍历；`size=0` 两段式先探长再分配，避免缓冲溢出 |

## 代码示例

```c
#include <sys/xattr.h>
#include <stdio.h>

int main(void) {
    const char *path = "/tmp/xattr_list_test.txt";

    /* 创建文件并设置多个 xattr */
    FILE *fp = fopen(path, "w");
    if (fp) { fprintf(fp, "test"); fclose(fp); }
    setxattr(path, "user.comment", "hello", 5, 0);
    setxattr(path, "user.author", "alice", 5, 0);
    setxattr(path, "user.version", "1", 1, 0);

    /* listxattr: 列出所有 xattr 名 (null 分隔) */
    char list[1024];
    ssize_t size = listxattr(path, list, sizeof(list));
    if (size > 0) {
        printf("扩展属性列表 (%zd bytes):\n", size);
        for (ssize_t i = 0; i < size; ) {
            printf("  %s\n", list + i);
            i += strlen(list + i) + 1;
        }
    } else {
        printf("无扩展属性\n");
    }
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[16.2 系统调用组](16.2-extended-attribute-implementation-detail.md)
- 下一节：[16.4 权限（Linux 要点）](16.4-summary.md)
- 前置：**前置**：[Ch15 File Attributes](../../chapter-15-file-attributes/notes)  
- 后置：**后置**：[Ch17 Access Control Lists](../../chapter-17-access-control-lists/notes) · [Ch39 Capabilities](../../chapter-39-capabilities/notes) · [Ch38 特权与安全](../../chapter-38-secure-privileged/notes)
