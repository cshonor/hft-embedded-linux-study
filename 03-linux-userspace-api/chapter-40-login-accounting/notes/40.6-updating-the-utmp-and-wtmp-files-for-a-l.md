# 40.6 现代注意

> 本章：[TLPI 第 40 章 — Login Accounting](../README.md)
> - 上一节：[40.5 更新](40.5-retrieving-the-login-name-getlogin.md)
> - 下一节：下一章+1:02d}-*/notes.md)

---

## 本节讲什么

systemd-logind 管会话；容器常无完整 utmp；wtmp 会被 logrotate 切段。也有 `utmpx` 接口（可移植变体）。 ---

---

## 要点

systemd-logind 管会话；容器常无完整 utmp；wtmp 会被 logrotate 切段。也有 `utmpx` 接口（可移植变体）。

| 命令 | 数据源 |
|------|--------|
| `who` | utmp + `USER_PROCESS` |
| `last` | wtmp |
| `w` | utmp + `/proc` 等 |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 现代注意 在嵌入式系统的应用场景 |
| HFT | 现代注意 对低延迟和性能的影响 |

## 代码示例

```c
#include <utmpx.h>
#include <stdio.h>
int main(void) {
    printf("登录记录: utmp(当前) / wtmp(历史)\n");
    printf("API: getutxent/pututxline/setutxent/endutxent\n");
    printf("查看: who / last / w\n");
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[40.5 更新](40.5-retrieving-the-login-name-getlogin.md)
- 前置：**前置**：[Ch39 Capabilities](../../chapter-39-capabilities/notes) · [Ch34 会话](../../chapter-34-process-groups-sessions/notes)  
- 后置：**后置**：[Ch41 共享库](../../chapter-41-shared-libraries/notes)
