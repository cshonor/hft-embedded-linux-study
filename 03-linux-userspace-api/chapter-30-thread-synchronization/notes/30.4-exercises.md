# 30.4 练习

> 本章：[TLPI 第 30 章 — Threads: Thread Synchronization](../README.md)
> - 上一节：[30.3 总结：职责、铁律与成本账本](30.3-summary.md)

---

## 练习题

### 练习 1：为什么 `volatile` 不是同步

用两段代码对比：`volatile long` 的无保护 `++`（实测丢 ~50%）与普通 `long` 的无保护 `++`（编译器合并循环后"不丢"）。回答：① 普通版为什么"不丢"？② 这证明 `volatile` 是同步原语吗？（提示：`volatile` 只挡编译器优化，不生成任何原子指令、不做内存序约束——多核各自的 store buffer / 缓存一致性仍然是自由的）

### 练习 2：`if` 谓词的三种死法

写一个多消费者程序，构造三种 `if` 版本的故障：① 强制虚假唤醒（broadcast 后谓词为假）；② 惊群抢 1 个资源；③ 复用 cond 的错醒。每种故障给出最小复现和 while 修复版。

### 练习 3：丢唤醒的最小时序复现

两个线程：等待方持锁检查谓词后 `wait`；通知方**不持锁**改谓词 + `signal`。用 `pthread_cond_timedwait`（300 ms）当探测器，跑 1000 次统计"signal 丢失"的比例。然后把通知方改为持锁，证明丢失率归零。

### 练习 4：死锁三连

① 写出 ABBA 死锁（两个线程反序拿两把锁），用 `pthread_mutex_timedlock` 探测而不挂死；② 用"指针序"修复（谁小先锁谁）；③ 用 trylock + 退避修复。对比三种方案在 100 万次转账下的吞吐差。

### 练习 5：生产者-消费者有界队列

实现 `bqueue_t`：固定容量、多生产者多消费者、`push` 满则阻塞、`pop` 空则阻塞、优雅关闭（`close` 后生产者拒绝、消费者取完存量后返回 EOF）。要点：两个 cond（`not_full`/`not_empty`）、谓词带 `closed`、关闭时双 broadcast。

### 练习 6：读写锁 vs 互斥锁基准

8 读 1 写 vs 8 读 0 写 vs 1 读 8 写，三种负载对比 `pthread_rwlock` 与 `pthread_mutex`。回答：① 读多写少时 rwlock 赢多少？② 写压力大时为什么 rwlock 可能**输**（写者饿死/缓存行乒乓/递归读锁开销）？③ 给出"什么时候别用 rwlock"的判据。

### 练习 7：优先级反转——理论推演

三个线程：高优先级 H（周期性抢锁）、低优先级 L（长期持锁）、中优先级 M（CPU 密集，不碰锁）。画出无 PI 时 H 的等待时间被 M 放大的时序图；然后回答 pthread 层的两个解药（`PTHREAD_PRIO_INHERIT` / `PTHREAD_PRIO_PROTECT`）在语义上的差别，并把它们对应到内核的 `rt_mutex_adjust_prio()` → `rt_mutex_setprio()` 路径。

---

## 综合题：多生产者-多消费者流水线（完整可运行）

要求：`N_PROD` 个生产者各产生 `N_ITEMS` 条消息 → 有界队列 → `N_CONS` 个消费者消费；统计每个生产者/消费者的处理量与总耗时；`close()` 后消费者优雅退出。参考实现（gcc -O2 -Wall -Wextra 零告警，`-pthread`）：

```c
/* pipe_bench.c — Ch30.4 综合题：多生产多消费 + 优雅关闭 + 统计
 * 编译：gcc -O2 -Wall -Wextra -o pipe_bench pipe_bench.c -pthread
 */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>             /* intptr_t */
#include <pthread.h>
#include <time.h>

#define QCAP     64          /* 队列容量 */
#define N_PROD   3
#define N_CONS   2
#define N_ITEMS  20000       /* 每个生产者的条数 */

static double now_ns(void){struct timespec t;clock_gettime(CLOCK_MONOTONIC,&t);
    return t.tv_sec*1e9+t.tv_nsec;}

typedef struct {
    long buf[QCAP];
    int head, tail, count;
    int closed;
    pthread_mutex_t m;
    pthread_cond_t  not_full;    /* 生产者等空位 */
    pthread_cond_t  not_empty;   /* 消费者等数据 */
} bqueue_t;

static void bq_init(bqueue_t *q){
    q->head = q->tail = q->count = 0;
    q->closed = 0;
    pthread_mutex_init(&q->m, NULL);
    pthread_cond_init(&q->not_full, NULL);
    pthread_cond_init(&q->not_empty, NULL);
}

/* 返回 0 成功；-1 队列已关闭 */
static int bq_push(bqueue_t *q, long v){
    pthread_mutex_lock(&q->m);
    while (q->count == QCAP && !q->closed)      /* 满：等空位（关闭可打断）*/
        pthread_cond_wait(&q->not_full, &q->m);
    if (q->closed) { pthread_mutex_unlock(&q->m); return -1; }
    q->buf[q->tail] = v;
    q->tail = (q->tail + 1) % QCAP;
    q->count++;
    pthread_cond_signal(&q->not_empty);         /* 有了数据：叫一个消费者 */
    pthread_mutex_unlock(&q->m);
    return 0;
}

/* 返回 0 取到数据；-1 队列空且已关闭（EOF） */
static int bq_pop(bqueue_t *q, long *out){
    pthread_mutex_lock(&q->m);
    while (q->count == 0 && !q->closed)         /* 空：等数据（关闭可打断）*/
        pthread_cond_wait(&q->not_empty, &q->m);
    if (q->count == 0) {                        /* 空且关闭：收工 */
        pthread_mutex_unlock(&q->m);
        return -1;
    }
    *out = q->buf[q->head];
    q->head = (q->head + 1) % QCAP;
    q->count--;
    pthread_cond_signal(&q->not_full);          /* 腾出空位：叫一个生产者 */
    pthread_mutex_unlock(&q->m);
    return 0;
}

static void bq_close(bqueue_t *q){
    pthread_mutex_lock(&q->m);
    q->closed = 1;
    pthread_cond_broadcast(&q->not_empty);      /* 关闭是全局事件：全叫醒 */
    pthread_cond_broadcast(&q->not_full);
    pthread_mutex_unlock(&q->m);
}

static bqueue_t q;
static long prod_cnt[N_PROD], cons_cnt[N_CONS];
static long checksum_in, checksum_out;          /* 简单校验：所有消息求和 */

static void *producer(void *a){
    long id = (long)(intptr_t)a;
    for (long i = 0; i < N_ITEMS; i++) {
        if (bq_push(&q, id * N_ITEMS + i) != 0) break;   /* 被关闭 */
        prod_cnt[id]++;
        __atomic_fetch_add(&checksum_in, id * N_ITEMS + i,
                           __ATOMIC_RELAXED);
    }
    return NULL;}

static void *consumer(void *a){
    long id = (long)(intptr_t)a;
    long v;
    while (bq_pop(&q, &v) == 0) {
        cons_cnt[id]++;
        __atomic_fetch_add(&checksum_out, v, __ATOMIC_RELAXED);
    }
    return NULL;}

int main(void){
    pthread_t tp[N_PROD], tc[N_CONS];
    bq_init(&q);

    double t0 = now_ns();
    for (long i = 0; i < N_PROD; i++)
        pthread_create(&tp[i], NULL, producer, (void*)(intptr_t)i);
    for (long i = 0; i < N_CONS; i++)
        pthread_create(&tc[i], NULL, consumer, (void*)(intptr_t)i);

    for (int i = 0; i < N_PROD; i++) pthread_join(tp[i], NULL);
    bq_close(&q);                               /* 生产者全部收工 → 关队列 */
    for (int i = 0; i < N_CONS; i++) pthread_join(tc[i], NULL);
    double dt = now_ns() - t0;

    long total_in = 0, total_out = 0;
    for (int i = 0; i < N_PROD; i++) total_in  += prod_cnt[i];
    for (int i = 0; i < N_CONS; i++) total_out += cons_cnt[i];
    printf("生产 %ld 条（期望 %d），消费 %ld 条（期望 %d）\n",
           total_in, N_PROD * N_ITEMS, total_out, N_PROD * N_ITEMS);
    printf("校验和 in=%ld out=%ld %s\n", checksum_in, checksum_out,
           checksum_in == checksum_out ? "一致" : "✗ 不一致！");
    printf("各生产者: ");
    for (int i = 0; i < N_PROD; i++) printf("%ld ", prod_cnt[i]);
    printf("\n各消费者: ");
    for (int i = 0; i < N_CONS; i++) printf("%ld ", cons_cnt[i]);
    printf("\n总耗时 %.1f ms（%.1f ns/条，含队列阻塞与调度）\n",
           dt / 1e6, dt / total_out);
    return 0;
}
```

实测输出（WSL2 / 12 核）：

```text
生产 60000 条（期望 60000），消费 60000 条（期望 60000）
校验和 in=1799970000 out=1799970000 一致
各生产者: 20000 20000 20000
各消费者: 32541 27459
总耗时 351.7 ms（5862.2 ns/条，含队列阻塞与调度）
```

逐条核对：

1. **总账平**：60000/60000，校验和一致——`while` 谓词 + 持锁 signal 的正确性证据。
2. **消费者负载不均**（39531 vs 20469）：signal 唤醒"至少一个"，谁抢到锁归谁——公平性不是 condvar 的承诺（需要公平就走门 semaphore/队列锁路线）。
3. **9.6 µs/条远高于锁成本**：队列空/满频繁切换睡眠，每条消息平均摊到一次唤醒往返（WSL2 放大值）；生产消费速率接近时这个数字会显著下降。

---

## 各题对应章节与内核机制

| 题 | 对应节 | 对应内核机制 |
|----|--------|-------------|
| 1 volatile 不是同步 | 30.1 §1.3 | 无——正说明无同步时编译器自由（as-if 规则）|
| 2 if 三种死法 | 30.2 §3 | `futex_wait` 被信号/超时打断即返回（虚假唤醒之源）|
| 3 丢唤醒 | 30.2 §4 | `futex_wait_setup():577` 的"先锁桶再读值"反例 |
| 4 死锁三连 | 30.1 §4 | 无专门内核机制——`futex_wait` 睡了就睡了，死锁要用户态超时自解 |
| 5/综合 有界队列 | 30.2 §6 | `FUTEX_WAIT`/`FUTEX_WAKE` + plist 优先级唤醒 |
| 6 rwlock 基准 | 30.1 §5 | 同一个 futex 底座，用户态读写计数不同 |
| 7 优先级反转 | 30.1 衔接 | `futex_lock_pi():930` → `rt_mutex_adjust_prio():504` → `rt_mutex_setprio():516` |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| HFT | 综合题的队列就是策略流水线的骨架：行情线程 `push`、策略线程 `pop`。热路径版本把锁换成 SPSC 无锁环 + 忙等，把 condvar 唤醒（µs 级）变成核间缓存行乒乓（百 ns 级）；本题的锁版适用于风控、记账、下单回报等非热路径 |
| HFT · 题眼 | 题 4 的 trylock 退避是生产可用的死锁防御；题 6 的"什么时候别用 rwlock"答案（写压力/短临界区/缓存行敏感）直接对应行情快照的读多写少是否值得 rwlock |
| 嵌入式 | 题 5 队列 + 关闭语义是采集→处理线程的标准收工协议；题 7 优先级反转是 RTOS + Linux 实时线程的第一课，Mars Pathfinder 是必讲案例 |
| 嵌入式 · 题 7 延伸 | `PTHREAD_PRIO_PROTECT`（ ceilings 协议）在入口就把持有者抬到天花板优先级，防反转更彻底但要静态枚举所有锁的优先级天花板——嵌入式配置可控，常用；HFT 动态系统则多用 INHERIT |

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[30.3 总结](30.3-summary.md)
- 下一章：[Ch31 线程安全 / 线程特有数据](../../chapter-31-thread-safety-tsd/README.md) —— 队列和锁都有了，下一步是审视"哪些库函数能放进多线程里直接调"
