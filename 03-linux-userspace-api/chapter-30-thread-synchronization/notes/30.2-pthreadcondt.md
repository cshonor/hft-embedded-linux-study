# 30.2 条件变量 `pthread_cond_t`

> 本章：[TLPI 第 30 章 — Threads: Thread Synchronization](./README.md)
> - 上一节：[30.1 互斥量 `pthread_mutex_t`](./30.1-pthreadmutext.md)
> - 下一节：[30.3 职责与易错](./30.3-section-30-3.md)

---

## 本节讲什么

mutex = 互斥访问；cond = **等某个状态成立**。cond **必须**与 mutex 一起用。 pthread_cond_t cond = PTHREAD_COND_INITIALIZER; pthread_cond_wait / timedwait / signal / broadcast

---

## 要点

mutex = 互斥访问；cond = **等某个状态成立**。cond **必须**与 mutex 一起用。

```c
pthread_cond_t cond = PTHREAD_COND_INITIALIZER;
pthread_cond_wait / timedwait / signal / broadcast
```

### `wait` 语义

原子：放锁 → 睡 → 醒后**再拿锁**再返回。避免「先 signal、后 wait」丢唤醒（在持锁改状态 + wait 的正确用法下）。

### 谓词必须 `while`

存在**虚假唤醒**；也可能 broadcast 后条件仍不满足：

```c
pthread_mutex_lock(&mtx);
while (!predicate)
    pthread_cond_wait(&cond, &mtx);
/* use shared state */
pthread_mutex_unlock(&mtx);
```

| | |
|--|--|
| `signal` | 唤醒至少一个等待者 |
| `broadcast` | 唤醒全部（多等待者 / 不确定谁该醒） |

### 生产者-消费者

mutex 保护缓冲与计数；空/满时 `wait`；变更后 `signal`/`broadcast`。

Demo：[`code/prod_condvar.c`](./code/prod_condvar.c)

---

---

## 与 C 语言学习的关系

Pthreads 是 C 语言多线程编程的标准。`pthread_create` 接收函数指针和 `void*` 参数——这是 C 语言回调模式的核心。线程返回 `void*` 需要 C 语言指针和类型转换的知识。注意 Pthreads 函数返回错误码（而非设置 errno），不能用 perror，要用 strerror。在 HFT 场景中，线程是低延迟并发的基础。
---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 条件变量 `pthread_cond_t` 在嵌入式系统的应用场景 |
| HFT | 条件变量 `pthread_cond_t` 对低延迟和性能的影响 |

---

## 常见陷阱

1. **mutex 忘记 unlock**

```c
pthread_mutex_lock(&m);
if (error) return;  /* 没 unlock! */
pthread_mutex_unlock(&m);
```

mutex lock 后在所有路径都必须 unlock。用 goto cleanup 模式确保清理。

2. **pthread 函数返回值处理错误**

```c
if (pthread_create(&t, NULL, func, arg) < 0)  /* 错误! */
    perror("pthread_create");
/* 正确：返回错误码，不是 -1 */
if ((err = pthread_create(&t, NULL, func, arg)) != 0)
    strerror(err);
```

Pthreads 函数返回错误码而不是设置 errno。不能用 perror，要用 strerror(err)。

3. **条件变量虚假唤醒**

```c
/* 错误：用 if */
if (ready) pthread_cond_wait(&cv, &m);
/* 正确：用 while */
while (!ready) pthread_cond_wait(&cv, &m);
```

pthread_cond_wait 可能被虚假唤醒。必须用 while 循环检查条件，不能用 if。

---

## 自测题

**题目 1（选择）：** `pthread_create` 失败时返回什么？
A. -1
B. 0
C. 错误码（正整数）
D. NULL

<details>
<summary>参考答案</summary>

C。Pthreads 函数返回错误码（如 EAGAIN）而不是 -1，也不设置 errno。用 strerror(err) 获取描述。

</details>

**题目 2（判断）：** 主线程退出时其他线程会继续运行。

<details>
<summary>参考答案</summary>

错误。主线程调用 exit() 或从 main() 返回时，所有线程都会被终止。只有 pthread_exit 才允许其他线程继续。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
void *thread_func(void *arg) {
    int x = *(int*)arg;
    return NULL;
}
int val = 42;
pthread_create(&t, NULL, thread_func, &val);
```

<details>
<summary>参考答案</summary>

如果 thread_func 在 val 被修改后才执行，会读到错误的值。应确保 arg 的生命周期覆盖线程执行期，或动态分配。

</details>

**题目 4（应用）：** 写代码创建线程、等待线程结束并获取返回值。

<details>
<summary>参考答案</summary>

```c
void *func(void *arg) { return (void*)42; }
pthread_t t;
pthread_create(&t, NULL, func, NULL);
void *retval;
pthread_join(t, &retval);
printf("result: %ld
", (long)retval);
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[30.1 互斥量 `pthread_mutex_t`](./30.1-pthreadmutext.md)
- 下一节：[30.3 职责与易错](./30.3-section-30-3.md)
- 前置：**前置**：[Ch29 线程导论](../chapter-29-threads-intro/notes.md)  
- 后置：**后置**：[Ch31 线程安全 / TLS](../chapter-31-thread-safety-tsd/notes.md)

---

## man 手册参考

- `man 2 wait`
- `man 2 signal`
