# 51.4 三件预览

> 本章：[TLPI 第 51 章 — Introduction to POSIX IPC](./README.md)
> - 上一节：[51.3 POSIX vs System V（核心表）](./51.3-system.md)
> - 下一节：[下一章](../chapter-{int(ch_num)+1:02d}-*/notes.md)

---

## 本节讲什么

- **mq**：优先级；通知/epoll；创建时定 maxmsg/msgsize - **sem**：命名（无关进程）/ 匿名（共享内存里，线程或亲缘） - **shm**：`shm_open` → `ftruncate` → `mmap`（常在 tmpfs `/dev/shm`）

---

## 要点

- **mq**：优先级；通知/epoll；创建时定 maxmsg/msgsize  
- **sem**：命名（无关进程）/ 匿名（共享内存里，线程或亲缘）  
- **shm**：`shm_open` → `ftruncate` → `mmap`（常在 tmpfs `/dev/shm`）

---

---

## 与 C 语言学习的关系

本节涉及的 `open()` 是 C 语言文件操作的基础系统调用。与 K&R 第 8 章的 `fopen()` 不同，`open()` 是 POSIX 系统调用而非 C 标准库函数。理解 `open()` 的 flags 参数（O_RDONLY、O_CREAT 等）是掌握 Unix 系统编程的第一步。对照 K&R 8.3 节的 `open/creat/close` 学习，注意 C 标准库 `fopen` 的 "r"/"w"/"a" 模式如何映射到 `open` 的 flags。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 三件预览 在嵌入式系统的应用场景 |
| HFT | 三件预览 对低延迟和性能的影响 |

---

## 常见陷阱

1. **MAP_FAILED vs NULL**

```c
void *p = mmap(NULL, size, PROT_READ|PROT_WRITE,
              MAP_PRIVATE|MAP_ANONYMOUS, -1, 0);
if (p == NULL) ...  /* 错误! */
if (p == MAP_FAILED) ...  /* 正确 */
```

mmap 失败返回 MAP_FAILED ((void*)-1)，不是 NULL。检查方式与 malloc 不同。

2. **munmap 忘记调用**

```c
void *p = mmap(NULL, 4096, ...);
/* 使用 p ... */
/* 没有 munmap → 内存泄漏 */
```

mmap 分配的内存不会自动释放。必须 munmap(p, size) 显式释放。

3. **信号处理中的 SIGSEGV**

```c
mmap(NULL, size, PROT_READ, MAP_PRIVATE|MAP_ANONYMOUS, -1, 0);
/* 写只读映射 → SIGSEGV */
```

对 PROT_READ 的映射写入会触发 SIGSEGV。确保保护属性和访问操作匹配。

---

## 自测题

**题目 1（选择）：** `mmap` 失败时返回什么？
A. NULL
B. -1
C. MAP_FAILED
D. 0

<details>
<summary>参考答案</summary>

C。mmap 失败返回 MAP_FAILED（定义为 (void *)-1），不是 NULL。这与 malloc 不同。

</details>

**题目 2（判断）：** `mmap` 映射的内存会在进程退出时自动释放。

<details>
<summary>参考答案</summary>

正确。进程退出时所有映射自动解除。但在长时间运行的程序中仍需手动 munmap。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
void *p = mmap(NULL, 4096, PROT_READ|PROT_WRITE,
               MAP_PRIVATE|MAP_ANONYMOUS, -1, 0);
memset(p, 0, 8192);
```

<details>
<summary>参考答案</summary>

写入了 8192 字节但只映射了 4096 字节。超出映射区域会触发 SIGSEGV。应确保操作不超过映射大小。

</details>

**题目 4（应用）：** 写代码用 mmap 分配匿名内存并初始化为 0。

<details>
<summary>参考答案</summary>

```c
void *p = mmap(NULL, 4096, PROT_READ|PROT_WRITE,
               MAP_PRIVATE|MAP_ANONYMOUS, -1, 0);
if (p == MAP_FAILED) { perror("mmap"); exit(1); }
memset(p, 0, 4096);
/* ... 使用 ... */
munmap(p, 4096);
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[51.3 POSIX vs System V（核心表）](./51.3-system.md)
- 前置：**前置**：[Ch45–48 SysV](../chapter-45-sysv-ipc-intro/notes.md) · [Ch49–50 mmap/VM](../chapter-49-memory-mappings/notes.md)  
- 后置：**后置**：[Ch52 mq](../chapter-52-posix-message-queues/notes.md) → [Ch53 sem](../chapter-53-posix-semaphores/notes.md) → [Ch54 shm](../chapter-54-posix-shared-memory/notes.md)

---

## man 手册参考

- `man 2 mmap`
