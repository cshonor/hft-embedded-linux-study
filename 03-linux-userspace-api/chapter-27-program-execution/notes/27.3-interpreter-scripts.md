# 27.3 `FD_CLOEXEC` / `O_CLOEXEC`

> 本章：[TLPI 第 27 章 — Program Execution](../README.md)
> - 上一节：[27.2 保留 vs 销毁](27.2-the-exec-library-functions.md)
> - 下一节：[27.4 PATH（`*p`）](27.4-file-descriptors-and-exec.md)

---

## 本节讲什么

打开时带 `O_CLOEXEC`（首选，减竞态）；或 `fcntl(F_SETFD, FD_CLOEXEC)`。 Demo：[`code/cloexec_demo.c`](../code/cloexec_demo.c) ---

---

## 要点

| | fork | exec |
|--|------|------|
| CLOEXEC fd | **不关** | **内核关掉** |

打开时带 `O_CLOEXEC`（首选，减竞态）；或 `fcntl(F_SETFD, FD_CLOEXEC)`。

Demo：[`code/cloexec_demo.c`](../code/cloexec_demo.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | O_CLOEXEC 首选（减竞态），exec 时内核关 fd 防泄漏 |
| HFT | O_CLOEXEC 防 fd 泄漏到 worker，安全 + 资源不失控（HFT 最佳实践） |

## 代码示例

```c
#include <fcntl.h>
#include <unistd.h>
#include <stdio.h>
int main(void) {
    /* O_CLOEXEC: exec 时自动关闭 fd, 防泄漏 */
    int fd = open("/tmp/cloexec.txt", O_CREAT | O_RDWR | O_CLOEXEC, 0644);

    pid_t pid = fork();
    if (pid == 0) {
        char *args[] = {"ls", "-la", "/proc/self/fd", NULL};
        execvp("ls", args);
        _exit(1);
    }
    wait(NULL);
    /* 没有 O_CLOEXEC 的 fd 在 exec 后仍然打开 (泄漏) */
    printf("O_CLOEXEC: exec 后 fd 自动关闭\n");
    close(fd);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[27.2 保留 vs 销毁](27.2-the-exec-library-functions.md)
- 下一节：[27.4 PATH（`*p`）](27.4-file-descriptors-and-exec.md)
- 前置：**前置**：[Ch24 fork](../../chapter-24-process-creation/notes) · [Ch25 终止](../../chapter-25-process-termination/notes) · [Ch26 wait](../../chapter-26-monitoring-child-processes/notes)  
- 后置：**后置**：[Ch28 fork/exec 细节](../../chapter-28-process-creation-exec-detail/notes) · [Ch9 凭证 / SUID](../../chapter-09-process-credentials/notes)（SUID 在 exec 时生效）
