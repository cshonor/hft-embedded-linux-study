# 12.1 The /proc File System `/proc` 文件系统

> 本章：[TLPI 第 12 章 — System and Process Information](../README.md)
> 源码核验：glibc **2.39**（`sysdeps/posix/gethostname.c`）+ Linux **v6.6**（`fs/proc/root.c`、`fs/proc/inode.c`、`fs/proc/self.c`、`fs/proc/array.c`、`fs/proc/base.c`、`fs/proc/meminfo.c`、`fs/proc/loadavg.c`、`fs/proc/uptime.c`、`fs/proc/task_mmu.c`、`fs/proc/proc_sysctl.c`、`include/uapi/linux/magic.h`）+ man-pages **6.19** `proc(5)` / `proc_sys(5)` / `sysinfo(2)` + CE 实测（gcc 13.3.0 / x86-64 / Ubuntu 24.04）
> - 上一节：[本章首页](../README.md)
> - 下一节：[12.2 System Identification: uname()](12.2-uname.md)

---

## 本节讲什么

TLPI 原书把 `/proc` 拆成**一个正节 + 三个子节**。本仓库沿原书节号把四者收在一篇里（子节用 `###` 编号，**不拆成多个文件**——同号小节会被 `build_all.py` 去重挤掉）：

| 官方节 | 原书页码 | 官方标题 | 本文位置 |
|--------|---------|---------|---------|
| **12.1** | p.219 | The `/proc` File System | §12.1 |
| **12.1.1** | p.220 | Obtaining Information about a Process: `/proc/PID` | §12.1.1 |
| **12.1.2** | p.225 | System Information under `/proc` | §12.1.2 |
| **12.1.3** | p.227 | Accessing `/proc` Files | §12.1.3 |

四个自编 demo 与四个节号**一一对应**，另加一个原书镜像：

| 文件 | 对应节 | 一句话 |
|------|--------|-------|
| `code/c12_1_proc_nodes.c` | §12.1 | 钉住三个反直觉事实：`f_blocks=0`、`st_size=0`、`/proc/self` 是符号链接 |
| `code/c12_1_proc_pid.c` | §12.1.1 | `/proc/PID` 下有什么 + 两个真实解析陷阱（`comm` 含 `)`、`status` 第 2 行不是 Pid） |
| `code/c12_1_proc_global.c` | §12.1.2 | 整机信息，并把 `/proc` 与 `sysinfo()` **并排对账** |
| `code/c12_1_proc_sys.c` | §12.1.3 | `/proc/sys` 写 knob 的完整细节（含「静默忽略」陷阱） |
| `code/procfs_pidmax.c` | §12.1.3 | **原书 Listing 12-1（p.228）**，逐字镜像 |

---

## §12.1 `/proc` 不是一个真的文件系统

man-pages 6.19 `proc(5)` 开篇第一句就是定调：

> The proc filesystem is a pseudo-filesystem which provides an interface to kernel data structures.

「伪文件系统（pseudo-filesystem）」这四个字决定了一切：它有 inode、有目录项、能被 `open`/`read`/`write`，但**没有任何后备存储块**。所以凡是「按普通文件推断」的做法都会错。

完整源码 [`code/c12_1_proc_nodes.c`](../code/c12_1_proc_nodes.c)：

```c
/* c12_1_proc_nodes.c — Ch12 §12.1（节首）：/proc 不是一个真的文件系统
 *
 * TLPI §12.1 第一件事不是「有哪些文件」，而是「这是一种什么文件系统」：
 * 它的目录项由内核**运行时生成**，没有后备存储块，所以拿它当一个普通文件
 * 来推断（比如按 st_size 分配缓冲、拿 statfs 算容量）一定会错。
 *
 * 本程序用三个反直觉点把它钉死：
 *   ① statfs("/proc").f_blocks == 0  —— 一个字节的存储都没有
 *   ② stat("/proc/version").st_size == 0，但实际能读出上百字节
 *   ③ /proc/self 不是目录，是内核给的**魔术符号链接**
 *
 * 编译： gcc -O0 -Wall -Wextra -o c12_1_proc_nodes c12_1_proc_nodes.c
 * 取材： man-pages 6.19 proc(5) 开篇（"The proc filesystem is a pseudo-filesystem
 *         which provides an interface to kernel data structures."）
 *       Linux v6.6 fs/proc/root.c:31-46（proc_fs_type = .name = "proc"）
 *                      fs/proc/inode.c（proc_reg_* 的 file_operations）
 *                      fs/proc/self.c:11-24（proc_self_get_link）
 *                      include/uapi/linux/magic.h（PROC_SUPER_MAGIC 0x9fa0）
 */
#define _GNU_SOURCE
#include <dirent.h>
#include <errno.h>
#include <fcntl.h>
#include <limits.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/stat.h>
#include <sys/statfs.h>
#include <unistd.h>

#define PROC_SUPER_MAGIC 0x9fa0
#define TMPFS_MAGIC      0x01021994

static const char *fsname(long t)
{
    switch ((unsigned long) t) {
    case PROC_SUPER_MAGIC: return "proc";
    case TMPFS_MAGIC:      return "tmpfs";
    case 0xef53:           return "ext2/3/4";
    case 0x6969:           return "nfs";
    case 0x73717368:       return "squashfs";
    default:               return "(其它)";
    }
}

static void show_statfs(const char *path)
{
    struct statfs sf;
    errno = 0;
    if (statfs(path, &sf) == -1) {
        printf("  statfs(%-8s) 失败 errno=%d(%s)\n", path, errno, strerror(errno));
        return;
    }
    printf("  statfs(%-8s) f_type=0x%-9lx (%-9s) f_bsize=%-6ld f_blocks=%-10llu f_files=%-10llu\n",
           path, (unsigned long) sf.f_type, fsname(sf.f_type), (long) sf.f_bsize,
           (unsigned long long) sf.f_blocks, (unsigned long long) sf.f_files);
}

int main(void)
{
    /* ---------- ① 与真的文件系统对比 ---------- */
    printf("== ① statfs 对比：/proc 的 f_blocks 是 0 ==\n");
    show_statfs("/");
    show_statfs("/proc");
    show_statfs("/dev");
    show_statfs("/tmp");
    printf("  → /proc 是伪文件系统：f_blocks/f_files 都是 0，"
           "「容量」这个概念对它不成立\n\n");

    /* ---------- ② st_size == 0 但不代表内容为空 ---------- */
    printf("== ② stat(\"/proc/version\")：st_size 是 0 ==\n");
    struct stat st;
    errno = 0;
    if (stat("/proc/version", &st) == -1) {
        printf("  stat 失败 errno=%d(%s)\n", errno, strerror(errno));
    } else {
        printf("  st_mode = 0%o  S_ISREG = %d  （普通文件，权限 0444）\n",
               st.st_mode, S_ISREG(st.st_mode) ? 1 : 0);
        printf("  st_size = %lld   st_blocks = %lld   st_nlink = %lu\n",
               (long long) st.st_size, (long long) st.st_blocks,
               (unsigned long) st.st_nlink);
        printf("  st_ino  = %llu（内核编的，不是磁盘上的 inode 号）\n",
               (unsigned long long) st.st_ino);

        /* 再看 offset 100 处有没有「数据块」：/proc 文件是顺序生成的流 */
        int fd = open("/proc/version", O_RDONLY);
        if (fd != -1) {
            char buf[512];
            ssize_t n = read(fd, buf, sizeof(buf) - 1);
            if (n == -1) {
                printf("  read 失败 errno=%d(%s)\n", errno, strerror(errno));
            } else {
                buf[n] = '\0';
                printf("  实际读出 %zd 字节（st_size 却报 0）\n", n);
                if (n && buf[n - 1] == '\n')
                    buf[n - 1] = '\0';
                printf("  内容: %s\n", buf);
            }
            close(fd);
        }
        printf("  ⚠️ 所以**不能**用 fstat 的 st_size 去 malloc 缓冲："
               "proc 文件的大小要「读到 EOF」才知道\n\n");

        /* 重复读同一个文件：内容每次重新生成 */
        printf("== ③ 同一个 /proc 文件反复打开，内容每次重新生成 ==\n");
        for (int i = 0; i < 2; i++) {
            /* 两次之间必须隔开一点时间，否则同一时钟节拍里读到的秒数一模一样，
               反而看不出「内容是按 read 时刻现算的」 */
            if (i == 1)
                usleep(200 * 1000);         /* 0.2 秒 */
            fd = open("/proc/uptime", O_RDONLY);
            if (fd != -1) {
                char b[128];
                ssize_t n = read(fd, b, sizeof(b) - 1);
                if (n > 0) {
                    b[n] = '\0';
                    printf("  第 %d 次 open(\"/proc/uptime\") + read → %s", i + 1, b);
                }
                close(fd);
            }
        }
        printf("  → 两次的秒数相差约 0.2（就是上面的 usleep）：/proc 文件并没有\n");
        printf("     「磁盘上那一份内容」，值由内核在 read 时按当前时刻现算\n\n");
    }

    /* ---------- ④ /proc/self 是魔术符号链接 ---------- */
    printf("== ④ /proc/self 不是目录，是内核造的符号链接 ==\n");
    char target[PATH_MAX];
    errno = 0;
    ssize_t n = readlink("/proc/self", target, sizeof(target) - 1);
    if (n == -1) {
        printf("  readlink(\"/proc/self\") 失败 errno=%d(%s)\n", errno, strerror(errno));
    } else {
        target[n] = '\0';
        printf("  readlink(\"/proc/self\") = \"%s\"   （本进程 getpid()=%d）\n",
               target, (int) getpid());
    }
    errno = 0;
    n = readlink("/proc/thread-self", target, sizeof(target) - 1);
    if (n != -1) {
        target[n] = '\0';
        printf("  readlink(\"/proc/thread-self\") = \"%s\"\n", target);
    }
    printf("  lstat 看它的类型：");
    struct stat lst;
    errno = 0;
    if (lstat("/proc/self", &lst) == 0)
        printf("S_ISLNK = %d\n", S_ISLNK(lst.st_mode) ? 1 : 0);
    else
        printf("lstat 失败 errno=%d(%s)\n", errno, strerror(errno));
    printf("  ⚠️ 但 open(\"/proc/self/status\") 照样能进 —— 链接由内核解析，"
           "不经过磁盘\n\n");

    /* ---------- ⑤ 顶层到底挂了些什么 ---------- */
    printf("== ⑤ /proc 顶层：数字目录 = 进程，其余 = 内核接口 ==\n");
    DIR *d = opendir("/proc");
    if (d == NULL) {
        printf("  opendir(\"/proc\") 失败 errno=%d(%s)\n", errno, strerror(errno));
        return EXIT_FAILURE;
    }
    struct dirent *e;
    int npids = 0, nothers = 0;
    while ((e = readdir(d)) != NULL) {
        if (e->d_name[0] == '.')
            continue;
        if (e->d_name[0] >= '0' && e->d_name[0] <= '9')
            npids++;
        else
            nothers++;
    }
    closedir(d);
    printf("  数字目录（进程）= %d 个\n", npids);
    printf("  其它条目（内核接口）= %d 个\n", nothers);
    printf("  ⚠️ 本容器里只有 %d 个进程，所以 /proc/PID 的样本极少；"
           "真机上是三位数\n", npids);
    return EXIT_SUCCESS;
}
```

编译运行：

```bash
gcc -O0 -Wall -Wextra -o c12_1_proc_nodes c12_1_proc_nodes.c && ./c12_1_proc_nodes
```

CE（gcc 13.3.0 / x86-64 / Ubuntu 24.04）实测：

```text
== ① statfs 对比：/proc 的 f_blocks 是 0 ==
  statfs(/       ) f_type=0x1021994   (tmpfs    ) f_bsize=4096   f_blocks=995363     f_files=995363
  statfs(/proc   ) f_type=0x9fa0      (proc     ) f_bsize=4096   f_blocks=0          f_files=0
  statfs(/dev    ) f_type=0x1021994   (tmpfs    ) f_bsize=4096   f_blocks=995363     f_files=995363
  statfs(/tmp    ) f_type=0x1021994   (tmpfs    ) f_bsize=4096   f_blocks=5120       f_files=100
  → /proc 是伪文件系统：f_blocks/f_files 都是 0，「容量」这个概念对它不成立

== ② stat("/proc/version")：st_size 是 0 ==
  st_mode = 0100444  S_ISREG = 1  （普通文件，权限 0444）
  st_size = 0   st_blocks = 0   st_nlink = 1
  st_ino  = 4026532026（内核编的，不是磁盘上的 inode 号）
  实际读出 219 字节（st_size 却报 0）
  ⚠️ 所以**不能**用 fstat 的 st_size 去 malloc 缓冲：proc 文件的大小要「读到 EOF」才知道
```

### 三个反直觉的事实

| 事实 | 实测 | 后果 |
|------|------|------|
| `statfs("/proc").f_blocks == 0`、`f_files == 0` | 见 ① | `/proc` 的「容量」无意义；`df /proc` 会报 0 |
| `stat("/proc/version").st_size == 0`，实际读出 **219** 字节 | 见 ② | **绝不能** `malloc(st_size)` 再 `read`——等于读 0 字节 |
| `/proc/self` 的 `lstat` 是 `S_ISLNK`，`readlink` 得到 `"2"` | 见 ③④ | 它是内核**现场生成**的魔术符号链接，不是真目录 |

`f_type = 0x9fa0` 就是 `PROC_SUPER_MAGIC`（`include/uapi/linux/magic.h`），与 tmpfs 的 `0x1021994`（`TMPFS_MAGIC`）不同——所以「`/proc` 是不是普通文件系统」用 `statfs` 一眼可判。

### 内容在 `read` 那一刻才生成

同一个 `/proc/uptime` 连读两次，两次的秒数**不同**（中间隔了 0.2 秒）：

```text
== ③ 同一个 /proc 文件反复打开，内容每次重新生成 ==
  第 1 次 open("/proc/uptime") + read → 306.48 252.19
  第 2 次 open("/proc/uptime") + read → 306.68 252.58
  → 两次的秒数相差约 0.2（就是上面的 usleep）：/proc 文件并没有
     「磁盘上那一份内容」，值由内核在 read 时按当前时刻现算

== ④ /proc/self 不是目录，是内核造的符号链接 ==
  readlink("/proc/self") = "2"   （本进程 getpid()=2）
  readlink("/proc/thread-self") = "2/task/2"
  lstat 看它的类型：S_ISLNK = 1
  ⚠️ 但 open("/proc/self/status") 照样能进 —— 链接由内核解析，不经过磁盘

== ⑤ /proc 顶层：数字目录 = 进程，其余 = 内核接口 ==
  数字目录（进程）= 2 个
  其它条目（内核接口）= 62 个
  ⚠️ 本容器里只有 2 个进程，所以 /proc/PID 的样本极少；真机上是三位数
```

> **⚠️ 这条实测是「一次运行有效」的**：`usleep(200ms)` 之后两次读数差约 0.2 秒，是**方向性**结论（内核现算），具体数字每次跑都不同。而 `/proc/uptime` 的**第二个数**不是 idle 比例——它是「所有 CPU 空闲时间**之和**」。本容器的两次采样是 `306.48 252.19`：开机 306 秒、累计空闲 252 秒（**比开机秒数还小**），说明这段时间机器不空闲。只有机器完全空闲时它才会 ≈ 2× 开机秒（2 核各闲满）。

`/proc/self` 的实现是 `fs/proc/self.c:11-24` 的 `proc_self_get_link()`：它在 `readlink` 时读当前 `task` 的 PID，再拼出字符串。所以同一个进程在不同时刻读、或不同进程读，答案不同（`/proc/thread-self` 更是拼成 `PID/task/TID`）。

---

## §12.1.1 `/proc/PID` 下有什么

`/proc/PID/` 是**每进程一份**的目录；`/proc/self` 是「我自己」那一份的别名（见上）。原书这个子节把主要文件逐个说了一遍，本仓库的 demo 把「说」变成「看」，并额外钉住两个**真的会咬人**的解析陷阱。

完整源码 [`code/c12_1_proc_pid.c`](../code/c12_1_proc_pid.c)：

```c
/* c12_1_proc_pid.c — Ch12 §12.1.1：/proc/PID/ 下到底有什么，以及两个真实的解析陷阱
 *
 * TLPI §12.1.1 讲「拿进程信息」，本程序做三件事：
 *   ① 把 /proc/self/ 下的主力文件逐个读一遍，看真实形态；
 *   ② 演示 **`/proc/PID/stat` 的 `(comm)` 字段解析陷阱**——
 *      用 prctl(PR_SET_NAME) 把进程名改成含 ')' 的串，再用「朴素 strchr(')')」
 *      和「第一个 '(' 配最后一个 ')'」两种切法对比，看差了多少字符；
 *   ③ 演示 **`/proc/PID/status` 的字段顺序不是直觉顺序**——
 *      第 2 行不是 Pid，是 Umask。
 *
 * 编译： gcc -O0 -Wall -Wextra -o c12_1_proc_pid c12_1_proc_pid.c
 * 取材： man-pages 6.19 proc(5) 的 /proc/pid/stat 与 /proc/pid/status 两节
 *       Linux v6.6 fs/proc/array.c（do_task_stat 的 `%d (%s) %c` 与 status 的
 *                        seq_printf 顺序：Name/Umask/State/Tgid/Ngid/Pid/PPid）
 *                      fs/proc/base.c（proc_pid_operations 注册了哪些文件）
 *                      fs/proc/task_mmu.c（maps 的 show_map_vma）
 */
#define _GNU_SOURCE
#include <ctype.h>
#include <dirent.h>
#include <errno.h>
#include <fcntl.h>
#include <limits.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/prctl.h>
#include <sys/stat.h>
#include <unistd.h>

/* 读一个文件的前 max 行；line_no 从 1 开始，打印时带序号 */
static void head_lines(const char *path, int max)
{
    FILE *fp = fopen(path, "r");
    if (fp == NULL) {
        printf("  %-26s 打开失败 errno=%d(%s)\n", path, errno, strerror(errno));
        return;
    }
    char line[512];
    int n = 0;
    while (n < max && fgets(line, sizeof(line), fp) != NULL) {
        size_t l = strlen(line);
        while (l && (line[l - 1] == '\n' || line[l - 1] == '\r'))
            line[--l] = '\0';
        printf("  %-26s [%d] %s\n", n == 0 ? path : "", n + 1, line);
        n++;
    }
    if (n == 0)
        printf("  %-26s (空)\n", path);
    fclose(fp);
}

/* 读整个小文件，把 NUL 显示成 '|' —— 看清 cmdline/environ 的分隔符 */
static void raw_nul(const char *path, int maxenv)
{
    int fd = open(path, O_RDONLY);
    if (fd == -1) {
        printf("  %-26s 打开失败 errno=%d(%s)\n", path, errno, strerror(errno));
        return;
    }
    static char buf[8192];
    ssize_t n = read(fd, buf, sizeof(buf) - 1);
    close(fd);
    if (n <= 0) {
        printf("  %-26s 读出 %zd 字节\n", path, n);
        return;
    }
    buf[n] = '\0';
    printf("  %-26s 共 %zd 字节，NUL 显示为 '|'：", path, n);
    for (ssize_t i = 0; i < n; i++)
        putchar(buf[i] == '\0' ? '|' : buf[i]);
    printf("\n");

    int cnt = 0;
    for (ssize_t i = 0; i < n; i++)
        if (buf[i] == '\0')
            cnt++;
    printf("  %-26s NUL 个数 = %d（= 条目数：每条都以 \\0 结尾）\n", "", cnt);
    if (maxenv > 0) {
        int shown = 0;
        for (ssize_t i = 0; i < n && shown < maxenv; i++) {
            if (i == 0 || buf[i - 1] == '\0') {
                printf("        %s\n", buf + i);
                shown++;
            }
        }
    }
}

static void show_link(const char *path)
{
    char t[PATH_MAX];
    errno = 0;
    ssize_t n = readlink(path, t, sizeof(t) - 1);
    if (n == -1)
        printf("  %-26s readlink 失败 errno=%d(%s)\n", path, errno, strerror(errno));
    else {
        t[n] = '\0';
        printf("  %-26s -> %s\n", path, t);
    }
}

int main(void)
{
    /* ---------- ① 主力文件逐个看 ---------- */
    printf("== ① /proc/self/status 的前 8 行（注意字段顺序）==\n");
    head_lines("/proc/self/status", 8);
    printf("  ⚠️ 第 2 行是 **Umask**，不是 Pid；真 Pid 在第 6 行。\n");
    printf("     顺序由内核 fs/proc/array.c 的 seq_printf 调用次序决定，\n");
    printf("     跨内核版本可能增删（Umask 就是较新内核才加的），别按行号取。\n\n");

    printf("== ② /proc/self/stat 是「单行 + 空格分隔」，第 2 字段是 (comm) ==\n");
    head_lines("/proc/self/stat", 1);
    printf("  ⚠️ comm 被一对括号包住，且 comm 里的空格**不算分隔符**。\n");
    printf("     注意那一片 0 里夹着 18446744073709551615 = RLIM_INFINITY，\n");
    printf("     说明这些字段有的按「无符号、-1 表示无限」解释。\n\n");

    printf("== ③ statm / comm / maps / ns ==\n");
    head_lines("/proc/self/statm", 1);
    head_lines("/proc/self/comm", 1);
    {
        FILE *fp = fopen("/proc/self/maps", "r");
        if (fp != NULL) {
            char line[512];
            int n = 0;
            while (fgets(line, sizeof(line), fp) != NULL) {
                if (n < 3) {
                    size_t l = strlen(line);
                    while (l && line[l - 1] == '\n')
                        line[--l] = '\0';
                    printf("  %-26s [%d] %s\n", n == 0 ? "/proc/self/maps" : "", n + 1, line);
                }
                n++;
            }
            printf("  %-26s 共 %d 行（每行 = 一段虚拟地址区间）\n", "", n);
            fclose(fp);
        }
    }
    printf("  /proc/self/ns/ 里的命名空间（都是符号链接，inode 号即 ns 身份）：\n");
    DIR *d = opendir("/proc/self/ns");
    if (d != NULL) {
        struct dirent *e;
        while ((e = readdir(d)) != NULL) {
            if (e->d_name[0] == '.')
                continue;
            char p[PATH_MAX];
            snprintf(p, sizeof(p), "/proc/self/ns/%s", e->d_name);
            show_link(p);
        }
        closedir(d);
    }
    printf("\n");

    printf("== ④ cmdline / environ：用 NUL 分隔，不是空格也不是换行 ==\n");
    raw_nul("/proc/self/cmdline", 0);
    raw_nul("/proc/self/environ", 6);
    printf("  ⚠️ 所以直接 fgets 只能拿到「第一条」；要全读得按 \\0 切。\n");
    printf("     environ 的条数每次运行都不同（CE 容器实测 7 条）。\n\n");

    printf("== ⑤ cwd / exe / root / fd/ 都是符号链接 ==\n");
    show_link("/proc/self/cwd");
    show_link("/proc/self/exe");
    show_link("/proc/self/root");
    d = opendir("/proc/self/fd");
    if (d != NULL) {
        struct dirent *e;
        while ((e = readdir(d)) != NULL) {
            if (e->d_name[0] == '.')
                continue;
            char p[PATH_MAX];
            snprintf(p, sizeof(p), "/proc/self/fd/%s", e->d_name);
            show_link(p);
        }
        closedir(d);
    }
    printf("  ⚠️ 遍历 fd/ 时，`opendir` 自己的那个 fd 也在里面，\n");
    printf("     而它的目标是 \"/proc/<pid>/fd\" —— 于是你会看到一条指向自己的链接。\n\n");

    /* ---------- ⑥ comm 含 ')' 的解析陷阱 ---------- */
    printf("== ⑥ 陷阱：comm 里含 ')' 时，朴素解析切错位置 ==\n");
    printf("  prctl(PR_SET_NAME, \"a)b\") = %d",
           prctl(PR_SET_NAME, "a)b", 0, 0, 0));
    head_lines("/proc/self/comm", 1);

    FILE *fp = fopen("/proc/self/stat", "r");
    if (fp == NULL)
        return EXIT_FAILURE;
    static char stat_line[2048];
    if (fgets(stat_line, sizeof(stat_line), fp) == NULL) {
        fclose(fp);
        return EXIT_FAILURE;
    }
    fclose(fp);
    size_t l = strlen(stat_line);
    while (l && stat_line[l - 1] == '\n')
        stat_line[--l] = '\0';
    printf("  /proc/self/stat = %s\n", stat_line);

    char *lp = strchr(stat_line, '(');          /* 第一个 '(' */
    char *last = strrchr(stat_line, ')');       /* 最后一个 ')' */
    char *first = strchr(stat_line, ')');       /* 第一个 ')' —— 朴素写法的错处 */
    if (lp != NULL && last != NULL && first != NULL) {
        printf("  正确切法（第一个 '(' 配最后一个 ')'）：%.*s\n",
               (int) (last - lp + 1), lp);
        printf("  朴素切法（第一个 '(' 配第一个 ')'）：(%.*s)\n",
               (int) (first - lp - 1), lp + 1);
        printf("  → 朴素算法丢掉 %d 个字符，后面所有字段的编号**全部错位**\n",
               (int) (last - first));
    }
    printf("  ⚠️ 这就是「别自己按空格/括号切 /proc/PID/stat」的原因。\n");
    printf("     要进程名就用 /proc/PID/comm；要完整字段建议 man 5 proc 逐字段数，\n");
    printf("     或直接用现成库（libprocps / osquery），别手搓。\n");
    return EXIT_SUCCESS;
}
```

CE 实测：

```text
== ① /proc/self/status 的前 8 行（注意字段顺序）==
  /proc/self/status          [1] Name:	output.s
                             [2] Umask:	0002
                             [3] State:	R (running)
                             [4] Tgid:	2
                             [5] Ngid:	0
                             [6] Pid:	2
                             [7] PPid:	1
                             [8] TracerPid:	0
  ⚠️ 第 2 行是 **Umask**，不是 Pid；真 Pid 在第 6 行。
     顺序由内核 fs/proc/array.c 的 seq_printf 调用次序决定，
     跨内核版本可能增删（Umask 就是较新内核才加的），别按行号取。

== ② /proc/self/stat 是「单行 + 空格分隔」，第 2 字段是 (comm) ==
  /proc/self/stat            [1] 2 (output.s) R 1 2 2 0 -1 4194304 68 0 0 0 0 0 0 0 39 19 1 0 4904108 2760704 346 18446744073709551615 4198400 4201317 140721982051840 0 0 0 0 0 0 0 0 0 17 0 0 0 0 0 0 4210136 4210864 233832448 140721982054115 140721982054126 140721982054126 140721982054381 0
  ⚠️ comm 被一对括号包住，且 comm 里的空格**不算分隔符**。
     注意那一片 0 里夹着 18446744073709551615 = RLIM_INFINITY，
     说明这些字段有的按「无符号、-1 表示无限」解释。
```

### 文件清单与本仓库实测形态

| 文件 | 实测 | 备注 |
|------|------|------|
| `status` | **61** 个 `key:` 行，`[1]Name [2]Umask [3]State [4]Tgid [5]Ngid [6]Pid [7]PPid [8]TracerPid [9…]Uid` | 人类可读；**顺序会变**，必须按 key 查，不能按行号 |
| `stat` | 单行、空格分隔；`comm` 被一对括号包住 | `ps` 的数据源；`')'` 之后约 **50** 个字段 |
| `statm` | 一行 7 个数字：`674 387 362 1 0 91 0` | 单位是**页** |
| `comm` | `output.s` | 单独一个文件给出进程名（最长 15 字符），**不用自己切 stat** |
| `maps` | 24 行；每行 = 一段虚拟地址区间 | 段来源见下文 |
| `ns/` | 10 个符号链接，形如 `net:[4026532377]` | inode 号即命名空间身份 |
| `cmdline` / `environ` | `\0` 分隔 | 见下文 |
| `cwd` / `exe` / `root` / `fd/` | 符号链接 | 见下文 |

```text
== ③ statm / comm / maps / ns ==
  /proc/self/statm           [1] 674 387 362 1 0 91 0
  /proc/self/comm            [1] output.s
  /proc/self/maps            [1] 00400000-00401000 r--p 00000000 103:02 358920                            /app/output.s
                             [2] 00401000-00402000 r-xp 00001000 103:02 358920                            /app/output.s
                             [3] 00402000-00403000 r--p 00002000 103:02 358920                            /app/output.s
                             共 24 行（每行 = 一段虚拟地址区间）
  /proc/self/ns/ 里的命名空间（都是符号链接，inode 号即 ns 身份）：
  /proc/self/ns/net          -> net:[4026532377]
  /proc/self/ns/uts          -> uts:[4026532372]
  /proc/self/ns/ipc          -> ipc:[4026532374]
  /proc/self/ns/pid          -> pid:[4026532375]
  /proc/self/ns/pid_for_children -> pid:[4026532375]
  /proc/self/ns/user         -> user:[4026532370]
  /proc/self/ns/mnt          -> mnt:[4026532371]
  /proc/self/ns/cgroup       -> cgroup:[4026532376]
  /proc/self/ns/time         -> time:[4026531834]
  /proc/self/ns/time_for_children -> time:[4026531834]
```

> `ns/time` 与 `ns/time_for_children` 的 inode 是 **4026531834**，而其它命名空间都是 `402653226x` 段——因为 **time namespace 是整个容器共享的那一个**（不是每容器新建）。这正是「用 `stat` 比 `ns` inode 判断两个进程是否同一命名空间」的用法：inode 相同 = 同一个 ns 实例。
>
> `maps` 的 `103:02` 是 `st_dev` 的「主:次」设备号，`358920` 是**该文件系统的 inode 号**——所以 `maps` 可以用来反查「这段内存映射的是哪个文件」。

### 陷阱一：`cmdline` / `environ` 用 `\0` 分隔

```text
== ④ cmdline / environ：用 NUL 分隔，不是空格也不是换行 ==
  /proc/self/cmdline         共 11 字节，NUL 显示为 '|'：./output.s|
                             NUL 个数 = 1（= 条目数：每条都以 \0 结尾）
  /proc/self/environ         共 255 字节，NUL 显示为 '|'：LD_LIBRARY_PATH=/opt/compiler-explorer/gcc-13.3.0/lib:/opt/compiler-explorer/gcc-13.3.0/lib32:/opt/compiler-explorer/gcc-13.3.0/lib64|ASAN_OPTIONS=color=always|UBSAN_OPTIONS=color=always|MSAN_OPTIONS=color=always|LSAN_OPTIONS=color=always|PATH=|HOME=/app|
                             NUL 个数 = 7（= 条目数：每条都以 \0 结尾）
  ⚠️ 所以直接 fgets 只能拿到「第一条」；要全读得按 \0 切。
     environ 的条数每次运行都不同（CE 容器实测 7 条）。
```

`fgets` 会在**第一个** `\0` 处停下（它按 `\n` 找行尾，但字符串函数按 `\0` 找结尾）——于是 `cmdline` 用 `fgets` 读只会得到 argv[0]。正确做法是 `read` 之后按字节扫 `\0`。

### 陷阱二：`stat` 的 `comm` 定界规则

这是本子节**最容易出错**的一处。用 `prctl(PR_SET_NAME)` 把进程名改成含 `)` 的 `"a)b"`，再对比两种切法：

```text
== ⑥ 陷阱：comm 里含 ')' 时，朴素解析切错位置 ==
  prctl(PR_SET_NAME, "a)b") = 0  /proc/self/comm            [1] a)b
  /proc/self/stat = 2 (a)b) R 1 2 2 0 -1 4194304 71 0 0 0 0 0 0 0 39 19 1 0 4904108 2760704 378 18446744073709551615 4198400 4201317 140721982051840 0 0 0 0 0 0 0 0 0 17 0 0 0 0 0 0 4210136 4210864 233832448 140721982054115 140721982054126 140721982054126 140721982054381 0
  正确切法（第一个 '(' 配最后一个 ')'）：(a)b)
  朴素切法（第一个 '(' 配第一个 ')'）：(a)
  → 朴素算法丢掉 2 个字符，后面所有字段的编号**全部错位**
  ⚠️ 这就是「别自己按空格/括号切 /proc/PID/stat」的原因。
     要进程名就用 /proc/PID/comm；要完整字段建议 man 5 proc 逐字段数，
     或直接用现成库（libprocps / osquery），别手搓。
```

规则来自 `proc(5)`：`comm` 可以含空格甚至括号，所以字段分隔符**不是**按空格切，而是靠**第一个 `(` 配最后一个 `)`** 定界。定界之后，`)` 后面的部分才按空格切出第 3、4、5… 个字段。

### 陷阱三：`cwd` / `exe` / `fd/` 是符号链接，不是文件

```text
== ⑤ cwd / exe / root / fd/ 都是符号链接 ==
  /proc/self/cwd             -> /app
  /proc/self/exe             -> /app/output.s
  /proc/self/root            -> /
  /proc/self/fd/0            -> socket:[1426492]
  /proc/self/fd/1            -> socket:[1426494]
  /proc/self/fd/2            -> socket:[1426496]
  /proc/self/fd/3            -> /proc/2/fd
  ⚠️ 遍历 fd/ 时，`opendir` 自己的那个 fd 也在里面，
     而它的目标是 "/proc/<pid>/fd" —— 于是你会看到一条指向自己的链接。
```

`exe` 指向可执行文件——**即使它已经被 `unlink`**（文件已删但进程还在），这个链接依然可用。这既是「找出被删但仍占空间的文件」的手段（`ls -l /proc/*/exe | grep deleted`），也是 `fuser` / `lsof` 能工作的原理。这也解释了原书习题 12-1 解答（`procfs_user_exe.c`）的名字：**通过 `/proc/PID/exe` 判断进程在跑什么程序**。

`fd/` 的完整用法（含四种 target 的判据）见本文 §12.1.3 末尾的延伸与 [12.4 Exercises](12.4-exercises.md)。

---

## §12.1.2 整机信息

`/proc` 下**非数字**的那些条目是内核的全局接口。它们和 `sysinfo(2)` 常常给出同一批数字——因为内核侧本来就是同一个函数在填。

完整源码 [`code/c12_1_proc_global.c`](../code/c12_1_proc_global.c)：

```c
/* c12_1_proc_global.c — Ch12 §12.1.2：整机信息，以及「/proc vs sysinfo」的交叉验证
 *
 * TLPI §12.1.2 说：整机信息既可以从 /proc 的全局文件读，也可以走 sysinfo()。
 * 本程序把两条路**并排打出来对账**，并回答三个工程问题：
 *   ① 同样的数字，两条路一致吗？（本容器实测：一致）
 *   ② 为什么老代码偏爱 sysinfo()？（一次系统调用 vs 若干次 open/read/解析）
 *   ③ 为什么又有人说「优先读 /proc/meminfo」？（meminfo 字段多得多，
 *      而且 sysinfo 的 totalram 在 32 位平台会溢出 —— 见 mem_unit 的作用）
 *
 * 编译： gcc -O0 -Wall -Wextra -o c12_1_proc_global c12_1_proc_global.c
 * 取材： man-pages 6.19 proc(5)（/proc/meminfo、/proc/loadavg、/proc/uptime、
 *         /proc/stat、/proc/cpuinfo 各节）+ sysinfo(2) 全文
 *       Linux v6.6 fs/proc/meminfo.c（seq_file 逐项输出）
 *                      fs/proc/loadavg.c（get_avenrun + LOAD_INT/LOAD_FRAC）
 *                      fs/proc/uptime.c（ktime_get_boottime_ts64 现算）
 *                      fs/proc/array.c（/proc/stat 的 cpu 行）
 *                      include/uapi/linux/sysinfo.h（struct sysinfo / SI_LOAD_SHIFT）
 */
#define _GNU_SOURCE
#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/sysinfo.h>
#include <sys/utsname.h>
#include <unistd.h>

/* 只取第一行，去掉行尾换行 */
static int first_line(const char *path, char *out, size_t cap)
{
    FILE *fp = fopen(path, "r");
    if (fp == NULL) {
        snprintf(out, cap, "<打开失败 errno=%d(%s)>", errno, strerror(errno));
        return -1;
    }
    if (fgets(out, (int) cap, fp) == NULL) {
        fclose(fp);
        snprintf(out, cap, "<空>");
        return -1;
    }
    fclose(fp);
    size_t l = strlen(out);
    while (l && (out[l - 1] == '\n' || out[l - 1] == '\r'))
        out[--l] = '\0';
    return 0;
}

/* 在 "Key:  <数字> kB" 形式的文件里找一个键 */
static long long lookup_kb(const char *path, const char *key, int *found)
{
    FILE *fp = fopen(path, "r");
    *found = 0;
    if (fp == NULL)
        return -1;
    char line[512];
    size_t klen = strlen(key);
    while (fgets(line, sizeof(line), fp) != NULL) {
        if (strncmp(line, key, klen) == 0 && line[klen] == ':') {
            long long v = 0;
            if (sscanf(line + klen + 1, "%lld", &v) == 1) {
                *found = 1;
                fclose(fp);
                return v;
            }
        }
    }
    fclose(fp);
    return -1;
}

int main(void)
{
    char line[512];

    /* ---------- ① 纯文本型：version / uptime / loadavg ---------- */
    printf("== ① 几个纯文本全局文件（原样一行）==\n");
    if (first_line("/proc/version", line, sizeof(line)) == 0)
        printf("  /proc/version : %s\n", line);
    if (first_line("/proc/uptime", line, sizeof(line)) == 0)
        printf("  /proc/uptime  : %s\n", line);
    if (first_line("/proc/loadavg", line, sizeof(line)) == 0)
        printf("  /proc/loadavg : %s\n", line);
    printf("  ⚠️ /proc/uptime 有**两个**数：第一个是开机秒数，第二个是「所有 CPU 的\n");
    printf("     空闲时间总和」—— 双核机器上它会约等于 2× 开机秒。它**不是** idle 比例。\n");
    printf("  ⚠️ /proc/loadavg 的第 4 段是 \"running/total\"，第 5 段是「最近分配的 PID」，\n");
    printf("     都不是负载。\n\n");

    /* ---------- ② 与 sysinfo() 对账 ---------- */
    printf("== ② sysinfo() 与 /proc 的对账 ==\n");
    struct sysinfo si;
    errno = 0;
    if (sysinfo(&si) == -1) {
        printf("  sysinfo 失败 errno=%d(%s)\n", errno, strerror(errno));
        return EXIT_FAILURE;
    }
    printf("  sysinfo: uptime=%lds  loads=[%lu %lu %lu]  mem_unit=%u  procs=%u\n",
           (long) si.uptime, si.loads[0], si.loads[1], si.loads[2],
           si.mem_unit, si.procs);
    printf("  sysinfo: totalram=%lu  freeram=%lu  sharedram=%lu  bufferram=%lu\n",
           si.totalram, si.freeram, si.sharedram, si.bufferram);
    printf("  sysinfo: totalswap=%lu  freeswap=%lu\n", si.totalswap, si.freeswap);

    unsigned long long total_bytes = (unsigned long long) si.totalram * si.mem_unit;
    unsigned long long free_bytes = (unsigned long long) si.freeram * si.mem_unit;
    printf("  → 字节数 = 字段 × mem_unit（本机 mem_unit=%u，所以字段就是字节数）\n",
           si.mem_unit);
    printf("    totalram × mem_unit = %llu 字节 = %llu MiB\n",
           total_bytes, total_bytes / 1024 / 1024);
    printf("    freeram  × mem_unit = %llu 字节 = %llu MiB\n",
           free_bytes, free_bytes / 1024 / 1024);

    int found = 0;
    long long memtotal = lookup_kb("/proc/meminfo", "MemTotal", &found);
    long long memfree = lookup_kb("/proc/meminfo", "MemFree", &found);
    printf("  /proc/meminfo : MemTotal=%lld kB  MemFree=%lld kB\n", memtotal, memfree);
    printf("  → MemTotal(kB) × 1024 = %lld 字节  vs  sysinfo 的 %llu 字节  → %s\n",
           memtotal * 1024, total_bytes,
           (memtotal > 0 && (unsigned long long) memtotal * 1024 == total_bytes) ? "一致" : "不一致");
    printf("  ⚠️ 但 **MemFree ≠ freeram 的口径**：meminfo 还有 MemAvailable / Buffers /\n");
    printf("     Cached / Slab 等十几项，sysinfo 只有 freeram。要看内存压力必须读\n");
    printf("     /proc/meminfo；只想「大概有多少」才用 sysinfo。\n\n");

    printf("== ③ 负载：sysinfo 是定点数，要除以 65536 ==\n");
    printf("  sysinfo.loads[0] = %lu  →  %lu/65536.0 = %.4f\n",
           si.loads[0], si.loads[0], si.loads[0] / 65536.0);
    if (first_line("/proc/loadavg", line, sizeof(line)) == 0) {
        double a = 0, b = 0, c = 0;
        sscanf(line, "%lf %lf %lf", &a, &b, &c);
        printf("  /proc/loadavg   = %.2f %.2f %.2f\n", a, b, c);
        printf("  → 两边对得上（差在 /proc 只给两位小数）\n");
    }
    printf("  ⚠️ 老内核里 loads[] 的定标不是 65536（是 1<<SI_LOAD_SHIFT，早期还有\n");
    printf("     直接给整数的版本）→ 跨内核解析要小心。\n\n");

    /* ---------- ④ CPU：三条路 ---------- */
    printf("== ④ 「几个 CPU」有三条路 ==\n");
    errno = 0;
    int np = get_nprocs();
    int npc = get_nprocs_conf();
    int err_after_nprocs = errno;
    printf("  get_nprocs()      = %d    get_nprocs_conf() = %d\n", np, npc);
    printf("  sysconf(_SC_NPROCESSORS_ONLN)   = %ld\n", sysconf(_SC_NPROCESSORS_ONLN));
    printf("  sysconf(_SC_NPROCESSORS_CONF)   = %ld\n", sysconf(_SC_NPROCESSORS_CONF));
    printf("  get_nprocs() 调用后 errno = %d（%s）← 这是**脏 errno**：\n",
           err_after_nprocs, strerror(err_after_nprocs));
    printf("     函数调用是**成功的**，这个值来自它内部的第一条尝试 ——\n");
    printf("     glibc 先读 /sys/devices/system/cpu/online（本容器没挂 /sys，\n");
    printf("     该文件不存在 → ENOENT 留在 errno），失败后才退到数 /proc/stat。\n");
    printf("     所以判错必须自己先 errno = 0、调用后立即取；别把 errno 当返回值用。\n");
    if (first_line("/proc/stat", line, sizeof(line)) == 0) {
        printf("  /proc/stat 第一行: %s\n", line);
        printf("  → 第一个字段 \"cpu\" 是**所有核的汇总**，之后才是 cpu0 / cpu1 …\n");
    }
    if (first_line("/proc/cpuinfo", line, sizeof(line)) == 0)
        printf("  /proc/cpuinfo 第一行: %s   （processor : N，每个逻辑核一段）\n", line);
    printf("\n");

    /* ---------- ⑤ hostname / osrelease 与 POSIX 接口对账 ---------- */
    printf("== ⑤ 内核参数的文本接口 vs POSIX 接口 ==\n");
    char host_kernel[256] = "", host_posix[256] = "";
    if (first_line("/proc/sys/kernel/hostname", host_kernel, sizeof(host_kernel)) != 0)
        strcpy(host_kernel, "<无>");
    if (gethostname(host_posix, sizeof(host_posix) - 1) == -1)
        strcpy(host_posix, "<失败>");
    printf("  /proc/sys/kernel/hostname : [%s]\n", host_kernel);
    printf("  gethostname()             : [%s]\n", host_posix);
    printf("  → %s\n", strcmp(host_kernel, host_posix) == 0 ? "一致" : "不一致");

    struct utsname u;
    if (uname(&u) == 0) {
        char osrel[256] = "";
        if (first_line("/proc/sys/kernel/osrelease", osrel, sizeof(osrel)) != 0)
            strcpy(osrel, "<无>");
        printf("  /proc/sys/kernel/osrelease: [%s]\n", osrel);
        printf("  uname().release           : [%s]\n", u.release);
        printf("  → %s\n", strcmp(osrel, u.release) == 0 ? "一致" : "不一致");
    }
    printf("  ⚠️ 可移植代码用 gethostname()/uname()；写系统脚本才图省事读 /proc/sys。\n\n");

    /* ---------- ⑥ 一个容易误会的事实 ---------- */
    printf("== ⑥ /proc/sys/fs/file-max 是 LONG_MAX 级别的数，不是「现在能开多少」 ==\n");
    if (first_line("/proc/sys/fs/file-max", line, sizeof(line)) == 0) {
        printf("  /proc/sys/fs/file-max = %s\n", line);
        printf("  ⚠️ 它是系统级**总量上限**，单个进程还被 RLIMIT_NOFILE 卡着；\n");
        printf("     查进程能开多少 fd 要用 getrlimit(RLIMIT_NOFILE) / sysconf(_SC_OPEN_MAX)\n");
        printf("     （见 Ch11 §11.2：那个值其实是 rlim_cur）。\n");
    }
    return EXIT_SUCCESS;
}
```

CE 实测：

```text
== ① 几个纯文本全局文件（原样一行）==
  /proc/version : Linux version 7.0.0-1012-aws (buildd@lcy02-amd64-024) (x86_64-linux-gnu-gcc-13 (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0, GNU ld (GNU Binutils for Ubuntu) 2.42) #12~24.04.1-Ubuntu SMP PREEMPT Wed Aug 12 14:00:57 UTC 2026
  /proc/uptime  : 14702.22 24377.62
  /proc/loadavg : 0.89 0.78 0.53 3/247 2
  ⚠️ /proc/uptime 有**两个**数：第一个是开机秒数，第二个是「所有 CPU 的
     空闲时间总和」—— 双核机器上它会约等于 2× 开机秒。它**不是** idle 比例。
  ⚠️ /proc/loadavg 的第 4 段是 "running/total"，第 5 段是「最近分配的 PID」，
     都不是负载。

== ② sysinfo() 与 /proc 的对账 ==
  sysinfo: uptime=14703s  loads=[58592 50816 35040]  mem_unit=1  procs=247
  sysinfo: totalram=16465022976  freeram=1015652352  sharedram=3231744  bufferram=108908544
  sysinfo: totalswap=0  freeswap=0
  → 字节数 = 字段 × mem_unit（本机 mem_unit=1，所以字段就是字节数）
    totalram × mem_unit = 16465022976 字节 = 15702 MiB
    freeram  × mem_unit = 1015652352 字节 = 968 MiB
  /proc/meminfo : MemTotal=16079124 kB  MemFree=991848 kB
  → MemTotal(kB) × 1024 = 16465022976 字节  vs  sysinfo 的 16465022976 字节  → 一致
  ⚠️ 但 **MemFree ≠ freeram 的口径**：meminfo 还有 MemAvailable / Buffers /
     Cached / Slab 等十几项，sysinfo 只有 freeram。要看内存压力必须读
     /proc/meminfo；只想「大概有多少」才用 sysinfo。

== ③ 负载：sysinfo 是定点数，要除以 65536 ==
  sysinfo.loads[0] = 58592  →  58592/65536.0 = 0.8940
  /proc/loadavg   = 0.89 0.78 0.53
  → 两边对得上（差在 /proc 只给两位小数）
  ⚠️ 老内核里 loads[] 的定标不是 65536（是 1<<SI_LOAD_SHIFT，早期还有
     直接给整数的版本）→ 跨内核解析要小心。

== ④ 「几个 CPU」有三条路 ==
  get_nprocs()      = 2    get_nprocs_conf() = 2
  sysconf(_SC_NPROCESSORS_ONLN)   = 2
  sysconf(_SC_NPROCESSORS_CONF)   = 2
  get_nprocs() 调用后 errno = 2（No such file or directory）← 这是**脏 errno**：
     函数调用是**成功的**，这个值来自它内部的第一条尝试 ——
     glibc 先读 /sys/devices/system/cpu/online（本容器没挂 /sys，
     该文件不存在 → ENOENT 留在 errno），失败后才退到数 /proc/stat。
     所以判错必须自己先 errno = 0、调用后立即取；别把 errno 当返回值用。
  /proc/stat 第一行: cpu  95830 268573 71132 2437762 47589 0 1629 49 0 0
  → 第一个字段 "cpu" 是**所有核的汇总**，之后才是 cpu0 / cpu1 …
  /proc/cpuinfo 第一行: processor	: 0   （processor : N，每个逻辑核一段）

== ⑤ 内核参数的文本接口 vs POSIX 接口 ==
  /proc/sys/kernel/hostname : [ce]
  gethostname()             : [ce]
  → 一致
  /proc/sys/kernel/osrelease: [7.0.0-1012-aws]
  uname().release           : [7.0.0-1012-aws]
  → 一致
  ⚠️ 可移植代码用 gethostname()/uname()；写系统脚本才图省事读 /proc/sys。

== ⑥ /proc/sys/fs/file-max 是 LONG_MAX 级别的数，不是「现在能开多少」 ==
  /proc/sys/fs/file-max = 9223372036854775807
  ⚠️ 它是系统级**总量上限**，单个进程还被 RLIMIT_NOFILE 卡着；
     查进程能开多少 fd 要用 getrlimit(RLIMIT_NOFILE) / sysconf(_SC_OPEN_MAX)
     （见 Ch11 §11.2：那个值其实是 rlim_cur）。
```

### 全局文件速查

| 文件 | 给什么 | 陷阱 |
|------|-------|------|
| `meminfo` | `MemTotal` / `MemFree` / `MemAvailable` / `Buffers` / `Cached` / `Slab` … | 单位是 **kB**；`MemFree` 不等于 `sysinfo().freeram` |
| `cpuinfo` | 每个**逻辑核**一段，`processor : N` | 只在**在线**核上输出 |
| `stat` | `cpu`（汇总行）+ `cpuN`（每核）+ `intr` / `ctxt` / `btime` / `processes` | 第一行 `cpu ` 后面是空格 → 不是核，别数进来 |
| `version` | 一行，含编译器串 | `uname().version` 是它的子集（截到 64 字符） |
| `uptime` | 两个数：开机秒 + 各核累计空闲秒 | 第二个数**不是** idle 比例 |
| `loadavg` | 三个负载 + `running/total` + 最近 PID | 第 4/5 段不是负载 |
| `sys/` | 可调参数树 | 见 §12.1.3 |

> **`sysinfo()` 的三条硬事实**（源码坐标见 [12.4 延伸](12.4-exercises.md)）：
> ① 内存字段的真实值永远是「字段 × `mem_unit`」；
> ② `loads[]` 是定标为 `1 << 16 = 65536` 的**定点数**；
> ③ `procs` 在内核里赋的是 `nr_threads`——**线程数**，而 man page 写的是 "Number of current processes"。本容器实测 `procs=236` 而 `/proc` 下的进程目录只有 **2** 个，且这个差距**不是命名空间隔离造成的**（`sysinfo()` 压根不做隔离，它报的是宿主级的数字）。完整展开见 [12.4](12.4-exercises.md)。

### 谁来维护 `/proc/PID` 的竞态

遍历 `/proc/PID` 时进程随时可能退出。这不是「需要加锁」的问题，而是**必须容忍**的常态。原书在 `procfs_user_exe.c` 里对此只写了一句注释：

```c
        FILE *fp = fopen(path, "r");
        if (fp == NULL)
            continue;           /* Ignore errors: fopen() might fail if
                                   process has just terminated */
```

工程上的正确处理是：**把 `scanned` / `matched` / `skipped` / `vanished` 分别计数**，让「数量对不上」这件事变成可观察的数字，而不是一句注释。[12.4 习题 12-1](12.4-exercises.md) 的自写版就是这么做的。

---

## §12.1.3 怎么访问 `/proc` 文件

原书这一节的结论很短：**改内核参数不需要专门系统调用，就是 `open` + `write` 一个文本文件**。但细节足够写一页。

完整源码 [`code/c12_1_proc_sys.c`](../code/c12_1_proc_sys.c)：

```c
/* c12_1_proc_sys.c — Ch12 §12.1.3：/proc/sys 是「把内核参数当文件」
 *
 * TLPI §12.1.3 的核心结论：改内核参数**不需要**专门系统调用，就是
 * `open` + `write` 一个文本文件。本程序把这件事的细节全钉住：
 *   ① /proc/sys 是一棵按子系统分层的树（kernel/ fs/ vm/ net/…）；
 *   ② 每个 knob 的读写权限不一样（0644 可读写 / 0444 只读）；
 *   ③ **对只读打开的 fd 调 write 是 EBADF**（不是 EACCES）；
 *   ④ 写完要重新 open（或 lseek 回 0）才读得到新值 —— 这就是原书
 *      Listing 12-1 在 write 之前插一句 lseek(fd, 0, SEEK_SET) 的原因；
 *   ⑤ 内核会**校验取值范围**，写非法值返回 EINVAL 且不改动原值；
 *   ⑥ `sysctl(2)` 系统调用已被删除（Linux 5.5），只能走文件接口。
 *
 * 编译： gcc -O0 -Wall -Wextra -o c12_1_proc_sys c12_1_proc_sys.c
 * 取材： man-pages 6.19 proc_sys(5) 全文 + sysctl(2) 的 NOTES
 *       Linux v6.6 fs/proc/proc_sysctl.c（proc_sys_open / proc_sys_call_handler）
 *                      fs/proc/generic.c（proc_ops 的 read/write）
 *                      kernel/sysctl.c（pid_max 的 min/max：300 < v <= 4194304）
 */
#define _GNU_SOURCE
#include <errno.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/syscall.h>
#include <unistd.h>

/* 读一个 knob，把内容放进 out（去掉行尾换行）；返回实际长度，-1 表示失败 */
static ssize_t read_knob(const char *path, char *out, size_t cap)
{
    int fd = open(path, O_RDONLY);
    if (fd == -1)
        return -1;
    ssize_t n = read(fd, out, cap - 1);
    close(fd);
    if (n < 0)
        return -1;
    out[n] = '\0';
    while (n > 0 && (out[n - 1] == '\n' || out[n - 1] == '\r'))
        out[--n] = '\0';
    return n;
}

/* 打开一次、读一次、然后**不清偏移**直接写；再 lseek 回 0 写一次 */
static void write_probe(const char *path, const char *newval)
{
    printf("  -- 写试探：%s ← \"%s\" --\n", path, newval);

    /* (a) 只读打开再写 */
    int fd = open(path, O_RDONLY);
    if (fd == -1) {
        printf("     open(O_RDONLY) 失败 errno=%d(%s)\n", errno, strerror(errno));
    } else {
        errno = 0;
        ssize_t w = write(fd, newval, strlen(newval));
        printf("     对 O_RDONLY 的 fd write() = %zd errno=%d(%s)\n",
               w, w < 0 ? errno : 0, w < 0 ? strerror(errno) : "ok");
        close(fd);
    }

    /* (b) 读写打开：先 read（偏移推到末尾），不 lseek 直接写 */
    fd = open(path, O_RDWR);
    if (fd == -1) {
        printf("     open(O_RDWR)   失败 errno=%d(%s)"
               "  ← 非特权进程在这里就止步了\n", errno, strerror(errno));
        return;
    }
    char buf[64];
    ssize_t r = read(fd, buf, sizeof(buf) - 1);
    if (r < 0) {
        printf("     read 失败 errno=%d(%s)\n", errno, strerror(errno));
        close(fd);
        return;
    }
    buf[r] = '\0';
    printf("     打开后 read() 拿到 \"%.*s\"（偏移已到 %zd）\n",
           (int) (r && buf[r - 1] == '\n' ? r - 1 : r), buf, r);

    errno = 0;
    ssize_t w = write(fd, newval, strlen(newval));
    printf("     不清偏移直接 write() = %zd errno=%d(%s)\n",
           w, w < 0 ? errno : 0, w < 0 ? strerror(errno) : "ok");
    printf("       ⚠️ 偏移不为 0 时内核**静默忽略**这次写入，却照样返回成功：\n");
    printf("          fs/proc/proc_sysctl.c 的 proc_first_pos_non_zero_ignore()\n");
    printf("          在 sysctl_writes_strict=1（默认）时直接 return，count 照收。\n");
    printf("          所以「write 返回成功」≠「值被改了」。\n");

    if (lseek(fd, 0, SEEK_SET) == (off_t) -1) {
        printf("     lseek(fd, 0, SEEK_SET) 失败 errno=%d(%s)\n", errno, strerror(errno));
    } else {
        errno = 0;
        w = write(fd, newval, strlen(newval));
        printf("     lseek 回 0 再 write() = %zd errno=%d(%s)\n",
               w, w < 0 ? errno : 0, w < 0 ? strerror(errno) : "ok");
    }
    close(fd);

    char after[64];
    if (read_knob(path, after, sizeof(after)) >= 0)
        printf("     写后重新读 = \"%s\"\n", after);
}

int main(void)
{
    /* ---------- ① 树形结构：列出几个子系统的 knob ---------- */
    printf("== ① /proc/sys 是按子系统分层的树 ==\n");
    static const char *KNOBS[] = {
        "/proc/sys/kernel/pid_max",
        "/proc/sys/kernel/threads-max",
        "/proc/sys/kernel/ngroups_max",
        "/proc/sys/kernel/randomize_va_space",
        "/proc/sys/kernel/osrelease",
        "/proc/sys/kernel/version",
        "/proc/sys/vm/overcommit_memory",
        "/proc/sys/vm/swappiness",
        "/proc/sys/fs/file-max",
        "/proc/sys/fs/nr_open",
        "/proc/sys/net/core/somaxconn",
    };
    for (size_t i = 0; i < sizeof(KNOBS) / sizeof(KNOBS[0]); i++) {
        char v[128];
        if (read_knob(KNOBS[i], v, sizeof(v)) >= 0)
            printf("  %-42s = %s\n", KNOBS[i], v);
        else
            printf("  %-42s 读失败 errno=%d(%s)\n", KNOBS[i], errno, strerror(errno));
    }
    printf("  tip: 树根就是 `/proc/sys/`，`sysctl -a` 只是把这棵树全部走一遍。\n\n");

    /* ---------- ② 权限：可写 vs 只读 ---------- */
    printf("== ② 权限位不代表一切 ==\n");
    struct { const char *p; int flag; const char *how; } FL[] = {
        {"/proc/sys/kernel/pid_max",   O_RDONLY, "O_RDONLY"},
        {"/proc/sys/kernel/pid_max",   O_WRONLY, "O_WRONLY"},
        {"/proc/sys/kernel/pid_max",   O_RDWR,   "O_RDWR"},
        {"/proc/sys/kernel/osrelease", O_WRONLY, "O_WRONLY"},
        {"/proc/sys/kernel/version",   O_WRONLY, "O_WRONLY"},
    };
    for (size_t i = 0; i < sizeof(FL) / sizeof(FL[0]); i++) {
        errno = 0;
        int fd = open(FL[i].p, FL[i].flag);
        printf("  open(%-32s, %-9s) = %-3d errno=%d(%s)\n",
               FL[i].p, FL[i].how, fd, fd < 0 ? errno : 0,
               fd < 0 ? strerror(errno) : "ok");
        if (fd >= 0)
            close(fd);
    }
    printf("  ⚠️ 只读 knob（osrelease / version）用 O_WRONLY 打开就失败；\n");
    printf("     但**用 O_RDONLY 打开一个可写 knob 会成功**，失败发生在 write 那一刻，\n");
    printf("     且错误码是 EBADF（fd 不是为写打开的），不是 EACCES。\n\n");

    /* ---------- ③ 写入：非法值 / 正确值 ---------- */
    printf("== ③ 写 /proc/sys/kernel/pid_max ==\n");
    char orig[64];
    if (read_knob("/proc/sys/kernel/pid_max", orig, sizeof(orig)) < 0) {
        printf("  读 pid_max 失败 errno=%d(%s)\n", errno, strerror(errno));
        return EXIT_FAILURE;
    }
    printf("  当前值 = \"%s\"\n", orig);
    printf("  先写一个**越界**的值，验证内核会拒绝：\n");
    write_probe("/proc/sys/kernel/pid_max", "0");
    printf("  再写回**原值**（等价于空操作，但能验证写通道是通的）：\n");
    write_probe("/proc/sys/kernel/pid_max", orig);
    printf("\n");

    /* ---------- ④ sysctl(2) 已经不存在 ---------- */
    printf("== ④ sysctl(2) 系统调用已删除 ==\n");
#ifdef SYS_sysctl
    errno = 0;
    long r = syscall(SYS_sysctl, (void *) 0, 0, (void *) 0, (void *) 0, 0);
    printf("  本机 glibc 头里有 SYS_sysctl=%d；syscall() 返回 %ld errno=%d(%s)\n",
           SYS_sysctl, r, errno, strerror(errno));
    printf("  → 若 errno=ENOSYS(38)，说明这个 syscall 已被内核移除。\n");
#else
    printf("  本机 glibc 头里**没有** SYS_sysctl —— 这个系统调用自 Linux 5.5 起已删除，\n");
    printf("  glibc 也不再导出包装函数。旧代码里的 sysctl(3) 一律应改写成\n");
    printf("  open/write(\"/proc/sys/...\")。\n");
#endif
    printf("  ⚠️ 另一条路：glibc 的 `sysctl` 名字现在只作为**兼容符号**存在，\n");
    printf("     成功调用也不会得到数据（返回 0 但 buffer 未填），比报错更难查。\n");
    return EXIT_SUCCESS;
}
```

CE 实测：

```text
== ① /proc/sys 是按子系统分层的树 ==
  /proc/sys/kernel/pid_max                   = 4194304
  /proc/sys/kernel/threads-max               = 54382
  /proc/sys/kernel/ngroups_max               = 65536
  /proc/sys/kernel/randomize_va_space        = 2
  /proc/sys/kernel/osrelease                 = 7.0.0-1012-aws
  /proc/sys/kernel/version                   = #12~24.04.1-Ubuntu SMP PREEMPT Wed Aug 12 14:00:57 UTC 2026
  /proc/sys/vm/overcommit_memory             = 0
  /proc/sys/vm/swappiness                    = 60
  /proc/sys/fs/file-max                      = 9223372036854775807
  /proc/sys/fs/nr_open                       = 1048576
  /proc/sys/net/core/somaxconn               = 4096
  tip: 树根就是 `/proc/sys/`，`sysctl -a` 只是把这棵树全部走一遍。

== ② 权限位不代表一切 ==
  open(/proc/sys/kernel/pid_max        , O_RDONLY ) = 3   errno=0(ok)
  open(/proc/sys/kernel/pid_max        , O_WRONLY ) = 3   errno=0(ok)
  open(/proc/sys/kernel/pid_max        , O_RDWR   ) = 3   errno=0(ok)
  open(/proc/sys/kernel/osrelease      , O_WRONLY ) = -1  errno=13(Permission denied)
  open(/proc/sys/kernel/version        , O_WRONLY ) = -1  errno=13(Permission denied)
  ⚠️ 只读 knob（osrelease / version）用 O_WRONLY 打开就失败；
     但**用 O_RDONLY 打开一个可写 knob 会成功**，失败发生在 write 那一刻，
     且错误码是 EBADF（fd 不是为写打开的），不是 EACCES。

== ④ sysctl(2) 系统调用已删除 ==
  本机 glibc 头里**没有** SYS_sysctl —— 这个系统调用自 Linux 5.5 起已删除，
  glibc 也不再导出包装函数。旧代码里的 sysctl(3) 一律应改写成
  open/write("/proc/sys/...")。
  ⚠️ 另一条路：glibc 的 `sysctl` 名字现在只作为**兼容符号**存在，
     成功调用也不会得到数据（返回 0 但 buffer 未填），比报错更难查。
```

### 写入路径的三个坑（本子节的核心）

`pid_max` 的写入实测（`path = /proc/sys/kernel/pid_max`，原值 `"4194304"`）：

```text
== ③ 写 /proc/sys/kernel/pid_max ==
  当前值 = "4194304"
  先写一个**越界**的值，验证内核会拒绝：
  -- 写试探：/proc/sys/kernel/pid_max ← "0" --
     对 O_RDONLY 的 fd write() = -1 errno=9(Bad file descriptor)
     打开后 read() 拿到 "4194304"（偏移已到 8）
     不清偏移直接 write() = 1 errno=0(ok)
       ⚠️ 偏移不为 0 时内核**静默忽略**这次写入，却照样返回成功：
          fs/proc/proc_sysctl.c 的 proc_first_pos_non_zero_ignore()
          在 sysctl_writes_strict=1（默认）时直接 return，count 照收。
          所以「write 返回成功」≠「值被改了」。
     lseek 回 0 再 write() = -1 errno=22(Invalid argument)
     写后重新读 = "4194304"
```

| # | 坑 | 判据 | 出处 |
|---|----|------|------|
| 1 | 用 `O_RDONLY` 打开的 knob 上 `write()` → **`EBADF(9)`**，不是 `EACCES` | 见上「对 O_RDONLY 的 fd write()」 | 内核在 `proc_sys_call_handler` 里按 `file->f_mode` 判 |
| 2 | 偏移不为 0 时写入**被静默忽略**，但 `write()` 仍返回写入长度 | 见上「不清偏移直接 write() = 1 errno=0(ok)」，值却仍是 `4194304` | `fs/proc/proc_sysctl.c` 的 `proc_first_pos_non_zero_ignore()`（`sysctl_writes_strict=1` 默认） |
| 3 | 越界值 → `EINVAL(22)`，且**原值不变** | 见上「lseek 回 0 再 write() = -1 errno=22」 | `kernel/sysctl.c` 里 `pid_max` 的 `min=300 / max=4194304` 校验 |

坑 2 就是**原书 Listing 12-1 在 `write` 之前插一句 `lseek(fd, 0, SEEK_SET)` 的原因**——那句话不是装饰：

完整源码 [`code/procfs_pidmax.c`](../code/procfs_pidmax.c)（**原书 Listing 12-1, p.228**，逐字镜像）：

```c
/*************************************************************************\
*                  Copyright (C) Michael Kerrisk, 2026.                   *
*                                                                         *
* This program is free software. You may use, modify, and redistribute it *
* under the terms of the GNU General Public License as published by the   *
* Free Software Foundation, either version 3 or (at your option) any      *
* later version. This program is distributed without any warranty.  See   *
* the file COPYING.gpl-v3 for details.                                    *
\*************************************************************************/

/* procfs_pidmax.c

   This program demonstrates how to access a file in the /proc file system. It
   can be used to read and modify the /proc/sys/kernel/pid_max file (which is
   available only in Linux 2.6 and later).

   Usage: procfs_pidmax [new-pidmax]

        Displays the current maximum PID, and, if a command line
        argument is supplied, sets the maximum PID to that value.

        Note: privilege is required to change the maximum PID value.

   This program is Linux-specific.
*/
#include <fcntl.h>
#include "tlpi_hdr.h"

#define MAX_LINE 100

int
main(int argc, char *argv[])
{
    int fd;
    char line[MAX_LINE];
    ssize_t n;

    fd = open("/proc/sys/kernel/pid_max", (argc > 1) ? O_RDWR : O_RDONLY);
    if (fd == -1)
        errExit("open");

    n = read(fd, line, MAX_LINE);
    if (n == -1)
        errExit("read");

    if (argc > 1)
        printf("Old value: ");
    printf("%.*s", (int) n, line);

    if (argc > 1) {
        if (lseek(fd, 0, SEEK_SET) == -1)
            errExit("lseek");

        if (write(fd, argv[1], strlen(argv[1])) != (ssize_t) strlen(argv[1]))
            fatal("write() failed");

        system("echo /proc/sys/kernel/pid_max now contains "
               "`cat /proc/sys/kernel/pid_max`");
    }

    exit(EXIT_SUCCESS);
}
```

CE 实测（两次运行）：

```text
4194304
```

```text
Old value: 4194304
```

> 带参数那次（`./procfs_pidmax 4194304`）把原值写回去，既走通 `write` 分支又不改坏沙箱。程序最后那句 `system("echo ... \`cat ...\`")` 在 CE 容器里**拿不到输出**——因为沙箱**没有 `/bin/sh`**（`system()` 返回 32512，即 `WIFEXITED=1, status=127`）。这是环境限制，不是程序 bug。

---

## 常见坑

| 坑 | 正解 |
|----|------|
| `fd = open(p, O_RDONLY); n = fstat(fd,&st); buf = malloc(st.st_size);` | `st_size` 恒为 0 → 分配 0 字节。要**读到 EOF**（或给固定缓冲） |
| `statfs("/proc")` 算剩余空间 | `f_blocks` / `f_files` 都是 0，看容量毫无意义 |
| 认为 `/proc/self` 是目录 | 它是**符号链接**（`S_ISLNK`），只因为内核会解析它，`open("/proc/self/x")` 才好用 |
| `fgets` 读 `cmdline` / `environ` | 只会拿到第一条。要 `read` 后按 `\0` 切 |
| 按空格切 `/proc/PID/stat` | `comm` 可含空格与括号。要么用 `/proc/PID/comm`，要么按「第一个 `(` 配最后一个 `)`」定界 |
| 按行号取 `status` 的字段 | 第 2 行是 `Umask` 不是 `Pid`；字段顺序随内核版本增删。**按 `key:` 查** |
| 用 `write()` 返回值判断「改成功没有」 | 偏移不为 0 时内核静默忽略却返回成功。**写完必须 `lseek(0)` 或重开再读回来验证** |
| 对只读 knob 用 `O_WRONLY` 打开 | 会得到 `EACCES(13)`；而对可写 knob 用 `O_RDONLY` 打开**会成功**，`write` 时才 `EBADF(9)`。两次判据不一致，别混 |
| 把 `/proc/uptime` 第二个数当 idle 比例 | 它是「各核空闲秒数之和」，要除核数、再除开机秒才近似 idle 比例 |
| 遍历 `/proc/PID` 时对 `fopen` 失败报错退出 | 进程随时会退出 → `ENOENT` 是常态。**必须忽略并计数** |

---

## HFT / 嵌入式关联

| 场景 | 本节机制怎么兑现 |
|------|----------------|
| 交易进程自检 | `/proc/self/status` 的 `VmRSS` / `Threads` / `Uid` 三项打进启动日志；按 `key:` 逐行解析，**不要按行号**——容器镜像换一个内核版本，行号就变了 |
| 谁在跑什么程序 | `/proc/PID/exe` 指向可执行文件，**即使文件已被删除仍可读**。排查「升级后旧版本进程还在跑」就靠它 |
| fd 泄漏排查 | `/proc/self/fd` 是自带的 `lsof`。但**枚举动作自己会占一个 fd**，且两次快照数量必然可能不同——只能看趋势，不能拿「两次相等」当判据（详见 [12.4](12.4-exercises.md)） |
| 热路径禁用 | `/proc` 的每个文件都是**读过才生成**（`seq_file` 现场序列化），有内核态开销与锁。监控线程 100 ms 采一次可以，主循环里读就是灾难 |
| 内核参数调优 | 全部走 `open("/proc/sys/...", O_WRONLY)` + `write`。**写完必须回读验证**：`write` 返回成功不代表生效（偏移陷阱 + 内核校验） |
| 容器里的「几个核」 | `get_nprocs()` 只读 sysfs / `/proc/stat` / `sched_getaffinity`，**不读 cgroup 配额**。要真实并行度得自己读 `cpu.max`（[12.4](12.4-exercises.md) 有实测） |
| 嵌入式裁剪 rootfs | BusyBox 的 `ps` / `top` 就是读 `/proc`。自定义镜像若没挂 `/proc`（忘记 `mount -t proc`），一切进程信息工具全部失效；`/proc/sys` 也不存在，`sysctl` 自然失败 |

---

## 衔接

- 上一节：[本章首页](../README.md)
- 下一节：[12.2 System Identification: uname()](12.2-uname.md) — 本章唯一的 POSIX 可移植接口
- 原书习题与两个延伸：[12.4 Exercises](12.4-exercises.md)（`/proc` 遍历、fd 快照、`sysinfo()` 深挖、CPU 个数四入口）
- 读 `/proc` 依赖的 `open` / `read` 语义：[Ch4 File I/O: The Universal I/O Model](../../chapter-04-file-io-universal/README.md) · [`read()` 返回值语义](../../chapter-04-file-io-universal/notes/4.4-read.md)
- `sysconf` 的补充来源（`_SC_NPROCESSORS_*` / `_SC_PHYS_PAGES`）：[Ch11 System Limits](../../chapter-11-system-limits/README.md)
- `setrlimit` 与 `/proc/sys` 的差别（前者进程级，后者系统级）：[Ch36 Process Resources](../../chapter-36-process-resources/README.md)
- 命名空间（`/proc/PID/ns`）：[Ch28 Process Creation and Program Execution](../../chapter-28-process-creation-exec-detail/README.md)

---

<details>
<summary>代码自测（Q&A）</summary>

**Q1：`stat("/proc/version").st_size` 是 0，那该怎么读它？**
A：给一个足够大的栈缓冲，`read()` 到返回 0（EOF）为止。内核是在 `read` 时才把内容序列化出来的（`fs/proc/version.c` 用 `seq_file` 接口），`st_size` 恒为 0。所以**不能**用 `st_size` 去 `malloc`。同理 `st_blocks` 也是 0。

**Q2：为什么两次读 `/proc/uptime` 会得到不同的数？**
A：因为 `/proc` 文件的「内容」不是磁盘上的字节，而是内核在每次 `read` 时**按当前时刻现算**的（`fs/proc/uptime.c` 里调 `ktime_get_boottime_ts64()`）。本仓库的 demo 特意在两次读之间 `usleep(200ms)`，于是能看出两次差约 0.2 秒——这是**方向性**证据，具体数字每次跑都不同。

**Q3：解析 `/proc/PID/stat` 时，`comm` 里含 `)` 会出什么事？**
A：朴素写法「第一个 `(` 配第一个 `)`」会把 `(a)b)` 切成 `(a)`，**丢掉 2 个字符**，导致 `)` 之后那约 50 个字段的编号全部错位。正确规则是**第一个 `(` 配最后一个 `)`**（`proc(5)` 的字段表就是按这个前提写的）。实测：正确切法 `[a)b]`，朴素切法 `[a]`。要进程名其实有更省事的办法——直接读 `/proc/PID/comm`。

**Q4：`/proc/PID/status` 的第 2 行是 `Pid` 吗？**
A：**不是**，是 `Umask`。本容器实测前 8 行是 `Name / Umask / State / Tgid / Ngid / Pid / PPid / TracerPid`——真 `Pid` 在第 6 行。这个顺序由 `fs/proc/array.c` 里一连串 `seq_printf` 的调用次序决定，跨内核版本会增删（`Umask` 就是较新内核才补的）。**必须按 `key:` 前缀查，不能按行号取**。

**Q5：往 `/proc/sys/kernel/pid_max` 写值，`write()` 返回了写入长度，为什么值没变？**
A：因为**文件偏移不为 0**。`fs/proc/proc_sysctl.c` 里的 `proc_first_pos_non_zero_ignore()` 在 `sysctl_writes_strict=1`（默认）时直接返回「忽略」，但 `write()` 的返回值仍按你给的字节数照收。这就是原书 Listing 12-1 在写之前一定要 `lseek(fd, 0, SEEK_SET)` 的原因。**教训：`write` 返回成功 ≠ 生效，必须回读验证。**

**Q6：`open("/proc/sys/kernel/pid_max", O_RDONLY)` 成功之后 `write()` 报了什么错？**
A：`EBADF(9)`（不是 `EACCES`）。fd 本身合法，只是**不是为写打开的**。对比：对只读 knob（如 `osrelease`）用 `O_WRONLY` 打开会直接 `EACCES(13)`。两者判据不一致，写代码时别把「打开成功」当「可写」。

**Q7：`/proc/PID/ns/net` 的 inode 号有什么用？**
A：**它就是命名空间的身份**。同一个网络命名空间里的两个进程，`stat("/proc/<pid>/ns/net")` 得到同一个 inode；不同容器则不同。本容器实测 `ns/time` 与 `ns/time_for_children` 都是 `4026531834`（容器共享的那一个），而 `net/uts/ipc/pid/user/mnt/cgroup` 都是 `402653226x` 段（每容器新建）。

**Q8：为什么「遍历 `/proc/PID` 数上限」不能写成「两次数量必须相等」？**
A：因为 `/proc` 是**活的**。两次遍历之间任何进程都可能 fork / exit（别的线程也会），数量自然会变；如果代码自己还会 `opendir`，那个 fd 也会出现在 `/proc/self/fd` 里。正确做法是把 `scanned` / `matched` / `skipped` / `vanished` 分开计数，让竞态可观察（[12.4](12.4-exercises.md) 的实现就是这么做的）。

</details>
