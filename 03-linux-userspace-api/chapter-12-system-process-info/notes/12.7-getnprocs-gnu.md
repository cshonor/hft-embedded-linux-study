# 12.7 `get_nprocs()` — GNU

> 本章：[TLPI 第 12 章 — System and Process Information](./README.md)
> - 上一节：[12.6 `sysinfo()` — Linux](./12.6-sysinfo.md)
> - 下一节：[12.8 可移植 vs Linux](./12.8-section-12-8.md)

---

## 本节讲什么

#include <sys/sysinfo.h> int get_nprocs(void);       /* 当前可用 CPU */ int get_nprocs_conf(void);  /* 配置的 CPU 总数 */

---

## 要点

```c
#include <sys/sysinfo.h>
int get_nprocs(void);       /* 当前可用 CPU */
int get_nprocs_conf(void);  /* 配置的 CPU 总数 */
```

便于多核检测（底层常读 `/proc`）。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `get_nprocs()` — GNU 在嵌入式系统的应用场景 |
| HFT | `get_nprocs()` — GNU 对低延迟和性能的影响 |

## 代码示例

```c
#include <sys/sysinfo.h>
#include <stdio.h>

int main(void) {
    /* get_nprocs: 可用 CPU 数 (GNU 扩展) */
    int nprocs = get_nprocs();
    int nprocs_conf = get_nprocs_conf();

    printf("配置的 CPU 数: %d\n", nprocs_conf);
    printf("可用的 CPU 数: %d\n", nprocs);

    /* 替代: sysconf(_SC_NPROCESSORS_ONLN) */
    long online = sysconf(_SC_NPROCESSORS_ONLN);
    long configured = sysconf(_SC_NPROCESSORS_CONF);
    printf("sysconf: online=%ld, configured=%ld\n", online, configured);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[12.6 `sysinfo()` — Linux](./12.6-sysinfo.md)
- 下一节：[12.8 可移植 vs Linux](./12.8-section-12-8.md)
- 前置：**前置**：[Ch11 System Limits](../chapter-11-system-limits/notes.md)（`sysconf` 的补充来源）  
- 后置：**后置**：[Ch13 File I/O Buffering](../chapter-13-file-io-buffering/notes.md) · 读 `/proc` 依赖的 **open/read** 见 [Ch4](../chapter-04-file-io-universal/notes.md)
