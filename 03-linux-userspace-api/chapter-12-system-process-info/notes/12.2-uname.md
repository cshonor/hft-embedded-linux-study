# 12.1 `uname()` — POSIX

> 本章：[TLPI 第 12 章 — System and Process Information](../README.md)
> - 上一节：本章首页
> - 下一节：[12.2 `/proc` — Linux 核心特色（非 POSIX）](12.1-proc-filesystem.md)

---

## 本节讲什么

#include <sys/utsname.h> int uname(struct utsname *buf); Demo：[`code/uname_demo.c`](../code/uname_demo.c)

---

## 要点

```c
#include <sys/utsname.h>
int uname(struct utsname *buf);
```

| 字段 | 含义 |
|------|------|
| `sysname` | OS 名（Linux） |
| `nodename` | 主机名 |
| `release` | 内核发布号（如 6.x.x） |
| `version` | 编译信息 |
| `machine` | 架构（`x86_64` / `aarch64`） |
| `domainname` | GNU 扩展：NIS 域名 |

> **可移植首选**；**拿不到** CPU 数、内存总量、进程列表。

Demo：[`code/uname_demo.c`](../code/uname_demo.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | uname 拿 sysname/release/machine（x86_64/aarch64），嵌入式跨架构移植据此判断；拿不到 CPU 数/内存/进程列表 |
| HFT | 交易程序启动时 uname 记录内核版本/架构到日志，便于问题定位；要 CPU 数/内存需配合 /proc 或 sysinfo |

## 代码示例

```c
#include <sys/utsname.h>
#include <stdio.h>

int main(void) {
    struct utsname info;
    if (uname(&info) != 0) { perror("uname"); return 1; }

    printf("系统名称: %s\n", info.sysname);   /* Linux */
    printf("主机名:   %s\n", info.nodename);
    printf("发行版:   %s\n", info.release);   /* 5.15.0-xxx */
    printf("版本信息: %s\n", info.version);
    printf("架构:     %s\n", info.machine);   /* x86_64 */

    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 下一节：[12.2 `/proc` — Linux 核心特色（非 POSIX）](12.1-proc-filesystem.md)
- 前置：**前置**：[Ch11 System Limits](../../chapter-11-system-limits/notes)（`sysconf` 的补充来源）  
- 后置：**后置**：[Ch13 File I/O Buffering](../../chapter-13-file-io-buffering/notes) · 读 `/proc` 依赖的 **open/read** 见 [Ch4](../../chapter-04-file-io-universal/notes)
