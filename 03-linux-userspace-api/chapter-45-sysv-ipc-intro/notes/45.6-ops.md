# 45.6 内核持久 · 运维

> 本章：[TLPI 第 45 章 — Introduction to System V IPC](./README.md)
> - 上一节：[45.5 `get()` 算法（高频）](./45.5-get.md)
> - 下一节：[45.7 –45.8 缺陷与限额](./45.7-defects-quotas.md)

---

## 本节讲什么

进程全退对象仍在 → `*ctl(..., IPC_RMID)` 或重启。 ipcs -q/-s/-m ipcrm -q <msqid>    # 或 -M key / -S / -m 等

---

## 要点

进程全退对象仍在 → `*ctl(..., IPC_RMID)` 或重启。

```bash
ipcs -q/-s/-m
ipcrm -q <msqid>    # 或 -M key / -S / -m 等
```

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 内核持久 · 运维 在嵌入式系统的应用场景 |
| HFT | 内核持久 · 运维 对低延迟和性能的影响 |

## 代码示例

```c
#include <stdio.h>
#include <sys/ipc.h>
#include <sys/msg.h>
#include <sys/sem.h>
#include <sys/shm.h>
#include <string.h>
/* xxxop: IPC 操作 (msgsnd/msgrcv, semop, shmat/shmdt) */
int main(void) {
    /* 消息队列操作 */
    int msqid = msgget(IPC_PRIVATE, IPC_CREAT | 0666);
    struct { long mtype; char mtext[32]; } msg = {1, "hello"};
    msgsnd(msqid, &msg, strlen(msg.mtext)+1, 0);
    msgrcv(msqid, &msg, sizeof(msg.mtext), 0, 0);
    printf("msgq: %s\n", msg.mtext);

    /* 信号量操作 */
    int semid = semget(IPC_PRIVATE, 1, IPC_CREAT | 0666);
    /* 初始化信号量 */
    semctl(semid, 0, SETVAL, 1);
    /* P 操作 (减1) */
    struct sembuf sop = {0, -1, 0};
    semop(semid, &sop, 1);
    printf("sem: P done, value=%d\n", semctl(semid, 0, GETVAL));
    /* V 操作 (加1) */
    sop.sem_op = 1;
    semop(semid, &sop, 1);
    printf("sem: V done, value=%d\n", semctl(semid, 0, GETVAL));

    /* 共享内存操作 */
    int shmid = shmget(IPC_PRIVATE, 4096, IPC_CREAT | 0666);
    char *shm = shmat(shmid, NULL, 0);
    strcpy(shm, "shared!");
    printf("shm: %s\n", shm);
    shmdt(shm);

    /* 清理 */
    msgctl(msqid, IPC_RMID, NULL);
    semctl(semid, 0, IPC_RMID);
    shmctl(shmid, IPC_RMID, NULL);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[45.5 `get()` 算法（高频）](./45.5-get.md)
- 下一节：[45.7 –45.8 缺陷与限额](./45.7-defects-quotas.md)
- 前置：**前置**：[Ch44 管道与 FIFO](../chapter-44-pipes-fifos/notes.md)  
- 后置：**后置**：[Ch46 SysV 消息队列](../chapter-46-sysv-message-queues/notes.md) → [Ch47 信号量](../chapter-47-sysv-semaphores/notes.md) → [Ch48 共享内存](../chapter-48-sysv-shared-memory/notes.md)
