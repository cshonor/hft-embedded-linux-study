# 45.5 `get()` 算法（高频）

> 本章：[TLPI 第 45 章 — Introduction to System V IPC](./README.md)
> - 上一节：[45.3 –45.4 标识符 · `ipc_perm`](./45.3-ipcperm.md)
> - 下一节：[45.6 内核持久 · 运维](./45.6-ops.md)

---

## 本节讲什么

`msgget(key, flag)`，`flag` = `IPC_CREAT` / `IPC_EXCL` + mode： 1. 按 key 查找 2. **不存在**：无 `IPC_CREAT` → `ENOENT`；有则创建

---

## 要点

`msgget(key, flag)`，`flag` = `IPC_CREAT` / `IPC_EXCL` + mode：

1. 按 key 查找  
2. **不存在**：无 `IPC_CREAT` → `ENOENT`；有则创建  
3. **已存在**：`CREAT|EXCL` → `EEXIST`；仅 `CREAT` → 返回已有 id  

服务端常用：`get(key, IPC_CREAT | IPC_EXCL | 0600)`。

Demo：[`code/`](./code/)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `get()` 算法（高频） 在嵌入式系统的应用场景 |
| HFT | `get()` 算法（高频） 对低延迟和性能的影响 |

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[45.3 –45.4 标识符 · `ipc_perm`](./45.3-ipcperm.md)
- 下一节：[45.6 内核持久 · 运维](./45.6-ops.md)
- 前置：**前置**：[Ch44 管道与 FIFO](../chapter-44-pipes-fifos/notes.md)  
- 后置：**后置**：[Ch46 SysV 消息队列](../chapter-46-sysv-message-queues/notes.md) → [Ch47 信号量](../chapter-47-sysv-semaphores/notes.md) → [Ch48 共享内存](../chapter-48-sysv-shared-memory/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
