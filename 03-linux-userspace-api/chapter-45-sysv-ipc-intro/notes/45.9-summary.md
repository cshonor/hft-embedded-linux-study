# 45.9 `IPC_RMID` 差异

> 本章：[TLPI 第 45 章 — Introduction to System V IPC](../README.md)
> - 上一节：[45.7 –45.8 缺陷与限额](45.7-obtaining-a-list-of-all-ipc-objects.md)
> - 下一节：下一章+1:02d}-*/notes.md)

---

## 本节讲什么

---

---

## 要点

| 对象 | 行为 |
|------|------|
| 消息队列 / 信号量集 | 立即标记删除；持有 id 者可继续用，引用尽后释放 |
| 共享内存 | 仅标记；**全部 `shmdt` 后才真正销毁** |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | mq/sem 立即标记删，shm 延迟到全 shmdt；持有者仍可继续用 |
| HFT | 理解 shm 延迟删除语义，避免一边 RMID 一边仍 shmat 访问 |

## 代码示例

```c
#include <stdio.h>
#include <sys/ipc.h>
#include <sys/msg.h>
#include <sys/sem.h>
#include <sys/shm.h>
/* IPC_RMID: 删除 IPC 对象 (必须显式调用) */
int main(void) {
    /* 创建三个 IPC 对象 */
    int msqid = msgget(IPC_PRIVATE, IPC_CREAT | 0666);
    int semid = semget(IPC_PRIVATE, 1, IPC_CREAT | 0666);
    int shmid = shmget(IPC_PRIVATE, 4096, IPC_CREAT | 0666);

    printf("Created: msgq=%d, sem=%d, shm=%d\n", msqid, semid, shmid);
    printf("Use 'ipcs' to see them\n");

    /* 删除消息队列 */
    if (msgctl(msqid, IPC_RMID, NULL) == 0)
        printf("msgq %d deleted\n", msqid);

    /* 删除信号量集 */
    if (semctl(semid, 0, IPC_RMID) == 0)
        printf("sem %d deleted\n", semid);

    /* 删除共享内存 */
    if (shmctl(shmid, IPC_RMID, NULL) == 0)
        printf("shm %d deleted\n", shmid);

    /* 如果忘记删除, 重启前一直存在 */
    /* 命令行删除: ipcrm -q <id> / ipcrm -s <id> / ipcrm -m <id> */
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[45.7 –45.8 缺陷与限额](45.7-obtaining-a-list-of-all-ipc-objects.md)
- 前置：**前置**：[Ch44 管道与 FIFO](../../chapter-44-pipes-fifos/notes)  
- 后置：**后置**：[Ch46 SysV 消息队列](../../chapter-46-sysv-message-queues/notes) → [Ch47 信号量](../../chapter-47-sysv-semaphores/notes) → [Ch48 共享内存](../../chapter-48-sysv-shared-memory/notes)
