# 64.4 –64.5 POSIX 打开 · 典型架构

> 本章：[TLPI 第 64 章 — Pseudoterminals](../README.md)
> - 上一节：[64.3 vs 管道](64.3-opening-a-master-ptymasteropen.md)
> - 下一节：[64.6 –64.7 特性](64.6-implementing-script-1.md)

---

## 本节讲什么

mfd = posix_openpt(O_RDWR | O_NOCTTY); grantpt(mfd); unlockpt(mfd); slave = ptsname(mfd);

---

## 要点

```c
mfd = posix_openpt(O_RDWR | O_NOCTTY);
grantpt(mfd); unlockpt(mfd);
slave = ptsname(mfd);
sfd = open(slave, O_RDWR | O_NOCTTY);
```

父持 master；子：`close(master)` → `setsid()` → dup2(slave, 0/1/2) → `exec` shell。  
`setsid` 后打开 slave → 常成为**控制终端**。  
**termios 只改 slave**，勿在 master 上改。

Demo：[`code/`](../code/)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | –64.5 POSIX 打开 · 典型架构 在嵌入式系统的应用场景 |
| HFT | –64.5 POSIX 打开 · 典型架构 对低延迟和性能的影响 |

## 代码示例

```c

/* PTY 架构：fork 子进程在 slave 端运行 */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/wait.h>

int main(void) {
    int master = posix_openpt(O_RDWR | O_NOCTTY);
    if (master < 0) { perror("posix_openpt"); exit(1); }
    grantpt(master);
    unlockpt(master);
    char *slave_name = ptsname(master);

    pid_t pid = fork();
    if (pid == 0) {
        /* 子进程：打开 slave，设为控制终端 */
        int slave = open(slave_name, O_RDWR);
        if (slave < 0) { perror("open slave"); _exit(1); }

        /* 重定向 stdin/stdout/stderr 到 slave */
        dup2(slave, STDIN_FILENO);
        dup2(slave, STDOUT_FILENO);
        dup2(slave, STDERR_FILENO);
        if (slave > STDERR_FILENO) close(slave);
        close(master);

        /* 子进程认为自己连着终端 */
        printf("child: isatty(stdout)=%d\n", isatty(STDOUT_FILENO));
        printf("child: running with PTY as terminal\n");
        _exit(0);
    }

    /* 父进程：通过 master 读取子进程输出 */
    close(STDIN_FILENO);  /* 简化：不重定向 */
    char buf[256];
    ssize_t n = read(master, buf, sizeof(buf) - 1);
    if (n > 0) {
        buf[n] = '\0';
        printf("parent read from PTY master:\n%s", buf);
    }

    wait(NULL);
    close(master);
    return 0;
}

```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[64.3 vs 管道](64.3-opening-a-master-ptymasteropen.md)
- 下一节：[64.6 –64.7 特性](64.6-implementing-script-1.md)
- 前置：**前置**：[Ch62 Terminals](../../chapter-62-terminals/notes) · Ch63 Alternative I/O  
- 后置：**后置**：地图内 TLPI 主线结束；附录/其他模块另见仓库路线
