# 18.5 `rename`（同 FS 原子）

> 本章：[TLPI 第 18 章 — Directories and Links](./README.md)
> - 上一节：[18.4 符号链接](./18.4-link-symbol.md)
> - 下一节：[18.6 路径与 TOCTOU](./18.6-toctou.md)

---

## 本节讲什么

int rename(const char *oldpath, const char *newpath); int renameat(...); 可靠写入：写临时文件 → `rename(tmp, target)`。

---

## 要点

```c
int rename(const char *oldpath, const char *newpath);
int renameat(...);
```

| 情况 | 行为（同 FS） |
|------|----------------|
| new 不存在 | 改名 |
| new 是文件 | 原子替换 |
| new 空目录 | 可替换 |
| new 非空目录 | 失败 |
| **跨 FS** | 不能当原子移动（常需复制+删） |

可靠写入：写临时文件 → `rename(tmp, target)`。

Demo：[`code/rename_safe_write.c`](./code/rename_safe_write.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `rename`（同 FS 原子） 在嵌入式系统的应用场景 |
| HFT | `rename`（同 FS 原子） 对低延迟和性能的影响 |

## 代码示例

```c
#include <stdio.h>
#include <unistd.h>

int main(void) {
    /* rename: 原子性改名/移动 */
    FILE *fp = fopen("/tmp/rename_old.txt", "w");
    if (fp) { fprintf(fp, "hello rename"); fclose(fp); }

    /* 同目录改名 */
    if (rename("/tmp/rename_old.txt", "/tmp/rename_new.txt") == 0) {
        printf("改名成功: old → new\n");
    } else {
        perror("rename");
    }

    /* 跨目录移动 (同文件系统内) */
    /* rename("/tmp/file.txt", "/tmp/subdir/file.txt"); */

    /* rename 是原子操作:
     * - 不会出现"旧名已删, 新名还没建"的中间状态
     * - 目标已存在则原子替换
     * - 常用于原子更新文件: 写临时文件 → rename 覆盖
     */
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[18.4 符号链接](./18.4-link-symbol.md)
- 下一节：[18.6 路径与 TOCTOU](./18.6-toctou.md)
- 前置：**前置**：[Ch17 ACL](../chapter-17-access-control-lists/notes.md) · [Ch14 FS/inode](../chapter-14-file-systems/notes.md) · [Ch15 stat/lstat](../chapter-15-file-attributes/notes.md)  
- 后置：**后置**：[Ch19 inotify](../chapter-19-monitoring-file-events/notes.md)
