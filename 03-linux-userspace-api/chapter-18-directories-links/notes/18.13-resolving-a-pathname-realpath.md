# 18.13 Resolving a Pathname: realpath()

> 本章：[TLPI 第 18 章](../README.md)

## 本节讲什么

`realpath()` 将包含符号链接、`.`、`..` 的路径解析为规范的绝对路径。它会展开所有符号链接并消除冗余路径分隔符，返回最终的真实路径。

## 要点

### realpath() 函数

```c
#include <stdlib.h>
char *realpath(const char *path, char *resolved_path);
// 返回：成功 resolved_path，失败 NULL
```

### 使用规则

| resolved_path 参数 | 行为 | 注意 |
|--------------------|------|------|
| NULL | `malloc` 分配缓冲区 | 需手动 `free` |
| 非 NULL | 写入调用者缓冲 | 缓冲需 ≥ `PATH_MAX` |
| POSIX.1-2008 | 支持 NULL 参数 | GNU 扩展先于标准 |

### realpath 解析过程

1. 沿路径逐段解析
2. 遇到符号链接时展开（递归，最多 `MAXSYMLINKS` 次）
3. 处理 `.` 和 `..`
4. 消除多余 `/`
5. 返回绝对路径

### 错误条件

| errno | 含义 |
|-------|------|
| `EACCES` | 路径中某目录无搜索权限 |
| `EINVAL` | path 为 NULL |
| `ENAMETOOLONG` | 路径超长或符号链接递归超限 |
| `ENOENT` | 路径不存在的部分 |
| `ENOMEM` | 内存不足（NULL 参数时） |
| `ELOOP` | 符号链接循环 |

### 安全用法

```c
/* 旧用法：缓冲区可能溢出（已废弃） */
char buf[PATH_MAX];
realpath(path, buf);  /* PATH_MAX 足够大时安全 */

/* 新用法：NULL 参数让系统分配 */
char *resolved = realpath(path, NULL);
if (resolved) { /* 使用 */ free(resolved); }
```

### realpath vs readlink

| 特性 | `realpath()` | `readlink()` |
|------|-------------|--------------|
| 输入 | 任意路径 | 符号链接路径 |
| 输出 | 绝对规范路径 | 链接目标原始内容 |
| 展开链接 | 全部递归展开 | 不展开 |
| 处理 `.`/`..` | 消除 | 不处理 |

## HFT / 嵌入式关联

- 安全路径验证：防止目录穿越攻击（`../` 注入）
- 配置文件路径规范化：确保唯一性避免重复加载
- 日志文件路径解析：展开符号链接获取真实存储位置

## 代码示例

```c
#include <stdlib.h>
#include <stdio.h>
#include <limits.h>

int main(int argc, char *argv[]) {
    if (argc != 2) {
        fprintf(stderr, "Usage: %s <path>\n", argv[0]);
        return 1;
    }

    /* POSIX.1-2008 方式：NULL 参数 */
    char *resolved = realpath(argv[1], NULL);
    if (resolved == NULL) {
        perror("realpath");
        return 1;
    }
    printf("Input:    %s\n", argv[1]);
    printf("Resolved: %s\n", resolved);
    free(resolved);

    /* 经典方式：调用者提供缓冲 */
    char buf[PATH_MAX];
    if (realpath(argv[1], buf) == NULL) {
        perror("realpath(buf)");
        return 1;
    }
    printf("Buffer:   %s\n", buf);

    return 0;
}
```

## 衔接

- 上一节 [18.12 chroot()](./18.12-changing-the-root-directory-of-a-process.md)
- 下一节 [18.14 路径解析 dirname/basename](./18.14-parsing-pathname-strings-dirname-and-bas.md)

## 代码自测

<details>
<summary>realpath(path, NULL) 为什么比传缓冲区更安全？</summary>

传入 NULL 让 `realpath` 自行 `malloc` 正确大小的缓冲区，避免了调用者猜测大小的问题。旧版本需要 `PATH_MAX` 大小的缓冲，但某些系统上 `PATH_MAX` 可能不够大或未定义。缺点是需要调用者 `free` 返回值，且分配可能失败。

</details>
