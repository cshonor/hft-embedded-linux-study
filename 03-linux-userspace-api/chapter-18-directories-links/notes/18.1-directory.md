# 18.1 目录基础

> 本章：[TLPI 第 18 章 — Directories and Links](./README.md)
> - 上一节：本章首页
> - 下一节：[18.2 读目录](./18.2-directory.md)

---

## 本节讲什么

目录是特殊文件：数据块存目录项 `(文件名, inode#)`。 `.` = 自己；`..` = 父；根的 `..` 指向自己。 int mkdir(const char *pathname, mode_t mode);      /* 受 umask */

---

## 要点

目录是特殊文件：数据块存目录项 `(文件名, inode#)`。  
`.` = 自己；`..` = 父；根的 `..` 指向自己。

### 目录权限语义

| 位 | 含义 |
|----|------|
| `r` | 可列目录项（见名字）；**不能**单靠它访问内容 |
| `x` | 可进入 / 解析路径内名字（**访问内部文件必备**） |
| `w` | 可在目录内创建、删除、重命名条目 |

> 仅 `r` 无 `x`：能 `ls` 见名，常无法 `stat`/`open` 内部文件。

```c
int mkdir(const char *pathname, mode_t mode);      /* 受 umask */
int mkdirat(int dirfd, const char *pathname, mode_t mode);
int rmdir(const char *pathname);                   /* 仅空目录 */
int chdir(const char *path);
int fchdir(int fd);
char *getcwd(char *buf, size_t size);
```

`rmdir` 非空失败；递归删除须自写（`rm -r` 逻辑）。CWD 是**进程属性**。

---

---

## 与 C 语言学习的关系

文件系统操作是 Unix 系统编程的核心。`stat`/`readdir`/`link`/`unlink` 等 API 需要 C 语言的 struct、指针和错误处理知识。注意 `readdir` 返回的 `struct dirent*` 指向静态存储，下次调用会覆盖——这是 C 语言指针生命周期的经典陷阱。对照 K&R 8.4-8.6 节学习文件系统接口。
---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 目录基础 在嵌入式系统的应用场景 |
| HFT | 目录基础 对低延迟和性能的影响 |

---

## 常见陷阱

1. **stat/lstat/fstat 混用**

```c
/* stat: 跟随符号链接 */
/* lstat: 不跟随符号链接 */
lstat(path, &sb);  /* 获取符号链接自身信息 */
```

stat 会跟随符号链接获取目标文件信息。想获取链接本身用 lstat。搞混会导致权限判断错误。

2. **readdir 返回值被覆盖**

```c
struct dirent *d1 = readdir(dir);
struct dirent *d2 = readdir(dir);
/* d1 指向的内容可能已被 d2 覆盖! */
```

readdir 返回指向静态存储的指针。下次调用会覆盖。需要保存信息应复制 d_name 到自己的缓冲区。

3. **符号链接循环**

```c
ln -s b a; ln -s a b;  /* 循环链接 */
/* 递归遍历时会无限循环 */
```

符号链接可能形成循环。递归遍历目录时应限制深度或检测已访问的 inode。

---

## 自测题

**题目 1（选择）：** `lstat()` 和 `stat()` 的区别？
A. 没有区别
B. lstat 不跟随符号链接
C. stat 不跟随符号链接
D. lstat 只用于链接

<details>
<summary>参考答案</summary>

B。lstat 获取符号链接自身的信息（不跟随），stat 跟随符号链接获取目标文件的信息。

</details>

**题目 2（判断）：** `readdir()` 返回的 `struct dirent*` 可以长期保存。

<details>
<summary>参考答案</summary>

错误。readdir 返回指向静态存储的指针，下次调用会覆盖。需要保存应复制 d_name 等字段到自己的缓冲区。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
DIR *dir = opendir(".");
struct dirent *d;
while ((d = readdir(dir)) != NULL) {
    printf("%s
", d->d_name);
}
/* 没有 closedir */
```

<details>
<summary>参考答案</summary>

没有 closedir(dir)，导致目录流泄漏。应在所有路径上 closedir。

</details>

**题目 4（应用）：** 写代码递归遍历目录，打印所有文件路径。

<details>
<summary>参考答案</summary>

```c
void list_dir(const char *path) {
    DIR *dir = opendir(path);
    if (!dir) return;
    struct dirent *d;
    while ((d = readdir(dir)) != NULL) {
        if (d->d_name[0] == '.') continue;
        printf("%s/%s
", path, d->d_name);
    }
    closedir(dir);
}
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 下一节：[18.2 读目录](./18.2-directory.md)
- 前置：**前置**：[Ch17 ACL](../chapter-17-access-control-lists/notes.md) · [Ch14 FS/inode](../chapter-14-file-systems/notes.md) · [Ch15 stat/lstat](../chapter-15-file-attributes/notes.md)  
- 后置：**后置**：[Ch19 inotify](../chapter-19-monitoring-file-events/notes.md)

---

## man 手册参考

- `man 2 stat`
- `man 2 open`
