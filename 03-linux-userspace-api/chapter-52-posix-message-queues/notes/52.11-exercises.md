# 52.11 Exercises

> 本章：[TLPI 第 52 章](../README.md)

## 本节讲什么

第 52 章的练习题与参考答案，巩固 POSIX 消息队列的核心概念。

## 要点

### 练习 1：优先级订单队列

**题目**：实现一个订单处理队列，高优先级订单先处理。模拟 3 个不同优先级的订单发送和接收。

<details>
<summary>参考答案</summary>

```c
#include <mqueue.h>
#include <string.h>
#include <stdio.h>

typedef struct {
    int order_id;
    int quantity;
    char symbol[8];
} order_t;

int main() {
    struct mq_attr attr = { .mq_maxmsg = 100, .mq_msgsize = sizeof(order_t) };
    mqd_t mqd = mq_open("/orders", O_CREAT | O_RDWR, 0644, &attr);

    /* 发送不同优先级订单 */
    order_t o1 = { .order_id=1, .quantity=100, .symbol="AAPL" };
    order_t o2 = { .order_id=2, .quantity=200, .symbol="GOOG" };
    order_t o3 = { .order_id=3, .quantity=50,  .symbol="TSLA" };

    mq_send(mqd, (char*)&o1, sizeof(o1), 0);   /* 普通 */
    mq_send(mqd, (char*)&o2, sizeof(o2), 10);  /* 紧急 */
    mq_send(mqd, (char*)&o3, sizeof(o3), 5);   /* 高 */

    /* 接收：按优先级排序 */
    order_t recv;
    unsigned prio;
    while (mq_receive(mqd, (char*)&recv, sizeof(recv), &prio) != -1) {
        printf("[pri=%u] ID=%d %s x%d\n",
               prio, recv.order_id, recv.symbol, recv.quantity);
    }
    /* 输出:
       [pri=10] ID=2 GOOG x200    ← 紧急先出
       [pri=5]  ID=3 TSLA x50    ← 高优先级
       [pri=0]  ID=1 AAPL x100   ← 普通
    */

    mq_close(mqd);
    mq_unlink("/orders");
    return 0;
}
```

</details>

### 练习 2：超时接收

**题目**：实现一个带 5 秒超时的消息接收函数。超时后打印警告并继续等待。

<details>
<summary>参考答案</summary>

```c
#include <mqueue.h>
#include <time.h>
#include <stdio.h>

int recv_with_warning(mqd_t mqd, char *buf, size_t len,
                       unsigned *prio) {
    struct timespec ts;
    for (;;) {
        clock_gettime(CLOCK_REALTIME, &ts);
        ts.tv_sec += 5;  /* 5秒超时 */

        ssize_t n = mq_timedreceive(mqd, buf, len, prio, &ts);
        if (n >= 0) return (int)n;

        if (errno == ETIMEDOUT) {
            printf("WARNING: 5s timeout, still waiting...\n");
            continue;  /* 超时后继续等待 */
        }
        return -1;  /* 其他错误 */
    }
}
```

</details>

### 练习 3：异步通知

**题目**：使用 `mq_notify(SIGEV_THREAD)` 实现异步消息处理。比较信号通知和线程通知的优缺点。

<details>
<summary>参考答案</summary>

```c
#include <mqueue.h>
#include <stdio.h>
#include <string.h>

static mqd_t g_mqd;

static void on_msg(union sigval sv) {
    char buf[4096];
    unsigned prio;
    ssize_t n;

    while ((n = mq_receive(g_mqd, buf, sizeof(buf), &prio)) >= 0) {
        buf[n] = '\0';
        printf("Received: %s\n", buf);
    }

    /* 重新注册 */
    struct sigevent sev = {0};
    sev.sigev_notify = SIGEV_THREAD;
    sev.sigev_value.sival_ptr = (void *)g_mqd;
    sev.sigev_notify_function = on_msg;
    mq_notify(g_mqd, &sev);
}

int main() {
    struct mq_attr attr = { .mq_maxmsg = 10, .mq_msgsize = 4096 };
    g_mqd = mq_open("/notify_test", O_CREAT | O_RDWR, 0644, &attr);

    struct sigevent sev = {0};
    sev.sigev_notify = SIGEV_THREAD;
    sev.sigev_value.sival_ptr = (void *)g_mqd;
    sev.sigev_notify_function = on_msg;
    mq_notify(g_mqd, &sev);

    /* 主线程可以做其他工作 */
    pause();  /* 等待 */

    mq_close(g_mqd);
    mq_unlink("/notify_test");
    return 0;
}
```

**信号通知 vs 线程通知**：

| 方面 | SIGEV_SIGNAL | SIGEV_THREAD |
|------|-------------|--------------|
| 回调环境 | 信号处理函数（限制多） | 普通线程（可自由操作） |
| 异步安全 | 只能调用异步安全函数 | 可调用任意函数 |
| 响应延迟 | 低（信号中断主线程） | 需创建线程，稍高 |
| 复杂度 | 需管理信号掩码 | 线程管理简单 |
| 适用 | 简单唤醒 | 复杂处理逻辑 |

HFT 推荐 `SIGEV_THREAD`：可以在回调中直接做复杂操作（如解析行情、更新状态），不受信号安全限制。

</details>

## 衔接

- 上一节 [52.10 总结](./52.10-summary.md)
- 第 53 章将介绍 POSIX 信号量
