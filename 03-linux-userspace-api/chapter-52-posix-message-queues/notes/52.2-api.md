# 52.2 API 要点

> 本章：[TLPI 第 52 章 — POSIX Message Queues](./README.md)
> - 上一节：[52.1 特性](./52.1-section-52-1.md)
> - 下一节：[52.3 fork / exec](./52.3-fork-exec.md)

---

## 本节讲什么

mqd_t mq_open(const char *name, int oflag, /* mode, attr if O_CREAT */); int mq_send(mqd_t, const char *ptr, size_t len, unsigned prio); ssize_t mq_receive(mqd_t, char *ptr, size_t len, unsigned *prio);

---

## 要点

```c
mqd_t mq_open(const char *name, int oflag, /* mode, attr if O_CREAT */);
int mq_send(mqd_t, const char *ptr, size_t len, unsigned prio);
ssize_t mq_receive(mqd_t, char *ptr, size_t len, unsigned *prio);
int mq_close(mqd_t);  int mq_unlink(const char *name);
```

- 名：`/myqueue`；`O_NONBLOCK` 挂在**描述符**上  
- `msg_len` ≤ `mq_msgsize` 否则 `EMSGSIZE`；0 长合法  
- `mq_setattr` **只能改** `mq_flags`（NONBLOCK）  
- timed*：超时为 **绝对时间**（`CLOCK_REALTIME`）

`mq_getattr`：可读 `mq_curmsgs` 等。

Demo：[`code/`](./code/)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | API 要点 在嵌入式系统的应用场景 |
| HFT | API 要点 对低延迟和性能的影响 |

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[52.1 特性](./52.1-section-52-1.md)
- 下一节：[52.3 fork / exec](./52.3-fork-exec.md)
- 前置：**前置**：[Ch51 POSIX IPC 导论](../chapter-51-posix-ipc-intro/notes.md)  
- 后置：**后置**：[Ch53 POSIX 信号量](../chapter-53-posix-semaphores/notes.md)
