# 52.1 特性

> 本章：[TLPI 第 52 章 — POSIX Message Queues](../README.md)
> - 上一节：本章首页
> - 下一节：[52.2 API 要点](52.2-opening-closing-and-unlinking-a-message-.md)

---

## 本节讲什么

- 消息边界；优先级（数值越大越高；同级 FIFO） - `mqd_t` ≈ fd → Linux **select/poll/epoll** - unlink 删名；**全 close 才销毁**（丢残留消息）

---

## 要点

- 消息边界；优先级（数值越大越高；同级 FIFO）  
- `mqd_t` ≈ fd → Linux **select/poll/epoll**  
- unlink 删名；**全 close 才销毁**（丢残留消息）  
- 创建时固定 `mq_maxmsg` / `mq_msgsize`，运行期不可改  

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 特性 在嵌入式系统的应用场景 |
| HFT | 特性 对低延迟和性能的影响 |

## 代码示例

```c

/* POSIX 消息队列基本概念：mq_open + 属性查询 */
#include <stdio.h>
#include <stdlib.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <mqueue.h>

int main(void) {
    /* 创建消息队列，设置属性 */
    struct mq_attr attr = {
        .mq_flags = 0,        /* 0 = 阻塞模式 */
        .mq_maxmsg = 10,      /* 最大消息数 */
        .mq_msgsize = 256,    /* 单条消息最大字节数 */
        .mq_curmsgs = 0       /* 当前消息数（只读） */
    };

    mqd_t mq = mq_open("/concept_demo", O_CREAT | O_RDWR, 0644, &attr);
    if (mq == (mqd_t)-1) { perror("mq_open"); exit(1); }
    printf("MQ created: /concept_demo\n");

    /* 查询当前属性 */
    struct mq_attr cur;
    mq_getattr(mq, &cur);
    printf("  flags=%ld, maxmsg=%ld, msgsize=%ld, curmsgs=%ld\n",
           cur.mq_flags, cur.mq_maxmsg, cur.mq_msgsize, cur.mq_curmsgs);

    /* 设置非阻塞模式 */
    mq_setattr(mq, &(struct mq_attr){ .mq_flags = O_NONBLOCK }, NULL);
    mq_getattr(mq, &cur);
    printf("after setattr: flags=%ld (O_NONBLOCK=%d)\n",
           cur.mq_flags, O_NONBLOCK);

    mq_close(mq);
    mq_unlink("/concept_demo");
    return 0;
}

```

---

## 衔接

- 本章 README：[README](../README.md)
- 下一节：[52.2 API 要点](52.2-opening-closing-and-unlinking-a-message-.md)
- 前置：**前置**：[Ch51 POSIX IPC 导论](../../chapter-51-posix-ipc-intro/notes)  
- 后置：**后置**：[Ch53 POSIX 信号量](../../chapter-53-posix-semaphores/notes)
