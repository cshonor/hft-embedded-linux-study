# 52.8 epoll（Linux）

> 本章：[TLPI 第 52 章 — POSIX Message Queues](../README.md)
> - 上一节：[52.7 `mq_notify`（难点）](52.7-linux-specific-features.md)
> - 下一节：[52.9 限额](52.9-comparison-of-posix-and-system-v-message.md)

---

## 本节讲什么

有可读消息则 `mqd_t` 可读。规范未强制；**工程上 notify 与 epoll 二选一**。 ---

---

## 要点

有可读消息则 `mqd_t` 可读。规范未强制；**工程上 notify 与 epoll 二选一**。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | epoll（Linux） 在嵌入式系统的应用场景 |
| HFT | epoll（Linux） 对低延迟和性能的影响 |

## 代码示例

```c

/* POSIX MQ 配合 epoll（Linux 扩展：mqd_t 可作为 fd） */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <mqueue.h>
#include <sys/epoll.h>
#include <unistd.h>

int main(void) {
    struct mq_attr attr = { .mq_maxmsg = 10, .mq_msgsize = 64 };
    mqd_t mq = mq_open("/epoll_demo", O_CREAT | O_RDWR | O_NONBLOCK, 0644, &attr);
    if (mq == (mqd_t)-1) { perror("mq_open"); exit(1); }

    /* Linux 上 mqd_t 本身就是 fd，可直接加入 epoll */
    int epfd = epoll_create1(0);
    struct epoll_event ev = { .events = EPOLLIN, .data.fd = (int)mq };
    epoll_ctl(epfd, EPOLL_CTL_ADD, (int)mq, &ev);
    printf("MQ fd added to epoll\n");

    /* fork 子进程发消息 */
    pid_t pid = fork();
    if (pid == 0) {
        sleep(1);
        mqd_t cm = mq_open("/epoll_demo", O_WRONLY);
        mq_send(cm, "epoll msg", 10, 1);
        mq_close(cm);
        _exit(0);
    }

    /* 等待 epoll 通知 */
    struct epoll_event events[4];
    int n = epoll_wait(epfd, events, 4, 5000);
    if (n > 0) {
        printf("epoll returned %d event(s)\n", n);
        char buf[64];
        unsigned int prio;
        ssize_t len = mq_receive(mq, buf, sizeof(buf), &prio);
        if (len >= 0) {
            buf[len] = '\0';
            printf("received: '%s' (prio=%u)\n", buf, prio);
        }
    }

    wait(NULL);
    close(epfd);
    mq_close(mq);
    mq_unlink("/epoll_demo");
    return 0;
}

```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[52.7 `mq_notify`（难点）](52.7-linux-specific-features.md)
- 下一节：[52.9 限额](52.9-comparison-of-posix-and-system-v-message.md)
- 前置：**前置**：[Ch51 POSIX IPC 导论](../../chapter-51-posix-ipc-intro/notes)  
- 后置：**后置**：[Ch53 POSIX 信号量](../../chapter-53-posix-semaphores/notes)
