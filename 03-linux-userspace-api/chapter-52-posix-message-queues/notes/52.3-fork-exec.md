# 52.3 fork / exec

> 本章：[TLPI 第 52 章 — POSIX Message Queues](./README.md)
> - 上一节：[52.2 API 要点](./52.2-api.md)
> - 下一节：[52.7 `mq_notify`（难点）](./52.7-mqnotify.md)

---

## 本节讲什么

---

---

## 要点

| | |
|--|--|
| `fork` | 复制 `mqd_t`；**不**继承 `mq_notify` |
| `exec` / 退出 | 自动 `mq_close`；清除 notify |

---

---

## 与 C 语言学习的关系

本节涉及 `close()`，对应 K&R 8.3 节。C 语言中资源管理靠手动——没有 C++ 的 RAII。`close` 必须在所有代码路径上调用，这是 C 语言内存/资源管理的核心训练点。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | fork / exec 在嵌入式系统的应用场景 |
| HFT | fork / exec 对低延迟和性能的影响 |

---

## 常见陷阱

1. **fork 返回值未区分**

```c
pid_t pid = fork();
/* 父子都执行同一代码 */
```

fork 返回两次：父进程返回子 PID，子进程返回 0，失败返回 -1。必须区分三个分支。

2. **fd 继承导致泄漏**

```c
int fd = open(...);
fork();
/* 父子都有 fd，都不关 → 泄漏 */
```

fork 后子进程继承父的 fd 表。父子都需要 close 各自的 fd。

3. **缓冲区被复制**

```c
printf("hello");  /* 在缓冲区，未 flush */
fork();  /* 子进程也会输出 hello */
```

fork 前如果 stdio 缓冲区有数据，子进程会复制并重复输出。fork 前应 fflush 或用 write。

---

## 自测题

**题目 1（选择）：** `fork()` 在子进程中返回什么？
A. 子进程 PID
B. 0
C. -1
D. 父进程 PID

<details>
<summary>参考答案</summary>

B。子进程中 fork 返回 0，父进程中返回子进程 PID，失败返回 -1。

</details>

**题目 2（判断）：** `fork` 后子进程和父进程的执行顺序是确定的。

<details>
<summary>参考答案</summary>

错误。fork 后父子进程的执行顺序不确定，取决于调度器。不能用 sleep 来保证顺序。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
printf("before fork\n");
fork();
printf("after fork\n");
```

<details>
<summary>参考答案</summary>

如果 stdout 是行缓冲（终端），"before fork" 会立即输出。但如果是全缓冲（管道/文件），缓冲区被复制，子进程也会输出 "before fork"。fork 前应 fflush(stdout)。

</details>

**题目 4（应用）：** 写代码：fork 一个子进程，父进程打印子 PID，子进程打印自己的 PID。

<details>
<summary>参考答案</summary>

```c
pid_t pid = fork();
if (pid < 0) { perror("fork"); exit(1); }
if (pid == 0) {
    printf("child: %d\n", getpid());
} else {
    printf("parent: child=%d\n", pid);
}
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[52.2 API 要点](./52.2-api.md)
- 下一节：[52.7 `mq_notify`（难点）](./52.7-mqnotify.md)
- 前置：**前置**：[Ch51 POSIX IPC 导论](../chapter-51-posix-ipc-intro/notes.md)  
- 后置：**后置**：[Ch53 POSIX 信号量](../chapter-53-posix-semaphores/notes.md)

---

## man 手册参考

- `man 2 fork`
