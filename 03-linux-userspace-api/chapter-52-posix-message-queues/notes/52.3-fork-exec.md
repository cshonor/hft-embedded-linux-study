# 52.3 fork / exec

> 本章：[TLPI 第 52 章 — POSIX Message Queues](./README.md)
> - 上一节：[52.2 API 要点](./52.2-api.md)
> - 下一节：[52.7 `mq_notify`（难点）](./52.7-mqnotify.md)

---

## 本节讲什么

---

---

## 要点

| | |
|--|--|
| `fork` | 复制 `mqd_t`；**不**继承 `mq_notify` |
| `exec` / 退出 | 自动 `mq_close`；清除 notify |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | fork / exec 在嵌入式系统的应用场景 |
| HFT | fork / exec 对低延迟和性能的影响 |

## 代码示例

```c

/* POSIX MQ 在 fork/exec 后的继承行为 */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <mqueue.h>
#include <sys/wait.h>
#include <unistd.h>

int main(void) {
    struct mq_attr attr = { .mq_maxmsg = 10, .mq_msgsize = 64 };
    mqd_t mq = mq_open("/inherit_demo", O_CREAT | O_RDWR, 0644, &attr);
    if (mq == (mqd_t)-1) { perror("mq_open"); exit(1); }

    /* 父进程发送消息 */
    mq_send(mq, "from parent", 12, 1);

    /* fork：子进程继承 mqd_t 描述符（同一队列引用） */
    pid_t pid = fork();
    if (pid == 0) {
        /* 子进程可以用继承的 mq 接收 */
        char buf[64];
        unsigned int prio;
        ssize_t n = mq_receive(mq, buf, sizeof(buf), &prio);
        if (n >= 0) {
            buf[n] = '\0';
            printf("child received: '%s' (prio=%u)\n", buf, prio);
        }
        /* 注意：mqd_t 在 exec 后不保留（不是 fd） */
        _exit(0);
    }
    wait(NULL);

    /* exec 后描述符丢失，但队列对象持久存在 */
    printf("fork: mqd_t inherited; exec: mqd_t NOT inherited\n");
    printf("queue object persists until mq_unlink or reboot\n");

    mq_close(mq);
    mq_unlink("/inherit_demo");
    return 0;
}

```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[52.2 API 要点](./52.2-api.md)
- 下一节：[52.7 `mq_notify`（难点）](./52.7-mqnotify.md)
- 前置：**前置**：[Ch51 POSIX IPC 导论](../chapter-51-posix-ipc-intro/notes.md)  
- 后置：**后置**：[Ch53 POSIX 信号量](../chapter-53-posix-semaphores/notes.md)
