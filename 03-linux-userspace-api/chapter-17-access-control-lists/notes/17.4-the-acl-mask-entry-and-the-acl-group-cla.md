# 17.4 Default ACL 继承

> 本章：[TLPI 第 17 章 — Access Control Lists](../README.md)
> - 上一节：[17.3 内核判定顺序（示意）](17.3-long-and-short-text-forms-for-acls.md)
> - 下一节：17.5 libacl API（链接 `-lacl`）

---

## 本节讲什么

父目录设 Default ACL 后： ---

---

## 要点

父目录设 Default ACL 后：

| 新建对象 | 行为 |
|----------|------|
| 普通文件 | Default → 自身 Access ACL；创建 `mode` 仍约束；**umask 通常不再按传统方式生效** |
| 子目录 | Access + **自己的 Default** 都从父 Default 继承（可继续向下传） |

> Default **不改**目录里**已有**文件，只影响之后新建的对象。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 父目录 Default ACL 只影响新建对象（文件→Access ACL，子目录→Access+Default 继续下传），不改已有文件 |
| HFT | 用目录 Default ACL 让新生成的日志/配置文件自动带上正确权限，免逐个 `chmod` |

## 代码示例

```c
#include <sys/acl.h>
#include <stdio.h>

/* Default ACL: 仅对目录有效
 * 新创建的文件/子目录自动继承目录的 default ACL
 */
int main(void) {
    /* 获取目录的 default ACL */
    acl_t def_acl = acl_get_file("/tmp", ACL_TYPE_DEFAULT);
    if (def_acl == NULL) {
        printf("/tmp 无 default ACL (常见)\n");
    } else {
        char *text = acl_to_text(def_acl, NULL);
        if (text) {
            printf("Default ACL:\n%s\n", text);
            acl_free(text);
        }
        acl_free(def_acl);
    }

    /* 设置 default ACL:
     * acl_t acl = acl_from_text("u::rwx,g::rwx,o::rwx");
     * acl_set_file("/tmp/mydir", ACL_TYPE_DEFAULT, acl);
     */
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[17.3 内核判定顺序（示意）](17.3-long-and-short-text-forms-for-acls.md)
- 下一节：17.5 libacl API（链接 `-lacl`）
- 前置：**前置**：[Ch16 Extended Attributes](../../chapter-16-extended-attributes/notes)  
- 后置：**后置**：[Ch18 Directories and Links](../../chapter-18-directories-links/notes) · [Ch38](../../chapter-38-secure-privileged/notes) · [Ch39 Capabilities](../../chapter-39-capabilities/notes)
