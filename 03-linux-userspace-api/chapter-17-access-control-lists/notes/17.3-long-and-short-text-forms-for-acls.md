# 17.3 内核判定顺序（示意）

> 本章：[TLPI 第 17 章 — Access Control Lists](../README.md)
> - 上一节：[17.2 `ACL_MASK`（核心坑）](17.2-acl-permission-checking-algorithm.md)
> - 下一节：[17.4 Default ACL 继承](17.4-the-acl-mask-entry-and-the-acl-group-cla.md)

---

## 本节讲什么

1. root → 放行（执行还需有 x，细节见手册） 2. EUID == 所有者 → `USER_OBJ` 3. 匹配某 `ACL_USER` → 权限 & MASK

---

## 要点

1. root → 放行（执行还需有 x，细节见手册）  
2. EUID == 所有者 → `USER_OBJ`  
3. 匹配某 `ACL_USER` → 权限 & MASK  
4. EGID/附加组匹配 `ACL_GROUP` / `GROUP_OBJ` → & MASK  
5. → `ACL_OTHER`  
6. 否则拒绝  

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 内核按 owner→ACL_USER→GROUP_OBJ/ACL_GROUP→OTHER 顺序判定，先匹配先生效 |
| HFT | 理解判定顺序才能预测多规则冲突时的实际授权，避免权限配置出现意外放行/拒绝 |

## 代码示例

```c
#include <sys/acl.h>
#include <stdio.h>

/* ACL 条目类型:
 * ACL_USER_OBJ  - 属主权限 (对应传统 owner rwx)
 * ACL_USER      - 指定用户权限
 * ACL_GROUP_OBJ - 属组权限 (对应传统 group rwx)
 * ACL_GROUP     - 指定组权限
 * ACL_MASK      - 最大有效权限过滤器
 * ACL_OTHER     - 其他用户 (对应传统 other rwx)
 */
int main(void) {
    acl_t acl = acl_get_file("/tmp", ACL_TYPE_ACCESS);
    if (acl == NULL) { perror("acl_get_file"); return 1; }

    acl_entry_t entry;
    acl_tag_t tag;
    int entry_id = ACL_FIRST_ENTRY;

    printf("=== ACL 条目 ===\n");
    while (acl_get_entry(acl, entry_id, &entry) == 1) {
        entry_id = ACL_NEXT_ENTRY;
        acl_get_tag_type(entry, &tag);
        switch (tag) {
            case ACL_USER_OBJ:  printf("user::"); break;
            case ACL_USER:      printf("user:uid:"); break;
            case ACL_GROUP_OBJ: printf("group::"); break;
            case ACL_GROUP:     printf("group:gid:"); break;
            case ACL_MASK:      printf("mask::"); break;
            case ACL_OTHER:     printf("other::"); break;
            default: printf("unknown:"); break;
        }
        printf("rwx\n");
    }
    acl_free(acl);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[17.2 `ACL_MASK`（核心坑）](17.2-acl-permission-checking-algorithm.md)
- 下一节：[17.4 Default ACL 继承](17.4-the-acl-mask-entry-and-the-acl-group-cla.md)
- 前置：**前置**：[Ch16 Extended Attributes](../../chapter-16-extended-attributes/notes)  
- 后置：**后置**：[Ch18 Directories and Links](../../chapter-18-directories-links/notes) · [Ch38](../../chapter-38-secure-privileged/notes) · [Ch39 Capabilities](../../chapter-39-capabilities/notes)
