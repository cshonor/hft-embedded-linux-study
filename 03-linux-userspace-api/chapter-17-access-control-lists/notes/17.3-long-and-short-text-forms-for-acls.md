# 17.3 Long and Short Text Forms for ACLs ACL 的两种文本形式

> 本章：[TLPI 第 17 章 — Access Control Lists](../README.md)
> 平台标注：`getfacl/setfacl` 文本协议属 Linux（libacl）生态；文本形式本身是**规范内容**，值得掌握
> 源码核验：man-pages **6.19** `acl(5)`（TEXT FORM 段）+ TLPI §17.3
> - 上一节：[17.2 判定算法](17.2-acl-permission-checking-algorithm.md)
> - 下一节：[17.4 The ACL Mask Entry](17.4-the-acl-mask-entry-and-the-acl-group-cla.md)

---

## 两种形式

**长形式**（long，每条目一行带全名，getfacl 默认输出）：

```
user::rw-
user:1001:r--           #effective:r--
group::r--
group:1002:-wx          #effective:-w-
mask::rw-
other::r--
```

**短形式**（short，setfacl -m 的参数格式）：

```
u::rw-,u:1001:r--,g::r--,g:1002:-wx,m::rw-,o::r--
```

要点：

- tag 可缩写（`u`/`g`/`o`/`m`），`ACL_USER_OBJ` 与 `ACL_GROUP_OBJ` 用 `u::` / `g::`（**双冒号**表示"无 id"），named 条目 `u:1001:r--`（**单冒号**接 id/名字）；
- id 与名字可互换（`u:liming:r--`），存储时统一为数字 id；
- 长形式里的 `#effective:` 注释是 getfacl 替你算好的"该条 AND mask"——短形式没有它。

**注释位（`#` 开头行）会被 setfacl 的解析器忽略**——所以 getfacl 的输出可以直接回灌给 setfacl：

```bash
getfacl file > acl.txt        # 备份
setfacl --restore=acl.txt     # 恢复（或 setfacl -M acl.txt file）
```

这是 ACL 权限模型做**版本化/迁移**的标准手法（chmod 做不到——9 位权限的语义上下文丢了）。

---

## 常见坑

| 坑 | 正解 |
|----|------|
| `u:1001` 与 `u::` 混写 | 双冒号=USER_OBJ（无 id），单冒号=named（带 id）；setfacl 对此敏感 |
| 忽略 `#effective` 注释 | 它就是 mask 生效后的真实权限，评审 ACL 文件先看它 |
| 拿 chmod 恢复权限备份 | chmod 只覆盖 USER_OBJ/GROUP_OBJ/OTHER 三条；named 条目与 mask 会原样残留 |

---

## HFT / 嵌入式关联

| 场景 | 本节机制怎么兑现 |
|------|----------------|
| 权限即代码 | `getfacl -R` 快照进 git，部署时 `--restore`——与 IaC 同一哲学 |
| 事故复盘 | `#effective` 列是"为什么他进不去"的一手证据（named 条目常被 mask 削） |

---

## 衔接

- 上一节：[17.2 判定算法](17.2-acl-permission-checking-algorithm.md)
- 下一节：[17.4 mask](17.4-the-acl-mask-entry-and-the-acl-group-cla.md)
- [17.5 getfacl/setfacl](17.5-the-getfacl-and-setfacl-commands.md) — 文本形式的工程化
