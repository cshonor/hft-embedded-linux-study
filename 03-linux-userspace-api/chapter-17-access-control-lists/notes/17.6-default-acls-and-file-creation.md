# 17.6 Default ACLs and File Creation 默认 ACL 与文件创建

> 本章：[TLPI 第 17 章 — Access Control Lists](../README.md)
> 平台标注：Linux 专有实现（`system.posix_acl_default` xattr）；语义按 POSIX draft 讲解
> 源码核验：man-pages **6.19** `acl(5)`（DEFAULT ACL 段）+ TLPI §17.6
> - 上一节：[17.5 getfacl/setfacl](17.5-the-getfacl-and-setfacl-commands.md)
> - 下一节：[17.7 Implementation Limits](17.7-acl-implementation-limits.md)

---

## 本节讲什么

**default ACL 只属于目录**：它不影响目录自身的访问控制（那是 access ACL），而是**新文件的权限模板**。本节钉住继承规则——它是"SGID 目录"方案的 ACL 加强版。

---

## 继承规则

目录带 default ACL 时，在其中**新建**对象：

| 新对象 | access ACL | default ACL |
|--------|-----------|-------------|
| 文件 | 继承 default ACL（权限 = 模板 **AND** open 的 mode 参数） | 无 |
| 目录 | 继承（同上） | **同时继承**目录的 default ACL（递归传播） |

关键细节：

1. **AND 语义**：default 模板是上限，`open(path, O_CREAT, 0644)` 的 0644 与模板逐位相与——模板给 rwx、mode 给 644 → 得 644。mode 参数仍是"第一道裁剪"；
2. **umask 在有 default ACL 时被忽略**——这是书上的重点：默认 umask 022 会悄悄砍权限，default ACL 让这个坑消失（对需要"创建即组可写"的共享目录是天大的好事）；
3. mask 自动生成（继承后取组类并集）。

## 与 15.3.1 新文件属组规则的关系

| 机制 | 作用 |
|------|------|
| SYSV 新文件属组 = 进程 egid | 谁创建，谁负责 |
| 目录 SGID | 新文件属组强制 = 目录属组 |
| **default ACL** | 不止属组：连 named 条目、mask、other 一起做成模板 |

三层是叠加关系；default ACL 是表达能力最强的一层。

---

## 常见坑

| 坑 | 正解 |
|----|------|
| 以为 default ACL 会改目录自身的权限 | 不会；它只管"未来新建的对象"。目录自己的控制靠 access ACL |
| open mode 给 0000 还想靠 default ACL 救 | AND 语义下 0000 AND anything = 0000；应用层 mode 必须给足 |
| 期望 default ACL 被 cp 继承 | cp 创建的新文件走 open mode；只有内核分配 inode 的"新建"才继承 |
| 忘了给已有目录补 default | 只对"设置之后"的新建生效；存量文件要 setfacl -m 批量补 |

---

## HFT / 嵌入式关联

| 场景 | 本节机制怎么兑现 |
|------|----------------|
| 共享落盘目录 | default ACL `g:traders:rw-` + 忽略 umask → 多账户写入即组可读，监控进程免 root 读 |
| 数据采集目录 | 子目录自动继承 default（递归传播），树型权限零维护 |
| 归档分层 | 归档目录 default 收紧到 `other::---`，新文件即私有 |

---

## 衔接

- 上一节：[17.5 getfacl/setfacl](17.5-the-getfacl-and-setfacl-commands.md)
- 下一节：[17.7 Implementation Limits](17.7-acl-implementation-limits.md)
- [15.3.1 新文件属主](../../chapter-15-file-attributes/notes/15.3-file-ownership.md) — SGID 目录方案（本节的前身）
- [15.4.6 umask](../../chapter-15-file-attributes/notes/15.4-file-permissions.md) — 被 default ACL 忽略的那个

---

<details>
<summary>代码自测（Q&A）</summary>

**Q1：default ACL 存在哪？**
A：`system.posix_acl_default` xattr，挂在**目录**上（与目录自己的 access ACL 分开）。

**Q2：default 模板 rwx，open mode 0644，结果？**
A：逐位 AND → rw-r--r--。模板是上限，mode 仍是第一道裁剪。

**Q3：有 default ACL 的目录里，umask 还起作用吗？**
A：不起。继承路径完全由 default 模板 AND mode 决定——这正是它能治"umask 022 砍共享权限"的原因。

**Q4：新建子目录会怎样？**
A：access ACL 继承模板，且**同时**把 default ACL 也继承下去——权限模型随树自动传播。

</details>
