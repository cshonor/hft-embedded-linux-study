# 17.7 ACL Implementation Limits

> 本章：[TLPI 第 17 章](../README.md)

## 本节讲什么

ACL（访问控制列表）的实现限制：每文件最大 ACE 数量、ACL 大小限制、文件系统支持差异。

## 要点

### 系统限制

| 限制 | 典型值 | 来源 |
|------|--------|------|
| 每文件最大 ACE | 25 | ext2/ext3 |
| 每文件最大 ACE | 无限（动态） | ext4/xfs/btrfs |
| ACL 总大小 | 32KB | 内核 |
| 命名空间数 | 1-3 | user/group/other |

### 文件系统支持

| 文件系统 | ACL 支持 | 需挂载选项 |
|---------|---------|-----------|
| ext4 | 是 | 默认开启 |
| xfs | 是 | 默认开启 |
| btrfs | 是 | 默认开启 |
| ext2/3 | 是 | `acl` 挂载选项 |
| tmpfs | 是 | `acl` 选项 |
| FAT/NTFS | 模拟 | 不原生 |
| NFS v4 | 是 | 协议级 |

### ACE（Access Control Entry）结构

```
user::rw-          # 文件属主
user:hft:rwx       # 特定用户
group::r--         # 文件属组
group:traders:rw- # 特定组
mask::rwx          # 最大有效权限掩码
other::r--         # 其他用户
```

### mask 的作用

`mask` 限制 named user 和 named group 的最大有效权限：

```
user:hft:rwx  # 请求 rwx
mask::r-x     # mask 只允许 r-x
→ hft 实际权限 = rwx & r-x = r-x
```

### 查看和设置

```bash
getfacl file           # 查看 ACL
setfacl -m u:hft:rwx file   # 设置用户权限
setfacl -m g:traders:rw file # 设置组权限
setfacl -m m::rwx file      # 设置 mask
setfacl -b file            # 删除所有 ACL
```

## HFT / 嵌入式关联

- 交易系统文件权限精细控制：不同策略组访问不同数据
- 嵌入式多用户场景：限制配置文件访问
- 注意文件系统是否原生支持 ACL

## 代码示例

```bash
#!/bin/bash
# 设置 HFT 目录 ACL

# 策略组可读写行情数据
setfacl -R -m g:strategy:rw /data/market/

# 风控组只读
setfacl -R -m g:riskcontrol:r /data/market/

# 开发组无权限
setfacl -R -m g:dev:--- /data/market/

# 验证
getfacl /data/market/
```

## 衔接

- [17.6 ACL 语义](17.2-acl-permission-checking-algorithm.md) | [17.8 ACL API](./17.8-the-acl-api.md)

## 代码自测

<details>
<summary>mask 在 ACL 中起什么作用？</summary>

mask 限定了所有 named user 和 named group ACE 的最大有效权限。实际有效权限 = ACE 权限 ∩ mask 权限。例如用户 ACE 设为 rwx 但 mask 为 r-x，则实际有效权限为 r-x。mask 的作用是集中控制所有额外用户的权限上限，无需逐个修改。

</details>
