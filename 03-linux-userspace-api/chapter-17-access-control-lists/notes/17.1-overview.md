# 17.1 基础概念

> 本章：[TLPI 第 17 章 — Access Control Lists](../README.md)
> - 上一节：本章首页
> - 下一节：[17.2 `ACL_MASK`（核心坑）](17.2-acl-permission-checking-algorithm.md)

---

## 本节讲什么

ACL = 多条 **ACE**：`tag + [qualifier uid/gid] + 权限(rwx)`。 ---

---

## 要点

ACL = 多条 **ACE**：`tag + [qualifier uid/gid] + 权限(rwx)`。

### 六种 Tag

| 常量 | 文本 | 说明 |
|------|------|------|
| `ACL_USER_OBJ` | `user::` | 所有者（固定 1） |
| `ACL_USER` | `user:uid:` | 命名用户（可多条） |
| `ACL_GROUP_OBJ` | `group::` | 属组（固定 1） |
| `ACL_GROUP` | `group:gid:` | 命名组（可多条） |
| `ACL_MASK` | `mask::` | **有效权限上限**（扩展 ACL 必需） |
| `ACL_OTHER` | `other::` | 其他人（固定 1） |

### 两类 ACL

| 类型 | 作用 | xattr |
|------|------|-------|
| **Access ACL** | 控制该对象访问 | `system.posix_acl_access` |
| **Default ACL** | **仅目录**；不控制本目录访问，供**新建**子对象继承 | `system.posix_acl_default` |

### 最小 vs 扩展

| | 组成 | MASK |
|--|------|------|
| **最小 ACL** | USER_OBJ + GROUP_OBJ + OTHER | 不需要（≈ 传统 mode） |
| **扩展 ACL** | 另有 `ACL_USER` / `ACL_GROUP` | **必须有** `ACL_MASK` |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | ACL 六种 tag（USER_OBJ/USER/GROUP_OBJ/GROUP/MASK/OTHER），Access ACL 控访问 / Default ACL 仅目录供继承 |
| HFT | 多用户/多进程部署用 ACL 精细授权（风控用户只读、交易用户读写），比传统 rwx 更细粒度 |

## 代码示例

```c
#include <sys/acl.h>
#include <stdio.h>

/* ACL (访问控制列表): 比 rwx 更细粒度的权限控制
 * 标准 POSIX 权限: 属主/组/其他 各 rwx
 * ACL: 可以给特定用户/组单独授权
 *
 * 需要 libacl: gcc -lacl
 */
int main(void) {
    /* 获取文件的 ACL */
    acl_t acl = acl_get_file("/tmp", ACL_TYPE_ACCESS);
    if (acl == NULL) {
        perror("acl_get_file");
        printf("(可能不支持 ACL 或未安装 libacl)\n");
        return 1;
    }

    /* 转为文本格式 */
    char *text = acl_to_text(acl, NULL);
    if (text) {
        printf("ACL:\n%s\n", text);
        acl_free(text);
    }
    acl_free(acl);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 下一节：[17.2 `ACL_MASK`（核心坑）](17.2-acl-permission-checking-algorithm.md)
- 前置：**前置**：[Ch16 Extended Attributes](../../chapter-16-extended-attributes/notes)  
- 后置：**后置**：[Ch18 Directories and Links](../../chapter-18-directories-links/notes) · [Ch38](../../chapter-38-secure-privileged/notes) · [Ch39 Capabilities](../../chapter-39-capabilities/notes)
