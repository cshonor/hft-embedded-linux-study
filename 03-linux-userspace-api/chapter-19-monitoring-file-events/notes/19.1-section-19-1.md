# 19.1 技术演进

> 本章：[TLPI 第 19 章 — Monitoring File Events](./README.md)
> - 上一节：本章首页
> - 下一节：[19.3 `struct inotify_event`](./19.3-struct-inotifyevent.md)

---

## 本节讲什么

---

---

## 要点

| 方案 | 评价 |
|------|------|
| 轮询 `readdir`/`stat` | 慢、费资源 |
| **dnotify** | 信号驱动、笨重，已淘汰 |
| **inotify** | 推荐 |
| fanotify | 可拦截访问；审计进阶，本章不展开 |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 技术演进 在嵌入式系统的应用场景 |
| HFT | 技术演进 对低延迟和性能的影响 |

## 代码示例

```c
#include <sys/inotify.h>
#include <unistd.h>
#include <stdio.h>
int main(void) {
    int fd = inotify_init();
    if (fd < 0) { perror("inotify_init"); return 1; }
    int wd = inotify_add_watch(fd, "/tmp", IN_CREATE | IN_DELETE | IN_MODIFY);
    printf("watching /tmp (wd=%d), 等待事件...\n", wd);
    char buf[4096];
    ssize_t n = read(fd, buf, sizeof(buf));
    if (n > 0) {
        struct inotify_event *ev = (struct inotify_event *)buf;
        printf("事件: wd=%d mask=0x%x name=%s\n", ev->wd, ev->mask, ev->name);
    }
    close(fd);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 下一节：[19.3 `struct inotify_event`](./19.3-struct-inotifyevent.md)
- 前置：**前置**：[Ch18 Directories and Links](../chapter-18-directories-links/notes.md)  
- 后置：**后置**：[Ch20 Signals](../chapter-20-signals-fundamentals/notes.md) · [Ch63 多路 I/O](../chapter-63-alternative-io/notes.md)
