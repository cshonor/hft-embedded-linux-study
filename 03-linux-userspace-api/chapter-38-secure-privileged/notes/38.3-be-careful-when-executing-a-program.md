# 38.3 安全准则（精要）

> 本章：[TLPI 第 38 章 — Writing Secure Privileged Programs](../README.md)
> - 上一节：[38.2 丢弃与恢复（核心）](38.2-operate-with-least-privilege.md)
> - 下一节：下一章+1:02d}-*/notes.md)

---

## 本节讲什么

1. **尽量别写 SUID**；改权限 / 专用 daemon / **Capability** 2. **最小特权**：特权代码短；默认无特权跑业务 3. 不再需要 → **永久** `setuid(getuid())`

---

## 要点

1. **尽量别写 SUID**；改权限 / 专用 daemon / **Capability**  
2. **最小特权**：特权代码短；默认无特权跑业务  
3. 不再需要 → **永久** `setuid(getuid())`  
4. **环境**：勿信 PATH/`LD_*`；清空后只注可信变量；用**绝对路径** `exec*`，禁 `execlp`/`execvp`/`system`/`popen`  
5. **禁 shell**：`system`/`popen` → 注入  
6. **TOCTOU**：勿 `stat` 再 `open`；`open`（可加 `O_NOFOLLOW`）+ `fstat(fd)`  
7. 符号链接：可写目录慎放敏感文件  
8. **校验一切输入**；防 `../`  
9. 创建文件：`umask` + 显式 mode + `O_EXCL`；少用世界可写 `/tmp` 存秘密  
10. `RLIMIT_CORE=0` 防敏感 core；信号里不做特权重活  
11. `O_CLOEXEC` / 关多余 fd  
12. 勿信 stdin/stdout/stderr  

Demo：[`code/open_fstat_safe.c`](../code/open_fstat_safe.c) · [`code/no_system_exec.c`](../code/no_system_exec.c)（注释对照）

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 安全准则（精要） 在嵌入式系统的应用场景 |
| HFT | 安全准则（精要） 对低延迟和性能的影响 |

## 代码示例

```c
#include <unistd.h>
#include <stdio.h>
int main(void) {
    /* SUID 程序常见攻击:
     * 1. LD_PRELOAD 注入 (Linux 自动忽略 SUID 程序的 LD_*)
     * 2. 硬链接攻击 (SUID 程序被替换)
     * 3. TOCTOU 竞态
     * 4. 缓冲区溢出
     * 5. PATH 劫持
     */
    printf("SUID 安全: 清环境 → 丢特权 → 防溢出 → 检链接\n");
    /* 清除危险环境变量 */
    unsetenv("LD_PRELOAD");
    unsetenv("LD_LIBRARY_PATH");
    unsetenv("IFS");
    printf("已清除 LD_PRELOAD/LD_LIBRARY_PATH/IFS\n");
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[38.2 丢弃与恢复（核心）](38.2-operate-with-least-privilege.md)
- 前置：**前置**：[Ch9 凭证](../../chapter-09-process-credentials/notes) · [Ch37 Daemons](../../chapter-37-daemons/notes)  
- 后置：**后置**：[Ch39 Capabilities](../../chapter-39-capabilities/notes)
