# 38.13 Exercises

> 本章：[TLPI 第 38 章](./README.md)

## 本节讲什么

第 38 章的练习题与参考答案，巩固安全特权程序设计的核心概念。

## 要点

### 练习 1：安全权限丢弃

**题目**：编写一个函数，在绑定 80 端口后丢弃 root 权限。需要考虑哪些安全因素？

<details>
<summary>参考答案</summary>

```c
#include <sys/socket.h>
#include <netinet/in.h>
#include <unistd.h>
#include <grp.h>
#include <syslog.h>

int bind_and_drop(int port)
{
    int sockfd;
    struct sockaddr_in addr = {0};

    /* 1. 以 root 身份创建 socket 并绑定特权端口 */
    sockfd = socket(AF_INET, SOCK_STREAM | SOCK_CLOEXEC, 0);
    if (sockfd < 0) return -1;

    int opt = 1;
    setsockopt(sockfd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));

    addr.sin_family = AF_INET;
    addr.sin_addr.s_addr = INADDR_ANY;
    addr.sin_port = htons(port);

    if (bind(sockfd, (struct sockaddr *)&addr, sizeof(addr)) < 0) {
        close(sockfd);
        return -1;
    }

    /* 2. 清除补充组（必须在 setgid 前） */
    if (setgroups(0, NULL) < 0) {
        syslog(LOG_ERR, "setgroups: %m");
        close(sockfd);
        return -1;
    }

    /* 3. 设置 GID */
    gid_t gid = getgid();
    if (setgid(gid) < 0) {
        syslog(LOG_ERR, "setgid: %m");
        close(sockfd);
        return -1;
    }

    /* 4. 设置 UID（不可逆） */
    uid_t uid = getuid();
    if (setuid(uid) < 0) {
        syslog(LOG_ERR, "setuid: %m");
        close(sockfd);
        return -1;
    }

    /* 5. 验证权限已丢弃 */
    if (setuid(0) != -1) {
        syslog(LOG_CRIT, "Failed to drop root!");
        _exit(1);
    }

    return sockfd;
}
```

安全因素：
- 先绑定再丢权限（顺序不可反）
- setgroups 必须在 setgid 前
- setuid 是最后一步且不可逆
- 必须验证 setuid(0) 失败
- FD 设置 CLOEXEC 防止泄漏
- 错误时安全退出

</details>

### 练习 2：临时文件安全

**题目**：为什么以下代码不安全？如何修正？

```c
/* 不安全版本 */
char tmpl[] = "/tmp/myappXXXXXX";
mktemp(tmpl);           /* 只生成文件名 */
int fd = open(tmpl, O_RDWR | O_CREAT, 0644);  /* 竞争窗口！ */
```

<details>
<summary>参考答案</summary>

`mktemp` 只生成文件名但不创建文件，在 `mktemp` 返回和 `open` 创建之间存在 TOCTOU 竞争窗口。攻击者可以预测文件名或抢先创建同名文件/符号链接。

修正方案：

```c
char tmpl[] = "/tmp/myappXXXXXX";
int fd = mkstemp(tmpl);   /* 原子创建，权限 0600 */
if (fd < 0) return -1;

/* 创建后立即 unlink，fd 关闭后文件自动删除 */
unlink(tmpl);

/* 设置 CLOEXEC */
fcntl(fd, F_SETFD, FD_CLOEXEC);
```

`mkstemp` 使用 `O_CREAT | O_EXCL` 原子创建，消除竞争窗口。默认权限 0600（仅属主可读写）。

</details>

### 练习 3：整数溢出

**题目**：以下代码有什么安全漏洞？

```c
char *read_data(int count, int size) {
    char *buf = malloc(count * size);  /* 整数溢出！ */
    /* ... 读取数据 ... */
    return buf;
}
```

<details>
<summary>参考答案</summary>

`count * size` 可能整数溢出。例如 `count = 0x10000, size = 0x10000`，乘积为 `0x100000000`，在 32 位系统上溢出为 0。`malloc(0)` 返回 NULL 或小缓冲区，后续写入造成堆溢出。

修正：

```c
char *read_data(size_t count, size_t size) {
    /* 检查乘法溢出 */
    if (count != 0 && count > SIZE_MAX / size) {
        errno = ENOMEM;
        return NULL;
    }
    char *buf = malloc(count * size);
    if (buf == NULL) return NULL;
    /* ... 读取数据 ... */
    return buf;
}
```

使用 `size_t` 避免负数，显式检查乘法溢出。

</details>

## 衔接

- 第 39 章将介绍 Linux capabilities，提供更细粒度的权限控制
- 第 36 章详细介绍了 `setrlimit` 资源限制机制
