# 38.1 两类特权程序

> 本章：[TLPI 第 38 章 — Writing Secure Privileged Programs](./README.md)
> - 上一节：本章首页
> - 下一节：[38.2 丢弃与恢复（核心）](./38.2-section-38-2.md)

---

## 本节讲什么

凭证细节见 Ch9：RUID / EUID / Saved-UID / FSUID。Saved-UID 支撑临时降权后再提权。 ---

---

## 要点

| 类型 | |
|------|--|
| root 启动的 daemon | 启动 EUID=0，可再降权 |
| SUID/SGID 文件 | exec 后 EUID=文件属主（如 `passwd`） |

凭证细节见 Ch9：RUID / EUID / Saved-UID / FSUID。Saved-UID 支撑临时降权后再提权。

---

---

## 与 C 语言学习的关系

`exec` 家族与 `fork` 配合是 Unix 进程模型的基石。C 语言的 `main` 函数参数 `argc/argv` 在 `exec` 时传入。理解 `execvp` 如何搜索 PATH 需要理解 C 字符串数组和 NULL 终止约定。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 两类特权程序 在嵌入式系统的应用场景 |
| HFT | 两类特权程序 对低延迟和性能的影响 |

---

## 常见陷阱

1. **fd 泄漏到 exec**

```c
int fd = open(...);
execvp(prog, argv);  /* fd 仍然打开 */
```

默认情况下 exec 后 fd 仍然打开。设 FD_CLOEXEC 或用 O_CLOEXEC 防止泄漏。

2. **exec 后代码继续执行**

```c
execvp(prog, argv);
printf("这里会执行吗?");  /* 会! exec 失败时 */
```

exec 成功时不返回，当前进程映像被替换。只有 exec 失败时才返回 -1。必须检查返回值。

3. **PATH 搜索差异**

```c
execlp("ls", "ls", NULL);   /* 搜索 PATH */
execl("/bin/ls", "ls", NULL);  /* 不搜索 PATH */
```

execlp/execvp 搜索 PATH，execl/execv 不搜索。用错可能找不到程序。

---

## 自测题

**题目 1（选择）：** `exec` 成功后会发生什么？
A. 创建新进程
B. 替换当前进程映像
C. 返回旧进程
D. 什么也不发生

<details>
<summary>参考答案</summary>

B。exec 用新程序替换当前进程映像。PID 不变，代码段/数据段/堆栈被替换。成功时不返回。

</details>

**题目 2（判断）：** `exec` 失败时返回 -1，原程序继续执行。

<details>
<summary>参考答案</summary>

正确。exec 失败时返回 -1，当前进程映像不变，代码继续执行。成功时不返回。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
execvp("ls", args);
perror("exec failed");
```

<details>
<summary>参考答案</summary>

没有检查 execvp 返回值。应该：
`if (execvp("ls", args) < 0) { perror("exec"); exit(1); }`
perror 本身没问题，但应该配合 exit 确保失败后退出。

</details>

**题目 4（应用）：** 写代码：fork + exec 执行 `ls -l`，父进程等待子进程结束。

<details>
<summary>参考答案</summary>

```c
pid_t pid = fork();
if (pid == 0) {
    execlp("ls", "ls", "-l", NULL);
    perror("exec"); exit(1);
}
wait(NULL);
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 下一节：[38.2 丢弃与恢复（核心）](./38.2-section-38-2.md)
- 前置：**前置**：[Ch9 凭证](../chapter-09-process-credentials/notes.md) · [Ch37 Daemons](../chapter-37-daemons/notes.md)  
- 后置：**后置**：[Ch39 Capabilities](../chapter-39-capabilities/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
