# 6.1 进程基本概念

> 本章：[TLPI 第 06 章 — Processes](../README.md)
> - 上一节：本章首页
> - 下一节：[6.2 PID 与 PPID](./6.2-pid-ppid.md)

---

## 本节讲什么

进程 = 内核管理的执行上下文；内核为其分配资源以运行程序。 内核视角两大块： 1. **用户空间内存**：代码、数据、栈、堆等

---

## 要点

| | |
|--|--|
| **程序（program）** | 磁盘上的静态可执行文件 |
| **进程（process）** | 加载进内存、正在执行的**动态实例** |

进程 = 内核管理的执行上下文；内核为其分配资源以运行程序。

内核视角两大块：

1. **用户空间内存**：代码、数据、栈、堆等  
2. **内核 PCB**（Linux：`task_struct`）：状态、PID、fd 表、信号掩码、资源限制等  

## 代码示例

```c
#include <unistd.h>
#include <stdio.h>
#include <sys/types.h>

int main(void) {
    /* 进程 = 内核管理的执行上下文 */
    printf("PID = %d\n", getpid());
    printf("PPID = %d (父进程)\n", getppid());

    /* 进程有自己的地址空间 */
    int x = 42;  /* x 在栈上 */
    printf("x 的地址: %p (栈区)\n", (void *)&x);

    /* 进程有自己的 fd 表 */
    printf("stdin fd = %d, stdout fd = %d\n", STDIN_FILENO, STDOUT_FILENO);

    /* 查看进程的内存映射（内核视角的进程资源） */
    printf("查看 /proc/%d/maps 可了解进程地址空间\n", getpid());
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 下一节：[6.2 PID 与 PPID](./6.2-pid-ppid.md)
- 前置：**前置**：[Ch3](../../chapter-03-system-programming-concepts/notes) · [Ch4](../../chapter-04-file-io-universal/notes) · [Ch5](../../chapter-05-file-io-further/notes)  
- 后置：**后置**：[Ch7 内存分配](../../chapter-07-memory-allocation/notes) · [Ch8 用户与组](../../chapter-08-users-and-groups/notes) · [Ch24 fork](../../chapter-24-process-creation/notes)
