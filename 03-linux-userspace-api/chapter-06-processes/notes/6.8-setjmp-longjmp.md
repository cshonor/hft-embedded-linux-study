# 6.6 非局部跳转 `setjmp` / `longjmp`

> 本章：[TLPI 第 06 章 — Processes](../README.md)
> - 上一节：[6.5 环境变量](6.5-stack-frames.md)
> - 下一节：下一章+1:02d}-*/notes.md)

---

## 本节讲什么

#include <setjmp.h> int setjmp(jmp_buf env);           /* 直接调用返回 0；保存栈上下文 */ void longjmp(jmp_buf env, int val); /* 回到 setjmp；表现为返回 val（0→1） */

---

## 要点

```c
#include <setjmp.h>
int setjmp(jmp_buf env);           /* 直接调用返回 0；保存栈上下文 */
void longjmp(jmp_buf env, int val); /* 回到 setjmp；表现为返回 val（0→1） */
```

- 普通 `goto`：函数内。  
- `setjmp`/`longjmp`：**跨函数**非局部跳转。

### 易踩坑：优化变量

`setjmp` 与 `longjmp` 之间被修改的局部变量，若放在寄存器里，恢复后可能「回滚」：

```c
volatile int flag;  /* 强制走内存，禁止只放寄存器 */
```

典型场景：深层错误跳出、简易异常路径（信号处理里的用法见 Ch21，限制更多）。

Demo：[`code/setjmp_vars.c`](../code/setjmp_vars.c)

## 代码示例

```c
#include <stdio.h>
#include <setjmp.h>
#include <stdlib.h>
/* setjmp/longjmp: 非局部跳转 (类似 goto 但可跨函数) */
static jmp_buf env;

void do_error(void) {
    printf("Error occurred, jumping back...\n");
    longjmp(env, 1);  /* 跳回 setjmp, 返回值=1 */
}

int main(void) {
    int ret = setjmp(env);
    if (ret == 0) {
        printf("Normal path\n");
        do_error();  /* 模拟出错 */
        printf("This line never executes\n");
    } else {
        printf("Error recovery path (ret=%d)\n", ret);
    }
    printf("Continuing after error recovery\n");
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[6.5 环境变量](6.5-stack-frames.md)
- 前置：**前置**：[Ch3](../../chapter-03-system-programming-concepts/notes) · [Ch4](../../chapter-04-file-io-universal/notes) · [Ch5](../../chapter-05-file-io-further/notes)  
- 后置：**后置**：[Ch7 内存分配](../../chapter-07-memory-allocation/notes) · [Ch8 用户与组](../../chapter-08-users-and-groups/notes) · [Ch24 fork](../../chapter-24-process-creation/notes)
