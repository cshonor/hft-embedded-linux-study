# 25.6 内核销毁时做什么

> 本章：[TLPI 第 25 章 — Process Termination](../README.md)
> - 上一节：[25.5 `atexit` / `on_exit`](25.5-summary.md)
> - 下一节：25.7 僵尸（衔 Ch26）

---

## 本节讲什么

关 fd、放内存与多数资源 → 进程成**僵尸**（保留 PID/退出状态）→ 向父发 `SIGCHLD` → 父 `wait*` 才摘掉僵尸。 ---

---

## 要点

关 fd、放内存与多数资源 → 进程成**僵尸**（保留 PID/退出状态）→ 向父发 `SIGCHLD` → 父 `wait*` 才摘掉僵尸。

| | |
|--|--|
| 刷 **stdio 用户缓冲** | 仅 `exit` 路径 |
| 内核页缓存 / 关 fd | `_exit` 也会走内核清理（≠ 用户态 fflush） |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 内核销毁时做什么 在嵌入式系统的应用场景 |
| HFT | 内核销毁时做什么 对低延迟和性能的影响 |

## 代码示例

```c
#include <stdlib.h>
#include <stdio.h>
void cleanup(void) { printf("cleanup\n"); }
int main(void) {
    atexit(cleanup);
    printf("退出流程: atexit → flush → _exit\n");
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[25.5 `atexit` / `on_exit`](25.5-summary.md)
- 下一节：25.7 僵尸（衔 Ch26）
- 前置：**前置**：[Ch24 fork](../../chapter-24-process-creation/notes)  
- 后置：**后置**：[Ch26 wait / 僵尸](../../chapter-26-monitoring-child-processes/notes) · [Ch27 exec](../../chapter-27-program-execution/notes)
