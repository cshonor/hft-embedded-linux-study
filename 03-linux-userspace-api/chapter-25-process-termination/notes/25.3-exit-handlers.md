# 25.3 `main` return

> 本章：[TLPI 第 25 章 — Process Termination](../README.md)
> - 上一节：[25.2 `exit` vs `_exit` / `_Exit`（核心）](25.1-terminating-a-process-exit-and-exit.md)
> - 下一节：[25.4 退出状态](25.4-interactions-between-fork-stdio-buffers-.md)

---

## 本节讲什么

`return N;` ≈ `exit(N)`。无 return 时 C99+ 默认 `return 0`。 ---

---

## 要点

`return N;` ≈ `exit(N)`。无 return 时 C99+ 默认 `return 0`。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | main return ≈ exit，走 atexit + 刷缓冲清理路径 |
| HFT | main return 会跑 atexit + 刷缓冲，延迟敏感退出点改用 _exit |

## 代码示例

```c
#include <stdio.h>
/* main 的 return(n) 等价于 exit(n):
 * - 调用 atexit 注册的函数
 * - flush 并关闭 stdio 流
 * - 调用 _exit(n)
 */
int main(void) {
    printf("main 中 return 0 == exit(0)\n");
    return 0;
    /* 如果 main 不写 return, C99 起默认 return 0 */
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[25.2 `exit` vs `_exit` / `_Exit`（核心）](25.1-terminating-a-process-exit-and-exit.md)
- 下一节：[25.4 退出状态](25.4-interactions-between-fork-stdio-buffers-.md)
- 前置：**前置**：[Ch24 fork](../../chapter-24-process-creation/notes)  
- 后置：**后置**：[Ch26 wait / 僵尸](../../chapter-26-monitoring-child-processes/notes) · [Ch27 exec](../../chapter-27-program-execution/notes)
