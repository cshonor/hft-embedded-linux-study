# 25.2 `exit` vs `_exit` / `_Exit`（核心）

> 本章：[TLPI 第 25 章 — Process Termination](../README.md)
> - 上一节：[25.1 终止分类](25.1-terminating-a-process-exit-and-exit.md)
> - 下一节：[25.3 `main` return](25.3-exit-handlers.md)

---

## 本节讲什么

void exit(int status);    /* stdlib：库清理后进内核 */ void _Exit(int status);   /* C99：直进内核 */ void _exit(int status);   /* POSIX：与 _Exit 等价（Linux） */

---

## 要点

```c
void exit(int status);    /* stdlib：库清理后进内核 */
void _Exit(int status);   /* C99：直进内核 */
void _exit(int status);   /* POSIX：与 _Exit 等价（Linux） */
```

| 步骤 | `exit` | `_exit` / `_Exit` |
|------|--------|-------------------|
| atexit / on_exit | ✅ 逆序跑 | ❌ |
| 刷 stdio | ✅ | ❌（用户缓冲可能丢） |
| 进内核销毁 | ✅ | ✅ |

### fork 规范

```c
if (fork() == 0)
    _exit(EXIT_FAILURE);   /* 不 exec 时禁止 exit() */
```

fork 复制了 atexit 列表与 stdio 缓冲 → 子调 `exit` 易重复回调/重复刷缓冲。

Demo：[`code/exit_vs_exit.c`](../code/exit_vs_exit.c) · [`code/fork_atexit.c`](../code/fork_atexit.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | _exit 直进内核不刷 stdio；fork 子进程必须 _exit 防重复刷缓冲 |
| HFT | 热路径退出用 _exit 绕过 atexit/刷缓冲延迟；fork 子进程禁止 exit() |

## 代码示例

```c
#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>
void cleanup(void) { printf("atexit cleanup\n"); }
int main(void) {
    atexit(cleanup);  /* 注册退出处理函数 */
    printf("三种退出方式:\n");
    printf("  exit(n)   → 调 atexit + flush stdio + 调 _exit\n");
    printf("  _exit(n)  → 直接进内核, 不 flush\n");
    printf("  _Exit(n)  → 同 _exit (ISO C)\n");

    fork();
    /* 子进程用 _exit 避免重复 flush */
    exit(0);  /* 父进程用 exit */
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[25.1 终止分类](25.1-terminating-a-process-exit-and-exit.md)
- 下一节：[25.3 `main` return](25.3-exit-handlers.md)
- 前置：**前置**：[Ch24 fork](../../chapter-24-process-creation/notes)  
- 后置：**后置**：[Ch26 wait / 僵尸](../../chapter-26-monitoring-child-processes/notes) · [Ch27 exec](../../chapter-27-program-execution/notes)
