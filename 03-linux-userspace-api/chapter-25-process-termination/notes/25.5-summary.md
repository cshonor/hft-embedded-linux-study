# 25.5 `atexit` / `on_exit`

> 本章：[TLPI 第 25 章 — Process Termination](../README.md)
> - 上一节：[25.4 退出状态](25.4-interactions-between-fork-stdio-buffers-.md)
> - 下一节：[25.6 内核销毁时做什么](25.6-exercise.md)

---

## 本节讲什么

int atexit(void (*func)(void)); int on_exit(void (*func)(int, void *), void *arg);  /* Linux 扩展 */ - 注册：**LIFO** 逆序执行

---

## 要点

```c
int atexit(void (*func)(void));
int on_exit(void (*func)(int, void *), void *arg);  /* Linux 扩展 */
```

- 注册：**LIFO** 逆序执行  
- 仅 `exit` / `main` return 触发  
- `_exit` / 信号杀：**不跑**  
- fork 复制列表；exec 成功清空  
- 回调里勿再乱调 `exit`（易乱序/递归感）

Demo：[`code/atexit_order.c`](../code/atexit_order.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | atexit 注册清理（关 fd/写日志），LIFO 逆序执行 |
| HFT | atexit 注册资源清理；但 _exit/信号杀不跑，崩溃路径需另设防护 |

## 代码示例

```c
#include <stdlib.h>
#include <stdio.h>
void f1(void) { printf("f1\n"); }
void f2(void) { printf("f2\n"); }
int main(void) {
    /* atexit: 注册函数, exit 时逆序调用 */
    atexit(f1);
    atexit(f2);
    printf("exit 时调用顺序: f2 → f1 (逆序)\n");
    /* 最多注册 32 个 (sysconf(_SC_ATEXIT_MAX)) */
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[25.4 退出状态](25.4-interactions-between-fork-stdio-buffers-.md)
- 下一节：[25.6 内核销毁时做什么](25.6-exercise.md)
- 前置：**前置**：[Ch24 fork](../../chapter-24-process-creation/notes)  
- 后置：**后置**：[Ch26 wait / 僵尸](../../chapter-26-monitoring-child-processes/notes) · [Ch27 exec](../../chapter-27-program-execution/notes)
