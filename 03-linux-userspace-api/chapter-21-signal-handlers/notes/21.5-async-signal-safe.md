# 21.5 Async-Signal-Safe（最重要）

> 本章：[TLPI 第 21 章 — Signals: Signal Handlers](./README.md)
> - 上一节：[21.4 `EINTR` 与 `SA_RESTART`](./21.4-eintr-sarestart.md)
> - 下一节：[21.6 `SIGCHLD`](./21.6-sigchld.md)

---

## 本节讲什么

handler 可随时打断用户态代码 → **禁止**非异步信号安全函数。 推荐范式： volatile sig_atomic_t got_sigint = 0;

---

## 要点

handler 可随时打断用户态代码 → **禁止**非异步信号安全函数。

| ✅ 常见允许 | ❌ 严禁 |
|-------------|--------|
| `write`、`_exit`、`sigaction`、`sigprocmask`、`sigpending`、…（见 POSIX 列表） | `printf`、`malloc`/`free`、stdio、多数锁、C++/库随意调用 |

推荐范式：

```c
volatile sig_atomic_t got_sigint = 0;
void handler(int sig) { (void)sig; got_sigint = 1; }
```

`sig_atomic_t`：读写在实现上视为原子，避免撕裂。

---

---

## 与 C 语言学习的关系

本节的 `write()` 对应 K&R 8.2 节。K&R 的 copy.c 用 `write(1, buf, n)` 输出到 stdout。注意 `write` 与 `printf`/`fprintf` 的区别：`write` 是无格式化的字节流输出，`printf` 有格式化和缓冲。HFT 场景中 `write` 比 `printf` 更可控。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | Async-Signal-Safe（最重要） 在嵌入式系统的应用场景 |
| HFT | Async-Signal-Safe（最重要） 对低延迟和性能的影响 |

---

## 常见陷阱

1. **signal() 不可移植**

```c
signal(SIGINT, handler);  /* 行为不一致 */
```

signal() 在不同系统上行为不一致（是否自动恢复默认、handler 期间是否阻塞自身）。生产代码用 sigaction()。

2. **handler 中调用不安全函数**

```c
void handler(int sig) {
    printf("got signal\n");  /* printf 不安全! */
}
```

printf/malloc 等不是异步信号安全函数。handler 中只能调用异步信号安全函数（如 write）。

3. **信号竞态**

```c
/* 检查标志后处理，但信号可能在此间到达 */
if (flag) { ... }  /* TOCTOU */
```

信号可能在检查和处理之间到达。需要用 sigprocmask 阻塞信号或用 volatile sig_atomic_t。

---

## 自测题

**题目 1（选择）：** `SIGKILL` 和 `SIGSTOP` 的共同特点是？
A. 可以被捕获
B. 可以被忽略
C. 可以被阻塞
D. 都不能被捕获/忽略/阻塞

<details>
<summary>参考答案</summary>

D。SIGKILL 和 SIGSTOP 不能被捕获、忽略或阻塞。这是设计上的保证。

</details>

**题目 2（判断）：** 可以在信号处理函数中安全地调用 `printf`。

<details>
<summary>参考答案</summary>

错误。printf 不是异步信号安全函数。它使用 stdio 缓冲区和 malloc，在信号处理函数中调用可能导致死锁或数据损坏。用 write 代替。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
volatile int flag = 0;
void handler(int sig) { flag = 1; }
int main() {
    signal(SIGINT, handler);
    while (!flag) { /* busy wait */ }
    printf("done\n");
}
```

<details>
<summary>参考答案</summary>

1) signal() 不可移植，应用 sigaction；2) busy wait 浪费 CPU，应用 pause() 或 sigsuspend()；3) printf 在信号后的主循环中调用是安全的，但如果信号在 printf 执行中到来则不安全。

</details>

**题目 4（应用）：** 写代码用 sigaction 捕获 SIGINT 并设置标志。

<details>
<summary>参考答案</summary>

```c
volatile sig_atomic_t flag = 0;
void handler(int sig) { flag = 1; }
struct sigaction sa = {0};
sa.sa_handler = handler;
sigemptyset(&sa.sa_mask);
sa.sa_flags = SA_RESTART;
sigaction(SIGINT, &sa, NULL);
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[21.4 `EINTR` 与 `SA_RESTART`](./21.4-eintr-sarestart.md)
- 下一节：[21.6 `SIGCHLD`](./21.6-sigchld.md)
- 前置：**前置**：[Ch20 信号基础](../chapter-20-signals-fundamentals/notes.md)  
- 后置：**后置**：[Ch22 pause / sigsuspend](../chapter-22-signals-advanced/notes.md) · [Ch24 进程创建 / wait](../chapter-24-process-creation/notes.md)

---

## man 手册参考

- `man 2 write`
- `man 2 sigaction`
- `man 2 sigprocmask`
- `man 3 printf`
- `man 3 malloc`
- `man 3 free`
