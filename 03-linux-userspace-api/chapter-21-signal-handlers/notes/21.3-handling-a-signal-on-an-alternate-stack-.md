# 21.3 常用 `sa_flags`

> 本章：[TLPI 第 21 章 — Signals: Signal Handlers](./README.md)
> - 上一节：[21.2 `sa_mask`（临时叠加）](./21.2-samask.md)
> - 下一节：[21.4 `EINTR` 与 `SA_RESTART`](./21.4-eintr-sarestart.md)

---

## 本节讲什么

---

## 要点

| 标志 | 含义 |
|------|------|
| `SA_SIGINFO` | 用 `sa_sigaction` |
| `SA_RESTART` | 多数慢调用被中断后自动重启（否则常 `EINTR`） |
| `SA_NODEFER` | 不自动阻塞本信号（易重入，慎用） |
| `SA_RESETHAND` | 一次后复位 `SIG_DFL`（老 `signal` 味） |
| `SA_NOCLDSTOP` | `SIGCHLD`：子停/续不通知 |
| `SA_NOCLDWAIT` | `SIGCHLD`：子退出不留僵尸 |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 常用 `sa_flags` 在嵌入式系统的应用场景 |
| HFT | 常用 `sa_flags` 对低延迟和性能的影响 |

## 代码示例

```c
#include <signal.h>
#include <stdio.h>
void handler(int sig) { printf("got %d\n", sig); }
int main(void) {
    struct sigaction sa = {0};
    sa.sa_handler = handler;
    /* SA_RESTART: 被信号中断的系统调用自动重启 */
    sa.sa_flags = SA_RESTART;
    /* SA_NOCLDSTOP: 子进程暂停不产生 SIGCHLD */
    /* SA_NODEFER: handler 期间不自动阻塞自身信号 */
    sigaction(SIGINT, &sa, NULL);
    printf("SA_RESTART: read 被 SIGINT 中断后自动重启\n");
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[21.2 `sa_mask`（临时叠加）](./21.2-samask.md)
- 下一节：[21.4 `EINTR` 与 `SA_RESTART`](./21.4-eintr-sarestart.md)
- 前置：**前置**：[Ch20 信号基础](../chapter-20-signals-fundamentals/notes.md)  
- 后置：**后置**：[Ch22 pause / sigsuspend](../chapter-22-signals-advanced/notes.md) · [Ch24 进程创建 / wait](../chapter-24-process-creation/notes.md)
