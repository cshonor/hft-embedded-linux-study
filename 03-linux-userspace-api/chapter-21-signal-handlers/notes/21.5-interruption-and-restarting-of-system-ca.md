# 21.5 Async-Signal-Safe（最重要）

> 本章：[TLPI 第 21 章 — Signals: Signal Handlers](../README.md)
> - 上一节：[21.4 `EINTR` 与 `SA_RESTART`](21.4-the-sa-siginfo-flag.md)
> - 下一节：[21.6 `SIGCHLD`](21.6-summary.md)

---

## 本节讲什么

handler 可随时打断用户态代码 → **禁止**非异步信号安全函数。 推荐范式： volatile sig_atomic_t got_sigint = 0;

---

## 要点

handler 可随时打断用户态代码 → **禁止**非异步信号安全函数。

| ✅ 常见允许 | ❌ 严禁 |
|-------------|--------|
| `write`、`_exit`、`sigaction`、`sigprocmask`、`sigpending`、…（见 POSIX 列表） | `printf`、`malloc`/`free`、stdio、多数锁、C++/库随意调用 |

推荐范式：

```c
volatile sig_atomic_t got_sigint = 0;
void handler(int sig) { (void)sig; got_sigint = 1; }
```

`sig_atomic_t`：读写在实现上视为原子，避免撕裂。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | Async-Signal-Safe（最重要） 在嵌入式系统的应用场景 |
| HFT | Async-Signal-Safe（最重要） 对低延迟和性能的影响 |

## 代码示例

```c
#include <signal.h>
#include <unistd.h>
#include <stdio.h>
#include <string.h>
/* 异步信号安全函数: 可在 handler 中安全调用 */
void handler(int sig) {
    /* 安全: write 是 async-signal-safe */
    const char *msg = "signal!\n";
    write(STDOUT_FILENO, msg, strlen(msg));
    /* 不安全: printf/malloc/free 都不能在 handler 中调用! */
}
int main(void) {
    struct sigaction sa = {0};
    sa.sa_handler = handler;
    sigaction(SIGUSR1, &sa, NULL);
    printf("handler 中只能用 write, 不能用 printf\n");
    raise(SIGUSR1);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[21.4 `EINTR` 与 `SA_RESTART`](21.4-the-sa-siginfo-flag.md)
- 下一节：[21.6 `SIGCHLD`](21.6-summary.md)
- 前置：**前置**：[Ch20 信号基础](../../chapter-20-signals-fundamentals/notes)  
- 后置：**后置**：[Ch22 pause / sigsuspend](../../chapter-22-signals-advanced/notes) · [Ch24 进程创建 / wait](../../chapter-24-process-creation/notes)
