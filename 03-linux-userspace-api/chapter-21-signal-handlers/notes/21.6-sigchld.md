# 21.6 `SIGCHLD`

> 本章：[TLPI 第 21 章 — Signals: Signal Handlers](./README.md)
> - 上一节：[21.5 Async-Signal-Safe（最重要）](./21.5-async-signal-safe.md)
> - 下一节：[21.7 `signal()` vs `sigaction()`](./21.7-signal-sigaction.md)

---

## 本节讲什么

子进程退出 → 父收 `SIGCHLD`。 Demo：[`code/sigchld_reap.c`](./code/sigchld_reap.c) ---

---

## 要点

子进程退出 → 父收 `SIGCHLD`。

| 做法 | |
|------|--|
| handler 里 | 循环 `waitpid(-1, …, WNOHANG)` 收尸（标准信号不排队，一次可能对应多个子） |
| `SA_NOCLDWAIT` | 内核自动收，不留僵尸 |

Demo：[`code/sigchld_reap.c`](./code/sigchld_reap.c)

---

---

## 与 C 语言学习的关系

本节内容属于 Unix 系统编程，是 C 语言学习的进阶实践。掌握这些 API 需要扎实的 C 语言基础：指针操作、结构体、错误处理（errno/返回值检查）、内存管理。建议对照 K&R 和 C 专家编程的相关章节学习，将系统 API 的使用与 C 语言底层机制（如函数调用约定、栈布局、内存对齐）联系起来理解。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `SIGCHLD` 在嵌入式系统的应用场景 |
| HFT | `SIGCHLD` 对低延迟和性能的影响 |

---

## 常见陷阱

1. **僵尸进程**

```c
fork();
/* 父进程不 wait → 子进程变僵尸 */
```

子进程退出后如果父进程不 wait，子进程变成僵尸（Z 状态），占用 PID 和少量内存。

2. **wait 只等第一个**

```c
wait(&status);  /* 只等任意一个子进程 */
```

wait 等待任意一个子进程。如果有多个子进程，需要循环 wait 或用 waitpid 指定 PID。

3. **status 宏使用错误**

```c
wait(&status);
if (status == 0) ...  /* 错误 */
if (WIFEXITED(status)) ...  /* 正确 */
```

不能直接用 status 值判断。必须用 WIFEXITED/WEXITSTATUS/WIFSIGNALED/WTERMSIG 宏。

---

## 自测题

**题目 1（选择）：** `wait(&status)` 中 status 的值 0 表示什么？
A. 子进程退出码 0
B. 不能直接判断
C. 子进程被信号杀死
D. 等待失败

<details>
<summary>参考答案</summary>

B。不能直接用 status 值判断。必须用 WIFEXITED(status) 等宏解析。status=9 可能表示被 SIGKILL 杀死，也可能不是。

</details>

**题目 2（判断）：** `waitpid(pid, &status, WNOHANG)` 会阻塞等待子进程退出。

<details>
<summary>参考答案</summary>

错误。WNOHANG 表示非阻塞，如果子进程还没退出立即返回 0。不带 WNOHANG 才会阻塞。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
pid_t pid = fork();
if (pid == 0) exit(42);
wait(&status);
printf("exit code: %d\n", status);
```

<details>
<summary>参考答案</summary>

status 不能直接用。应该：
`WEXITSTATUS(status)` 提取退出码。
`printf("exit code: %d\n", WEXITSTATUS(status));`

</details>

**题目 4（应用）：** 写代码等待指定子进程，获取退出码或终止信号。

<details>
<summary>参考答案</summary>

```c
int status;
waitpid(pid, &status, 0);
if (WIFEXITED(status))
    printf("exit: %d\n", WEXITSTATUS(status));
else if (WIFSIGNALED(status))
    printf("signal: %d\n", WTERMSIG(status));
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[21.5 Async-Signal-Safe（最重要）](./21.5-async-signal-safe.md)
- 下一节：[21.7 `signal()` vs `sigaction()`](./21.7-signal-sigaction.md)
- 前置：**前置**：[Ch20 信号基础](../chapter-20-signals-fundamentals/notes.md)  
- 后置：**后置**：[Ch22 pause / sigsuspend](../chapter-22-signals-advanced/notes.md) · [Ch24 进程创建 / wait](../chapter-24-process-creation/notes.md)

---

## man 手册参考

- `man 2 waitpid`
