# 48.3 Using Shared Memory

> 本章：[TLPI 第 48 章](../README.md)

## 本节讲什么

`shmat()` 将共享内存段映射到进程虚拟地址空间，`shmdt()` 解除映射。映射后的内存区域可被多个进程直接读写，是最快的 IPC 方式。

## 要点

### 核心 API

```c
#include <sys/shm.h>
void *shmat(int shmid, const void *shmaddr, int shmflg);
int shmdt(const void *shmaddr);
```

### shmat() 参数

| 参数 | 含义 |
|------|------|
| `shmid` | `shmget()` 返回的共享内存 ID |
| `shmaddr` | 期望映射地址（NULL 为自动选择） |
| `shmflg` | `SHM_RDONLY` 只读 / 0 读写 |

### 映射地址选择

| shmaddr | shmflg | 行为 |
|---------|--------|------|
| NULL | 任意 | 内核自动选择地址（推荐） |
| 非 NULL | 无 `SHM_RND` | 尝试精确映射到指定地址 |
| 非 NULL | `SHM_RND` | 向下对齐到 SHMLBA |

**推荐使用 NULL**：让内核选择可用地址，避免地址冲突。

### 使用流程

```c
/* 1. 创建/获取共享内存 */
int shmid = shmget(key, size, IPC_CREAT | 0666);

/* 2. 映射到进程地址空间 */
void *addr = shmat(shmid, NULL, 0);
/* addr 现在指向共享内存 */

/* 3. 直接读写共享内存 */
memset(addr, 0, size);

/* 4. 解除映射 */
shmdt(addr);

/* 5. 删除共享内存 */
shmctl(shmid, IPC_RMID, NULL);
```

### 注意事项

| 问题 | 说明 |
|------|------|
| 非自动删除 | `shmdt` 只是解除映射，段仍存在 |
| 删除时机 | `IPC_RMID` 不立即删除，等所有映射解除后才回收 |
| 写入后再读取 | 需要内存屏障或同步机制保证可见性 |
| 多进程地址不同 | 不同进程映射到不同虚拟地址，用指针需用偏移 |
| fork 继承 | 子进程继承映射 |

### 共享内存中的指针问题

```c
/* 错误：指针在另一个进程中无效 */
struct shared_data {
    int count;
    char *name;  /* 在进程 A 中的地址，进程 B 无法使用 */
};

/* 正确：使用偏移量 */
struct shared_data {
    int count;
    size_t name_offset;  /* 相对于共享内存基址的偏移 */
};
/* 访问：base + name_offset */
```

### 同步需求

共享内存本身没有同步机制，必须配合：
- 信号量（SysV 或 POSIX）
- pthread 互斥锁（多进程共享需设置 `PTHREAD_PROCESS_SHARED`）
- 原子操作（简单的 flag/counter）

## HFT / 嵌入式关联

- HFT 最快 IPC：行情数据通过共享内存传递，纳秒级延迟
- 多策略进程共享行情快照：只读映射，避免拷贝
- 嵌入式多进程共享配置/状态数据
- 必须配合同步机制防止竞争

## 代码示例

```c
#include <sys/shm.h>
#include <sys/ipc.h>
#include <string.h>
#include <stdio.h>

typedef struct {
    int seq;
    int valid;
    char data[4096];
} shared_state_t;

int main() {
    /* 创建共享内存 */
    int shmid = shmget(IPC_PRIVATE, sizeof(shared_state_t),
                       IPC_CREAT | 0666);
    if (shmid == -1) { perror("shmget"); return 1; }

    /* 父进程映射 */
    shared_state_t *state = shmat(shmid, NULL, 0);
    if (state == (void*)-1) { perror("shmat"); return 1; }

    /* 初始化 */
    memset(state, 0, sizeof(*state));

    /* fork 子进程 */
    pid_t pid = fork();
    if (pid == 0) {
        /* 子进程映射（fork 自动继承，也可手动 shmat） */
        /* state 已继承 */
        for (int i = 0; i < 10; i++) {
            while (!state->valid);  /* 忙等（简化，实际需锁） */
            printf("Child read: seq=%d\n", state->seq);
            state->valid = 0;
        }
        shmdt(state);
        _exit(0);
    }

    /* 父进程写入 */
    for (int i = 0; i < 10; i++) {
        state->seq = i;
        snprintf(state->data, sizeof(state->data), "msg-%d", i);
        state->valid = 1;
        usleep(10000);  /* 等子进程读取 */
    }

    waitpid(pid, NULL, 0);

    shmdt(state);
    shmctl(shmid, IPC_RMID, NULL);
    return 0;
}
```

## 衔接

- 上一节 [48.2 创建共享内存](48.2-creating-or-opening-a-shared-memory-segm.md)
- 下一节 [48.4 共享内存属性](48.2-creating-or-opening-a-shared-memory-segm.md)

## 代码自测

<details>
<summary>shmdt() 后共享内存段会被删除吗？</summary>

不会。`shmdt()` 只是解除当前进程的映射，共享内存段仍然存在。即使调用了 `shmctl(shmid, IPC_RMID, NULL)`，段也不会立即删除——它被标记为"待删除"，直到所有进程都调用 `shmdt` 解除映射后才真正回收。这个设计确保正在使用共享内存的进程不会被中断。

</details>
