# 48.1 原理

> 本章：[TLPI 第 48 章 — System V Shared Memory](../README.md)
> - 上一节：本章首页
> - 下一节：[48.2 –48.3 · 48.7 API](48.2-creating-or-opening-a-shared-memory-segm.md)

---

## 本节讲什么

多进程映射同一物理页 → **无用户↔内核数据拷贝**。 本身不同步 → 须信号量/文件锁。 **内核持久**；detach ≠ 销毁。

---

## 要点

多进程映射同一物理页 → **无用户↔内核数据拷贝**。  
本身不同步 → 须信号量/文件锁。  
**内核持久**；detach ≠ 销毁。  
术语：SysV 说 **attach/detach**；`mmap` 说 map — 思想近、API 不同。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 原理 在嵌入式系统的应用场景 |
| HFT | 原理 对低延迟和性能的影响 |

## 代码示例

```c
#include <stdio.h>
#include <sys/shm.h>
#include <string.h>
#include <unistd.h>
#include <sys/wait.h>
/* 共享内存原理: 多个进程映射同一物理内存页 */
int main(void) {
    /* shmget: 在内核中分配共享内存段 */
    int shmid = shmget(IPC_PRIVATE, 4096, IPC_CREAT | 0666);
    printf("Shared memory segment: id=%d, size=4096\n", shmid);

    /* shmat: 将段映射到进程地址空间 */
    char *shm = shmat(shmid, NULL, 0);
    printf("Attached at: %p\n", (void *)shm);

    /* 父进程写入 */
    strcpy(shm, "Hello from parent!");

    pid_t pid = fork();
    if (pid == 0) {
        /* 子进程映射同一段, 看到相同数据 */
        char *child_shm = shmat(shmid, NULL, 0);
        printf("Child sees: %s\n", child_shm);
        strcpy(child_shm, "Modified by child!");
        shmdt(child_shm);
        _exit(0);
    }

    waitpid(pid, NULL, 0);
    printf("Parent sees: %s\n", shm);

    /* shmdt: 解除映射 (段仍然存在于内核中) */
    shmdt(shm);

    /* shmctl(IPC_RMID): 删除段 (标记删除, 引用计数归零时真正释放) */
    shmctl(shmid, IPC_RMID, NULL);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 下一节：[48.2 –48.3 · 48.7 API](48.2-creating-or-opening-a-shared-memory-segm.md)
- 前置：**前置**：[Ch47 SysV 信号量](../../chapter-47-sysv-semaphores/notes)  
- 后置：**后置**：[Ch49 mmap](../../chapter-49-memory-mappings/notes) · [Ch51 POSIX IPC 导论](../../chapter-51-posix-ipc-intro/notes)
