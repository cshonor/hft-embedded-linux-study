# 48.4 工程模型

> 本章：[TLPI 第 48 章 — System V Shared Memory](../README.md)
> - 上一节：48.6 陷阱：段内勿存指针
> - 下一节：[48.9 限额 · 运维](48.9-shared-memory-limits.md)

---

## 本节讲什么

**SysV sem（互斥/握手）+ SysV shm（大数据）** → 一方 `IPC_RMID` 打标 → 各方 `shmdt` → 回收。 ---

---

## 要点

**SysV sem（互斥/握手）+ SysV shm（大数据）** → 一方 `IPC_RMID` 打标 → 各方 `shmdt` → 回收。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 工程模型 在嵌入式系统的应用场景 |
| HFT | 工程模型 对低延迟和性能的影响 |

## 代码示例

```c
#include <stdio.h>
#include <sys/shm.h>
#include <sys/sem.h>
#include <string.h>
#include <unistd.h>
#include <sys/wait.h>
/* 共享内存 + 信号量: 经典 IPC 模型 */
union semun { int val; };

int main(void) {
    /* 创建共享内存 + 信号量 */
    int shmid = shmget(IPC_PRIVATE, 256, IPC_CREAT | 0666);
    int semid = semget(IPC_PRIVATE, 1, IPC_CREAT | 0666);

    /* 信号量初始化为 1 (mutex) */
    union semun arg = {.val = 1};
    semctl(semid, 0, SETVAL, arg.val);

    /* 映射共享内存 */
    char *shm = shmat(shmid, NULL, 0);
    shm[0] = '\0';  /* 初始为空字符串 */

    pid_t pid = fork();
    if (pid == 0) {
        char *child_shm = shmat(shmid, NULL, 0);
        struct sembuf lock = {0, -1, 0};
        struct sembuf unlock = {0, 1, 0};

        /* 获取锁 -> 写数据 -> 释放锁 */
        semop(semid, &lock, 1);
        strcat(child_shm, "child wrote; ");
        semop(semid, &unlock, 1);

        shmdt(child_shm);
        _exit(0);
    }

    /* 父进程也写 */
    struct sembuf lock = {0, -1, 0};
    struct sembuf unlock = {0, 1, 0};
    semop(semid, &lock, 1);
    strcat(shm, "parent wrote; ");
    semop(semid, &unlock, 1);

    waitpid(pid, NULL, 0);
    printf("Shared content: %s\n", shm);

    shmdt(shm);
    shmctl(shmid, IPC_RMID, NULL);
    semctl(semid, 0, IPC_RMID);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：48.6 陷阱：段内勿存指针
- 下一节：[48.9 限额 · 运维](48.9-shared-memory-limits.md)
- 前置：**前置**：[Ch47 SysV 信号量](../../chapter-47-sysv-semaphores/notes)  
- 后置：**后置**：[Ch49 mmap](../../chapter-49-memory-mappings/notes) · [Ch51 POSIX IPC 导论](../../chapter-51-posix-ipc-intro/notes)
