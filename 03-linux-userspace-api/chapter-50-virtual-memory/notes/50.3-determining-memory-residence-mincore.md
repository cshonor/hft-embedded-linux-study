# 50.3 驻留查询：mincore()

> 源码核验：Linux v6.6 · `mm/mincore.c`（`mincore_pte_range():100` · `can_do_mincore():158` · `do_mincore():187` · `SYSCALL_DEFINE3(mincore):232`）
> 本章：[TLPI 第 50 章 — Virtual Memory Operations](../README.md)
> - 上一节：[50.2 `mlock` / `mlockall`](50.2-memory-locking-mlock-and-mlockall.md)
> - 下一节：[50.4 访问提示 `madvise`](50.4-advising-future-memory-usage-patterns-ma.md)

---

## 本节讲什么

`mincore`（MINimal CORE，名字来自「minimal information about core/residency」）查询区间内**哪些页当前在物理内存**。三个灵魂问题：

1. 返回的 `vec[i] & 1` 到底是什么？（PTE **present** 位的快照——页表里有映射）
2. 它能当同步条件吗？（**不能**——瞬时、非原子，查完的瞬间内核可能就把页回收了）
3. 零页算驻留吗？（**算**！`mincore` 说 resident，`RSS` 却不计——两者口径不同）

```c
#include <sys/mman.h>
int mincore(void *addr, size_t length, unsigned char *vec);
/* vec[i] & 1 == 1：第 i 页（从 addr 页对齐起点数）当前驻留 */
```

---

## 要点

### 三种 PTE 状态的处理（`mincore_pte_range():100` 逐行核验）

| PTE 状态 | `vec` 值 | 说明 |
|----------|----------|------|
| `pte_present(pte)` | **1** | 真页**或零页**（零页是 present+special 的特殊 PTE）——`mincore` 区分不了 |
| 空映射（`pte_none`） | 0 | 还没缺页过 |
| swap 项 | `mincore_page(swap cache)` 查交换缓存 | 换出的页若还在 swap cache 里算 1 |
| migration/hwpoison 项 | 1 | 迁移中的页视为驻留 |
| **透明大页（THP）** | `memset(vec, 1, nr)` 整块全 1 | 2MB 大页一次标满 |

### ⭐ 零页语义：mincore 与 RSS 的口径差（实测验证）

匿名私有映射**只读触碰**的页，内核映射零页（`do_anonymous_page` 只读分支，见 [49.7](../../chapter-49-memory-mappings/notes/49.7-anonymous-mappings.md)）：

```
实测（WSL Ubuntu，2026-09-05）：
  4 页匿名映射：页 0 写触碰、页 1 读触碰、页 2/3 不碰
  mincore = [1,1,0,0]     ← 零页 present，mincore 说驻留
  同区 Rss = 4 kB         ← RSS 只算页 0 的真页（vm_normal_page 对零页返回 NULL）
```

一页内存，两个口径打架——**mincore 回答「PTE 能直接翻译吗」，RSS 回答「这个进程独占多少真物理页」**。

### ⭐ 无权限文件映射：静默返回 0（防侧信道，v6.6 新增语义）

`can_do_mincore():158`：对**文件映射**，仅当调用者能写该文件（或拥有 inode）才返回真实驻留信息；否则 `do_mincore()` 把整个 vec 填 **0**——不报错，就是不给真话：

```c
static inline bool can_do_mincore(struct vm_area_struct *vma)
{
	if (vma_is_anonymous(vma)) return true;
	if (!vma->vm_file) return false;
	/* 只对「能以写模式打开对应文件」的映射泄露 page cache 驻留信息，
	   否则构成侧信道（探测别的进程读了什么文件页） */
	return inode_owner_or_capable(...) || file_permission(vma->vm_file, MAY_WRITE) == 0;
}
```

这是 Spectre/Meltdown 时代加固的一部分：驻留模式本身是信息（能推断「谁在预读什么文件」）。

### 瞬时性与「非原子」

`walk_page_range` 逐页读 PTE 时只持**该页表的 spinlock**，页与页之间不连贯；返回用户态的过程中任何页都可能被回收/换出/换入。正确用法：**诊断**（启动后验证 mlock 生效、观察 MADV_DONTNEED 效果），不是同步原语。

### 错误码

| errno | 条件 |
|-------|------|
| `EINVAL` | addr 未页对齐 / length ≥ UINTPTR_MAX/pagesize |
| `ENOMEM` | 区间不在任何映射内（含未映射空洞） |
| `EAGAIN`（少见） | 页表遍历时 PTE 竞态（`ACTION_AGAIN`） |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 启动自检：预填充（mlock/MAP_POPULATE）后 `mincore` 全 1 即「热身完成」的可验证标准；XIP（eXecute In Place）场景 mincore 恒 1（NOR flash 直接映射）——用它区分「真 RAM 驻留」还是「flash 就地执行」时**要小心口径** |
| HFT | `mlockall` 之后的验收步骤：对热缓冲 `mincore` 全 1 才能宣告「无缺页风险」；监控线程周期性采样非锁定区的驻留率，驻留率骤降 = 内存压力前兆（比等 swap 毛刺打在延迟分布上早得多）；注意对共享行情文件映射若无写权限，mincore 全 0 是**假象**（can_do_mincore 防侧信道分支） |

---

## 代码示例

```c

/* mincore 零页口径差实测：mincore 说驻留，RSS 不认账
 * 实测（WSL Ubuntu，gcc -O2 -Wall -Wextra，2026-09-05）：
 *   mincore = [1,1,0,0]（预期 [1,1,0,0]：零页也算 resident）
 *   同区 Rss = 4 kB（预期 4：零页 present 却不计入 RSS）
 */
#define _GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mman.h>
#include <unistd.h>

int main(void)
{
    long pagesz = sysconf(_SC_PAGESIZE);
    size_t P = (size_t)pagesz;

    char *q = mmap(NULL, 4 * P, PROT_READ | PROT_WRITE,
                   MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
    if (q == MAP_FAILED) { perror("mmap"); return 1; }

    q[0] = 1;                        /* 页 0：写触碰 → 分配真页 */
    volatile char c = q[P]; (void)c; /* 页 1：读触碰 → 映射零页 */
                                     /* 页 2/3：不触碰 */

    unsigned char vec[4];
    if (mincore(q, 4 * P, vec) != 0) { perror("mincore"); return 1; }
    printf("mincore = [%d,%d,%d,%d]（页0=写触碰 页1=读触碰零页）\n",
           vec[0] & 1, vec[1] & 1, vec[2] & 1, vec[3] & 1);
    printf("页 1 的零页：mincore 说 resident，但它是全局唯一共享页\n");
    return 0;
}
```

（RSS 对照需要解析 `/proc/self/smaps`，完整程序见本章 [50.6 练习](50.6-exercises.md) 实验 4 的参考实现。）

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[50.2 `mlock` / `mlockall`](50.2-memory-locking-mlock-and-mlockall.md)——mincore 是 mlock 的验收工具
- 下一节：[50.4 访问提示 `madvise`](50.4-advising-future-memory-usage-patterns-ma.md)——MADV_DONTNEED 的效果就用 mincore 验证
- 零页机制：[49.7 匿名映射](../../chapter-49-memory-mappings/notes/49.7-anonymous-mappings.md)——零页只读分支的缺页路径（`do_anonymous_page():4067`）
- RSS 统计：[Ch12 /proc 进程信息](../../chapter-12-system-process-info/notes/12.1-proc-filesystem.md)——/proc/PID/statm 的六个字段口径
- 内核遍历机制：[06-linux-mm](../../../06-linux-mm/README.md)——`walk_page_range` 通用页表遍历框架

---

## 自测题

**题目 1（选择）：** 一个普通用户对别人拥有的文件的只读映射调用 `mincore`，返回的 vec 里全是 0，最可能的原因？

A. 文件页都不在内存　B. `can_do_mincore` 防侧信道拒绝泄露　C. 映射坏了　D. mincore 失败了

<details>
<summary>参考答案</summary>

**B**。v6.6 起 `can_do_mincore():158` 只对「能写该文件/拥有 inode」的映射返回真实驻留，否则静默填 0（不报错）。这是防侧信道加固——驻留模式能推断其他进程的文件访问行为。

</details>

**题目 2（判断）：** `mincore` 返回全 1 之后，程序可以放心访问整段区间而不会发生任何缺页。

<details>
<summary>参考答案</summary>

错误。mincore 是**瞬时快照**，无任何原子性保证——返回用户态的微秒级窗口里内核完全可以把页回收（尤其无 mlock 的普通匿名页）。要保证不缺页必须 `mlock`；mincore 只能做诊断/验收，不能做同步条件。

</details>

**题目 3（应用）：** 为什么页 1 读触碰后 mincore=1 而 Rss 不增加？写出两者的统计口径。

<details>
<summary>参考答案</summary>

读触碰的匿名私有页映射**零页**（全局唯一物理页，`zero_pfn`）。mincore 的口径是 **PTE present**（`mincore_pte_range` 里 `pte_present(pte) → *vec = 1`），零页 PTE 是 present+special，所以算 1；RSS 的口径是**进程独占的真物理页**（`vm_normal_page()` 对 special PTE 返回 NULL，跳过统计），零页全系统共享一份，不计入任何进程的 RSS。一句话：mincore 看「有没有映射」，RSS 看「占了多少真页」。

</details>
