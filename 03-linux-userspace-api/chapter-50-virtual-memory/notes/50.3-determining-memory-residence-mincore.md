# 50.3 `mlock` / `mlockall`

> 本章：[TLPI 第 50 章 — Virtual Memory Operations](../README.md)
> - 上一节：[50.2 `mprotect`](50.1-changing-memory-protection-mprotect.md)
> - 下一节：[50.4 `mincore`](50.3-determining-memory-residence-mincore.md)

---

## 本节讲什么

int mlock(const void *addr, size_t length); int munlock(const void *addr, size_t length); int mlockall(int flags);   /* MCL_CURRENT | MCL_FUTURE */

---

## 要点

```c
int mlock(const void *addr, size_t length);
int munlock(const void *addr, size_t length);
int mlockall(int flags);   /* MCL_CURRENT | MCL_FUTURE */
int munlockall(void);
```

禁 swap，减缺页 IO 延迟（实时 / HFT）。  
- 用户有 **`ulimit -l`** 上限；超限失败  
- 同区多次 `mlock`，**一次 `munlock` 即解**（非叠加计数）  
- `fork` 继承锁；**`exec` 全解锁**  
- `MCL_FUTURE`：后续分配也锁 → 易耗尽限额  
慎用：挤占 RAM → 他进程 OOM。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 无 swap 的 RTOS/嵌入式锁热代码+数据防换出；但 RAM 紧，锁多易挤占致 OOM |
| HFT | HFT 铁律：mlockall(MCL_CURRENT|MCL_FUTURE) 锁全部热路径内存，彻底消除缺页/换出毛刺；需 RLIMIT_MEMLOCK/CAP_IPC_LOCK |

## 代码示例

```c

/* mlock / munlock 锁定内存页，防止被换出到 swap */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mman.h>
#include <sys/resource.h>
#include <unistd.h>

int main(void) {
    size_t len = 4096;
    char *p = mmap(NULL, len, PROT_READ | PROT_WRITE,
                   MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
    if (p == MAP_FAILED) { perror("mmap"); exit(1); }

    /* mlock 锁定这一页，确保不换出（HFT 延迟敏感场景常用） */
    if (mlock(p, len) < 0) {
        perror("mlock (need CAP_IPC_LOCK or RLIMIT_MEMLOCK)");
    } else {
        printf("mlock: %zu bytes locked in RAM\n", len);
    }

    strcpy(p, "locked memory");
    printf("data: %s\n", p);

    /* munlock 解锁 */
    munlock(p, len);
    printf("munlock: unlocked\n");

    /* mlockall 锁定进程所有页 */
    if (mlockall(MCL_CURRENT | MCL_FUTURE) < 0) {
        perror("mlockall");
    } else {
        printf("mlockall: all current+future pages locked\n");
        munlockall();
    }

    munmap(p, len);
    return 0;
}

```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[50.2 `mprotect`](50.1-changing-memory-protection-mprotect.md)
- 下一节：[50.4 `mincore`](50.3-determining-memory-residence-mincore.md)
- 前置：**前置**：[Ch49 mmap](../../chapter-49-memory-mappings/notes)  
- 后置：**后置**：[Ch51 POSIX IPC 导论](../../chapter-51-posix-ipc-intro/notes)
