# 50.6 练习

> 源码核验：Linux v6.6 · `mm/mprotect.c` · `mm/mlock.c` · `mm/mincore.c` · `mm/madvise.c`（行号见各节）
> 本章：[TLPI 第 50 章 — Virtual Memory Operations](../README.md)
> - 上一节：[50.5 总结](50.5-summary.md)

---

## 练习题

### 练习 1：mmap 文件映射

编写程序，使用 `mmap()` 将文件映射到内存，通过指针读写文件内容，最后 `munmap()` 解除映射。

### 练习 2：mprotect 改变内存保护

分配一页内存（`mmap`），先用 `PROT_READ` 映射，然后尝试写入（应该 SIGSEGV），再用 `mprotect` 改为 `PROT_READ|PROT_WRITE`，写入成功。

### 练习 3：mlockall 延迟测试

编写程序，对比 `mlockall` 前后访问大数组（1GB）的延迟分布。

### 实验 4（实测）：mincore 口径差 + MADV_DONTNEED 双语义

四页匿名映射做「页 0 写 / 页 1 读 / 页 2、3 不碰」的不对称触碰，用 `mincore` 与 `/proc/self/smaps` 的 Rss 双口径对照；随后 `MADV_DONTNEED` 验证匿名归零与文件 PRIVATE 回读原内容。

### 实验 5（实测）：mlock 限额错误码

`getrlimit(RLIMIT_MEMLOCK)` 打印限额，请求 2 倍限额的 `mlock`，断言 `errno == ENOMEM`（不是 EPERM）；再锁单页验证正常路径。

---

## 参考答案

### 练习 1 参考代码

```c
#include "tlpi_hdr.h"
#include <sys/mman.h>
#include <fcntl.h>

int main(int argc, char *argv[])
{
    if (argc != 2) usageErr("usage: %s <file>\n", argv[0]);

    int fd = open(argv[1], O_RDWR);
    if (fd == -1) errExit("open");

    off_t len = lseek(fd, 0, SEEK_END);

    char *map = mmap(NULL, len, PROT_READ | PROT_WRITE, MAP_SHARED, fd, 0);
    if (map == MAP_FAILED) errExit("mmap");

    /* 通过指针读写 */
    printf("first 10 bytes: %.10s\n", map);
    map[0] = 'X';   /* 写入会同步到文件（MAP_SHARED） */

    munmap(map, len);
    close(fd);
    return 0;
}
```

### 练习 2 参考代码

```c
#include "tlpi_hdr.h"
#include <sys/mman.h>
#include <signal.h>
#include <setjmp.h>

static sigjmp_buf env;

void handler(int sig)
{
    siglongjmp(env, 1);
}

int main(void)
{
    signal(SIGSEGV, handler);

    char *p = mmap(NULL, 4096, PROT_READ, MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
    if (p == MAP_FAILED) errExit("mmap");

    /* 尝试写入 → SIGSEGV */
    if (sigsetjmp(env, 1) == 0) {
        p[0] = 'X';
        printf("write succeeded (unexpected)\n");
    } else {
        printf("write failed with SIGSEGV (expected)\n");
    }

    /* 改为可读写 */
    if (mprotect(p, 4096, PROT_READ | PROT_WRITE) == -1)
        errExit("mprotect");

    p[0] = 'X';  /* 现在成功了 */
    printf("write after mprotect: %c\n", p[0]);

    munmap(p, 4096);
    return 0;
}
```

### 练习 3 答案

```c
#include <sys/mman.h>
#include <time.h>
#include <stdio.h>
#include <stdlib.h>

#define SIZE (1UL << 30)  /* 1GB */
#define ITERATIONS 1000

/* 随机偏移写一次，测单次访问延迟 */
static void bench_access(char *buf, const char *label)
{
    struct timespec start, end;
    long long max_ns = 0, min_ns = 1LL << 62, total_ns = 0;
    unsigned int seed = 42;

    for (int i = 0; i < ITERATIONS; i++) {
        size_t offset = ((size_t)rand_r(&seed) * 4096) % SIZE;
        clock_gettime(CLOCK_MONOTONIC, &start);
        buf[offset] = 'x';      /* 未驻留的页在此触发缺页 */
        clock_gettime(CLOCK_MONOTONIC, &end);

        long long ns = (end.tv_sec - start.tv_sec) * 1000000000LL
                     + (end.tv_nsec - start.tv_nsec);
        if (ns > max_ns) max_ns = ns;
        if (ns < min_ns) min_ns = ns;
        total_ns += ns;
    }
    printf("%s: avg=%lldns min=%lldns max=%lldns\n",
           label, total_ns / ITERATIONS, min_ns, max_ns);
}

int main(void)
{
    char *buf = mmap(NULL, SIZE, PROT_READ | PROT_WRITE,
                     MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
    if (buf == MAP_FAILED) { perror("mmap"); return 1; }

    /* 注意：bench 本身会逐步触碰页——先「冷测」再 mlockall 后「热测」
       严格冷测应 fork 子进程跑第一轮，父进程 mlock 后再测 */
    bench_access(buf, "before mlockall");

    if (mlockall(MCL_CURRENT | MCL_FUTURE) != 0) {
        /* 失败本身即 50.2 的语义演示：非特权进程受 RLIMIT_MEMLOCK 约束，
           超限部分锁不住，errno=ENOMEM（do_mlock 超限路径保持初值） */
        perror("mlockall（超限 ENOMEM，见 50.2）");
        return 0;   /* 演示目的已达成：失败路径本身就是结果 */
    }
    bench_access(buf, "after  mlockall");

    munmap(buf, SIZE);
    return 0;
}
```

**预期**：mlockall 前 max 延迟可能达毫秒级（硬缺页：分配页 + 清零 + 建映射），mlockall 后所有页驻留（`do_mlock` 的 `__mm_populate` 已预填充），max 降到微秒级（缓存 miss 而非缺页）。

**实测（WSL Ubuntu，非 root，RLIMIT_MEMLOCK 默认限额，2026-09-05）**：

```text
before mlockall: avg=1687ns min=17ns max=39278ns
mlockall（超限 ENOMEM，见 50.2）: Cannot allocate memory
```

1GB 远超限额 → 失败路径触发，**失败本身就是 50.2 的语义演示**：`can_do_mlock` 放行（限额非零即有资格）但 `do_mlock` 超限拒绝，errno 是 **ENOMEM 而非 EPERM**（超限路径保持初值 ENOMEM，EPERM 只在 `can_do_mlock` 拒绝时出现）。root 下 `ulimit -l unlimited` 后可跑出完整的前后对比。before 行的 max≈39µs 即硬缺页成本（分配+清零+建映射）；min=17ns 是 THP 大页摊薄后的热页写。

### 实验 4 参考代码（实测：2026-09-05，WSL Ubuntu，gcc -O2 -Wall -Wextra 零告警）

```c

/* mincore 零页口径差 + MADV_DONTNEED 双语义
 * 实测输出：
 *   mincore = [1,1,0,0]（页0=写触碰 页1=读触碰零页）
 *   同区 Rss = 4 kB（预期 4：零页 present 却不计入 RSS）
 *   全写后 mincore = [1,1,1,1]
 *   MADV_DONTNEED 后 mincore = [0,0,0,0]，q[0]=0x00
 *   文件 PRIVATE 改 'Z' 后 f[0]='Z'；DONTNEED 后 f[0]='O'
 *   madvise(99) = -1 errno=22(Invalid argument)
 */
#define _GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <fcntl.h>
#include <errno.h>
#include <sys/mman.h>
#include <unistd.h>

/* /proc/self/smaps 中包含 addr 的 VMA 的 Rss(kB) */
static long smaps_rss(void *addr)
{
    FILE *fp = fopen("/proc/self/smaps", "r");
    if (!fp) return -1;
    char line[300];
    unsigned long lo, hi, a = (unsigned long)addr;
    long rss = -1;
    int in_target = 0;
    while (fgets(line, sizeof line, fp)) {
        if (sscanf(line, "%lx-%lx", &lo, &hi) == 2)
            in_target = (a >= lo && a < hi);
        else if (in_target && strncmp(line, "Rss:", 4) == 0) {
            sscanf(line + 4, "%ld", &rss);
            break;
        }
    }
    fclose(fp);
    return rss;
}

int main(void)
{
    long pagesz = sysconf(_SC_PAGESIZE);
    size_t P = (size_t)pagesz;

    char *q = mmap(NULL, 4 * P, PROT_READ | PROT_WRITE,
                   MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
    if (q == MAP_FAILED) { perror("mmap"); return 1; }
    q[0] = 1;                        /* 页 0：写触碰（真页） */
    volatile char c = q[P]; (void)c; /* 页 1：读触碰（零页） */

    unsigned char vec[4];
    if (mincore(q, 4 * P, vec) != 0) { perror("mincore"); return 1; }
    printf("mincore = [%d,%d,%d,%d]（页0=写触碰 页1=读触碰零页）\n",
           vec[0] & 1, vec[1] & 1, vec[2] & 1, vec[3] & 1);
    printf("同区 Rss = %ld kB（预期 4：零页 present 却不计入 RSS）\n",
           smaps_rss(q));

    memset(q, 0x5A, 4 * P);
    mincore(q, 4 * P, vec);
    printf("全写后 mincore = [%d,%d,%d,%d]\n",
           vec[0] & 1, vec[1] & 1, vec[2] & 1, vec[3] & 1);
    if (madvise(q, 4 * P, MADV_DONTNEED) != 0) { perror("madvise"); return 1; }
    mincore(q, 4 * P, vec);
    printf("MADV_DONTNEED 后 mincore = [%d,%d,%d,%d]，q[0]=0x%02x（预期 0x00）\n",
           vec[0] & 1, vec[1] & 1, vec[2] & 1, vec[3] & 1, (unsigned char)q[0]);

    int fd = open("/tmp/ch50_d.dat", O_RDWR | O_CREAT | O_TRUNC, 0644);
    if (fd < 0) { perror("open"); return 1; }
    if (write(fd, "ORIG", 4) != 4) { perror("write"); return 1; }
    char *f = mmap(NULL, 4, PROT_READ | PROT_WRITE, MAP_PRIVATE, fd, 0);
    if (f == MAP_FAILED) { perror("mmap f"); return 1; }
    f[0] = 'Z';
    printf("文件 PRIVATE 改 'Z' 后 f[0]='%c'；", f[0]);
    if (madvise(f, 4, MADV_DONTNEED) != 0) { perror("madvise f"); return 1; }
    printf("DONTNEED 后 f[0]='%c'（预期 O：私有修改丢弃）\n", f[0]);

    errno = 0;
    int r = madvise(q, P, 99);
    printf("madvise(99) = %d errno=%d(%s)，预期 -1/EINVAL\n",
           r, errno, r ? strerror(errno) : "OK");

    munmap(q, 4 * P);
    munmap(f, 4);
    close(fd);
    unlink("/tmp/ch50_d.dat");
    return 0;
}
```

**解读三连**：
1. `mincore=[1,1,0,0]` 但 `Rss=4kB`——页 1 读触碰映射**零页**：PTE present（mincore 认）+ 全系统共享（RSS 不认）。
2. `MADV_DONTNEED` 后 mincore 全 0 且读回 `0x00`——匿名页的**确定性丢弃**。
3. 文件 PRIVATE 读回 'O'——丢的是 COW 副本，page cache 原页完好（对照 [49.4](../../chapter-49-memory-mappings/notes/49.4-file-mappings.md) 的可见性规则）。

### 实验 5 参考代码（实测：2026-09-05）

见 [50.2 代码示例](50.2-memory-locking-mlock-and-mlockall.md)（`mlock` 限额实测）。实测要点：WSL Ubuntu 默认 `RLIMIT_MEMLOCK=65536kB`（64MB，与桌面 Ubuntu 8MB 不同）；128MB 请求返回 `ENOMEM`（`do_mlock():575` 超限路径，非 EPERM）；单页 mlock 成功。**注意超限请求不消耗物理内存**——限额检查在 `apply_vma_lock_flags` 之前，填充根本不会执行。

---

## HFT / 嵌入式关联

- **mlockall**：HFT 必须锁定所有内存防止换页（超限是 ENOMEM 不是 EPERM）
- **mmap**：HFT 用文件映射共享行情数据
- **Huge Pages**：`mmap(MAP_HUGETLB)` / `madvise(MADV_HUGEPAGE)` 减少 TLB miss
- **嵌入式**：无 MMU 系统（MMU-less NOMMU）不支持 mprotect；mincore 在 XIP 场景口径要甄别

---

## 衔接

- **Ch49** Memory Mappings — [mmap 详解](../../chapter-49-memory-mappings/README.md)
- **Ch36** Process Resources — [RLIMIT_MEMLOCK](../../chapter-36-process-resources/README.md)
- **Ch49** 对比总结 — [mmap vs read/write 对比表](../../chapter-49-memory-mappings/notes/49.12-summary.md)
- 内核深挖：[06-linux-mm](../../../06-linux-mm/README.md)
