# 50.2 `mprotect`

> 本章：[TLPI 第 50 章 — Virtual Memory Operations](./README.md)
> - 上一节：[50.1 总览](./50.1-section-50-1.md)
> - 下一节：[50.3 `mlock` / `mlockall`](./50.3-mlock-mlockall.md)

---

## 本节讲什么

int mprotect(void *addr, size_t length, int prot); `PROT_NONE|READ|WRITE|EXEC`。违例 → **SIGSEGV**。 不能超过后备权限（只读文件映射 → 不可 `PROT_WRITE` → `EACCES`）。

---

## 要点

```c
int mprotect(void *addr, size_t length, int prot);
```

`PROT_NONE|READ|WRITE|EXEC`。违例 → **SIGSEGV**。  
不能超过后备权限（只读文件映射 → 不可 `PROT_WRITE` → `EACCES`）。  
JIT：`W` 写码 → 改成 `R|X`；现代常强制 **W^X**（不可同时 W+X）。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `mprotect` 在嵌入式系统的应用场景 |
| HFT | `mprotect` 对低延迟和性能的影响 |

## 代码示例

```c

/* mprotect 修改内存保护属性 */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <signal.h>
#include <setjmp.h>
#include <sys/mman.h>
#include <unistd.h>

static jmp_buf jmpbuf;
static void handler(int sig) { longjmp(jmpbuf, 1); }

int main(void) {
    size_t len = 4096;
    char *p = mmap(NULL, len, PROT_READ | PROT_WRITE,
                   MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
    if (p == MAP_FAILED) { perror("mmap"); exit(1); }
    strcpy(p, "hello");

    /* 改为只读 */
    if (mprotect(p, len, PROT_READ) < 0) { perror("mprotect"); exit(1); }
    printf("mprotect -> PROT_READ OK\n");

    /* 尝试写入会触发 SIGSEGV */
    signal(SIGSEGV, handler);
    if (setjmp(jmpbuf) == 0) {
        p[0] = 'X';  /* 这里会触发 SIGSEGV */
    } else {
        printf("caught SIGSEGV: write to read-only page\n");
    }

    /* 改回可写 */
    if (mprotect(p, len, PROT_READ | PROT_WRITE) < 0) { perror("mprotect 2"); exit(1); }
    p[0] = 'X';
    printf("mprotect -> PROT_READ|PROT_WRITE OK, wrote 'X'\n");

    munmap(p, len);
    return 0;
}

```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[50.1 总览](./50.1-section-50-1.md)
- 下一节：[50.3 `mlock` / `mlockall`](./50.3-mlock-mlockall.md)
- 前置：**前置**：[Ch49 mmap](../chapter-49-memory-mappings/notes.md)  
- 后置：**后置**：[Ch51 POSIX IPC 导论](../chapter-51-posix-ipc-intro/notes.md)
