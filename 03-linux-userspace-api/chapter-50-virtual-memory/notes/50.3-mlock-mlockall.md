# 50.3 `mlock` / `mlockall`

> 本章：[TLPI 第 50 章 — Virtual Memory Operations](./README.md)
> - 上一节：[50.2 `mprotect`](./50.2-mprotect.md)
> - 下一节：[50.4 `mincore`](./50.4-mincore.md)

---

## 本节讲什么

int mlock(const void *addr, size_t length); int munlock(const void *addr, size_t length); int mlockall(int flags);   /* MCL_CURRENT | MCL_FUTURE */

---

## 要点

```c
int mlock(const void *addr, size_t length);
int munlock(const void *addr, size_t length);
int mlockall(int flags);   /* MCL_CURRENT | MCL_FUTURE */
int munlockall(void);
```

禁 swap，减缺页 IO 延迟（实时 / HFT）。  
- 用户有 **`ulimit -l`** 上限；超限失败  
- 同区多次 `mlock`，**一次 `munlock` 即解**（非叠加计数）  
- `fork` 继承锁；**`exec` 全解锁**  
- `MCL_FUTURE`：后续分配也锁 → 易耗尽限额  
慎用：挤占 RAM → 他进程 OOM。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `mlock` / `mlockall` 在嵌入式系统的应用场景 |
| HFT | `mlock` / `mlockall` 对低延迟和性能的影响 |

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[50.2 `mprotect`](./50.2-mprotect.md)
- 下一节：[50.4 `mincore`](./50.4-mincore.md)
- 前置：**前置**：[Ch49 mmap](../chapter-49-memory-mappings/notes.md)  
- 后置：**后置**：[Ch51 POSIX IPC 导论](../chapter-51-posix-ipc-intro/notes.md)
