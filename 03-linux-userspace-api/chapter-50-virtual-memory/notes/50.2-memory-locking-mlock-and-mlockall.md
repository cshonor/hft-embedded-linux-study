# 50.2 内存锁定：mlock() 与 mlockall()

> 源码核验：Linux v6.6 · `mm/mlock.c`（`can_do_mlock():40` · `__mlock_posix_error_return():566` · `do_mlock():575` · `SYSCALL_DEFINE2(mlock):622` · `SYSCALL_DEFINE1(mlockall):705`）
> 本章：[TLPI 第 50 章 — Virtual Memory Operations](../README.md)
> - 上一节：[50.1 `mprotect`](50.1-changing-memory-protection-mprotect.md)
> - 下一节：[50.3 驻留查询 `mincore`](50.3-determining-memory-residence-mincore.md)

---

## 本节讲什么

`mlock` 家族把页**钉在物理内存**（页标记 `Mlocked`，从 LRU 链表摘除），换出器永不回收。三个灵魂问题：

1. 为什么要锁？（缺页/换入换出是**毫秒级**延迟毛刺的根源——HFT 大忌）
2. 谁有资格锁？（`RLIMIT_MEMLOCK` 限额，超限返回什么？——**`ENOMEM`，不是 `EPERM`**，经典面试题）
3. 锁住了什么？（只锁**当前已驻留**的页 + 顺带预填充；`MCL_FUTURE` 锁未来）

```c
#include <sys/mman.h>
int mlock(const void *addr, size_t length);
int munlock(const void *addr, size_t length);
int mlock2(const void *addr, size_t length, int flags);   /* Linux 扩展 */
int mlockall(int flags);    /* MCL_CURRENT | MCL_FUTURE | MCL_ONFAULT(2) */
int munlockall(void);
```

---

## 要点

### 语义规则

| 规则 | 说明 |
|------|------|
| `mlock` 立即预填充 | `do_mlock()` 末尾 `__mm_populate()`：把区间内所有页**当场缺页进来**（不是等访问） |
| 多次 `mlock` 不叠加 | 同区 `mlock` 两次，一次 `munlock` 即解除（无计数语义，与 `fcntl` 记录锁不同） |
| `addr` 页对齐，`length` 向上取整 | 与 `mprotect` 一致 |
| `fork`：**子进程继承锁** | 页在父子间 COW；`VM_LOCKED` 标志遗传给子 VMA |
| `exec`：**全部解锁** | 新程序映像替换整个地址空间 |
| `munmap` 自动解除该区锁定 | 生命周期跟随 VMA |

### 权限模型（v6.6 `can_do_mlock():40` 逐行核验）

```c
bool can_do_mlock(void)
{
	if (rlimit(RLIMIT_MEMLOCK) != 0)   /* 限额非零（哪怕 4KB）→ 有资格 */
		return true;
	if (capable(CAP_IPC_LOCK))         /* 或持有 CAP_IPC_LOCK */
		return true;
	return false;                      /* 否则 EPERM——限额为 0 且无特权 */
}
```

`do_mlock():575` 的限额检查（简化）：

```c
locked = len >> PAGE_SHIFT;                     /* 本次请求页数 */
locked += current->mm->locked_vm;               /* 加已锁总数 */
if ((locked > lock_limit) && (!capable(CAP_IPC_LOCK)))
    /* error 保持初值 -ENOMEM → 返回 ENOMEM */
```

⭐ **错误码分界**：
- 限额为 0 且无 `CAP_IPC_LOCK` → `EPERM`（连资格都没有）
- 有资格但**超量** → `ENOMEM`（不是 EPERM！）——TLPI 与 man page 都强调这一点
- 另有 `__mlock_posix_error_return():566`：GUP 层的 `-EFAULT→-ENOMEM`、`-ENOMEM→-EAGAIN`（POSIX 规定的换算）

### 与 msync 的联动（实测验证）

`msync(MS_INVALIDATE)` 作用到带 `VM_LOCKED` 的区域 → **`EBUSY`**（mm/msync.c 的显式检查：`if ((flags & MS_INVALIDATE) && (vma->vm_flags & VM_LOCKED)) error = -EBUSY`）。锁定的页不允许从进程地址空间「作废」。

### `MCL_FUTURE` 的坑

`mlockall(MCL_CURRENT|MCL_FUTURE)` 之后**每次** mmap/malloc/栈增长都要过限额检查——超限时 `mmap` 返回 `ENOMEM`，malloc 返回 NULL。程序会以「莫名其妙的内存分配失败」形式死亡，而不是死在 mlock 调用点。

### 实测数据（WSL Ubuntu，2026-09-05）

```
RLIMIT_MEMLOCK soft=65536 kB hard=65536 kB     ← WSL Ubuntu 默认 64MB
mlock(128MB) = -1 errno=12(Cannot allocate memory)   ← 超限走 ENOMEM ✓
mlock(1 页) = 0 成功
```

注意 WSL 的默认限额（64MB）与桌面 Ubuntu 22.04（8MB）不同，脚本化部署时必须显式 `ulimit -l` / systemd `LimitMEMLOCK`。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 无 swap 的系统 mlock 几乎无意义（没地方换出），但**预填充**语义仍有价值——启动时把关键页摸一遍，避免运行时首次缺页抖动；RAM 紧张时锁多了直接挤爆伙伴系统（`mlock` 的页不参与回收 → 别人 OOM） |
| HFT | **`mlockall(MCL_CURRENT\|MCL_FUTURE)` 是行业标配**：预热阶段把行情缓冲/策略热数据全部驻留，消除两类毛刺——缺页（µs 级）与换入换出（**ms 级**）；配套动作：① `setrlimit`/systemd `LimitMEMLOCK=infinity`（或 `CAP_IPC_LOCK`）② 预分配所有缓冲再 `MCL_CURRENT` ③ 慎用 `MCL_FUTURE`——它会掩盖「忘了锁哪块」的 bug，让 malloc 在意想不到的地方失败 |

实测经验：单纯 `mlockall` 不够——**从未触碰过的页不在 `MCL_CURRENT` 的填充范围**……不对，v6.6 的 `mlockall→do_mlockall→apply_vma_lock_flags + __mm_populate` 会主动填充全部当前 VMA；真正的坑是 `MAP_NORESERVE` 大映射 + `mlockall` 会把整个稀疏区灌满物理页。**锁之前先想清楚区间的真实工作集大小**。

---

## 代码示例

```c

/* mlock 限额实测：超限 ENOMEM（非 EPERM）+ 小块成功
 * 实测（WSL Ubuntu，gcc -O2 -Wall -Wextra，2026-09-05）：
 *   RLIMIT_MEMLOCK soft=65536 kB hard=65536 kB
 *   mlock(128MB) = -1 errno=12(Cannot allocate memory)   ← 超限 ENOMEM
 *   mlock(1 页) = 0 成功
 */
#define _GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <errno.h>
#include <sys/mman.h>
#include <sys/resource.h>
#include <unistd.h>

int main(void)
{
    long pagesz = sysconf(_SC_PAGESIZE);

    struct rlimit rl;
    if (getrlimit(RLIMIT_MEMLOCK, &rl) != 0) { perror("getrlimit"); return 1; }
    printf("RLIMIT_MEMLOCK soft=%lu kB hard=%lu kB\n",
           (unsigned long)rl.rlim_cur / 1024, (unsigned long)rl.rlim_max / 1024);

    char *p = mmap(NULL, (size_t)pagesz, PROT_READ | PROT_WRITE,
                   MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
    if (p == MAP_FAILED) { perror("mmap"); return 1; }

    /* 超限请求：按实测限额动态取 2 倍，保证真的超 */
    size_t big_len = 64UL << 20;
    if (big_len <= rl.rlim_cur) big_len = rl.rlim_cur * 2;
    char *big = mmap(NULL, big_len, PROT_READ | PROT_WRITE,
                     MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
    if (big == MAP_FAILED) { perror("mmap big"); return 1; }
    errno = 0;
    int r = mlock(big, big_len);
    printf("mlock(%zuMB) = %d", big_len >> 20, r);
    if (r != 0)
        printf(" errno=%d(%s)  ← 超限是 ENOMEM 不是 EPERM\n",
               errno, strerror(errno));
    munmap(big, big_len);

    errno = 0;
    r = mlock(p, (size_t)pagesz);
    printf("mlock(1 页) = %d %s\n", r, r ? strerror(errno) : "成功");

    munlock(p, (size_t)pagesz);
    munmap(p, (size_t)pagesz);
    return 0;
}
```

超限请求是安全的：`do_mlock()` 在限额检查**不通过时不会执行填充**（`apply_vma_lock_flags` 被跳过），64MB 只是虚拟区，一页物理内存都不会消耗。

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[50.1 `mprotect`](50.1-changing-memory-protection-mprotect.md)
- 下一节：[50.3 驻留查询 `mincore`](50.3-determining-memory-residence-mincore.md)——锁完之后怎么验证真的驻留了？mincore
- 资源限制：[Ch36 进程资源](../../chapter-36-process-resources/README.md)——`RLIMIT_MEMLOCK` 的设置与继承
- msync 联动：[49.5 `msync`](../../chapter-49-memory-mappings/notes/49.5-synchronizing-a-mapped-region-msync.md)——`MS_INVALIDATE` 遇 `VM_LOCKED` 返回 `EBUSY`（实测）
- 同类锁定：[Ch50.3](50.3-determining-memory-residence-mincore.md) + [Ch49.9 `MAP_NORESERVE`](../../chapter-49-memory-mappings/notes/49.9-map-noreserve-and-swap-space-overcommitt.md)

---

## 自测题

**题目 1（选择）：** 普通用户（RLIMIT_MEMLOCK=8MB）执行 `mlock(p, 64MB)` 返回 -1，errno 是？

A. `EPERM`　B. `ENOMEM`　C. `EAGAIN`　D. `EINVAL`

<details>
<summary>参考答案</summary>

**B（ENOMEM）**。`do_mlock()`（mm/mlock.c:575）限额检查失败时 error 保持初值 `-ENOMEM`。`EPERM` 只出现在「限额为 0 且无 CAP_IPC_LOCK」（`can_do_mlock()` 拒绝）。TLPI 原文和 man page 都专门强调这个反直觉点。

</details>

**题目 2（判断）：** 对同一区域调用两次 `mlock`，需要调用两次 `munlock` 才能解锁。

<details>
<summary>参考答案</summary>

错误。`mlock` 无计数语义（与 `fcntl` 记录锁不同）：VMA 的 `VM_LOCKED` 是布尔标志，同区重复 `mlock` 幂等，一次 `munlock` 全解。跨进程各自 `mlock` 同一共享区时，页在**所有**引用者的 VMA 都解锁后才真正回到可换出状态。

</details>

**题目 3（应用）：** HFT 进程启动脚本里 `ulimit -l unlimited` 报错「operation not permitted」，怎么办？

<details>
<summary>参考答案</summary>

普通用户不能把 soft limit 提到 hard limit 之上。三条路：① root 下 `ulimit -l` 后再降权运行（setpriv/--cap-drop）；② systemd service 单元写 `LimitMEMLOCK=infinity`；③ 给二进制 file capability `cap_ipc_lock+ep`（`setcap cap_ipc_lock+ep ./trader`），进程持 `CAP_IPC_LOCK` 后 `can_do_mlock()` 直接放行、限额检查也被跳过。生产上 ③ 最常见，但要注意 capabilities 随 exec 的继承规则（见 Ch39）。

</details>
