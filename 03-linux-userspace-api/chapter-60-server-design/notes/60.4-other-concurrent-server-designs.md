# 60.4 多线程

> 本章：[TLPI 第 60 章 — Sockets: Server Design](./README.md)
> - 上一节：[60.2 –60.3 fork 并发](./60.2-fork.md)
> - 下一节：[60.5 事件驱动（select/poll 入门）](./60.5-select-poll.md)

---

## 本节讲什么

`accept → pthread_create(handler)`。 **坑**：传 `&connfd` → 主循环覆盖 → 多线程同一 fd。 修：堆上分配 fd 副本，或加锁传递。

---

## 要点

`accept → pthread_create(handler)`。  
**坑**：传 `&connfd` → 主循环覆盖 → 多线程同一 fd。  
修：堆上分配 fd 副本，或加锁传递。  
优：轻、易共享状态；劣：一线程崩整进程、须加锁、线程过多调度差。进阶：线程池。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 多线程 在嵌入式系统的应用场景 |
| HFT | 多线程 对低延迟和性能的影响 |

## 代码示例

```c

/* 线程并发服务器：每个连接一个线程 */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <pthread.h>

static void *handle_client(void *arg) {
    int cfd = *(int *)arg;
    free(arg);

    char buf[128];
    ssize_t n = recv(cfd, buf, sizeof(buf)-1, 0);
    if (n > 0) {
        buf[n] = '\0';
        printf("thread: received '%s'\n", buf);
    }
    send(cfd, "thread reply", 13, 0);
    close(cfd);
    return NULL;
}

int main(void) {
    int sfd = socket(AF_INET, SOCK_STREAM, 0);
    struct sockaddr_in addr = { .sin_family = AF_INET, .sin_port = htons(9993) };
    addr.sin_addr.s_addr = htonl(INADDR_ANY);
    int opt = 1; setsockopt(sfd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));
    bind(sfd, (struct sockaddr *)&addr, sizeof(addr));
    listen(sfd, 5);

    printf("thread server: port 9993\n");

    for (int i = 0; i < 3; i++) {
        int cfd = accept(sfd, NULL, NULL);
        if (cfd < 0) continue;

        int *arg = malloc(sizeof(int));
        *arg = cfd;

        pthread_t tid;
        pthread_create(&tid, NULL, handle_client, arg);
        pthread_detach(tid);  /* 自动回收 */
    }

    close(sfd);
    sleep(1);  /* 等线程完成 */
    return 0;
}

```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[60.2 –60.3 fork 并发](./60.2-fork.md)
- 下一节：[60.5 事件驱动（select/poll 入门）](./60.5-select-poll.md)
- 前置：**前置**：[Ch59 Internet Domains](../chapter-59-internet-domains/notes.md)  
- 后置：**后置**：[Ch61 Socket Advanced](../chapter-61-sockets-advanced/notes.md)
