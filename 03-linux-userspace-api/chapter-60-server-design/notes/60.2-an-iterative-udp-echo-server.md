# 60.2 –60.3 fork 并发

> 本章：[TLPI 第 60 章 — Sockets: Server Design](../README.md)
> - 上一节：[60.1 迭代服务器](60.1-iterative-and-concurrent-servers.md)
> - 下一节：[60.4 多线程](60.4-other-concurrent-server-designs.md)

---

## 本节讲什么

accept → fork 子: close(listen); handle; close(conn); _exit 父: close(conn); 继续 accept

---

## 要点

```
accept → fork
  子: close(listen); handle; close(conn); _exit
  父: close(conn); 继续 accept
```

| 要点 | |
|------|--|
| fd 引用 | fork 后 +1；双方关无用端 |
| 僵尸 | `SIGCHLD` 里循环 `waitpid(-1,NULL,WNOHANG)` |
| 隔离 | 强；崩溃不易拖垮主服务 |
| 代价 | fork/页表贵；海量连接受限 |

Demo：[`code/fork_sv.c`](../code/fork_sv.c)

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | –60.3 fork 并发 在嵌入式系统的应用场景 |
| HFT | –60.3 fork 并发 对低延迟和性能的影响 |

## 代码示例

```c

/* fork 并发服务器：每个连接一个子进程 */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <sys/wait.h>

static void handle_client(int cfd) {
    char buf[128];
    ssize_t n = recv(cfd, buf, sizeof(buf)-1, 0);
    if (n > 0) {
        buf[n] = '\0';
        printf("child %d: received '%s'\n", getpid(), buf);
    }
    send(cfd, "fork reply", 11, 0);
    close(cfd);
    _exit(0);
}

int main(void) {
    int sfd = socket(AF_INET, SOCK_STREAM, 0);
    struct sockaddr_in addr = { .sin_family = AF_INET, .sin_port = htons(9994) };
    addr.sin_addr.s_addr = htonl(INADDR_ANY);
    int opt = 1; setsockopt(sfd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));
    bind(sfd, (struct sockaddr *)&addr, sizeof(addr));
    listen(sfd, 5);

    printf("fork server: port 9994\n");

    /* SIGCHLD 回收子进程 */
    signal(SIGCHLD, SIG_IGN);  /* 简化：自动回收 */

    for (int i = 0; i < 3; i++) {
        int cfd = accept(sfd, NULL, NULL);
        if (cfd < 0) continue;

        pid_t pid = fork();
        if (pid == 0) {
            close(sfd);  /* 子关闭监听 fd */
            handle_client(cfd);
        }
        close(cfd);  /* 父关闭连接 fd */
    }

    close(sfd);
    return 0;
}

```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[60.1 迭代服务器](60.1-iterative-and-concurrent-servers.md)
- 下一节：[60.4 多线程](60.4-other-concurrent-server-designs.md)
- 前置：**前置**：Ch59 Internet Domains  
- 后置：**后置**：[Ch61 Socket Advanced](../../chapter-61-sockets-advanced/notes)
