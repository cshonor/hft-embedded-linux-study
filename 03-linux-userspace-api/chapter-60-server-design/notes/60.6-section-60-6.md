# 60.6 工程问题

> 本章：[TLPI 第 60 章 — Sockets: Server Design](./README.md)
> - 上一节：[60.5 事件驱动（select/poll 入门）](./60.5-select-poll.md)
> - 下一节：[60.7 对比](./60.7-comparison.md)

---

## 本节讲什么

---

---

## 要点

| 问题 | 处理 |
|------|------|
| `Address already in use` / TIME_WAIT | `SO_REUSEADDR`（与 `SO_REUSEPORT` 不同） |
| 惊群 | 多进程堵同一 accept 时全醒；现代内核 listen 侧已改善 |
| fd 耗尽 | `ulimit -n` / `RLIMIT_NOFILE` |
| 慢客户端 | 迭代最惨；事件环须超时踢闲置 |

---

---

## 与 C 语言学习的关系

Socket 编程是 C 语言网络编程的核心。`socket/bind/listen/accept` 的调用顺序需要理解 C 语言的 struct（如 `sockaddr_in`）、字节序（`htons/ntohs`）和字符串处理。在 HFT 场景中，UDP socket 和零拷贝技术是低延迟网络的基础。注意 `SIGPIPE` 是网络编程的经典陷阱。
---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 工程问题 在嵌入式系统的应用场景 |
| HFT | 工程问题 对低延迟和性能的影响 |

---

## 常见陷阱

1. **SIGPIPE 未处理**

```c
send(fd, buf, n, 0);  /* 对端关闭 → SIGPIPE */
send(fd, buf, n, MSG_NOSIGNAL);  /* 安全 */
```

向已关闭连接的 socket 写数据触发 SIGPIPE。用 MSG_NOSIGNAL 或忽略 SIGPIPE。

2. **TIME_WAIT 导致端口复用失败**

```c
bind(fd, (struct sockaddr*)&addr, len);
/* EADDRINUSE if port in TIME_WAIT */
/* 解决：SO_REUSEADDR */
```

服务端重启时端口可能还在 TIME_WAIT。用 SO_REUSEADDR 允许绑定。

3. **connect 返回值未检查**

```c
connect(sfd, (struct sockaddr*)&addr, sizeof(addr));
send(sfd, "data", 4, 0);  /* connect 可能失败! */
```

connect 可能失败（连接被拒绝、超时等）。不检查直接 send 会导致错误。

---

## 自测题

**题目 1（选择）：** TCP 服务端的 socket API 调用顺序是？
A. connect → send → recv
B. socket → bind → listen → accept
C. socket → connect → send
D. bind → socket → listen

<details>
<summary>参考答案</summary>

B。服务端：socket() → bind() → listen() → accept()。客户端：socket() → connect()。

</details>

**题目 2（判断）：** `bind` 可以绑定任意端口号。

<details>
<summary>参考答案</summary>

错误。1-1023 是特权端口，需要 root 权限。已占用的端口会返回 EADDRINUSE。

</details>

**题目 3（踩坑）：** 以下代码有什么问题？
```c
struct sockaddr_in addr;
addr.sin_family = AF_INET;
addr.sin_port = 8080;  /* 没有用 htons! */
addr.sin_addr.s_addr = INADDR_ANY;
bind(fd, (struct sockaddr*)&addr, sizeof(addr));
```

<details>
<summary>参考答案</summary>

端口号没有用 htons 转换字节序。8080 = 0x1F90，在小端机器上变成 0x901F = 36895。应该 `addr.sin_port = htons(8080);`

</details>

**题目 4（应用）：** 写代码创建 TCP 服务端 socket 并监听。

<details>
<summary>参考答案</summary>

```c
int sfd = socket(AF_INET, SOCK_STREAM, 0);
int opt = 1;
setsockopt(sfd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));
struct sockaddr_in addr = {0};
addr.sin_family = AF_INET;
addr.sin_port = htons(8080);
addr.sin_addr.s_addr = INADDR_ANY;
bind(sfd, (struct sockaddr*)&addr, sizeof(addr));
listen(sfd, 128);
```

</details>

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[60.5 事件驱动（select/poll 入门）](./60.5-select-poll.md)
- 下一节：[60.7 对比](./60.7-comparison.md)
- 前置：**前置**：[Ch59 Internet Domains](../chapter-59-internet-domains/notes.md)  
- 后置：**后置**：[Ch61 Socket Advanced](../chapter-61-sockets-advanced/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
