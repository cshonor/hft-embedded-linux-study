# 57.2 STREAM

> 本章：[TLPI 第 57 章 — Sockets: UNIX Domain](../README.md)
> - 上一节：[57.1 `struct sockaddr_un`](57.1-unix-domain-socket-addresses-struct-sock.md)
> - 下一节：[57.3 DGRAM（高频）](57.3-datagram-sockets-in-the-unix-domain.md)

---

## 本节讲什么

同 TCP 流程：`bind→listen→accept` / `connect`；全双工字节流。 退出**不删** socket 文件 → 再 bind `EADDRINUSE` → bind 前 `unlink`。 相对 pipe：**双向**。路径流式示例见 [Ch56 `us_stream`](../../chapter-56-sockets-intro/code/)。

---

## 要点

同 TCP 流程：`bind→listen→accept` / `connect`；全双工字节流。  
退出**不删** socket 文件 → 再 bind `EADDRINUSE` → bind 前 `unlink`。  
相对 pipe：**双向**。路径流式示例见 [Ch56 `us_stream`](../../chapter-56-sockets-intro/code/)。

## 代码示例

```c

/* AF_UNIX SOCK_STREAM 服务器+客户端完整示例 */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/socket.h>
#include <sys/un.h>
#include <sys/wait.h>

int main(void) {
    const char *path = "/tmp/ch57_stream.sock";
    unlink(path);

    int sfd = socket(AF_UNIX, SOCK_STREAM, 0);
    struct sockaddr_un addr = { .sun_family = AF_UNIX };
    strncpy(addr.sun_path, path, sizeof(addr.sun_path) - 1);
    bind(sfd, (struct sockaddr *)&addr, sizeof(addr));
    listen(sfd, 5);

    pid_t pid = fork();
    if (pid == 0) {
        int cfd = socket(AF_UNIX, SOCK_STREAM, 0);
        struct sockaddr_un caddr = { .sun_family = AF_UNIX };
        strncpy(caddr.sun_path, path, sizeof(caddr.sun_path) - 1);
        connect(cfd, (struct sockaddr *)&caddr, sizeof(caddr));

        /* 发送多段数据 */
        send(cfd, "part1-", 6, 0);
        send(cfd, "part2-", 6, 0);
        send(cfd, "part3", 5, 0);

        char buf[64];
        ssize_t n = recv(cfd, buf, sizeof(buf) - 1, 0);
        if (n > 0) { buf[n] = '\0'; printf("client: reply='%s'\n", buf); }
        close(cfd);
        _exit(0);
    }

    int afd = accept(sfd, NULL, NULL);
    /* 流式 socket 保证有序可靠，但可能合并多次 send */
    char buf[128];
    ssize_t n = recv(afd, buf, sizeof(buf) - 1, 0);
    if (n > 0) { buf[n] = '\0'; printf("server: received='%s' (%zd bytes)\n", buf, n); }
    send(afd, "ack", 4, 0);
    close(afd);

    wait(NULL);
    close(sfd);
    unlink(path);
    return 0;
}

```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[57.1 `struct sockaddr_un`](57.1-unix-domain-socket-addresses-struct-sock.md)
- 下一节：[57.3 DGRAM（高频）](57.3-datagram-sockets-in-the-unix-domain.md)
- 前置：**前置**：[Ch56 Socket 导论](../../chapter-56-sockets-intro/notes)  
- 后置：**后置**：[Ch58 TCP/IP 基础](../../chapter-58-tcpip-fundamentals/notes)
