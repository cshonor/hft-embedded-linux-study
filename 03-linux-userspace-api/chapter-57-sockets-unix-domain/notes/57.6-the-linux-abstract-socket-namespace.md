# 57.6 抽象命名空间（Linux）

> 本章：[TLPI 第 57 章 — Sockets: UNIX Domain](../README.md)
> - 上一节：[57.5 `socketpair`](57.5-creating-a-connected-socket-pair-socketp.md)
> - 下一节：下一章+1:02d}-*/notes.md)

---

## 本节讲什么

`sun_path[0] = '\0'`，其后为名字。 HFT/嵌入式守护进程常用抽象名，避免升级残留。 Demo：[`code/`](../code/)

---

## 要点

`sun_path[0] = '\0'`，其后为名字。  

| | 路径 UDS | 抽象 |
|--|----------|------|
| 文件 | 有，须 unlink | **无** |
| 残留 | 易卡启动 | 全 close 即消 |
| 权限 | 文件 rwx | 不受目录权限 |
| 移植 | 可移植 | **仅 Linux** |

HFT/嵌入式守护进程常用抽象名，避免升级残留。

Demo：[`code/`](../code/)

## 代码示例

```c

/* AF_UNIX 抽象命名空间：不创建文件系统条目 */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/socket.h>
#include <sys/un.h>
#include <sys/wait.h>

int main(void) {
    int sfd = socket(AF_UNIX, SOCK_STREAM, 0);

    /* 抽象命名空间：sun_path[0] = '\0'，后面是名字 */
    struct sockaddr_un addr;
    memset(&addr, 0, sizeof(addr));
    addr.sun_family = AF_UNIX;
    addr.sun_path[0] = '\0';  /* 抽象命名空间标志 */
    strncpy(addr.sun_path + 1, "ch57_abstract", sizeof(addr.sun_path) - 1);

    /* bind 不会在文件系统创建文件 */
    if (bind(sfd, (struct sockaddr *)&addr, sizeof(struct sockaddr_un)) < 0) {
        perror("bind"); exit(1);
    }
    listen(sfd, 1);
    printf("abstract socket: \\0ch57_abstract (no file on disk)\n");

    /* 优点：不需要清理文件，不会残留 */
    /* 生命周期：随 socket fd 关闭自动消失 */

    pid_t pid = fork();
    if (pid == 0) {
        int cfd = socket(AF_UNIX, SOCK_STREAM, 0);
        struct sockaddr_un caddr;
        memset(&caddr, 0, sizeof(caddr));
        caddr.sun_family = AF_UNIX;
        caddr.sun_path[0] = '\0';
        strncpy(caddr.sun_path + 1, "ch57_abstract", sizeof(caddr.sun_path) - 1);
        connect(cfd, (struct sockaddr *)&caddr, sizeof(struct sockaddr_un));
        send(cfd, "abstract", 9, 0);
        close(cfd);
        _exit(0);
    }

    int afd = accept(sfd, NULL, NULL);
    char buf[32];
    ssize_t n = recv(afd, buf, sizeof(buf) - 1, 0);
    if (n > 0) { buf[n] = '\0'; printf("received: %s\n", buf); }
    close(afd);

    wait(NULL);
    close(sfd);
    /* 无需 unlink（抽象命名空间没有文件） */
    return 0;
}

```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[57.5 `socketpair`](57.5-creating-a-connected-socket-pair-socketp.md)
- 前置：**前置**：[Ch56 Socket 导论](../../chapter-56-sockets-intro/notes)  
- 后置：**后置**：[Ch58 TCP/IP 基础](../../chapter-58-tcpip-fundamentals/notes)
