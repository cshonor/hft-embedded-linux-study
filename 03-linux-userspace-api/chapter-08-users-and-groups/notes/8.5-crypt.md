# 8.5 `crypt()` — 密码加密

> 本章：[TLPI 第 08 章 — Users and Groups](./README.md)
> - 上一节：[8.4 `/etc/shadow`（仅 root 可读）](./8.4-etc-shadow-root.md)
> - 下一节：[8.6 附属组 Supplementary Groups](./8.6-supplementary-groups.md)

---

## 本节讲什么

#define _XOPEN_SOURCE #include <unistd.h> char *crypt(const char *key, const char *salt);

---

## 要点

```c
#define _XOPEN_SOURCE
#include <unistd.h>
char *crypt(const char *key, const char *salt);
```

| | |
|--|--|
| key | 明文密码 |
| salt | 取自 shadow 密文头部（算法标识 + 盐） |
| 返回 | 静态缓冲；**勿 free**；非线程安全 |

认证：`getspnam` → 取 `sp_pwdp` 作 salt → `crypt` → 与密文比字符串。  
验证后立刻**覆盖清空**明文缓冲。

Demo：[`code/check_password.c`](./code/check_password.c)（需能读 shadow，通常 root）

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[8.4 `/etc/shadow`（仅 root 可读）](./8.4-etc-shadow-root.md)
- 下一节：[8.6 附属组 Supplementary Groups](./8.6-supplementary-groups.md)
- 前置：**前置**：[Ch6 Processes](../chapter-06-processes/notes.md)  
- 后置：**后置**：[Ch9 进程凭证](../chapter-09-process-credentials/notes.md) · [Ch15 文件属性/权限](../chapter-15-file-attributes/notes.md)

---

## man 手册参考

- `man 2` / `man 3` — 查阅本节涉及的系统调用和库函数
- `man 7` — 查阅概述类页面（如 `man 7 signal`、`man 7 ip`）
