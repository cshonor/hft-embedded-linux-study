# 59.14 UNIX 域 vs Internet 域 Socket

> 本章：[TLPI 第 59 章 — Internet 域 Socket](../../README.md)
> - 上一节：59.13 并发服务端
> - 下一节：[59.15 进一步信息](./59.15-further-information.md)

---

## 本节讲什么

UNIX 域 socket 与 Internet 域 socket 的对比和选型。

---

## 要点

### 对比表

| 维度 | UNIX 域 (AF_UNIX) | Internet 域 (AF_INET) |
|------|-------------------|----------------------|
| 范围 | 本机 | 跨主机 |
| 地址 | 文件路径 | IP + 端口 |
| 性能 | 更快（无网络栈） | 较慢（TCP/IP 栈） |
| 权限 | 文件系统权限 | 无内置权限控制 |
| 连接管理 | 无 TIME_WAIT | 有 TIME_WAIT |
| 数据拷贝 | 可零拷贝（SCM_RIGHTS） | 需拷贝 |

### 性能差异

- UNIX 域 socket 比 TCP 快 2-3 倍（无网络栈开销）
- 大块数据传输差异更明显
- UNIX 域 socket 支持 `SCM_RIGHTS` 传递 fd（零拷贝）

### 何时用 UNIX 域

- 本机进程间通信（如 Docker daemon, X11）
- 需要文件系统权限控制
- 性能敏感场景
- 需要传递 fd

### 何时用 Internet 域

- 跨主机通信
- 客户端分布在不同机器
- 需要标准网络协议

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 本机组件间通信用 UNIX 域更快更安全 |
| HFT | 行情网关用 TCP 接收，内部组件间用 UNIX 域或共享内存 |

## 代码示例

```c
#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <sys/socket.h>
#include <sys/un.h>
#include <sys/wait.h>
/* UNIX 域 socket echo */
#define PATH "/tmp/test_unix_sock"

int main(void) {
    pid_t pid = fork();
    if (pid == 0) {
        /* 服务端 */
        int sfd = socket(AF_UNIX, SOCK_STREAM, 0);
        struct sockaddr_un addr = { .sun_family = AF_UNIX };
        strcpy(addr.sun_path, PATH);
        unlink(PATH);
        bind(sfd, (struct sockaddr *)&addr, sizeof(addr));
        listen(sfd, 5);
        int cfd = accept(sfd, NULL, NULL);
        char buf[64];
        ssize_t n = read(cfd, buf, sizeof(buf)-1);
        if (n > 0) { buf[n] = 0; printf("server: %s\n", buf); }
        close(cfd); close(sfd); unlink(PATH);
        _exit(0);
    }

    usleep(100000);
    /* 客户端 */
    int fd = socket(AF_UNIX, SOCK_STREAM, 0);
    struct sockaddr_un addr = { .sun_family = AF_UNIX };
    strcpy(addr.sun_path, PATH);
    connect(fd, (struct sockaddr *)&addr, sizeof(addr));
    write(fd, "hello unix", 10);
    close(fd);
    wait(NULL);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../../README.md)
- 上一节：59.13 并发服务端
- 下一节：[59.15 进一步信息](./59.15-further-information.md)
