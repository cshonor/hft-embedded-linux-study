# 59.10 协议无关的主机与服务转换

> 本章：[TLPI 第 59 章 — Internet 域 Socket](./README.md)
> - 上一节：[59.9 /etc/services](./59.9-the-etc-services-file.md)
> - 下一节：[59.11 客户端-服务端示例](./59.11-client-server-example-stream-sockets.md)

---

## 本节讲什么

`getaddrinfo` + `getnameinfo` 实现协议无关（IPv4/IPv6）的网络编程。

---

## 要点

### 协议无关的核心思想

- 代码不硬编码 IPv4 (`sockaddr_in`) 或 IPv6 (`sockaddr_in6`)
- 用 `sockaddr_storage` 通用结构存储地址
- `getaddrinfo` 返回的 `addrinfo` 链表可遍历尝试

### getaddrinfo hints 设置

| hints 字段 | 推荐值 | 含义 |
|------------|--------|------|
| `ai_family` | `AF_UNSPEC` | IPv4 或 IPv6 都行 |
| `ai_socktype` | `SOCK_STREAM` | TCP |
| `ai_flags` | `AI_PASSIVE` | 服务端 bind 用（host=NULL 时） |

### 服务端绑定模式

```c
struct addrinfo hints = {
    .ai_family = AF_UNSPEC,
    .ai_socktype = SOCK_STREAM,
    .ai_flags = AI_PASSIVE,
};
struct addrinfo *res;
getaddrinfo(NULL, "8080", &hints, &res);

for (struct addrinfo *p = res; p; p = p->ai_next) {
    int fd = socket(p->ai_family, p->ai_socktype, p->ai_protocol);
    if (bind(fd, p->ai_addr, p->ai_addrlen) == 0) {
        listen(fd, 5);
        break;  /* 成功绑定 */
    }
    close(fd);
}
```

### 客户端连接模式

```c
struct addrinfo hints = {
    .ai_family = AF_UNSPEC,
    .ai_socktype = SOCK_STREAM,
};
struct addrinfo *res;
getaddrinfo("example.com", "80", &hints, &res);

for (struct addrinfo *p = res; p; p = p->ai_next) {
    int fd = socket(p->ai_family, p->ai_socktype, p->ai_protocol);
    if (connect(fd, p->ai_addr, p->ai_addrlen) == 0) break;
    close(fd);
}
```

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | `AF_UNSPEC` 让代码同时支持 IPv4/IPv6，面向未来 |
| HFT | 启动时解析一次，选择最优地址（IPv6 优先或延迟最低） |

## 代码示例

```c
#include <stdio.h>
#include <netdb.h>
#include <string.h>
#include <sys/socket.h>
#include <unistd.h>
/* 协议无关: getaddrinfo 遍历 IPv4+IPv6 */
int main(void) {
    struct addrinfo hints, *res;
    memset(&hints, 0, sizeof(hints));
    hints.ai_family = AF_UNSPEC;
    hints.ai_socktype = SOCK_STREAM;

    int err = getaddrinfo("localhost", "daytime", &hints, &res);
    if (err) { fprintf(stderr, "%s\n", gai_strerror(err)); return 1; }

    for (struct addrinfo *p = res; p; p = p->ai_next) {
        char host[NI_MAXHOST], serv[NI_MAXSERV];
        getnameinfo(p->ai_addr, p->ai_addrlen,
                    host, sizeof(host), serv, sizeof(serv),
                    NI_NUMERICHOST | NI_NUMERICSERV);
        printf("family=%d, %s:%s\n", p->ai_family, host, serv);

        /* 尝试连接 */
        int fd = socket(p->ai_family, p->ai_socktype, p->ai_protocol);
        if (connect(fd, p->ai_addr, p->ai_addrlen) == 0) {
            char buf[256];
            ssize_t n = read(fd, buf, sizeof(buf)-1);
            if (n > 0) { buf[n] = 0; printf("  → %s", buf); }
            close(fd);
            break;
        }
        close(fd);
    }
    freeaddrinfo(res);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](./README.md)
- 上一节：[59.9 /etc/services](./59.9-the-etc-services-file.md)
- 下一节：[59.11 客户端-服务端示例](./59.11-client-server-example-stream-sockets.md)
