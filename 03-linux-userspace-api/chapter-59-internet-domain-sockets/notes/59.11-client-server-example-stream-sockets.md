# 59.11 流式 Socket 客户端-服务端示例

> 本章：[TLPI 第 59 章 — Internet 域 Socket](../../README.md)
> - 上一节：[59.10 协议无关转换](./59.10-protocol-independent-host-and-service-co.md)
> - 下一节：[59.12 Internet Socket 库](./59.12-an-internet-domain-sockets-library.md)

---

## 本节讲什么

一个完整的 TCP 客户端-服务端示例，展示 Internet 域 socket 的完整流程。

---

## 要点

### 服务端流程

```
socket → bind → listen → accept → read/write → close
```

### 客户端流程

```
socket → connect → read/write → close
```

### 关键 API 串联

| 步骤 | 服务端 | 客户端 |
|------|--------|--------|
| 创建 | `socket(AF_INET, SOCK_STREAM, 0)` | 同左 |
| 地址 | `bind(fd, &addr, len)` | `connect(fd, &addr, len)` |
| 监听 | `listen(fd, backlog)` | — |
| 接受 | `accept(fd, &cli, &len)` | — |
| 通信 | `read`/`write` | `read`/`write` |

### 常见错误

| 错误 | 原因 |
|------|------|
| bind EADDRINUSE | 端口被占用（加 SO_REUSEADDR） |
| connect ECONNREFUSED | 服务端未运行 |
| accept EINTR | 被信号中断（SA_RESTART 或手动重试） |

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | TCP 保证可靠传输，适合嵌入式远程管理 |
| HFT | TCP 有 Nagle 延迟，设 `TCP_NODELAY`；或用 UDP/共享内存 |

## 代码示例

```c
#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <arpa/inet.h>
#include <sys/socket.h>
#include <sys/wait.h>
/* TCP echo 服务端 + 客户端 */
#define PORT 9999

int main(void) {
    pid_t pid = fork();
    if (pid == 0) {
        /* 服务端 */
        int sfd = socket(AF_INET, SOCK_STREAM, 0);
        struct sockaddr_in addr = {
            .sin_family = AF_INET,
            .sin_port = htons(PORT),
            .sin_addr.s_addr = INADDR_ANY,
        };
        int opt = 1;
        setsockopt(sfd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));
        bind(sfd, (struct sockaddr *)&addr, sizeof(addr));
        listen(sfd, 5);

        struct sockaddr_in cli;
        socklen_t clen = sizeof(cli);
        int cfd = accept(sfd, (struct sockaddr *)&cli, &clen);
        char buf[64];
        ssize_t n = read(cfd, buf, sizeof(buf)-1);
        if (n > 0) { buf[n] = 0; printf("server recv: %s\n", buf); }
        write(cfd, "ack", 3);
        close(cfd); close(sfd);
        _exit(0);
    }

    sleep(1);  /* 等服务端启动 */
    /* 客户端 */
    int fd = socket(AF_INET, SOCK_STREAM, 0);
    struct sockaddr_in addr = {
        .sin_family = AF_INET,
        .sin_port = htons(PORT),
    };
    inet_pton(AF_INET, "127.0.0.1", &addr.sin_addr);
    connect(fd, (struct sockaddr *)&addr, sizeof(addr));
    write(fd, "hello", 5);

    char buf[64] = {0};
    read(fd, buf, sizeof(buf));
    printf("client recv: %s\n", buf);
    close(fd);
    wait(NULL);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../../README.md)
- 上一节：[59.10 协议无关转换](./59.10-protocol-independent-host-and-service-co.md)
- 下一节：[59.12 Internet Socket 库](./59.12-an-internet-domain-sockets-library.md)
