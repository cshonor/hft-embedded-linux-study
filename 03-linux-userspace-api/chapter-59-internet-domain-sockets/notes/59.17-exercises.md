# 59.17 练习题

> 本章：[TLPI 第 59 章 — Internet 域 Socket](./README.md)

## 本节讲什么

原书第 59 章练习题要点。

<details>
<summary>1. 为什么 bind 失败时应该设 SO_REUSEADDR？</summary>

TCP 连接关闭后，端口进入 TIME_WAIT 状态（持续 2*MSL ≈ 60 秒）。在此期间 bind 同一端口会失败（EADDRINUSE）。SO_REUSEADDR 允许绑定处于 TIME_WAIT 的端口，使服务端能快速重启。
</details>

<details>
<summary>2. 如何让代码同时支持 IPv4 和 IPv6？</summary>

1. 用 `getaddrinfo` + `AF_UNSPEC` 获取所有地址
2. 遍历结果链表，尝试 socket + bind/connect
3. 用 `sockaddr_storage` 通用结构存储地址
4. 不硬编码 `sockaddr_in`，用 `sockaddr *` 通用指针
</details>

<details>
<summary>3. connect 失败时应该怎么处理？</summary>

遍历 `getaddrinfo` 返回的链表，对每个地址尝试 `socket` + `connect`，成功则停止。一个主机可能有多个 IP（如 IPv4 + IPv6），某个 IP 连接失败不代表全部失败。
</details>

## 与后续衔接

- [Ch60 TCP 详解](../chapter-60-tcp-ip-protocol/README.md)
- [Ch63 备用 I/O 模型](../chapter-63-alternative-i-o-models/README.md)

---

## 代码自测

<details>
<summary>参考答案</summary>

参见原书附录 F。
</details>
