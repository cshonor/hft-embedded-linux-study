# 39.3 进程能力集（**每线程**一份）

> 本章：[TLPI 第 39 章 — Capabilities](../README.md)
> - 上一节：[39.1 动机](39.1-rationale-for-capabilities.md)
> - 下一节：[39.5 exec 转换（简化）](39.5-transformation-of-process-capabilities-d.md)

---

## 本节讲什么

`setcap 'cap_net_raw+ep' ./ping` · `getcap` · `/proc/$$/status` 的 `Cap*`。 ---

---

## 要点

| 集合 | 作用 |
|------|------|
| **Permitted** | 可拥有的上限；可从中抬到 Effective；**删掉难自恢复**（除非再 exec 文件能力） |
| **Effective** | **内核检查用**；按需开关 |
| **Inheritable** | fork 继承；参与 exec 算新 Permitted |
| **Bounding** | exec 能拿到的全局上界；**只删不增**；容器常用 |
| **Ambient**（4.3+） | 非 root 跨 exec 传能力；root exec 时清空 |

### 文件能力（xattr）

| 项 | |
|----|--|
| File Permitted | 并入新进程 Permitted（∩ Bounding） |
| File Inheritable | 与进程 Inheritable 与后并入 |
| File Effective | **1 bit**：1 → 新 Effective=新 Permitted；0 → Effective 空（或 Ambient） |

`setcap 'cap_net_raw+ep' ./ping` · `getcap` · `/proc/$$/status` 的 `Cap*`。

---

## HFT / 嵌入式关联

| 路线 | 要点 |
|------|------|
| 嵌入式 | 进程能力集（**每线程**一份） 在嵌入式系统的应用场景 |
| HFT | 进程能力集（**每线程**一份） 对低延迟和性能的影响 |

## 代码示例

```c
#include <sys/capability.h>
#include <stdio.h>
int main(void) {
    /* 进程有三组 capabilities:
     * permitted: 允许使用的上限
     * effective: 当前生效的
     * inheritable: exec 后可继承的
     */
    cap_t caps = cap_get_proc();
    printf("进程 capabilities 获取 %s\n", caps ? "成功" : "失败");
    if (caps) cap_free(caps);
    return 0;
}
```

---

## 衔接

- 本章 README：[README](../README.md)
- 上一节：[39.1 动机](39.1-rationale-for-capabilities.md)
- 下一节：[39.5 exec 转换（简化）](39.5-transformation-of-process-capabilities-d.md)
- 前置：**前置**：[Ch9 凭证](../../chapter-09-process-credentials/notes) · [Ch38 特权安全](../../chapter-38-secure-privileged/notes) · [Ch16 xattr](../../chapter-16-extended-attributes/notes)  
- 后置：**后置**：[Ch40 登录记账](../../chapter-40-login-accounting/notes)
