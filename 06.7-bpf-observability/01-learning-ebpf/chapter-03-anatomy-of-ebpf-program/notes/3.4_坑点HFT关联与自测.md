# 坑点HFT关联与自测（原书 §3.7–3.9）
> 合并自原 section-7～9。文件名序号是合并后的笔记序号；正文小节标题沿用原书节号，历史 § 引用依然有效。

## 3.7 坑点清单

1. **XDP 返回 0 = XDP_ABORTED ≠ 成功**——返回 0 挂到 eth0 会丢掉所有包，SSH 直连机器直接失联。五种返回码（ABORTED=0/DROP=1/PASS=2/TX=3/REDIRECT=4，v6.6 `bpf.h` 核对）与直觉相反：C 语言的"0=成功"惯性在这里是毒。测试 XDP 放容器/虚拟网卡里（参考 lizrice/lb-from-scratch），物理网卡上先 `XDP_PASS` 起步再逐步加逻辑
2. 忘了 `SEC("license")` 或用了 GPL-only helper 却声明专有许可 → 验证器拒绝（`bpf_probe_read_kernel` 等全是 GPL-only；LSM 程序整体必须 GPL 兼容）
3. 编译不加 `-g`：没 BTF → bpftool 无法漂亮打印、CO-RE 不可用（第 5 章）、验证器日志里没有 C 源码行——排错成本直接翻倍
4. bpftool 必定 pin；忘删 pin 文件 = 程序滞留内核（重启前一直占 memlock 内存），下次 load 同名路径会报"File exists"
5. `net.core.bpf_jit_enable` sysctl / `CONFIG_BPF_JIT` 控制 JIT——性能测试前先确认开着（`bpftool prog list` 看 `jited` 标志）；解释执行的吞吐差一个量级，还带着 Spectre 侧信道风险
6. tag 与 id 的区别：tag 跟着内容走（指令的 SHA 哈希，内容变 tag 变），id 跟着加载实例走（每次加载新分配）。同一程序重加载：tag 不变、id 变——CI 里做"程序是否真的是这版"的校验用 tag

---

## 3.8 HFT 关联

### 1. `bpftool prog dump jited` = eBPF 侧的"反汇编确认"

交易热路径调优时你会反汇编二进制确认编译器没自作聪明；eBPF 侧同理——验证器会改写字节码（如把全局变量访问重定位成绝对地址、dead code 消除），JIT 再翻译成机器码。**"我写的源码"和"CPU 执行的机器码"之间有两层改写**，性能异常时 `dump jited` 是唯一真相。bytes_xlated/bytes_jited 两个数字（[3.2](./3.2_编译检查与加载附加.md)）先看规模是否合理。

### 2. memlock 常驻内存：与 mlockall 哲学天然一致

eBPF 程序和数据锁定内存、永不换页——交易机把策略进程 mlockall 防缺页尖刺，eBPF 观测程序天然享受同等待遇，**观测层自己不会成为缺页抖动源**。memlock rlimit（老内核）或 memcg 计费（新内核）要注意额度。

### 3. XDP 卸载 = 行情前置过滤的理论最优层

包在进主机内存**之前**就被网卡处理——比 DPDK 少一次 DMA、比 tc-BPF 更早。交易机网口的"非行情流量 early drop"（心跳风暴、异常广播）放这层做，CPU 完全无感。限制：offload 只支持 helper 子集、网卡固件版本绑定（换网卡=回归测试）。

### 4. 512B 栈 → 观测探针的"薄"是强制的

[2.3](../../chapter-02-hello-world/notes/2.3_坑点HFT关联与自测.md) 已论证：小栈强制薄探针、测量工具越薄数据越可信。本章补充：**复杂逻辑的正确去处是用户态**（内核只做"取时间戳+写 map"），子程序/尾调用是"复用与接力"的组织手段，不是"塞复杂逻辑"的许可。

### 5. 读字节码是验证器日志的前置技能

第 6 章验证失败报的全是**指令偏移**（`0: (bf) r6 = r1`）——不会读字节码就只会对着报错发呆。[3.2](./3.2_编译检查与加载附加.md) 的反汇编精读 + 宽指令占两槽的细节（offset 0 的下一条在 2）是基础功。

---

## 3.9 自测题

1. eBPF 的 10 个寄存器各司什么职？R1 和 R0 的特殊用途？
2. 什么是宽指令编码？为什么需要它？
3. 一个 XDP 程序源码没有 map，为什么 bpftool 显示关联了 2 个 map？
4. id、name、tag、pinned path 四种引用方式，哪些唯一？
5. BPF-to-BPF 调用和尾调用在字节码层和栈使用上的区别？
6. 为什么 trace 输出显示 `<idle>-0`？这反映 XDP 事件的什么特性？

<details>
<summary>参考答案</summary>

**1.** R0=返回值（helper 返回 + 程序返回码共用）；R1–R5=函数调用参数（最多 5 个），R1 兼任程序入口的 ctx 指针；R6–R9=跨 helper 调用保持（callee-saved，所以入口先 `r6 = r1` 备份 ctx）；R10=栈帧指针（只读，512B 栈的底）。寄存器模型与 x86-64 ABI 刻意同构，JIT 近乎直译。

**2.** `bpf_insn` 的 imm 字段只有 32 位，装不下 64 位立即数（如全局变量的内核态地址）→ `ldimm64` 占**两个指令槽**（16 字节），反汇编里 offset 0 的下一条指令在 offset 2。

**3.** 全局变量实现为 map：`counter`（0 初始化）→ `.bss` 段 map；格式串（const）→ `.rodata` 段 map（frozen）。源码没写 map，但 ELF 段被加载器变成了 2 个 array map。

**4.** **id 和 pinned path 唯一**；name 和 tag 可重复（不同进程可加载同名程序、相同内容程序 tag 相同）。

**5.** 字节码层：子程序是 `call pc+N` 相对偏移跳转、正常函数调用；尾调用是特殊操作码 + PROG_ARRAY 查表替换。栈：子程序**叠**在调用者栈上、可返回（受 512B 总量限制，几层就满）；尾调用**替换**调用者、栈从零开始、永不返回（33 层上限）。5.10 起可混用。

**6.** 包到达网卡入口时**没有任何进程与之关联**——XDP 在驱动层触发，早于中断/软中断分配给任何 socket，`bpf_get_current_pid_tgid()` 返回的是当时 CPU 上恰好在跑的（无关）任务，通常是 idle。这解释了网络类程序为何不能按"当前进程"过滤。

</details>

---

**衔接**：第 4 章把 BCC 的黑盒完全拆开——bpf() syscall 的全量命令、对象生命周期、kprobe 挂载的 syscall 三件套。
