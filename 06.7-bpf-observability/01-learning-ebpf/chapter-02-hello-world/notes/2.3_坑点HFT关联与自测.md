# 坑点HFT关联与自测（原书 §2.6–2.8）
> 合并自原 section-6～8。文件名序号是合并后的笔记序号；正文小节标题沿用原书节号，历史 § 引用依然有效。

## 2.6 坑点清单

1. trace_pipe 全机共享且慢——生产一律用 ring buffer / map
2. `bpf_get_current_pid_tgid()` 的 PID 在**高** 32 位（tgid），UID 在**低** 32 位——位移方向最容易写反
3. shell 内建命令（echo 等）不 execve，不触发事件
4. BCC 方言 C 不是标准 C，换 libbpf 时 lookup/update 语法要全部重写
5. ring buffer 太小 → 静默丢数据，务必检查 drop 计数
6. 尾调用 33 层上限、5.10 前与子程序互斥
7. **内核里拿到用户态指针不能直接解引用**（真机实测）——如 openat 的 `ctx->filename` 是用户地址，内核态直接读会崩，必须 `bpf_probe_read_user_str()` 先拷到栈数组；verifier 会在加载时强制这一点
8. **`bpf_trace_printk` 的 `%s` 收的是指针**——传本地栈数组合法，传用户指针非法；所以"先 probe_read 再打印"是两步操作，不能一步到位

---

## 2.7 HFT 关联

> 本节讲什么：第 2 章的四个构件（map / per-CPU / ring buffer / 尾调用）在 HFT 工程里的对应物。

## 1. per-CPU map + 用户态汇总 = 高频计数器标准架构

跨核共享计数器的问题：每次 `++` 都是对**同一条 cache line** 的争用——核 A 和核 B 同时改，一个核的写让另一个核的 cache line 失效（cache line bouncing），争用激烈时计数器本身成了热点。per-CPU map 让每核只写自己的副本，**零锁零争用**，用户态读时再汇总——与你 TLPI/CSAPP 里学的用户态 per-core 无锁计数完全同构：

```
核0: counter[0]++（私有cache line）  ┐
核1: counter[1]++（私有cache line）  ├─▶ 用户态定期读：sum(counter[0..N-1])
核2: counter[2]++（私有cache line）  ┘
```

**适用判断**：计数型指标（包数、事件数、丢弃数）一律 per-CPU；要精确 read-modify-write 序列的场景才考虑锁（或干脆移到用户态）。

## 2. ring buffer 的 drop 计数 = 观测数据的可信度

对 HFT 最典型的坑：**延迟直方图丢了最慢的那批样本**。突发延迟毛刺期恰好是事件洪峰期——用户态消费跟不上，buffer 溢出，毛刺样本被丢 → 直方图显示一切正常 → 调优方向全错。

工程规则：

- buffer 尺寸按**突发速率**（不是平均速率）留余量
- 工具侧**必须检查 drop 计数**并显式报告（丢了多少条要说出来，不能装没事）
- 越关键的指标，越要在"可能丢"和"可能慢但全"之间选后者（加大 buffer / 分流到独立 buffer）

## 3. 尾调用分发 = 内核态消息路由

§5 的 syscall 分发表结构，在 HFT 里的对应物：

- **行情多级处理**：XDP 收包 → 尾调用分发给"解析层程序" → "过滤层程序" → "转发/上送层程序"，每级独立加载替换，不用改整体
- **按 opcode/策略分发**：风控前置逻辑做成 map 配置，改路由不用重编 eBPF 程序——**把策略做成数据，不做成代码**（热更新的老智慧）
- 33 层上限对这个场景绰绰有余（实际链深 3-5 层）

## 4. 512 字节栈 = 延迟测量代码的纪律

栈 512 字节意味着：放不下大数组（一个 64 项的 u64 直方图就占满）、递归想都别想、连 `char fname[256]` 都要斟酌。对 HFT 测量代码的要求因此非常明确：

- **程序极简**：一个事件只做"取时间戳 + 写 map"级别的动作，复杂计算留给用户态
- **状态进 map**，不进栈——map 容量是 MB 级，栈只是临时草稿纸
- 大缓冲需求用 per-CPU array map 或 ring buffer，别想着栈上开

这其实是好事：小栈强制 eBPF 观测程序保持"薄探针"属性——**测量工具本身越薄，对被测路径的扰动越小**，数据才可信。

---

**衔接**：第 3 章撕开 BCC 黑盒——手工走一遍编译/加载/挂载，eBPF 的全貌就闭环了。

---

## 2.8 自测题

> 先自己答，再展开参考答案。第 4、5 题建议动手验证（树莓派 + ebpf-gate 环境现成）。

1. trace_pipe 的三个局限是什么？生产环境用什么替代？
2. 加载 tracing 程序和 networking 程序分别需要哪些 capability？
3. `bpf_get_current_pid_tgid()` 和 `bpf_get_current_uid_gid()` 的 64 位返回值各字段怎么分布？
4. maps 的三大用途？per-CPU 变体解决什么问题？
5. 尾调用和普通函数调用的本质区别？为什么 eBPF 栈只有 512 字节使得尾调用更重要？
6. 33 层尾调用 × 100 万指令限制，对写复杂内核逻辑意味着什么？
7. （真机加餐）lab02 里为什么要用 `bpf_probe_read_user_str()` 拿 filename，而不是直接 `ctx->filename` 解引用？

<details>
<summary>参考答案</summary>

**1.** ①全机唯一，多程序混写无法区分；②只支持字符串无结构化；③锁+格式化开销大。替代：拉模式用 map（用户态轮询），推模式用 ring/perf buffer（事件驱动回调）——后者是标准答案。

**2.** `CAP_BPF`（5.8+）是基础；tracking 程序加 `CAP_PERFMON`，网络程序加 `CAP_NET_ADMIN`。root 全有。

**3.** **方向相反，最易写反**：`pid_tgid` 返回值 **高 32 位 = PID(tgid)、低 32 位 = 线程 ID**（取 pid 用 `>> 32`）；`uid_gid` 返回值 **低 32 位 = UID、高 32 位 = GID**（取 uid 用 `& 0xFFFFFFFF`）。

**4.** 用途：①用户态写配置→eBPF 读；②eBPF 跨事件/跨程序存状态；③eBPF 写结果→用户态展示。per-CPU 解决多核并发写同一计数器的 cache line 争用：每核独立副本免锁，用户态读时汇总——代价是内存×核数和跨核读不即时一致。

**5.** 本质区别：普通调用会返回（子程序共享调用者的 512 字节栈）；尾调用永不返回，被调程序**替换**调用者、栈从零开始（类比 execve 对进程）。栈只有 512 字节→调用链深了就爆栈，尾调用是唯一"串长逻辑不叠栈"的办法，还能绕过单程序指令数限制。

**6.** 意味着"复杂逻辑"要拆解：最多 33 个程序接力、每个程序内 ≤100 万条已验证指令；实际是引导你把策略做成 map 数据（查表分发）而非硬编码——总复杂度上限约 3300 万指令 + 无限的 map 容量，对观测类程序绰绰有余。

**7.** 因为 `ctx->filename` 是**用户态地址的指针**——syscall 参数本来就是用户传进来的。eBPF 程序跑在内核态，直接解引用用户地址：轻则数据错（用户页可能还没换入），重则崩内核。`bpf_probe_read_user_str()` 是 helper，内部用安全的用户态访问机制（处理缺页、跨页）拷贝到 eBPF 栈数组；verifier 会在加载时强制这一点——直接解引用根本到不了运行期。（你的真实案例：Ch2 坑点 7、lab02 README。）

</details>
