# perf与RingBuffer（原书 §4.5–4.6）
> 合并自原 section-5～6。文件名序号是合并后的笔记序号；正文小节标题沿用原书节号，历史 § 引用依然有效。

## 4.5 perf buffer 初始化：为什么是每核一个

每颗 CPU 核执行一轮：

```
perf_event_open({PERF_TYPE_SOFTWARE, config=PERF_COUNT_SW_BPF_OUTPUT, ...}, -1, X, ...)
                                        # pid=-1, cpu=X → 测量该核上所有进程
ioctl(Y, PERF_EVENT_IOC_ENABLE)
bpf(BPF_MAP_UPDATE_ELEM, {map_fd=4, ...})   # map 第 X 项指向该核的 perf 缓冲
```

四核机器 = 4 轮 = `PERF_EVENT_ARRAY` 里 4 个条目——"array" 就是每核一缓冲的数组。之后用户态用 `ppoll()` 同时等 4 个 fd，哪个核触发读哪个。

**为什么必须每核一个**：perf buffer 是无锁环形缓冲，单缓冲多核同写必然加锁/争用——每核独立缓冲让**内核侧写入零锁**（事件在哪个核发生就写哪个核的缓冲，天然无竞争），代价转嫁给用户态（要聚合多个 fd）。这与 [2.3](../../chapter-02-hello-world/notes/2.3_坑点HFT关联与自测.md) 的 per-CPU map 是同一设计哲学：**把同步成本从热路径挪到冷路径**。

**PERF_EVENT_ARRAY 的条目数 = 核数**（不是随便定的 max_entries）——这也是 [4.5 坑点](./4.5_坑点HFT关联与自测.md) 里"perf buffer 事件跨核乱序"的根源：各核独立缓冲，用户态聚合时的到达顺序与真实时间序无关。

---

## 4.6 Ring buffer：单缓冲 + epoll

`BPF_MAP_CREATE` 一条搞定（`key_size=0, value_size=0, max_entries=4096`），无每核 setup。

| 维度 | perf buffer（每核一个） | ring buffer（全局一个） |
|---|---|---|
| 缓冲数量 | N 核 N 个 | **1 个**（内部按页对齐的 reserve 区域） |
| 内核侧写 | 零锁（每核私有） | 零锁（reserve/commit 两阶段，跨核靠原子操作） |
| 内存效率 | 每核固定预算，核多浪费 | **按需 reserve**——大事件不必适配每核页大小，整体利用率高 |
| 跨核顺序 | 无保证（各核独立） | **提交保序**（commit 时间戳定序） |
| 用户态等待 | ppoll 等 N 个 fd | **epoll 等 1 个 fd** |
| 内核版本 | 4.x 起广泛可用 | 5.8+ |

**ring buffer 的两阶段提交**是关键设计：`bpf_ringbuf_reserve()` 在环形区里划一块（此时别的核 reserve 别的区域，无锁），填完数据 `bpf_ringbuf_submit()` 才让它对消费者可见——**"写数据"和"发布"分离**，用户态永远只看到完整事件，杜绝读到半截数据。丢弃路径用 `discard()` 静默归还。

等待机制从 ppoll 升级为 epoll：

```
epoll_create1(EPOLL_CLOEXEC)                       = 8   # 内核里建一个 epoll 实例
epoll_ctl(8, EPOLL_CTL_ADD, 4, {EPOLLIN})          = 0   # 把 ringbuf fd 4 加入集合
epoll_pwait(8, ...)                                 # 阻塞等数据
```

ppoll 每次返回都要重新传整个 fd 集合（用户态→内核态拷贝 + 内核线性扫描）；epoll 的 fd 集合由**内核对象**持有，注册一次即可，就绪回调挂到 fd 上——fd 数量大时差距是 O(n) vs O(1)。（与 03.5/04-cpp 网络编程模块里 poll vs epoll 的对比完全同源；3.3-3.4 里 epoll 作为 Linux 里程碑的 O(1) 论述也在此落地。）

**选型结论**：新内核（5.8+）一律 ring buffer——内存省、保序、等待简单；perf buffer 只在老内核或工具链绑定 BCC 老范式时出现。**需要时间序的事件流（延迟分布、事件序列重建）ring buffer 是硬需求**。

**衔接**：用户态读 map 的完整 syscall 序列 → [4.4](./4.4_遍历map的syscall序列.md)；本章坑点与 HFT → [4.5](./4.5_坑点HFT关联与自测.md)。
