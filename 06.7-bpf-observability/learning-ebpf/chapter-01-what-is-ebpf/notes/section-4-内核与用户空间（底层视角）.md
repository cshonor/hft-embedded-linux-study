# 内核与用户空间（底层视角）

- 应用跑在非特权用户态，通过 **syscall 接口**请求内核做事（文件、网络、内存、进程协调）
- `strace -c echo "hello"` 实测：一个 echo 触发 **111 次系统调用**（openat 30、mmap 20、newfstatat 19、close 20…）
- **推论**：应用与内核的交互面几乎等于应用的全部外部行为——在内核里插桩 = 看穿一切应用，且应用无感知、无需修改
