# 4.10 开发 BCC 工具

> 库本：《BPF之巅》第 4 章 BCC（印刷 p91–136），4.10 节

## 内容详解

### 什么时候用 BCC 开发，什么时候用 bpftrace？

| 维度 | **BCC**（Python + BPF C） | **bpftrace**（第 5 章） |
|------|--------------------------|--------------------------|
| 适合场景 | **复杂工具**：多参数、复杂输出（直方图+表格）、错误处理、长生命周期维护 | 单行/短脚本、快速假设验证 |
| 代码量 | 同样功能约 **10 倍**于 bpftrace | 极简 |
| 开发周期 | 小时级 | 分钟级 |
| 生态位 | tools/ 下 70+ 工具的实现载体 | 交互式探索（附录 A 单行宝典） |

### 判断标准（书中的选型逻辑）

- 工具要**带命令行参数**、要**定制输出格式**、要**长期维护**、要在 BCC 仓库发布 → BCC；
- 只是**临时看一眼**计数/直方图/一行输出 → bpftrace。

### 选型的完整阶梯（不要跳级）

写代码之前其实还有两级免费的台阶，完整阶梯是：

```text
1. tools/ 现成工具     → 70+ 个，先 man 找（多半已有）
2. 现成工具 + 参数     → -p/-x/-i/--cgroup 组合常能凑出答案
3. bpftrace 单行/短脚本 → 分钟级原型，验证"这个指标有没有用"
4. BCC 自研           → 10 倍代码量，只有 3 验证过价值才配得上
5. (现代追加) libbpf+CO-RE → 4 的产品化形态，可分发单二进制
```

跳级的代价真实存在：直接跳到 4 的团队常发现 tools/ 里早有等价工具；跳过 3 的验证直接工程化，常做出"精致但没人看"的仪表盘。**阶梯的本质是让每一级的最小成本验证过，再为下一级付费**。

### 开发流程概览

1. 写 Python 脚本：`BPF(text=...)` 嵌入 BPF C；
2. BPF C 中：定义映射表（`BPF_HASH` 等）+ 探针函数（`int probe(struct pt_regs *ctx)`）；
3. 用户态：`b["table"]` 读数据 / `open_perf_buffer()` 回调；
4. 打磨：参数解析（argparse）、输出格式化、OVERHEAD 文档、example 文件。

参考实现：**附录 C** 提供 BCC 工具开发的完整例子（本书配套）。

第 4 步不是可有可无的打磨——**参数化 + 文档化是"脚本"和"工具"的分界线**：没有 `-p PID` 的工具在共享机上没法安全跑（全系统输出混入他人噪声），没有 OVERHEAD 的工具没法过生产评审。BCC 生态的工程化标准（见 [4.9](./section-9-工具文档.md)）从开发的最后一步就开始执行。

## HFT 关联

- 团队固化工具（如"撮合延迟分布"、"订单队列深度"）就是典型的 BCC 场景：参数化日期/标的、输出结构化日志、随内核升级维护——这些 bpftrace 都做不好。
- 一次性排查（"futex 是不是变多了？"）用 bpftrace 单行，别小题大做写 BCC。
- 自研工具同样要有"上线门槛"：对照真值校验（与 /proc 统计对数）、开销预算（OVERHEAD 实测）、升级预案（依赖接口的 STABILITY 自评）——把 4.9 的选型三角反向用在自己身上。

## 陷阱

- ⚠️ 10 倍代码量是真实成本：先问"单用途工具（tools/ 已有 70+）能不能覆盖"，再问 bpftrace，最后才轮到自研 BCC。
- ⚠️ 自研 BCC 工具要跟着内核版本走（BCC API、内核函数签名变化）——没有维护承诺就不要上生产。
- ⚠️ 阶梯第 3 级（bpftrace 原型）跳不得：没验证过指标价值的工具，工程化越精致浪费越大。

<details>
<summary>自测题</summary>

1. 同样功能的工具，BCC 版大约是 bpftrace 版代码量的几倍？
   <details><summary>答案</summary>约 10 倍。</details>

2. 哪三种需求会把选型推向 BCC？
   <details><summary>答案</summary>复杂命令行参数、复杂/定制输出格式、长期维护（发布级工具）。</details>

3. 画出完整选型阶梯并说明"不跳级"的本质。
   <details><summary>答案</summary>现成工具 → 工具+参数 → bpftrace 原型 → BCC 自研 → CO-RE 产品化。本质是每级用最小成本验证（有没有现成的？指标有没有价值？），验证过了才为下一级付费——跳级买到的是精致但无用的东西。</details>

4. 为什么"参数化 + 文档化"是脚本与工具的分界线？
   <details><summary>答案</summary>无 -p 过滤的工具在共享机不安全（混入他人噪声）；无 OVERHEAD 的工具过不了生产评审；无 example 的工具无法交接——这三样恰是 BCC 生态工程化的最低标准。</details>
</details>
