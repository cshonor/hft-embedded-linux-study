# 6.1 背景知识

> 底本：《BPF之巅》第 6 章 CPU，6.1 节（印刷 p191–198）。Part II（具体工具各论）开篇 — CPU 是性能分析的第一个切入点。

## 6.1.1 CPU 基础

### CPU 运行模式

- **System（内核）模式**：执行特权指令、内核代码（系统调用、驱动、中断处理）
- **User 模式**：执行应用程序代码
- 性能工具分别统计两类时间（如 `mpstat` 的 `%usr` / `%sys`）；BPF 工具可进一步区分到**每个进程、每个线程、每个函数、每个调用栈**

### CPU 调度器（图 6-1）

线程三状态：

| 状态 | 含义 |
|------|------|
| **ON-PROC** | 正在 CPU 上运行 |
| **RUNNABLE** | 可运行但需等待 CPU（在运行队列中排队） |
| **SLEEP** | 阻塞等待事件（I/O、锁、网络…） |

关键机制：

- **上下文切换**：
  - **主动（VCX，voluntary）**：线程阻塞（等 I/O/锁）→ 让出 CPU，进入 SLEEP
  - **被动（ICX，involuntary）**：线程耗尽调度器分配的时间片（或被更高优先级抢占）→ 回到 RUNNABLE 排队
- **线程迁移**：调度器可把线程从 CPU A 的运行队列迁移到 CPU B（负载均衡），代价是丢失 CPU 本地缓存热度（L1/L2 冷启动）
- 调度器内部用**红黑树**（CFS）管理可运行任务 — "运行队列"是历史叫法，`runqlat`/`runqlen` 沿用了这个名字

### CPU 硬件缓存（图 6-2）

| 层级 | 位置 | 速度 |
|------|------|------|
| L1 I$（指令）/ L1 D$（数据） | 每核私有 | ~1ns，通常 1~4 周期 |
| LLC（末级缓存，L2/L3） | 多核共享 | 数十周期 |
| 内存 | — | ~100ns（比 CPU 慢 100 倍量级） |

- MMU + TLB 负责虚拟地址→物理地址转换；TLB 未命中同样昂贵
- **内存性能增速长期落后 CPU** → 缓存命中率是"CPU 利用率"之外另一个"CPU 用得好不好"的维度（对应 6.3.16 `llcstat`）

## 6.1.2 BPF 分析能力（表 6-1）

CPU 观测的事件源：调度器跟踪点/kprobes、定时采样（`profile:hz:N`）、PMC（性能监控计数器）、软件事件（上下文切换、CPU 迁移）等。

⚠️ **开销警示**：跟踪 CPU 调度器事件（唤醒 + 上下文切换）最坏情况可消耗 **~10% 系统性能**。繁忙系统中每秒切换可达百万次，每事件增加 1µs 就是显著开销 → 调度器跟踪只适合**短期诊断**，不适合 7×24 常驻。

## 6.1.3 分析策略（八步法）

1. 确认 **CPU 是否受限**（`uptime`/`mpstat`：使用率、负载、饱和度）
2. **`runqlat`** 识别软限制（排队等待）
3. 分解 CPU 使用量：usr/sys、每 CPU、每进程
4. **火焰图 / `profile`** 找出在 CPU 上跑的代码路径
5. 不在 CPU 上的时间 → **`offcputime`** 分析阻塞原因
6. **`execsnoop`** 排查短命进程（top 抓不到）
7. **`hardirqs`/`softirqs`** 检查中断占用
8. 深入微架构 → **IPC / `llcstat`**（缓存命中率）

## HFT 关联

- 共置交易机绑核 + 专用核场景：ON-PROC↔RUNNABLE 的切换延迟（`runqlat`）应接近 0；isolcpus 减少迁移，但内核线程/定时器中断仍会骚扰 — 需 nohz_full + irqaffinity 配合
- 被动上下文切换（ICX）对延迟敏感线程是头号敌人：时间片被抢 = 一次不可控的微秒级停顿
- 缓存热度迁移代价解释了为什么策略线程要 taskset 绑核：迁移一次 = L1/L2 全冷

## 常见陷阱

1. **只看 CPU 使用率不看饱和度** — 42% 使用率的系统 runqlat 仍可能有毫秒级长尾（见 6.3.5 runqslower 实例）
2. **把 run queue 长度和调度延迟混淆** — 长度是瞬时快照，延迟才是线程真实体验
3. **忽略 System 模式占比** — HFT 上 `%sys` 突增常意味着中断/内核锁，比 `%usr` 波动更值得追查

<details>
<summary>📝 自测题（点击展开）</summary>

1. **ON-PROC、RUNNABLE、SLEEP 三状态分别对应什么？主动和被动上下文切换的区别？**

   <details>
   <summary>参考答案</summary>

   ON-PROC=正在 CPU 上执行；RUNNABLE=可运行但在运行队列排队；SLEEP=阻塞等事件。主动切换（VCX）是线程自己阻塞让出 CPU 进入 SLEEP；被动切换（ICX）是时间片耗尽/被抢占，线程回到 RUNNABLE 继续排队。HFT 延迟敏感线程应尽量避免被动切换。
   </details>

2. **为什么跟踪调度器事件最坏可耗 10% 系统性能？怎么缓解？**

   <details>
   <summary>参考答案</summary>

   繁忙系统上下文切换每秒可达百万次，BPF 程序每事件增加哪怕 1µs，累计就是巨大开销（100 万次/s × 1µs = 1 个 CPU 核）。缓解：改用定时采样的 runqlen（99Hz，开销可忽略）做 7×24 监控，事件跟踪的 runqlat 只在诊断时短期运行；或用 -P 只跟踪目标进程。
   </details>

</details>
