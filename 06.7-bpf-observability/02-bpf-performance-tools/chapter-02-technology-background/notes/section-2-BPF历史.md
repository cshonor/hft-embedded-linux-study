# 2.2 BPF（经典 BPF 的历史）

> 底本：《BPF之巅》第 2 章技术背景，2.2 节（印刷 p18–19）

## 经典 BPF 是什么

BPF（Berkeley Packet Filter，伯克利数据包过滤器）最早是为 **tcpdump(1) 抓包过滤**设计的：在内核中运行一个小过滤程序，决定哪些数据包复制给抓包程序，避免把所有包都搬到用户态再过滤。

它解决的问题在今天依然经典——**把过滤前置到数据产生的地方**：

```text
无过滤:   网卡 ──全部包──▶ 内核 ──全部包──▶ tcpdump（用户态逐包判断丢/留）
有 BPF:   网卡 ──全部包──▶ 内核 ──BPF 程序在内核里判断──▶ 只把命中的包拷给 tcpdump
```

这个"移动计算到数据旁"的思想，正是 20 年后 eBPF 内核态聚合的祖先——变的只是"能跑什么程序"，不变的是"别把原始数据搬到用户态"。

## 时间线

| 时间 | 事件 |
|---|---|
| 1992 | McCanne & Jacobson 发表 BPF 论文（USENIX Winter） |
| 1997 | BPF 进入 Linux 内核 2.1.75，用于 tcpdump/libpcap |
| 2011 | eBPF JIT 出现（内核 2.6.35 为经典 BPF 引入 JIT 之后的演进） |
| 2012 | seccomp 使用经典 BPF |
| 2013–2014 | Alexei Starovoitov 的 eBPF 补丁系列 |
| 3.18 | bpf(2) 系统调用引入，eBPF 正式通用化 |

值得注意的演进节奏：**1992→2011 近二十年 BPF 几乎静止**（一个足够好的包过滤器不需要演进），而 2013 之后的十年从包过滤长成通用执行引擎——一旦"通用化"这个方向打开，演进速度完全换挡。技术演进的非线性，这是极好的样本。

## 经典 BPF 的限制（也是 eBPF 出现的原因）

- 只有 **2 个寄存器**（A 和 X）、**16 个 32 位槽位**的暂存空间
- 程序只能做包过滤这一件事，无法调用内核函数、无法与用户态共享状态
- 没有映射（map）机制

把限制翻译成"能力缺失"：

| 缺失能力 | 后果 | eBPF 的补法 |
|---|---|---|
| 寄存器/暂存不足 | 复杂逻辑编不出来 | 11 个 64 位寄存器 |
| 无共享状态 | 过滤器之间、内核↔用户态无法协作 | map |
| 不能调内核函数 | 只能看包数据，干不了别的 | helper 函数集 |
| 无加载接口 | 只能作为 socket 附件 | bpf(2) 系统调用 |

最后一条常被忽略但最关键：**没有 bpf(2)，BPF 程序只能是其他子系统的配件**（socket 过滤器）；有了它，BPF 才成为可独立加载、可挂到任意事件源的一等公民。

## HFT 关联

- 经典 BPF 至今活在两处：tcpdump 的包过滤表达式、seccomp 沙箱规则。HFT 系统若用 seccomp 加固交易进程，底层跑的就是经典 BPF（或 eBPF，取决于内核与配置）。
- 理解这段历史的价值在于明白：eBPF 的本质突破不是"过滤包"，而是**把"内核中安全运行小程序"泛化成通用机制**。
- "过滤前置到数据旁"的原始思想直接适用于行情链路：与其把全量行情搬进策略进程再丢弃，不如在收包路径上（XDP/TC 层）先过滤——这正是 eBPF 网络能力在 HFT 里的正确打开方式（见 [12.5-modern-networking](../../../../12.5-modern-networking/) 的 XDP 章）。

## 陷阱

- tcpdump 的 filter 语法（如 `tcp port 8000`）编译出的就是经典 BPF 字节码，与 eBPF 不是一回事，但两者常被混称为"BPF"。
- seccomp 的"经典 BPF"描述也过时了：现代内核的 seccomp 默认用 eBPF 实现（CONFIG_SECCOMP_FILTER），只有极老内核才是纯 cBPF——文档里看到"seccomp-BPF"要先问哪个 BPF。

## 自测

<details>
<summary>1. 经典 BPF 有几个寄存器？最初用途是什么？</summary>

2 个（A、X）；最初用途是 tcpdump 的内核态包过滤。
</details>

<details>
<summary>2. 哪个内核版本引入了 bpf(2) 系统调用？它为什么重要？</summary>

Linux 3.18。没有独立的加载接口，BPF 只能作为 socket 等子系统的配件过滤器；bpf(2) 让 BPF 程序成为可独立加载、可挂任意事件源的一等公民——通用化的起点。
</details>

<details>
<summary>3. 经典 BPF"把计算移到数据旁"的思想在现代 HFT 网络路径上如何复现？</summary>

行情过滤前置：与其把全量行情拷进用户态再筛选，不如在 XDP/TC 层用 eBPF 在收包路径上直接过滤/分流——数据不动、计算靠近数据，思想同源，能力从"包过滤"扩到"任意程序"。
</details>
