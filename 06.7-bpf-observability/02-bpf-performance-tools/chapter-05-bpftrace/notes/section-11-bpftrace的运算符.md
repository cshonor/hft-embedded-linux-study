# 5.11 bpftrace 的运算符

> 底本：《BPF之巅》第 5 章 bpftrace（印刷 p137–190），5.11 节（印刷 p164–165）

## 内容详解

布尔运算符（5.10 已列：`== != > < >= <= && ||`）之外，bpftrace 还支持：

| 类别 | 运算符 |
|------|--------|
| 赋值 | `=` |
| 算术 | `+` `-` `*` `/` `%` |
| 自增自减 | `++` `--` |
| 位运算 | `&`（按位与）、`|`（按位或）、`^`（按位异或）、`<<` `>>`（左右位移） |
| 逻辑 | `!`（逻辑非） |
| 复合赋值 | `+=` `-=` `*=` `/=` `%=` `&=` `^=` `<<=` `>>=` |

定义与 C 语言类似。

### "类 C"的例外：全是 64 位无符号

运算符表看着像 C，但求值语义有一个根本差异——**所有整数都是 64 位无符号**（5.12.1）。这个差异在四个运算符族上爆雷：

| 运算 | C 里你习惯的 | bpftrace（无符号语义）下的实际行为 |
|------|-------------|----------------------------------|
| 比较 `x < 0` | 负数为真 | **永假**——无符号数没有"负" |
| 减法 `a - b`（a<b） | 得负数 | 得**巨大正数**（2⁶⁴ 附近的回绕值） |
| 右移 `x >> 56` | 算术/逻辑移位看类型 | 一律逻辑移位（高位补 0），符号位不保 |
| 除法 `/` | 同左 | 一致，但被 0 除是验证器/运行期错误 |

**为什么单行 7 的过滤是 `/args->ret > 0/` 而不是 `args->ret != -1`**：无符号世界里 errno 是 2⁶⁴-N 的回绕值，按负数思维写比较全部失真；按"只要大于 0 就是字节数"过滤，语义上天然正确，还顺便滤掉了 0（EOF）。排障实践中"结果大得离谱"（直方图最右桶爆表）往往就是无符号回绕的指纹。

### 位运算的三个实战形态

```awk
// 1. 标志位测试（最常用）：按位与，非零为真
args->flags & O_NONBLOCK        // open 的非阻塞标志
args->gfp_mask & __GFP_FS       // 分配是否允许递归文件系统操作

// 2. 位域抽取：移位 + 掩码
(args->gfp_mask >> 1) & 0x3     // 取第 1-2 位组成的子字段

// 3. 对齐判断：
$addr & 0xFFF                   // 非零 = 不是页对齐（4K 页）
```

## HFT 关联

- 位运算常用于标志位解析：`args->flags & O_NONBLOCK`、`args->gfp_mask & __GFP_FS`；
- `++`/`+=` 配映射表做快速计数（注意 `@x++` 并发精度问题，见 5.7.11）；
- 无符号语义对延迟计算的反向保护：`nsecs - @start[tid]` 依赖"@start 先于 nsecs 记录"这一时序——一旦时序被破坏（5.17 的入口未记录案例，@start 缺失按 0 算），回绕值巨大直接把直方图打爆，**离群点反而是 bug 的显影剂**。工程上可在求差后加合理性过滤：`/(nsecs - @start[tid]) < 10000000000/`（10s 上限）兜底。

## 陷阱

- ⚠️ 所有整数当前都是 **64 位无符号**（见 5.12.1）——`-` 结果的符号判断、`>>` 语义都按无符号处理；比较负错误码要用 `args->ret < 0` 这类写法时留意版本行为（可先转 str/强转）。
- ⚠️ 无浮点运算——比率计算在 print 时用除数参数（见 5.14.7 print 的 div）。
- ⚠️ 移位量超过 63 是未定义行为（和 C 一样）——`$x << 64` 在不同后端可能直接得到 0 或垃圾值，别依赖。

<details>
<summary>自测题</summary>

1. bpftrace 支持浮点数吗？
   <details><summary>答案</summary>不支持；整数运算，比率用 print(@m, top, div) 的除数处理。</details>

2. 怎样测试标志位 flags 中是否含 O_NONBLOCK？
   <details><summary>答案</summary>`args->flags & O_NONBLOCK`（按位与，非零为真）。</details>

3. 无符号语义下 `5 - 7` 等于多少？这对 sum 类统计意味着什么？
   <details><summary>答案</summary>2⁶⁴-2（回绕成巨大正数）——任何"逻辑上应为负"的差值混进聚合，都会把 sum/avg/hist 彻底打爆；所以过滤必须在进聚合之前做。</details>

4. 直方图最右桶出现天文数字，按什么顺序排查？
   <details><summary>答案</summary>① 无符号回绕（负差值/错误码混入——查过滤器）→ ② 双探针入口未记录（/@start[tid]/ 缺失）→ ③ 才考虑系统真有离群慢事件。</details>
</details>
