# 3.9 字符串字面量只读

## 本节讲什么

字符串字面量存储在 **只读段（`.rodata`）**。`char *p = "hello"` 后 **修改 `p[i]`** 为 UB，常 **SIGSEGV**。

## 要点

```c
char *p = "hello";
p[0] = 'H';        /* UB / 段错误 */

char arr[] = "hello";
arr[0] = 'H';      /* 合法：栈/数据段副本 */
```

### 与 ch01 衔接

[ch01 1.3](../ch01-lexical-pitfalls/1.3-字符与字符串常量.md)：`"0"` 是指针，`'0'` 是字符。

### 与链接衔接

跨文件 `extern char msg[]` vs `extern char *msg` — [Expert C ch04/ch05](../../03-Advanced-Expert-C-Programming/ch04-arrays-are-not-pointers/)。

### 工程规范

- 只读文本用 **`const char *`**
- 需要修改 → **`char buf[]`** 或 `malloc` + 拷贝

### demo06

```bash
make -C demo/demo06_ro_string && ./demo/demo06_ro_string/main
```

（只读修改可能崩溃；demo 默认仅打印地址对比。）

## 与后续衔接

- **ch04 连接**：符号类型不匹配
- **ch05**：`strcmp` 参数类型

---

## 代码自测

**题目 1：** 以下代码会发生什么？
```c
char* s = "hello";
s[0] = 'H';  // 合法吗？
```

<details>
<summary>参考答案</summary>

未定义行为。字符串字面量存储在只读段（.rodata），修改它可能段错误。正确做法：
```c
char s[] = "hello";  // 在栈上复制一份
s[0] = 'H';  // OK
```
`char* s = "hello"` 中 s 指向只读内存；`char s[] = "hello"` 中 s 是栈数组，内容可修改。C11 起字符串字面量类型为 `const char[]`（但为了向后兼容，赋给 `char*` 只警告不报错）。

</details>
