# 7.12 函数指针

## 本节讲什么

**函数指针**保存函数入口地址，实现回调、虚表、**跳转表（jump table）** 与运行时策略切换。内核 `file_operations`、IRQ handler、DPDK burst API 均依赖函数指针。用 typedef 声明签名是工程惯例；**demo04** 用 `enum op_t` 索引分发表。

## 要点

```c
typedef int (*calc_fn)(int, int);

static int add(int a, int b) { return a + b; }
static int sub(int a, int b) { return a - b; }

calc_fn fp = add;
fp(3, 4);                    /* 7 */
calc_fn table[] = { add, sub };
table[0](10, 3);
```

**demo04 jump table**：

```c
typedef enum { OP_ADD, OP_SUB, OP_MUL, OP_DIV, OP_COUNT } op_t;

static calc_fn jump_table[OP_COUNT] = {
    [OP_ADD] = add,
    [OP_SUB] = sub,
    [OP_MUL] = mul,
    [OP_DIV] = div_safe,
};

static int dispatch(op_t op, int x, int y) {
    if (op >= OP_COUNT || !jump_table[op])
        return 0;
    return jump_table[op](x, y);
}
```

| 模式 | 用途 |
|------|------|
| 回调 | `qsort` 比较函数、`request_irq` |
| ops 表 | `struct file_operations` |
| 状态机 | `handlers[state]()` |
| 替代 switch | 密集 op 码分派 |

```c
/* 内核 file_operations 片段（示意） */
struct file_operations {
    ssize_t (*read)(struct file *, char __user *, size_t, loff_t *);
    ssize_t (*write)(struct file *, const char __user *, size_t, loff_t *);
};
```

```bash
gcc demo04_func_ptr.c -o demo04 && ./demo04
nm demo04 | grep -E ' add| sub'
```

- **函数名 decay**：`add` 与 `&add` 同义
- **NULL 槽**：分发表可留空，调用前检查
- **与数据指针互转**：标准不要求，勿玩弄

## 与后续衔接

- **ch08** OOP 风格 ops 与位操作回调
- **7.6** `op_t` 枚举边界
- **7.5.1** typedef 函数指针
- **demo04** 完整 jump table

---

## 代码自测

**题目 1：** 函数指针的作用是什么？以下代码如何实现回调？
```c
typedef int (*compare_t)(const void *, const void *);
int int_cmp(const void *a, const void *b) {
    return *(const int *)a - *(const int *)b;
}
int arr[5] = {3, 1, 4, 1, 5};
qsort(arr, 5, sizeof(int), int_cmp);
```
<details>
<summary>参考答案</summary>

函数指针：指向函数的指针，可通过它调用函数。

- `compare_t`：typedef，函数指针类型，指向 `int(const void*, const void*)` 函数
- `int_cmp`：比较函数，返回 a-b
- `qsort`：接收函数指针作为参数，排序时调用 `int_cmp` 比较元素

函数指针用途：
1. **回调函数**：qsort/bsearch 的比较器、信号处理器
2. **策略模式**：不同排序/查找策略
3. **虚函数表**：C 实现 OOP 多态
4. **中断向量表**：裸机中断处理
5. **插件/驱动注册**：内核 file_operations

函数指针 vs 直接调用：
- 直接调用：编译时确定地址，可内联
- 函数指针调用：运行时确定地址，不可内联（有间接调用开销）

内核中函数指针无处不在：`struct file_operations` 的每个成员都是函数指针。

</details>
