# 10.8.2 操作寄存器

## 本节讲什么

把 **MMIO 读写** 封装成 **类型安全、可审查** 的内联函数：读状态、写配置、 **RMW 辅助**、 **延迟与 barrier**。本节给出驱动层推荐写法，避免魔法数字散落。

## 要点

**寄存器结构体映射**（可选）：

```c
typedef struct {
    volatile uint32_t DR;
    volatile uint32_t SR;
    volatile uint32_t CR;
    uint32_t _rsvd;
    volatile uint32_t BRR;
} uart_regs_t;

#define UART1  ((uart_regs_t *)0x40011000)
```

**内联 API**：

```c
static inline uint32_t mmio_read32(uintptr_t addr) {
    return *(volatile uint32_t *)addr;
}

static inline void mmio_write32(uintptr_t addr, uint32_t val) {
    *(volatile uint32_t *)addr = val;
}

static inline void mmio_set_bits(uintptr_t addr, uint32_t mask) {
    mmio_write32(addr, mmio_read32(addr) | mask);
}
```

**写后读确认（可选）**：

- 部分 IP 需要 **write → readback** 确认
- 或 **dsb** 保证写入到达外设：

```c
mmio_write32(UART_BASE + CR, val);
__DSB();
```

**批量初始化表**：

```c
static const struct { uint32_t off; uint32_t val; } uart_init[] = {
    { 0x0C, 0x0000 },  /* CR 复位 */
    { 0x08, 0x0683 },  /* BRR 9600 */
    { 0x0C, UART_CR_UE | UART_CR_TE | UART_CR_RE },
};
for (size_t i = 0; i < ARRAY_SIZE(uart_init); i++)
    mmio_write32(UART_BASE + uart_init[i].off, uart_init[i].val);
```

- 清晰、易 code review；与 **ch09** 配置模块结合

**Linux regmap**：

- 统一 **cache、bus、debugfs**；I2C SPI 尤其有用

**多任务/ISR**：

- 修改 **共享控制寄存器** 的 RMW 段 → **临界区**（**10.2.4**）
- 优先使用 **硬件 SET/CLR 寄存器**

## 与后续衔接

- **10.8.3** 何时用位域 struct
- **10.7.3** volatile 与 ioremap
- **demo** BSP 层 `hw_write_reg()`

---

## 代码自测

**题目 1：** 以下代码用结构体映射硬件寄存器，有什么优点和注意事项？
```c
typedef struct {
    volatile uint32_t CR;      // 0x00: 控制寄存器
    volatile uint32_t SR;      // 0x04: 状态寄存器
    volatile uint32_t DR;      // 0x08: 数据寄存器
    volatile uint32_t BRR;     // 0x0C: 波特率寄存器
} uart_regs_t;

#define UART1 ((uart_regs_t *)0x40011000)

UART1->BRR = 0x0271;  // 设置波特率
```
<details>
<summary>参考答案</summary>

优点：
1. 可读性好：UART1->CR 比 *(uint32_t*)0x40011000 直观
2. 编译器自动计算偏移：结构体成员地址自动按偏移计算
3. 类型安全：每个寄存器有明确类型
4. 便于维护：添加新寄存器只需加成员

注意事项：
1. volatile 必须加：确保编译器不优化掉寄存器访问
2. 结构体对齐：编译器可能插入 padding，应使用 __packed 或手动确保对齐
3. 保留寄存器：硬件手册中某些地址保留不用，应加保留成员占位（如 RESERVED[2]）
4. 不保证原子性：结构体访问仍然是非原子的读-改-写，需要额外保护

</details>
