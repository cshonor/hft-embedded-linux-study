# 10.6.2 存储映射

## 本节讲什么

**存储映射（Memory Map）** 把 **物理地址范围** 分配给各设备与 RAM；软件通过 **指针/基址+偏移** 访问。本节说明 **链接脚本、scatter file、Device Tree、`mmap`** 如何描述映射，衔接 **10.9 虚拟地址**。

## 要点

**MCU 平坦映射**：

```c
#define UART1_BASE  0x40011000
#define UART1_DR    (*(volatile uint32_t *)(UART1_BASE + 0x04))
```

- 编译器与 CPU 使用 **同一数字地址**
- **链接脚本** 定义段放置：

```ld
MEMORY {
  FLASH (rx) : ORIGIN = 0x08000000, LENGTH = 512K
  RAM   (rwx): ORIGIN = 0x20000000, LENGTH = 128K
}
```

**Linux 内核映射**：

- 内核 **线性映射** 或 **HIGHMEM**；用户态见 **虚拟地址**
- **`ioremap(phys, size)`**：内核把 MMIO 物理页映射到内核虚拟地址
- **`mmap(/dev/mem)`** 或 **`mmap` char driver**：用户态映射（需权限，**10.4.2**）

**Device Tree（ARM Linux）**：

```dts
uart0: serial@40011000 {
    compatible = "vendor,uart";
    reg = <0x40011000 0x400>;
    interrupts = <33>;
};
```

- 内核 probe 时 `reg` → `ioremap`；驱动 `devm_ioremap_resource`

**Cache 属性**：

| 区域 | 属性 |
|------|------|
| 普通 RAM | cacheable, write-back |
| 设备寄存器 | **device**, non-cacheable |
| 共享 DMA 缓冲 | **non-cache** 或 software coherency |

- 对 MMIO 用 cache → **总线错误** 或读 stale 值

**MPU 区域**（RTOS）：

- 任务可配置 **仅访问** `0x20000000–0x2001FFFF` RAM，碰 MMIO  fault

## 与后续衔接

- **10.9.1** VA→PA 页表
- **10.7.3** I/O 内存 vs 端口
- **10.8.2** 安全访问寄存器

---

## 代码自测

**题目 1：** 以下链接脚本将变量放在特定地址，启动代码需要做什么？
```c
/* 链接脚本片段 */
SECTIONS {
    . = 0x08000000;
    .data : { *(.data) }
    . = 0x20000000;
    .bss : { *(.bss) }
}

/* C 代码 */
int global_var = 42;    // .data 段
int uninit_var;         // .bss 段
```
<details>
<summary>参考答案</summary>

.data 和 .bss 在不连续的地址（.data 在 Flash 0x08000000，.bss 在 RAM 0x20000000），启动代码必须做两件事：

1. 从 Flash 拷贝 .data 到 RAM：C 标准要求全局变量有初始值，但 .data 段的初始值存储在 Flash 中，运行时需要在 RAM 中，启动时要从 Flash 加载到 RAM。

2. 清零 .bss 段：C 标准要求未初始化的全局变量为 0，但 .bss 段在 RAM 中，上电后内容随机，启动时必须清零。

启动代码通过链接脚本导出的符号（_data_load, _data_start, _data_end, _bss_start, _bss_end）来执行拷贝和清零操作，然后调用 main()。

</details>
