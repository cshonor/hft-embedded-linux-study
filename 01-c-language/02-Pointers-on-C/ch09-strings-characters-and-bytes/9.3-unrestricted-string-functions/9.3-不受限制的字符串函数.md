# 9.3 不受限制的字符串函数

## 本节讲什么

**`<string.h>`** 中依赖 **`'\0'`** 作为扫描终点的经典函数族：`strcpy`、`strcat`、`strcmp`、`strlen`、`strchr`、`strstr` 等。它们**没有任何长度参数**，扫描到 `\0` 才停，因此得名"不受限制"（unrestricted）。这族函数是 UNIX 文本处理传统的产物，在**文本场景**尚可用（前提是数据确实 `\0` 终止），但在**二进制报文场景**是高危 API，生产环境**基本禁用**。

本章后续小节逐一展开各函数的细节，本节是总览。

## 要点

### 函数清单

| 函数 | 作用 | 返回值 | 风险等级 |
|------|------|--------|----------|
| **`strcpy(dst, src)`** | 拷贝 src（含 `\0`）到 dst | `dst` | **致命**：dst 溢出 |
| **`strcat(dst, src)`** | 在 dst 末尾追加 src | `dst` | **致命**：dst 溢出 |
| **`strcmp(s1, s2)`** | 字典序比较 | `<0/0/>0` | 低（只读） |
| **`strlen(s)`** | 长度 | `size_t` | 中：无 `\0` 越界读 |
| **`strchr(s, c)`** | 查找字符 | 位置指针或 NULL | 中：依赖 `\0` |
| **`strstr(s, sub)`** | 查找子串 | 位置指针或 NULL | 中：依赖 `\0` |

### 共性特征

1. **无长度参数**：扫描边界完全由 `\0` 决定
2. **`\0` 必须存在**：缓冲无 `\0` → 越界读 UB
3. **写函数不检查 dst 容量**：`strcpy`/`strcat` 不知道 dst 多大
4. **仅适用于 C 字符串**：二进制数据含 0x00 时语义被破坏
5. **多数返回 `dst`**：便于链式写法，但也容易让人忽略返回值检查

### 与 mem* 系列的根本区别

| 维度 | **str\*** | **mem\*** |
|------|-----------|-----------|
| 边界 | `\0` 终止 | 显式 `n` 字节 |
| 0x00 处理 | 终止扫描 | 合法数据 |
| 长度参数 | 无 | **必须有** |
| 二进制适用 | **否** | **是** |
| 典型场景 | 文本、日志、命令行 | **报文、内存块、struct** |
| HFT 角色 | 辅助（日志/配置） | **主力** |

### 历史背景

这族函数源自 1970s UNIX V6/V7 的 `<strings.h>`，当时 C 还没有 `size_t`（1979 年 K&R 时期），字符串就是字符数组+哨兵的代名词。ANSI C89 将其标准化进 `<string.h>` 并新增了 `strn*` 限长版（9.4）。但 `strn*` 仍依赖 `\0` 语义，**不是**真正的二进制安全 API。

### 工程规范分层

| 场景 | 推荐 API | 理由 |
|------|----------|------|
| **文本日志、命令行参数** | `str*` 可用，但优先 `snprintf` | 数据有 `\0`，但仍需防溢出 |
| **配置文件解析** | `snprintf` / `strlcpy` | 数据有 `\0`，但用户输入不可信 |
| **二进制报文、mbuf payload** | **`mem*` + len**（9.9） | 数据可能含 0x00 |
| **结构体字段拷贝** | `memcpy` + `sizeof` | 二进制语义，长度编译期已知 |
| **跨线程共享** | 避免 `strerror`/`strtok` | 非线程安全（9.6/9.7） |

### 典型漏洞模式

```c
/* 经典栈溢出：strcpy 不检查 dst 大小 */
void bad(char *user_input) {
    char buf[16];
    strcpy(buf, user_input);   /* user_input > 15 字节即溢出 */
}
```

这正是 Morris 蠕虫（1986）、Code Red（2001）、无数 CVE 的根因。现代编译器在 `-O2 -D_FORTIFY_SOURCE=2` 下会对已知大小的 `strcpy` 插入边界检查，但**仅限编译期能推断大小的场景**，动态缓冲仍无保护。

### 编译期硬化选项

```bash
gcc -O2 -D_FORTIFY_SOURCE=2 -fstack-protector-strong ...
```

- `-D_FORTIFY_SOURCE=2`：让 glibc 的 `strcpy`/`memcpy` 等在编译期能推断大小时插入 `__strcpy_chk`，运行时检测溢出
- `-fstack-protector-strong`：栈 canary，防栈溢出覆盖返回地址
- **但不替代**正确的 API 选择——`mem*` + len 才是根治

## HFT 关联

- **DPDK rte_memcpy**：基于 `memcpy` 的优化版，按对齐和长度选择 SIMD 路径，HFT 报文拷贝主力
- **FIX 解析**：tag-value 用 SOH(0x01) 分隔，**不是** `\0`，所以 `strtok(s, "\x01")` 可用但低效，生产用偏移表
- **日志拼接**：`rte_log` 内部用 `snprintf`，**不用** `strcat`
- **协议头比较**：Ethernet/IP/TCP 头部用 `memcmp`，**不用** `strcmp`
- **配置字符串**：`rte_eal_init` 的 argv 是 C 字符串，可用 `str*`，但解析后转结构体

## 与后续衔接

- **9.3.1** strcpy 细节与替代方案
- **9.3.2** strcat 与安全拼接
- **9.3.3** 返回值语义与链式写法
- **9.3.4** strcmp 与二进制比较的对立
- **9.4** strn* 限长版——仍是文本 API
- **9.9** mem* —— HFT 真正的主力
- **内核** `strlcpy`/`strscpy` —— OpenBSD 始创，Linux 2016 年引入，比 `strncpy` 更安全
- **《C 陷阱与缺陷》ch05** 经典 strcpy 坑案例分析

## 自测题

1. 为什么 `strcpy` 比 `memcpy` 更危险？至少给出 2 个结构性原因。
2. `-D_FORTIFY_SOURCE=2` 在什么条件下才能拦截 `strcpy` 溢出？什么条件下无效？
3. `strcat(dst, src)` 要扫描几次字符串？复杂度是多少？
4. 内核 `strlcpy(dst, src, size)` 与 `strncpy(dst, src, size)` 的关键区别是什么？为什么前者更安全？
5. 给定 `uint8_t *pkt, size_t len`，要拷贝前 64 字节到 `header[64]`，应该用 `strcpy`、`strncpy`、`strlcpy` 还是 `memcpy`？为什么？
