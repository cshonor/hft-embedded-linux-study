# 15.4.2 文件

## 本节讲什么

**文件**在 stdio 中由 **`fopen`** 打开的路径对象绑定到 **`FILE *` 流**。

## 要点

- 磁盘文件、管道、设备均可 fopen（用户态）
- 文本 vs 二进制：Linux 下 **`b` 无换行转换**，跨平台代码仍应 **`rb`/`wb`**
- 一个 FILE 对应一次 fopen；重复打开同一路径得不同 FILE *

## 与后续衔接

**15.6** 打开模式；pcap、行情 **`.dat`** 用二进制。
