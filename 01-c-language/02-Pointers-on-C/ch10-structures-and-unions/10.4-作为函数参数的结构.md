# 10.4 作为函数参数的结构

## 本节讲什么

struct 作为函数参数有**按值传递**和**按指针传递**两种方式，这是 HFT 延迟优化的关键选择。按值拷贝整个 struct，对小 struct 简洁安全，对大 struct 是性能灾难；按指针只传地址，快但需注意所有权和生命周期。本节剖析两种方式的 ABI 实现、性能权衡、`const` 的作用、以及返回 struct 的细节。

## 要点

### 两种传递方式

```c
/* 按值：拷贝整个 struct 到栈 */
void process_by_value(QuoteHdr pkt);

/* 按指针：只传地址 */
void process_by_ptr(const QuoteHdr *p);
```

| 方式 | 参数开销 | 调用方 | 被调方 | 适用 |
|------|----------|--------|--------|------|
| **按值** | `sizeof(struct)` 字节拷贝 | 简洁 | 副本，安全 | 小 struct（≤16 字节） |
| **指针** | 8 字节地址 | 取地址 | 共享原对象 | 大 struct、热路径、需修改 |
| **`const T *`** | 8 字节地址 | 取地址 | 只读共享 | 只读访问，避免拷贝 |

### 按值传递的 ABI 细节

x86-64 System V ABI：

- struct ≤ 16 字节：通过寄存器传递（整数用 rdi/rsi/...，浮点用 xmm0-7）
- struct > 16 字节：通过栈传递，或由调用方分配空间传指针（RAX 指向）

```c
struct small { int a, b; };   /* 8 字节 */
void f(struct small s);       /* 通过 rdi 传 8 字节 */

struct big { int arr[100]; }; /* 400 字节 */
void g(struct big b);         /* 栈拷贝 400 字节 */
```

ARM64 ABI 类似，但阈值不同（≤16 字节用寄存器）。

### 按指针的性能优势

```c
/* 反例：按值传 256 字节 struct */
void process(struct big b);   /* 每次调用拷贝 256 字节 */

/* 正例：传指针 */
void process(const struct big *b);   /* 8 字节地址 */
```

- 256 字节拷贝：约 30-50 ns（memcpy）
- 指针传递：约 1-2 ns（寄存器移动）
- **HFT 铁律**：大于 16 字节的 struct **传指针**

### const 指针的语义

```c
/* 只读：承诺不修改 struct */
uint32_t get_seq(const QuoteHdr *p) {
    return p->seq;
}

/* 读写：可能修改 struct */
void set_seq(QuoteHdr *p, uint32_t seq) {
    p->seq = seq;
}
```

- `const T *p`：API 契约，告知调用方"我不会改你的数据"
- 编译器在函数内对 `p->field = ...` 报错
- 是 C 代码中**最重要的 const 修饰**，影响 API 设计

### 所有权与生命周期

```c
/* 借用：调用方仍拥有 */
void process(const QuoteHdr *p) {
    /* p 的生命周期由调用方保证 */
    printf("%u\n", p->seq);
}

/* 转移所有权：被调方负责释放 */
void consume(QuoteHdr *p) {
    /* 用完后 free(p) */
    free(p);
}
```

C 没有所有权标注（C++ 有 `unique_ptr`），靠**注释和命名约定**：

- `process` / `print` / `get`：通常借用
- `consume` / `destroy` / `free`：通常转移所有权
- DPDK 用 `rte_pktmbuf_free(m)` 明确释放语义

### 返回 struct

```c
/* 按值返回小 struct：通过寄存器 */
struct point make_point(int x, int y) {
    return (struct point){ .x = x, .y = y };
}

/* 按值返回大 struct：调用方传入隐藏指针 */
struct big make_big(void) {
    struct big b = {0};
    /* ... */
    return b;   /* 实际写入调用方提供的缓冲 */
}
```

- 小 struct：寄存器返回，高效
- 大 struct：调用方分配空间，被调方填充（RVO 类似优化）
- **HFT 铁律**：大 struct 返回用"输出参数"风格

```c
/* 输出参数风格：避免大 struct 返回 */
int make_big(struct big *out) {
    if (!out) return -1;
    *out = (struct big){0};
    /* ... */
    return 0;
}
```

### 与 memcpy 的关系

```c
/* 按值传参 */
void f(struct big b);
struct big x = /* ... */;
f(x);   /* 等价 memcpy(&b_stack_slot, &x, sizeof(struct big)) */
```

- 按值传递本质是编译器生成的 `memcpy`
- 大 struct 时这就是性能开销

### struct 数组参数

```c
/* 数组退化为指针，无拷贝 */
void process_all(struct order orders[], size_t n);
void process_all(struct order *orders, size_t n);   /* 等价 */
```

- struct 数组退化为 `struct order *`，只传首地址
- 调用 `process_all(arr, 100)` 不拷贝 100 个 struct

## HFT 关联

- **DPDK mbuf**：`rte_eth_rx_burst(port, queue, pkts, n)` 传 `struct rte_mbuf *pkts[]`（指针数组），不拷贝 mbuf
- **协议头**：`parse(const struct eth_hdr *eth)` 传只读指针，零拷贝
- **订单处理**：`process_order(const struct order *o)` 传指针，避免 64 字节 struct 拷贝
- **回调函数**：`void cb(const struct event *e, void *user)` 标准事件回调签名
- **性能铁律**：热路径所有 struct 参数传 `const T *`，仅在需要修改时用 `T *`

### 实操：按值 vs 按指针性能

```c
struct big { uint64_t data[32]; };   /* 256 字节 */

uint64_t sum_by_value(struct big b) {
    uint64_t s = 0;
    for (int i = 0; i < 32; i++) s += b.data[i];
    return s;
}

uint64_t sum_by_ptr(const struct big *b) {
    uint64_t s = 0;
    for (int i = 0; i < 32; i++) s += b->data[i];
    return s;
}

/* 调用 100 万次：
 * sum_by_value: ~50 ms（含拷贝）
 * sum_by_ptr:   ~2 ms（零拷贝）
 */
```

### 实操：const 契约

```c
/* API 契约：不修改 hdr */
uint16_t get_eth_type(const struct eth_hdr *hdr) {
    return ntohs(hdr->type);
}

/* 调用方：放心传只读数据 */
const struct eth_hdr *eth = (const struct eth_hdr *)pkt;
uint16_t type = get_eth_type(eth);   /* 安全：const 保证不修改 */
```

### 实操：输出参数风格

```c
/* 反例：返回大 struct */
struct order_book build_book(void) {
    struct order_book b = {0};
    /* ... 填充 */
    return b;   /* 编译器 RVO 优化，但仍可能拷贝 */
}

/* 正例：输出参数 */
int build_book(struct order_book *out) {
    if (!out) return -1;
    *out = (struct order_book){0};
    /* ... 填充 out->entries */
    return 0;
}
```

## 与后续衔接

- **ch11** 堆上 struct 的指针传递
- **ch12** 结构体指针链表 API 设计
- **ch13** 函数指针与回调签名
- **x86-64 ABI** 寄存器传参规则

## 自测题

1. `void f(struct big b)` 与 `void f(const struct big *b)` 在 x86-64 上的调用开销差异？大 struct 多大时差距显著？
2. `const QuoteHdr *p` 在 API 设计中起什么作用？调用方如何受益？
3. 返回大 struct 时编译器如何处理？输出参数风格有什么优势？
4. `void process(struct order orders[], size_t n)` 传参时会拷贝 100 个 struct 吗？为什么？
5. HFT 报文解析函数 `parse_eth(const uint8_t *pkt, size_t len)`，要返回解析出的 `struct eth_hdr`，应该按值返回还是输出参数？为什么？
