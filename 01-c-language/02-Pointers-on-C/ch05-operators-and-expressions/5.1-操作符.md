# 5.1 操作符

## 本节讲什么

C **全部运算符**概览:算术、移位、位运算、关系、逻辑、一元、三元、赋值、逗号。这些是 DPDK mbuf 标志位、寄存器读写、报文解析的日常工具。本节给全景,5.4 系列再深挖求值顺序与 UB。**HFT 里大半"数值错乱"事故的根因在这一章。**

## 要点

### 算术 `+ - * / %`

| 运算 | 规则 | 例子 |
|------|------|------|
| 整数 `/` | 截断小数,向 0 取整 | `7/3=2`,`-7/3=-2` |
| `%` | 仅整型;符号随**被除数** | `7%3=1`,`-7%3=-1` |
| 无符号溢出 | 模 2^N 环绕(**定义良好**) | `uint32_t` 计数到 0xFFFFFFFF+1=0 |
| 有符号溢出 | **UB** | `INT_MAX+1` 行为未定义 |

```c
uint32_t seq = 0xFFFFFFFF;
seq++;                 /* 0, 定义良好 —— 序列号环绕合法 */
int32_t  v = INT_MAX;
v++;                   /* UB, 编译器可假定不溢出做激进优化 */
```

> **HFT 铁律**:价格用 `int64_t` 定点(如 1e-7 精度),序列号/时间戳用 `uint64_t`。避免 `double` 精度分歧,避免有符号溢出 UB。

### 移位 `<<` `>>`

| 运算 | 规则 | 陷阱 |
|------|------|------|
| `a << n` | 低位补 0,等价 ×2^n(正数/无符号安全) | 左移负数是 UB;有符号溢出 UB |
| `a >> n`(无符号) | 逻辑右移,补 0 | 安全 |
| `a >> n`(有符号负数) | **结果实现定义**(实践中 x86/ARM 多为算术右移补符号位,标准不保证) | 可移植性差,不要对负数做 `>>` |
| 位数 ≥ **提升后左操作数位宽** | **UB** | `1 << 32`(int 32 位)是 UB;注意是提升后类型,不是声明类型 |
| 位数 < 0 | **UB** | 移位量必须 ≥ 0 且 < 提升后位宽 |

> **两个易错点(标准原文 C11 §6.5.7)**

**(1) "位宽"看的是整型提升后的类型,不是声明类型。** `uint8_t b;` 做 `b << 16` 合法——b 提升为 int(32 位),16 < 32,不 UB。同理 `b << 1` 也合法,但**结果类型是 int 不是 uint8_t**,赋回窄类型会静默截断:

```c
uint8_t b = 0x80;
uint8_t r = b << 1;    /* 0x80<<1 = 0x100, 截断回 uint8_t = 0x00, 静默丢数据 */
```

这是比符号位 UB 更常见的 bug——`-Wconversion` 能抓到这种隐式截断。

**(2) 有符号右移的"实现定义"措辞要精确。** 标准原文只说"结果实现定义",**没规定必须算术右移**——某编译器给逻辑右移也合规。x86 编 `SAR`(算术),无符号编 `SHR`(逻辑),都是 1 周期;但编译器不敢跨平台假设它是算术,所以你写 `int x = -1; x >> 1;` 在 x86 上得 `-1`,换平台可能得超大正数。

```c
uint8_t b = pkt[0];
uint16_t len = (pkt[1] << 8) | pkt[2];   /* 大端读字段, 注意整型提升 */
uint32_t mask = 1u << 5;                  /* 第 5 位掩码, 用 1u 避免 UB */
uint32_t high = val >> 24;                /* 取最高字节(无符号安全) */
```

> ⚠️ `1 << 31`(int 32 位)是 UB(1×2^31 在 int 中不可表示, C11 §6.5.7p4);要 `1u << 31` 或 `1ULL << 63`。`pkt[1] << 8` 中 pkt[1] 最大 255, `255<<8=65280` 在 32 位 int 里安全(不碰符号位)——这一行本身不 UB, 注释提醒的是"运算发生在 int 上"这个事实。

> **HFT 视角**:移位本身几乎零成本(`1u << k` 掩码常被编译器折叠成立即数, 热路径零指令);UB 真正可怕的不是慢, 而是编译器基于"有符号移位不溢出"的假设**删掉你写的溢出检查**——移位用 `uintXX_t` 不只是可移植, 是防编译器激进优化。

### 位运算 `~ & | ^`

| 符 | 用途 | 例子 |
|----|------|------|
| `&` | 提取/过滤位 | `flags & PKT_ORDER` |
| `\|` | 置位 | `flags \|= PKT_ERR` |
| `^` | 翻转位 | `flags ^= PKT_VLAN` |
| `~` | 掩码取反 | `flags &= ~PKT_VLAN`(清位) |

```c
flags |=  PKT_ERR;          /* 置位 */
flags &= ~PKT_VLAN;         /* 清位 */
flags ^= PKT_DUP;           /* 翻转 */
if (flags & PKT_ORDER) {    /* 测试位 —— 注意是 & 不是 && */
    /* ... */
}
```

> ⚠️ **`if (flags & MASK)` ≠ `if (flags && MASK)`**。前者位与,得到掩码内的值;后者逻辑与,只给 0/1。测试位必须用 `&`。

### 关系与判等 `< <= > >= == !=`

- 结果类型 `int`,值为 `1` 或 `0`
- **`=` vs `==`**:`if (flag = 1)` 恒真陷阱(4.2)
- 比较无符号与有符号负数 → 提升为无符号,负数变超大正数(5.4.2)

### 逻辑 `&&` `||`(短路)

```c
if (mbuf != NULL && mbuf->len > 0)   /* mbuf==NULL 时不解引用, 安全 */
    process(mbuf);
```

- **`A && B`**:A 假则不求 B(短路)
- **`A || B`**:A 真则不求 B
- 短路是"安全空指针"模式的语义基础,**不能重排序**

### 一元 `& * ! ~ sizeof ++ -- + -`

| 符 | 语义 |
|----|------|
| `&` | 取址(左值 → 指针),ch06 |
| `*` | 解引用(指针 → 左值),ch06 |
| `!` | 逻辑非,`!x` 等价 `x==0` |
| `~` | 按位取反 |
| `sizeof` | 编译期字节数;**数组传参后退化, sizeof(p) 是指针宽** |
| `++ --` | 自增自减,前置先变后用,后置先用后变 |
| `+ -` | 一元正负 |

```c
int arr[10];
sizeof(arr);          /* 40, 数组大小 */
void f(int a[10]){ sizeof(a); }   /* 8, 退化成指针 */

int a = 5, b;
b = a++;              /* b=5, a=6(后置: 先用后变) */
b = ++a;              /* a=6, b=6(前置: 先变后用) */
```

> **同一表达式多次 `++`/`--` 同一变量 → UB**(序列点规则,5.4.3):`a = a++ + ++a;`

### 三元 `?:`

```c
uint64_t price = (bid > 0) ? bid : ask;   /* 二选一 */
const char *s = (err == 0) ? "ok" : "err";
```

- 条件非 0 取第二操作数,否则取第三
- **短路**:只求值被选中的分支
- 三元是表达式,有值,可嵌套但可读性差

### 复合赋值 `+= -= *= /= %= <<= >>= &= ^= |=`

```c
a += b;     /* 等价 a = a + b, 但 a 只求值一次 */
a <<= 2;    /* 等价 a = a << 2 */
```

- 复合赋值中**左操作数只求值一次**:对复杂左值(`a[compute()] += b`)避免重复计算
- 与 `a = a + b` 在 a 是简单变量时等价;复杂左值时复合赋值更高效

### 逗号 `,`

```c
for (i = 0, j = n - 1; i < j; i++, j--)   /* 多变量循环 */
    swap(&a[i], &a[j]);

int x = (a, b);   /* 逗号表达式: 求值 a 丢弃, 取 b —— 几乎不用, 可读性差 */
```

- 逗号运算符:左到右求值,结果取最右
- 主要用途:for 循环多变量;其他场景少用

## 汇编证据:移位与位运算

```c
uint32_t shift_demo(uint32_t a){
    return (a << 4) | (a >> 28);   /* 循环左移 4 位 */
}
```

```asm
        mov     eax, edi
        rol     eax, 4        ; x86 有 rol 指令, 直接循环移位
        ret
```

- 移位常被编译器识别为更优指令(`rol`/`ror` 循环移位,`shld`/`shrd` 双精度移位)
- `*2`/`*4`/`*8` 编译为 `shl` 而非 `imul`,更快

## 与 HFT/DPDK

### mbuf 标志位操作

```c
struct rte_mbuf *m = ...;
m->ol_flags |= PKT_TX_IP_CKSUM | PKT_TX_L4_CKSUM;   /* 置多个标志 */
m->ol_flags &= ~PKT_TX_VLAN_PKT;                      /* 清标志 */
if (m->ol_flags & PKT_RX_IP_CKSUM_BAD) {              /* 测试位 */
    stats.rx_bad_cksum++;
}
```

### 寄存器位字段

```c
volatile uint32_t *ctrl = (volatile uint32_t *)NIC_CTRL;
*ctrl |= TX_ENABLE;          /* 置位启用发送 */
*ctrl &= ~RX_PROMISC;        /* 清位关闭混杂 */
uint32_t v = *ctrl;
if (v & LINK_UP) { ... }     /* 测试链路状态 */
```

### 报文字段提取(大端/小端)

```c
/* 网络字节序(大端)读 16 位字段 */
uint16_t len = (pkt[0] << 8) | pkt[1];
/* 读 32 位 */
uint32_t seq = ((uint32_t)pkt[0] << 24) |
               ((uint32_t)pkt[1] << 16) |
               ((uint32_t)pkt[2] <<  8) |
               ((uint32_t)pkt[3]);
```

- 注意整型提升:`pkt[0] << 8` 中 pkt[0] 是 uint8_t,先提升为 int 再移位
- 字段 ≥ 32 位要显式 cast 到 uint32_t/uint64_t,避免窄类型 UB

## 常见误区

| 误区 | 真相 |
|------|------|
| `1 << 31` 安全 | UB(int 符号位溢出),要 `1u << 31` |
| `if (flags && MASK)` 测试位 | 错,逻辑与只给 0/1,要用 `&` |
| 有符号溢出"环绕" | 不,UB;编译器可激进优化 |
| 无符号溢出是 bug | 不,定义良好,序列号环绕合法 |
| `a++ + a++` 是 2a | UB,序列点内多次修改 |
| `sizeof(arr)` 在函数内得数组大小 | 形参退化,得指针大小 |

## 实操

```bash
cat > t.c <<'EOF'
#include <stdio.h>
#include <stdint.h>
int main(void){
  uint8_t b = 0xFF;
  printf("b<<8 = %d\n", b << 8);          /* 整型提升: 0xFF00 = 65280 */
  printf("1<<31 = %d (UB!)\n", 1 << 31);   /* UB, 可能 -2147483648 */
  printf("1u<<31 = %u\n", 1u << 31);       /* 2147483648, 安全 */
  uint32_t v = 0xDEADBEEF;
  printf("high byte = 0x%x\n", v >> 24);   /* 0xde */
  uint32_t mask = 1u << 5;
  printf("mask = 0x%x\n", mask);           /* 0x20 */
  return 0;
}
EOF
gcc -std=c11 -Wall -Wconversion t.c -o t && ./t

# 移位汇编
cat > s.c <<'EOF'
unsigned rol4(unsigned a){ return (a<<4)|(a>>28); }
EOF
gcc -O2 -S s.c -o s.s && grep -E 'rol|shl|shr' s.s
```

## 自测要点(口述)

1. 整数 `/` 截断方向?`-7/3` 和 `-7%3` 各是多少?
2. `1 << 31` 为什么 UB?正确写法?`1 << 32` 呢?
3. `flags & MASK` 和 `flags && MASK` 区别?测试位用哪个?
4. 有符号溢出和无符号溢出分别是什么?为什么 HFT 用 uint64_t 存序列号?
5. `a++ + a++` 为什么是 UB?复合赋值 `a += b` 中 a 求值几次?

## 与后续衔接

- **5.2 布尔值** 真假判定与陷阱;**5.3 左值右值** 副作用
- **5.4.3** 优先级表与序列点;**ch06** 指针算术;**ch10** 位域

---

## 代码自测

**题目 1：** 以下代码输出什么？为什么？
```c
int x = 1, y = 2;
printf("%d\n", x << y);
printf("%d\n", x && y);
printf("%d\n", x & y);
```

<details>
<summary>参考答案</summary>

输出 4、1、0。x << y 是左移：1 左移 2 位 = 4。x && y 是逻辑与：两个非零值为真，结果 1。x & y 是按位与：01 & 10 = 00 = 0。注意区分逻辑运算符和按位运算符。

</details>

**题目 2：** 以下哪几句是 UB？为什么？
```c
uint8_t b = 0x80;
uint8_t r1 = b << 1;        /* A */
int     r2 = b << 25;       /* B */
uint32_t r3 = 1u << 31;    /* C */
int     r4 = 1 << 31;      /* D */
```

<details>
<summary>参考答案</summary>

- **A 不 UB，但会截断丢数据**：b 提升为 int，0x80<<1 = 0x100 = 256，结果类型是 int，赋回 uint8_t 静默截断为 0x00。`-Wconversion` 能抓。
- **B 是 UB**：b 提升为 int，0x80<<25 = 2^32，在 32 位 int 中不可表示（碰符号位），C11 §6.5.7p4 明确未定义。即使 b 声明成 uint8_t 也没用——提升后是 int。
- **C 安全**：1u 是 unsigned int，2^31 在 unsigned int 中可表示。
- **D 是 UB**：1 是 int，1×2^31 在 int 中不可表示，符号位溢出未定义。
关键：移位的"位宽"和"结果类型"都看**提升后的左操作数**，不是声明类型。窄类型（uint8_t/uint16_t）做移位会先提升为 int，既可能 UB（碰符号位），也可能截断（赋回窄类型）。

</details>
