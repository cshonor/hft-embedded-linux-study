# 5.2 布尔值

## 本节讲什么

C 用**整型 0/1** 表示真假,没有原生布尔类型(C99 才引入 `_Bool`)。关系、逻辑运算结果是 `int` 型的 `1` 或 `0`,而 `if`/`while` 条件接受"非 0 即真"。这种"整型当布尔用"的设计是 C 简洁的根源,也是 `if (x = 1)` 这类陷阱的温床。本节讲布尔语义、惯用写法、典型陷阱。

## 要点

### 真假规则

- **0 → 假**;**非 0 → 真**(不限于 1,任何非 0 整数、非空指针、非 0 浮点都为真)
- 关系/逻辑表达式结果类型是 **`int`**,值为 **`1`(真)或 `0`(假)** —— 不是操作数的原值
- `NULL`、`0.0`、`'\0'`、`0` 都是"假";其他都是"真"

```c
int a = 5;
if (a)        /* a 非 0, 真 */
if (a == 5)   /* 表达式结果 1, 真 */
int r = (a > 3);   /* r = 1, 不是 a 的值 */
```

### C99 的 `_Bool` 与 `stdbool.h`

```c
#include <stdbool.h>      /* C99 */
bool ok = true;           /* _Bool, 真正的布尔类型 */
bool ready = false;
if (!ready) return;
```

- `_Bool` 是 C99 引入的真布尔类型,只有 0/1 两个值
- `stdbool.h` 提供 `bool`/`true`/`false` 宏(C++ 兼容)
- 赋值时自动归一化:`bool b = 5;` → b = 1(任何非 0 变 1)
- C23 把 `bool`/`true`/`false` 提升为关键字,不再需要 `stdbool.h`

### 惯用写法(HFT/驱动风格)

```c
if (mbuf)           /* 指针非 NULL, 等价 mbuf != NULL */
if (!err)           /* err == 0 */
if (cnt)            /* cnt != 0 */
if (flags & X)      /* 位测试, 注意是 & 不是 && */
if (!*p)            /* *p == 0, 字符串结尾 */
```

- 不必写 `== 1` 或 `!= 0`;指针、整数均可直接作条件
- 布尔语义的函数返回值用 `bool` 更清晰:`bool is_valid(struct x *)`

### 陷阱集

| 错误写法 | 实际语义 | 后果 |
|----------|----------|------|
| `if (x = 1)` | 赋值 1,条件恒真 | 意图 `==` 却写成 `=`,死循环/逻辑错 |
| `if (flags && MASK)` | 逻辑与,flags 非零则取 MASK 真值 | 测试位要用 `&`,不是 `&&` |
| `if (a > -1)`,a 为 unsigned | a 提升为 unsigned,-1 变超大正数 | 恒真,见 5.4.1 |
| `if (p = malloc())` | 赋值,判断指针非空 | 合法但易误读,加括号注释 |
| `if (strlen(s))` | 长度非零为真 | 合法但 `strlen(s) > 0` 更清晰 |

### `!` 运算符

```c
if (!ptr)        /* ptr == NULL */
if (!err)        /* err == 0 */
int zero = !5;   /* 0, 逻辑非把非 0 变 0, 0 变 1 */
```

- `!x` 等价 `x == 0`,结果是 int 的 1 或 0
- 双重否定 `!!x` 把任意非 0 归一化为 1:`!!5 == 1`,常用于"转布尔"

### 短路与布尔

```c
if (p && p->len > 0)    /* p 为 NULL 时短路, 不解引用 */
```

- `&&` 和 `||` 短路是布尔语义的安全基础,见 5.1
- 短路保证:左侧假/真时右侧不求值,可安全检查空指针

## 与 HFT/DPDK

### 状态标志的布尔化

```c
bool is_trading_hours = (now >= open_ns && now < close_ns);
bool can_send = (ready && !paused && buffer_avail);
if (can_send) enqueue(order);
```

- 多重条件用 `bool` 变量名承载语义,比一长串 `&&` 更可读
- C99 `_Bool` 占 1 字节(至少),节省结构体空间

### 位标志 vs 布尔数组

```c
/* 位标志: 紧凑, 8 个状态 1 字节 */
uint8_t flags = 0;
flags |= RUNNING;        /* 置位 */
if (flags & RUNNING) ... /* 测试 */

/* 布尔数组: 可读, 但占空间 */
bool states[8];
states[RUNNING] = true;
if (states[RUNNING]) ...
```

- HFT 协议头、设备寄存器用**位标志**(紧凑、对齐硬件)
- 应用层状态多用 `bool` 数组或枚举(可读、可调试)

### 比较的显式化

```c
/* 反例: 隐式布尔, 含义不清 */
if (strcmp(a, b))   /* 不等? 但读起来像"等" */

/* 正解: 显式 */
if (strcmp(a, b) != 0)   /* 明确: 不等 */
if (strcmp(a, b) == 0)   /* 明确: 相等 */
```

- `strcmp` 返回 0 表示相等,直接 `if (strcmp(...))` 易误读
- 显式 `== 0` / `!= 0` 让意图清晰

## 常见误区

| 误区 | 真相 |
|------|------|
| 关系运算结果是操作数原值 | 不,是 int 的 1 或 0 |
| `if (x = 1)` 和 `if (x == 1)` 等价 | 前者赋值恒真,后者判等 |
| `if (flags && MASK)` 测试位 | 错,逻辑与;要用 `&` |
| C 没有布尔类型 | C99 起有 `_Bool`/`bool` |
| `bool b = 5;` 后 b == 5 | 不,归一化为 1 |
| `if (strlen(s))` 清晰 | 合法但 `> 0` 更明确 |

## 实操

```bash
cat > t.c <<'EOF'
#include <stdio.h>
#include <stdbool.h>
#include <string.h>
int main(void){
  int a = 5;
  int r = (a > 3);              /* r = 1, 不是 5 */
  printf("r=%d\n", r);
  bool b = !!a;                  /* 归一化, b=1 */
  printf("b=%d\n", b);
  if (a = 1) printf("always runs (a=%d)\n", a);   /* 赋值恒真 */
  const char *s = "abc";
  if (strcmp(s, "abc")) printf("not equal (wrong!)\n");
  else printf("equal\n");
  return 0;
}
EOF
gcc -std=c11 -Wall -Wparentheses t.c -o t && ./t
```

## 自测要点(口述)

1. C 真假判定规则?哪些值是"假"?关系运算结果的类型和值是什么?
2. `if (x = 1)` 和 `if (x == 1)` 区别?哪个编译器警告?防御写法?
3. `if (flags && MASK)` 为什么错?测试位该用什么?
4. C99 的 `_Bool`/`bool` 有什么特点?`bool b = 5;` 后 b 是多少?
5. `!!x` 的作用?为什么用它?`strcmp` 返回值怎么用才清晰?

## 与后续衔接

- **5.1 操作符** 逻辑短路 `&&`/`||`;**ch04** if/while 条件
- **5.4.1 隐式转换** 无符号与负数比较的陷阱
- **《C 陷阱与缺陷》ch03** 语义陷阱

---

## 代码自测

**题目 1：** 以下代码在 C89 和 C99 中有什么区别？
```c
bool flag = true;
if (flag) { ... }
```

<details>
<summary>参考答案</summary>

C89 没有 bool/true/false 关键字——需要 #include <stdbool.h> 或自己 typedef。C99 引入了 _Bool 类型和 <stdbool.h> 中的 bool/true/false 宏。C 中的真是任何非零值，假是 0。if (flag) 和 if (flag != 0) 等效。

</details>
