# 1.1.5 rearrange 函数

## 本节讲什么

书中 `rearrange` 函数：对读入的列宽数据**重排**，综合运用 **if-else**、**for**、**while** 三种控制流。本章只是让语法"混个眼熟"，正式规则在 **ch04-statements**；运算符优先级陷阱在 **ch05** 和《C 陷阱与缺陷》。这节真正的价值是引入"程序的控制结构组织"这个工程视角。

## 控制流（本章入门，正式见 ch04）

### 三种基本结构

```c
/* 分支：if-else */
if (a[i] > a[j]) {
    int t = a[i]; a[i] = a[j]; a[j] = t;   /* 经典三变量交换 */
}

/* 循环：for —— 已知次数 */
for (i = 0; i < n; i++)
    /* ... */

/* 循环：while —— 条件驱动 */
while (i < n && scanf("%d", &a[i]) == 1)
    i++;
```

### 结构化编程的三种基本结构

任何可计算问题都能用三种控制结构表达——这是 **1966 年 Böhm-Jacopini 定理**的结论，也是结构化编程的基础：

| 结构 | 对应 C 语法 | 何时用 |
|------|------------|--------|
| **顺序** | 语句依次执行 | 默认 |
| **选择** | `if`/`else if`/`else`/`switch` | 根据条件走不同分支 |
| **迭代** | `for`/`while`/`do-while` | 重复执行直到条件不满足 |

> `goto` 不在这三种里——Dijkstra 1968 年《Go To Statement Considered Harmful》主张少用。但内核/异常处理/状态机里 `goto` 有合理用途，ch04.9 详谈。

### for vs while 的选择

```c
/* for：循环次数已知，三个子句（init/cond/step）一目了然 */
for (int i = 0; i < n; i++) { ... }

/* while：条件驱动，次数不定，如读到 EOF */
while (scanf("%d", &x) == 1) { ... }

/* do-while：至少执行一次 */
do { ... } while (cond);
```

工程上：**次数已知用 for，条件驱动用 while，至少一次用 do-while**。乱用 while 写已知次数循环，可读性变差。

## rearrange 在示例中的角色

```
┌─────────────────────┐
│ main                │  协调者：调用读入和重排
│  ├─ read_column_numbers  读入数据
│  └─ rearrange           重排数据
└─────────────────────┘
```

- `rearrange` 接收数组 + 长度（指针形式 ch06/ch08 再 formalize）
- 按题目要求调整列顺序（如升序），供后续格式化输出
- 体现工程原则：**main 协调，子函数单一职责**——每个函数只做一件事，便于测试和复用

## 工程视角：程序的控制结构组织

这一节表面讲语法，实际在示范**怎么组织程序的控制流**：

1. **入口校验**：函数开头先检查参数合法性（`n < 0`? `arr == NULL`?），早返回
2. **主循环明确**：一个循环做一件事，不要在循环里塞过多逻辑
3. **错误传播**：失败时返回错误码或特殊值，让调用者决定怎么办
4. **状态最小化**：循环里用的临时变量越少越好，越少越不容易出 bug

```c
/* 好的函数结构示意 */
int rearrange(int *arr, int n)
{
    if (arr == NULL || n <= 0)   /* 1. 入口校验 */
        return -1;

    for (int i = 0; i < n - 1; i++) {   /* 2. 主循环单一职责 */
        for (int j = i + 1; j < n; j++) {
            if (arr[j] < arr[i]) {     /* 3. 选择结构 */
                int t = arr[i];
                arr[i] = arr[j];
                arr[j] = t;
            }
        }
    }
    return 0;                          /* 4. 成功返回 0 */
}
```

> 这里用的是**选择排序**——简单但 O(n²)，不是高效算法。HFT 真正的排序需求走 `qsort`（ch16 标准库）或专用算法。本章只为练习控制流。

## 算法视角：为什么这节对 HFT 学习者是铺垫

- 现实 HFT 代码极少手写排序——`qsort`、`std::sort`、或针对固定模式的专用算法
- 但**控制流组织**是写一切代码的基础：状态机、解析器、事件循环都建立在 if/for/while 上
- 真正影响 HFT 性能的不是"用了哪种循环"，而是**数据布局、缓存局部性、分支预测**——这些在 ch05/ch08/ch18 深入

## 常见误区

| 误区 | 纠正 |
|------|------|
| `if (a = b)` 写成赋值 | `=` 赋值、`==` 比较；这是经典 bug，`-Wall -Wparentheses` 会警告 |
| `for (int i=0; i<=n; i++)` 多走一步 | 边界条件：`< n` 还是 `<= n` 要想清楚，差一是数组越界 |
| `while (i < n) ... ` 忘了 `i++` | 死循环，CPU 100%——调试时先看循环条件是否推进 |
| 三变量交换写错顺序 | `t=a; a=b; b=t;` 顺序不能乱，否则两个都变成同一个值 |
| 在循环里做能移到循环外的事 | 比如 `for (...) sum += arr[i] * magic_constant;`，常量每次重算——编译器一般会提到循环外，但别依赖 |

## 实操

1. 仿写一个循环求和函数，校验 `scanf` 返回值，处理 EOF
2. 故意把 `==` 写成 `=`，用 `-Wall -Wparentheses` 看警告
3. 故意写 `for (i=0; i<=n; i++)` 越界，看是否崩溃（不一定崩，UB）
4. 写一个 `while` 忘了 `i++`，观察 CPU 满载（用 `top`）
5. 把选择排序换成 `qsort`（ch16），对比代码量和可读性

## 自测要点

- 结构化编程的三种基本结构是什么？`goto` 为什么不在此列？
- for / while / do-while 各自适合什么场景？举一个 do-while 比 while 更合适的例子。
- `if (a = b)` 和 `if (a == b)` 有什么区别？怎么避免写错？
- 循环边界 `< n` 和 `<= n` 的差别是什么？什么时候用哪个？
- "main 协调、子函数单一职责"为什么是工程原则？违反了会有什么后果？

## 与后续衔接

- **ch04-statements**：语句与控制流的完整规则（空语句、表达式语句、代码块、if、while、for、do、switch、goto）
- **ch05-operators-and-expressions**：运算符优先级、求值顺序、`=` vs `==` 的陷阱
- **ch07-functions**：参数传递（值 vs 指针）、函数设计原则
- **ch08-arrays**：数组形参退化、数组与排序算法
- **ch16-standard-library**：`qsort`、`bsearch` 等标准算法
- **《C 陷阱与缺陷》ch03**：语句与控制流的典型陷阱
