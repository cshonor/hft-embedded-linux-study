# 1.1.4 read_column_numbers 函数

## 本节讲什么

书中示例 `read_column_numbers`：从**标准输入**循环读入一组整数（列宽），存入数组。这一节的核心价值不在于函数本身，而在于它**第一次在工程代码里使用 `&` 取地址** —— 全书指针章的前置铺垫。同时引入 `scanf` 返回值校验、栈上数组、缓冲等关键概念。

## 第一个工程级 I/O 模式

```c
#include <stdio.h>

int main(void)
{
    int val;
    if (scanf("%d", &val) == 1)
        printf("input = %d\n", val);
    else
        printf("invalid input\n");
    return 0;
}
```

这段代码浓缩了三个工程要点：

| 知识点 | 说明 |
|--------|------|
| **`&val`** | 取 `val` 的内存地址；`scanf` 需要**指针**才能把读到的值**写入** `val` 的内存 |
| **返回值校验** | `scanf` 返回成功赋值的**项数**；不校验就用结果 → UB（读未初始化变量） |
| **无 `&` 的后果** | `scanf("%d", val)` 把 `val` 的**值**当指针解引用 → 几乎必崩 |

## 为什么 scanf 必须要指针

C 的函数参数是**按值传递**——函数内部拿到的是副本，无法修改调用者的变量。要修改调用者的变量，必须传**地址**：

```c
void bad_set(int x)   { x = 42; }              /* 改的是副本，外面没变 */
void good_set(int *p) { *p = 42; }             /* 通过指针写回真实内存 */
```

`scanf("%d", &val)` 本质就是 `good_set` 模式：`scanf` 在 libc 内部不知道 `val` 的地址，必须由调用者用 `&` 告诉它。这是 C **指针存在意义的第一个体现**——**跨函数修改调用者的变量**。

> 这里埋的伏笔：`&` 取地址、`*` 解引用、指针作为"输出参数"——将在 **ch06** 正式展开。

## scanf 返回值：必须校验

```c
int n = scanf("%d %d", &a, &b);
```

| 输入 | 返回值 |
|------|--------|
| `"10 20"` | 2（两个都成功赋值） |
| `"10 abc"` | 1（第一个成功，第二个失败） |
| `"abc"` | 0（第一个就失败） |
| EOF（Ctrl-D） | `EOF`（-1） |

不校验返回值直接用 `a`、`b`，可能读到**未初始化变量**——C 不保证清零，是 UB。**所有 `scanf` 都要检查返回值**，这是工程铁律。

## 栈上数组与栈溢出

```c
int read_column_numbers(int arr[], int max)
{
    int n = 0;
    while (n < max && scanf("%d", &arr[n]) == 1)
        n++;
    return n;
}
```

- `arr` 是调用者传进来的（形参退化为指针，见 ch08），函数本身不分配数组内存
- 如果在 `read_column_numbers` 内部声明 `int arr[10000]`，那是**栈上分配**——栈空间有限（默认 8MB），大数组易溢出

### 栈 vs 堆 vs 全局：什么时候用什么

| 位置 | 何时用 | 风险 |
|------|--------|------|
| **栈**（局部自动变量） | 小、生命周期在函数内、确定大小 | 大数组 → 栈溢出；返回其地址 → 悬垂指针 |
| **堆**（`malloc`，ch11） | 大、生命周期跨函数、运行时才知大小 | 忘 free → 泄漏；free 后再用 → 悬垂 |
| **全局/静态** | 大缓冲、跨函数共享、整个进程生命周期 | 多线程并发访问要加锁；初始化时机晚 |

**HFT 常见踩坑**：在 `main` 里开 `char buf[1024*1024]`（1MB 局部数组），栈直接溢出 SIGSEGV。改用静态缓冲或 `malloc`，或用 `ulimit -s` 调大栈（治标不治本）。

## stdio 缓冲：HFT 热路径的延迟源

`scanf`/`printf` 走 **stdio 缓冲**：

- 终端是**行缓冲**（遇到 `\n` 才 flush）——交互时感觉"即时"
- 重定向到文件是**全缓冲**（缓冲满才 flush）——批量高效但有延迟
- HFT 热路径上 `printf` 的微秒级抖动来自这里：缓冲满 + syscall + 内核写 → 抖动

详见 **ch15**。热路径替代方案：直接 `write(2)`，或 lock-free ring + 后台落盘。

## read_column_numbers 的工程要点小结

- **循环 + 边界检查**：`n < max` 防止数组越界
- **循环 + 返回值检查**：`scanf == 1` 处理非法输入和 EOF
- **main 协调、子函数单一职责**：`read_column_numbers` 只负责读，不负责排序或输出
- **错误传播**：返回实际读到的个数 `n`，让调用者知道读了多少

## 常见误区

| 误区 | 纠正 |
|------|------|
| `scanf("%d", val)` 不加 `&` | 把 val 的值当地址解引用 → 段错误；`%d` 必须配 `int *` |
| 不检查 scanf 返回值 | 读到未初始化变量 → UB，可能"恰好正常"数月后崩溃 |
| `scanf("%d", &arr[i])` 会越界 | 只要 `i < max` 守好边界就不会；忘了边界就 UB |
| 在函数内开大局部数组 | 栈有限，大缓冲用 `malloc` 或静态 |
| `scanf` 失败后直接重试 | 失败的输入会留在 stdin 缓冲，要 `while (getchar()!='\n');` 清掉 |
| 指针是"高级特性" | 第一章就在用 `&`；指针是 C 的基本运作方式，不是高级 |

## 实操

1. **故意写 `scanf("%d", val)`（无 `&`）**，`gcc -g` 编译，gdb `run` 看崩溃，`bt` 看栈——这是第一次用 gdb 定位段错误
2. 输入字母 `abc`，观察 `scanf` 返回 0，走 else 分支
3. 输入 Ctrl-D（EOF），观察返回 `EOF`，与"非法输入"区分
4. 写一个循环读 N 个数，故意输入超过 N 个，看是否被边界挡住
5. 在函数内声明 `int huge[1000000]`，运行看 SIGSEGV——体验栈溢出

## 自测要点

- 为什么 `scanf` 的参数必须是**指针**？C 按值传递意味着什么？
- `scanf("%d %d", &a, &b)` 输入 `"10 abc"` 返回什么？`a` 和 `b` 分别是什么状态？
- 不校验 `scanf` 返回值直接用变量，为什么是 UB？
- 在函数内开 `int arr[10000]`，会有什么风险？应该怎么改？
- HFT 热路径为什么慎用 `printf`/`scanf`？底层是哪一层带来的延迟？

## 与后续衔接

- **ch06-pointers**：`*` 与 `&` 的完整体系，指针变量的内容、间接访问
- **ch08-arrays**：数组形参退化为指针、数组与指针的关系
- **ch11-dynamic-memory-allocation**：`malloc` 替代大栈数组
- **ch15-io-functions**：stdio 缓冲、`read`/`write` syscall 层 I/O
- **《C 陷阱与缺陷》ch03**：`scanf` 返回值、`&` 遗漏的典型陷阱
