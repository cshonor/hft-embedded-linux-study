# 6.8 指针和变量

## 本节讲什么

指针变量和普通变量一样：有存储、有内容、有地址、能被重新赋值。这一节把"指针自身也是变量"这个事实讲透——它是理解 `int *const`、二级指针、迭代器式遍历的前提。

## 要点

```c
int a = 1, b = 2;
int *p = &a;
p = &b;          /* 改指向，不影响 a（除非已通过 *p 写过） */
int **pp = &p;   /* pp 指向指针变量 p 本身 */
```

- `p` 的**内容**是某个对象的地址；改 `p` 只改"指向谁"，不改变原指向对象的值
- `p` 自身有地址：`&p`，类型 `int **`
- 指针可重新指向，但**指向 `const` 的指针**有约束（**6.9**）
- 迭代器式遍历：`for (p = arr; p < arr+n; p++)`（**6.13**），改 `p` 本身

### 三种"指针常量"组合（详见 **6.9**）

| 声明 | 指向对象 | 指针自身 |
|------|----------|----------|
| `int *p` | 可改 | 可改 |
| `const int *p` | 只读 | 可改 |
| `int *const p` | 可改 | 只读（初始化后不能改指向） |
| `const int *const p` | 只读 | 只读 |

`int *const p = &a;` 的含义：`p` 这个变量本身是 const，不能 `p = &b`，但可以 `*p = 1`。读法：**`const` 修饰其左边的 `*`，即修饰指针自身**。

## 底层映射

### 重新指向的内存视图

```
初始：p → a(=1), b(=2)
  p 的内容 = &a

p = &b;
  p 的内容 = &b  (a 不变，仍为 1；除非之前 *p 改过)

*pp = &b;   /* 等价于 p = &b，因为 *pp 即 p */
**pp = 5;   /* 等价于 *p = 5，即改 b */
```

```
              ┌───┐
   pp  ──→   │ p │ ──→  a 或 b
              └───┘
   pp 是"指向指针的指针"，*pp 是 p，**pp 是 p 指向的对象
```

### `const` 指针的编译检查

```c
int *const p = &a;
p  = &b;   /* 编译错误：p 是 const */
*p = 5;    /* OK：改的是所指对象 */
```

`const` 在**编译期**就拦下，没有运行时开销。常用于"函数内不让指针被改指向"的防御性编程：

```c
size_t sum(const int *arr, size_t n) {
    int *const end = (int *)arr + n;   /* end 锁定，不会被误改 */
    size_t s = 0;
    while (arr < (const int *)end) s += *arr++;
    return s;
}
```

`arr` 可改（迭代），`end` 锁死——读代码的人一眼看出 end 不变。

## HFT / DPDK

| 用法 | 例子 |
|------|------|
| 游标指针遍历 | `for (p = base; p < end; p++) decode(p);` |
| 锁定的尾指针 | `int *const end = base + n;` 防误改 |
| 可重指向的缓冲 | `rte_mbuf *m = ...; m = m->next;` |
| 多级管理 | `rte_mbuf **pp = &m;` 改 m 的指向 |

## 常见误区

| 误区 | 事实 |
|------|------|
| "`p = &b` 会改 a" | 只改 p 的内容，a 不变（除非已通过 `*p` 改过） |
| "`int *const p` 表示所指对象只读" | 错——只读的是 p 自身；要看 `const` 在 `*` 哪一侧（**6.9**） |
| "`*p` 和 `p` 是一回事" | `p` 是指针变量，`*p` 是它指向的对象 |
| "const 指针有运行时开销" | 没有，纯编译期检查 |

## 实操

```bash
# 看重新指向生成的 mov
gcc -O0 -g t.c -o t
gdb ./t -ex 'b 6' -ex r -ex 'p p' -ex 'p &p' -ex 'p *p'
# 看 const 指针的编译期拦截
cat > t.c << 'EOF'
int main(void) { int a=1; int *const p = &a; p = &a; return *p; }
EOF
gcc -c t.c       # 报错: assignment of read-only variable 'p'
```

## 与后续衔接

**6.9** const 四层与 restrict；**6.10** 二级指针改 p 自身；**6.13** 迭代器式 `p++` 遍历。

## 自测要点

1. `int a=1,b=2; int *p=&a; p=&b;` 之后 `a` 的值是什么？为什么？
2. `int *const p = &a;` 里 const 修饰的是 p 还是 `*p`？怎么读这个声明？
3. 重新指向 `p = &b` 生成什么汇编？有访存吗？
4. 为什么工程里常写 `int *const end = base + n;` 而不是普通 `int *end`？
5. `*pp = &b;` 和 `p = &b;`（其中 `pp = &p`）为什么等价？画图说明。

---

## 代码自测

**题目 1：** 以下代码合法吗？
```c
int x = 1;
int *p = &x;
int *q = &x;
*p = 10;
printf("%d\n", *q);
```

<details>
<summary>参考答案</summary>

合法。p 和 q 都指向 x，通过 *p = 10 修改了 x 的值，*q 读取同一个地址，输出 10。多个指针可以指向同一个变量——这是 C 中常见的情况。

</details>
