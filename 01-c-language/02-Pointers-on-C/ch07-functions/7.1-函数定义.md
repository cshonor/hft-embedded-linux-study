# 7.1 函数定义

## 本节讲什么

**函数定义** = 签名 + 函数体；产生 `.text` 段机器指令，外部链接符号全局唯一（`static` 除外）。函数是 C 工程的基本单元——DPDK API、内核驱动、HFT 策略模块都以函数为接口边界。这一节把"定义"落到 ABI、栈帧、链接三个层面。

## 要点

```c
int rte_mbuf_refcnt_add(struct rte_mbuf *m, int delta)
{
    m->refcnt += delta;
    return m->refcnt;
}
```

- 函数体 `{}` 是块作用域；局部 `auto` 变量在**栈帧**分配
- `static` 修饰函数 → **内部链接**，仅本 `.c` 可见（DPDK/内核私有 helper）
- 函数名在表达式中退化为**函数地址**（**ch13** 函数指针）

### return 与返回值

| 类型 | 规则 | ABI 返回位置 |
|------|------|-------------|
| 整型 ≤ 64 位 | `return expr;` | `rax` |
| `float`/`double` | `return expr;` | `xmm0` |
| 大 struct | 隐式拷贝，调用者传隐式首参数 | 调用者缓冲（**ch10**） |
| `void` | `return;` 或落到末尾 | 无 |
| 指针 | `return expr;` | `rax` |

返回值隐式转为声明类型；转换若溢出是 **UB**。

### 禁止：返回局部栈指针

```c
char *bad(void) {
    char buf[64] = "hello";
    return buf;   /* 悬垂指针：buf 随栈帧销毁 */
}
```

栈帧在 `ret` 时销毁，`buf` 地址被下一个函数复用。合法的返回指针来源：
- `malloc` 堆内存（调用者 `free`）
- `static` 缓冲（非线程安全）
- 调用者提供的缓冲区（最干净）

### inline（C99）

```c
static inline uint32_t get_flags(const struct rte_mbuf *m)
{
    return m->ol_flags;
}
```

- 建议编译期展开，减少 `call/ret` 开销
- 高频小函数放**头文件**（`static inline` 才能多处包含不冲突）
- 过大函数勿 inline：I-cache 膨胀、代码段增大
- DPDK/内核：`static inline` 是标配

## 底层映射

### call/ret 栈帧（**ch18** 详）

```
调用者                 被调函数
  ┌────────┐
  | call f | → 压返回地址 → ┌────────────┐
  |        |               | prologue:  |
  |        |               |   push rbp |
  |        |               |   sub rsp,N | 局部变量空间
  |        |               | ... 函数体 |
  |        |               | epilogue:  |
  |        |               |   add rsp,N |
  |        | ← ret 弹返回 ← |   pop rbp  |
  └────────┘               |   ret      |
                            └────────────┘
```

`call` = `push 返回地址; jmp f`；`ret` = `pop 返回地址; jmp 返回地址`。每层调用占一栈帧，深度过大 → 栈溢出 `SIGSEGV`（**7.5**）。

### ABI 寄存器传参与返回

x86-64 System V：前 6 个整数/指针参数 → `rdi, rsi, rdx, rcx, r8, r9`；浮点 → `xmm0..xmm7`；超出进栈。返回值：整数/指针 → `rax`；浮点 → `xmm0`。

```c
int add(int a, int b) { return a + b; }
```

```
; x86-64 -O2
add:
    lea     eax, [rdi + rsi]   ; a 在 rdi, b 在 rsi, 返回值 eax
    ret
```

参数不走内存，直接寄存器算——这是 `int` 等小标量传值几乎零开销的原因（**7.3**）。

## HFT / DPDK

| 用法 | 要点 |
|------|------|
| `static inline` helper | 高频小函数入头文件，避免 call 开销 |
| PMD 私有函数 | `static` 内部链接，不污染全局符号 |
| 返回错误码 | `int` 返回值走 `rax`，零开销 |
| 避免热路径返回大 struct | 改用输出指针参数 |

## 常见误区

| 误区 | 事实 |
|------|------|
| "返回局部数组没事" | 栈帧销毁即悬垂，UB |
| "inline 一定更快" | 大函数 inline 反伤 I-cache |
| "函数调用零开销" | 有 call/ret + 栈帧建立开销，深递归放大 |
| "static 函数和普通函数一样" | static 改链接属性，仅本 TU 可见 |

## 实操

```bash
# 看函数的 prologue/epilogue
gcc -O0 -S t.c -o - | grep -A5 '^add:'
# 对比 -O2（叶子函数常省略栈帧）
gcc -O2 -S t.c -o - | grep -A3 '^add:'
# 看符号表：static 函数是 local 符号
gcc -c t.c -o t.o && nm t.o | grep ' t '   # t = local text
```

## 与后续衔接

**7.2** 声明与原型；**7.3** 传参 ABI；**ch10** 返回大 struct 的隐式参数；**ch13** 函数指针；**ch18** 栈帧布局。

## 自测要点

1. 函数定义产生哪个段的机器指令？`static` 修饰函数改变什么？
2. `char *bad(void) { char buf[64]; return buf; }` 为什么是悬垂？三种合法替代？
3. `int add(int a, int b)` 在 x86-64 上参数走哪些寄存器？返回值在哪？
4. `call` 和 `ret` 在栈上分别做什么？栈帧由谁建立、谁销毁？
5. `static inline` 为什么是 DPDK/内核 helper 的标配？过大函数为何不 inline？
