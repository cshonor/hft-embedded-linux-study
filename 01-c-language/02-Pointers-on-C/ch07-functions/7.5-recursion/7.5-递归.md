# 7.5 递归

## 本节讲什么

函数**调用自身**；每层调用占一层**栈帧**。递归是树/链表遍历的天然写法，但 HFT 数据面**禁止深递归**——栈深度不可控、call/ret 开销、cache 不友好。这一节是总览，细节在 7.5.1（追踪）和 7.5.2（与迭代）。

## 要点

- 组成：**基线条件**（base case）+ 向基线推进的递归步
- 每层：局部变量、返回地址保存在栈上，占一栈帧
- 深度过大 → **栈溢出** `SIGSEGV`（栈区到栈/堆边界）
- 无基线或推进失败 → 无限递归 → 必爆栈

### 最小例子

```c
int sum(int n) {
    if (n <= 0) return 0;          /* 基线 */
    return n + sum(n - 1);          /* 推进 */
}
```

每层 `sum` 占一栈帧：`n`、返回地址、可能的对齐填充。`sum(10000)` = 10000 层栈帧。

### 栈帧叠加

```
sum(3) ──┐
         │  [n=3, ret=main]      ← 栈帧 1
sum(2) ──┐
         │  [n=2, ret=sum(3)]    ← 栈帧 2
sum(1) ──┐
         │  [n=1, ret=sum(2)]    ← 栈帧 3
sum(0) → return 0                  ← 基线，开始回退
```

回退时逐层 `ret` 销毁栈帧，返回值逐层累加。

### 适用 vs 不适用

| 适用 | 不适用（HFT 热路径） |
|------|---------------------|
| 内核树/目录遍历 | 报文收发主循环 |
| AST、配置树（深度有界） | 订单簿遍历 |
| 排序（教学，快排/归并） | 链表 walk（用迭代） |
| 非热路径工具代码 | 信号处理 |

HFT 数据面若需"栈式"处理，用**显式栈数组**模拟递归（**7.5.2**、**ch17**）。

## 底层映射

### 栈大小与溢出

```bash
$ ulimit -s        # 栈大小，单位 KB，典型 8192（8MB）
8192
```

每栈帧几十到几百字节，8MB 栈大约能撑几万到几十万层递归。深递归爆栈时：

```
sum(1000000) → 栈帧叠加超过 8MB → 踩到栈保护区 → SIGSEGV
```

栈保护区（guard page）让溢出确定性崩溃，而非静默踩堆。

### call/ret 开销（**7.1**）

每层递归 = 一次 `call` + 一次 `ret` + 栈帧建立/销毁。叶子函数优化（省栈帧）对递归有限——递归不是叶子。深度 N 的递归 = N 次 call/ret，远慢于一个循环。

## HFT / DPDK

| 场景 | 选择 |
|------|------|
| 报文解析 | 迭代，显式游标 |
| 链表遍历 | 迭代 `for (; p; p=p->next)` |
| 树（限深） | 递归可接受，或显式栈 |
| 配置加载（启动期） | 递归无所谓，不在热路径 |
| 策略回测 | 迭代优先，避免爆栈 |

## 常见误区

| 误区 | 事实 |
|------|------|
| "递归一定优雅" | 爆栈、慢、cache 差，热路径禁用 |
| "深递归最多慢点" | 会 `SIGSEGV`，进程崩 |
| "尾递归和迭代一样" | C 标准不保证 TCO，gcc 常做但不总能（**7.5.2**） |
| "栈大小无限" | `ulimit -s` 限制，默认 8MB |

## 实操

```bash
# 故意深递归看爆栈
cat > t.c << 'EOF'
int f(int n) { return n ? f(n-1) : 0; }
int main(void) { return f(100000000); }
EOF
gcc -O0 t.c -o t && ./t          # Segmentation fault
# ulimit 调栈大小
ulimit -s 65536 && ./t           # 撑更多层
# gdb 看递归深度
gdb ./t -ex r -ex bt | head -50  # 一长串 f 帧
```

## 小节

- [7.5.1 追踪递归函数](./7.5.1-追踪递归函数.md)
- [7.5.2 递归与迭代](./7.5.2-递归与迭代.md)

## 与后续衔接

**7.5.1** 追踪调用栈；**7.5.2** 迭代替代与尾递归；**ch12** 链表递归删除；**ch17** 显式栈 ADT 替代递归；**ch18** 栈布局与 guard page。

## 自测要点

1. 递归的两个必要组成是什么？缺一个会怎样？
2. 每层递归在栈上占什么？深度 N 的递归开销 vs 一个循环？
3. `sum(1000000)` 为什么崩？guard page 起什么作用？
4. HFT 数据面为什么禁止深递归？需要"栈式"处理时怎么办？
5. `ulimit -s` 默认多少？调大能解决深递归吗？根因解决靠什么？
