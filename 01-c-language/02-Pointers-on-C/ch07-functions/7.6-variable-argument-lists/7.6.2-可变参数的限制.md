# 7.6.2 可变参数的限制

## 本节讲什么

可变参的**类型安全、可移植性、默认参数提升**限制——《C 陷阱与缺陷》里库函数陷阱的集中地。这一节讲清三大限制，并给出 `__attribute__((format))` 让编译器帮你检查格式串的工程利器。

三个容易想当然、实测才准的点，本节用 gcc 13.3 实测钉死：

- **`va_arg(args, float)` 到底怎么个 UB？** 实测是**编译器插 `ud2` 当场 SIGILL**（退出码 132），不是"读个错值"。
- **`__attribute__((format(printf, 2, 3)))` 的 2 和 3 是什么？** 用在普通函数上是**索引越界、属性静默失效**；它真正对应的是 **C++ 非静态成员函数**（`this` 隐式占第 1 位）。
- **"没有元信息"到底会错成什么样？** 实测 `printf("%d %d", 1, 2.0)` 打出 `1 1068962088`——**不是值偏一点，是走了另一个寄存器区**；而且格式串类有 `-Wformat` 兜底，`open` 漏写 `mode` 这类**连一个警告都没有**（实测建出带 setuid 位的文件）。
- **`void sum(...)` 能自动知道传了几个吗？** 两层坑：① 这行声明在 C99/C11/C17 下**根本不合法**（`before C2X`），C2X 才解禁；② 即使 C2X 下合法，`sum(1,2)` 读出 `3` 也是**巧合**——污染寄存器后同一次调用得到 `303` / `603`。根因：变参调用点唯一多传的元信息是 `al`（**向量寄存器个数**），**不是参数个数**。

## 要点

### 限制 1：无类型检查

```c
void log_msg(const char *fmt, ...);
log_msg("%d", 3.14);    /* UB：期望 int 给了 double */
log_msg("%s", 42);      /* UB：期望 char* 给了 int */
log_msg("%d", &x);      /* UB：期望 int 给了指针 */
```

编译器**不检查**可变参与格式串的匹配——除非你加 `__attribute__((format))`（见下）。运行时取错类型 → 读错位置 → 垃圾值或崩。

> **注意检查范围的不对称性**：带 `...` 的原型**仍然检查固定参数**。实测 `-Wall -Wextra` 下
> `sum("not-an-int", 1, 2)` 会报 `-Wint-conversion`，而 `sum(2, 3.14, 9.9)` 的可变部分零警告。
> 也就是说"可变参不检查"只针对 `...` 覆盖的那一段（→ **7.6**）。

### 限制 0：至少一个固定参数（C23 起不再强制）

C17 及以前硬性要求 `...` 前必须有具名参数；**C23（N2975）解禁了 `void f(...)`
并引入 `va_start(ap, ...)` 新形式**（→ **7.6** 的实测矩阵）。这一条是限制也是语言演进的样本：
写需要跨 C17/C23 移植的代码时，保留固定参数仍是唯一稳妥选择。

### 限制 2：默认参数提升

可变参里实参做**默认参数提升**（**7.2.2** 同规则）：

| 实参类型 | 提升后 | va_arg 必须用 |
|---------|--------|--------------|
| `char` / `short` | `int` | `int` |
| `unsigned char` / `unsigned short` | `int`/`unsigned` | `int` 或 `unsigned` |
| `float` | `double` | **`double`** |

```c
void f(...);
f('a');          /* 实参提升为 int，va_arg(ap, int) */
f(3.14f);        /* 实参提升为 double，va_arg(ap, double) —— 不是 float！ */
```

`va_arg(ap, float)` 永远错——可变参里没有 `float`，都是 `double`。

#### 提升的物证：同一个 3.14，两种位模式

实测（小端，x86-64）：

```
  float  3.14f : 4 字节  c3 f5 48 40
  double 3.14  : 8 字节  1f 85 eb 51 b8 1e 09 40
```

**注意 double 3.14 不是"float 3.14 后面补 4 个零"**——浮点格式完全不同（指数的位数和偏移都不一样）。所以"提升"对浮点而言**不是零扩展，是重新编码**。这也解释了为什么拿 4 字节 float 当 8 字节 double 读，必然连吃后面 4 字节邻居。

#### 提升会让调用"合法"还是"UB"？看你怎么取

```c
print_all("ddf", 1, 2, 3.14f);
```

| 写法 | 判定 | 实测 |
|------|------|------|
| `va_arg(args, double)` 取 `3.14f` | ✅ **合法、定义明确** | 输出 `123.140000` |
| `va_arg(args, float)` 取 `3.14f` | ❌ **UB** | `ud2` → SIGILL，退出码 **132** |

关键点：**`3.14f` 本身不是问题，取法才是**。实参提升成 double 之后，用 `double` 取是完全正当的——这是 printf 里 `%f` 能同时吃 `float` 和 `double` 实参的原因。

### 限制 3：无法获知个数与类型

可变参不携带"有几个、什么类型"的元信息——`va_start` 之后你手里的 `va_list` 只是**几个游标字段**（x86-64 上是 `gp_offset` / `fp_offset` / `overflow_arg_area` / `reg_save_area`，见 **7.6.1** 的实测），**里面没有任何"还剩几个"的计数**。函数看到的确实只是一串原始字节 + 一个不知道终点在哪的游标。所以必须靠外部渠道告诉它：

- **格式串**（`printf` 的 `%d %s %f`）
- **显式 count + 约定**（`void log_n(int n, ...)` 约定每个都是 `int`）
- **哨兵**（`execl(path, arg0, arg1, ..., NULL)`）

```c
/* 更安全：固定类型数组 + count */
void log_events(int n, const struct evt *evs);   /* 同质批量 */
```

上面三条是"元信息外挂"的三种渠道。严格说还漏了两类同样常见的：

- **类型标签流**（tag list）：每个实参前面自带一个类型码，本质是"自描述 count"
- **元信息编码在固定参数里**：不额外传描述，而是让**固定参数本身的取值**决定可变段长什么样

#### 渠道 4：类型标签流（自描述）

```c
enum { T_INT, T_STR, T_DBL, T_END };
void emit(int tag, ...);          /* 用法：emit(T_INT, 1, T_STR, "a", T_DBL, 2.5, T_END) */

va_list ap; va_start(ap, tag);
for (int t = tag; t != T_END; t = va_arg(ap, int)) {
    switch (t) {
    case T_INT: printf("%d", va_arg(ap, int));    break;
    case T_STR: printf("%s", va_arg(ap, char *)); break;
    case T_DBL: printf("%f", va_arg(ap, double)); break;
    }
}
va_end(ap);
```

把"有几个"升级成"每个是什么"，代价是每个实参多传一个 tag（8 字节）和一次 switch。它和格式串是同一思路的两端：格式串是**紧凑的字符串编码**，tag list 是**展开的二进制编码**。

#### 渠道 5：固定参数自带元信息（内核最爱的做法）

这一类最容易被忽略，但它是 POSIX / 内核里出现频率最高的：

| 例子 | 固定参数 | 如何描述可变段 |
|------|---------|---------------|
| `open(path, flags, ...)` | `flags` | **仅当 `flags` 含 `O_CREAT`（某些实现含 `O_TMPFILE`）时，才存在第 3 个 `mode` 参数** |
| `ioctl(fd, cmd, arg)` | `cmd` | `cmd` 的**位域**里编码了方向（`_IOC_DIR`）和**实参大小**（`_IOC_SIZE`） |
| `fcntl(fd, cmd, ...)` | `cmd` | 不同 `cmd` 决定第 3 参是 `int`、指针还是 struct |
| `printf(fmt, ...)` | `fmt` | 格式串 = 元信息（渠道 1，归入同一族） |

`ioctl` 的 cmd 是"自描述"的教科书样本——实测（gcc 13.3，WSL）：

```c
#include <linux/ioctl.h>
struct big   { char b[64]; };
struct small { int x; };
#define CMD_BIG   _IOWR(0x6d, 3, struct big)
#define CMD_SMALL _IOWR(0x6d, 4, struct small)
/* CMD_BIG   = 0xc0406d03  dir=3 size=64
   CMD_SMALL = 0xc0046d04  dir=3 size=4   */
```

`size` 被 `_IOC` 宏直接编进 `cmd` 的 30 位里，内核侧 `copy_from_user` 用 `_IOC_SIZE(cmd)` 就知道拷多少字节——**类型信息从来没走可变参，它藏在固定参数的位里**。这也是为什么内核里 `ioctl` 的第三个参数看起来"什么都能传"。

> **👉 这条线索后面会反复遇到**：TLPI 的 `fcntl` / `ioctl`、内核 `struct file_operations::unlocked_ioctl` 都建立在这个"cmd 自描述"约定上。

#### 底层：ABI 层面为什么元信息必然缺失

x86-64 SysV 下实参分布在**两个互不相干的区域**（→ **7.6.1** 实测过 `gp_offset` / `fp_offset`）：

```
寄存器保存区：  [ gp 槽 0..5 各 8B ][ fp 槽 0..7 各 16B ]   栈溢出区：超出的实参
va_arg(ap,int)    → 走 gp 区，gp_offset += 8
va_arg(ap,double) → 走 fp 区，fp_offset += 16   ← 两个游标互不干扰
```

槽位里**只有值，没有 tag 字段**。所以 `va_arg` 完全按你给的类型"结账"：**类型说错 = 走错区**，不是"值偏了一点"，而是读到另一个实参甚至残留垃圾。实测（`-O0`，gcc 13.3）：

```c
printf("A: %d %d\n", 1, 2.0);        /* A: 1 1068962088 */
printf("B: %d %d\n", 1, 2.0, 3);     /* B: 1 3          */
printf("C: %d %d %d\n", 1, 2.0, 3);  /* C: 1 3 0        */
```

逐个拆：

| 调用 | 现象 | 原因 |
|------|------|------|
| A | 第二个 `%d` 打出 **1068962088** | `2.0` 进了 fp 区，gp 区第二个槽**没人写**，`va_arg(ap,int)` 读的是寄存器残留垃圾 |
| B | 打出 **3** | 第二个 `%d` 从 gp 区取，正好取到实参 `3`——**double 被彻底跳过**，后面所有实参"前移一格" |
| C | 第三个 `%d` 打出 **0** | gp 区第三个槽同样没人写，读残留 |

**这就是元信息缺失最狠的地方：错位会接力。** 一个类型不匹配，后续所有实参的解读全部错位一路到底，而且没有 EOF 标记，`va_arg` 会一直读到栈上去。

#### 调用点汇编：被调方能拿到的唯一元信息是 `al`

会有人问："编译器明明知道我传了几个，为什么不能顺手塞个个数进去？"——**它没塞**。看 `-O0` 下 4 个调用点的实测汇编（gcc 13.3）：

```asm
# fixed3(1, 2, 3)                  定参：个数编译期已知
mov $0x3,%edx
mov $0x2,%esi
mov $0x1,%edi
call fixed3                  ← 没有任何"共 3 个"的信息

# fixed3(1, 2, 0)                  第 3 个是 0 也得老老实实写
mov $0x0,%edx                ← 编译器不会替你省

# varf(3, 1, 2, 3)                 变参：edi=3 是源码里自己写的 count
mov $0x3,%ecx
mov $0x2,%edx
mov $0x1,%esi
mov $0x3,%edi                ← 这个 3 是第一个实参 n，不是编译器生成的
mov $0x0,%eax                ← al = 0
call varf

# varf(2, 1, 2)
mov $0x2,%edx
mov $0x1,%esi
mov $0x2,%edi                ← edi=2 同样是源码里那个 n
mov $0x0,%eax
call varf
```

两件事值得记住：

1. **变参调用点唯一多出来的元信息是 `mov $0x0,%eax`**——也就是 `al` 寄存器。x86-64 SysV 规定：变参调用时 `al` = **实际用到的向量寄存器（SSE）个数**。这是给被调方的**优化提示**（决定要不要把 8 个 xmm 全存进保存区），**不是参数个数**。
2. `varf(3,1,2,3)` 里那个 `3` 是**你自己写的第一个实参**。写 `sum(1,2,3)`（不传 count）时这个位置就没有值，被调方于是一个字节的元信息都拿不到。

> 这也从根上解释了为什么 `printf` 必须有格式串：除了 `%` 占位符，**ABI 里没有任何字段写着"这次传了几个"**。

#### 小思考题：`void sum(...)` 能自动知道传了 3 个 int 吗？

```c
int sum(...);
sum(1, 2, 3);
```

**答案：不能。而且这道题有两层坑。**

**第一层：这行声明在 C99/C11/C17 下根本不合法。**

```
$ gcc -std=c11 -pedantic-errors -c t.c
error: ISO C requires a named argument before '...' before C2X [-Wpedantic]
```

`c99` / `c17` 同样报错（措辞里那句 **before C2X** 就是标准自己给出的时间线），**只有 `-std=c2x` 通过**——C23 的 N2975 解禁了无固定参数形式（详见 **7.6.1**）。不加 `-pedantic-errors` 时 gcc 会当扩展默默接受，所以你可能从没见过这个报错；但换个编译器或在 CI 里开 `-pedantic-errors`，它就是硬错误。

**第二层：即使 C2X 下合法，函数内部也拿不到个数。**

C2X 配套允许了 `va_start(ap, )`（省略 last 参数），但**没有配套任何"查询个数"的接口**。所以只能硬编码读几次：

```c
int sum(...)
{
    va_list ap; va_start(ap, );
    int t = 0;
    for (int i = 0; i < 3; i++)          /* ← 这个 3 只能写死 */
        t += va_arg(ap, int);
    va_end(ap);
    return t;
}
```

实测（gcc 13.3，`-std=c2x`）：

| 调用 | `-O0` | 说明 |
|------|-------|------|
| `sum(1,2,3)` | **6** | 唯一正确的用法 |
| `sum(1,2)` | **3** | 看着像"自动知道没有第 3 个"——**纯属巧合** |
| `sum(1,2,3,4)` | **6** | 第 4 个 `4` 被丢掉，零警告 |

**`sum(1,2)` 那个 `3` 是假的。** 让它现形：先在旁边调一个函数把第 3 个参数寄存器（`edx`）污染掉，再调 `sum(1,2)`：

```
A) poison3(100,200,777)    之后： sum(1,2) = 303   ← 1 + 2 + 300（残留）
B) poison4(100,200,300,555) 之后： sum(1,2) = 603  ← 1 + 2 + 600（残留）
   -O2 下同一份代码：        4 和 3                  ← 换优化级别，残留也跟着变
```

多读出来的那个"实参"是**上一个函数调用留下的寄存器残留**，值随上下文和优化级别变化。第一次测出 `3` 只是因为那个时刻 `edx` 恰好是 0——巧合到像设计过一样，而这正是它危险的地方：**错的代码在最简单的测试里表现正常**。

> **限制 3 的完整说法**：不是"元信息不好拿"，而是 **ABI 里压根没有这个字段**。`va_arg` 是一个完全靠你报数记账的游标——多报一个就往后多读一格垃圾，少报一个就悄悄丢掉尾部实参，**两种方向编译器都一声不吭**。

#### 三类失败模式汇总

| 元信息错在哪 | 后果 | 能否被编译器发现 |
|-------------|------|-----------------|
| **个数**（count 说谎 / 哨兵漏写 / 格式串转换符多） | 越界 `va_arg` → 读栈上返回地址、金丝雀、上一个栈帧 | 仅格式串类可被 `-Wformat` 发现 |
| **类型**（`%d` 给了 `double`、`float` 当 `double` 取） | 走错 gp/fp 区 → 垃圾值 + 后续连锁错位 | 同上；裸 `va_arg` 完全不管 |
| **生命周期**（`va_arg` 出来的指针指向 caller 的临时对象） | 悬垂指针 | 不能 |

#### 哨兵不是语言机制，只是约定 + 编译器善意

`execl` 要求以 `NULL` 结尾，**这不是 C 语言的规定，是 POSIX 的约定**。gcc 会帮你查，但查的方式很绕——实测：

```c
#include <unistd.h>
execl("/bin/echo", "echo", "hi");    /* 故意漏 NULL */

$ gcc -Wall -Wextra -c t.c
warning: missing sentinel in function call [-Wformat=]      ← 报了
$ gcc -Wall -Wextra -fno-builtin -c t.c
（零警告）                                                   ← 关掉 builtin 就没了
```

**`-fno-builtin` 一关警告就消失**，说明这份知识来自 **gcc 编译器内建**，不是头文件：`/usr/include/unistd.h` 里 `execl` 的声明只有 `__THROW __nonnull((1,2))`，`sentinel` 这个词在 unistd.h 里出现 **0 次**。

自定义函数默认**完全没人管**——想让 gcc 帮你查，得自己声明：

```c
void exec_like(const char *p, ...) __attribute__((sentinel));
/* exec_like("a","b","c");   → warning: missing sentinel in function call
   exec_like("a","b",NULL);  → 干净                                      */
```

> ⚠️ 注意 sentinel 检查挂在 **`-Wformat`** 下（不是自己的 `-Wsentinel`），且只是 warning。CI 里要兜底就加 `-Werror=format`。

#### 哨兵的两个实测后果

**① 中间出现 NULL 会被当成"到此结束"。** 哨兵的本质是"拿某个值当终止符"，那业务数据里就不能出现这个值：

```c
static void run_all(const char *first, ...)
{
    va_list ap; va_start(ap, first);
    int n = 0;
    for (const char *s = first; s != NULL; s = va_arg(ap, const char *))
        fprintf(stderr, "    arg[%d] = %s\n", n++, s);
    va_end(ap);
    fprintf(stderr, "    total = %d\n", n);
}
```

实测（gcc 13.3，`-O0` 与 `-O2` 结果一致）：

```
[case1] run_all("ls", "-l", "-a", NULL)
    arg[0] = ls   arg[1] = -l   arg[2] = -a      total = 3     ← 正常

[case2] run_all("ls", NULL, "-a", NULL)
    arg[0] = ls                                  total = 1     ← "-a" 整段丢失
```

**零警告、退出码 0，`-a` 就这么没了。** 这正是"哨兵不能当业务数据用"的具体代价。选型判据很简单：**你的数据集合里允不允许出现那个终止值？**

| 场景 | 能否用哨兵 | 原因 |
|------|-----------|------|
| `execl` 的 argv 字符串列表 | ✅ | argv 元素本身不会是 NULL |
| fd 列表用 `-1` 当哨兵 | ✅ | `-1` 不是合法 fd |
| 价格/数量列表用 `0` 当哨兵 | ❌ | 0 是合法业务值 |
| 指针列表用 `NULL` 当哨兵，但允许"空槽" | ❌ | 空槽会被当成终止 |
| 长度可变的二进制数据 | ❌ | 任意字节都可能出现，用 count |

**② 漏写哨兵不一定崩，可能只是多读一个。** 实测 `run_all("ls", "-l")`（故意不写末尾 NULL）：

```
[case3] 完全忘记写末尾 NULL：
    arg[0] = ls
    arg[1] = -l
    total = 2        ← 停下来了，exit=0
```

它停住是因为第 3 次 `va_arg` 恰好读到一个 0。**这绝不意味着安全**——换个调用上下文残留值就非 0，循环会继续往下扫，拿到的野指针被当字符串解引用就是段错误。用污染寄存器的方式逼它现形：漏写哨兵时读到 **3** 个才停（正确是 2 个），多出来的那个是残留值，**不崩溃、退出码 0、静默多处理一个野指针**。

> 两个方案的失败形状不同：count **说大**了 → 多读垃圾，**说小**了 → 丢尾部实参；哨兵**漏写** ≡ count 说大了。三种情况都没有任何诊断。

#### 反面实测：`open` 漏写 mode 是零警告 + 真实权限漏洞

`open` 是 `int open(const char *, int, ...)`——`O_CREAT` 时必须有第三个参数。实测漏写：

```c
int fd = open("/tmp/va_probe", O_CREAT | O_WRONLY);   /* 故意漏 mode */
```

- **编译期：零警告**（`fcntl.h` 里 `open` 只有 `__nonnull((1))`，gcc 也没有为它内建 sentinel/格式知识）
- **运行期：用垃圾值当 mode 建了文件** → `ls -l` 显示 `--ws--x---`

`--ws--x---` 里的 `s` 是 **setuid 位**被置上了。漏写 `mode` 不是"权限随机"这么轻，而是**可能造出一个带 setuid 的文件**——这是历史上真实出现过的一类漏洞。也正因如此，现代代码应该用 `open(path, flags, mode)` 三参全写，或改用 `openat` + 显式 `mode`。

> **一句话**：格式串类有编译器兜底（`-Wformat`），count 和哨兵类**只在你自己加属性之后才有兜底**，而 `open` 这类连属性都没人给你加。

## 底层映射

### `__attribute__((format))` 让编译器检查格式串

```c
void log_msg(const char *fmt, ...)
    __attribute__((format(printf, 1, 2)));
/*           ↑              ↑     ↑
         printf 语义   fmt 是第 1 参  可变参从第 2 参开始 */
```

声明后，编译器会检查格式串与可变参类型匹配：

```c
log_msg("%d", 3.14);    /* -Wformat: expected 'int' but arg is 'double' */
log_msg("%s", 42);      /* -Wformat: expected 'char *' but arg is 'int' */
log_msg("%d %d", 1);    /* -Wformat: too few arguments */
log_msg("%d", 1, 2);    /* -Wformat: too many arguments */
```

> 实测（`-Wall -Wextra`）：`log_msg("%d", 3.14)` 报
> `warning: format '%d' expects argument of type 'int', but argument 2 has type 'double' [-Wformat=]`

### Quiz：`format(printf, 2, 3)` 里的 2 和 3 是什么？

```c
void test(const char *fmt, ...) __attribute__((format(printf, 2, 3)));
```

**如果这个 `test` 是普通函数（上面这种写法），那 2 和 3 是错的。** 实测：

```
warning: 'format' attribute argument 2 value '2' exceeds the number of function parameters 1 [-Wattributes]
```

它只有 **1 个具名参数**（`fmt`），你却说格式串在第 2 位——**索引越界，gcc 直接把这条属性丢掉**，而且**不会检查 `test` 的任何调用点**（实测 `test("%d", 3.14)` 零报警）。

> ⚠️ 这是最阴险的失效方式：**属性写错不会让编译失败，只会让检查静默消失**。你以为有保护，其实一点都没有。

#### 那 2 和 3 什么时候才对？

两种场景：

**① C++ 非静态成员函数**——`this` 隐式占第 1 位，所以 `fmt` 是 2、`...` 是 3：

```cpp
struct Log {
    void good(const char *fmt, ...) __attribute__((format(printf, 2, 3)));  /* ✅ */
    void bad (const char *fmt, ...) __attribute__((format(printf, 1, 2)));  /* ❌ */
};
```

实测（g++ 13.3）：

| 写法 | 结果 |
|------|------|
| `format(printf, 2, 3)` | ✅ 生效，`L.good("%d", 3.14)` 报 `argument 3 has type 'double'` |
| `format(printf, 1, 2)` | ❌ **编译错误**：`'format' attribute argument 2 value '1' refers to parameter type 'Log*'` |

gcc 的报错直接说出了答案——**第 1 个参数的类型是 `Log*`，也就是 `this`**。

**② 有两个（及以上）固定参数的函数**——格式串不在第 1 位：

```c
void two(int lvl, const char *fmt, ...) __attribute__((format(printf, 2, 3)));  /* ✅ */
/*                                                                    ↑   ↑
                                                             fmt 是第 2 参  ... 从第 3 参开始 */
```

实测：`two(1, "%d", 3.14)` 报 `argument 3 has type 'double'` ✅

#### 四种场景对照（全部实测）

| 场景 | 正确写法 | 写错会怎样 |
|------|---------|-----------|
| 普通函数 `f(fmt, ...)` | `format(printf, 1, 2)` | 写 `2,3` → `-Wattributes` 越界警告 + **检查静默失效** |
| 两个固定参 `f(a, fmt, ...)` | `format(printf, 2, 3)` | 写 `1,2` → 把 `a` 当格式串，误报一堆 |
| C++ 成员函数 `C::f(fmt, ...)` | `format(printf, 2, 3)`（`this`=1） | 写 `1,2` → **编译错误**，提示参数类型是 `Log*` |
| C++ **static** 成员函数 | `format(printf, 1, 2)`（无 `this`） | 实测生效，报 `argument 2` |

> **记忆法**：这两个数字是**从 1 开始的位置编号，不是偏移量**。先数清楚"格式串前面站了几个参数"——普通函数 0 个（fmt 就是 1），成员函数多一个 `this`，前置参数各自 +1。

```bash
# 开启格式串检查（强烈建议 CI 必开）
gcc -Wformat -Wformat-security -Werror=format t.c
# 跨平台：__attribute__ 是 GCC/Clang 扩展，MSVC 用 _Analysis_assume_
# 排查"属性写了却没生效"：单独看 -Wattributes，它是唯一会告诉你索引越界的开关
gcc -Wall -Wextra -Wattributes t.c
```

### 格式串安全漏洞

`printf(user_input)` 是**格式串漏洞**——攻击者控制 `%n`（写内存）、`%x`（泄漏栈）：

```c
printf(buf);            /* 危险：若 buf 含 %n 可改内存 */
printf("%s", buf);      /* 安全：buf 是数据不是格式 */
```

#### 实测：`%x` 真的把栈读出来了

```
user_input = "%x %x %x %x"
  危险写法 printf(buf)      :  596045c0 0 0 25      ← 栈上的原始值，每次运行都不同
  安全写法 printf("%s", buf):  %x %x %x %x          ← 原样打印，只是普通字符
```

同一份输入，两种写法，一个把栈内容吐出来、一个老实当字符串。这就是**"数据"和"代码"混淆**的经典形态——`printf` 的第一个参数不是数据，是一段**被解释的程序**。

#### 实测：`%n` 是**写入**，不是打印

```c
int written = 0;
printf("hello%n world\n", &written);
printf("written = %d\n", written);   /* written = 5 */
```

`%n` 把"到目前为止已输出的字符数"写进指针参数。`hello` 是 5 个字符 → `written == 5`。**它不打印任何东西，它是往内存里写。** 攻击者控制格式串时，就能让这个"写入"落到可控地址上。

> 关于防护：实测在 `-O2 -D_FORTIFY_SOURCE=2` 下，**字面量格式串的 `%n` 仍然照写不误**（`w=5`，退出码 0）。不要以为开了 `_FORTIFY_SOURCE` 就万事大吉——真正的防线是**永远不让外部输入当格式串**。

#### 编译器能拦多少

| 开关 | 实测效果 |
|------|---------|
| `-Wformat-security` | `warning: format not a string literal and no format arguments` |
| `-Werror=format-security` | **变成 error，退出码 1**，CI 直接挂 |

`-Wformat-security` 拦的是"格式串不是字面量且没有格式参数"这一类——**它拦得住 `printf(buf)`，拦不住 `printf(buf, x)`**（有额外实参时 gcc 认为你"可能是故意的"）。所以这关必须开，但不能只靠它。

## HFT / DPDK 工程规范

| 推荐 | 避免 |
|------|------|
| 固定签名 API | 数据面核心路径可变参 |
| `rte_log(level, fmt, ...)` 仅日志 | 解析/交易用 `...` |
| `__attribute__((format))` 配 `-Wformat` | 裸 `...` 无检查 |
| `snprintf(buf, n, "%s", input)` | `sprintf(buf, input)` 溢出+格式漏洞 |
| 同质批量用数组+count | 异质杂糅用 `...` |

### 实测：每次调用到底差多少

2 亿次调用，用 `volatile` 函数指针防内联（`-O2`，gcc 13.3）：

| 调用形式 | ns/次 | 相对定参 |
|---------|-------|---------|
| 定参 `fixed(1,2,3)` | **2.125** | 1.0× |
| 数组 + count `arr(v,3)` | **5.209** | 2.45× |
| 可变参 `va(3,1,2,3)` | **9.053** | **4.26×** |

**1 ns 在 HFT 里不是小数**：行情回调里一个订单对象走 20 次 getter 就是 20 ns，已经吃掉一整个 tick-to-trade 预算的相当一部分。

#### ⚠️ 但"压栈开销"这个说法不准确

常见解释是"可变参要压栈所以慢"——**恰恰相反**：x86-64 上前 6 个整数参数**本来就在寄存器里，根本不压栈**。可变参慢在两处（→ **7.6.1** 实测）：

1. **prologue 无条件建寄存器保存区**——`subq $240` + 5 条 `movq` + `testb %al` + 8 条 `movaps`，约 14 条 store，**哪怕一个可变参都不读也照付**
2. **`va_arg` 每次都要判断** `gp_offset` 有没有越界、要不要切到 `overflow_arg_area`——这是运行时分支，不是编译期算好的偏移

所以准确的结论是：**可变参的代价是"把编译期能定的事推到了运行期"**，而不是"多压了几个栈"。这个区别在优化时很关键——减少参数个数救不了你，因为 prologue 开销与参数个数无关。

### DPDK RTE_LOG 的做法

```c
#define RTE_LOG(l, t, ...) \
    rte_log(RTE_LOG_##l, RTE_LOGTYPE_##t, ##__VA_ARGS__)

RTE_LOG(INFO, "pkt=%p len=%zu\n", pkt, len);
```

`rte_log` 内部用 vfprintf，宏展开转 `__VA_ARGS__`（**ch14** 详）。带 `format` 属性，编译期检查。

## 常见误区

| 误区 | 事实 |
|------|------|
| "`va_arg(ap, float)` 只是读错值" | gcc 实测插 `ud2` → **SIGILL，退出码 132**。但这是编译器善意，标准仍只说 UB |
| "`format(printf, 2, 3)` 是"两参函数"专用" | 也对（两个固定参时），但**典型用途是 C++ 成员函数**（`this` 占 1）。用在普通函数上不报错，只**静默失效** |
| "属性写错了编译会失败" | 不会——只有 `-Wattributes` 一条警告，检查悄悄消失 |
| "开了 `_FORTIFY_SOURCE` 就不怕 `%n`" | 实测字面量格式串的 `%n` 照写（`w=5`，退出码 0） |
| "可变参慢是因为要压栈" | 恰恰相反：x86-64 前 6 个参数在寄存器里**不压栈**。慢在 prologue 无条件建保存区 + `va_arg` 运行时分支 |
| "可变参开销随参数个数增长" | 不随：`subq $240` 与个数无关，一个可变参都不读也照付 |
| "`va_arg(ap, float)` 取 float" | 错，可变参 float 提升为 double |
| "可变参类型编译器会检查" | 不检查，除非 `__attribute__((format))` |
| "`printf(buf)` 没事" | 格式串漏洞，`%n` 可写内存、`%x` 泄栈（实测读出 `596045c0 0 0 25`） |
| "可变参能传任意类型" | 能传但取回要匹配，否则 UB |
| "`char`/`short` 在可变参里保持原类型" | 提升为 int |
| "类型不匹配就是读错一个值" | 实测 `printf("%d %d",1,2.0)` → `1 1068962088`：**走错 gp/fp 区**，且后续实参**连锁错位**（`1 2.0 3` 打 `1 3 0`） |
| "`execl` 的哨兵是 C 语言规定" | 是 POSIX 约定。`unistd.h` 里 `sentinel` 出现 0 次，gcc 的检查来自**编译器 builtin**——`-fno-builtin` 一开警告就没 |
| "自定义哨兵函数漏 NULL 编译器会提醒" | 不会，得自己加 `__attribute__((sentinel))`（检查还挂在 `-Wformat` 下） |
| "漏写 `open` 的 mode 顶多权限随机" | 实测零警告，建出 `--ws--x---` —— **setuid 位可能被置上**，是真实漏洞类型 |
| "可变参的元信息只能靠格式串 / count / 哨兵" | 还有**类型标签流**（tag list）和**编码进固定参数**（`ioctl` 的 `_IOC_SIZE` 实测 = 64） |

## 实操

```bash
# 开格式串检查为 error
gcc -Wformat -Wformat-security -Wformat-y2k -Werror=format t.c
# 故意传错类型看告警
cat > t.c << 'EOF'
#include <stdio.h>
int main(void) { printf("%d\n", 3.14); return 0; }
EOF
gcc -Wformat t.c -o t            # warning: format '%d' expects 'int' but arg is 'double'
# 格式串漏洞演示（不要在生产代码做）
cat > vuln.c << 'EOF'
#include <stdio.h>
int main(int argc, char **argv) { printf(argv[1]); return 0; }  /* 危险 */
EOF
gcc -Wformat-security vuln.c    # warning: format not a string literal
gcc -Werror=format-security vuln.c   # 变 error，退出码 1

# ⭐ 查"属性写了却没生效"：只有 -Wattributes 会告诉你索引越界
cat > attr.c << 'EOF'
void quiz(const char *fmt, ...) __attribute__((format(printf, 2, 3)));
void quiz(const char *fmt, ...) { (void)fmt; }
int main(void) { quiz("%d", 3.14); return 0; }   /* 零报警 = 属性已失效 */
EOF
gcc -Wall -Wextra -Wattributes attr.c
#   warning: 'format' attribute argument 2 value '2' exceeds the number of function parameters 1

# ⭐ C++ 成员函数：gcc 的报错直接说出 this 占第 1 位
cat > attr.cpp << 'EOF'
struct Log {
    void good(const char *fmt, ...) __attribute__((format(printf, 2, 3)));
    void bad(const char *fmt, ...) __attribute__((format(printf, 1, 2)));
};
void Log::good(const char *fmt, ...) { (void)fmt; }
void Log::bad(const char *fmt, ...) { (void)fmt; }
int main() { Log L; L.good("%d", 3.14); L.bad("%d", 3.14); return 0; }
EOF
g++ -Wall attr.cpp
#   error: 'format' attribute argument 2 value '1' refers to parameter type 'Log*'
#   warning: format '%d' expects 'int' but argument 3 has 'double'   ← good 生效了

# ⭐ %n 是写入不是打印（现代 glibc 下仍可用）
printf '#include <stdio.h>\nint main(void){int w=0;printf("hello%%n\\n",&w);printf("w=%%d\\n",w);return 0;}\n' > n.c
gcc -O2 -D_FORTIFY_SOURCE=2 n.c -o n && ./n     # w=5，退出码 0

# ⭐ 元信息缺失 = 走错寄存器区（gp 区 vs fp 区），不是"值偏一点"
cat > zone.c << 'EOF'
#include <stdio.h>
int main(void) {
    printf("A: %d %d\n", 1, 2.0);       /* A: 1 1068962088  ← gp 槽没写，读残留 */
    printf("B: %d %d\n", 1, 2.0, 3);    /* B: 1 3           ← double 被跳过，实参前移 */
    printf("C: %d %d %d\n", 1, 2.0, 3); /* C: 1 3 0         ← 连锁错位 */
    return 0;
}
EOF
gcc -O0 zone.c -o zone && ./zone

# ⭐ ioctl cmd 把 size 编进固定参数（自描述元信息）
cat > ioc.c << 'EOF'
#include <stdio.h>
#include <linux/ioctl.h>
struct big   { char b[64]; };
struct small { int x; };
#define CMD_BIG   _IOWR(0x6d, 3, struct big)
#define CMD_SMALL _IOWR(0x6d, 4, struct small)
int main(void) {
    printf("BIG=0x%08lx size=%lu  SMALL=0x%08lx size=%lu\n",
        (unsigned long)CMD_BIG,   (unsigned long)_IOC_SIZE(CMD_BIG),
        (unsigned long)CMD_SMALL, (unsigned long)_IOC_SIZE(CMD_SMALL));
    return 0;
}
EOF
gcc ioc.c -o ioc && ./ioc     # BIG=0xc0406d03 size=64  SMALL=0xc0046d04 size=4

# ⭐ 哨兵检查来自 gcc builtin，不是头文件
cat > sent.c << 'EOF'
#include <unistd.h>
int main(void) { execl("/bin/echo", "echo", "hi"); return 0; }   /* 漏 NULL */
EOF
gcc -Wall -Wextra -c sent.c -o /dev/null
#   warning: missing sentinel in function call [-Wformat=]
gcc -Wall -Wextra -fno-builtin -c sent.c -o /dev/null     # 零警告
grep -c sentinel /usr/include/unistd.h                    # 0  ← 头文件里根本没有

# ⭐ 自定义哨兵函数：不加属性零警告，加了才查
cat > sent2.c << 'EOF'
void mine(const char *a, ...);                                  /* 无属性 */
void guarded(const char *a, ...) __attribute__((sentinel));     /* 有属性 */
int main(void) { mine("a","b"); guarded("a","b"); return 0; }
EOF
gcc -Wall -Wextra -c sent2.c -o /dev/null
#   只有 guarded 那一行报 missing sentinel

# ⭐ open 漏 mode：零警告 + 垃圾 mode（实测建出带 setuid 位的文件）
cat > mode.c << 'EOF'
#include <fcntl.h>
#include <stdio.h>
int main(void) { printf("fd=%d\n", open("/tmp/probe", O_CREAT|O_WRONLY)); return 0; }
EOF
gcc -Wall -Wextra mode.c -o mode 2>&1 | head -3      # 零警告
./mode && ls -l /tmp/probe                            # --ws--x---  ← s = setuid
```

## 与后续衔接

**ch15** printf 族完整；**ch14** `__VA_ARGS__` 宏与可变参；GCC `__printf__` 属性；**7.6.1** stdarg 五宏。

⭐ **`format` 属性只是 `__attribute__` 家族的一员**——其余高频属性（`noreturn` / `used` / `pure` / `const` / `noinline` / `packed` / `aligned` / `section` / `constructor`）见 **18.10 GCC 属性总览**，其中：

- `format` 的兄弟坑：属性值 `m,n` 只对**当前函数签名**成立，`my_log(int prio, const char *fmt, ...)` 用 `2,3` 是对的，单参数 `test(const char *fmt, ...)` 沿用就失效（本节已测）
- 谎称属性的代价有两类：编译期报错（`always_inline`）与**静默出错**（`pure`/`const` 副作用被合并、`noreturn` 实测 SIGSEGV 139）

## 自测要点

1. 可变参的三大限制是什么？各自带来什么后果？
2. `va_arg(ap, float)` 为什么永远错？正确写法是什么？
3. `__attribute__((format(printf, 1, 2)))` 的两个数字分别表示什么？能拦哪类错误？
4. `printf(user_input)` 为什么危险？`%n` 能做什么？
5. 为什么 HFT 数据面核心路径禁可变参？批量数据应该用什么替代？
6. `format(printf, 2, 3)` 用在**普通函数**上会怎样？gcc 给什么诊断？为什么这比编译失败更危险？
7. C++ 非静态成员函数为什么要用 `2, 3`？gcc 报错里怎么说的？`static` 成员函数呢？
8. "元信息外挂"除了格式串 / count / 哨兵，还有哪两类？`ioctl` 的 `cmd` 属于哪一类，怎么自描述大小？
9. `printf("%d %d", 1, 2.0)` 实测打什么？为什么不是"读错一点"而是"走错区"？后续实参会怎样？
10. `execl` 漏写 `NULL`，gcc 的警告从哪来？怎么实测证明它不来自头文件？
11. `open(path, O_CREAT)` 漏写 `mode` 会怎样？为什么说它比"权限随机"更严重？
12. `void sum(...)` 在 C99/C11/C17 下合法吗？C2X 呢？即使合法，函数内部能不能知道传了几个？
13. 变参调用点唯一多传给被调方的元信息是什么寄存器？它表示的是"参数个数"吗？
14. 哨兵方案的两个实测后果是什么？什么场景下不能用哨兵？漏写哨兵一定崩吗？
8. 实测里可变参比定参慢约 4 倍，原因是什么？为什么说"因为要压栈"是错的？
9. `va_arg(args, float)` 在 gcc 上的可观测后果是什么（退出码几）？这跟"标准说 UB"是什么关系？

---

## 代码自测

**题目 1：** 以下可变参数调用会发生什么？
```c
void print_all(const char *fmt, ...) {
    va_list args;
    va_start(args, fmt);
    while (*fmt) {
        if (*fmt == 'd') printf("%d", va_arg(args, int));
        if (*fmt == 'f') printf("%f", va_arg(args, double));
        fmt++;
    }
    va_end(args);
}
// 调用
print_all("ddf", 1, 2, 3.14f);
```

<details>
<summary>参考答案</summary>

**实测输出：`123.140000`。本调用合法、行为定义明确。**

拆解：

1. `3.14f` 是 `float`，但作为可变参传入时触发**默认参数提升** → 变成 `double`
2. `va_arg(args, double)` 取的类型 == 提升后的类型 → **匹配，无 UB**
3. `"ddf"` 前两个 `d` 各取一个 `int`（1 和 2），第三个 `f` 取 `double`（3.14）
4. `printf("%f")` 本来就期待 `double` → 输出 `3.140000`

位模式证明了提升不是"补零"：

```
  float  3.14f : 4 字节  c3 f5 48 40
  double 3.14  : 8 字节  1f 85 eb 51 b8 1e 09 40
```

推广规律：**`va_arg` 的 type 必须写"提升之后"的类型**——`char`/`short`/`_Bool` → `int`，`float` → `double`。

</details>

**题目 2：** 把上题的 `'f'` 分支改成 `va_arg(args, float)`，会发生什么？请给出可观测后果，不要只说"UB"。

```c
if (*fmt == 'f') printf("%f", va_arg(args, float));   /* ★ 改这一行 */
```

<details>
<summary>参考答案</summary>

实测（gcc 13.3，x86-64）：

```
warning: 'float' is promoted to 'double' when passed through '...'
note: (so you should pass 'double' not 'float' to 'va_arg')
note: if this code is reached, the program will abort
```

- 反汇编里 `print_bad` 的**第 70 条指令是 `ud2`**（未定义指令）
- 运行：**`Illegal instruction (core dumped)`，退出码 132**（128+SIGILL）

所以可观测后果是**当场 SIGILL 崩溃**，不是"读到一个错值"。

⚠️ 但要分清两层：

| 层面 | 说法 |
|------|------|
| C 标准 | UB——没规定编译器该怎么做 |
| gcc/Clang 实测 | 插 `ud2` 主动 abort（**善意陷阱**，省得你查三小时） |

**别把 gcc 的善意当成保证。** 换个编译器（或 `va_arg` 类型不是"提升类"错误，比如传 `int` 却用 `double` 读），就是真的静默读垃圾——那种情况**零警告、退出码 0**（→ **7.6.1**）。

另注：`printf("%f", ...)` 本身期待 `double`，所以即使 `va_arg` 返回了 float，它也会再被提升成 double 传进去——**错误发生在 `va_arg` 那一步，与 printf 无关**。

</details>

**题目 3：** 下面这个声明有什么问题？2 和 3 到底代表什么？

```c
void test(const char *fmt, ...) __attribute__((format(printf, 2, 3)));
```

<details>
<summary>参考答案</summary>

**问题：索引越界，属性静默失效。**

`test` 只有 1 个具名参数 `fmt`，却说格式串在第 2 位。实测 gcc 13.3：

```
warning: 'format' attribute argument 2 value '2' exceeds the number of function parameters 1 [-Wattributes]
```

后果不是"报错"，而是**属性被丢掉，所有调用点都不再检查**——实测 `test("%d", 3.14)` 零报警。**你以为有保护，其实没有。**

正确写法取决于场景：

| 场景 | 正确写法 |
|------|---------|
| 普通函数 `test(fmt, ...)` | `format(printf, 1, 2)` |
| 两个固定参 `test(a, fmt, ...)` | `format(printf, 2, 3)` |
| C++ 成员函数 `C::test(fmt, ...)` | `format(printf, 2, 3)`——**`this` 隐式占第 1 位** |
| C++ `static` 成员函数 | `format(printf, 1, 2)`——无 `this` |

gcc 对成员函数写 `1,2` 的报错直接点破了原因：

```
error: 'format' attribute argument 2 value '1' refers to parameter type 'Log*'
```

第 1 个参数的类型是 `Log*`，就是 `this`。

**记忆法**：两个数字是**从 1 开始的位置编号，不是偏移量**。数清楚"格式串前面站了几个参数"。而排查"属性写了却没生效"，唯一线索是 **`-Wattributes`**。

</details>

<details>
<summary>题目 4（小思考题）：<code>int sum(...); sum(1,2,3);</code> —— 函数内部能不能自动知道"传了 3 个 int"？为什么？</summary>

**参考答案：不能。分两层回答。**

**第一层：这行声明在 C99/C11/C17 下根本不合法。**

```
$ gcc -std=c11 -pedantic-errors -c t.c
error: ISO C requires a named argument before '...' before C2X [-Wpedantic]
```

`c99`/`c17` 同样报错，**只有 `-std=c2x` 通过**（C23 N2975 解禁无固定参数形式）。不加 `-pedantic-errors` 时 gcc 当扩展默默接受，所以很多人没见过这条报错——但它在 CI 里就是硬错误。

注意报错措辞自带时间线：**before C2X**。

**第二层：即使 C2X 下合法，也拿不到个数。**

`va_start(ap, )` 允许省略 last 参数，但没有任何配套接口能查询"还剩几个"。你只能硬编码读几次：

```c
int sum(...)
{
    va_list ap; va_start(ap, );
    int t = 0;
    for (int i = 0; i < 3; i++)
        t += va_arg(ap, int);       /* 这个 3 只能写死 */
    va_end(ap);
    return t;
}
```

实测（gcc 13.3，`-std=c2x`）：

| 调用 | 结果 | 说明 |
|------|------|------|
| `sum(1,2,3)` | 6 | 唯一正确用法 |
| `sum(1,2)` | **3** | 看着像"自动知道没有第 3 个"——**巧合** |
| `sum(1,2,3,4)` | **6** | 第 4 个被丢掉，零警告 |

**为什么 `sum(1,2)` 那个 3 不能信？** 把第 3 个参数寄存器（`edx`）先用另一个函数污染掉：

```
poison3(100,200,777)      之后： sum(1,2) = 303   ← 1 + 2 + 300（残留）
poison4(100,200,300,555)  之后： sum(1,2) = 603   ← 1 + 2 + 600（残留）
-O2 下同一份代码：          4 和 3                  ← 换优化级别，残留也变
```

多读出来的那个"实参"是**上一个函数调用留下的寄存器残留**。第一次测出 `3` 只是因为那一刻 `edx` 恰好是 0。

**第三层（根因）**：调用点汇编里，变参调用唯一多传的一条元信息是 `mov $0x0,%eax`（即 `al`），它表示**用到的向量寄存器个数**——是给被调方的优化提示，**不是参数个数**。`varf(3,1,2,3)` 里 `edi=3` 是源码自己写的第一个实参 n，编译器没有额外生成任何"还有 N 个"的标记。

**结论**：C 的 `...` 从 ABI 到语言都没有"个数"字段。`va_arg` 完全靠你报数记账——多报一个读垃圾，少报一个丢实参，两个方向都无诊断。

**正确写法**（三选一）：

```c
int sum_n(int n, ...);                        /* 显式 count */
int sum_arr(int n, const int *a);             /* 数组 + count（工程首选） */
#define sum3(a,b,c) sum_n(3,(a),(b),(c))      /* 或者干脆用宏固定个数 */
```

</details>
