# 8.1.5 数组和指针

## 本节讲什么

数组与指针的**区别**：数组是对象；指针是保存地址的变量。

## 要点

| | `int a[10]` | `int *p` |
|---|-------------|----------|
| `sizeof` | 40（通常） | 8 |
| 赋值 `a = ...` | 非法 | `p = a` 合法 |
| 生命周期 | 块/静态 | 指针本身在栈 |

- **`int a[]`** 仅在参数列表中等价指针（见 8.1.6）

## 与后续衔接

**8.3** 指针数组；勿混淆。
