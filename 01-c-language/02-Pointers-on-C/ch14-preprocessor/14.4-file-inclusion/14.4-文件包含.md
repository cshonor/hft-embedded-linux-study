# 14.4 文件包含

## 本节讲什么

`#include` 把头文件**文本嵌入**当前翻译单元，是预处理器第一阶段的核心操作。这一节给出 `#include` 的展开流程、两种形式（`<...>` 系统头 vs `"..."` 本地头）的搜索规则、嵌套深度的工程控制，以及 HFT 工程里头文件依赖管理的实践。

认知锚点：`#include` 不是「导入模块」，而是**纯文本拼接**——预处理器把头文件内容原样插入到 `#include` 处，然后递归展开。理解这一点，才能理解 include guard 为什么必要、为什么循环包含会无限递归、为什么头文件互相 include 会让编译变慢。

## 要点

### 预处理展开流程

```
源文件 a.c:
  #include "b.h"
  #include <stdio.h>
  int main(void) { ... }

预处理步骤:
  1. 读取 a.c
  2. 遇到 #include "b.h"，找到 b.h，把内容插入 a.c 的 #include 位置
  3. 递归展开 b.h 里的 #include
  4. 遇到 #include <stdio.h>，从系统路径找到 stdio.h，插入
  5. 递归展开 stdio.h 里的 #include
  6. 所有 #include 处理完后，得到完整的翻译单元（.i 文件）
  7. 编译器接管，分析这个展开后的 .i 文件
```

**`#include` 是递归的**——头文件里的 `#include` 也会被展开，直到所有依赖都拼入。`gcc -E a.c -o a.i` 可以查看展开后的结果，是排查包含问题的第一手段。

### 两种形式

```c
#include <stdio.h>          /* 系统头：从系统路径搜索 */
#include "my_header.h"      /* 本地头：先搜当前目录，再搜系统路径 */
#include "../common/util.h" /* 本地头：相对路径 */
#include "/opt/dpdk/include/rte_mbuf.h"  /* 绝对路径（不推荐） */
```

| 形式 | 搜索顺序 | 典型用途 |
|------|----------|----------|
| `<...>` | 系统标准路径 + `-I` 指定的路径 | libc、内核 UAPI、第三方库 |
| `"..."` | 当前文件所在目录 → `-I` 路径 → 系统路径 | 工程私有头文件 |

```bash
# 查看系统 include 路径
gcc -E -Wp,-v - < /dev/null

# 追加搜索路径
gcc -I/opt/dpdk/include -I./src/include ...
```

`<...>` 和 `"..."` 的搜索顺序由实现定义，但 POSIX 规定 `"..."` 必须先搜当前目录。某些编译器（MSVC）行为略有不同。

### Include Guard（强制）

```c
/* quote_proto.h */
#ifndef QUOTE_PROTO_H
#define QUOTE_PROTO_H

/* 头文件内容 */
struct quote_msg {
    uint32_t type;
    uint64_t seq;
    /* ... */
};

#endif /* QUOTE_PROTO_H */
```

**include guard 防止重复包含**：

1. 第一次包含：`QUOTE_PROTO_H` 未定义，进入 `#ifndef`，定义 `QUOTE_PROTO_H`，处理内容。
2. 第二次包含：`QUOTE_PROTO_H` 已定义，跳过整个 `#ifndef` 块。

**没有 guard 会怎样**：

```c
/* 没有 guard 的 bad.h */
struct foo { int x; };

/* a.c */
#include "bad.h"
#include "bad.h"   /* struct foo 重复定义，编译错误 */
```

C 标准不允许同一翻译单元内重复定义结构体。include guard 是唯一可靠的防护手段。

#### Guard 命名规范

```c
#ifndef MODULE_PATH_FILE_H
#define MODULE_PATH_FILE_H
/* ... */
#endif
```

- **唯一性**：guard 名必须全局唯一，否则不同头文件互相屏蔽。用「项目_模块_文件」前缀保证唯一。
- **全大写**：与宏命名一致。
- **末尾注释**：`#endif /* QUOTE_PROTO_H */` 标注配对的 guard 名，可读性。

### `#pragma once`

```c
#pragma once

/* 头文件内容 */
struct foo { int x; };
```

`#pragma once` 是编译器扩展（GCC/Clang/MSVC 全部支持），告诉编译器「本文件只包含一次」。相比 include guard：

| 维度 | include guard | `#pragma once` |
|------|---------------|-----------------|
| 标准 | C 标准 | 编译器扩展（主流全支持） |
| 性能 | 预处理器仍要打开文件读 `#ifndef` | 编译器基于文件路径跳过，更快 |
| 可靠性 | guard 名冲突会出错 | 基于文件路径，无冲突 |
| 可移植性 | 完全可移植 | 主流编译器支持，部分嵌入式不支持 |
| 跨符号链接 | 同一文件不同路径会包含两次 | 基于路径，可能漏包含 |

**DPDK/内核用 include guard**（可移植性优先），**现代项目用 `#pragma once`**（简洁高效）。混用也可，但通常选其一。

### 嵌套深度控制

```c
/* a.h -> b.h -> c.h -> d.h ... 深度递增 */
```

过度嵌套的 include 会导致：

1. **编译变慢**：每个 `.c` 文件展开后可能包含数万行头文件代码。
2. **依赖复杂**：改一个头文件触发大量重编译。
3. **循环包含**：A 包含 B，B 包含 A，无限递归（guard 阻断但导致内容不可见）。

#### 前置声明减少依赖

```c
/* bad: 包含完整定义 */
#include "order.h"
#include "quote.h"

struct order_engine {
    struct order *orders;     /* 只需要指针 */
    struct quote *quotes;     /* 只需要指针 */
};

/* good: 前置声明 */
struct order;    /* 前置声明，不需要完整定义 */
struct quote;

struct order_engine {
    struct order *orders;     /* 指针只需前置声明 */
    struct quote *quotes;
};
```

**指针和引用只需前置声明**——不需要知道结构体大小和布局。前置声明把依赖从「需要完整定义」降级为「只需名字」，大幅减少 include 传播。

### PIMPL 模式

```c
/* order_engine.h */
struct order_engine;   /* 不透明指针，实现细节隐藏 */

struct order_engine *order_engine_create(void);
void order_engine_destroy(struct order_engine *e);
int order_engine_submit(struct order_engine *e, const struct order *o);

/* order_engine.c */
#include "order_engine.h"
#include "order.h"
#include "quote.h"

struct order_engine {   /* 完整定义只在 .c 里 */
    struct order *orders;
    struct quote *quotes;
};
```

**PIMPL（Pointer to IMPLementation）**：头文件只暴露不透明指针 `struct order_engine *`，完整定义在 `.c` 文件里。用户代码只 `#include "order_engine.h"`，不需要 `order.h`/`quote.h`。依赖完全隔离，改内部结构不触发用户重编译。详见 ch10 结构体。

### 头文件的职责

```c
/* good: 头文件只放声明 */
/* quote_proto.h */
#ifndef QUOTE_PROTO_H
#define QUOTE_PROTO_H

#include <stdint.h>     /* 依赖的标准头 */

struct quote_msg {      /* 结构体定义（需要完整定义） */
    uint32_t type;
    uint64_t seq;
};

/* 函数声明 */
int quote_parse(const uint8_t *buf, size_t len, struct quote_msg *out);

#endif

/* bad: 头文件放实现 */
/* bad.h */
static inline int helper(int x) {  /* static inline 可以，但大段实现不该在头文件 */
    /* 大段实现 */
}
```

头文件职责：

- **类型定义**：结构体、枚举、typedef。
- **函数声明**：原型，不含实现。
- **宏定义**：常量、标志位、内联小函数。
- **`static inline` 函数**：小工具函数，可以放头文件。
- **extern 声明**：全局变量声明（定义在 `.c`）。

**头文件不放**：函数实现（非 inline）、变量定义、`static` 变量定义。

## HFT 关联

### DPDK 头文件结构

```c
/* rte_mbuf.h */
#ifndef _RTE_MBUF_H_
#define _RTE_MBUF_H_

#include <stdint.h>
#include <rte_common.h>
#include <rte_mempool.h>

struct rte_mbuf { /* ... */ };
static inline void rte_pktmbuf_free(struct rte_mbuf *m) { /* ... */ }

#endif /* _RTE_MBUF_H_ */
```

DPDK 头文件全部用 include guard，依赖 `<rte_*.h>` 系列头文件。`#include <rte_mbuf.h>` 从 `-I/opt/dpdk/include` 搜索。

### 内核 UAPI 头文件

```c
#include <linux/if_ether.h>   /* 内核对用户态稳定接口 */
#include <linux/ip.h>
#include <netinet/in.h>       /* libc 的网络头 */
```

内核 UAPI（Userspace API）头文件在 `/usr/include/linux/`，提供对用户态稳定的结构定义。HFT 报文解析直接用这些头，不需要手写协议结构。

### 减少编译依赖

```c
/* strategy_engine.h */
struct strategy;        /* 前置声明 */
struct order;
struct quote;

struct strategy_engine {
    struct strategy **strategies;   /* 指针数组，只需前置声明 */
    int count;
};

/* 只在 .c 里包含完整定义 */
/* strategy_engine.c */
#include "strategy_engine.h"
#include "strategy.h"
#include "order.h"
#include "quote.h"
```

HFT 项目头文件依赖复杂，前置声明 + PIMPL 是控制编译时间的标准手段。改一个内部结构不触发所有文件重编译。

### 头文件的 include 顺序

```c
/* 推荐顺序 */
#include "对应本 .c 的头文件"   /* 1. 自身头文件，验证自包含 */
#include <标准库头>              /* 2. C 标准库 */
#include <系统头>                /* 3. POSIX/系统 */
#include <第三方库头>            /* 4. DPDK/内核 */
#include "工程内其他头"          /* 5. 工程内部 */
```

**自身头文件第一**：验证头文件自包含（不依赖 `.c` 里其他 include）。Google C++ Style 推荐此顺序，HFT 工程同样适用。

### 编译时间优化

```bash
# 预编译头（PCH）
gcc -x c-header common.h -o common.h.gch
gcc -include common.h -c foo.c -o foo.o   # 复用 PCH，加速编译

# ccache 缓存编译结果
ccache gcc -c foo.c -o foo.o

# 并行编译
make -j$(nproc)
```

HFT 大型项目编译时间可能数十分钟，预编译头 + ccache + 并行编译是标准优化手段。

## UB 边界

| 情形 | 后果 | 防护 |
|------|------|------|
| 无 include guard 重复包含 | 重复定义编译错误 | 强制 include guard 或 `#pragma once` |
| 循环包含 A→B→A | guard 阻断但内容不可见 | 前置声明打破循环 |
| 头文件包含实现 | 多文件包含导致重复定义 | 实现放 `.c`，头文件只声明 |
| `static` 变量在头文件 | 每个 `.c` 包含后各自一份 | 改用 `extern` 声明 + `.c` 定义 |
| guard 名冲突 | 不同头文件互相屏蔽 | 用「项目_模块_文件」前缀 |
| 绝对路径 include | 不可移植 | 用 `-I` + 相对路径 |

### 循环包含的陷阱

```c
/* a.h */
#include "b.h"
struct a { struct b *bp; };

/* b.h */
#include "a.h"
struct b { struct a *ap; };
```

`a.c` 包含 `a.h` → 展开 `b.h` → `b.h` 又 include `a.h` → guard 阻断（`A_H` 已定义）→ `b.h` 继续，`struct a` 未定义 → `struct b { struct a *ap; }` 编译错误。

**解决方案**：前置声明替代 include。

```c
/* a.h */
struct b;            /* 前置声明 */
struct a { struct b *bp; };

/* b.h */
struct a;            /* 前置声明 */
struct b { struct a *ap; };
```

指针只需前置声明，不需要完整定义，打破循环。

## 与后续衔接

- **14.4.1 函数库文件**：`<...>` 系统头的搜索机制深入。
- **14.4.2 本地文件**：`"..."` 本地头与 include guard 实战。
- **14.3 条件编译**：include guard 是条件编译的标准应用。
- **ch10 结构体**：PIMPL 模式与不透明指针。
- **ch17 经典 ADT**：头文件与实现的分离规范。

## 口述自测题

1. `#include "x.h"` 和 `#include <x.h>` 的搜索顺序有什么不同？**POSIX 规定 `"..."` 必须先搜哪里**？`-I` 路径在两者中分别什么时候搜？
2. include guard 为什么必须？**没有 guard 时重复包含 `struct foo { int x; };` 会发生什么**？guard 名冲突会导致什么问题？
3. `#pragma once` 相比 include guard 有什么优势？**基于文件路径跳过**为什么比 guard 更快？它的可靠性问题是什么？
4. 头文件里只放声明不放实现，为什么？**`static inline` 函数为什么可以放头文件**？普通函数实现放头文件会导致什么问题？
5. 前置声明如何打破循环包含？**`struct a; struct b { struct a *ap; };` 为什么不需要 `a.h` 的完整定义**？指针为什么只需前置声明？
