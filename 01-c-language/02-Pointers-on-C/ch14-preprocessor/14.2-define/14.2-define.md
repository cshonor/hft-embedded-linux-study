# 14.2 #define

## 本节讲什么

`#define` 是预处理器的核心指令，它在编译第一阶段做**纯文本 token 替换**——无类型、无作用域检查、无求值。这一节给出预处理的完整流程、两大宏类型（对象宏与函数宏）的对比，以及 `#define` 在 C 工程里的定位。

认知锚点：`#define` 不是 C 语言的语法，而是**预处理器的元指令**。预处理器在编译器之前运行，把 `.c` 文件变换成另一份 `.c` 文件（翻译单元），编译器再处理变换后的结果。理解这条管线，才能理解宏为什么「无类型」——预处理器根本不懂 C 类型，它只认 token。

## 要点

### 预处理流程

C 标准定义的翻译阶段共 8 个，前 4 个属于预处理器：

```
源文件 (.c)
   |
   | 1. 三字符替换、行续接（\ 换行）
   | 2. token 化、注释替换为空格
   | 3. #include 嵌入头文件（递归）
   | 4. #define 宏展开、#if/#ifdef 条件裁剪
   v
翻译单元 (.i，纯 C 代码)
   |
   | 5-8. 编译器接管：语法分析、代码生成
   v
目标文件 (.o)
```

预处理器的输出可以用 `gcc -E foo.c -o foo.i` 查看，是排查宏展开问题的第一手段。`#include` 在第 3 步把头文件**文本嵌入**当前文件，`#define` 在第 4 步做**文本替换**——两者都是纯文本操作，不理解 C 语义。

### 两大宏类型

| 类型 | 语法 | 示例 | 用途 |
|------|------|------|------|
| **对象宏** | `#define NAME 替换体` | `#define BURST_SIZE 32` | 常量、标志位、配置 |
| **函数宏** | `#define NAME(params) 替换体` | `#define MIN(x,y) ((x)<(y)?(x):(y))` | 内联计算、日志、符号操作 |

```c
/* 对象宏：无参数，纯文本替换 */
#define BURST_SIZE 32
#define PKT_FLAG_ORDER (1U << 2)
#define APP_NAME "hft-engine"

/* 函数宏：带参数，参数也是文本替换 */
#define MIN(x, y)     ((x) < (y) ? (x) : (y))
#define MAX_PKT_NUM   64
#define LOG(fmt, ...) fprintf(stderr, fmt, ##__VA_ARGS__)
```

### 宏展开的规则

1. **递归展开**：宏体里若引用其他宏，继续展开。
2. **不递归自引用**：`#define FOO FOO` 不会无限展开，`FOO` 保持原样。
3. **字符串内不展开**：`"BURST_SIZE"` 里的 `BURST_SIZE` 是字符串内容，不替换。
4. **字符常量内不展开**：`'BURST_SIZE'` 同理。
5. **参数替换后再展开**：函数宏的参数先替换到宏体，再对整体展开。

```c
#define A 10
#define B A
#define C B

int x = C;   /* C → B → A → 10 */

#define FOO FOO + 1
int y = FOO;  /* FOO → FOO + 1，不递归自引用 */
```

### 对象宏的典型用途

```c
/* 配置常量 */
#define MAX_ORDERS        100000
#define ORDERBOOK_DEPTH   10
#define HEARTBEAT_NS      1000000000ULL

/* 标志位掩码 */
#define FLAG_BID    (1U << 0)
#define FLAG_ASK    (1U << 1)
#define FLAG_CANCEL (1U << 2)

/* 字符串常量 */
#define PROTO_VERSION "1.3"
#define LOG_PREFIX    "[hft] "
```

对象宏在编译期替换为字面量，**零运行开销**，比 `const` 变量更早可用（预处理阶段）。但 `const` 变量有类型、有作用域、可调试——两者选型见 14.2.2。

### 函数宏的典型用途

```c
/* 内联计算 */
#define MIN(x, y)  ((x) < (y) ? (x) : (y))
#define MAX(x, y)  ((x) > (y) ? (x) : (y))
#define ABS(x)     ((x) < 0 ? -(x) : (x))

/* 日志 */
#define LOG(level, fmt, ...) \
    rte_log(level, RTE_LOGTYPE_APP, fmt, ##__VA_ARGS__)

/* 符号拼接 */
#define PKT_TYPE(name) PKT_##name

/* 断言 */
#define ASSERT(cond) do { \
    if (!(cond)) { \
        fprintf(stderr, "assert failed: %s at %s:%d\n", \
                #cond, __FILE__, __LINE__); \
        abort(); \
    } \
} while (0)
```

函数宏的深入用法（`#`/`##`/`__VA_ARGS__`）见 14.2.3。

### 宏的作用域

```c
#define DEBUG 1

void f(void) {
    /* DEBUG 在此可见 */
#ifdef DEBUG
    log_debug("entering f");
#endif
}

#undef DEBUG   /* 取消定义，后续不可见 */

void g(void) {
    /* DEBUG 在此不可见 */
#ifdef DEBUG
    log_debug("entering g");   /* 不编译 */
#endif
}
```

宏的作用域从 `#define` 到 `#undef` 或文件末，**无 C 作用域概念**——函数边界、块边界对宏无效。这是宏「污染全局命名空间」的根源。

## HFT 关联

### DPDK 标志位

```c
/* rte_mbuf.h 中的标志位定义 */
#define PKT_RX_RSS_HASH     (1ULL << 0)
#define PKT_RX_FDIR         (1ULL << 1)
#define PKT_RX_L4_CKSUM_BAD (1ULL << 3)
#define PKT_RX_IP_CKSUM_BAD (1ULL << 4)
#define PKT_TX_IP_CKSUM     (1ULL << 54)
#define PKT_TX_L4_CKSUM     (1ULL << 55)

/* 使用 */
if (mbuf->ol_flags & PKT_RX_L4_CKSUM_BAD)
    stats_inc(STATS_CKSUM_ERR);
```

DPDK 用对象宏定义 64 位标志位，编译期替换为位掩码。**为什么用宏不用 `const`**？因为标志位要用于预处理条件（`#if PKT_RX_RSS_HASH`）和静态初始化（`uint64_t mask = PKT_RX_RSS_HASH | PKT_RX_FDIR;`），`const` 变量在预处理阶段不可用。

### rte_log 可变参数宏

```c
#define rte_log(level, type, fmt, ...) \
    do { \
        if (rte_log_get_level(type) >= level) \
            __rte_log(level, type, fmt, ##__VA_ARGS__); \
    } while (0)
```

`##__VA_ARGS__` 是 GCC 扩展，无参时去掉前面的逗号。这让 `rte_log(ERR, "ok")` 和 `rte_log(ERR, "x=%d", x)` 都能编译。详见 14.2.3。

### 编译期配置

```c
/* 编译时通过 -D 注入配置 */
/* gcc -DBURST_SIZE=64 -DMAX_CORES=16 ... */

#ifndef BURST_SIZE
    #define BURST_SIZE 32   /* 默认值 */
#endif

_Static_assert(BURST_SIZE >= 1 && BURST_SIZE <= 256,
               "BURST_SIZE must be in [1, 256]");
```

构建系统通过 `-D` 注入配置，源码用 `#ifndef` 提供默认值。`_Static_assert` 在编译期校验配置合法性，优于运行期检查。

## UB 边界

| 情形 | 后果 | 防护 |
|------|------|------|
| 宏体缺括号 | 优先级错乱（见 14.2.1） | 整体加括号，参数各自加括号 |
| 宏名与变量同名 | 宏展开覆盖变量，编译错误或行为异常 | 宏名全大写，变量名小写 |
| 宏跨文件泄漏 | 头文件宏污染下游 | `#undef` 局部取消，或用 `static const` 替代 |
| 递归宏定义 | 预处理器不递归，宏体保留原样 | 避免自引用，用 `#undef` + 重定义 |
| 函数宏调用缺括号 | 部分编译器不展开 | 函数宏调用必须 `NAME(args)` 形式 |

### 宏与 const 的边界

```c
#define SIZE 100           /* 宏：预处理替换，无类型 */
const int size = 100;       /* const：编译期常量，有类型，有作用域 */
static const int sz = 100;  /* static const：文件作用域，放 .rodata */
```

选型规则：

- **预处理可用性**：需用于 `#if` 或数组大小（C89）→ 宏。
- **类型安全**：需要编译器类型检查 → `const`。
- **调试可见**：gdb 能看到变量名 → `const`。
- **作用域控制**：需要文件内或块内作用域 → `const`。
- **C99 起 VLA**：数组大小可用变量 → `const` 也可。

HFT 工程惯例：**配置常量用宏**（编译期注入、预处理可用），**运行时常量用 `static const`**（类型安全、调试友好）。

## 与后续衔接

- **14.2.1 宏**：对象宏的括号陷阱与特点深入。
- **14.2.2 宏与函数**：宏 vs `static inline` 的工程选型。
- **14.2.3 带参数的宏**：`#`/`##`/`__VA_ARGS__` 的高级用法。
- **14.2.4 副作用**：宏参数重复求值的线上陷阱。
- **14.2.5 命名约定**：GNU statement expression、`#undef` 实践。
- **ch07 函数**：`static inline` 的内联语义。
- **ch10 结构体**：`#define` 定义位掩码与结构体字段的关系。

## 口述自测题

1. C 标准定义的翻译阶段共 8 个，**预处理器负责哪几个**？`#include` 和 `#define` 分别在第几步？怎么用 `gcc -E` 查看预处理后的结果？
2. 对象宏和函数宏的区别是什么？`#define BURST_SIZE 32` 和 `#define MIN(x,y) ((x)<(y)?(x):(y))` 分别属于哪类？**函数宏调用时括号能省吗**？
3. 宏的作用域从 `#define` 到 `#undef` 或文件末。**为什么说宏「没有 C 作用域概念」**？函数边界对宏有效吗？这导致什么问题？
4. 配置常量用宏（`#define SIZE 100`）还是 `const`（`const int size = 100`）？**各自的优劣**是什么？HFT 工程惯例是怎么选的？
5. DPDK 用宏定义标志位 `#define PKT_RX_RSS_HASH (1ULL << 0)`，**为什么不用 `const uint64_t`**？标志位在哪些场景需要预处理可用性？
