# 11.6 内存分配实例

## 本节讲什么

本节是 ch11 的综合实践章：内存碎片（外部/内部）、对齐分配、多线程 malloc 的锁竞争、DPDK mempool 与标准堆的对照、链表释放模板、调试工具链、用户态与内核的内存 API 对照。通过这些实例，把前 5 节的概念落地为可操作的工程技能。

## 要点

### 内存碎片

| 类型 | 说明 | 示例 |
|------|------|------|
| **外部碎片** | 空闲块离散，无法拼成大块 | 1 MB 空闲但分散在 100 个 10 KB 块里，无法分配 1 MB |
| **内部碎片** | 对齐向上取整浪费 | 请求 13 字节，malloc 返回 16 字节块，浪费 3 字节 |

**外部碎片**：

```
初始：|####|####|####|####|####|####|####|####|
反复 malloc/free 后：
     |    |####|    |####|    |####|    |####|
     ← 空闲 16 字节，但分散在 4 个 4 字节块里 →
     malloc(16) 失败：无连续 16 字节块
```

**对策**：

- 预分配内存池，统一块大小
- DPDK `rte_mempool`：固定块大小，零外部碎片
- jemalloc/tcmalloc：按 size class 分桶，减少外部碎片
- 避免频繁小块 malloc

### 对齐分配

```c
#include <stdlib.h>

/* C11 aligned_alloc */
void *p = aligned_alloc(64, 1024);   /* 64 字节对齐，1024 字节 */
free(p);

/* POSIX posix_memalign */
void *p;
int rc = posix_memalign(&p, 64, 1024);
if (rc == 0) {
    /* p 是 64 字节对齐的 1024 字节 */
    free(p);
}

/* 旧式 memalign（不推荐，不可移植） */
void *p = memalign(64, 1024);
free(p);
```

**用途**：

- **cache 行对齐**（64 字节）：避免 false sharing
- **DMA 对齐**：网卡 DMA 要求缓冲对齐
- **SIMD 对齐**：AVX-512 要求 64 字节对齐
- **大页**：2 MB 对齐的 mmap

**DPDK 对齐分配**：

```c
/* DPDK rte_malloc 支持对齐参数 */
void *p = rte_malloc_socket("order", size, 64, numa_node);
rte_free(p);
```

### 多线程 malloc 的锁竞争

glibc ptmalloc2 用**多 arena**减少锁竞争，但仍有全局锁：

```c
/* 线程 A */
void *p = malloc(100);   /* 可能阻塞等 arena 锁 */

/* 线程 B 同时 */
void *q = malloc(100);   /* 等 A 释放锁 */
```

**性能影响**：

- 8 线程并发 malloc，吞吐约单线程的 30-50%
- 延迟抖动：锁等待时间不确定，HFT 不可接受

**对策**：

| 方案 | 机制 | 适用 |
|------|------|------|
| **tcmalloc/jemalloc** | thread-local cache | 通用多线程 |
| **mimalloc** | per-thread heap | 现代多核 |
| **线程本地池** | 预分配 per-thread | HFT |
| **DPDK mempool** | per-lcore cache，无锁 | HFT 极致 |

**DPDK mempool 的无锁设计**：

```c
struct rte_mempool *pool = rte_pktmbuf_pool_create(
    "mbuf_pool", 8192, 256, 0,
    RTE_MBUF_DEFAULT_BUF_SIZE, rte_socket_id()
);

/* 每 lcore 有独立 cache（256 个 mbuf） */
/* alloc/free 操作 cache，无锁 */
struct rte_mbuf *m = rte_pktmbuf_alloc(pool);   /* ~10 ns，无锁 */
rte_pktmbuf_free(m);                             /* ~10 ns，无锁 */
```

cache 耗尽时才从共享池补充，此时有锁但频率低。

### 简易链表（防泄漏模板）

```c
typedef struct node {
    uint64_t id;
    struct node *next;
} Node;

Node *head = NULL;

/* 插入若干节点 */
for (int i = 0; i < 1000; i++) {
    Node *n = malloc(sizeof *n);
    if (!n) goto cleanup;
    n->id = i;
    n->next = head;
    head = n;
}

/* 使用链表 */
for (Node *p = head; p; p = p->next) {
    process(p->id);
}

/* 释放整链 */
cleanup:
while (head) {
    Node *next = head->next;   /* 先保存 next */
    free(head);
    head = next;
}
head = NULL;
```

**关键点**：

- 释放前**先保存 `next`**——free 后 `head->next` 是 UAF
- 用 while 循环而非 for，因为 free 后不能访问 `head->next`
- 释放后 `head = NULL`

### 调试工具链

**ASan（AddressSanitizer）**：

```bash
gcc -O0 -g -fsanitize=address,undefined -fno-omit-frame-pointer prog.c -o prog_asan
./prog_asan
```

- 检测：越界、UAF、double free、泄漏、UB
- 开销：2-5x 慢，3x 内存
- 是 HFT 代码 CI 的标配

**valgrind**：

```bash
valgrind --leak-check=full --show-leak-kinds=all ./prog
valgrind --tool=massif ./prog   # 内存增长分析
valgrind --tool=callgrind ./prog # 性能分析
```

- 检测：越界、UAF、泄漏、未初始化
- 开销：20-50x 慢
- 不需重新编译

**gdb core 分析**：

```bash
ulimit -c unlimited   # 允许 core dump
./prog                 # 崩溃生成 core
gdb prog core
(gdb) bt               # 栈回溯
(gdb) info registers
```

**jemalloc prof**：

```bash
MALLOC_CONF=prof:true ./prog
# 运行后分析内存分配栈
```

### 用户态 vs 内核态内存 API

| 用户态 | 内核 | 用途 |
|--------|------|------|
| `malloc`/`free` | `kmalloc`/`kfree` | 小块，物理连续 |
| `calloc` | `kzalloc` | 清零小块 |
| `posix_memalign` | `kmalloc` + `GFP_DMA` | 对齐分配 |
| `mmap` | `vmalloc` | 大块，虚拟连续 |
| — | `__get_free_page` | 整页 |
| — | `kmem_cache` | slab 分配器，固定大小 |
| hugepage mmap | `hugetlbfs` | 大页，减少 TLB miss |

**内核 slab 分配器**：

- 类似用户态 mempool
- 固定大小对象（如 `task_struct`、`file`）
- 预分配 + 复用，无碎片
- `/proc/slabinfo` 可查看

## HFT 关联

- **DPDK mempool**：固定块大小，无碎片，无锁，NUMA 感知
- **hugepage**：DPDK 用 2 MB 大页 mmap，减少 TLB miss
- **NUMA 感知**：每 NUMA 节点独立 mempool，避免跨节点访问（延迟差 2-3 倍）
- **零 malloc 原则**：热路径完全不调 malloc，所有内存启动时预分配
- **ASan CI**：所有 DPDK 单元测试在 ASan 下运行

### 实操：DPDK mempool 性能

```c
#include <rte_mempool.h>
#include <rte_cycles.h>

struct rte_mempool *pool = /* 创建 */;

uint64_t start = rte_rdtsc();
for (int i = 0; i < 1000000; i++) {
    struct rte_mbuf *m = rte_pktmbuf_alloc(pool);
    rte_pktmbuf_free(m);
}
uint64_t end = rte_rdtsc();
printf("DPDK: %f ns/op\n", (double)(end - start) / 1000000 / 3.0);
/* 约 10-30 ns/op */

/* 对比 glibc malloc */
start = rte_rdtsc();
for (int i = 0; i < 1000000; i++) {
    void *p = malloc(2048);
    free(p);
}
end = rte_rdtsc();
printf("glibc: %f ns/op\n", (double)(end - start) / 1000000 / 3.0);
/* 约 50-200 ns/op */
```

### 实操：对齐分配

```c
/* cache 行对齐，避免 false sharing */
struct counter {
    uint64_t value;
} __attribute__((aligned(64)));

struct counter counters[8];   /* 每 CPU 一个，64 字节对齐 */

void inc(int cpu) {
    counters[cpu].value++;   /* 无 false sharing */
}
```

### 实操：链表释放

```c
Node *head = /* 已填充的链表 */;

/* 正确：先保存 next */
while (head) {
    Node *next = head->next;
    free(head);
    head = next;
}

/* 错误：free 后访问 head->next（UAF） */
for (Node *p = head; p; p = p->next) {
    free(p);   /* p->next 是 UAF */
}
```

## 与后续衔接

- **ch12** 结构体指针链表的完整实现
- **ch17** 经典 ADT（栈、队列、链表）的内存管理
- **ch18** 运行时环境：brk/mmap 系统调用
- **DPDK rte_mempool** 官方文档
- **Linux 内核** slab/slub 分配器

## 自测题

1. 外部碎片和内部碎片有什么区别？DPDK mempool 如何消除外部碎片？
2. `posix_memalign` 和 `aligned_alloc` 的用途是什么？HFT 代码为什么要 cache 行对齐？
3. glibc malloc 的锁竞争在多线程下有什么影响？DPDK mempool 如何实现无锁？
4. 释放链表时为什么不能 `for (p = head; p; p = p->next) free(p)`？正确写法是什么？
5. ASan、valgrind、gdb core 各适合什么场景？HFT CI 流程应该用哪个？
