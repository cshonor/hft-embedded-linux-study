# 8.2 面向对象编程基础

## 本节讲什么

**面向对象编程（OOP）** 用「对象」组织数据与行为，强调**封装、继承、多态**三大特性。C 语言没有 class 关键字，但 Linux 内核、DPDK、RTOS 驱动框架普遍用 **struct + 函数指针 + ops 表** 模拟 OOP。本节是 **8.2.1–8.2.4** 的总览：建立 C 与 C++/Java OOP 概念的映射表，为后续内核源码阅读打底。

## 要点

**C ↔ OOP 概念映射**：

| OOP 概念 | C 语言实现 | 内核/DPDK 示例 |
|----------|------------|----------------|
| **类（class）** | `struct` + 配套 API | `struct net_device` |
| **对象（object）** | struct 变量或 `malloc` 实例 | `struct uart_dev uart0` |
| **成员变量** | struct 字段 | `int baudrate` |
| **成员方法** | 以对象指针为第一参数的函数 | `netdev->ops->ndo_open()` |
| **虚函数 / 多态** | 函数指针 / ops 结构体 | `file_operations` |
| **继承** | **基类 struct 作为首成员** | `UartDev` 内嵌 `Device base` |
| **接口** | 只含函数指针的 struct | `PowerOps`、`CommOps` |

```c
/* 最小「类」骨架 */
struct counter {
    int value;
};

void counter_inc(struct counter *self) {
    self->value++;
}
```

- OOP 解决的是**大规模代码的组织问题**，不是语法糖；C 用约定与 discipline 达到同样目标
- **首成员继承**依赖 C 标准「struct 首地址 = 首成员地址」，可用 `(Device *)&uart_dev` 向上转型
- **ops 表**把行为从数据分离，同一框架代码遍历 heterogeneous 设备列表

## 与后续衔接

- **8.2.1** 四大特性详解
- **8.2.2** 封装与 create/destroy → **demo01**
- **8.2.3** 继承与运行时多态 → **demo02/demo03**
- **8.2.4** 虚函数、纯虚函数与 `DevOps`
- **8.3–8.5** Linux/DPDK 真实案例

---

## 代码自测

**题目 1：** C 语言（非 C++）如何模拟面向对象的封装、继承、多态？
```c
// 封装
struct animal {
    int age;
    void (*speak)(struct animal *);
};
// 继承
struct dog {
    struct animal base;  // 第一个成员
    int breed;
};
// 多态
void make_speak(struct animal *a) { a->speak(a); }
```
<details>
<summary>参考答案</summary>

C 模拟 OOP 的方式：

**封装**：struct + 函数指针 + 头文件隐藏实现
- struct 放头文件（公开接口），实现放 .c 文件
- 函数指针模拟成员方法

**继承**：结构体嵌套（第一个成员是父类）
- `struct dog { struct animal base; ... }`
- `struct animal *a = (struct animal *)&dog;` 安全转换（C 标准保证首成员地址 = 结构体地址）

**多态**：函数指针表（vtable）
- 不同子类设置不同的 `speak` 函数
- `make_speak` 通过函数指针调用，运行时决定调用哪个函数

这是 Linux 内核的核心设计模式：file_operations/device_driver/socket_ops 都是此模式。

</details>
