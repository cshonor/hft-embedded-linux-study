# 1.3.2 Git 的安装和配置

## 本节讲什么

安装 Git 与 **user.name / user.email** 一次性配置。

## 要点

```bash
sudo apt install -y git

git config --global user.name "Your Name"
git config --global user.email "you@example.com"
git config --global init.defaultBranch main
```

```bash
git config --list
ssh-keygen -t ed25519    # 可选：GitHub SSH
```

## 与后续衔接

**1.3.3** init/add/commit；团队统一 `.gitignore`。

---

## 代码自测

**题目 1：** 以下 Git 配置分别设置什么？
```bash
git config --global user.name "John"
git config --global user.email "john@example.com"
git config --global core.editor vim
```

<details>
<summary>参考答案</summary>

设置全局用户名、邮箱（用于提交记录的作者信息）和默认编辑器（用于写提交信息）。--global 表示对当前用户所有仓库生效。每个仓库也可以单独配置（不加 --global）。提交记录中的作者信息是代码追溯的重要依据。

</details>
