# 10.5 揭开文件系统的神秘面纱

## 本节讲什么

**文件系统** 把块设备上的字节组织成目录、文件与权限，是 Linux 应用访问持久化数据的主路径。本节总览 **VFS、挂载、根文件系统**，为嵌入式 Linux（Buildroot/Yocto）与 **10.4 syscall** 中的 `open/read/write` 建立整体图。

## 要点

**为什么需要文件系统**：

- 裸块设备只有 **扇区/LBA**；应用需要 **路径名、层次目录、并发访问**
- 不同介质：**eMMC、SD、NOR/NAND Flash、UBIFS**

**Linux 分层**：

```
应用 open("/etc/config.ini")
    │
    ▼
VFS（虚拟文件系统）── 统一 inode/dentry/file 抽象
    │
    ├── ext4 模块
    ├── procfs / sysfs（伪文件系统）
    └── devtmpfs（/dev）
    │
    ▼
块层 / MTD ──► 驱动 ──► 硬件（**10.6/10.7**）
```

**RTOS 对比**：

- FreeRTOS + **FatFs** / **LittleFS**：单任务或 mutex 保护，无 VFS 多 FS 并存
- 资源受限 MCU 常用 **裸 Flash 分区表** 而非完整 FS

**嵌入式 Linux 典型布局**：

```
/
├── bin/ lib/      用户态程序与库
├── etc/           配置
├── dev/           设备节点
├── proc/ sys/     内核接口
├── mnt/           挂载点
└── userdata/      可写分区（ext4/ubifs）
```

**与 syscall 关系**（**10.4.4**）：

- 每个进程 **FD 表**；`open` 分配 fd → 指向内核 `struct file`
- `read/write` 经 VFS → 具体 FS → 块 I/O

## 与后续衔接

- **10.5.1** inode、超级块概念
- **10.5.2** mount 命名空间
- **10.5.3** initramfs 与启动
- **10.6.3** 从 Flash 启动 Linux

---

## 代码自测

**题目 1：** 临界区保护的方式有哪些？各有什么优缺点？
```c
// 1. 关中断
// 2. 自旋锁
// 3. 互斥锁
// 4. 原子操作
```

<details>
<summary>参考答案</summary>

1) 关中断：单核最简单，`cli`/`sti`——但影响系统响应，不适合长时间临界区。
2) 自旋锁：多核忙等——适合极短临界区，不适合用户态（浪费 CPU）。
3) 互斥锁：阻塞等待——适合长临界区，有上下文切换开销。
4) 原子操作：`__atomic`/`stdatomic.h`——无锁，适合简单操作（计数器、标志位）。
HFT 场景通常用无锁数据结构（原子操作 + 内存序）。

</details>
