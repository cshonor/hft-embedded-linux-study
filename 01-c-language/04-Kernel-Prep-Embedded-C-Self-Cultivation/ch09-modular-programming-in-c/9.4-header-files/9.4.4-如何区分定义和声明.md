# 9.4.4 如何区分定义和声明

## 本节讲什么

链接错误往往源于「以为写了声明，实际写了定义」。本节给出函数、变量、类型、宏的**一眼辨识规则**与常见混淆场景，便于 code review 和排查 `multiple definition`。

## 要点

**函数**：

| 代码 | 性质 |
|------|------|
| `void foo(int x);` | **声明** |
| `void foo(int x) { ... }` | **定义** |
| `static void bar(void);` | 声明 |
| `static void bar(void) { }` | 定义（internal 链接） |
| `inline void baz(void) { }` | 定义（inline 规则特殊，**9.4.9**） |

- 头文件放**声明**；**至多一个** `.c` 放带函数体的定义
- `static inline` 在头文件 → 每翻译单元一份，通常 OK

**变量**：

| 代码 | 性质 |
|------|------|
| `extern int n;` | 声明 |
| `int n;` / `int n = 0;` | 定义 |
| `static int s;` | 定义（文件作用域） |

**类型**：

| 代码 | 性质 |
|------|------|
| `struct foo;` | 不完整类型声明（前向声明） |
| `struct foo { int x; };` | 类型定义 |
| `typedef struct foo foo_t;` |  typedef 定义 |

- 结构体**定义**在头文件是允许的（POD 数据包），但会暴露布局
- 不透明模块：`typedef struct foo foo_t;` 在 `.h`，`struct foo { ... };` 仅在 `.c`

**宏**：

```c
#define BUF_SIZE 256   /* 预处理替换，无链接概念 */
```

- 宏在头文件重复定义若值不同 → 编译警告/错误
- 复杂宏放 dedicated `config.h`

**快速决策树**：

```
有函数体 { } ？  → 定义（慎放头文件）
有初始化 = ？    → 变量定义（全局只能一处）
仅有 extern ？  → 声明
struct 有字段？ → 类型定义（考虑是否应移到 .c）
```

**典型混淆案例**：

```c
/* 看起来像声明，实为定义 — 若放头文件灾难 */
int buffer[1024];

/* 正确：头文件 */
extern int buffer[1024];
/* 一处 .c */
int buffer[1024];
```

## 与后续衔接

- **9.4.5** 不完整类型与前向声明减少头文件依赖
- **9.4.6** 声明与定义签名一致
- **9.1** 链接器对重复定义的处理

---

## 代码自测

**题目 1：** C 语言中如何实现模块化封装？
```c
// point.h
typedef struct Point Point;  // 不透明指针
Point* point_create(int x, int y);
void point_destroy(Point* p);
int point_get_x(const Point* p);
```

<details>
<summary>参考答案</summary>

不透明指针（opaque pointer）实现封装：1) 头文件只暴露 `typedef struct Point Point;`（不暴露成员）；2) .c 文件中定义完整结构体；3) 通过函数接口操作。调用方无法直接访问成员——实现信息隐藏。优点：ABI 稳定（改结构体不需重编译调用方）、封装性好。缺点：必须堆分配、间接访问。Linux 内核的 `struct file`、`struct task_struct` 等也使用类似模式。

</details>
