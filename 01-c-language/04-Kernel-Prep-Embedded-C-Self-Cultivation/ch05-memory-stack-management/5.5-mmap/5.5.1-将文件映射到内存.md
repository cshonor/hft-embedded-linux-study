# 5.5.1 将文件映射到内存

## 本节讲什么

**文件 mmap** 把磁盘内容映射为虚拟页：读时缺页从页缓存载入，写 `MAP_SHARED` 回刷文件。适合大文件随机访问、共享只读配置，避免 `read/write` 双拷贝。

## 要点

```c
int fd = open("data.bin", O_RDONLY);
void *p = mmap(NULL, st.st_size, PROT_READ, MAP_PRIVATE, fd, 0);
/* 访问 p[i] 等价读文件 offset i */
munmap(p, st.st_size);
close(fd);
```

```bash
gcc -o fmmap fmmap.c
strace -e mmap,openat ./fmmap 2>&1
cat /proc/self/maps | grep data.bin
```

| 标志 | 行为 |
|------|------|
| `MAP_PRIVATE` | CoW，不写回文件 |
| `MAP_SHARED` | 修改可见于其他映射/文件 |

- **内核** `filp->f_op->mmap` 驱动实现；如 **/dev/mem** 需 root
- **DPDK**：配置文件、 hugepage 文件在 `/dev/hugepages`

## 与后续衔接

- **5.5.2** 页表与 `do_mmap`
- **5.5.3** 设备节点同样用 mmap

---

## 代码自测

**题目 1：** 函数调用栈帧（stack frame）的结构是什么？
```c
void f(int a, int b) {
    int local = a + b;
}
```

<details>
<summary>参考答案</summary>

栈帧包含：1) 返回地址（调用方的下一条指令）；2) 旧的帧指针（RBP/FP）；3) 局部变量（local）；4) 保存的寄存器；5) 参数（如果不在寄存器中传递）。`SP` 指向栈顶，`FP` 指向当前帧基址。函数序言（prologue）建立帧，函数结语（epilogue）恢复帧。栈溢出：递归太深或局部变量太大。用 `-fomit-frame-pointer` 省略帧指针（多一个寄存器，但调试困难）。

</details>
