# 4.6.2 裸机环境下的程序运行

## 本节讲什么

无 OS 时，CPU 复位后从 **向量表/Reset Handler** 开始，启动代码负责 **初始化时钟/内存控制器**、把 **`.data` 从 Flash 拷到 RAM**、**清零 `.bss`**、设置栈指针，再跳转到 `main`（或永不返回的内核式循环）。布局由 **链接脚本**（**4.14**）与 startup `.S` 决定。

## 要点

### 与 Linux 运行的差异

| 项目 | Linux | 裸机 |
|------|-------|------|
| 装载 | `execve` + loader | 烧录 Flash，复位直接执行 |
| 库 | `ld.so` | 通常静态链接 |
| BSS | loader 清零 | startup 汇编循环清零 |
| 入口 | `_start`（CRT） | `Reset_Handler` / `_start` |

### 典型启动顺序

1. 设置 SP（栈）
2. 拷贝 `.data`（LMA → VMA）
3. 清零 `.bss`
4. 调用 `SystemInit()`、`.ctors`
5. `bl main`

### 链接脚本关联

```bash
arm-none-eabi-gcc -T linker.ld -nostdlib -o firmware.elf startup.o main.o
arm-none-eabi-objcopy -O binary firmware.elf firmware.bin
readelf -l firmware.elf
```

### 查看段地址

```bash
nm firmware.elf | grep -E '_start|main'
objdump -d firmware.elf | head
```

## 与后续衔接

- **4.14** `MEMORY`/`SECTIONS` 定义 Flash 与 RAM
- **4.12** U-Boot 重定位是裸机启动的复杂实例
- **ch03** 启动汇编；**4.11** 内核解压后自_reloc

---

## 代码自测

**题目 1：** 静态库（.a）和动态库（.so）的链接时机有什么区别？
```bash
gcc main.c -lfoo -o main  # 什么时候链接？
```

<details>
<summary>参考答案</summary>

静态库（.a）：链接时（link time）从库中提取用到的目标文件，复制到可执行文件中。动态库（.so）：链接时只记录依赖信息，运行时（load time 或 runtime）由动态链接器加载库代码。静态库文件大但自包含；动态库小但依赖运行时环境。`ldd main` 查看动态库依赖。`-Wl,-rpath` 指定运行时搜索路径。

</details>
