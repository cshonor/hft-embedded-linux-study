# 13.4 命令行参数

## 本节讲什么

`main(int argc, char **argv)` 是 `char **` 的标准应用——一个字符串指针数组。这一节把 `argv` 的内存模型画清楚，给出 `getopt` 与自研解析的两种范式，以及 DPDK EAL 参数解析在 HFT 工程里的实际形态。

认知锚点：`argv` 不是「特殊语法」，而是 13.1 多级指针的标准实例。理解 `argv` 的内存布局，就理解了所有「指针数组 + 字符串字面量」的工程形态——配置表、命令表、错误码字符串表全部同构。

## 要点

### argv 的内存模型

```c
int main(int argc, char **argv)
{
    for (int i = 0; i < argc; i++)
        printf("%s\n", argv[i]);
    return 0;
}
```

执行 `./myapp -l 0-3 -n 4 -- -p 0x1` 时，内存布局：

```
栈/内核缓冲区:
  argv[0] ----> "./myapp\0"
  argv[1] ----> "-l\0"
  argv[2] ----> "0-3\0"
  argv[3] ----> "-n\0"
  argv[4] ----> "4\0"
  argv[5] ----> "--\0"
  argv[6] ----> "-p\0"
  argv[7] ----> "0x1\0"
  argv[8] = NULL       <-- 哨兵
```

| 符号 | 类型 | 含义 |
|------|------|------|
| `argc` | `int` | 参数个数（含程序名） |
| `argv` | `char **` | 指向 `char *` 数组的指针 |
| `argv[i]` | `char *` | 第 i 个参数字符串（只读） |
| `argv[argc]` | `char *` | 恒为 `NULL`（哨兵） |

**`argv[argc] == NULL` 是 C 标准保证的哨兵**，可以用 `while (argv[i])` 安全遍历，不需要 `argc`：

```c
for (char **p = argv; *p; p++)
    printf("%s\n", *p);
```

### argv 的可写性边界

| 部分 | 存储 | 可写 |
|------|------|------|
| `argv[i]` 指向的字符串内容 | 内核传递的缓冲区 | **实现定义**（POSIX 通常可写，但不要依赖） |
| `argv[i]` 指针本身 | 内核分配的指针数组 | **不可写**（修改 `argv[i]` 是 UB） |
| `argv` 本身 | 栈上局部变量 | 可改（但无意义） |

工程实践：**把 argv 字符串视为只读**。需要修改时先 `strdup` 到堆上再操作。部分嵌入式平台 argv 字符串在只读段，写入直接 SIGSEGV。

### 等价写法

```c
int main(int argc, char **argv);     /* 指针形式 */
int main(int argc, char *argv[]);    /* 数组形式，等价 */
int main(int argc, char *argv[argc]);/* C99 大小标注，等价 */
```

三种写法完全等价——`char *argv[]` 作为函数参数退化为 `char **`（见 ch08 数组退化规则）。第三种 `char *argv[argc]` 是 C99 VLA 语法，标注大小但不改变语义，仅文档作用。

### 第三参数 envp（非标准扩展）

```c
int main(int argc, char **argv, char **envp);
```

POSIX 扩展，第三个参数是环境变量数组，格式 `"KEY=VALUE"`，以 NULL 结尾。**可移植代码不要用**，应通过 `extern char **environ;` 或 `getenv`/`setenv` 访问环境变量。

### getopt：标准参数解析

```c
#include <unistd.h>

int main(int argc, char **argv)
{
    int opt;
    const char *optstr = "l:n:p::";
    /* l: 需要参数，n: 需要参数，p:: 可选参数 */

    while ((opt = getopt(argc, argv, optstr)) != -1) {
        switch (opt) {
            case 'l':
                printf("cores: %s\n", optarg);
                break;
            case 'n':
                printf("channels: %s\n", optarg);
                break;
            case 'p':
                printf("port mask: %s\n", optarg ? optarg : "(none)");
                break;
            default:
                fprintf(stderr, "Usage: %s -l cores -n channels [-p mask]\n", argv[0]);
                return 1;
        }
    }
    /* optind 指向第一个非选项参数 */
    for (int i = optind; i < argc; i++)
        printf("positional: %s\n", argv[i]);
    return 0;
}
```

`getopt` 的全局变量：`optarg`（当前选项参数）、`optind`（下一个待处理下标）、`opterr`（错误时打印）。**非线程安全**，多线程场景用 `getopt_long` 或自研解析。

### getopt_long：长选项支持

```c
#include <getopt.h>

static struct option long_opts[] = {
    {"cores",   required_argument, 0, 'l'},
    {"channels",required_argument, 0, 'n'},
    {"port",    optional_argument, 0, 'p'},
    {"help",    no_argument,       0, 'h'},
    {0, 0, 0, 0},
};

int main(int argc, char **argv)
{
    int opt, idx;
    while ((opt = getopt_long(argc, argv, "l:n:p::h",
                              long_opts, &idx)) != -1) {
        /* ... */
    }
}
```

支持 `--cores 0-3` 和 `-l 0-3` 两种形式。大型 HFT 应用的配置脚本常用这种。

### 自研解析：完全控制

```c
struct app_cfg {
    char     core_list[32];
    uint32_t num_channels;
    uint32_t port_mask;
};

static int parse_args(int argc, char **argv, struct app_cfg *cfg)
{
    for (int i = 1; i < argc; i++) {
        if (strcmp(argv[i], "-l") == 0 && i + 1 < argc) {
            snprintf(cfg->core_list, sizeof(cfg->core_list),
                     "%s", argv[++i]);
        } else if (strcmp(argv[i], "-n") == 0 && i + 1 < argc) {
            cfg->num_channels = atoi(argv[++i]);
        } else if (strcmp(argv[i], "--") == 0) {
            /* DPDK EAL 分隔符，之后是应用参数 */
            return i + 1;   /* 返回应用参数起点 */
        } else {
            fprintf(stderr, "unknown arg: %s\n", argv[i]);
            return -1;
        }
    }
    return argc;
}
```

DPDK 框架自己实现了一套类似 `getopt` 的解析（`rte_eal_init`），因为需要支持双段参数（EAL 参数 + 应用参数，以 `--` 分隔）。

## HFT 关联

### DPDK EAL 参数解析

```c
/* 典型启动命令 */
/* ./my_hft -l 0-3 -n 4 --log-level=8 -- -p 0x1 --strategy=alpha */

int main(int argc, char **argv)
{
    int ret = rte_eal_init(argc, argv);   /* 解析 EAL 参数 */
    if (ret < 0) rte_panic("EAL init failed\n");

    /* rte_eal_init 返回应用参数起点 */
    argc -= ret;
    argv += ret;

    /* 应用自己的参数解析 */
    struct app_cfg cfg = {0};
    if (parse_app_args(argc, argv, &cfg) < 0)
        return 1;

    /* 启动数据面 */
    run_dataplane(&cfg);
    return 0;
}
```

EAL 参数包括：`-l` 核心列表、`-n` 内存通道数、`--log-level`、`--no-huge`、`--vdev` 虚拟设备等。`--` 是 EAL 与应用参数的分隔符。**HFT 应用必须在 `rte_eal_init` 之后才能使用 DPDK 其他 API**——hugepage、mempool、网卡初始化都在 EAL 里完成。

### 配置文件 vs 命令行

| 维度 | 命令行参数 | 配置文件 |
|------|------------|----------|
| 启动时灵活 | 高 | 低 |
| 参数量 | 少（<20） | 多（任意） |
| 修改成本 | 重启进程 | 可热加载 |
| 环境隔离 | 同机多实例易混 | 文件路径隔离 |
| HFT 实践 | EAL + 基础配置 | 策略参数、风控阈值 |

HFT 工程惯例：**基础设施参数走命令行，业务参数走配置文件**。前者改动少、启动时确定；后者频繁调整、可能热加载。

### 策略选择参数

```c
/* ./my_hft -l 0-3 -n 4 -- --strategy=alpha --symbols=IF2406,IF2409 */

static int parse_app_args(int argc, char **argv, struct app_cfg *cfg)
{
    for (int i = 1; i < argc; i++) {
        if (strncmp(argv[i], "--strategy=", 11) == 0) {
            cfg->strategy_name = argv[i] + 11;
        } else if (strncmp(argv[i], "--symbols=", 10) == 0) {
            cfg->symbols_csv = argv[i] + 10;
        }
    }
    return 0;
}
```

`--key=value` 形式便于扩展，`strncmp` 前缀匹配避免误解析。生产代码建议用配置文件 + `getopt_long` 双管。

### 调试参数与生产参数分离

```c
/* 调试参数仅在 DEBUG 构建生效 */
#ifdef DEBUG
    if (strcmp(argv[i], "--trace") == 0) {
        enable_trace();
    }
#endif
```

调试参数在 release 构建里编译掉，避免误用导致性能下降。这是 HFT 进程管理的常见实践。

## UB 边界

| 情形 | 后果 | 防护 |
|------|------|------|
| 修改 `argv[i]` 指针本身 | UB，可能崩溃 | 把 argv 当只读，需要时 `strdup` |
| 修改 `argv[i]` 指向的字符串 | 实现定义，可能 SIGSEGV | 视为只读，写入前复制 |
| `argv[argc]` 非空访问 | 越界 UB | 用 `i < argc` 或 `argv[i] != NULL` 终止 |
| `getopt` 多线程并发 | 全局状态污染 | 用 `getopt_long` 的 `opterr=0` 或自研解析 |
| `atoi` 解析失败 | 静默返回 0 | 用 `strtol` + 错误检查 |
| `--` 后参数未处理 | 静默丢失 | 显式记录 `optind` 或自研返回起点 |

### `atoi` vs `strtol`

```c
/* 危险：失败返回 0，无法区分 "0" 和非法输入 */
int n = atoi(argv[i]);

/* 安全：endptr 检查是否完全解析 */
char *end;
long n = strtol(argv[i], &end, 10);
if (*end != '\0' || end == argv[i])
    fprintf(stderr, "invalid number: %s\n", argv[i]);
```

`atoi("abc")` 返回 0，与 `atoi("0")` 无法区分。HFT 配置解析必须用 `strtol`，否则一个 typo 可能让 `num_channels = 0` 触发后续除零或空分配。

### `getopt` 的全局状态

```c
extern char *optarg;
extern int   optind, opterr, optopt;
```

`getopt` 用全局变量传递状态，**不可重入**。多线程调用会互相覆盖。可移植代码在 `main` 单线程阶段完成所有 `getopt`，之后再启动工作线程。

## 与后续衔接

- **13.1 多级指针**：`argv` 是 `char **` 的标准实例，内存模型与字符串指针数组同构。
- **13.5 字符串常量**：`argv[i]` 指向的字符串存储区与字面量的关系。
- **ch09 字符串**：`strtol`、`strcmp`、`strtok` 在参数解析中的应用。
- **ch15 I/O**：环境变量、`getenv`、进程启动与环境的关系。
- **ch18 运行时环境**：内核如何把 `argv` 传递给新进程（`execve` 系统调用）。

## 口述自测题

1. `argv[argc]` 的值是什么？**为什么 C 标准保证它是 NULL**？用 `while (argv[i])` 遍历相比 `for (i = 0; i < argc; i++)` 有什么好处？
2. 修改 `argv[i]` 指针本身和修改 `argv[i]` 指向的字符串内容，**哪个是 UB**？哪个是「实现定义」？为什么工程实践把两者都视为只读？
3. `int main(int argc, char *argv[])` 和 `int main(int argc, char **argv)` **是否等价**？为什么？第三种写法 `char *argv[argc]` 改变了什么？
4. DPDK `rte_eal_init(argc, argv)` 返回一个整数，调用方用它做 `argc -= ret; argv += ret;`。**这个返回值代表什么**？为什么 EAL 参数和应用参数要用 `--` 分隔？
5. `atoi(argv[i])` 解析失败返回 0，与合法输入 "0" 无法区分。**`strtol` 怎么解决这个问题**？`endptr` 的作用是什么？HFT 配置解析为什么必须用 `strtol`？
