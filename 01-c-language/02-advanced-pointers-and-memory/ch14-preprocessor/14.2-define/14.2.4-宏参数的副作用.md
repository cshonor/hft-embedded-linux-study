# 14.2.4 宏参数的副作用

## 本节讲什么

函数宏的参数在宏体中每出现一次就会被求值一次。如果实参含有副作用（自增、自减、函数调用、赋值），多次求值会导致**非预期行为**——变量被改多次、函数被调多次。这是宏相比 `static inline` 函数最危险的特性，是 C 工程线上逻辑异常的高频主因。

认知锚点：`MIN(a++, b)` 这种调用在宏里 `a` 可能自增两次，在 inline 函数里只自增一次。这种差异不是「编译器优化差异」，而是**宏与函数的语义根本不同**。HFT 工程的硬性准则：**带副作用的表达式不要传给宏**，复杂逻辑改用 `static inline`。

## 要点

### 经典反例：MIN(a++, b)

```c
#define MIN(x, y)  ((x) < (y) ? (x) : (y))

int a = 1, b = 3;
int m = MIN(a++, b);
```

宏展开：

```c
int m = ((a++) < (b) ? (a++) : (b));
```

求值过程：

1. `(a++) < (b)`：先比较 `a`（值为 1）与 `b`（值为 3），结果 `1 < 3` 为真。**`a++` 后缀自增，a 变成 2**。
2. 条件为真，执行 `(a++)`：返回 `a`（当前值 2），**`a++` 再次后缀自增，a 变成 3**。
3. `m = 2`，`a = 3`。

**预期**：`m = 1`（用自增前的 `a` 比较），`a = 2`（自增一次）。
**实际**：`m = 2`，`a = 3`（自增两次）。

### 副作用的分类

| 副作用形式 | 例子 | 宏里的风险 |
|-----------|------|------------|
| 自增/自减 | `a++`、`++a`、`a--` | 多次求值导致多次自增 |
| 函数调用 | `f(x)` | 多次调用，可能有副作用 |
| 赋值 | `x = 5`、`x += 1` | 多次赋值 |
| 复合表达式 | `x++, y++` | 多次求值 |
| I/O 操作 | `getchar()` | 多次读取 |
| volatile 读 | `*hw_reg` | 多次硬件访问 |

```c
#define MAX(x, y)  ((x) > (y) ? (x) : (y))

/* 函数调用副作用 */
int m = MAX(rand(), rand());   /* rand() 可能调两次，结果不可预测 */

/* volatile 副作用 */
volatile uint32_t *counter = HW_COUNTER;
int latest = MAX(*counter, threshold);   /* *counter 可能读两次，值不一致 */

/* I/O 副作用 */
int c = MAX(getchar(), 'a');   /* getchar() 可能调两次，吞掉一个字符 */
```

### 三条防御措施

#### 1. 复杂逻辑改用 `static inline`

```c
static inline int min_int(int a, int b)
{
    return a < b ? a : b;
}

int a = 1, b = 3;
int m = min_int(a++, b);   /* a 只自增一次，m = 1，a = 2 */
```

`static inline` 函数的参数在调用时求值一次，传入函数后是局部变量，**不存在重复求值**。这是最安全的方案。

#### 2. 宏内参数只引用一次

```c
/* 危险：x 引用两次 */
#define SQR(x)  ((x) * (x))
int y = SQR(a++);   /* a 自增两次 */

/* 安全：x 只引用一次 */
#define INCR(x)  ((x) + 1)
int z = INCR(a++);  /* a 只自增一次 */
```

但「只引用一次」难以保证——MIN/MAX 必须引用两次（条件和返回值）。这种宏只能用 inline 函数或 GNU `({})` 扩展。

#### 3. 调用处不用带副作用的表达式

```c
/* 危险：a++ 有副作用 */
int m = MIN(a++, b);

/* 安全：先求值，再传宏 */
int tmp = a++;
int m = MIN(tmp, b);
```

调用方自律——不把带副作用的表达式传给宏。但这依赖开发者自觉，**大型项目难免有人疏忽**。

### GNU statement expression：两全其美

```c
#define MIN(a, b) ({ \
    __typeof(a) _a = (a); \
    __typeof(b) _b = (b); \
    _a < _b ? _a : _b; \
})

int x = 1, y = 3;
int m = MIN(x++, y);
/* 展开：
   ({ __typeof(x++) _a = (x++); __typeof(y) _b = (y); _a < _b ? _a : _b; })
   _a = (x++) 求值一次，x 变 2，_a = 1
   _b = (y) 求值一次，_b = 3
   比较 _a < _b，返回 _a = 1
   结果：m = 1，x = 2（自增一次）
*/
```

GNU `({})` 扩展让宏在块内声明局部变量保存参数值，**参数只求值一次**，同时保留宏的泛型性。DPDK `RTE_MIN`、内核 `min()` 都用这种写法。代价是非标准（GCC/Clang 扩展，MSVC 不支持）。

### C23 的 `constexpr` 与泛型

C23 引入 `constexpr`、`_Generic` 增强、`__VA_OPT__` 等，部分缓解宏的问题，但**副作用问题仍需 GNU `({})` 或 inline 函数**解决。标准 C 没有等价于 `({})` 的语法。

## HFT 关联

### 线上事故的典型形态

```c
#define NEXT_SEQ()  (seq_counter++)

/* 危险：NEXT_SEQ() 在宏里可能调多次 */
#define MAKE_ORDER(side, qty) ({ \
    struct order o = { \
        .seq   = NEXT_SEQ(), \
        .side  = (side), \
        .qty   = (qty), \
    }; \
    o; \
})

struct order o = MAKE_ORDER('B', NEXT_SEQ());
/* NEXT_SEQ() 调一次，OK（宏内只引用一次） */

#define BAD_MAX(x, y) ((x) > (y) ? (x) : (y))
uint64_t s = BAD_MAX(NEXT_SEQ(), last_seq);
/* NEXT_SEQ() 可能调两次，seq_counter 跳号 */
```

HFT 订单序号必须连续严格递增，`NEXT_SEQ()` 被宏调两次导致序号跳号，撮合对账失败。**这种 bug 在测试环境很难复现**（只在特定分支才触发），上线后才暴露。

### 数据面热点的宏使用

```c
/* DPDK 报文计数宏 */
#define PKT_INC(stat)  __sync_fetch_and_add(&(stat), 1)

/* 危险：宏内若多次引用 stat，原子操作多次 */
#define BAD(stat, val) ({ \
    if ((stat) > (val)) (stat) = (val); \
    (stat); \
})

uint64_t s = BAD(PKT_INC(rx_count), threshold);
/* PKT_INC 调多次，rx_count 乱跳 */
```

原子操作宏如果被通用宏嵌套引用，会触发多次原子操作，性能和正确性都受影响。**原子操作必须用 inline 函数封装**，不要让宏嵌套。

### 硬件寄存器访问

```c
volatile uint32_t *hw_counter = (void *)0xFE000000;

/* 危险：*hw_counter 读两次，值可能不一致 */
uint32_t c = MAX(*hw_counter, last_count);

/* 安全：先读到局部变量 */
uint32_t cur = *hw_counter;
uint32_t c = MAX(cur, last_count);
```

硬件寄存器每次读都可能返回不同值（计数器、状态寄存器）。宏里多次读会得到不一致的值，逻辑错误。**volatile 读必须先存局部变量再传宏**。

### 日志宏的安全性

```c
#define LOG(level, fmt, ...) \
    do { \
        if (log_level >= (level)) \
            log_impl((level), fmt, ##__VA_ARGS__); \
    } while (0)

/* __VA_ARGS__ 只在 log_impl 调用处出现一次，安全 */
LOG(INFO, "seq=%lu\n", NEXT_SEQ());
/* NEXT_SEQ() 只调一次 */
```

日志宏的 `__VA_ARGS__` 只在宏体出现一次，副作用安全。这是宏可以放心用副作用的少数场景。

## UB 边界

| 情形 | 后果 | 防护 |
|------|------|------|
| `MIN(a++, b)` | `a` 自增两次 | 改用 inline 或 GNU `({})` |
| `MAX(rand(), rand())` | `rand()` 调两次，不可预测 | 先存局部变量 |
| `ABS(*volatile_ptr)` | volatile 读两次，值不一致 | 先存局部变量 |
| `SQR(f())` | `f()` 调两次，副作用加倍 | 改用 inline |
| 宏嵌套原子操作 | 原子操作多次，性能与正确性问题 | 原子操作用 inline 函数封装 |
| 条件编译关掉宏 | 副作用整个消失，行为变化 | 宏关闭时副作用应可接受 |

### 宏关闭时的副作用

```c
#if DEBUG
    #define DBG_EVAL(x) (x)
#else
    #define DBG_EVAL(x) ((void)0)
#endif

uint64_t s = DBG_EVAL(NEXT_SEQ());
/* DEBUG 模式：NEXT_SEQ() 调用，seq_counter 自增 */
/* Release 模式：NEXT_SEQ() 不调用，seq_counter 不变 */
```

宏在条件编译关闭时，参数副作用整个消失。**如果副作用是程序逻辑必须的（如序号递增），不能放在可能被关闭的宏里**。

## 与后续衔接

- **14.2.5 命名约定**：GNU statement expression 的工程实践。
- **14.2.2 宏与函数**：宏 vs `static inline` 的选型准则。
- **《C 陷阱与缺陷》ch06**：重复求值的经典案例分析。
- **ch07 函数**：`static inline` 的内联语义。
- **ch11 动态内存**：`malloc` 在宏里的多次求值陷阱。

## 口述自测题

1. `#define MIN(x,y) ((x)<(y)?(x):(y))` 调用 `MIN(a++, b)`（a=1, b=3），**`a` 自增几次**？`m` 的最终值是多少？为什么 inline 函数 `min_int(a++, b)` 只自增一次？
2. `MAX(*hw_counter, threshold)` 中 `hw_counter` 是 volatile 指针。**宏里 `*hw_counter` 读几次**？为什么这种读法在 HFT 硬件访问里危险？正确写法是什么？
3. 三条防御措施分别是什么？**为什么「宏内参数只引用一次」对 MIN/MAX 不可行**？GNU `({})` 扩展是怎么解决这个问题的？
4. `#if DEBUG #define DBG_EVAL(x) (x) #else #define DBG_EVAL(x) ((void)0)` 这种条件编译宏，**release 模式下 `DBG_EVAL(NEXT_SEQ())` 会发生什么**？为什么「必须的副作用不能放在可能被关闭的宏里」？
5. HFT 订单序号必须连续严格递增。`BAD_MAX(NEXT_SEQ(), last_seq)` 这种调用为什么会导致序号跳号？**这种 bug 为什么在测试环境难复现**？工程上应该怎么避免？
