# 11.3 calloc 和 realloc

## 本节讲什么

**`calloc`** 是 `malloc` 的清零版——分配并初始化为 0，适合新数组和结构体池。**`realloc`** 扩缩容已分配的块，但有**丢指针泄漏**的高危写法。本节剖析二者的机制、与 `malloc` 的区别、`realloc` 的安全模式、以及 `calloc` 的乘法溢出陷阱。

## 要点

### calloc 函数原型

```c
#include <stdlib.h>
void *calloc(size_t nmemb, size_t size);
```

- 参数：`nmemb`（元素数）× `size`（每元素字节数）
- 返回：成功返回清零的内存，失败返回 `NULL`
- **清零**：所有字节初始化为 0
- 等价 `malloc(nmemb * size)` + `memset(0)`，但 calloc 检测乘法溢出

### calloc 基本用法

```c
/* 分配 32 个 rte_mbuf，清零 */
struct rte_mbuf *pkts = calloc(32, sizeof *pkts);
if (!pkts) {
    return -ENOMEM;
}
/* pkts 的所有字节都是 0 */
```

### calloc vs malloc + memset

| 维度 | **calloc** | **malloc + memset** |
|------|------------|---------------------|
| 清零 | 是 | 需手动 memset |
| 溢出检测 | **内置** | 需手动 |
| 性能 | **更快**（OS 已清零页） | 慢（实际写 0） |
| 语义 | 元素数 × 大小 | 总字节数 |

**calloc 更快的原因**：

- Linux 的零页（zero page）已是 0，calloc 用 `mmap` 映射零页时 COW（copy-on-write）
- 首次写才分配物理页
- `malloc + memset` 必须实际写 0，触发 COW

### calloc 的乘法溢出保护

```c
/* 危险：malloc 的乘法可能溢出 */
size_t n = SIZE_MAX;
int *p = malloc(n * sizeof(int));   /* 溢出，分配远小于预期 */
/* p 实际指向小块，写入越界 */

/* 安全：calloc 内置溢出检测 */
int *p = calloc(n, sizeof(int));   /* 检测到溢出，返回 NULL */
```

- `nmemb * size` 若溢出，calloc 返回 NULL
- malloc 无此检测，需手动：`if (n > SIZE_MAX / sizeof(int)) return NULL;`
- **铁律**：用户输入驱动的大块分配用 calloc

### realloc 函数原型

```c
#include <stdlib.h>
void *realloc(void *ptr, size_t size);
```

- 参数：`ptr`（已分配块，或 NULL）、`size`（新大小）
- 返回：成功返回新块地址（可能与 `ptr` 相同），失败返回 `NULL`
- `ptr == NULL` 时等价 `malloc`
- `size == 0` 且 `ptr != NULL` 时等价 `free`（C89）；C11 行为实现定义

### realloc 的行为

| `ptr` | `size` | 行为 |
|-------|--------|------|
| `NULL` | 任意 | 等价 `malloc(size)` |
| 非 NULL | > 原大小 | **可能**新块 + 拷贝旧数据 + 释放旧块 |
| 非 NULL | < 原大小 | 截断（实现相关，可能不移动） |
| 非 NULL | 0 | 等价 `free(ptr)`，返回 NULL（C89） |

**关键**：`realloc` 可能返回**不同地址**——原块不够大时分配新块、拷贝、释放旧块。

### realloc 的丢指针陷阱

```c
/* 反例：丢指针泄漏 */
pkt_buf = realloc(pkt_buf, new_len);
/* 若 realloc 失败返回 NULL：
 * 1. pkt_buf 被赋值为 NULL
 * 2. 原块地址丢失，无法 free
 * 3. 内存泄漏
 */
```

**安全模式**：

```c
/* 正例：临时变量保存 */
void *tmp = realloc(pkt_buf, new_len);
if (tmp != NULL) {
    pkt_buf = tmp;   /* 成功才更新 */
} else {
    /* 失败：pkt_buf 仍有效，业务降级或退出 */
    /* 原块仍需 free */
}
```

**铁律**：`realloc` 永远用临时变量接收，判成功后再更新原指针。

### realloc 的性能

```c
/* 逐个增长：O(n²) */
for (int i = 0; i < 1000; i++) {
    buf = realloc(buf, i + 1);   /* 每次可能拷贝 */
}

/* 倍增策略：O(n) amortized */
size_t cap = 16;
buf = malloc(cap);
for (int i = 0; i < 1000; i++) {
    if (i >= cap) {
        cap *= 2;
        void *tmp = realloc(buf, cap);
        if (!tmp) { free(buf); return -1; }
        buf = tmp;
    }
    buf[i] = /* ... */;
}
```

- realloc 在原地扩展时 O(1)
- 需要移动时 O(n)（拷贝旧数据）
- 倍增策略让平均 O(1)（几何级数摊还）
- C++ `std::vector` 的增长策略即此

### realloc 的移动语义

```c
int *p = malloc(10 * sizeof(int));
int *old = p;
p = realloc(p, 1000000 * sizeof(int));   /* 大概率移动 */
/* p 可能 != old，old 已释放，不能再访问 */
```

- realloc 后**所有指向原块的指针失效**
- 必须用返回的新地址
- 若有其他指针指向原块内部，它们也失效——需更新

### calloc/realloc 与 free

calloc 和 realloc 返回的内存都用 `free` 释放：

```c
int *a = calloc(10, sizeof(int));
free(a);

int *b = malloc(10 * sizeof(int));
int *c = realloc(b, 20 * sizeof(int));
if (c) { b = c; }   /* b 指向新块 */
free(b);             /* 释放 */
```

## HFT 关联

- **DPDK rte_calloc**：DPDK 的 calloc 等价，清零分配
- **DPDK rte_realloc**：DPDK 的 realloc，但通常用 mempool 避免
- **订单池预分配**：`calloc(MAX_ORDERS, sizeof(struct order))` 启动时一次分配，运行时不 realloc
- **报文缓冲扩容**：HFT 报文通常定长，不用 realloc
- **mempool 替代**：热路径用 `rte_pktmbuf_alloc`/`free`，不调 calloc/realloc

### 实操：calloc 清零 vs malloc 脏数据

```c
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    /* malloc：脏数据 */
    int *a = malloc(5 * sizeof(int));
    printf("malloc:");
    for (int i = 0; i < 5; i++) printf(" %d", a[i]);
    printf("\n");   /* 随机值 */
    free(a);

    /* calloc：清零 */
    int *b = calloc(5, sizeof(int));
    printf("calloc:");
    for (int i = 0; i < 5; i++) printf(" %d", b[i]);
    printf("\n");   /* 全 0 */
    free(b);
}
```

### 实操：realloc 丢指针

```c
int *buf = malloc(100);
/* ... */
buf = realloc(buf, 1000000000);   /* 大概率失败 */
if (!buf) {
    /* 灾难：原 100 字节地址丢失，泄漏 */
    /* 无法 free 原块 */
}

/* 修复 */
int *tmp = realloc(buf, 1000000000);
if (!tmp) {
    free(buf);   /* 原块仍可访问 */
    return -1;
}
buf = tmp;
```

### 实操：倍增策略

```c
struct vector {
    int *data;
    size_t size, cap;
};

int vec_push(struct vector *v, int x) {
    if (v->size >= v->cap) {
        size_t new_cap = v->cap ? v->cap * 2 : 16;
        int *tmp = realloc(v->data, new_cap * sizeof(int));
        if (!tmp) return -1;
        v->data = tmp;
        v->cap = new_cap;
    }
    v->data[v->size++] = x;
    return 0;
}
```

这是 C++ `std::vector` 的核心逻辑。

## 与后续衔接

- **11.2** malloc/free 基础
- **11.4** 动态内存的完整生命周期
- **11.5** 常见错误（含 realloc 丢指针）
- **《C 陷阱与缺陷》ch5** realloc 经典坑
- **C++ STL** vector 的 growth 策略

## 自测题

1. `calloc(32, sizeof(int))` 与 `malloc(32 * sizeof(int))` 有何区别？哪个更快？为什么？
2. `calloc(n, sizeof(int))` 如何检测 `n * sizeof(int)` 溢出？`malloc` 能检测吗？
3. `buf = realloc(buf, new_size)` 为什么危险？正确的写法是什么？
4. realloc 后原指针是否一定有效？指向原块内部的其他指针呢？
5. 写一个动态数组的 push 函数，用倍增策略，处理 realloc 失败。
