# 6.11 内建函数

> **归属**：A 档 · CH2 GNU C 高级特性（源码看不见，但真实发生）
> **实测环境**：WSL · gcc 13.3.0（GNU ld 2.42）/ clang 18.1.3 · x86-64 · glibc 2.39
> **源码核对**：Linux v6.6 `include/linux/compiler.h`、`include/linux/log2.h`、`include/asm-generic/bitops/__fls.h`
> **配套小节**：[6.11.1 什么是内建函数](6.11.1-什么是内建函数.md) · [6.11.2 常用的内建函数](6.11.2-常用的内建函数.md) · [6.11.3 C 标准库的内建函数](6.11.3-C标准库的内建函数.md) · [6.11.4 `__builtin_constant_p`](6.11.4-内建函数-__builtin_constant_p-n.md) · [6.11.5 `__builtin_expect`](6.11.5-内建函数-__builtin_expect-exp-c.md) · [6.11.6 内核中的 likely/unlikely](6.11.6-Linux内核中的likely和unlikely.md)
> **相关**：[6.6 `__attribute__` 总纲](../6.6-section/6.6.1-GNU-C编译器扩展关键字-__attribute__.md) · [6.8 format](../6.8-format/6.8-属性声明-format.md) · [6.10 inline](../../01-ch1-gnu-c-basics/6.10-inline/6.10-内联函数.md) · [6.1 C 标准与 gnu11](../../01-ch1-gnu-c-basics/6.1-c-standard/6.1-C语言标准和编译器.md)

## 本节讲什么

前面 6.6–6.9 讲的 `__attribute__` 是**告诉编译器某个声明有什么性质**。内建函数（builtins）走的是另一条路——**它本身就是编译器内置的伪函数**：语法上像函数调用，编译期被编译器「拦截」，展开成零到多条目标指令。

一句话概括：

> **`__builtin_*` 是编译器「看得懂」的函数名。编译器遇到它，直接生成对应的目标平台指令（或编译期常量），不需要链接、不产生符号表条目。**

但「直接映射到目标指令」这句话有**一个致命陷阱**——书里没讲、实测才暴露：

1. **`__builtin_popcount` 不加 `-mpopcnt` 根本不用 `POPCNT` 指令**——它偷偷调用 `__popcountdi2`（libgcc 软件实现）。你以为零开销，其实有一个函数调用 + 30 条 SWAR 指令；
2. **`__builtin_constant_p` 在 `-O0` 下几乎全返回 0**——连 `static const` 都不认。内核永远 `-O2` 没事，但你用 `-O0 -g` 调试时宏的行为会变；
3. **`__builtin_expect` 改变的是基本块排列顺序**——标错方向会把 hot path 变成跳转目标，实测汇编一目了然；
4. **libc 函数被自动当 builtin 处理**——`strlen("hello")` 在 `-O2` 下折叠成编译期常量 `5`，但加 `-fno-builtin` 后变成真实的 `strlen@plt` 调用。**`__builtin_strlen` 不受 `-fno-builtin` 影响**；
5. **`__builtin_add_overflow` 只需一条 `add` + 一条 `seto`（溢出标志读取）**——比手写的溢出检查快且安全；
6. **`__builtin_unreachable` 让编译器删除不可能到达的分支**——但如果你骗了它，函数返回的是寄存器里的垃圾值（UB）。

---

## 一、内建函数不是函数

先建立一个直觉：内建函数在编译完成后**不存在于任何符号表**。

```c
/* t6_control.c 片段 */
void do_trap(void) {
    __builtin_trap();    /* 不是函数调用 */
}
```

```
$ gcc -std=gnu11 -O2 -c t6_control.c -o t6.o
$ objdump -d t6.o | sed -n '/<do_trap>:/,/^\$/p'
00000000000011f0 <do_trap>:
    11f0:  endbr64
    11f4:  push   %rax
    11f5:  pop    %rax
    11f6:  lea    0xe07(%rip),%rdi        # 字符串地址
    11fd:  sub    $0x8,%rsp
    1201:  call   1080 <puts@plt>          # printf("before trap\n")
    1206:  ud2                             # ← __builtin_trap() = 一条指令
    1208:  nopl   ...
$ nm -u t6.o        # ← 没有 __builtin_trap 符号
```

| 特性 | 普通函数 | 内建函数 |
|------|---------|----------|
| 符号表 | 有（`nm` 可见） | **无**（编译期消解） |
| 链接依赖 | 需要 `.o` / 库 | **无** |
| `#include` | 需要 | **不需要**（编译器直接识别） |
| 常量参数 | 运行时计算 | **可能编译期折叠** |
| 目标指令 | `call func` | **零到多条指令内联展开** |
| `-fno-builtin` | 不影响 | **影响 libc 隐式识别；不影响 `__builtin_*` 显式形式** |

**验证 `__builtin_*` 不受 `-fno-builtin` 影响**：

```c
/* t10_fno_builtin.c 片段 */
size_t my_strlen_const(void)  { return strlen("hello"); }           /* libc 隐式 */
size_t my_builtin_strlen(void) { return __builtin_strlen("hello"); } /* 显式 */
```

```
$ gcc -O2 -fno-builtin -c t10_fno_builtin.c
$ objdump -d t10_nobuiltin.o
<my_strlen_const>:         jmp  strlen@plt     ← libc 被 -fno-builtin 关了
<my_builtin_strlen>:       mov  $0x5,%eax       ← __builtin_ 不受影响
```

**规则**：`-fno-builtin` 关掉的是「编译器把 `strlen`/`memcpy`/`strcmp` 等 libc 函数当 builtin 处理」的隐式行为；显式 `__builtin_strlen` 永远是编译器内建，不受任何 flag 影响。

---

## 二、位操作全家桶：六个函数，五个映射到硬件，一个不是

这是嵌入式和 HFT 里用得最多的一组。先把实测结果列出来：

| 内建函数 | 含义 | gcc -O2 生成（x86-64） | 硬件指令？ | 需要额外 flag？ |
|----------|------|------------------------|-----------|----------------|
| `__builtin_popcount(v)` | 数 1 的个数 | **`call __popcountdi2`** | **否！软件实现** | **`-mpopcnt` 才用 `POPCNT`** |
| `__builtin_popcountll(v)` | 64 位版 | **`call __popcountdi2`** | **否！** | 同上 |
| `__builtin_clz(v)` | 前导零计数 | `bsr %edi,%eax; xor $0x1f,%eax` | 是 | 否 |
| `__builtin_ctz(v)` | 尾随零计数 | `tzcnt %edi,%eax` | 是（BMI1） | 否 |
| `__builtin_ffs(v)` | 最低位 1 的位置（1-based） | `bsf %edi,%eax; cmove; add $1` | 是 | 否 |
| `__builtin_parity(v)` | 奇偶校验（1 的个数 mod 2） | `shr; xor; xor; setnp` | 是（用 PF 标志位） | 否 |

### 2.1 popcount 的陷阱

这是**本节最反直觉的发现**。很多人写 `__builtin_popcount` 时以为它在 x86 上一定生成 `POPCNT` 指令——**错**。

```
$ gcc -std=gnu11 -O2 -c t13_popcnt.c -o t13_def.o
$ objdump -d t13_def.o | sed -n '/<do_popcount_default>:/,/^\$/p'
0000000000000000 <do_popcount_default>:
   0:  endbr64
   4:  sub    $0x8,%rsp
   8:  mov    %edi,%edi
   a:  call   f <do_popcount_default+0xf>     # ← 调 __popcountdi2！
   f:  add    $0x8,%rsp
  13:  ret

$ gcc -std=gnu11 -O2 -mpopcnt -c t13_popcnt.c -o t13_popcnt.o
$ objdump -d t13_popcnt.o | sed -n '/<do_popcount_default>:/,/^\$/p'
0000000000000000 <do_popcount_default>:
   0:  endbr64
   4:  xor    %eax,%eax
   6:  popcnt %edi,%eax                        # ← 硬件指令！
   a:  ret
```

**`__popcountdi2` 是什么**？它是 libgcc 的运行时函数，用 SWAR（SIMD Within A Register）算法软件计算 popcount：

```
__popcountdi2:
    movabs $0x5555555555555555,%rdx    # 0x55 = 01010101...
    mov    %rdi,%rax
    shr    $1,%rax
    and    %rdx,%rax                    # 每 2 位一组
    sub    %rax,%rdi
    movabs $0x3333333333333333,%rax     # 0x33 = 00110011...
    ...（共 13 条指令）
    imul   %rdx,%rax
    shr    $0x38,%rax
    ret
```

**总结**：

| 编译选项 | `__builtin_popcount` 生成的代码 | 开销 |
|---------|-------------------------------|------|
| `-O2`（默认） | `call __popcountdi2` | 函数调用 + 13 条 SWAR 指令 |
| `-O2 -mpopcnt` | `popcnt %edi,%eax` | **1 条指令** |
| `-O2 -march=native` | `popcnt %edi,%eax` | 1 条指令（CPU 支持 POPCNT 时） |

> **HFT/嵌入式教训**：如果你的热点循环用 `__builtin_popcount` 做位图统计，**一定要在 Makefile 里加 `-mpopcnt`（或 `-march=native`）**，否则你以为的一条指令其实是 14 条指令 + 一次函数调用。内核不担心这个——内核的 `hweight32()` 在编译时根据 `CONFIG_ARCH_HAS_FAST_MULTIPLIER` 等选项选择内联汇编或 builtin。

### 2.2 clang 的 popcount：直接内联 SWAR

clang 不调用 `__popcountdi2`，而是**直接内联** SWAR 算法（不生成 `popcnt` 指令，但也不生成 `call`）：

```
$ clang -std=gnu11 -O2 -c t1_bitops.c -o t1_clang
$ objdump -d t1_clang | sed -n '/<do_popcount>:/,/^\$/p'
0000000000001150 <do_popcount>:
    1150:  mov    %edi,%eax
    1152:  shr    $1,%eax
    1154:  and    $0x55555555,%eax
    1159:  sub    %eax,%edi
    ...（内联 SWAR，约 12 条指令）
    1182:  ret
```

clang 也不加 `-mpopcnt` 就不生成 `popcnt` 指令。但至少它内联了，省了函数调用开销。加了 `-mpopcnt` 后 clang 也用 `popcnt` 指令。

### 2.3 clz/ctz/ffs/parity：直接硬件

这四个不加任何 flag 就映射到硬件指令：

```c
int do_clz(uint32_t v)     { return __builtin_clz(v); }
int do_ctz(uint32_t v)     { return __builtin_ctz(v); }
int do_ffs(uint32_t v)     { return __builtin_ffs(v); }
int do_parity(uint32_t v)  { return __builtin_parity(v); }
```

```
do_clz:    bsr    %edi,%eax ; xor $0x1f,%eax        # Bit Scan Reverse + 补码
do_ctz:    tzcnt  %edi,%eax                         # Trailing Zero Count (BMI1)
do_ffs:    bsf    %edi,%eax ; cmove ; add $1        # Bit Scan Forward + 空值保护 + 1-based
do_parity: shr $16 ; xor ; xor %ah,%al ; setnp %al  # 用 x86 PF（奇偶）标志位！
```

| 函数 | 对应 x86 指令 | 输入为 0 的行为 |
|------|-------------|----------------|
| `__builtin_clz(0)` | `bsr 0` → `ZF=1`，`xor $0x1f` 后返回 32 | **UB**（返回 32，但标准未定义） |
| `__builtin_ctz(0)` | `tzcnt 0` → 返回 32（BMI1 语义） | BMI1 下返回 32（非 UB）；无 BMI1 时 `bsf 0` → **UB** |
| `__builtin_ffs(0)` | `bsf 0` + `cmove $-1` → 返回 0 | 安全：返回 0 |
| `__builtin_parity(0)` | 算出 0 → `setnp` → 0 | 安全 |

> **安全规则**：`__builtin_ffs(0)` 返回 0（安全）；`__builtin_clz(0)` 和 `__builtin_ctz(0)` 是 **UB**。内核 `fls()` 在 `include/asm-generic/bitops/__fls.h` 中先检查 `v == 0` 再调用 `__builtin_clz`。

### 2.4 clang 对比：生成完全相同的指令

```
$ objdump -d t1_clang | sed -n '/<do_clz>:/,/^\$/p'
do_clz:    bsr    %edi,%eax ; xor $0x1f,%eax        # 与 gcc 完全相同
do_ctz:    tzcnt  %edi,%eax                         # 与 gcc 完全相同
do_ffs:    bsf    %edi,%ecx ; cmove ; inc           # clang 用 inc 替代 add 1
do_parity: shr $16 ; xor ; xor %ch,%cl ; setnp      # 等价但寄存器分配不同
```

除了 popcount（gcc 调函数、clang 内联），其他五个位的 builtin 在两个编译器上生成的指令**本质相同**。

### 2.5 宽度变体

| 变体 | 操作宽度 | 示例 |
|------|---------|------|
| `__builtin_popcount` | `unsigned int`（通常 32 位） | `__builtin_popcount(0xFF)` → 8 |
| `__builtin_popcountl` | `unsigned long`（LP64 = 64 位） | `__builtin_popcountl(0xFFFFFFFFFFFFFFFFL)` → 64 |
| `__builtin_popcountll` | `unsigned long long`（64 位） | `__builtin_popcountll(1ULL << 63)` → 1 |
| `__builtin_clz` / `clzl` / `clzll` | 32/64/64 | 同上 |
| `__builtin_ctz` / `ctzl` / `ctzll` | 32/64/64 | 同上 |
| `__builtin_ffs` / `ffsl` / `ffsll` | 32/64/64 | 同上 |

> 内核用 `__builtin_clzl` 居多（`unsigned long` 在 LP64 = 64 位，一次处理一个 `unsigned long` 的位图字）。

---

## 三、字节序：bswap 三件套

```c
uint16_t do_bswap16(uint16_t v) { return __builtin_bswap16(v); }
uint32_t do_bswap32(uint32_t v) { return __builtin_bswap32(v); }
uint64_t do_bswap64(uint64_t v) { return __builtin_bswap64(v); }
```

```
do_bswap16:  rol $0x8,%ax          # x86 没有 bswap16，用 rotate left 8
do_bswap32:  bswap %eax            # 硬件 bswap 指令
do_bswap64:  bswap %rax            # 硬件 bswap 指令（64 位版）
```

| 函数 | 生成指令 | 说明 |
|------|---------|------|
| `__builtin_bswap16` | `rol $8, %ax` | 16 位没有专用 bswap，用 8 位旋转 |
| `__builtin_bswap32` | `bswap %eax` | 专用指令 |
| `__builtin_bswap64` | `bswap %rax` | 专用指令 |

gcc 和 clang 生成**完全相同**的指令。常量参数时编译期折叠：`__builtin_bswap32(0xAABBCCDDu)` → `0xDDCCBBAA`。

> **内核**：`include/linux/byteorder/generic.h` 在大端机上 `#define cpu_to_le32(x) ((__force __le32)__builtin_bswap32(x))`，小端机上 `#define cpu_to_le32(x) ((__force __le32)(x))`（no-op）。

---

## 四、`__builtin_constant_p`：编译期常量检测器

```c
int r1 = __builtin_constant_p(5);           // → 1
int r2 = __builtin_constant_p(volatile_var); // → 0
```

| 表达式 | `-O2` 返回 | `-O0` 返回 | 说明 |
|--------|-----------|-----------|------|
| `__builtin_constant_p(5)` | 1 | 1 | 字面量永远是常量 |
| `__builtin_constant_p(volatile v)` | 0 | 0 | volatile 永远不是常量 |
| `__builtin_constant_p(static const g)` | 1 | **0** | static const 在 -O0 下不识别 |
| `__builtin_constant_p(&local)` | **0** | 0 | 栈地址不是编译期常量 |
| `__builtin_constant_p(local)` | **1** | 0 | local=10 未被修改，-O2 能推断 |
| `__builtin_constant_p(n)` (运行时变量) | 0 | 0 | 运行时变量 |

### 4.1 `-O0` 的陷阱

```
=== gcc -O2 ===
constant_p(5) = 1
constant_p(volatile v) = 0
constant_p(static const g) = 1      ← -O2 识别
constant_p(&local) = 0
constant_p(local) = 1               ← -O2 推断出 local=10

=== gcc -O0 ===
constant_p(5) = 1
constant_p(volatile v) = 0
constant_p(static const g) = 0      ← -O0 不识别！
constant_p(&local) = 0
constant_p(local) = 0               ← -O0 不推断！
```

**根因**：`__builtin_constant_p` 依赖编译器的常量传播（constant propagation）能力。`-O0` 不做任何优化，不做常量传播，所以除了字面量以外几乎全返回 0。

> **工程教训**：如果你写了一个用 `__builtin_constant_p` 做编译期分流的宏，在 `-O0 -g` 调试模式下**走的永远是 else 分支**。内核永远 `-O2` 编译，不会遇到；但用户态调试时要注意。

### 4.2 内核的用法

内核用 `__builtin_constant_p` 实现 memset/memcpy 的编译期大小分流：

```c
/* 简化版，实际在 include/linux/string.h / lib/string.c */
#define __memset(s, c, n) ({                              \
    size_t _n = (n);                                      \
    if (__builtin_constant_p(_n) && _n <= 16) {           \
        /* 编译器内联展开：几条 mov 指令 */               \
        char *_p = (s);                                    \
        switch (_n) {                                      \
        case 16: _p[15] = c;                              \
        /* ... fall through ... */                         \
        case 1:  _p[0] = c;                               \
        }                                                 \
    } else {                                               \
        __real_memset(s, c, _n);  /* 调库函数 */            \
    }                                                     \
})
```

实测 `my_memset(buf, 'X', 5)` 在 `-O2` 下生成：

```
main:
    movl   $0x58585858,(%rsp)        # 4 个 'X'
    movb   $0x58,0x4(%rsp)           # 1 个 'X' = 共 5 字节
    # 无 call 指令！
```

而 `my_memset(buf, 'Y', n)`（n 是运行时变量）生成 `call memset@plt`。

### 4.3 `__builtin_choose_expr`

```c
int y = __builtin_choose_expr(
    __builtin_constant_p(x),    // 编译期条件
    x * 2,                      // 真：编译期保留
    -1                          // 假：编译期丢弃
);
```

`__builtin_choose_expr` 像 `?:`，但**在编译期选择**——非选中分支的代码**完全不生成**。与 `__builtin_constant_p` 配合，可以在宏里写「编译期分流」。

实测：

```
choose_expr(var x)  = -1     ← x 是运行时变量 → 走 else
choose_expr(const 10) = 20   ← 10 是常量 → 走 then，10*2=20
```

> **gcc 和 clang 都支持 `__builtin_choose_expr`**（实测确认）。

---

## 五、`__builtin_expect`：分支布局，不是分支预测

```c
#define likely(x)   __builtin_expect(!!(x), 1)
#define unlikely(x) __builtin_expect(!!(x), 0)
```

书里说了「告诉编译器分支大概率方向」，但**没展示汇编**。实测一目了然：

### 5.1 不标 expect

```c
int check_plain(int *p) {
    if (!p) return -1;     // 没标
    return *p;
}
```

```
check_plain:
    test   %rdi,%rdi        # 检查 NULL
    je     .Lerr            # 为空 → 跳错误路径
    mov    (%rdi),%eax      # 成功路径（fall-through）
    ret
.Lerr:
    mov    $0xffffffff,%eax # 错误路径
    ret
```

gcc 猜对了：成功路径在 fall-through 位置。

### 5.2 标 `unlikely`（正确方向）

```c
int check_unlikely(int *p) {
    if (__builtin_expect(!p, 0)) return -1;  // !p 大概率为 0（假）
    return *p;
}
```

```
check_unlikely:
    test   %rdi,%rdi
    je     .Lerr              # 错误跳走
    mov    (%rdi),%eax        # 成功路径（fall-through）
    ret
    nopl   0x0(%rax)          # ← padding！把错误路径推远
.Lerr:
    mov    $0xffffffff,%eax
    ret
```

与 plain 版的差异：**多了 padding**（`nopl`），把错误路径推到更远的地址。icache 中 hot path 更紧凑。

### 5.3 标 `likely`（故意标反方向）

```c
int check_likely(int *p) {
    if (__builtin_expect(!p, 1)) return -1;  // 告诉编译器 !p 大概率为 1（真）= p 大概率为 NULL
    return *p;
}
```

```
check_likely:
    mov    $0xffffffff,%eax   # ← 预先加载错误返回值！
    test   %rdi,%rdi
    jne    .Lok               # p != NULL → 跳到成功路径
    ret                        # p == NULL → 返回 -1（fall-through）
.Lok:
    mov    (%rdi),%eax
    ret
```

**完全反转**：错误路径变成了 fall-through（hot），成功路径变成了跳转目标（cold）。如果实际运行中 `p` 经常非 NULL，**每次都要跳转**——icache miss。

### 5.4 clang 的行为

clang 生成的指令**完全相同**（`test/je/mov/ret`），但布局顺序一致。说明 `__builtin_expect` 对两个编译器的作用机制相同：**重排基本块**，不改指令选择。

### 5.5 `!!` 归一化

```c
#define likely(x)   __builtin_expect(!!(x), 1)
```

`!!(x)` 把任意类型归一化为 0/1。为什么？因为 `__builtin_expect` 的第二参数是 `long`，如果 `x` 是指针或浮点，直接传会出类型问题。`!!` 强制转 int 0/1。

实测：`likely(NULL)` → 0、`likely(42)` → 1、`likely((void*)0)` → 0。归一化生效。

### 5.6 使用准则

| 准则 | 说明 |
|------|------|
| **profile 验证** | 先用 `perf record` / `gcov` 确认分支偏斜度。50/50 分支不要标 |
| **仅热路径** | 冷函数里标 likely 毫无意义 |
| **猜错代价** | 5-20% 性能损失（icache miss） |
| **替代方案** | PGO（`-fprofile-generate`/`-fprofile-use`）让编译器自己学 |

> 详见 [6.11.5](6.11.5-内建函数-__builtin_expect-exp-c.md) 和 [6.11.6 内核中的 likely/unlikely](6.11.6-Linux内核中的likely和unlikely.md)。

---

## 六、`__builtin_prefetch`：缓存预取

```c
__builtin_prefetch(ptr, rw, locality);
```

| 参数 | 取值 | 含义 | x86 生成指令 |
|------|------|------|-------------|
| `rw` | 0 | 读预取 | `prefetcht0` / `prefetchnta` / `prefetcht1` / `prefetcht2` |
| `rw` | 1 | 写预取 | `prefetcht0`（x86 不区分读写预取） |
| `locality` | 0 | 无时间局部性 | `prefetchnta`（Non-Temporal Access，不污染 cache） |
| `locality` | 1 | 低时间局部性 | `prefetcht2`（L2） |
| `locality` | 2 | 中时间局部性 | `prefetcht1`（L1） |
| `locality` | 3 | 高时间局部性 | `prefetcht0`（all levels） |

实测 `demo_prefetch_args`：

```
demo_prefetch_args:
    prefetchnta (%rdi)        # (p, 0, 0) → Non-Temporal
    prefetcht2 (%rdi)         # (p, 0, 1) → L2
    prefetcht1 (%rdi)         # (p, 0, 2) → L1
    prefetcht0 (%rdi)         # (p, 0, 3) → all levels
    prefetcht0 0x4(%rdi)      # (p+1, 1, 3) → write, all levels
    ret
```

### 6.1 在 `-O0` 下也生成真指令

```
$ gcc -O0 -c t5_prefetch.c -o t5_O0.o
$ objdump -d t5_O0.o | sed -n '/<prefetch_at_O0>:/,/^\$/p'
prefetch_at_O0:
    push   %rbp
    mov    %rsp,%rbp
    mov    %rdi,-0x8(%rbp)
    mov    -0x8(%rbp),%rax
    prefetcht0 (%rax)          # ← 真指令！
    nop
    pop    %rbp
    ret
```

与 `__builtin_constant_p` 在 `-O0` 下失效不同，`__builtin_prefetch` 在任何优化级别下都生成真实预取指令。

### 6.2 链表遍历中的实际效果

```c
int sum_with_prefetch(struct node *head) {
    int sum = 0;
    struct node *p = head;
    while (p) {
        __builtin_prefetch(p->next, 0, 3);  // 预取下一个节点
        sum += p->val;
        p = p->next;
    }
    return sum;
}
```

```
sum_with_prefetch:
    xor    %eax,%eax
    test   %rdi,%rdi
    je     .Ldone
.Lloop:
    mov    %rdi,%rdx
    mov    0x8(%rdi),%rdi      # 加载 p->next
    add    (%rdx),%eax          # sum += p->val
    prefetcht0 (%rdi)           # ← 预取 p->next 指向的数据
    test   %rdi,%rdi
    jne    .Lloop
    ret
```

编译器把 `prefetch` 指令插在 `p->next` 加载之后、`p = p->next` 赋值之前——让 CPU 在处理当前节点时**后台预取下一个节点的 cache line**。

---

## 七、控制流内建：trap / unreachable / abort

| 内建函数 | 作用 | gcc 生成 | clang 生成 |
|---------|------|---------|-----------|
| `__builtin_trap()` | 触发非法指令异常 | `ud2` | `ud2` |
| `__builtin_unreachable()` | 告诉编译器此路径不可达 | **无指令**（后续代码被删除） | **无指令** |
| `__builtin_abort()` | 等价于 `abort()` | `call abort@plt` | `call abort@plt` |

### 7.1 `__builtin_trap` → `ud2`

```
do_trap:
    push   %rax
    lea    "before trap\n",%rdi
    call   puts@plt
    ud2                          # ← __builtin_trap() = 一条指令，触发 SIGILL
    # "after trap" 那行 printf 被编译器删了（死代码消除）
```

`ud2`（0F 0B）是 x86 的「未定义指令」，CPU 执行到它时触发 `SIGILL`（非法指令信号）。ARM 上等价物是 `UDF` 或 `BKPT`。

### 7.2 `__builtin_unreachable` → 什么也不生成

```c
int do_unreachable(int x) {
    switch (x) {
    case 0:  return 0;
    case 1:  return 1;
    case 2:  return 2;
    }
    __builtin_unreachable();  // 告诉编译器 x 只会是 0/1/2
}
```

```
do_unreachable:
    mov    %edi,%eax           # 直接返回 x（编译器知道只可能是 0/1/2）
    ret
    # 没有 switch 跳转表！编译器内联了所有 case。
```

### 7.3 骗编译器的代价

```c
int lie_to_compiler(int x) {
    if (x > 0) return x;
    __builtin_unreachable();  // "x 永远 > 0"
}
```

```
lie_to_compiler:
    mov    %edi,%eax           # 直接返回 x
    ret
```

编译器删除了 `x <= 0` 分支。如果你传 `lie_to_compiler(-5)`，返回的是 `%edi` 寄存器里的值（即 -5 本身）——**但标准说是 UB，什么结果都可能**。

> **内核用法**：`BUG()` → `__builtin_trap()`；`BUG_ON(cond)` → `if (cond) __builtin_trap()`。`unreachable()` 用于标注不可能到达的 default 分支。见 `include/asm-generic/bug.h`。

---

## 八、libc 折叠：`strlen("hello")` → `mov $5, %eax`

gcc 默认把很多 libc 函数当作 builtin 处理——**当参数是编译期常量时，直接折叠成常量**。

### 8.1 折叠演示

```c
size_t do_strlen_const(void)  { return strlen("hello"); }  // 常量参数
size_t do_strlen_rt(const char *s) { return strlen(s); }  // 运行时参数
```

```
=== gcc -O2 ===
do_strlen_const:
    mov    $0x5,%eax           # ← strlen("hello") 编译期折叠成 5
    ret

do_strlen_rt:
    jmp    strlen@plt          # ← 运行时参数 → 调真实函数
```

**同理**：

| libc 函数 | 常量参数（-O2） | 运行时参数 |
|----------|----------------|-----------|
| `strlen("hello")` | `mov $5, %eax` | `jmp strlen@plt` |
| `memcpy(dst, src, 5)` | 5 条 `mov` 指令内联 | `jmp memcpy@plt` |
| `memcpy(dst, src, 256)` | 16 条 `movups`（SSE 128 位） | `jmp memcpy@plt` |
| `strcmp("abc","abd")` | `mov $-1, %eax` | `jmp strcmp@plt` |
| `memset(buf, 0, 16)` | 1 条 `movups %xmm0,(%rdi)` | `jmp memset@plt` |

### 8.2 `-fno-builtin` 的效果

```
=== gcc -O2 -fno-builtin ===
my_strlen_const:
    lea    "hello",%rdi
    jmp    strlen@plt          # ← 不再折叠！调真实函数

my_builtin_strlen:
    mov    $0x5,%eax           # ← __builtin_strlen 不受影响
    ret
```

**三层对照**：

| 标志 | `strlen("hello")` | `__builtin_strlen("hello")` | `memcpy(dst, src, 5)` |
|------|-------------------|---------------------------|----------------------|
| `-O2`（默认） | `mov $5, %eax` | `mov $5, %eax` | 5 条 `mov` 内联 |
| `-O2 -fno-builtin` | `jmp strlen@plt` | **`mov $5, %eax`** | 5 条 `mov` 内联 |
| `-O2 -fno-builtin-strlen` | `jmp strlen@plt` | **`mov $5, %eax`** | 5 条 `mov` 内联 |

**关键**：`-fno-builtin` 关掉的是**隐式识别**（编译器看到 `strlen` 就当 builtin）；`__builtin_strlen` 是**显式形式**，永远不被 `-fno-builtin` 关掉。`memcpy` 的内联属于 tree-ssa 的 `BUILTIN_MEMCPY` 优化路径，与 `-fno-builtin` 独立——实测在 `-fno-builtin` 下 memcpy 仍然内联。

### 8.3 `-ffreestanding` 与内核

```
$ gcc -std=gnu11 -O2 -ffreestanding -c t7_libc.c -o t7_free.o
# 编译成功，无报错
```

`-ffreestanding` 告诉编译器「不假设有 hosted 环境（没有标准库）」。但它**不关掉 `__builtin_*`**——因为内建函数是编译器自带的，不依赖 libc。

内核 Makefile 里 `-ffreestanding` 一直开着，`__builtin_*` 照用。但 libc 隐式识别在 `-ffreestanding` 下会被关掉——内核自己实现了 `memcpy`/`memset`/`strlen` 等，不需要编译器隐式折叠。

### 8.4 C23：`__builtin_stdc_*` 标准化

C23（ISO/IEC 9899:2024）把一组 freestanding 安全的 libc 函数标准化为内建函数，前缀 `__builtin_stdc_`：

| C23 内建 | 等价 libc |
|---------|----------|
| `__builtin_stdc_memcmp` | `memcmp` |
| `__builtin_stdc_memcpy` | `memcpy` |
| `__builtin_stdc_memmove` | `memmove` |
| `__builtin_stdc_memset` | `memset` |
| `__builtin_stdc_strlen` | `strlen` |
| `__builtin_stdc_strcmp` | `strcmp` |
| `__builtin_stdc_strchr` | `strchr` |

这些在 freestanding 模式下也可用，且不受 `-fno-builtin` 影响。GCC 14+ 和 Clang 19+ 支持。当前 WSL gcc 13.3 尚未完全支持，但 `__builtin_memset` 等旧形式仍可用。

---

## 九、溢出检查：`__builtin_add_overflow` 等

```c
int __builtin_add_overflow(T1 a, T2 b, T3 *res);  // *res = a + b，返回是否溢出
int __builtin_sub_overflow(T1 a, T2 b, T3 *res);
int __builtin_mul_overflow(T1 a, T2 b, T3 *res);
```

### 9.1 硬件映射

```c
int safe_add(int a, int b, int *result) {
    return __builtin_add_overflow(a, b, result);
}
int safe_mul(int a, int b, int *result) {
    return __builtin_mul_overflow(a, b, result);
}
int safe_add_u(uint32_t a, uint32_t b, uint32_t *r) {
    return __builtin_add_overflow(a, b, r);
}
```

```
safe_add:         add %esi,%edi ; mov %edi,(%rdx) ; seto %al ; ret
                  #                              ^^^ 溢出标志 → 布尔

safe_mul:         imul %esi,%edi ; mov %edi,(%rdx) ; seto %al ; ret
                  #                               ^^^ 溢出标志

safe_add_u:       add %esi,%edi ; mov %edi,(%rdx) ; setb %al ; ret
                  #                              ^^^ 进位标志（无符号）
```

| 操作类型 | 溢出检测指令 | x86 标志 |
|---------|-------------|---------|
| 有符号加法 | `seto` | OF（Overflow Flag） |
| 有符号乘法 | `seto` | OF |
| 无符号加法 | `setb` | CF（Carry Flag） |
| 无符号乘法 | `setb` / `seto`（取决于类型宽度） | CF 或 OF |

**两条指令搞定**——比手写检查（`if (a > INT_MAX - b)`）更短、更安全、更优化。

### 9.2 宽度截断检测

```c
uint8_t r8;
int ovf = __builtin_add_overflow(300, 300, &r8);
// → ovf=1, r8 = (uint8_t)(600 % 256) = 88
```

当结果类型比操作数窄时，builtin 会自动做宽度截断并正确检测溢出。

### 9.3 常量折叠

当操作数是编译期常量时，结果在编译期确定：

```
test_all_overflow:
    ...
    mov    $0x1,%edx           # ← INT_MAX+1 overflow=1（编译期常量）
    mov    $0x80000000,%ecx    # ← result=INT_MIN（编译期常量）
    ...
```

编译器在编译期就算出了 `INT_MAX + 1` 溢出、结果为 `INT_MIN`，直接嵌入了常量。

> **内核**：`include/linux/overflow.h` 定义了 `check_add_overflow(a, b, d)` → `__builtin_add_overflow(a, b, d)`。GCC 5+ 和 Clang 3.5+ 支持。

---

## 十、类型与对齐内建

### 10.1 `__builtin_types_compatible_p`

```c
__builtin_types_compatible_p(type1, type2)  // 编译期返回 0 或 1
```

实测：

| 检查 | 返回 | 说明 |
|------|------|------|
| `types_compatible_p(int, int)` | 1 | 同类型 |
| `types_compatible_p(int, unsigned)` | 0 | signed ≠ unsigned |
| `types_compatible_p(int, long)` | 0 | int ≠ long（LP64） |
| `types_compatible_p(struct A, struct B)` | 0 | 不同结构体（即使布局相同） |
| `types_compatible_p(char*, const char*)` | **0** | 顶层 const 使类型不兼容 |

> **注意**：C 标准里 `char*` 和 `const char*` 在某些上下文里是「兼容」的（例如赋值），但 `__builtin_types_compatible_p` 按**严格类型规则**返回 0。gcc 和 clang 行为一致。

内核用 `typeof` + `__builtin_types_compatible_p` 做 `min()`/`max()` 宏的类型检查（`include/linux/minmax.h`）。

### 10.2 `__builtin_assume_aligned`

```c
int *ap = __builtin_assume_aligned(p, 16);
```

告诉编译器「`p` 至少 16 字节对齐」，编译器据此生成向量化指令（SSE/AVX）。

实测 `sum_aligned` vs `sum_plain` 在当前 gcc 版本下生成的代码**相同**（因为简单标量循环没有向量化机会）。但如果循环体适合向量化（如 `s += p[i] * 2`），`assume_aligned` 版本会使用 `movdqa`（对齐加载），而 plain 版本用 `movdqu`（未对齐加载，慢）。

### 10.3 `__builtin_offsetof`

```c
struct S { char a; int b; char c; };
size_t off = __builtin_offsetof(struct S, b);  // → 4
```

等价于标准 `<stddef.h>` 的 `offsetof`，但 `__builtin_offsetof` 是编译器内建，不需要 `#include`。

---

## 十一、GCC vs Clang 支持矩阵

| 内建函数 | gcc | clang | 说明 |
|---------|-----|-------|------|
| `__builtin_popcount/ll` | ✅ | ✅ | 但不加 `-mpopcnt` 都不生成 `popcnt` 指令 |
| `__builtin_clz/ctz/ffs/parity` | ✅ | ✅ | 指令完全一致 |
| `__builtin_bswap16/32/64` | ✅ | ✅ | 指令完全一致 |
| `__builtin_constant_p` | ✅ | ✅ | `-O0` 行为一致（都几乎全返回 0） |
| `__builtin_expect` | ✅ | ✅ | 基本块布局一致 |
| `__builtin_prefetch` | ✅ | ✅ | 指令一致 |
| `__builtin_trap` | ✅ | ✅ | 都生成 `ud2` |
| `__builtin_unreachable` | ✅ | ✅ | 都删除死代码 |
| `__builtin_abort` | ✅ | ✅ | 都调用 `abort@plt` |
| `__builtin_add/sub/mul_overflow` | ✅ | ✅ | 都用 `seto`/`setb` |
| `__builtin_types_compatible_p` | ✅ | ✅ | 行为一致 |
| `__builtin_choose_expr` | ✅ | ✅ | 行为一致 |
| `__builtin_assume_aligned` | ✅ | ✅ | |
| `__builtin_offsetof` | ✅ | ✅ | |
| `__builtin_strlen/memcpy/memset` | ✅ | ✅ | libc 内建 |
| `__builtin_stdc_*` (C23) | GCC 14+ | Clang 19+ | 尚未在当前环境测试 |
| `__builtin_return_address` | ✅ | ✅ | 内核用（见下文） |
| `__builtin_frame_address` | ✅ | ✅ | |

> **结论**：在当前 gcc 13.3 / clang 18.1 测试范围内，**核心内建函数的支持矩阵完全一致**。差异主要在 popcount 的 fallback 策略（gcc 调函数、clang 内联）。

---

## 十二、`__builtin_return_address`：调用链追踪

```c
void *__builtin_return_address(unsigned int level);
```

返回 `level` 层调用者的返回地址。`level=0` 是当前函数的调用者，`1` 是调用者的调用者，以此类推。

| level | 返回 | 可靠性 |
|-------|------|--------|
| 0 | 直接调用者的返回地址 | **可靠** |
| 1 | 祖父调用者 | 依赖栈帧完整性 |
| ≥ 2 | 更深层 | **不可靠**（可能因优化/内联导致栈帧不存在） |

> **内核**：`include/linux/kallsyms.h` 用 `__builtin_return_address(0)` 做 stack trace。`dump_stack()` 底层也依赖它。**使用前提**：函数不能被内联（否则没有独立栈帧）。

```c
/* 内核 dump_stack 简化版 */
void dump_stack(void) {
    unsigned long sp = (unsigned long)__builtin_frame_address(0);
    void *ra = __builtin_return_address(0);
    printk("Call trace:\n");
    printk("  [<%p>] %pS\n", ra, ra);   /* %pS = 打印符号名 */
    /* 继续 unwind... */
}
```

`__builtin_frame_address(level)` 类似，返回第 `level` 层栈帧的基地址。

---

## 十三、速查表

### 13.1 按场景分类

| 场景 | 推荐内建 | 说明 |
|------|---------|------|
| 分支布局 | `likely()` / `unlikely()` | 错误检查标 unlikely，成功路径标 likely |
| 编译期常量分流 | `__builtin_constant_p` + `__builtin_choose_expr` | 小尺寸内联、大尺寸调库 |
| 位图统计 | `__builtin_popcount`（**记得 `-mpopcnt`**） | |
| 对齐/桶排序 | `__builtin_clz` / `ctz` | 注意 0 输入是 UB |
| 网络字节序 | `__builtin_bswap32/64` | |
| 链表预取 | `__builtin_prefetch(p->next, 0, 3)` | |
| 断言/panic | `__builtin_trap()` → `ud2` | |
| 不可达标注 | `__builtin_unreachable()` | 消除 default 分支 |
| 溢出检查 | `__builtin_add/sub/mul_overflow` | 比手写检查快 |
| 调用链追踪 | `__builtin_return_address(0)` | 调试/panic 栈打印 |
| 类型检查 | `__builtin_types_compatible_p` | 泛型宏类型安全 |
| 对齐假设 | `__builtin_assume_aligned` | 允许向量化 |

### 13.2 编译选项交互

| 选项 | 影响 |
|------|------|
| `-O0` | `__builtin_constant_p` 几乎全返回 0；`__builtin_prefetch` 仍生成真指令 |
| `-O2` | `__builtin_constant_p` 可以推断 `static const`、未修改的局部变量 |
| `-fno-builtin` | 关掉 libc 隐式识别（`strlen`/`memcpy`/`strcmp`）；**不影响 `__builtin_*` 显式形式** |
| `-fno-builtin-strlen` | 只关 `strlen` 的隐式识别 |
| `-ffreestanding` | 假设无 hosted 环境；不影响 `__builtin_*`；libc 隐式识别关闭 |
| `-mpopcnt` | 让 `__builtin_popcount` 生成 `POPCNT` 硬件指令 |
| `-march=native` | 隐含 `-mpopcnt`（CPU 支持时） |
| `-fprofile-generate/-use` | PGO，替代手写 `likely`/`unlikely` |

---

## 常见陷阱

### 1. 以为 `__builtin_popcount` 一定用硬件指令

**错**。不加 `-mpopcnt`（或 `-march=native`），gcc 调用 `__popcountdi2`（13 条 SWAR 指令 + 函数调用），clang 内联 SWAR（12 条指令）。都不用 `popcnt` 指令。

**解法**：Makefile 里加 `-mpopcnt` 或 `-march=native`。

### 2. 在 `-O0 -g` 调试时宏行为变了

用了 `__builtin_constant_p` 的宏在 `-O0` 下走 else 分支（因为 `__builtin_constant_p` 几乎全返回 0）。

**解法**：知道这是预期的；要测宏行为用 `-O2 -g`。

### 3. `__builtin_clz(0)` / `__builtin_ctz(0)` 是 UB

`__builtin_ffs(0)` 返回 0（安全），但 `clz`/`ctz` 对 0 输入是 UB。内核先检查 `v == 0`。

### 4. `__builtin_unreachable` 骗了编译器

标了 `__builtin_unreachable()` 的路径如果真的被执行，返回值是**寄存器里的垃圾**。编译器还可能基于「不可达」假设做更激进的优化（删除其他分支的检查），导致更严重的 UB。

### 5. `__builtin_expect` 标错方向

`likely` 标在实际冷路径上，编译器把冷路径放 fall-through，hot path 变成跳转——每次都 icache miss。**必须 profile**。

### 6. `-fno-builtin` 不影响 `__builtin_*`

以为 `-fno-builtin` 能关掉 `__builtin_strlen`——**不能**。`-fno-builtin` 只关 libc 隐式识别。要关 `__builtin_*` 没有标准方法（也没必要——它们是编译器内建，不是库函数）。

---

## HFT/嵌入式关联

| 内建 | HFT 场景 | 嵌入式场景 |
|------|---------|-----------|
| `__builtin_expect` | 快速路径标 likely，错误检查标 unlikely | ISR 入口检查 unlikely |
| `__builtin_constant_p` | 日志级别编译期决定（关了就不生成格式串到 `.rodata`） | 配置参数编译期分流 |
| `__builtin_popcount` | 订单位图统计 | 位图 IRQ 掩码统计 |
| `__builtin_clz` | 快速 log2 求桶号 | 优先级编码器 |
| `__builtin_bswap64` | FIX/SBE 字段解包 | 网络包头部解析 |
| `__builtin_prefetch` | 预取下一批订单指针 | DMA 描述符预取 |
| `__builtin_add_overflow` | 下单数量×价格溢出检查 | 计数器溢出检查 |
| `__builtin_trap` | 断言/panic | assert_panic |
| `__builtin_unreachable` | switch case 覆盖标注 | 状态机 default 标注 |

---

## 与后续衔接

- **[6.11.1–6.11.6]**：子节展开每个类别
- **[3.6 内联汇编](../../02-ch2-gnu-c-advanced/3.6-mixed-programming/)**：`__asm__` 与 `__builtin_*` 的关系——有些 builtin 本质上是内联汇编的语法糖（如 `__builtin_prefetch` = `prefetcht0` 指令）
- **[6.10 inline](../../01-ch1-gnu-c-basics/6.10-inline/6.10-内联函数.md)**：`always_inline` + `__builtin_constant_p` = 编译期生成特化版本
- **[6.8 format](../6.8-format/6.8-属性声明-format.md)**：`no_printk` 的 `if (0)` 技巧依赖 `__builtin_constant_p` 判断日志级别
- **[6.1 C 标准与 gnu11](../../01-ch1-gnu-c-basics/6.1-c-standard/6.1-C语言标准和编译器.md)**：`__builtin_*` 在所有方言（c89/c99/c11/gnu11）下都可用——因为双下划线前缀是 ISO C 保留给实现的

- **demo**：`demo/09-builtin/`（含本节 t1–t13 全部源码与 `run.sh`/`run2.sh`）

---

## 代码自测

**题目 1：** 以下代码在 `gcc -O2` 下，`do_popcount` 生成的是 `popcnt` 指令还是软件函数调用？为什么？
```c
int do_popcount(uint32_t v) { return __builtin_popcount(v); }
```

<details>
<summary>参考答案</summary>

**软件函数调用**（`call __popcountdi2`）。因为默认编译选项**不包含 `-mpopcnt`**，gcc 不知道目标 CPU 支持 `POPCNT` 指令，所以回退到 libgcc 的 `__popcountdi2`（SWAR 算法，13 条指令 + 一次函数调用）。

要生成 `popcnt %edi,%eax`（1 条指令），需要加 `-mpopcnt` 或 `-march=native`。这是最常见的 builtin 误解——不是所有 `__builtin_*` 都无条件映射到硬件指令，有些取决于目标架构标志。

</details>

**题目 2：** 以下宏在 `gcc -O0` 和 `gcc -O2` 下行为有何不同？
```c
#define OPT_MEMSET(s, c, n) do {                                  \
    if (__builtin_constant_p(n) && (n) <= 16)                     \
        /* 内联展开 */                                            \
    else                                                          \
        memset(s, c, n);                                         \
} while (0)
```

<details>
<summary>参考答案</summary>

**`-O0`**：`__builtin_constant_p` 几乎对所有非字面量表达式返回 0（包括 `static const` 和赋值后未修改的局部变量）。因此宏**几乎总是走 else 分支**调用 `memset`，即使 `n` 是 `static const int = 5`。

**`-O2`**：编译器做常量传播，能识别 `static const`、未修改的局部变量等为编译期常量。当 `n` 是编译期常量且 ≤ 16 时，走内联展开路径。

**根因**：`__builtin_constant_p` 依赖编译器的常量传播能力，`-O0` 不做任何优化传播。

</details>

**题目 3：** 以下三种方式检查溢出，哪个在 `-O2` 下生成的代码最优？
```c
// A: 手写
if (a > INT_MAX - b) { /* overflow */ }

// B: 编译器扩展
int r;
if (__builtin_add_overflow(a, b, &r)) { /* overflow */ }

// C: 内联汇编
int r, ovf;
asm("add %2, %1\n\tseto %0" : "=r"(ovf), "=r"(r) : "r"(a), "1"(b));
```

<details>
<summary>参考答案</summary>

**B 最优**。`__builtin_add_overflow` 生成 `add; seto; ret`（3 条指令），且编译器理解其语义，可以做常量折叠等优化。手写 A 需要额外比较和分支，且对无符号不适用。内联汇编 C 虽然也能用 `seto`，但编译器不理解 asm 的语义，不能做后续优化（如常量折叠、死代码消除），且不可移植。

实测 `safe_add` 的汇编：`add %esi,%edi; mov %edi,(%rdx); seto %al; ret`——干净利落。

</details>

**题目 4：** `__builtin_unreachable()` 标注的路径如果真的被执行了，会发生什么？
```c
int f(int x) {
    if (x > 0) return x;
    __builtin_unreachable();  // "x 永远 > 0"
}
// 调用：f(-5)
```

<details>
<summary>参考答案</summary>

**UB（未定义行为）**。编译器删除了 `x <= 0` 分支后，`f(-5)` 会直接返回 `%edi` 寄存器的值（即 -5 本身），但这只是巧合——标准说结果是未定义的。更危险的是，编译器可能基于「不可达」假设做更激进的优化，例如删除函数中对 `x <= 0` 的其他检查（如果有的话），导致连锁 UB。

实测汇编：`mov %edi,%eax; ret`——连比较都没了。

</details>
