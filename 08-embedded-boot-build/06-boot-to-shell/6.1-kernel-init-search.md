# 内核怎么找 init：四级尝试与「init= 失败会 panic」

> **本节讲什么：** 从源码层面（`init/main.c` v6.6）讲清 PID 1 是怎么被选出来的，以及为什么 `init=` 写错路径会导致**直接 panic 而不是回退**。
> **前置：** [05-rootfs](../05-rootfs/) · [5.2 initramfs](../05-rootfs/5.2-initramfs.md)
> **动手：** [P5 · B2](../../projects/P5-raspberry-pi-embedded/RASPBERRY-PI5-LABS.md)
> **卡住查书：** MELP Ch13 · Primer Ch6

---

## 源码级流程（v6.6 `init/main.c`）

```
rest_init()  →  kernel_thread(kernel_init)          ← PID 1 诞生（内核线程）
                     │
                     ├─ kernel_init_freeable()      ← 干活：驱动、挂根、开 console
                     │      ├─ do_basic_setup()     （initcall：驱动全在这初始化）
                     │      ├─ wait_for_initramfs()
                     │      ├─ console_on_rootfs()  ← 打开 /dev/console  :1552
                     │      └─ if (init_eaccess("/init") != 0)           :1558
                     │             ramdisk_execute_command = NULL;
                     │             prepare_namespace();   ← 没有 initramfs 才挂真根
                     │
                     └─ kernel_init()               ← 选 PID 1         :1428
                            ① ramdisk_execute_command   （默认 /init）
                            ② execute_command           （init= 指定）
                            ③ CONFIG_DEFAULT_INIT
                            ④ /sbin/init /etc/init /bin/init /bin/sh
                            全失败 → panic
```

---

## 四级尝试（源码 :1462–1499）

```c
/* ① initramfs 的 init（rdinit= 覆盖，默认 "/init"） */
if (ramdisk_execute_command) {
        ret = run_init_process(ramdisk_execute_command);
        if (!ret) return 0;
        pr_err("Failed to execute %s (error %d)\n", ramdisk_execute_command, ret);
}

/* ② init= 命令行指定 */
if (execute_command) {
        ret = run_init_process(execute_command);
        if (!ret) return 0;
        panic("Requested init %s failed (error %d).", execute_command, ret);   /* ★ */
}

/* ③ 编译期默认（CONFIG_DEFAULT_INIT，通常为空） */
if (CONFIG_DEFAULT_INIT[0] != '\0') { ... }

/* ④ 硬编码的四个兜底路径 */
if (!try_to_run_init_process("/sbin/init") ||
    !try_to_run_init_process("/etc/init")  ||
    !try_to_run_init_process("/bin/init")  ||
    !try_to_run_init_process("/bin/sh"))
        return 0;

panic("No working init found.  Try passing init= option to kernel. ...");
```

### 两条反直觉的规则

**规则一：①② 失败的处理完全不同。**

| 层级 | 失败后 | 后果 |
|------|--------|------|
| ① `/init`（initramfs） | **打日志，继续往下试** | 容错，会回退到 ②③④ |
| ② `init=` 指定 | ⭐ **直接 panic** | **不回退！** |
| ④ 四个兜底 | 试完仍失败 → panic | 最终兜底 |

> ⚠️ 这是最容易踩的坑：**`init=/bin/bash` 写错成 `init=/bin/bas`，系统不会"好心"地回退到 `/sbin/init`，而是直接 panic。**
> 因为 `init=` 是用户**显式**要求，内核认为"你要的东西我给不了，给别的没意义"。

**规则二：④ 的兜底里 `/bin/sh` 是故意设计的。**

源码注释写得很直白：

```
* The Bourne shell can be used instead of init if we are
* trying to recover a really broken machine.
```

所以 **内核自带救砖机制**：什么都不做，只要 `/bin/sh` 存在，系统就能起来给你一个 shell。

---

## `run_init_process` vs `try_to_run_init_process`

```c
static int run_init_process(const char *init_filename)
{
        argv_init[0] = init_filename;
        pr_info("Run %s as init process\n", init_filename);
        return kernel_execve(init_filename, argv_init, envp_init);
}

static int try_to_run_init_process(const char *init_filename)
{
        int ret = run_init_process(init_filename);
        if (ret && ret != -ENOENT)
                pr_err("Starting init: %s exists but couldn't execute it (error %d)\n",
                       init_filename, ret);
        return ret;
}
```

| 函数 | 用在 | 区别 |
|------|------|------|
| `run_init_process` | ①②③ | 失败就返回错误码，**不打额外日志** |
| `try_to_run_init_process` | ④ | 额外判断：非 `-ENOENT` 的错误（即"文件存在但执行不了"）会**明确告警** |

⭐ **这个区别是给排错用的**：

- `-ENOENT`（文件不存在）→ 静默，因为"试下一个路径"本来就是预期行为
- 其他错误（`EACCES` 没执行权限、`ENOEXEC` 不是可执行格式、`EFAULT`）→ **明确报 `exists but couldn't execute it`**

**看到 `exists but couldn't execute it` 就该去查权限和架构**，而不是怀疑路径。

---

## `init=` 的一个隐藏行为

```c
static int __init init_setup(char *str)
{
        execute_command = str;
        /* ... 忽略 init= 之前的所有参数 ... */
        for (i = 1; i < MAX_INIT_ARGS; i++)
                argv_init[i] = NULL;
        return 1;
}
__setup("init=", init_setup);
```

**`init=` 之后的所有命令行参数会被清空**（`argv_init[1..]` 置 NULL）。

这意味着：

```
init=/sbin/init foo=bar        → foo=bar 不会传给 init 进程
```

想给 init 传参，要显式写：
```
init=/sbin/init -- arg1 arg2   （不同 init 语法不同）
```

历史原因：LILO 会在 cmdline 前面塞一个 `auto`，内核为了兼容把它清掉。

---

## 常用命令行参数速查

| 参数 | 作用 | 典型用法 |
|------|------|---------|
| `init=` | 指定最终 rootfs 的 PID 1 | `init=/bin/sh`（救砖）· `init=/opt/app`（直跑应用） |
| `rdinit=` | 指定 initramfs 里的 PID 1 | `rdinit=/bin/sh`（排查挂根失败） |
| `root=` | 真根设备 | `root=/dev/mmcblk0p2` · `root=PARTUUID=xxxx-02` |
| `rootfstype=` | 根文件系统类型 | `rootfstype=ext4` |
| `rootwait` | 等根设备出现（SD/USB 必须） | `rootwait` |
| `ro` / `rw` | 只读/可写挂载 | `ro`（配合 initramfs 里 fsck） |

**Pi 5 的 cmdline 在** `/boot/firmware/cmdline.txt`（**必须是一整行，不能换行**）。

---

## 故障对照

| 内核消息 | 含义 | 排查方向 |
|---------|------|---------|
| `Failed to execute /init (error -2)` | initramfs 里没有 `/init` | 打包时路径错了（`find .` 要在根执行） |
| `Requested init /sbin/xxx failed (error -2)` | `init=` 指的文件不存在 | 检查 rootfs 里有没有它；**它会 panic** |
| `Starting init: /sbin/init exists but couldn't execute it (error -8)` | 文件在但**格式不对**（-8 = ENOEXEC） | **架构不对**（编成 x86 了）或不是 ELF |
| `No working init found.` | 四个兜底全失败 | rootfs 里连 `/bin/sh` 都没有 |
| `Warning: unable to open an initial console.` | 缺 `/dev/console` | `mknod dev/console c 5 1` |
| `VFS: Unable to mount root fs on unknown-block(0,0)` | 根设备没找到 | `root=` 写错 / 驱动是模块 |

---

## HFT / 嵌入式关联

- **`init=/opt/app` 直跑交易进程是"去掉一切不必要"的极致形态**：没有 systemd、没有 getty、没有 cron，PID 1 就是你的交易进程。启动时间从秒级降到百毫秒级，**也消除了所有"谁在后台抢 CPU"的不确定性**。代价是失去进程管理 —— 交易进程自己得处理重启与监控。
- **"显式指定失败就 panic，不回退"这个设计值得品味**：它来自"显式意图优先于隐式兜底"的原则。HFT 系统的配置校验也该如此 —— 配置写错应该**拒绝启动**，而不是悄悄用默认值跑出错误的结果。**静默降级在生产系统里是事故来源。**
- `try_to_run_init_process` 区分 `-ENOENT` 与其他错误的做法，是一个**可观测性设计范例**：预期内的失败静默，预期外的失败告警。写自己的重试逻辑时同样该区分"没有"和"坏了"。

---

<details>
<summary>Q1：为什么 <code>init=</code> 失败要 panic，而 <code>/init</code> 失败只是打日志继续？</summary>

语义不同：

- `ramdisk_execute_command`（`/init`）是**内核的默认猜测** —— initramfs 里可能压根没有它（很多 initramfs 靠 `pivot_root` 后由 `init=` 指定）。所以失败是**预期内**的，继续试下一个合理。
- `execute_command`（`init=`）是**用户的显式指令**。如果内核悄悄换一个别的程序跑，你根本不知道"我要的 init 没跑起来"，系统看起来正常但行为完全不对 —— 这比 panic 危险得多。

**原则：隐式兜底可以容错，显式意图失败必须暴露。**
</details>

<details>
<summary>Q2：内核先挂根还是先找 init？</summary>

**先有 rootfs，再找 init。** 但"rootfs"在内核里分两个阶段：

1. 内核一启动就有一个**空的 rootfs**（tmpfs），initramfs 的内容被解压到这里 → 这是 `wait_for_initramfs()` 之后的状态
2. 然后 `init/main.c:1558` 判断：
   - 有 `/init` → 就停在阶段 1，让 `/init` 自己去挂真根并 `pivot_root`
   - 没有 `/init` → 内核自己 `prepare_namespace()` 按 `root=` 挂设备，然后 mount 到 `/`

**所以 `root=` 参数只在"没有 initramfs init"时才由内核直接使用。** 有 initramfs 时，`root=` 只是被 initramfs 里的脚本读走、由脚本决定怎么用。
</details>

<details>
<summary>Q3：<code>kernel_execve</code> 之后那个"内核线程"去哪了？</summary>

它**变成了用户态进程**，PID 仍是 1。

`kernel_execve()` 在内核里构造好 `pt_regs`（用户态入口、栈指针），然后走正常的返回到用户态路径。区别在于：普通 `fork` 出来的进程是从 `ret_from_fork` 返回用户态，而这里是**直接篡改内核线程的执行上下文**，让它"返回"到 `init` 的入口。

**这就是为什么 PID 1 永远不会变（除非它退出 → panic）**：它是从内核第一个线程直接变身的，不是 fork 出来的。
</details>

<details>
<summary>Q4：PID 1 退出了会怎样？</summary>

**内核 panic**：`panic("Attempted to kill init!")`。

因为 PID 1 是所有孤儿进程的"收养者"，它没了整个进程树就没有根。

⭐ 这正是 systemd / supervisor 类进程必须**自己保证不崩**的原因，也是容器里"PID 1 要正确处理信号、要 wait 掉僵尸进程"的根源 —— 普通程序被放进 PID 1 位置会踩一堆坑（比如默认忽略没有 handler 的信号）。
</details>

---

## 衔接

- **同节点下一篇：** [6.2 控制台从哪来](./6.2-console-and-serial.md)
- **上一篇节点：** [05-rootfs](../05-rootfs/)
- **卡住查书：** MELP Ch13–14 · Primer Ch6
