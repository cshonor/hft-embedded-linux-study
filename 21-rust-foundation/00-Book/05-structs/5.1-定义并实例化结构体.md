# 5.1 定义并实例化结构体

结构体（`struct`）与元组类似：都能把多个值组合在一起；不同的是结构体**为每一部分命名字段**，不依赖位置顺序。

对应 demo：[5.1-structs-demo](./5.1-structs-demo/)

---

## 子笔记（按需跳转）

| 文档 | 内容 |
|------|------|
| **[5.1.1 修改字段与可变性](./5.1.1-修改字段与可变性.md)** | `let mut`、承袭可变性、原地改 vs `..` |
| **[5.1.2 结构体更新语法](./5.1.2-结构体更新语法.md)** | `..orig`、部分 move、clone 保留原实例 |
| **[5.1.3 字段所有权与引用](./5.1.3-字段所有权与引用.md)** | `String` vs `&str` 字段、`mut` 换指向 |
| **[5.1.4 Debug / Display 与 Trait](./5.1.4-Debug-Display与Trait约束.md)** | `{:?}` vs `{}`、非鸭子类型、静态约束 |
| **[5.1.5 实体 vs 引用 vs Box](./5.1.5-结构体变量实体与引用.md)** | 变量存整块 struct，不是指针；对照 C |

---

## 定义结构体

```rust
struct User {
    active: bool,
    username: String,
    email: String,
    sign_in_count: u64,
}
```

---

## 实例化与访问

字段顺序不要求与定义一致：

```rust
let user1 = User {
    email: String::from("someone@example.com"),
    username: String::from("someusername123"),
    active: true,
    sign_in_count: 1,
};

println!("{}", user1.email);
```

改字段、更新语法 → 见 **5.1.1 / 5.1.2**（不在此重复）。

---

## 函数返回结构体

```rust
fn build_user(email: String, username: String) -> User {
    User {
        email,
        username,
        active: true,
        sign_in_count: 1,
    }
}
```

**字段初始化简写**：变量名与字段名相同时可写 `email, username`。

---

## 元组结构体（tuple struct）

```rust
struct Color(i32, i32, i32);
struct Point(i32, i32, i32);

let black = Color(0, 0, 0);
```

- `Color` 与 `Point` 是不同类型
- 访问：`black.0`

---

## 类单元结构体（unit-like struct）

```rust
struct AlwaysEqual;
let subject = AlwaysEqual;
```

无字段；常用于实现 trait 但不存数据（第 10 章）。

---

## 记忆卡片

| 要点 | 一句 |
|------|------|
| 定义 | `struct Name { field: Type }` |
| 实例化 | `Name { field: value, ... }` |
| 改字段 | 整实例 `mut` → 5.1.1 |
| `..orig` | 新实例 + 部分 move → 5.1.2 |
| 字段类型 | 拥有用 `String` → 5.1.3 |
