[package]
name = "crates_io_publish_demo"
version = "0.1.0"
edition = "2021"
description = "Learning demo: rustdoc, pub use re-exports, crates.io metadata (for local study only)."
license = "MIT OR Apache-2.0"

[dependencies]
