# 8.2 使用字符串存储 UTF-8 编码的文本

← [8.1 Vector](./8.1-vector.md) · 所有权：[4.1 §String](../04-ownership/4.1-什么是所有权.md)

对应 demo：`00-Book/08-collections/8.2-string-demo/`

---

## 结论（先看这两句）

1. **`str` 是 DST（不定长）**：不能 `let s: str`，要用 **`&str`** 等定长外壳访问。
2. **「不定长」≠「一定在堆」**：`str` 可在**静态只读段、栈、堆**；铁律是**栈上不能直接存不定长类型变量**。

---

## 子笔记（建议阅读顺序）

| 文档 | 内容 |
|------|------|
| **[8.2.1 `str`、DST 与定长规则](./8.2.1-str与DST定长规则.md)** | 定长 vs 不定长；为何只能 `&str` |
| **[8.2.2 不定长数据放哪](./8.2.2-不定长数据放哪.md)** | 静态段 / 栈 / 堆；类比 |
| **[8.2.3 `str` 与 `String` 互转](./8.2.3-str与String互转.md)** | 容器 vs 数据；三条链路；只读与 `&mut str` |
| **[8.2.4 UTF-8 实操](./8.2.4-UTF-8字符串实操.md)** | `push`、拼接、slice、`.chars()` / `.bytes()` |
| **[8.2.5 四种类型底层结构](./8.2.5-四种类型底层结构.md)** | `String` struct vs `&str`/`&[T]` 胖指针 vs `[T;N]` |

---

## 一句话串起来

| 类型 | 栈上 | 数据常见位置 | 所有权 |
|------|------|--------------|--------|
| **`&str`** | 定长胖指针 | 静态 / 栈 / 堆 | 借 |
| **`String`** | 定长头 `{ptr,len,cap}` | 堆（可扩容） | 有 |

---

## demo 与章节对应

| demo 段 | 子笔记 |
|---------|--------|
| §1–§4 新建 / push / 拼接 | [8.2.3](./8.2.3-str与String互转.md) · [8.2.4](./8.2.4-UTF-8字符串实操.md) |
| §5–§7 slice / 遍历 | [8.2.4](./8.2.4-UTF-8字符串实操.md) |
| §8 内存布局 / size_of | [8.2.5](./8.2.5-四种类型底层结构.md) |

```bash
cd 00-Book/08-collections/8.2-string-demo
cargo run
```

下一节：[8.3 HashMap](./8.3-hashmap.md)
