# 8.2.1 `str`、DST 与定长规则

← 主笔记：[8.2 字符串](./8.2-string.md) · 下一篇：[8.2.2 不定长放哪](./8.2.2-不定长数据放哪.md)

---

## 结论：三个核心问题（一次拆干净）

| # | 结论 |
|---|------|
| 1 | **`str` 是 DST**：编译期不知占多大 → **不能** `let s: str` 放栈；只能靠 **`&str` 胖指针**（栈上）间接访问 |
| 2 | **字面量 `"abc"`** 的字节在 **`.rodata` 只读常量段**，既不在栈也不在堆 |
| 3 | 只有 **`String`** 把字符数据放堆；**`&str` 永远是栈上胖指针**，指向别处的 `str` 数据 |

→ 放哪详解：[8.2.2](./8.2.2-不定长数据放哪.md) · `'static` 与 `.rodata`：[8.2.2 §五](./8.2.2-不定长数据放哪.md#五static-与-rodata)

---

## 结论（两句）

1. **`str` 是 DST（不定长）**：不能 `let s: str`，要用 **`&str`** 等定长外壳。
2. **「不定长」≠「一定在堆」** → [8.2.2](./8.2.2-不定长数据放哪.md)

---

## 一、定长 vs 不定长

### 栈的规则

栈上**不能直接放**不定长类型（`str`、`[T]`）当独立变量——编译期不知道占多少字节。

### 定长（可直接放栈）

| 类型 | 说明 |
|------|------|
| `i32`、`bool` | 大小固定 |
| `[u8; 4]` | 长度写死在类型里 |

```rust
let a: i32 = 10;
let buf: [u8; 4] = [0; 4];
```

### 不定长（`str`、`[T]`）

`str` = 连续 UTF-8 字节；不同文本长度不同 → **DST**，与 `[u8]` 同类。

---

## 二、为什么不能用 `let s: str`？

```rust
// let s: str = "abc"; // ❌
```

1. 编译器不知道栈上留多少字节；
2. Rust **禁止**编译期大小未知的类型当栈变量。

**规定：** 必须搭配带长度的引用 → **`&str`**（胖指针：ptr + len，本身定长）。

> **`str` = 不定长毛坯字符块**（尺寸飘忽，没法单独装箱放栈）  
> **`&str` = 固定大小的小纸条**（ptr + len），纸条在栈上，指向别处数据

```rust
let s: &str = "abc"; // ✅ 栈上是「书签」，不是整本书
```

```text
栈:  &str { ptr, len }  ──►  [ a b c ]
```

---

## 三、极简背诵

1. **`str`**：不定长；不能 `let s: str`。
2. **铁律**：栈上不能直接存不定长变量 → 用 `&str`、`String` 等定长外壳。
3. **`&str`**：栈上定长胖指针，指向别处的 `str` 数据。

→ 数据在静态/栈/堆：[8.2.2](./8.2.2-不定长数据放哪.md) · `String`：[8.2.3](./8.2.3-str与String互转.md) · 底层布局：[8.2.5](./8.2.5-四种类型底层结构.md)
