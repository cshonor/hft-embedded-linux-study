# 7.4 使用 `use` 关键字将名称引入作用域

← 路径语法：[7.3](./7.3-路径用于引用模块树中的项.md) · 跨包 `use`：[7.3.1](./7.3.1-跨Package路径与Workspace依赖.md) · 分文件 → [7.5](./7.5-将模块分割进不同文件.md)

对应 demo：`00-Book/07-packages-modules/7.4-use-demo/` · 作用域对照 → `7.5-split-modules-demo/`

## 子笔记

| 文档 | 内容 |
|------|------|
| **本篇（7.4）** | 惯用写法 · `as` · `pub use` · 嵌套路径 · glob |
| **[7.4.1 use 作用域](./7.4.1-use作用域仅限当前文件.md)** | 没有全局 `use`；`lib.rs` 不继承；`pub use` 是模块出口 |
| **[7.4.2 `use` vs `pub use`](./7.4.2-use与pub-use.md)** | 内部简写 vs 重导出；clox `scanner::Token`；与 `mod` 公私统一 |

---

## 一、`use` 核心作用

**`use` = 把完整路径引入当前作用域，生成路径别名，后续简写调用** —— 解决 `crate::xxx::yyy::func()` 全路径冗长的问题。

```rust
// 原始全路径
crate::front_of_house::hosting::add_to_waitlist();

// use 简写
use crate::front_of_house::hosting;
hosting::add_to_waitlist();
```

| 要点 | 说明 |
|------|------|
| **`use` 只改名字可见性** | 不编译代码、不引入文件；文件进模块树靠 **`mod`** |
| **权限** | 路径上任意节点非 `pub`，**无法 `use` 导入** |
| **作用域** | **`use` 仅限当前文件** — 子模块不继承 → [7.4.1](./7.4.1-use作用域仅限当前文件.md) |

---

## 二、Rust 惯用 `use` 编码规范

### 1. 函数：导入父模块，保留 `模块::函数()`

```rust
// ✅ 推荐：一眼看出不是本地函数
use crate::front_of_house::hosting;
hosting::add_to_waitlist();

// ❌ 不推荐：add_to_waitlist() 分不清来源
use crate::front_of_house::hosting::add_to_waitlist;
add_to_waitlist();
```

### 2. 结构体 / 枚举 / 类型：直接导入类型名

```rust
use std::collections::HashMap;
let map = HashMap::new();
```

---

## 三、同名冲突：`as` 别名

场景：不同模块同名类型（最常见 `std::io::Result` vs `std::fmt::Result`）。

**方案 1：导入父模块区分（优先）**

```rust
use std::fmt;
use std::io;

fn a() -> fmt::Result { Ok(()) }
fn b() -> io::Result<()> { Ok(()) }
```

**方案 2：`as` 自定义别名**

```rust
use std::io::Result as IoResult;
fn test() -> IoResult<()> { Ok(()) }
```

---

## 四、`pub use`：重导出（re-export，企业级 lib 必备）

| | 作用域 |
|---|--------|
| 普通 `use` | **仅当前模块**内可用 |
| **`pub use`** | 对外暴露路径，**外部 crate 可直接 `use`** |

```rust
// lib.rs 内部
pub use crate::front_of_house::hosting;

// 外部依赖本包时，不必知道 front_of_house 内部结构
use your_crate::hosting;
hosting::add_to_waitlist();
```

**跨 Package（A 调 B）**：B 的 `lib.rs` 用 `pub use` 导出 API → A 的 `Cargo.toml` 依赖 B → A 里 `use b_crate::xxx`。  
→ 完整链路 [7.3.1](./7.3.1-跨Package路径与Workspace依赖.md) · demo `7.3-cross-package-paths-demo/`

---

## 五、嵌套路径合并导入

同前缀路径合并；`{ self, … }` 中 **`self` = 父模块自身**：

```rust
// 拆分
use std::cmp::Ordering;
use std::io;
use std::io::Write;

// 合并
use std::{cmp::Ordering, io};
use std::io::{self, Write}; // self = std::io 模块本身
```

---

## 六、glob 通配符 `*`：全量导入公有项

```rust
use std::collections::*;
```

| | |
|---|---|
| **适用** | 单元测试、脚本、小作用域块 |
| **禁止** | 正式业务代码 — 易冲突、难溯源 |

demo 在 `main.rs` 里用 `{ … }` 块演示 glob，限制作用域。

---

## 七、跨 Package / crates.io 第三方

1. **`Cargo.toml` 依赖 Package**（不是 Crate）：

```toml
[dependencies]
serde = "1.0"
sqlx = "0.7"
```

2. **代码 `use` 对方 Lib-Crate 路径**：

```rust
use serde::Serialize;
use sqlx::postgres::PgPool;
```

→ Package / crates.io 概念：[7.1.1 §六](./7.1.1-二进制与库crate.md) · [14.4 cargo install](../14-cargo-crates/14.4-使用cargo-install从Crates.io安装二进制文件.md)

---

## 八、易混关键字对照

| 关键字 | 作用 |
|--------|------|
| **`mod`** | 定义模块、关联 `.rs` 源码文件 |
| **`pub`** | 开启公有访问权限 |
| **`use`** | 引入已有路径、简写名称（仅当前模块） |
| **`pub use`** | 对外重导出 API（`lib.rs` 标配） |

---

## demo 运行

```bash
cd 00-Book/07-packages-modules/7.4-use-demo
cargo run
```

演示：`pub use` 重导出 · 函数保留模块前缀 · 嵌套路径 · `as` 别名 · glob 块。

`mod.rs` 里 `mod` vs `use` → [7.5.1](./7.5.1-mod.rs里的mod与use.md)

跨 Package 的 `use pkg_b::…` → [7.3-cross-package-paths-demo](./7.3-cross-package-paths-demo/)（`cargo run -p pkg_a`）。
