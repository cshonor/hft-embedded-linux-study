# 7.2 定义模块来控制作用域与私有性

**Module** = Crate 内的代码分组 + **`pub` 可见性**。  
前置：[7.1.8 Package vs Module](./7.1.8-Package与Module.md) · **Crate root 与模块树** → [7.2.1](./7.2.1-crate-root与模块树.md)

对应 demo：[7.2-modules-privacy-demo](./7.2-modules-privacy-demo/)

---

## 子笔记

| 文档 | 内容 |
|------|------|
| **[7.2.1 Crate Root 与模块树](./7.2.1-crate-root与模块树.md)** | 餐厅案例、模块树、`crate::` 路径 |
| **[7.2.2 结构体与字段私有性](./7.2.2-结构体与字段私有性.md)** | 同模块随便改；跨模块字段须 `pub` 或方法封装 |
| **本篇（7.2）** | 函数/模块默认私有、`pub` 规则 |
| **延伸** | `lib.rs` 里 `mod` vs `pub mod` → [7.5.3](./7.5.3-lib.rs里mod与pub-mod.md) |

路径语法 → [7.3](./7.3-路径用于引用模块树中的项.md)（`crate::` / `super::` / `self::`） · **`use` / `pub use`** → [7.4](./7.4-使用use关键字将名称引入作用域.md) · 分文件 → [7.5](./7.5-将模块分割进不同文件.md)

---

## 私有性（privacy）：默认私有，`pub` 才公开

| 规则 | 说明 |
|------|------|
| **默认私有** | 模块内项同模块可访问；模块外不能直接访问 |
| **`pub`** | 模块或函数标记公有后，才可通过路径从外部访问 |

```rust
mod front_of_house {
    pub mod hosting {
        pub fn add_to_waitlist() { /* … */ }
        fn seat_at_table() {}  // 私有：仅 hosting 内部
    }
}
```

- `add_to_waitlist`：`pub` → crate 外可 `use` / 路径调用
- `seat_at_table`：无 `pub` → 只有 `hosting` 内部能调

**模式**：对外 `pub fn eat_at_restaurant()`，内部调私有实现，不暴露细节。

---

## demo：lib 模块树 + main 调公开 API

| 文件 | Crate | 内容 |
|------|-------|------|
| `src/lib.rs` | lib root | 餐厅 `mod front_of_house { … }` + `pub fn eat_at_restaurant()` |
| `src/main.rs` | bin root | `use 包名::eat_at_restaurant()` |

两棵**独立**模块树；main **不**重复声明 `front_of_house`，只调 lib 已 `pub` 的 API。

```bash
cd 00-Book/07-packages-modules/7.2-modules-privacy-demo
cargo run
```

模块树全貌 → [7.2.1](./7.2.1-crate-root与模块树.md)
