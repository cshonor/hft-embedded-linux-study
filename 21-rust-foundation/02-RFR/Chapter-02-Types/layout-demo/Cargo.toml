[package]
name = "rfr-layout-demo"
version = "0.1.0"
edition = "2021"
description = "RFR ch02: repr(Rust) vs repr(C) layout — size_of / align_of / offset_of"

[[bin]]
name = "layout_demo"
path = "src/main.rs"

[dependencies]
