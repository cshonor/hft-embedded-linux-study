## 1.1.3.2 为什么必须 `Send`

> 书 §1.1（3.2）· 索引：[1.1.3](./1.1.3-send-static-spawn.md) · 前：[1.1.3.1 签名](./1.1.3.1-spawn-signature-constraints.md) · 后：[1.1.3.3 `'static` 设计](./1.1.3.3-static-why-design.md)

---

### 二、为什么必须 `Send`？

**`Send`**：类型可以**安全地把所有权从一个线程转移到另一个线程**。

`spawn` 时发生的事：

1. 在**当前线程**构造闭包 `F`；
2. `spawn` 把闭包 **move 到新 OS 线程**执行；
3. 子线程结束后，`join` 把 **`T` move 回**等待线程。

若闭包内含 **`Rc<T>`** 等非 `Send` 类型，跨线程 move 会破坏 `Rc` 不变量（非原子计数）→ **数据竞争 / 双重释放**。编译器用 **`F: Send`** 禁止。

**一句话**：**`Send` 保证闭包能安全「搬家」到新线程**。

---

