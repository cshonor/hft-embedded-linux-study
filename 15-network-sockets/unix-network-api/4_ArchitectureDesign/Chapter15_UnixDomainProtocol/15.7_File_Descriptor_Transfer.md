# 15.7 描述符传递（Passing Descriptors）

> [Ch 14.5 sendmsg](../../2_AdvancedSkill/Chapter14_AdvancedIO_Func/14.5_Recvmsg_Sendmsg_Func.md) · [Ch 14.6 辅助数据](../../2_AdvancedSkill/Chapter14_AdvancedIO_Func/14.6_Ancillary_Data.md)

---

## 核心问题

`fork` 子进程可继承 fd；**无亲缘关系**的两进程如何让 B 使用 A 已打开的文件/套接字？

| 错误 | 正确 |
|------|------|
| 把整数 `3` 当普通数据发给 B | fd 仅是进程内索引；B 的 `3` 可能无关 |

实质：内核在 B 中**分配新 fd**，指向与 A **相同的内核文件表项**。

---

## API 要求

必须用 **`sendmsg` / `recvmsg`** + **`msghdr` 辅助数据（Ancillary Data）**：

| 字段 | 值 |
|------|-----|
| `cmsg_level` | **`SOL_SOCKET`** |
| `cmsg_type` | **`SCM_RIGHTS`** |
| 数据区 | 要传递的 **int fd**（可多个） |

---

## 实战：Master/Worker

```text
Master（root）bind :80 → accept 得 connfd
  → 经 Unix 域 + SCM_RIGHTS 把 connfd 交给 Worker
Worker（低权）直接 read/write 该连接
```

Nginx、Gunicorn 等架构的核心机制之一。

---

## 个人学习总结

（待填）
