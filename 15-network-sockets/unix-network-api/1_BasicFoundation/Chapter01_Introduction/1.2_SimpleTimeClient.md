# 1.2 一个简单的时间获取客户程序

> 章级：[study.md#ch1-2](./study.md#ch1-2) · 服务端 [1.5](./1.5_SimpleTimeServer.md#ch1-5)

## 阅读地图（拆书不删内容）

上次把 **500+ 行压成 1 个文件 ~140 行**，详文显得「没了」。实际是 **拆成多文件、去掉重复段落**；需要细读时按表打开附录即可。

| 文件 | 写什么 | 何时读 |
|------|--------|--------|
| **本页** | 五步法、**IP/端口字段**、`socket` 三参数、易错 4 条 | 第一遍必看 |
| [附录·API 精读](./1.2_Appendix_API精读.md) | **`connect` / `read` / 内核表** | 懂骨架后精读 |
| [附录·源码与考点](./1.2_Appendix_源码与考点.md) | **整段 C 源码 + 逐行表 + 编译** | 对照代码时 |
| [附录·C 新手与字节序](./1.2_Appendix_C新手与字节序.md) | **`argc` / `bzero` / `pton` / htons 深化** | C 不熟时 |
| [附录·Rust](./1.2_Appendix_Rust客户端.md) | Rust 对照与运行 | 用 Rust 练手时 |
| [code/…/daytimetcpcli.c](./code/1.2_SimpleTimeClient/original_c/daytimetcpcli.c) | **最长逐行注释** | 和附录源码二选一即可 |

**原则**：同一知识点**只在一处展开**；主笔记不贴整段源码（避免和附录、`*.c` 重复三遍）。

## 本节要会什么

**TCP 客户端五步法** + 在 `sockaddr_in` 里分清 **IP 和端口各填哪个字段**。

**程序干什么**：连对方 **TCP 端口 13（Daytime）**，读一行时间文字并打印。  
→ **为何是 13？** [端口/RFC865](./1.2_Appendix_Daytime端口13.md) · **与图 1-9 服务端怎么配合？** [C/S 联合流程](./1.12_Appendix_DaytimeCS联合流程.md)

---

<a id="ch1-2"></a>

<a id="ch1-2-flow"></a>

## 一、五步法（第 2 步：IP 和端口分开看）

| # | 步骤 | 做什么 |
|---|------|--------|
| 1 | `socket` | 创建 TCP/IPv4 套接字 → 得到 `sockfd` |
| 2 | **填服务器地址** | 往 `sockaddr_in servaddr` 里写 **IP + 端口**（见下表） |
| 3 | `connect` | 把地址交给内核 → **三次握手** |
| 4 | `while (read…)` | 循环读 TCP **字节流**（可能分多次） |
| 5 | `exit` / `close` | 关连接（发 FIN） |

### 第 2 步必记：哪个是 IP？哪个是端口？

连服务器要像打电话：**拨哪个号（IP）** + **分机几（端口）**。1.2 里写进 **`struct sockaddr_in servaddr`**：

| 你要填的 | 结构体字段 | 1.2 代码 | 数据从哪来 |
|----------|------------|----------|------------|
| **IP 地址** | **`sin_addr`** | `inet_pton(AF_INET, argv[1], &servaddr.sin_addr)` | 命令行 **`argv[1]`**，如 `"127.0.0.1"` |
| **端口号** | **`sin_port`** | `servaddr.sin_port = htons(13);` | 固定 **13**（Daytime 服务） |
| **IPv4** | **`sin_family`** | `servaddr.sin_family = AF_INET;` | 和 `socket(AF_INET,…)` 一致 |
| **先清零** | 整个 `servaddr` | `bzero(&servaddr, sizeof(servaddr));` | 避免垃圾字节 |

```text
  argv[1] "192.168.0.1" ──inet_pton──► sin_addr   （IP，4 字节）
  整数 13 ──htons───────────────► sin_port  （端口，网络序）
                    servaddr
                         │
                    connect()
                         ▼
                   内核：连到 该IP:13
```

**「网络字节序」**只影响**怎么把数字写进 `sin_port` / `sin_addr`**（端口用 `htons`；IP 用 `inet_pton`）。详解不重复写 → [附录·字节序](./1.2_Appendix_C新手与字节序.md#ch1-2-byteorder)。

```text
socket → 填 servaddr(IP+端口) → connect → while read → exit
```

---

<a id="ch1-2-api"></a>

## 二、API 速查

<a id="ch1-2-socket"></a>

### `socket()` 三个参数（1.2 只用 IPv4，没写 IPv6）

```c
sockfd = socket(AF_INET, SOCK_STREAM, 0);
```

| 参数 | 1.2 取值 | 什么意思 |
|------|----------|----------|
| **① 地址族** | **`AF_INET`** | **用 IPv4 地址**（点分如 `192.168.0.1`）。告诉内核：后面 `sockaddr_in` / `inet_pton` 都按 **4 字节 IPv4** 来，不是 IPv6。 |
| **② 套接字类型** | **`SOCK_STREAM`** | **字节流、可靠、有序** → 就是 **TCP**。 |
| **③ 协议** | **`0`** | **默认**：前面已选 TCP 流式时，由系统选 **TCP 协议**（一般不必手写 `IPPROTO_TCP`）。 |

**什么叫「IPv4 地址族」？**

- **族（family）** = 用哪一套地址格式。
- **`AF_INET`** = **Address Family, Internet** = **IPv4 那一套**（32 位 IP）。
- 你没写 IPv6，所以这里**不是** `AF_INET6`；1.3 才会出现 IPv6 / 协议无关。

**返回值 `sockfd`：**

| 返回值 | 含义 |
|--------|------|
| **≥ 0** | 成功，得到一个整数 **套接字描述符**（像“网络连接的句柄”），后面 `connect` / `read` 都用它 |
| **< 0** | 失败，查 **`errno`**（如资源不够）；**不能**拿这个 fd 去 `connect` |

| API | 一句话 | 展开 |
|-----|--------|------|
| `connect(…)` | 交内核三次握手；失败须 close 再 socket | [附录·connect](./1.2_Appendix_API精读.md#ch1-2-connect) |
| `read` / `while` | TCP 无边界；`n==0` 为 FIN | [附录·read](./1.2_Appendix_API精读.md#ch1-2-read) |

```c
struct sockaddr_in {
    sin_family;   /* AF_INET */
    sin_port;     /* 端口 ← htons(13) */
    sin_addr;     /* IP   ← inet_pton(..., argv[1], ...) */
    sin_zero[8];  /* 填 0 */
};
```

---

<a id="ch1-2-cheat"></a>

## 三、易错（4 条）

| ❌ | ✅ |
|----|-----|
| 一次 `read` 读完 | **`while` 循环** |
| `sin_port = 13` 不 htons | **`htons(13)`** |
| `connect` 失败再 connect 同 fd | **close → 新 socket** |
| 对 `inet_pton` 结果再 `htonl` | **只 pton，别重复转** |

---

<a id="ch1-2-source"></a>

## 四、源码与运行（详情见附录，此处不重复贴代码）

| 类型 | 入口 |
|------|------|
| **C 源码 + 逐行表 + gcc** | [附录·源码与考点](./1.2_Appendix_源码与考点.md) |
| **C 最长注释版** | [daytimetcpcli.c](./code/1.2_SimpleTimeClient/original_c/daytimetcpcli.c) |
| **Rust** | [附录·Rust](./1.2_Appendix_Rust客户端.md) · [daytimetcpcli.rs](./code/1.2_SimpleTimeClient/rewrite_rust/daytimetcpcli.rs) |

---

<a id="ch1-2-todo"></a>

## 五、后章对接

| 主题 | 节 |
|------|-----|
| `read` / `EINTR`、readn | [Ch 3.9](../Chapter03_SocketProgramIntro/3.9_Readn_Writen_Readline.md) |
| 协议无关 / 双栈 | [1.3](./1.3_ProtocolIndependence.md)、[Ch 11](../../2_AdvancedSkill/Chapter11_Name_Address_Convert/study.md) |
| 错误处理包裹 | [1.4](./1.4_ErrorHandlingWrapper.md#ch1-4) · `Socket`/`Connect` |
| `argc` / `bzero` / 字节序 | [附录·C](./1.2_Appendix_C新手与字节序.md) |
| `connect` / `read` / 内核 | [附录·API](./1.2_Appendix_API精读.md) |

---

<a id="ch1-2-summary"></a>

## 六、个人总结

1. 五步法；**第 2 步**：**`sin_addr`=IP（`inet_pton`+`argv[1]`），`sin_port`=端口（`htons(13)`）**。
2. TCP 要 **while read**；C 里 `recvline[n]=0` 只为 `fputs` 当字符串。
3. 深材料看附录，主笔记只留骨架。

**30 字**：建套接字 → 填 IP 与 13 端口 → connect → 循环读到 0 → 退出。

<!-- 兼容旧锚点 -->
<a id="ch1-2-rust"></a>
<a id="ch1-2-faq"></a>
<a id="ch1-2-byteorder"></a>
<a id="ch1-2-exam-detail"></a>
<a id="ch1-2-build"></a>
<a id="ch1-2-runtime"></a>

旧锚点跳转：`#ch1-2-source` / `#ch1-2-exam-detail` / `#ch1-2-build` → [附录·源码](./1.2_Appendix_源码与考点.md)；`#ch1-2-rust` → [Rust 附录](./1.2_Appendix_Rust客户端.md)；`#ch1-2-faq` / `#ch1-2-byteorder` → [C 附录](./1.2_Appendix_C新手与字节序.md)；`#ch1-2-kernel` → [API 附录·内核表](./1.2_Appendix_API精读.md#ch1-2-kernel)。
