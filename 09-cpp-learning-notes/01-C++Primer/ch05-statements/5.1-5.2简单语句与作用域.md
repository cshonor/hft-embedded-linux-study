# 5.1–5.2 简单语句与作用域

## 本节讲什么

本节讲解「–5.2 简单语句与作用域」。

## 要点

- 表达式语句、空语句、复合语句（块）
- 控制结构（`if`、`for` 等）内部定义变量的**块作用域**

## 示例

```cpp
#include <iostream>
int main() {
    int sum = 0;
    for (int i = 1; i <= 10; ++i)
        sum += i;
    std::cout << sum << std::endl;
    return 0;
}
```
