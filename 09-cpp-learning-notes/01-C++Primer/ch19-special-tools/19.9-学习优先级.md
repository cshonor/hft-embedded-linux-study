# 学习优先级

## 本节讲什么

**写框架、高性能库、底层组件**再啃本章；业务 CRUD 基本用不上。

## 示例

```cpp
#include <iostream>
// 第 19 章回顾：完美转发 + 可变参模板
#include <utility>
template<typename T>
void wrap(T &&v) {
    std::cout << std::forward<T>(v) << std::endl;
}
int main() {
    wrap(42);
    return 0;
}
```
