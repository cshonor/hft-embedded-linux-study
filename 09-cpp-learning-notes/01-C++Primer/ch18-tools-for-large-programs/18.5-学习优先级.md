# 学习优先级

## 本节讲什么

**中大型项目架构必学**：异常、命名空间、多重继承、RTTI 是工程基础。

## 示例

```cpp
#include <iostream>
// 第 18 章回顾：异常 + 命名空间
namespace app {
    void run() { std::cout << "running\n"; }
}
int main() {
    try { app::run(); }
    catch (...) { std::cerr << "error\n"; }
    return 0;
}
```
