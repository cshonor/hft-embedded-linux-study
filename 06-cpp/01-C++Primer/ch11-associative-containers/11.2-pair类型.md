# pair 类型

## 本节讲什么

本节讲解「pair 类型」。

## 要点

- 辅助关联容器（如 `map`）存储关键字-值对
- `pair` 类型及其基本操作

## 示例

```cpp
#include <iostream>
#include <utility>
int main() {
    std::pair<std::string, int> p{"key", 42};
    std::cout << p.first << ": " << p.second << std::endl;
    return 0;
}
```
