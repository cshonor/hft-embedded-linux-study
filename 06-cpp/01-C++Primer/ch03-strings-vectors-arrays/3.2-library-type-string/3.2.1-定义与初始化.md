# 3.2.1 定义与初始化

## 本节讲什么

`std::string` 的多种构造方式：默认空串、字面值、拷贝、`string(n, c)` 重复字符，以及 C++11 列表初始化。

```cpp
#include <string>

std::string s1;                    // 空串 ""
std::string s2 = "hello";          // 字面值
std::string s3(s2);                // 拷贝
std::string s4(10, 'c');           // "cccccccccc"
std::string s5 = std::string(5, 'x');  // "xxxxx"
```

| 写法 | 结果 |
|------|------|
| `string s;` | 默认构造，空串 |
| `string s = "abc";` | 从 C 风格字面量拷贝内容 |
| `string s(other);` | 深拷贝 `other` 的字符序列 |
| `string s(n, c);` | `n` 个字符 `c` |

列表初始化（C++11）：

```cpp
std::string s{"IF2406"};   // 与 = "IF2406" 等价
```

---

← [3.2 string](./3.2-标准库类型string.md) | [3.2.2 读写操作 →](./3.2.2-读写操作.md)
