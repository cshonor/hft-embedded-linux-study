# 直接内存管理

## 本节讲什么

本节讲解「直接内存管理」。

## 要点

- 传统 `new`、`delete` 的使用方式及其危险性
- 将智能指针与 `new` 结合使用

## 示例

```cpp
#include <iostream>
int main() {
    int *p = new int(42);
    std::cout << *p << std::endl;
    delete p;
    p = nullptr;
    return 0;
}
```
