# 19.3 类成员指针

## 本节讲什么

本节讲解「类成员指针」。

## 要点

- `::*` 成员指针语法：指向类的成员变量、成员函数
- 绑定到具体对象后才能访问；用于通用框架、简易反射

## 示例

```cpp
#include <iostream>
void f(int) { std::cout << "int\n"; }
void f(int *) { std::cout << "pointer\n"; }
int main() {
    f(nullptr);  // 调用 f(int*)
    return 0;
}
```
