# 17.6 可选：特殊数值类型

## 本节讲什么

智能指针相关补充、`numeric_limits` 等工具。

## 示例

```cpp
#include <iostream>
#include <optional>
int main() {
    std::optional<int> o;
    o = 42;
    if (o)
        std::cout << *o << std::endl;
    o.reset();
    std::cout << o.value_or(-1) << std::endl;
    return 0;
}
```
