# 函数指针

## 本节讲什么

本节讲解「函数指针」。

## 要点

- 指向函数而非对象的指针
- 类型由返回类型和形参类型共同决定
- 常用于将函数作为参数传递或其他动态调用

## 示例

```cpp
#include <iostream>
int &get(int *arr, int i) { return arr[i]; }
int main() {
    int ia[3] = {0, 1, 2};
    get(ia, 1) = 42;
    std::cout << ia[1] << std::endl;
    return 0;
}
```
