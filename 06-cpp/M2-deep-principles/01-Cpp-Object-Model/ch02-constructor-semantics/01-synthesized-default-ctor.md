# 2.1 编译器合成默认构造的四种情况

> 第 2 章 构造函数语义 · 上一节：[本章导读](README.md) · 下一节：[2.2 按位拷贝 vs 逐成员拷贝](02-bitwise-vs-memberwise.md)

## 这节讲什么

你写的类没定义默认构造函数，编译器何时"偷偷"合成一个？答案是四种情况——POD 类不合成。

---

## 四种情况

仅当以下情况，编译器才合成默认构造（且只合成它需要的）：

1. 类含**有默认构造的成员对象**（需调成员默认构造）
2. **基类有默认构造**（需调基类默认构造）
3. 类有**虚函数**（需初始化 vptr）
4. 类有**虚基类**（需初始化虚基类指针/偏移）

```cpp
struct POD { int x; double y; };      // 不合成默认构造，memset 安全
class WithVirtual { virtual void f(); }; // 合成（设 vptr）
class WithMember { std::string s; };    // 合成（调 string 默认构造）
```

---

## 新手要点（和 C 的区别）

- **C 的结构体不合成构造**：C 的 `struct { int x; }` 定义后 `x` 是未初始化值。C++ 的 POD 类也不合成构造——行为和 C 一致。
- **非 POD 会合成**：有虚函数/有成员对象/有基类的类，编译器自动合成构造做初始化。这就是为什么 `std::string s;` 能自动初始化为空字符串。

---

## HFT 关联

- **POD 零构造开销**：POD 类不合成构造，`vector<Tick>` 扩容用 `memcpy` 移动——HFT 数据结构尽量 POD。

---

## 自测题

1. 编译器合成默认构造的四种情况是什么？
2. POD 类会合成默认构造吗？为什么？
3. `class A { virtual void f(); };` 会合成默认构造吗？为什么？

---

## 参考与延伸

- 下一节：[2.2 按位拷贝 vs 逐成员拷贝](02-bitwise-vs-memberwise.md)
- 回到：[第 2 章 构造函数语义](README.md)
