# 2.6.3 Sales_data 完整版

## 本节讲什么

教材完整版：构造、成员函数、友元 IO 接口。

## 三、《C++ Primer》完整版 `Sales_data`

教材后续版本增加构造函数、成员函数与友元（`add` / `read` / `print`）。**一旦有自定义构造，不再是聚合**，初始化改用构造函数，例如 `Sales_data("978123456", 5, 39.9)`。

```cpp
#include <string>
#include <iostream>

// 友元前置声明
Sales_data add(const Sales_data&, const Sales_data&);
std::ostream& print(std::ostream&, const Sales_data&);
std::istream& read(std::istream&, Sales_data&);

struct Sales_data
{
    Sales_data() = default;
    Sales_data(const std::string& s) : bookNo(s) {}
    Sales_data(const std::string& s, unsigned n, double p)
        : bookNo(s), units_sold(n), revenue(p * n) {}
    Sales_data(std::istream& is);

    std::string isbn() const { return bookNo; }
    Sales_data& combine(const Sales_data& rhs);
    double avg_price() const;

    std::string bookNo;
    unsigned units_sold = 0;
    double revenue = 0.0;

    friend Sales_data add(const Sales_data&, const Sales_data&);
    friend std::ostream& print(std::ostream&, const Sales_data&);
    friend std::istream& read(std::istream&, Sales_data&);
};

Sales_data& Sales_data::combine(const Sales_data& rhs)
{
    units_sold += rhs.units_sold;
    revenue += rhs.revenue;
    return *this;
}

double Sales_data::avg_price() const
{
    if (units_sold)
        return revenue / units_sold;
    return 0;
}

Sales_data::Sales_data(std::istream& is) { read(is, *this); }

std::istream& read(std::istream& is, Sales_data& item)
{
    double price = 0;
    is >> item.bookNo >> item.units_sold >> price;
    item.revenue = price * item.units_sold;
    return is;
}

std::ostream& print(std::ostream& os, const Sales_data& item)
{
    os << item.isbn() << " " << item.units_sold << " "
       << item.revenue << " " << item.avg_price();
    return os;
}

Sales_data add(const Sales_data& lhs, const Sales_data& rhs)
{
    Sales_data sum = lhs;
    sum.combine(rhs);
    return sum;
}
```

**简单使用：**

```cpp
int main()
{
    Sales_data item1;                              // 空 ISBN、0 本、0 收入
    Sales_data item2("978123456", 5, 39.9);        // 5 本，单价 39.9
    print(std::cout, item2);
    return 0;
}
```

---

## 和 C 的区别（完整版展示 C++ struct 的全部能力）

这个完整版 `Sales_data` 展示了 C++ 的 struct 相对 C 的**全部新增能力**——C 程序员看这段代码会发现 struct 变得"像类"了：

| 能力 | C | C++（本例） |
|------|---|------------|
| 数据成员 | ✅ | ✅ |
| 成员函数 | ❌ | ✅ `isbn()`、`combine()` |
| 构造函数 | ❌ | ✅ 多个重载 |
| 访问控制 | ❌ | ✅ `friend` |
| `*this` 返回 | ❌ | ✅ `return *this` 链式调用 |

**给 C 程序员的关键解读：**
1. **成员函数**：`isbn()` 直接写在 struct 里。C 要写 `char* get_isbn(struct Sales_data* this)`，C++ 把 `this` 隐式化了。
2. **构造函数**：`Sales_data(s, n, p)` 替代 C 的 `init` 函数。创建对象时自动调用，不会忘初始化。
3. **`return *this`**：`combine` 返回 `*this`（当前对象的引用），支持链式调用 `a.combine(b).combine(c)`。C 做不到这种风格。
4. **`friend`**：让外部函数 `read`/`print` 能访问 private 成员。C 没有 private，也不需要 friend。

```c
/* C 等价写法：数据和函数分离，手动传 this */
struct Sales_data { char bookNo[16]; unsigned units_sold; double revenue; };
char* isbn(struct Sales_data* s) { return s->bookNo; }
void combine(struct Sales_data* lhs, struct Sales_data* rhs) {
    lhs->units_sold += rhs->units_sold;
    lhs->revenue += rhs->revenue;
}
```
```cpp
// C++：数据和函数绑定，this 隐式
struct Sales_data {
    std::string isbn() const { return bookNo; }
    Sales_data& combine(const Sales_data& rhs) {
        units_sold += rhs.units_sold;
        return *this;   // 链式调用
    }
};
```

## 自测题

**1（概念）** 完整版 `Sales_data` 为什么不再是聚合？

**2（选择）** `combine` 返回 `Sales_data&`（`*this`）的目的是什么？
- A. 省一次拷贝
- B. 支持链式调用 `a.combine(b).combine(c)`

**3（判断）** C 的 struct 也能写成员函数，只是默认不写。

<details><summary>参考答案</summary>
1. 因为有用户自定义的构造函数（`Sales_data(const std::string& s)` 等）。聚合要求"无用户提供的构造函数"，有了构造函数就不再是聚合，不能用 `{}` 按顺序初始化，要改用构造函数。
2. B。返回 `*this`（当前对象引用）让调用可以连写：`a.combine(b).combine(c)`，每次返回的还是 `a`。这是 C++ 链式调用的常见模式。
3. 错。C 的 struct 只能有数据成员，不能有成员函数、构造函数、访问控制。C++ 的 struct（和 class）才能有这些。C 的 struct 等于 C++ struct 去掉所有函数和访问控制。
</details>

---

← [2.6 自定义数据结构](./2.6-自定义数据结构.md) | [← 2.6.2 类内初始值](./2.6.2-类内初始值.md) | [2.6.4 友元与前置声明 →](./2.6.4-友元与前置声明.md)
