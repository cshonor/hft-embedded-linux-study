# 1.1 并发 vs 并行

> 第 1 章 你好，并发 · 下一节：[1.2 并发动机](02-motivation.md)

## 这节讲什么

并发和并行常被混用，但它们是不同概念。理解区别才能正确选择并发设计。

---

## 核心区别

- **并发（concurrency）**：逻辑上的"同时"——单核时间片切换也能并发。并发是**设计**（管理多个任务）。
- **并行（parallelism）**：物理上的"同时"——多核真正同时执行。并行是**执行**（同时跑）。

```
单核并发：[A][B][A][B][A][B]  时间片切换
多核并行：核心1: [A A A]
          核心2: [B B B]  真正同时
```

并发不一定需要多核——单核也能并发（时间片切换）。并行一定需要多核。

---

## 新手要点（和 C 的区别）

- **C 也有并发**：C 用 `pthread` 或 `fork` 做并发。C++11 把线程标准化到语言里（`std::thread`）。
- **先学并发设计**：并发是"如何拆分任务让它们独立运行"，并行是"如何让它们真正同时跑"。先想清楚怎么拆，再考虑怎么同时跑。

---

## HFT 关联

HFT 是并发的重度场景：行情线程 + 策略线程 + 风控线程 + IO 线程并行。但 HFT 并发的目标不只是"快"，更是**确定性**。

---

## 自测题

1. 并发和并行的区别是什么？单核能并发吗？
2. "并发是设计，并行是执行"如何理解？

---

## 参考与延伸

- 下一节：[1.2 并发动机](02-motivation.md)
- 回到：[第 1 章 你好，并发](README.md)
