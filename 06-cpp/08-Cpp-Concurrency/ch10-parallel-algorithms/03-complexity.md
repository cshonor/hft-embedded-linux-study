# 10.3 并行算法的复杂度保证

> 第 10 章 · 上一节：[10.2 par_unseq 的严格约束](02-par-unseq.md) · 下一节：[10.4 适合并行的算法](04-suitable-algorithms.md)

## 这节讲什么

标准库对并行算法的复杂度保证和串行版不同——通常允许更多操作次数（因为并行分块有额外开销）。本节讲并行算法的复杂度保证、何时并行化有收益、以及小数据量时为什么并行反而更慢。

---

## 核心规则（代码+表格）

### 并行算法的复杂度保证

| 算法 | 串行复杂度 | 并行复杂度保证 | 备注 |
|------|-----------|--------------|------|
| `for_each` | O(N) | O(N) | 线性，无额外 |
| `transform` | O(N) | O(N) | 线性 |
| `reduce` | O(N) | O(N) | 要求结合律 |
| `fill` | O(N) | O(N) | 线性 |
| `sort` | O(N log N) | O(N log N) | 但允许更多常数 |
| `find` | O(N) | O(N) | 但可能不提前终止 |
| `copy` | O(N) | O(N) | 线性 |

注意：并行算法的复杂度保证通常**不比串行更好**——并行的收益来自常数因子（多核加速），而非渐近复杂度。

### `find` 的提前终止问题

```cpp
// 串行 find：找到就返回，不扫描剩余
auto it = std::find(v.begin(), v.end(), 42);
// 如果 v[3] == 42，只扫描 4 个元素

// 并行 find：可能扫描更多元素
auto it = std::find(std::execution::par, v.begin(), v.end(), 42);
// 多线程同时扫描不同区间，某线程找到后
// 其他线程可能还在扫描（不能立即停止）
// → 并行 find 可能比串行扫描更多元素
```

但标准允许并行 `find` 提前终止（只是不保证）——取决于实现。

### 小数据量的并行陷阱

```cpp
// 小数据：并行更慢
std::vector<int> small(100);
std::sort(std::execution::par, small.begin(), small.end());
// 并行开销：线程池调度 + 分块 + 合并 ≈ 10-50μs
// 串行排序 100 元素 ≈ 1μs
// → 并行慢 10-50 倍！

// 大数据：并行更快
std::vector<int> large(10'000'000);
std::sort(std::execution::par, large.begin(), large.end());
// 串行：~500ms
// 并行（8核）：~80ms  → 6x 加速
```

### 并行化的收益阈值

| 数据量 | 并行收益 | 建议 |
|--------|----------|------|
| < 1000 | 负收益（开销 > 收益） | 用 `seq` |
| 1000 - 100000 | 可能正收益（取决于操作复杂度） | benchmark |
| > 100000 | 通常正收益 | 用 `par` |
| > 1000000 + 纯计算 | 显著正收益 | 用 `par_unseq` |

```cpp
// 实用模式：根据数据量选择策略
void smart_sort(std::vector<int>& v) {
    if (v.size() < 10000) {
        std::sort(std::execution::seq, v.begin(), v.end());
    } else {
        std::sort(std::execution::par, v.begin(), v.end());
    }
}
```

---

## 新手要点（和 C 的区别）

- **C 程序员可能觉得"并行总更快"**：这是误区。并行有固定开销（线程池调度、分块、合并），小数据量时这个开销超过并行收益。C++ 并行算法的标准复杂度保证不比串行好——收益来自常数因子。
- **`find` 的提前终止差异**：C 程序员可能期望并行 `find` 和串行一样"找到就停"——但并行实现可能扫描更多元素（其他线程已经在扫描了）。如果数据中目标很靠前，串行 `find` 可能更快。
- **收益阈值要实测**：C 程序员可能不知道"什么时候该并行"——答案是在目标硬件上 benchmark。1000-100000 的阈值因操作复杂度和硬件而异。C++ 没有自动选择策略的机制（C++26 可能引入）。
- **复杂度保证不等于性能**：C 程序员可能看 Big-O 复杂度判断性能——但并行算法的 Big-O 和串行相同，性能差异在常数。不能只看复杂度，要实测。

---

## HFT 关联

- **HFT 热路径数据量小，不并行**：一个 tick 的数据量极小（几个字段），并行开销远超收益。HFT 热路径一律串行。
- **盘后批处理数据量大，并行收益显著**：全市场 5000 标的 × 240 分钟 × 250 天 = 3 亿条记录——并行排序/统计有 4-8x 加速。
- **收益阈值在 HFT 中的意义**：HFT 系统的批处理工具应内置阈值判断——数据量小时自动回退串行，避免并行开销。
- **`find` 在 HFT 中的应用**：如"查找某标的的最新成交"——如果数据量大且目标靠后，并行 `find` 有收益；如果目标可能靠前，串行更快。要根据数据分布选择。

---

## 自测题

1. 并行算法的复杂度保证比串行更好吗？为什么？
2. 并行 `find` 和串行 `find` 在提前终止方面有什么差异？
3. 小数据量（如 100 元素）用并行排序为什么会更慢？
4. 什么数据量适合用 `par`？什么适合 `par_unseq`？
5. 为什么不能只看 Big-O 复杂度判断并行算法的性能？

---

## 参考与延伸

- 下一节：[10.4 适合并行的算法](04-suitable-algorithms.md)
- 上一节：[10.2 par_unseq 的严格约束](02-par-unseq.md)
- 回到：[第 10 章](README.md)
