## 3.6 内核比较

> [3.5 其他系统模型](./section-3.5-其他系统模型.md) · [3.3–3.4 内核演进与 Linux 特性](./section-3.3-3.4-内核演进与-Linux-特性.md) · [本章导读](../README.md)

---

### 本节讲什么

「哪个内核最快」这个问题本身有什么问题；Linux vs FreeBSD 的两个实证案例（TOP500 与 Netflix CDN）；微基准比较为什么极易出错；以及把这一切落到 HFT 的版本选型决策上。

### 要点

| # | 要点 | 一句话展开 |
|---|------|-----------|
| 1 | 「哪个内核快」没有单一答案 | 取决于配置、workload、内核参与程度三个变量 |
| 2 | 领域内证据：TOP500 100% Linux；Netflix CDN 用 FreeBSD | 超算=生态收敛；CDN=FreeBSD 在该 workload 上实测更优 |
| 3 | 微基准比较的系统脆弱性 | syscall 快 10× 但生产根本不用那个 syscall/flag 组合 |
| 4 | 准确比较 = 高级工程师数周的工作 | 多数公开对比都是没调过的默认配置互相打 |
| 5 | Linux 的复杂性是双刃剑 | 特性/参数最多 = 上限最高也最容易没调就上线 |
| 6 | HFT 选型：LTS + RT 补丁 / 云厂商内核 | 事实标准是生态，不是单点性能 |

---

### 一、哪个内核最快？

取决于三个变量——这个「取决于」不是和稀泥，每个变量都独立改变结论：

| 变量 | 为什么能翻盘 | 例子 |
|------|-------------|------|
| **OS 配置** | 同一内核调与不调差出一个量级 | 默认内核 vs isolcpus+nohz_full 的 tick 延迟稳定性 |
| **工作负载** | 各内核对不同路径优化投入不同 | 高并发网络转发 vs 单进程重计算 vs 文件服务 |
| **内核参与程度** | 内核不在路径上就无所谓快慢 | 内核旁路（DPDK）跑起来，网络栈差异归零 |

| 内核 | 优势 | 典型场景 |
|------|------|----------|
| **Linux** | 性能改进最多、驱动/应用支持最广、社区最大 | 通用服务器、云、HFT |
| **FreeBSD** | 特定工作负载可优于 Linux | Netflix CDN（OCA 团队内核优化） |
| **其他** | Solaris、AIX 等 | 传统企业遗留系统 |

**两个领域内实证：**

- **TOP500 超算**自 2017 年起 **100% Linux**——不是「Linux 最快」的证明，而是**生态收敛**的证明：ISV 软件、MPI 栈、运维经验全部围绕 Linux，换内核的机会成本碾压任何单点性能差异。
- **Netflix：云上用 Linux，CDN 用 FreeBSD**——OCA（Open Connect Appliance）节点是极简专用 workload（收流、缓存、发流），Netflix 团队对 FreeBSD 网络栈做了多年深度优化，**在该 workload 上性能高于生产 Linux**（2019 年 Linux 5.0 vs FreeBSD 对比验证）。这个案例的真正教训：**单一 workload + 团队深耕，非主流选择也能赢**——前提是你有 Netflix 那样的团队。

---

### 二、微基准陷阱

内核性能比较常用**微基准**——但极易出错：

```
测得：内核 X 的 foo() 比 Linux 快 10×
真相可能：
  ① 生产根本不调 foo()           —— 测了没人用的东西
  ② 调了但 flag 组合不同          —— 微基准没测那个组合
  ③ 快是因为没实现某个安全检查     —— 语义不对等
  ④ 两次测试的 gcc 版本不同        —— 差异在编译器不在内核
```

- **准确比较是高级性能工程师的工作**——控制配置、workload、编译器、硬件全部变量，可能花数周。这也是 [12.4 基准测试拷问](../../chapter-12-benchmarking/notes/section-12.4-基准测试拷问Benchmark-Questions.md) 的问题清单为什么长成那样——每个问题都在堵一个这类漏洞。

**微基准的正确用法**不是「谁快」，而是**归因**：拿到一个生产环境的可疑差异，用微基准隔离出「差异来自哪一层」（syscall 路径？调度器？网络栈?）。先有生产现象，后有微基准定位——顺序不能反。

方法论：[Ch12.3.2 Active Benchmarking](../../chapter-12-benchmarking/)

---

### 三、Linux 的演进与复杂性

- 第一版时 Linux **缺成熟动态追踪器** → Gregg 转全职 Linux 后帮助开发了 **BCC 和 bpftrace**（基于 eBPF）——DTrace 的思想火种跨血统落地（呼应 [3.3 血统](./section-3.3-3.4-内核演进与-Linux-特性.md)）。
- **OpenZFS** 现以 Linux 为**主内核**——提供高性能成熟文件系统。
- 3.1→5.8 跨版本大量性能改进（调度器多次换代、锁优化、网络栈重构、blk-mq…）。

**但复杂性是代价：** Linux 性能特性和调优参数**太多** → 很多部署**没调就上线**。比较内核性能时必问：**每个内核都调过吗？** 这句话是对一切「A 比 B 快 X%」公开对比的第一道拷问——最常见的答案是「A 是默认配置，B 也是默认配置」，而两个默认都不代表它们的上限。

```
Linux 的真实性能分布：
  默认配置 ──────── 调优后
     │                │
     ▼                ▼
  （常见基准点）    （HFT/超算实际运行点）
     └──── 差距可达数量级 ────┘
```

---

### 四、HFT 视角：版本与内核选型

| 要点 | 说明 |
|------|------|
| **选 Linux** | HFT 圈事实标准——社区、工具链、PREEMPT_RT、eBPF 生态；换内核的机会成本（招聘、排错经验、FPGA/网卡厂商支持）远超单点性能差 |
| **调优是必须** | 默认配置远非最优——isolcpus、NOHZ_FULL、大页、IRQ 亲和都要手动设；[6.9 完整隔离栈](../../chapter-06-cpus/notes/section-6.9-CPU-调优.md) 有全套拼图 |
| **不要轻信微基准** | 「内核 X syscall 快 2×」对 HFT 无意义——要看**端到端 tick-to-trade** 的分布（P99/P999），不是单点均值 |
| **版本选择** | LTS + 低延迟补丁（PREEMPT_RT）或云厂商优化内核（AWS Nitro 等）；跟最新 mainline 的冲动要克制——新特性 vs 回归风险 |
| **升级纪律** | 每次内核升级 = 重跑全套基线（tick-to-trade 分布、隔离验证四命令）；「升级没事」必须用数据说话 |

**为什么 PREEMPT_RT 而不是「调优过的普通内核」**：普通内核的延迟预算被内核态不可抢占区间主导（某段内核代码持锁跑几十 µs，你的高优先级线程干等）；RT 补丁把这些区间几乎全部消掉，把「最坏情况」压到 µs 级。HFT 买的是**尾部稳定性**，不是均值——与全书「看分位数不看均值」的主线一致（[2.5 方法论](../../chapter-02-methodologies/notes/section-2.5-性能分析方法论.md)）。

---

### 衔接

- 上一节 [3.5 其他系统模型](./section-3.5-其他系统模型.md)：不换内核也能「旁路」的思路。
- [Ch12 基准测试](../../chapter-12-benchmarking/)：微基准陷阱的完整方法论（有效性三支柱、拷问清单）。
- [Ch15 BPF](../../chapter-15-bpf/)：Linux 观测生态这个「事实标准」的核心组件。
- [HFT ch05 内核调优](../../../14-hft-engineering/chapter-05-os-kernel-tuning/README.md)：14 系列的实际调优手册。

---

### 常见陷阱

1. 「哪个内核快」当单一答案问题——配置、workload、内核参与程度三个变量各自能翻盘结论
2. 公开对比不问「都调过吗」——两个默认配置互打很常见，默认都不代表上限
3. 微基准结论直接外推生产——syscall 快 10× 但生产不用那个 syscall/flag 组合；微基准的正确用途是归因而非排名
4. TOP500 100% Linux 解读成「Linux 性能最强」——那是生态收敛的证据，不是单点性能的证据
5. HFT 跟最新内核——新特性的诱惑 vs 回归风险；LTS+RT 或厂商内核 + 升级重跑基线的纪律更划算
6. 买内核只看均值——RT 补丁买的是尾部稳定性（消灭不可抢占区间的最坏情况），P99/P999 才是 HFT 的评价标准

<details>
<summary>自测题（点击展开）</summary>

1. 「哪个内核最快」为什么没有单一答案？
   <details><summary>答</summary>三个变量各自能翻盘：OS 配置（调与不调差量级）、工作负载（各内核对不同路径的优化投入不同）、内核参与程度（旁路跑起来内核差异归零）</details>
2. TOP500 100% Linux 证明了什么？没证明什么？
   <details><summary>答</summary>证明生态收敛（ISV/MPI/运维全围绕 Linux，换内核机会成本碾压性能差）；没证明 Linux 单点性能最强——Netflix CDN 用 FreeBSD 且在该 workload 上实测更优就是反例</details>
3. 微基准比较的四个典型漏洞？
   <details><summary>答</summary>①测了生产根本不用的函数 ②用了生产不用的 flag 组合 ③快是因为少做了安全检查（语义不对等）④编译器版本不同（差异不在内核）。微基准的正确用途是归因定位，不是排名</details>
4. HFT 为什么选 PREEMPT_RT 而非「调优过的普通内核」？
   <details><summary>答</summary>普通内核延迟由不可抢占区间主导（内核代码持锁跑几十 µs，高优先级线程干等）；RT 补丁消掉这些区间，把最坏情况压到 µs 级。HFT 买的是尾部稳定性不是均值</details>
5. 「Linux 复杂性是代价」对部署意味着什么？
   <details><summary>答</summary>特性和调优参数最多=上限最高但也最容易没调就上线；一切「A 比 B 快 X%」的对比先问双方是否都调过——默认配置互打的结论无效</details>

</details>


---

← [3.5 其他系统模型](./section-3.5-其他系统模型.md) · [本章导读](../README.md)
