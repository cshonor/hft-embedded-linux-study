# 13.5 TCP 状态与 TIME_WAIT

> 章级精读：[../study.md](../study.md)

## 本节核心目标

理解 TIME_WAIT 状态的作用、持续时间和工程调优。

---

## TIME_WAIT

主动关闭方在四次挥手后进入 TIME_WAIT，持续 **2×MSL**（常 60-120s）。

```
Client → FIN → Server
Client ← ACK ← Server
Client ← FIN ← Server
Client → ACK → Server → CLOSED
Client → TIME_WAIT (2×MSL) → CLOSED
```

---

## 为什么需要 TIME_WAIT

| 原因 | 说明 |
|------|------|
| **防旧报文** | 网络中残留的旧段不会影响新连接 |
| **确保对端关闭** | 最后的 ACK 若丢失，对端重发 FIN，TIME_WAIT 方可响应 |

---

## TIME_WAIT 的代价

- 每个 TIME_WAIT 占用端口+内存
- 高并发短连接服务器 → 大量 TIME_WAIT → 端口耗尽

```bash
# 查看 TIME_WAIT 数量
ss -s | grep TIME-WAIT

# 调优
sysctl net.ipv4.tcp_tw_reuse=1      # 允许复用 TIME_WAIT 端口（推荐）
sysctl net.ipv4.tcp_tw_recycle=0    # 已废弃（NAT 环境有问题）
sysctl net.ipv4.tcp_max_tw_buckets  # TIME_WAIT 上限
```

---

## HFT 关联

HFT 系统使用**长连接**（预热+复用），一般不产生大量 TIME_WAIT。但短连接场景（如 HTTP API）需调优。

---

## 自测题

1. TIME_WAIT 持续多久？为什么是 2×MSL？
2. `tcp_tw_reuse` 和 `tcp_tw_recycle` 的区别？为什么后者被废弃？
3. 为什么 TIME_WAIT 只在主动关闭方出现？

---

## 交叉引用

- 连接建立：[13.2](13.2-tcp-connection-setup.md)
- TCP 选项：[13.3](13.3-tcp-options.md)
