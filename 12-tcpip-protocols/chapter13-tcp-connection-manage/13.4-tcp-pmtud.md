# 13.4 TCP 与 PMTUD

> 章级精读：[../study.md](../study.md) · UDP PMTUD：[ch10 §10.8](../chapter10-udp-ip-fragment/10.8-udp-pmtud.md)

## 本节核心目标

理解 TCP 如何通过 MSS 协商和 PMTUD 避免分片。

---

## TCP 的优势

TCP 通过 MSS 协商在握手阶段就限制段大小 → 尽量避免 IP 分片：

```
1. 握手: 双方宣布 MSS（常 1460）
2. 发送: TCP 段 ≤ MSS → IP 总长 ≤ MTU → 不分片
3. 若路径 MTU 更小 → PMTUD 动态调整
```

对比 UDP：无 MSS 协商 → 应用一次发太多 → IP 被动分片。

---

## TCP PMTUD 流程

```
1. 发送 MSS 大小的段（DF=1）
2. 中间路由器超 MTU → ICMP PTB（含下一跳 MTU）
3. TCP 收到 PTB → 降低 MSS → 重发
4. 缓存路径 MTU
```

---

## 黑洞问题

ICMP PTB 被防火墙过滤 → TCP 不知道该降 MSS → 持续发大包 → 持续被丢。

```bash
# Linux: PMTUD 黑洞检测
sysctl net.ipv4.tcp_mtu_probing
# 0=禁用, 1=黑洞时启用, 2=始终启用
```

---

## HFT 关联

TCP PMTUD 比 UDP 更可靠（内核自动处理），但黑洞检测有延迟。HFT 交易链路应确保路径 MTU 已知且固定。

---

## 自测题

1. TCP 的 MSS 协商如何帮助避免分片？
2. PMTUD 黑洞是什么？如何检测？
3. `tcp_mtu_probing` 有什么作用？

---

## 交叉引用

- TCP 选项 MSS：[13.3](13.3-tcp-options.md)
- UDP PMTUD：[ch10 §10.8](../chapter10-udp-ip-fragment/10.8-udp-pmtud.md)
- ICMP PTB：[ch08](../chapter08-icmpv4-icmpv6/study.md)
