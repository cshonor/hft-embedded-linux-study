# 3.3 Mac 实测：一次完整的 lldb 会话

[← 3.2 两套 clang 与版本矩阵](./3.2-两套clang与版本矩阵.md) · [CH3 macOS/Xcode](./README.md) · 下一节：[3.4 Mach-O 与 dSYM →](./3.4-Mach-O与dSYM分离税.md)

## 本节讲什么

把 [CH1 1.4 调试四件事](../01-common-core/1.4-调试到底是什么.md) 的四件事在你这台 Mac 上真跑一遍。
**下面所有终端输出都是 2026-09-26 本机真实输出**，不是示意。

> ⚠️ **一个前置事实（实测确认）：**
> 你 `cdev` 环境里的 lldb 22.1.8 **不能用** —— 它缺 `debugserver`：
>
> ```text
> $ lldb --version
> lldb version 22.1.8
> $ lldb --batch -o "breakpoint set --file ptrbug.c --line 34" -o run -- ./ptrbug
> (lldb) run
> error: could not find 'debugserver'. Please ensure it is properly installed and available in your PATH
> ```
>
> 原因：macOS 上 lldb 依赖一个叫 `debugserver` 的调试服务端（负责真正操作进程），
> 它由 Xcode 提供，不在 LLVM/conda 的分发里。好消息是你装了 **Xcode 27.0**，
> 它自带配套的 lldb 与 debugserver：
>
> ```text
> /Applications/Xcode.app/Contents/Developer/usr/bin/lldb          (lldb-2103.0.34.103)
> /Applications/Xcode.app/Contents/SharedFrameworks/LLDB.framework/Versions/A/Resources/debugserver
> ```
>
> **即使没同意 Xcode 许可协议，`/Applications/Xcode.app/.../bin/lldb` 这个二进制也能直接调用**
> （✅ 实测 —— 被挡的是 `xcrun`，不是 lldb 本体）。
> 所以下文用它。
>
> ⚠️ 另一个实测约束：**进程附加会被沙箱拦住**，报
> `attach failed (attached to process, but could not pause execution)`。
> 在终端里正常执行不受影响。

## 要点

### 准备：编译带调试信息的靶子

```bash
$ cd demo
$ clang -g -O0 -Wall -Wextra -std=c11 ptrbug.c -o ptrbug
$ ./ptrbug
sizeof(buf)      = 16 bytes, 4 elems
sum_n(buf, 4)   = 100
sum_n(buf, 8)    = 100   <-- 越界读，值不确定但通常不崩
buf=0x1020d4010  buf[0]=10  *(p+3)=40
```

✅ 实测。注意最后第二行：**越界读竟然返回了正确的 100**。
一个也不崩、一个也不错 —— 这种"看起来没事"的 bug 才是最贵的那种。

### 第一次断点：看四件套里的①④

```text
$ /Applications/Xcode.app/Contents/Developer/usr/bin/lldb --batch \
    -o "breakpoint set --file ptrbug.c --line 34" \
    -o "run" -o "frame variable" -o "bt" -o "continue" -- ./ptrbug

(lldb) target create "./ptrbug"
Current executable set to '.../demo/ptrbug' (arm64).
(lldb) breakpoint set --file ptrbug.c --line 34
Breakpoint 1: where = ptrbug`sum_n + 44 at ptrbug.c:34:14, address = 0x0000000100003ea4
(lldb) run
sizeof(buf)      = 16 bytes, 4 elems
Process 13057 launched: '.../demo/ptrbug' (arm64)
Process 13057 stopped
* thread #1, queue = 'com.apple.main-thread', stop reason = breakpoint 1.1
       frame #0: 0x0000000100003ea4 ptrbug`sum_n(a=0x0000000100008010, n=4) at ptrbug.c:34:14
   31  	{
   32  	    int s = 0;
   33  	    for (int i = 0; i < n; i++) {
-> 34  	        s += a[i];
    	             ^
   35  	    }
   36  	    return s;
   37  	}
(lldb) frame variable
(const int *) a = 0x0000000100008010
(int) n = 4
(int) s = 0
(int) i = 0
(lldb) bt
* thread #1, queue = 'com.apple.main-thread', stop reason = breakpoint 1.1
   * frame #0: 0x0000000100003ea4 ptrbug`sum_n(a=0x0000000100008010, n=4) at ptrbug.c:34:14
     frame #1: 0x0000000100003ddc ptrbug`main at ptrbug.c:48:15
     frame #2: 0x0000000192837e80 dyld`start + 6688
```

✅ 实测。四个关键观察：

1. **断点信息里带了行号和列号**：`at ptrbug.c:34:14` —— 靠 DWARF 才能有
2. **源码上下文自动打印**，`->` 指着当前行 —— 这就是 VS 里那个黄色箭头
3. `frame variable` 一次给出**全部局部变量 + 参数**（`a` 是 int 常量指针、`n`、`s`、`i`）
4. `bt` 显示三层栈：`sum_n` ← `main:48` ← `dyld start`

对照 VS：这段输出 = 局部变量窗口 + 调用堆栈窗口 + 编辑器里的黄色箭头，**三合一**。

### 条件断点：直达越界那一刻

`sum_n` 被调用两次。用条件断点只看越界那次（`n==8`）：

```text
$ lldb --batch \
    -o "breakpoint set --file ptrbug.c --line 34 --condition 'n==8'" \
    -o "run" -o "continue" -o "continue" -o "continue" -o "continue" -o "continue" \
    -o "frame variable" -o "x/12w &buf" -o "bt" -o "kill" -- ./ptrbug

(lldb) frame variable
(const int *) a = 0x0000000100008010
(int) n = 8
(int) s = 100
(int) i = 5
(lldb) x/12w &buf
0x100008010: 0x0000000a 0x00000014 0x0000001e 0x00000028
0x100008020: 0x00000000 0x00000000 0x00000000 0x00000000
0x100008030: 0x00000000 0x00000000 0x00000000 0x00000000
(lldb) bt
* thread #1, queue = 'com.apple.main-thread', stop reason = breakpoint 1.1
   * frame #0: 0x0000000100003ea4 ptrbug`sum_n(a=0x0000000100008010, n=8) at ptrbug.c:34:14
     frame #1: 0x0000000100003df0 ptrbug`main at ptrbug.c:49:15
     frame #2: 0x0000000192837e80 dyld`start + 6688
(lldb) kill
Process 13097 exited with status = 9 (0x00000009) killed
```

✅ 实测。**这一段是整个模块的点睛之笔**，把谜底解开了：

**① `i = 5`** —— `buf` 只有 4 个元素（下标 0–3），程序正在读 `buf[5]`，早就越界了。

**② `s = 100`** —— 前 4 个元素的和是 100，越界那几格没改变结果。为什么？看内存：

| 内存地址 | 值（十六进制） | 含义 |
|----------|---------------|------|
| `0x100008010` | `0x0a` | `buf[0]` = 10 ✓ |
| `0x100008014` | `0x14` | `buf[1]` = 20 ✓ |
| `0x100008018` | `0x1e` | `buf[2]` = 30 ✓ |
| `0x10000801c` | `0x28` | `buf[3]` = 40 ✓ |
| `0x100008020` | `0x00` | ← **越界区开始，全是 0** |
| `0x100008024` | `0x00` | ← 0，加进 `s` 不影响结果 |
| … | `0x00` | 全是 0 |

**`buf` 后面那段内存恰好是 0，所以 `sum_n(buf, 8)` 碰巧返回了正确答案。**
这不是"程序没 bug"，这是"这一次运气好"。换变量布局、换编译器、换优化级别，随时可能翻脸。

**③ `bt` 的 `main at ptrbug.c:49:15`** —— 精确告诉你越界是从第 49 行发起的，
而不是 48 行（48 行是那次正确的调用）。这个数字是 printf 调试法永远给不了你的。

### 同一个 bug，换个优化级别就翻脸

把 `-O0` 换成 `-O2`，同一个源文件：

```text
$ clang -g -O2 -Wall -Wextra -std=c11 ptrbug.c -o ptrbug_o2
$ ./ptrbug_o2
sizeof(buf)      = 16 bytes, 4 elems
（然后永久卡住，3 秒后被强制终止，status=9）
```

✅ 实测：`-O0` 下 4 行正常输出、`exit=0`；**`-O2` 下只吐出第 1 行就再也不返回了**。

看它生成了什么（截取 `main` 的关键部分，✅ 实测自 `--batch` 的 `-S` 输出）：

```asm
_main:
	.cfi_startproc
; %bb.0:
	sub	sp, sp, #32
	stp	x29, x30, [sp, #16]
	add	x29, sp, #16
	mov	w8, #4
	mov	w9, #16
	stp	x9, x8, [sp]
	adrp	x0, l_.str@PAGE
	add	x0, x0, l_.str@PAGEOFF
	bl	_printf          ← 只调用了一次 printf（第 1 行输出）
LBB0_1:                                 ; =>This Inner Loop Header: Depth=1
	b	LBB0_1          ← 无条件跳回自己：无限空循环
	.cfi_endproc
```

汇编里 **`main` 只剩下一次 `printf` 和一个跳向自己的无限循环**。
其余所有代码（第二个第三个 printf、`sum_n` 的调用）**全部被删除了**。

**发生了什么：**

1. clang 看见 `sum_n(buf, 8)` 会读 `buf[4..7]` → 判定为 **UB（未定义行为）**
2. 优化器的基本公理是"**UB 永远不会发生**"，于是推断这条路径不可达
3. 依赖它的返回值变成 poison（无意义），所有后续 `printf` 被常量折叠 / 消除
4. 但控制流真的流到了那个"不可能到达"的点，需要兜底 → 生成一个**无限空循环**

> **这才是 UB 的真正含义**：不是"偶尔算错"，是**编译器有权按它的假设重写你的整个程序**。
> MSVC 遇到同样的 UB 也会做类似推断，只是表现形式不同。

### 让工具替你抓：ASan

```text
$ clang -g -O1 -fsanitize=address -fno-omit-frame-pointer -Wall -std=c11 ptrbug.c -o ptrbug_asan
$ ./ptrbug_asan
=================================================================
==13007==ERROR: AddressSanitizer: global-buffer-overflow on address 0x0001042c41f0
READ of size 4 at 0x0001042c41f0 thread T0
    #0 0x0001042bfba4 in sum_n ptrbug.c:34
    #1 0x0001042bfba4 in main ptrbug.c:49
    #2 0x000192837e7c in start+0x1a1c (dyld:arm64e+0x31e7c)

0x0001042c41f0 is located 0 bytes after global variable 'buf' defined in 'ptrbug.c' (0x0001042c41e0) of size 16
SUMMARY: AddressSanitizer: global-buffer-overflow ptrbug.c:34 in sum_n
==13007==ABORTING
sizeof(buf)      = 16 bytes, 4 elems
$ echo $?
134
```

✅ 实测。ASan 给出的信息和 lldb 完全一致，而且更明确：

- `READ of size 4` —— 越界读取 4 字节
- `in sum_n ptrbug.c:34` ← `in main ptrbug.c:49` —— 精确的行号链
- **`0 bytes after global variable 'buf' ... of size 16`** —— 正好落在 `buf` 尾部之后第 0 字节，
  和我们用 `x/12w &buf` 看到的那片 `0x00` 区域严丝合缝
- `exit=134`（SIGABRT）

> `-O0` 看不出来、`-O2` 直接卡死、ASan 一句话命中 —— 三种手段的成本收益一目了然。
> 系统化的工具清单见 [03.6 用户态调试](./../../03.6-userspace-debugging/README.md)。

### 复现这些输出（本机）

```bash
cd demo
make            # 编译 -g 版本
make lldb       # 跑一次 lldb batch 会话
make asan       # ASan 版本，跑 ./ptrbug_asan
```

> ⚠️ `make lldb` 里的 `lldb` 默认会解析到 `PATH` 里的版本（你的 `cdev` lldb 缺 debugserver）。
> 建议直接敲 `/Applications/Xcode.app/Contents/Developer/usr/bin/lldb`，
> 或者先执行 `sudo xcodebuild -license` 把 Apple 整套工具链解锁。

## 与后续衔接

调试搞定技术层面之后，最后回答那个根本问题：
**为什么学会这套之后，还是得回 Linux 才能学 TLPI？** → [CH4](../05-boundary/README.md)。
