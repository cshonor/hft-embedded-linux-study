# 原厂 BSP：开箱能跑，量产仍要大量改

> FAQ · 锚定 Primer Ch3（SoC）· Ch4（内核工程）· Ch7（Boot / DT）  
> 重心 SoC：[3.0-focus-on-soc.md](./3.0-focus-on-soc.md) · 树莓归类：[3.2-raspberry-pi-is-soc.md](./3.2-raspberry-pi-is-soc.md)

---

## 结论

原厂**确实**给完整 BSP（内核、U-Boot、驱动、参考 DTS、工具链 / Yocto·Buildroot…），  
但只保证 **官方评估板 / 参考设计** 开箱可用。  
**自研 PCB = 以 BSP 为模板做二次开发**；不是「拷贝即量产」。

---

## 一、原厂会交什么（BSP 常见内容）

SoC 厂（NXP i.MX、TI、Broadcom、全志、瑞芯微等）板级支持包通常含：

1. Linux 内核（源码和/或预编译）  
2. U-Boot（或厂商等价 boot）  
3. 片内外设驱动：USB、以太、SD、LCD、Camera、I2C/SPI/UART、编解码、PMIC…  
4. **参考设备树**（绑在**评估板**原理图上）  
5. 工具链、Buildroot / Yocto 脚本、rootfs 模板  
6. 原理图、寄存器/TRM 类文档  

**评估板现实：** i.MX / Beagle 等 — 按文档编镜像烧录，外设基本通。  
**树莓派：** 官方镜像 + 内核树；不改板则刷机即用（[Pi Labs](../../../13-embedded-projects/RASPBERRY-PI5-LABS.md)）。

---

## 二、自研硬件：改在哪里（工作量主战场）

原厂 DTS/驱动对齐的是**他们的板**，不是你的原理图。

### 1. 设备树 — 几乎必改（Ch7 / [12 DT](../../../12-device-drivers-dt/)）

常见差异：

- GPIO / 串口 / LED / 按键换脚  
- 存储：eMMC → NAND（MTD 分区重配）或换 SD 方案  
- 网：PHY 型号、复位脚不同  
- 屏 / Camera 换料  
- PMIC 电压与上电时序  
- 删掉不用的节点（多余 USB、音频）省资源与攻击面  

**改一脚也要动 DTS**；严重时驱动时序一起调。

### 2. 驱动 — 分三档

| 类型 | 你通常做什么 |
|------|----------------|
| **片内模块**（MAC、SDHC、UART、GPU…） | 少重写；DTS 配引脚、时钟、DMA |
| **板级外接芯片**（PHY、屏、Codec、传感器） | 原厂驱动当参考；换料就改驱动或重移植 |
| **自研外设** | 无现成驱动 → 字符/平台驱动从零写（本书 Ch8 · [12](../../../12-device-drivers-dt/)） |

### 3. 内核裁剪与产品化（Ch4）

原厂 `.config` 求「演示全绿」→ 体积大、模块杂。量产常要：关无用驱动/协议/调试；可选 PREEMPT_RT；开机 logo / 日志策略；分区与 Flash FS 参数（JFFS2/UBIFS 等）。

### 4. U-Boot（Ch7）

DDR 颗粒、Flash/eMMC、环境变量、启动介质一变，就要改初始化与分区/env；不是「评估板 u-boot.bin 直接量产」。

---

## 三、两种人，两种工作量

| 路径 | 底层改动量 | 日常重心 |
|------|------------|----------|
| **只用不画板**（树莓 / 原厂 EVK） | 极小 | 应用、业务、偶尔 overlay |
| **自研 PCB**（同款 SoC 也一样） | U-Boot + DTS + 部分驱动 + 内核配置 **大规模** | 硬件适配 = 嵌入式 Linux 岗核心产出 |

---

## 四、两个误区

1. **「SoC 内部固定 → 软件不用改」**  
   片内互联固定；**引脚接到 PCB 的方式**由原理图决定。换脚 = 改 DTS（常还要改时序/驱动）。

2. **「厂家给了内核 → 不用懂底层」**  
   量产要抠启动时间、功耗、实时、磨损、安全。默认 BSP **达不到**指标时，不懂内核 / DT / 存储 / FS 就无法调优。

---

## 五、和本仓库怎么用

| 你现在 | 含义 |
|--------|------|
| Project #1 跟树莓官方镜像 | 走「开发板用户」路径；先闭环应用与驱动课 |
| 以后自研板 / 改原理图 | 把现用 BSP 当**模板**；主课变成 DT + U-Boot + 驱动 + 裁核 |
| 读 Primer Ch3–4–7 | 先建立「BSP 从哪来、改什么」心智，再进 MELP 实操 |

---

## 总结

1. 原厂 BSP **完整且能跑自家评估板**  
2. 自研硬件：**DTS 必改**；外接驱动常改；Boot/内核必裁剪适配  
3. 工程师价值 = 在模板上做 **板级适配 + 系统定制 + 缺件驱动**，不是只会 `dd` 官方镜像  

**下一环精读：** [Ch4 内核工程视角](../chapter-04-kernel-construction/notes.md) · [Ch7 Bootloaders](../chapter-07-bootloaders/notes.md)（笔记待填时先记本 FAQ）。
