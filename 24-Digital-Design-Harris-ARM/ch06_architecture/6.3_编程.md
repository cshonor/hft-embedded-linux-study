# 6.3 编程

> **Core Concept:** C → ARM 汇编；NZCV；分支；LDR/STR；AAPCS；MMIO 直觉  
> **Link Target:** Ch5 ALU Flags · CSAPP §3.x · 驱动反汇编  
> **档位:** 精读

## 状态

- [x] 已读（口述巩固）
- [x] 已写要点
- [x] AAPCS + 驱动读码要点

## 笔记

高级语言结构如何落到 ARM 汇编（AArch32）。

### 6.3.1 数据处理指令

| 类 | 例子 |
|----|------|
| 算术/逻辑 | `ADD` `SUB` `AND` `ORR` `EOR` … |
| 移位 | `LSL` `LSR` `ASR` `ROR`（可与数据处理同条） |
| 乘法 | `MUL` 等 |

与 Ch5 ALU/移位器硬件对应。

### 6.3.2 条件标志（NZCV）

**CPSR** 中：

| 位 | 含义 | 软件常见 |
|----|------|----------|
| **N** | Negative | 有符号比较 |
| **Z** | Zero | `beq` / 判零 |
| **C** | Carry | 无符号进位/借位 |
| **V** | oVerflow | 有符号溢出 |

许多指令带 `S` 后缀才更新标志（`ADDS`、`CMP`=`SUBS` 不写回）。  
条件分支 / 条件执行看这些位。

### 6.3.3–6.3.5 分支 · 循环 · 调用入口

| 高级语言 | 汇编手段 |
|----------|----------|
| `if` / `else` | 比较 + `BEQ`/`BNE`… 或条件执行 |
| `while` / `for` | 比较 + 回跳 |
| 调用 | **`BL`**（LR←返回地址） |
| 返回 | `BX LR` / `MOV PC, LR` 等 |

### 6.3.6 存储器：LDR / STR

| 指令 | 作用 |
|------|------|
| **LDR** | Mem → Reg |
| **STR** | Reg → Mem |

寻址：`[Rn, #imm]`、`[Rn, Rm]`、预/后索引写回等。  
ARM = **load/store**：算术一般不直接吃内存操作数（对照 x86）。

### 6.3.7 函数调用（AAPCS 直觉）

| 约定 | |
|------|--|
| 参数 | **R0–R3**，更多走栈 |
| 返回值 | **R0**（宽返回再扩） |
| 返回地址 | **LR** |
| 栈 | **SP** 向下；对齐要求看版本 |

被调者保存 vs 调用者保存：跨调用必须活着的寄存器 → 进函数压栈；临时寄存器调用方自己保。

### 6.3.8 对驱动 / 内核

| 场景 | |
|------|--|
| 读 oops 反汇编 | 认 R0–R3 参数、LR 返回链、SP 栈帧 |
| MMIO | C 里 `readl`/`writel` 或 `volatile` 指针 → 编译器生成 LDR/STR；**禁止**被优化掉 |
| 内联汇编 | 约束列出破坏的寄存器，避免与编译器打架 |

细节以 AAPCS / Procedure Call Standard 为准。
