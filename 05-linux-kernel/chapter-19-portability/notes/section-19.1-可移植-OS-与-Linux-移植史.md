## ① 可移植 OS 与 Linux 移植史

| 路线 | 说明 |
|------|------|
| **折中** | 核心接口尽量 **架构无关 C** |
| 性能路径 | **`arch/`** 下 **高度优化** 甚至 **汇编** |

| 历史 | 始于 **i386** → **Alpha、MIPS、SPARC、ARM、PowerPC…** |

**v6.6 实测：22 个架构目录**（`arch/` 下直接子目录）：

```
alpha  arc  arm  arm64  csky  hexagon  ia64  loongarch  m68k  microblaze
mips  nios2  openrisc  parisc  powerpc  riscv  s390  sh  sparc  um  x86  xtensa
```

> **版本冷知识：v6.7 删掉了 `ia64`**（Itanium，代码量大而硬件已停售）→ 变成 21 个。
> 这是理解内核演进的好例子：**维护成本 > 硬件存量时，架构会被移除**，没有"永远支持"这回事。

---

### 三层抽象：通用 → asm-generic → 特化

可移植性不是"每个架构各写一遍"，而是**三层复用**：

```
第 1 层：通用代码（mm/ fs/ net/ drivers/…）
              │  用 <asm/xxx.h> 里的接口
              ▼
第 2 层：include/asm-generic/     ← 用**纯 C** 写的"默认实现"
              │  某个架构不覆盖就用这版
              ▼
第 3 层：arch/<arch>/include/asm/ ← 架构特化（内联汇编/专用指令）
```

```makefile
# arch/xxx/include/asm/<头文件> 存在 → 用它
# 不存在 → 回落到 include/asm-generic/<头文件>
```

> 现在的移植工作大部分是：**先什么都不写，用 asm-generic 的默认版跑起来**，
> 再挑真正值得优化的几个（原子操作、自旋锁、页表遍历）用内联汇编重写。

---

### `arch/` 到底隔离了什么

| 类别 | 具体内容 | 为什么不能通用 |
|------|---------|--------------|
| **启动与初始化** | 解压内核、解析固件表（DT/ACPI）、建初始页表 | 硬件复位后的状态各架构完全不同 |
| **内存管理** | 页表格式（x86 4 级 / arm64 可配 3-4 级）、TLB 刷新指令、缺页入口 | 页表**格式是硬件规定的** |
| **中断与异常** | IDT/向量表、中断入口汇编、EOI | 中断控制器与入口约定不同 |
| **原子与屏障** | `cmpxchg`、`atomic_*`、内存屏障指令 | 指令集不同（lock 前缀 vs ldaxr/stlxr） |
| **上下文切换** | `switch_to()`、保存/恢复哪些寄存器、TLS 怎么切 | 寄存器文件不同 |
| **`current` 宏** | 怎么拿到当前进程的 `task_struct` | x86 用 `percpu` 读 `current_task`；有些架构用 sp 掩码（栈底放 thread_info） |
| **系统调用入口** | `syscall`/`sysenter`/`svc` 指令与派发表 | 陷入指令不同 |
| **cache 维护** | 指令/数据 cache 一致性（自修改代码、DMA） | x86 硬件自动一致；arm64 需要显式 `dc/ic` 指令 |
| **字节序** | `__BIG_ENDIAN` / `__LITTLE_ENDIAN` | 硬件可配（arm64 双端） |

> `current` 是个很好的例子：**同一个宏，在 x86 和 arm64 上实现完全不同**，
> 但 `mm/`、`fs/` 里的代码完全不关心——这就是抽象层的价值。

---

### 现代的"一次编译，多平台运行"

LKD 时代每个 ARM 板子要单独编译一个内核。现在：

| 机制 | 效果 |
|------|------|
| **设备树（Device Tree）** | 硬件信息从**数据**里读，不再硬编码进内核 → 一个内核镜像跑所有同架构板子 |
| **ACPI** | x86 服务器的标准固件表 |
| **`fwnode` 统一抽象** | 驱动代码不关心自己拿到的是 DT 还是 ACPI 节点（见 [Ch 17.2](../../chapter-17-devices-modules/notes/section-17.2-统一设备模型.md)） |
| **CONFIG_ARCH_MULTIPLATFORM** | 一个 `defconfig` 覆盖多家 SoC |

```
通用内核代码（mm/ fs/ net/…）
        │
        ▼
arch/arm64/  arch/x86/  …  — 启动、页表、原子、屏障实现
        │
        ▼
  DT / ACPI   — 这一次具体的机器长什么样（数据，不是代码）
```

---

### HFT 需要关心的架构差异（x86_64 vs ARM64）

| 维度 | x86_64 | ARM64 | 对 HFT 代码的影响 |
|------|--------|-------|-----------------|
| **内存模型** | TSO（较强） | **弱序** | 无锁代码在 ARM64 上要多插屏障（见 19.7） |
| **cache line** | 64 B | 64 B（多数实现） | false sharing 规避手法通用 |
| **页大小** | 4KB / 2MB / 1GB | **4KB / 16KB / 64KB** + 2MB/32MB/1GB | 不能硬编码 4096 |
| **字节序** | 小端 | 双端（**服务器几乎都是小端**） | 线上格式仍要显式转换 |
| **TSC** | 有稳定 TSC（`rdtsc`） | 用 `cntvct_el0`，频率可能随调频变 | 时间戳源要抽象 |
| **`long` 宽度** | 64 | 64（LP64） | 一致，但不要依赖 |
| **`char` 符号性** | signed | **unsigned** | 拿 char 做数组下标/比较时会踩坑（见 19.3） |

→ **Ch 2** `arch/` 目录 · **Ch 12** per-arch 页大小

**HFT 对照：** 用户态协议解码（行情二进制）同样 **显式字节序、固定宽度类型** — 与内核规则同构。
内核靠 `arch/` 隔离差异，用户态靠 **`stdint.h` + 显式 hton/ntoh + 不假设对齐**，
本质是同一条纪律：**把"依赖具体实现的东西"显式化，而不是藏在默认行为里。**



<details>
<summary>自测题（点击展开）</summary>

**Q1.** Linux 能移植到不同架构的关键设计是什么？

<details><summary>答案</summary>

1) arch/ 目录隔离架构相关代码（中断入口/页表/原子操作/字节序）；2) 可移植类型（u32/s64 不依赖 int/long 大小）；3) 宏抽象（__raw_readl → 各架构实现）；4) 构建系统按架构选择正确代码。用户态代码移植性靠 ABI 兼容层；内核移植性靠 arch/ 层。HFT 代码从 x86 迁到 ARM64 需要关注字节序/对齐/cache line 大小差异。

</details>

**Q2.** 移植一个新架构，是不是要把 `arch/` 下的东西全写一遍？

<details><summary>答案</summary>

不用。现代 Linux 的移植策略是 **`asm-generic` 优先**：

```
include/asm-generic/     ← 纯 C 写的默认实现（大部分头文件）
arch/<新架构>/include/asm/  ← 只放你**特地要覆盖**的那几个
```

内核的构建规则是：如果某个 `asm/xxx.h` 在新架构下不存在，就**自动回落**到 `asm-generic/xxx.h`。
所以一个最小可启动的移植，**只需要提供**：

1. **启动入口与早期初始化**（解压、建初始页表、解析 DT/ACPI）；
2. **中断/异常入口**（汇编部分，跳到通用 C 处理函数）；
3. **页表操作**（格式是硬件规定的，无法通用）；
4. **`switch_to()` 上下文切换** + 寄存器保存约定；
5. **`current` 宏的取法**（x86 用 percpu 变量，某些架构用栈指针掩码）；
6. 少量原子/屏障（可以先用关中断的**最朴素实现**跑通，之后再优化）。

其余全部先用 `asm-generic` 的默认版。Linux 内核社区移植 RISC-V 时就是这么推进的：
先让它**能启动能跑**，再逐个把热点换成指令级优化版本。

**这条方法论对工程实践也有启发**：抽象层的设计目标应该是"**默认可跑，按需特化**"，
而不是"每个实现都必须自己写一遍"。后者会极大抬高接入成本。

顺带一个真实案例：`ia64`（Itanium）因为在 v6.7 时维护成本超过硬件存量，
被**整体移除**——架构支持不是永久的，这是内核对待"没人用的代码"的一贯态度。

</details>

**Q3.** 都说"Linux 可移植"，那为什么驱动还是经常换个板子就跑不了？

<details><summary>答案</summary>

因为"可移植"分**三个层次**，Linux 只保证第一层，后两层靠数据描述和生态：

| 层次 | 谁保证 | v6.6 的现状 |
|------|--------|------------|
| **内核核心（mm/fs/net/sched）** | 内核抽象层 | ✅ 真正可移植，换架构编译即可 |
| **硬件拓扑描述** | **设备树 / ACPI（数据）** | ⚠️ 需要针对具体板子提供正确的 DT/ACPI 表 |
| **具体设备的驱动** | 是否有人写过 | ❌ 没有驱动就是没有 |

驱动跑不了通常卡在**第二、三层**，不是抽象层的问题：

1. **硬件信息缺失**：同一颗 SoC 在不同板子上的 GPIO 编号、供电（regulator）、时钟（clock）、
   引脚复用（pinctrl）全都不一样。这些信息**不在驱动代码里**，在 DT 里。
   DT 写错 → 驱动 probe 返回 `-EPROBE_DEFER` 或直接失败（见 Ch 17.2）。
2. **驱动根本不存在**：一颗新网卡芯片没人写驱动，它就是不工作，跟可移植性无关。
3. **闭源/用户态依赖**：有些硬件需要厂商提供的固件（firmware blob）或用户态库，
   内核这边抽象得再好也无能为力。

**所以更准确的说法是**：Linux 让"**写一个可移植的驱动**"变得容易（设备模型 + 统一接口），
但不能让"**没写过的驱动凭空出现**"。

对 HFT 的启示：选硬件时**先看内核有没有主线驱动**、驱动是否在你要用的 LTS 版本里，
比看硬件参数更重要。自研加速卡最贵的往往不是卡，是驱动和维护它的人。

</details>

</details>
---
