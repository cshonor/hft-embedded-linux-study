# 8.1.3 指针与下标

## 本节讲什么

**下标与指针算术完全等价**；两种遍历风格。

## 要点

```c
/* 下标 */
for (i = 0; i < n; i++)
    sum += arr[i];

/* 指针 */
for (p = arr; p < arr + n; p++)
    sum += *p;
```

- `p[i]` 合法：`p` 指向某元素时
- **`3[arr]`** 合法但禁止写

## 与后续衔接

**ch06 6.13** 指针算术；热路径常指针遍历利于优化。
