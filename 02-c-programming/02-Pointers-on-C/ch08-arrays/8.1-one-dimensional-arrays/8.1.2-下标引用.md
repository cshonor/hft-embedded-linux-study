# 8.1.2 下标引用

## 本节讲什么

**`arr[i]`** 访问第 i 个元素；下标从 **0** 到 **length-1**。

## 要点

- 编译器将 `arr[i]` 转为 `*(arr + i)`
- 越界 → **UB**（无运行时检查）
- 负下标仅当指针已偏移到合法区域前才有定义（少见）

## DPDK/HFT

```c
for (i = 0; i < nb_pkts; i++)
    process(pkts[i]);
```

## 与后续衔接

**8.1.3** 指针写法等价。
