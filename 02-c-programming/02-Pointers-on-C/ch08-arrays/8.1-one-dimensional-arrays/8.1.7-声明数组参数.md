# 8.1.7 声明数组参数

## 本节讲什么

形参声明的三种等价写法；**方括号内长度被忽略**（除多维见 8.2.5）。

## 要点

```c
void f(int arr[]);
void f(int arr[1024]);  /* 1024 无约束作用，仅文档 */
void f(int *arr);
```

- C 标准：数组参数调整为 **指针**
- 文档性长度可写 `char name[64]` 提示读者

## 与后续衔接

**8.2.5** 二维：除第一维外长度**必须**保留。
