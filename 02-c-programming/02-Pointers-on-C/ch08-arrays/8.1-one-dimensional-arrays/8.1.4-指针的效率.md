# 8.1.4 指针的效率

## 本节讲什么

指针 vs 下标：**现代编译器通常生成相同机器码**；选用可读性更好的写法。

## 要点

- 优化后 `arr[i]` 与 `*(p+i)` 无必然性能差
- 指针遍历有时少一次乘法（取决于 ISA 与优化）
- HFT：**算法与缓存局部性** >> 下标/指针风格差异

## 与后续衔接

**restrict**、连续内存（**ch06**）对性能影响更大。
