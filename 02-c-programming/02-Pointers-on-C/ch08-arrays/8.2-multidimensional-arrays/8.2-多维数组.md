# 8.2 多维数组

## 本节讲什么

**数组的数组**：`int buf[4][3]` 为 4 行 × 3 列，**行优先**连续存储。内核页表、DPDK 多端口队列、HFT 批量行情池的基础。

## 要点

- **`buf[i][j] == *(*(buf + i) + j)`**
- **`buf[i]`** 是第 i 行（`int[3]`），退化为 `&buf[i][0]`
- 传参：**仅第一维可省略**，其余维度必须写死（**8.2.5**）

## 小节

8.2.1–8.2.7

## 与后续衔接

**8.2.4** 数组指针 vs **8.3** 指针数组。
