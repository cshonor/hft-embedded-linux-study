# 8.2.3 下标

## 本节讲什么

二维下标展开与行/列访问。

## 要点

```c
buf[i][j] == *(buf[i] + j) == *(*(buf + i) + j)
```

```c
for (i = 0; i < 4; i++)
    for (j = 0; j < 3; j++)
        buf[i][j] = 0;
```

- 内核设备表 `dev[id][chan]` 同类模式

## 与后续衔接

三维及以上同理；VLA：`char t[row][col]`（C99）。
