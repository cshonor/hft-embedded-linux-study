# 10.2.1 结构的直接访问

## 本节讲什么

对**结构体实体变量**用 **`.`** 读写字段。

## 要点

```c
QuoteHdr pkt;
pkt.seq      = 10001;
pkt.price    = 9999;
pkt.pkt_type = PKT_QUOTE;
```

- 嵌套：`pkt_head.quote.seq`（若外层为变量）
- 数组：`arr[i].seq`

## 与后续衔接

栈上小 struct 可用 `.`；大对象与热路径用指针 **10.2.2**。
