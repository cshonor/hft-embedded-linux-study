# 3.3.4 寄存器间接寻址

## 本节讲什么

地址 = 寄存器内容，零偏移——指针解引用 `*p` 的直接汇编对应。

## 要点

```arm
ldr r0, [r1]          @ r0 = *r1
str r0, [r2]          @ *r2 = r0
ldrb r3, [r4]         @ 字节读（char）
```

- 空 `[]` 表示偏移为 0
- 内核 `readl(addr)` 类宏最终落到 `ldr` + 设备寄存器基址

## 与后续衔接

- 加偏移变 **3.3.5**；多级指针多次 `ldr`
