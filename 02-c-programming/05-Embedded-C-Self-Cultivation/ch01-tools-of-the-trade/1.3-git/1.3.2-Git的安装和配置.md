# 1.3.2 Git 的安装和配置

## 本节讲什么

安装 Git 与 **user.name / user.email** 一次性配置。

## 要点

```bash
sudo apt install -y git

git config --global user.name "Your Name"
git config --global user.email "you@example.com"
git config --global init.defaultBranch main
```

```bash
git config --list
ssh-keygen -t ed25519    # 可选：GitHub SSH
```

## 与后续衔接

**1.3.3** init/add/commit；团队统一 `.gitignore`。
