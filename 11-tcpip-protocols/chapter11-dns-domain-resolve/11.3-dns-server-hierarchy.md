# 11.3 名称服务器与区域

> 章级精读：[../study.md#ch11-3](study.md#ch11-3)

## 本节核心目标

区分**权威服务器**与**递归服务器**，理解区域（Zone）概念和递归解析流程。

---

## Zone（区域）

**Zone** = 某机构**实际托管**的连续名称空间（可小于一个"域"）。

| 术语 | 说明 |
|------|------|
| **委派点** | 父区通过 **NS** 把子区管理权交出 |
| **Primary** | 区域**唯一写**入点（主服务器） |
| **Secondary** | **区域传输**（AXFR/IXFR）同步副本（从服务器） |

---

## 权威 vs 递归

| | 递归服务器 | 权威服务器 |
|--|------------|------------|
| 角色 | 替客户端**跑腿**迭代查询 | 对本区数据**负责** |
| 典型 | ISP/8.8.8.8/企业递归 | 域名注册商/自托管权威 |
| 数据来源 | 缓存 + 迭代查询 | 本地区域文件 |
| 响应标志 | 常无 AA | **AA=1**（Authoritative Answer） |

---

## 根服务器

- 13 个根 **IP**（字母 a–m.root-servers.net）
- 广泛 **Anycast** → 每 IP 对应全球多物理节点，降延迟、抗 DDoS
- 并非 13 台机器，而是数百个 Anycast 节点共享 13 个 IP

---

## 递归解析五步

```text
1. Client → 递归器 (UDP/53)
2. 递归器 → 根服务器 → 返回 TLD NS 地址
3. 递归器 → TLD NS → 返回权威 NS 地址
4. 递归器 → 权威 NS → 返回 A/AAAA 记录
5. 递归器 → Client (返回结果，可缓存)
```

每一步递归器都会**缓存**结果（按 TTL），后续相同查询直接命中缓存。

---

## 代码示例

```bash
# 追踪完整递归过程
dig +trace www.example.com

# 查询权威服务器
dig @a.iana-servers.net example.com NS

# 查看缓存
dig www.example.com +stats | grep "Query time"
```

---

## HFT 关联

HFT 系统不应依赖递归 DNS——启动时解析一次，缓存到本地。若必须查询：
- 指定可信递归器（`dig @8.8.8.8`）
- 设短超时 + fallback IP
- 避免在交易热路径上做 DNS 查询

---

## 自测题

1. 权威服务器和递归服务器的区别是什么？同一台机器能同时充当两者吗？
2. 13 个根 IP 如何服务全球？Anycast 的原理是什么？
3. `dig +trace` 展示了什么过程？

---

## 交叉引用

- DNS 名称空间：[11.2](11.2-domain-space-structure.md)
- 缓存机制：[11.4](11.4-dns-cache.md)
- DNS 协议格式：[11.5](11.5-dns-packet-structure.md)
