# 14.4 基于计时器的重传

> 章级精读：[../study.md#ch14-4](../study.md#ch14-4)

## 本节核心目标

理解 TCP 重传计时器的工作机制：启动、重启、超时。

---

## 计时器规则

| 条件 | 动作 |
|------|------|
| 有未确认在途数据 | **启动/维持**重传计时器 |
| 收到推进左边界的新 ACK | **重启**计时器，超时 = 当前 RTO |
| 计时器溢出 | 重传最老未确认段 + 指数退避 |
| 所有数据已确认 | **关闭**计时器 |

**关键**：始终盯住**最老**未确认段。ACK 推进左边界后，计时器重置为剩余最老段的 RTO。

---

## 重传队列管理

```text
发送窗口:
| 已确认 | 待ACK(在途) | 可发 | 不可发 |
         ^最老未确认段
         ↑ 计时器盯这个段
```

- 重传后 RTO ×2（指数退避）
- 不立即重传所有在途段——只重传最老的
- SACK 模式下可选择性地重传多个空洞

---

## 与快速重传的协作

```
1. 段丢失 → dup ACK 开始累积
2. 3 dup ACK → 快速重传（不等 RTO）→ 进入快速恢复
3. 如果快速重传也失败 → RTO 计时器溢出 → 超时重传
```

---

## 自测题

1. 重传计时器在什么时候启动？什么时候关闭？
2. 收到部分 ACK（推进但未完全确认）时计时器如何处理？
3. 为什么重传后 RTO 要翻倍？

---

## 交叉引用

- RTO 估计：[14.3](14.3-rto-estimation.md)
- 快速重传：[14.5](14.5-fast-retransmit.md)
- 拥塞控制：[ch16](../../chapter16-tcp-congestion-control/study.md)
