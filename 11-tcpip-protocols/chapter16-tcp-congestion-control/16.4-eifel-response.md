# 16.4 伪 RTO：Eifel 响应

> 章级精读：[../study.md#ch16-4](study.md#ch16-4) · 伪超时：[ch14 §14.7](../chapter14-tcp-timeout-retransmit/14.7-spurious-timeout.md)

## 本节核心目标

理解伪超时对拥塞控制的影响和 Eifel 响应的撤销机制。

---

## 问题

无线等导致 **RTT 尖峰** → 伪超时 → cwnd 无谓减半 → 性能雪崩。

```
正常: cwnd=20, ssthresh=∞
伪超时: cwnd=1, ssthresh=10 → 吞吐崩塌
即使发现是伪超时 → cwnd 已被错误降低 → 需要时间恢复
```

---

## Eifel 检测

用**时间戳（TSOPT）**判断 ACK 对应**原传**还是重传：

```
原始段 TS=1000 → 丢失
重传段 TS=1000 → 到达
ACK TSecr=1000 → 但到达时间极早 → 原始段其实到了（伪超时）
```

---

## Eifel Response

伪超时确认后 → **撤销** ssthresh/cwnd 削减 → 恢复到超时前的拥塞状态。

---

## HFT 关联

HFT 有线环境伪超时少见，但跨数据中心 WAN 可能遇到路径切换尖峰。保持 `tcp_timestamps=1` 是基础。

---

## 自测题

1. 伪超时如何导致 cwnd 错误降低？
2. Eifel 检测利用了什么 TCP 选项？
3. Eifel Response 撤销了什么操作？

---

## 交叉引用

- 伪超时检测：[ch14 §14.7](../chapter14-tcp-timeout-retransmit/14.7-spurious-timeout.md)
- 经典算法：[16.2](16.2-classic-algorithms.md)
- TCP 选项：[ch13 §13.3](../chapter13-tcp-connection-manage/13.3-tcp-options.md)
