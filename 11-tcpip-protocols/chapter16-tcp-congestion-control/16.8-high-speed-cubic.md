# 16.8 高速 TCP：CUBIC

> 章级精读：[../study.md#ch16-8](study.md#ch16-8)

## 本节核心目标

理解标准 TCP 在高 BDP 链路的瓶颈和 CUBIC 算法。

---

## LFN 问题

标准 TCP **线性增 cwnd** → 填满高 **BDP** 需**数千 RTT**。

```
BDP = 10Gbps × 100ms = 125MB → cwnd 需 ~87000 MSS
线性增长: 87000 RTT ≈ 8700s ≈ 2.4 小时才填满！
```

---

## BIC / CUBIC

**CUBIC**（Linux 默认）：`W(t) = C(t-K)³ + W_max`

- 三次函数曲线
- 远离上次丢包点：**快增**
- 接近推测容量（W_max）：**缓增**
- 超过 W_max 后继续探测新上限

```
cwnd
 ↑          ╱ ← 超过 W_max 继续探测
 │        ╱
 │      ╱ ← 接近 W_max 缓增
 │    ╱
 │  ╱ ← 远离 W_max 快增
 │╱ ← 丢包后从 W_max/2 开始
 └────────────→ 时间
```

---

## Linux 配置

```bash
# 查看当前算法
sysctl net.ipv4.tcp_congestion_control
# 输出: cubic

# 切换到 BBR
sysctl -w net.ipv4.tcp_congestion_control=bbr

# BBR 需要 fq qdisc
tc qdisc add dev eth0 root fq
```

---

## HFT 关联

| 算法 | HFT 影响 |
|------|---------|
| CUBIC | Linux 默认，控制面够用 |
| BBR | 基于带宽探测，丢包不降速 → 可能影响延迟可预测性 |
| HFT 专用 | 行情走 UDP 不受影响 |

---

## 自测题

1. 标准 TCP 在 10Gbps × 100ms 链路上需要多久才能填满 BDP？
2. CUBIC 的三次函数曲线有什么优势？
3. BBR 和 CUBIC 的主要区别是什么？

---

## 交叉引用

- 经典算法：[16.2](16.2-classic-algorithms.md)
- 延迟型算法：[16.9](16.9-delay-based-cc.md)
- Bufferbloat：[16.10](16.10-bufferbloat.md)
