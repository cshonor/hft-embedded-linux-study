# 18.2 安全原则

> 章级精读：[../study.md](study.md)

## 本节核心目标

掌握网络安全的核心设计原则。

---

## 关键原则

| 原则 | 含义 |
|------|------|
| **纵深防御** | 多层安全，不依赖单一机制 |
| **最小权限** | 只授予必要的访问权限 |
| **默认拒绝** | 未明确允许的即拒绝 |
| **失败安全** | 出错时进入安全状态（如断开而非放行） |
| **开放设计** | 安全不依赖算法保密（Kerckhoffs 原则） |

---

## 信任边界

```
互联网 (不可信) → 防火墙 → DMZ (半可信) → 内网 (可信) → 核心交易 (高可信)
```

每跨越一个信任边界都需要认证 + 加密。

---

## HFT 关联

HFT 交易网络通常在数据中心内部（可信），但跨数据中心 WAN 需要加密。交易服务器应遵循最小权限原则——只开放必要的端口和连接。

---

## 自测题

1. Kerckhoffs 原则是什么？为什么安全不依赖算法保密？
2. "纵深防御"为什么比单层防御更可靠？
3. HFT 交易网络的信任边界在哪里？

---

## 交叉引用

- 安全引言：[18.1](18.1-introduction.md)
- 网络威胁：[18.3](18.3-network-threats.md)
