# 18.9 TLS 与 DTLS

> 章级精读：[../study.md](study.md) · PKI：[18.5](18.5-pki-certificates.md)

## 本节核心目标

理解 TLS 握手流程、TLS 1.3 改进和 DTLS。

---

## TLS 1.2 vs 1.3

| | TLS 1.2 | TLS 1.3 |
|--|---------|---------|
| 握手 | 2 RTT | **1 RTT** |
| 0-RTT | 不支持 | **支持**（会话恢复） |
| 密钥交换 | RSA/DHE/ECDHE | **仅 ECDHE** |
| 加密 | CBC/RC4/etc | **仅 AEAD** (AES-GCM/ChaCha20) |
| 证书 | 明文传输 | **加密** |

---

## TLS 1.3 握手

```
Client → Server: ClientHello (含密钥共享)
Server → Client: ServerHello + 证书 + Finished (加密)
Client → Server: Finished (加密)
→ 1 RTT 完成

0-RTT (会话恢复):
Client → Server: ClientHello + 早期数据 (加密)
→ 0 RTT 发送数据
```

---

## DTLS

DTLS = UDP 上的 TLS。解决 UDP 无连接下的安全问题（如 WebRTC、CoAP）。

| | TLS | DTLS |
|--|-----|------|
| 传输 | TCP | UDP |
| 可靠性 | TCP 保证 | **自行处理**重传 |
| 分片 | TCP 分段 | **自行处理**分片 |

---

## HFT 关联

| TLS 特性 | HFT 影响 |
|----------|---------|
| TLS 1.3 1-RTT | 首次连接延迟降低 |
| 0-RTT | 会话恢复几乎无延迟 |
| AEAD 加密 | AES-GCM 硬件加速 → CPU 开销低 |
| 证书加密 | 隐私提升 |

**注意**：TLS 0-RTT 有重放攻击风险——不适合非幂等交易指令。

---

## 自测题

1. TLS 1.3 相比 1.2 有哪些改进？
2. 0-RTT 的重放攻击风险是什么？如何缓解？
3. DTLS 和 TLS 的主要区别是什么？

---

## 交叉引用

- PKI：[18.5](18.5-pki-certificates.md)
- 密码学：[18.4](18.4-cryptography-basics.md)
- UDP：[ch10](../chapter10-udp-ip-fragment/study.md)
