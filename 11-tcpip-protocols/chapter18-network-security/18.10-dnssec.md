# 18.10 DNSSEC

> 章级精读：[../study.md](study.md) · DNS：[ch11](../chapter11-dns-domain-resolve/study.md)

## 本节核心目标

理解 DNSSEC 的签名链和验证机制。

---

## DNSSEC 解决的问题

传统 DNS 无认证 → 可被缓存投毒 → DNSSEC 通过数字签名验证数据来源。

---

## 签名链

```
根 (Root) → 签名 → TLD (.com) → 签名 → 权威 (example.com)
  ↑ K root key                ↑ DS record               ↑ RRSIG
```

| 记录 | 作用 |
|------|------|
| **RRSIG** | 记录的数字签名 |
| **DNSKEY** | 公钥（验证 RRSIG） |
| **DS** | 委派签名（父区对子区公钥的哈希） |
| **CDNSKEY/CDS** | 自动委派信号 |

---

## 验证流程

```
1. 递归器收到 example.com A 记录 + RRSIG
2. 用 example.com 的 DNSKEY 验证 RRSIG
3. 用 .com 的 DS 验证 example.com 的 DNSKEY
4. 用根的 DS 验证 .com 的 DNSKEY
5. 用信任锚（根 KSK）验证根 → 信任链完整
```

**DNSSEC 不加密 DNS** → 仅验证完整性 + 来源。加密需 DoT/DoH。

---

## HFT 关联

HFT 系统一般不依赖公网 DNS → DNSSEC 价值有限。但使用公网 DNS 时应启用验证。

---

## 自测题

1. DNSSEC 验证了什么？没有验证什么？
2. DS 记录的作用是什么？
3. 为什么 DNSSEC 不能替代 DoT/DoH？

---

## 交叉引用

- DNS 安全：[ch11 §11.12](../chapter11-dns-domain-resolve/11.12-dns-security-threat.md)
- 密码学：[18.4](18.4-cryptography-basics.md)
- PKI：[18.5](18.5-pki-certificates.md)
