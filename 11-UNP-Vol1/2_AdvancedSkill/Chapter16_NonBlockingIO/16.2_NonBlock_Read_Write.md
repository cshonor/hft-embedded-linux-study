# 16.2 非阻塞读和写：str_cli 函数（修订版）

> [Ch 6 str_cli](../../1_BasicFoundation/Chapter06_IO_Select_Poll/6.4_Str_Cli_Revised.md) · [Ch 6.7 终版](../../1_BasicFoundation/Chapter06_IO_Select_Poll/6.7_Str_Cli_Final_Revised.md)

---

## 核心主旨

Ch 6 用 `select` 同时监听 stdin 与套接字，仍不够健壮：

- 恶意服务器灌数据 → **stdout 阻塞**  
- 发送窗口满 → **`write` 阻塞**  
→ 整个 `select` 循环被挂起（DoS）

**终极方案**：stdin、stdout、套接字**全部非阻塞**。

---

## 用户态缓冲区管理

| 缓冲区 | 作用 |
|--------|------|
| **`to`** | 从 stdin 读入、待发往套接字 |
| **`fr`** | 从套接字读入、待发往 stdout |

每缓冲区维护 **`iptr`**（下次写入空闲位）、**`optr`**（下次读出有效数据）。

---

## 事件驱动逻辑

```text
to 有空闲 → 才把 stdin 加入 select 读集
to 有待发数据 → 才把 sockfd 加入 select 写集
fr 同理（sock 读、stdout 写）
每次 I/O 后调整 iptr/optr；排空则指针归位
```

---

## 重点结论

| 收益 | 代价 |
|------|------|
| 更高吞吐、抗死锁 | 代码量剧增、指针/状态易错 |

这正是 **Netty、libuv** 等框架封装非阻塞状态机的原因。

---

## 个人学习总结

（待填）
