# 30.6 预先派生子进程：accept 无上锁保护

> [30.7 文件锁](../30.7_PreFork_Server_FileLock.md) · [30.5 fork](../30.5_TCP_Fork_Concurrent_Server.md)

---

## Prefork 思想

启动时主进程 **`fork` N 个子进程**（进程池），避免每连接再 `fork`。

---

## 运作机制

**所有子进程**在**同一监听套接字**上直接 **`accept`** 阻塞。

---

## 惊群（Thundering Herd）

新连接到达 → **所有**阻塞在 `accept` 的子进程被唤醒 → **仅一个**成功，其余 **`EAGAIN`** 再睡 → CPU 浪费。

---

## 兼容性

Berkeley 系内核尚可；部分 **System V** 上多进程并发 `accept` 可能**内核混乱**。

---

## 个人学习总结

（待填）
