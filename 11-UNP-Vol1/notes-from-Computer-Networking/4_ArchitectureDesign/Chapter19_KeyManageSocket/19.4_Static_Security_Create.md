# 19.4 创建静态安全关联

> [19.3 SADB_DUMP](../19.3_Security_DB_Dump.md) · [19.5 动态 SA](../19.5_Dynamic_Security_Maintain.md)

---

## 核心场景

无 IKE 守护进程时，管理员用 **`SADB_ADD`** 向内核**硬编码** SA（含明文密钥）— **静态 SA**。

> 生产环境慎用；密钥长期不变风险高。

---

## 组装步骤（繁琐）

| 步 | 内容 |
|----|------|
| 1 | `sadb_msg`，`type = SADB_ADD` |
| 2 | **`sadb_sa`**：**SPI**（同目的 IP 下区分 SA 的唯一索引） |
| 3 | **`sadb_address`**：源 IP、目的 IP 各一块 |
| 4 | **`sadb_key`**：命令行十六进制密钥 → 二进制，附认证/加密密钥扩展 |
| 5 | **对齐**：每扩展长度为 **8 字节整数倍**，不足则 padding |
| 6 | **`write`** → 合法则 SADB 立即激活 |

---

## 个人学习总结

（待填）
