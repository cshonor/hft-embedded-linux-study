# 31.6 TPI：传输提供者接口

> [Ch 3 socket API](../../1_BasicFoundation/Chapter03_SocketProgramIntro/study.md) · [31.3 putmsg](../31.3_Getmsg_Putmsg_Func.md)

---

## 核心主旨

**TPI（Transport Provider Interface）**：基于 STREAMS 消息的规范，抽象底层传输（TCP、UDP、OSI 等）。

符合 TPI 的**驱动/提供者**即可接入同一套上层服务。

---

## 通信方式

应用通过 **`M_PROTO`** 控制消息操作网络，例如：

| TPI 消息 | 类比 Berkeley API |
|----------|-------------------|
| **`T_BIND_REQ`** | `bind()` |
| **`T_CONN_REQ`** | `connect()` |
| … | `listen` / `accept` 等均有对应原语 |

由库或应用 **`putmsg`** 下发。

---

## “套接字的错觉”（SVR4）

部分 System V 系统：

```text
socket()  → 打开 STREAMS 设备 + 压入模块
connect() → 库封装为 T_CONN_REQ + putmsg
```

内核里**可能根本没有**传统 Berkeley socket 实现 — **libc 翻译层**。

---

## 与 XTI / TLI

TPI 之上曾有 **TLI/XTI** API — 商业上曾风光，终被 **Sockets** 生态击败。

---

## 个人学习总结

（待填）
