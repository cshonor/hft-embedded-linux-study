# 18.5 get_ifi_info 函数

> [Ch 17.6 ioctl 版](../../3_DeepMaster/Chapter17_Ioctl_Operate/17.6_Get_Ifi_Info_Func.md) · [18.4 sysctl](../18.4_Sysctl_Operate.md) · [Ch 21 多播](../../3_DeepMaster/Chapter21_Multicast/study.md)

---

## 架构重构

书中**重写**第 17 章 `get_ifi_info` — 底层从 `ioctl(SIOCGIFCONF)` 改为 **`sysctl` + `NET_RT_IFLIST`**。

---

## 数据流解析

内核返回的缓冲区交织两类消息：

| 消息 | 内容 |
|------|------|
| **`if_msghdr`** | 接口级：索引、`sockaddr_dl`（名 + **MAC**） |
| **`ifa_msghdr`** | 地址级：该接口上 IPv4/IPv6 单播、掩码、广播 |

```text
遍历可变长流
  → 见 if_msghdr：新建 ifi_info 节点
  → 见后续 ifa_msghdr：把 IP/掩码/广播挂到该接口
```

---

## 相对 ioctl 版的优势

| 点 | 说明 |
|----|------|
| 无截断 | Two-Pass sysctl |
| IPv4 + IPv6 | 同一套解析路径 |
| MAC | 从 `sockaddr_dl` 直接得到 |
| 健壮性 | 不依赖加倍猜缓冲区 |

仍是多播、代理、拓扑发现程序的**基石**。

---

## 个人学习总结

（待填）
