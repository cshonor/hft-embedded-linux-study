# 1.4 错误处理：包裹函数

> Permalink · **1.4 错误处理：包裹函数**  
> 章级：[study.md#ch1-4](./study.md#ch1-4) · 前置裸写：[1.2 §六源码](1.2_Appendix_源码与考点.md#ch1-2-source) · 后章 EINTR：[Ch 5](../Chapter05_TCP_Client_Server_Demo/study.md) · [Ch 6](../Chapter06_IO_Select_Poll/study.md)

<a id="ch1-4"></a>

## 本节要会什么

原生 `socket` / `connect` / `read` 失败返回 **-1** 并置 **`errno`**；裸写要处处 `if (fd < 0) err_sys(...)`。  
UNP 约定：**首字母大写 = 包裹函数**，内部统一报错退出，业务代码只写一行 **`Socket(...)`**。

**记法**：`socket()` 小写 = 内核 API；`Socket()` 大写 = 本书封装。

---

<a id="ch1-4-core"></a>

## 一、核心主旨

| 痛点 | 包裹函数做法 |
|------|----------------|
| 每个系统调用后手写 `if (<0) err_sys` | 大写函数内部做完，调用方**只拿合法返回值** |
| 报错口径不统一 | 统一走 **`err_sys` / `err_quit` / `err_ret` / `err_dump`** |
| `close` 等常被忽略返回值 | `Close(fd)` 强制检查（原书后文习惯） |

---

<a id="ch1-4-wrap"></a>

## 二、标准包裹模板（图 1-7 · `Socket` 示例）

```c
int Socket(int family, int type, int protocol)
{
    int fd;
    if ((fd = socket(family, type, protocol)) < 0)
        err_sys("socket error");
    return fd;
}
```

调用方：

```c
int sockfd = Socket(AF_INET, SOCK_STREAM, 0);
/* 无需再 if (sockfd < 0) */
```

→ 仓库示例：[code/1.4/…/wrapper_socket.c](./code/1.4_ErrorHandlingWrapper/original_c/wrapper_socket.c)（含 **Socket / Connect / Accept**）

---

<a id="ch1-4-errno"></a>

## 三、`errno` 三条铁律

| # | 规则 |
|---|------|
| 1 | **仅当函数返回失败（常 -1）时**，`errno` 才有意义；**成功后不要读 errno** |
| 2 | 现代 Linux 上 `errno` 多为 **TLS**（每线程一份），多线程不会互相踩 |
| 3 | 下一次失败调用会**覆盖**旧值 → **出错后立刻读**（或在 `err_sys` 里立刻读） |

---

<a id="ch1-4-err-func"></a>

## 四、UNP 四大错误输出函数（`unp.h`）

| 函数 | 作用 | 是否退出 |
|------|------|----------|
| **`err_sys`** | 系统调用错；拼 **`errno`** + `strerror` | **`exit(1)`** |
| **`err_quit`** | 业务错（如 `argc` 不对）；**不读 errno** | **`exit(1)`** |
| **`err_ret`** | 只打印，**不退出** | 返回调用方 |
| **`err_dump`** | 打印 + 栈回溯 | **`abort()`** |

`err_sys` 典型实现（教学简化，完整见 libunp）：

```c
void err_sys(const char *msg)
{
    int err = errno;   /* 立刻保存 */
    fprintf(stderr, "%s: %s\n", msg, strerror(err));
    exit(1);
}
```

→ 示意源码：[err_sys_demo.c](./code/1.4_ErrorHandlingWrapper/original_c/err_sys_demo.c)

---

<a id="ch1-4-two-styles"></a>

## 五、两类 API 错误范式（易错）

### 1. Unix 风格（socket / connect / read / write …）

| 结果 | 行为 |
|------|------|
| 失败 | 返回 **-1**，**`errno`** 有值 → 包裹里 **`err_sys`** |
| 成功 | 返回 fd 或字节数 ≥0 |

### 2. Pthread 风格（`pthread_create` 等）— **不走 errno**

| 结果 | 行为 |
|------|------|
| 成功 | 返回 **0** |
| 失败 | 返回 **错误码本身**（非 0），**不设置 errno** |

```c
void Pthread_create(pthread_t *tid, const pthread_attr_t *attr,
                    void *(*func)(void *), void *arg)
{
    int n;
    if ((n = pthread_create(tid, attr, func, arg)) != 0)
        err_exit(n, "pthread_create error");  /* 用返回值，不是 errno */
}
```

（书中常用 `err_exit(errno, msg)` 一类把 **Pthread 返回值** 当第一个参数传入。）

---

<a id="ch1-4-special"></a>

## 六、三大特殊错误（不能一律 `exit`）

> 标准大写包裹默认**失败就退出**；下面三类要**自定义小写 + 分支**（Ch 5/6 落地）。

| errno | 场景 | 处理思路 |
|-------|------|----------|
| **`EINTR`** | 阻塞 `accept`/`read`/`connect` 被信号打断 | **重试**同一调用，不退出 |
| **`EAGAIN` / `EWOULDBLOCK`** | 非阻塞 I/O 暂无数据/缓冲区满 | 稍后重试或交事件循环 |
| **`ECONNRESET`** | 对端 RST，连接作废 | **close fd**，退出读写循环 |

---

<a id="ch1-4-when-not"></a>

## 七、什么时候**不用**大写包裹？

需要**捕获特定错误、不直接杀进程**时，用小写原生 API + 手动判断：

| 场景 | 原因 |
|------|------|
| `accept` 遇 **EINTR** 重试 | `Accept()` 会直接 `exit` |
| 忽略 **ECONNABORTED** 的 `accept` | 要分支，不能一刀 `err_sys` |
| 非阻塞 + **EAGAIN** | 属正常空转，不是致命错 |

---

<a id="ch1-4-cheat"></a>

## 八、易错汇总

| ❌ | ✅ |
|----|-----|
| 成功后还判断 `errno` | 只看**返回值**是否失败 |
| `pthread_create` 失败读 `errno` | 读**返回值** + `strerror(ret)` |
| 处处裸 `if (fd<0)` 与 `Socket` 混用同一 fd | 统一风格：业务层优先 **大写** |
| 所有错误都用 `Accept()` | **EINTR** 等场景必须小写 + 重试 |

---

<a id="ch1-4-source"></a>

## 九、源码归档（`code/1.4/…/original_c/`）

| 文件 | 内容 |
|------|------|
| [wrapper_socket.c](./code/1.4_ErrorHandlingWrapper/original_c/wrapper_socket.c) | **Socket / Connect / Accept** 包裹（图 1-7 风格） |
| [err_sys_demo.c](./code/1.4_ErrorHandlingWrapper/original_c/err_sys_demo.c) | **`err_sys` 简化实现** + 注释 |

依赖原书 **`unp.h` / `libunp`** 链接 `err_sys` 时，以附录 D 为准；`err_sys_demo.c` 可单独理解逻辑。

---

<a id="ch1-4-summary"></a>

## 十、个人学习总结

1. **目的**：统一错误处理，业务代码不被 `if (fd<0)` 淹没。  
2. **两套范式**：Unix **-1 + errno**；Pthread **返回错误码、不用 errno**。  
3. **规范**：常规路径用 **大写包裹**；**EINTR / EAGAIN / 可恢复 accept** 用小写 + 分支。  
4. **链路**：**1.2 裸写** → **1.4 包裹** → **Ch 5/6 改造包裹适配特殊 errno**。

**30 字**：小写系统调用；大写封装；失败 err_sys；Pthread 看返回值；EINTR 勿盲 exit。
