# 1.7 OSI 模型与 TCP/IP 分层

> Permalink · **1.7 OSI & TCP/IP 分层**  
> 章级：[study.md#ch1-7](./study.md#ch1-7) · Daytime 实例 [1.2](./1.2_SimpleTimeClient.md) / [1.5](./1.5_SimpleTimeServer.md) · Raw [Ch28](../../3_DeepMaster/Chapter28_RawSocket/study.md) · 链路 [Ch29](../../3_DeepMaster/Chapter29_DataLinkAccess/study.md)

<a id="ch1-7"></a>

## 本节要会什么

**OSI 七层 ↔ TCP/IP 四层**映射；**Socket API 在哪一层**；三类套接字能「穿透」到哪一层；收发时的**封装 / 解封装**。

---

<a id="ch1-7-osi-tcpip"></a>

## 一、分层映射速记

### 1. OSI 七层（上 → 下）

```text
7 应用 → 6 表示 → 5 会话 → 4 运输 → 3 网络 → 2 数据链路 → 1 物理
```

### 2. TCP/IP 四层（工程实际标准 · 套接字依托此模型）

| TCP/IP | 涵盖 OSI | 典型协议 |
|--------|----------|----------|
| **应用层** | 7+6+5（应用、表示、会话合并） | HTTP、DNS、**Daytime(13)**、FTP |
| **传输层** | 4 运输层 | **TCP**、**UDP** |
| **网际层** | 3 网络层 | IPv4、IPv6、ICMP |
| **链路层**（常含物理） | 2+1 | 以太网、ARP |

> **UNP 全书 Socket 编程**默认工作在：**应用层 ↔ 传输层之间**的边界。

---

<a id="ch1-7-socket"></a>

## 二、Socket API 的精准位置（核心）

| 层级 | 是什么 |
|------|--------|
| **应用层（用户态）** | 你写的 C/Rust（如 [1.2](./1.2_SimpleTimeClient.md) / [1.5](./1.5_SimpleTimeServer.md)）：业务、`read`/`write` 的缓冲区 |
| **系统调用分界** | **`socket` / `bind` / `connect` / `listen` / `accept` / `read` / `write`**：用户态 → 内核态的入口 |
| **内核协议栈** | **传输层**（TCP/UDP）、**网络层**（IP 路由）、**链路层**（ARP、以太网帧）均由内核实现 |

**一句话**：**应用程序通过 Socket 调用内核里的 TCP/IP 协议栈。**

系统调用会带来 **用户态↔内核态切换**，是开销来源之一 → 后文 **I/O 多路复用、零拷贝** 的优化动机。

---

<a id="ch1-7-sock-types"></a>

## 三、三类套接字与穿透层级（Ch28 / Ch29）

| 类型 | 示例 | 你能动到哪一层 | 说明 |
|------|------|----------------|------|
| **常规** `SOCK_STREAM` / `SOCK_DGRAM` | 1.2/1.5 TCP Daytime | **应用数据**；头由内核填 | 内核自动加 **TCP/UDP + IP + 链路** 头，程序员只见载荷 |
| **原始套接字** RAW | [Ch28](../../3_DeepMaster/Chapter28_RawSocket/study.md) | 常到 **网络层 IP** | 可自写传输层头或发 ICMP 等；内核主要帮你处理 IP 首部 |
| **链路层套接字** | [Ch29](../../3_DeepMaster/Chapter29_DataLinkAccess/study.md) | **数据链路层帧** | 读写以太网帧（MAC + 载荷），抓包、自定义二层 |

```text
普通 TCP/UDP socket：应用数据 ──► 内核加 TCP/UDP + IP + 以太网
Raw socket (Ch28)：     应用可带头 ──► 内核加 IP（绕过常规 TCP/UDP 栈路径）
链路 socket (Ch29)：    应用构造帧 ──► 直达二层
```

---

<a id="ch1-7-encap"></a>

## 四、封装与解封装（以 Daytime 回包为例）

### 发送（1.5 服务端 `Write` 时间字符串）

```text
应用层：时间字符串
  → Socket(write)
  → 内核：+ TCP 头（传输层）
  →       + IP 头（网络层）
  →       + 以太网帧头（链路层）
  → 物理比特流
```

### 接收（1.2 客户端 `read`）

```text
物理信号
  → 剥以太网头 → 剥 IP 头 → 剥 TCP 头
  → 净载荷进用户缓冲区（read）
```

> **抓包对照**：Wireshark 中同一连接可见 **Ethernet II → IPv4 → TCP → Data**；应用载荷即 Daytime 文本。→ [拓展](#ch1-7-todo)

---

<a id="ch1-7-cheat"></a>

## 五、易错

| ❌ | ✅ |
|----|-----|
| 以为 `read` 直接读「网线」 | `read` 读的是内核**已解封装**后的 TCP 字节流 |
| 普通 socket 能改 TCP 序号/IP 地址 | 要 **Raw / 链路** 套接字（Ch28/29） |
| OSI 七层与 TCP/IP 四层一一同名 | TCP/IP **合并了 OSI 上三层**为应用层 |

---

<a id="ch1-7-todo"></a>

## 六、拓展留白

| # | 主题 | 对接 |
|---|------|------|
| 1 | 用户态/内核态拷贝、上下文切换 | Ch6 多路复用、高级 I/O |
| 2 | Wireshark 逐层拆帧（Daytime 13 端口） | [wireshark-packet-analysis](../../../wireshark-packet-analysis/) |
| 3 | 与自顶向下 / TCP/IP 卷1 分层对照 | [top_down §1.5](../../../top_down/01_network_basics/1.5_protocol_layer_architecture/study.md) |

---

<a id="ch1-7-summary"></a>

## 七、个人学习总结

1. **TCP/IP 合并 OSI 上三层**为应用层，是工程简化；**Socket 隔离业务与内核协议栈**。  
2. **常规 TCP/UDP** 只在应用–传输边界；改 IP/TCP 头 → **Raw（Ch28）**；改 MAC 帧 → **链路（Ch29）**。  
3. **系统调用 = 进内核**，是性能与后续 **select/epoll、零拷贝** 的出发点。

**30 字**：OSI 七层；TCP/IP 四层；Socket 在用户态调内核栈；普通 socket 不见包头。

**链路**：1.2/1.5 普通 TCP → 1.7 分层 → Ch28 Raw → Ch29 链路。
