# 4.2 socket 函数

> [4.3 connect](../4.3_Connect_Function.md)

---

## 核心主旨与关键论据

进程要进行网络 I/O，必须先 **`socket`** 指定期望的通信协议类型，获得 **套接字描述符 `sockfd`**。

---

## 关键函数与结构体

```c
int socket(int family, int type, int protocol);
```

| 返回值 | 含义 |
|--------|------|
| ≥ 0 | **sockfd**（小非负整数） |
| -1 | 失败 |

### family（协议族）

| 常量 | 用途 |
|------|------|
| **`AF_INET`** | IPv4 |
| **`AF_INET6`** | IPv6 |
| **`AF_LOCAL` / `AF_UNIX`** | Unix 域（Ch 15） |
| **`AF_ROUTE`** | 路由套接字（Ch 18） |
| **`AF_KEY`** | 密钥管理（Ch 19） |

### type（套接字类型）

| 常量 | 对应传输 |
|------|----------|
| **`SOCK_STREAM`** | 字节流（TCP / SCTP 流） |
| **`SOCK_DGRAM`** | 数据报（UDP） |
| **`SOCK_SEQPACKET`** | 有序分组（SCTP） |
| **`SOCK_RAW`** | 原始（Ch 28） |

### protocol

- 通常 **`0`**：由系统根据 family+type 选默认（STREAM→TCP）  
- 可显式：`IPPROTO_TCP`、`IPPROTO_UDP`、`IPPROTO_SCTP`

---

## 易错细节：AF_ 与 PF_

| 历史 | 现状 |
|------|------|
| `AF_` = Address Family，`PF_` = Protocol Family | 曾设想一对多，实际**相等** |
| **标准建议** | 调用 **`socket` 一律用 `AF_` 前缀** |

---

> 💡 **后续拓展留白**  
> - `socket` + `SOCK_CLOEXEC`（Linux）  

---

## 个人学习总结

（待填）
