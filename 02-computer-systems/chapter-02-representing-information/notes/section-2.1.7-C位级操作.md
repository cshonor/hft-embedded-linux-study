## 2.1.7 C 语言中的位级操作

> **Ch2 §2.1** · [章导读](../README.md) · 上节 [§2.1.6 布尔](./section-2.1.6-布尔代数简介.md) · 下节 [§2.1.8 逻辑](./section-2.1.8-C逻辑操作.md)

这张表就是 C 里专门操作 **二进制位** 的运算符 — 和 [§2.1.8](./section-2.1.8-C逻辑操作.md) 的 `&&`/`||`/`!`（逻辑真假）不是一套。

---

| 运算符 | 含义 | 典型用途（口述对应） |
|--------|------|----------------------|
| `&` | 按位与 | **掩码**：提取某几个特定位（`x & mask`） |
| `\|` | 按位或 | **合并/置位**：把标志位设进去（`x \| flag`） |
| `^` | 按位异或 | **切换/翻转** 某几位（同位再异或一次还原） |
| `~` | 按位取反 | 把整个数的 **0↔1 全部颠倒**（常配合掩码求补） |
| `<<` `>>` | 左/右移 | **×2 / ÷2（2 的幂）**，或把数据 **挪到位字段位置** → [§2.1.9](./section-2.1.9-C移位操作.md) |

```c
/* 取第 k 位（从 0 起） */
int get_bit(unsigned x, int k) { return (x >> k) & 1; }

/* 第 k 位置 1 */
unsigned set_bit(unsigned x, int k) { return x | (1u << k); }

/* 第 k 位翻转 */
unsigned toggle_bit(unsigned x, int k) { return x ^ (1u << k); }

/* 协议常见：从 word 中抠 bit 域 */
uint32_t field = (word >> shift) & mask;
```

**HFT：** 操作寄存器标志、解析网络包 / SBE header — **shift + mask** 是热路径基本动作；保证 `mask` 与 `shift` 来自 schema，别手写魔数散落各处。

---

### 口述巩固 · 自测

1. `&` / `|` / `^` / `~` / 移位各干什么？举一个「掩码提取」和一个「标志合并」的例子。  
2. 解码 16 位 header 里的 5-bit 字段，典型步骤是什么？（shift + mask）  
3. 位运算和逻辑运算（`&&`/`||`）差在哪？→ [§2.1.8](./section-2.1.8-C逻辑操作.md)

### 自测题

<details>
<summary>1. 如何用位运算设置、清除、翻转、测试第 n 位？</summary>

假设 `mask = 1 << n`：
- **设置**：`x |= mask`
- **清除**：`x &= ~mask`
- **翻转**：`x ^= mask`
- **测试**：`(x & mask) != 0`

HFT 场景：硬件寄存器控制（中断使能/状态标志位）、协议 flags 字段解析。注意位编号从 0 开始（LSB = bit 0）。

</details>

<details>
<summary>2. `x & (x-1)` 的效果是什么？有什么用途？</summary>

`x & (x-1)` 清除 x 的**最低有效 1 位**（lowest set bit）。例如 `12 & 11 = 1100 & 1011 = 1000`（清除了 bit 2）。用途：1. **判断 2 的幂**：`x & (x-1) == 0` 则 x 是 2 的幂
2. **计算 popcount（1 的个数）**：循环 `count++; x &= x-1` 直到 x=0
3. **找最低 set bit 位置**：`x & ~(x-1)` 提取最低 set bit

</details>


---

← [本章导读](../README.md) · [§2.1.6 ←](./section-2.1.6-布尔代数简介.md) · [§2.1.8 →](./section-2.1.8-C逻辑操作.md)
