## 5.13 现实生活：性能提高技术

> **Ch5 §5.13** · [章导读](../README.md) · 上节 [§5.12 ←](./section-5.12-加载与存储性能.md) · 下节 [§5.14 →](./section-5.14-确认和消除性能瓶颈.md)

---

| 层级 | 手段 |
|------|------|
| 算法 | 更低复杂度、更好数据结构 |
| 源码 | 本章 5.4–5.12 |
| 编译 | `-O3`、`-march=native`、PGO、LTO |
| 并行 | 多线程、绑核、无锁（→ [Ch 12](../../chapter-12-concurrent-programming/)） |
| 系统 | hugepage、NUMA、隔离核（→ [16-HFT](../../../16-hft-engineering/)） |

**顺序：** 先正确 + profile，再小步改；每次改 **测一遍**。

---

### 常见陷阱

1. **跳过 profile 直接「优化」** — 凭直觉改代码，可能优化了非瓶颈部分（阿姆达尔定律：占 5% 的函数优化 2× 只提升 2.5%）。先 profile 找到 >10% 的热点。
2. **优化顺序错误** — 先调系统层（hugepage/NUMA）再调算法层，本末倒置。正确顺序：算法 → 源码 → 编译 → 并行 → 系统，每层收益递减。
3. **改完不测** — 每次优化后必须用**同一 workload、同一硬件、同一编译 flags** benchmark。微基准只验证 CPE，端到端用 replay 验证 P99。

### 自测题

<details>
<summary>1. 性能优化的正确层级顺序是什么？为什么？</summary>

**算法 → 源码 → 编译 → 并行 → 系统**。算法层收益最大（O(n²) → O(n) 是数量级提升）；源码层是本章内容（循环优化等）；编译层（O3/LTO/PGO）是自动化收益；并行层（多线程/绑核）需要前面打好基础；系统层（hugepage/NUMA）是最后几%的微调。**每层收益递减，跳层优化是浪费**。
</details>

<details>
<summary>2. 为什么不能跳过 profile 直接优化？</summary>

阿姆达尔定律：如果某函数占总时间 5%，即使优化到 0 也只提升 5%。凭直觉改代码很可能优化了非瓶颈部分。先 profile 找到占 >10% 的热点，集中精力改最大瓶颈，投入产出比最高。
</details>

<details>
<summary>3. HFT 优化的标准工作流是什么？</summary>

①生产/trace replay 收集数据 → ②`perf record` 采样 → ③火焰图找热点 → ④改热函数 → ⑤回归 P99 延迟（不只看平均）→ ⑥确保 binary flags 与生产对齐 → ⑦跑 regression + 压力测试。每次只改一个变量。
</details>

---

← [§5.12 ←](./section-5.12-加载与存储性能.md) · [本章导读](../README.md) · [§5.14 →](./section-5.14-确认和消除性能瓶颈.md)
