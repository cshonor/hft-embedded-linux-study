## 5.8 循环展开 (Loop Unrolling)

> **Ch5 §5.8** · [章导读](../README.md) · 上节 [§5.7 ←](./section-5.7-现代处理器抽象模型.md) · 下节 [§5.9 →](./section-5.9-提高并行性.md)

---

**手动或编译器展开：**

```c
// 2 路展开
for (i = 0; i < n-1; i += 2) {
    acc = (acc + data[i]) + data[i+1];
}
// 处理尾部 n%2
```

**收益：**

- 减少 **分支**（每迭代处理多元素）
- 增 **指令级并行** — 多条独立 load/add 可并行发射
- 常与 **多累加器** 联用

**代价：** 代码膨胀、I-cache 压力、寄存器压力（→ 5.11）

```bash
gcc -O3 -funroll-loops ...
```

---

### 常见陷阱

1. **展开因子太大导致寄存器溢出** — 展开太多路，寄存器不够用，编译器把变量溢出到栈内存，反而变慢。通常 2-4 路展开收益最佳，超过后边际递减。
2. **忘了处理尾部剩余元素** — `n` 不一定是展开因子的整数倍，循环后要处理 `n % k` 个剩余元素，否则结果错误。
3. **手动展开干扰编译器自动优化** — `-O3 -funroll-loops` 已经会自动展开；手写展开可能干扰编译器的向量化和其他优化。先测编译器自动展开的效果，不够再手动。

### 自测题

<details>
<summary>1. 循环展开为什么能提高性能？</summary>

三个原因：①减少**分支指令**（每迭代处理多个元素，分支数减少）；②增加**指令级并行**——多条独立的 load/add 可并行发射到不同执行单元；③减少循环开销（计数器更新、条件判断）。常与多累加器联用打破串行依赖。
</details>

<details>
<summary>2. 循环展开的代价是什么？</summary>

**代码膨胀**（二进制变大）、**I-cache 压力**（指令多到放不进 cache 反而变慢）、**寄存器压力**（展开太多路寄存器不够用，溢出到内存）。过度展开可能负优化。
</details>

<details>
<summary>3. 手动展开还是用 -funroll-loops？</summary>

优先用 `-O3 -funroll-loops` 让编译器自动展开——它能处理尾部、选择最优因子、配合向量化。手动展开可能干扰编译器优化。**先测自动展开效果，不够再手动**，且手动展开后务必 benchmark 验证。
</details>

---

← [§5.7 ←](./section-5.7-现代处理器抽象模型.md) · [本章导读](../README.md) · [§5.9 →](./section-5.9-提高并行性.md)
