# 3.1 调试到底在做哪四件事

[← CH3 调试器心智模型](./README.md) · 下一节：[3.2 命令对照表 →](./3.2-命令对照表.md)

## 本节讲什么

把调试这件事剥到第一性原理：**四件事**。任何调试器（VS / Xcode / VS Code / lldb / gdb）
都是这四件事的组合。认领这四件事，你就不会再问"这个 IDE 的调试功能怎么用"。

## 要点

### 四件事

| # | 做什么 | 回答的问题 | VS 里的入口 | lldb 命令 |
|---|--------|-----------|-------------|-----------|
| ① | **让它停下来** | "第 34 行执行之前，程序是什么状态？" | 行号左边点红点 / F9 | `breakpoint set` / `b` |
| ② | **往前挪一点** | "下一步执行完会变成什么？" | F10（跳过函数）/ F11（进函数） | `next` / `step` |
| ③ | **看当前状态** | "变量现在的值是多少？内存里是什么？" | 悬停、监视窗口、局部变量窗口 | `frame variable` / `p` / `x` |
| ④ | **看怎么来的** | "是谁调用了我、带着什么参数？" | 调用堆栈窗口 | `bt` / `thread backtrace` |

**第 ④ 件事是新手最缺、也最省时间的一件事。** printf 调试法永远看不到调用栈，
而调用栈往往直接告诉你答案。

### 每层解剖（对照同一个 demo）

本模块的 [demo/ptrbug.c](../demo/ptrbug.c) 长这样：

```c
static int buf[N] = { 0, 0, 0, 0 };

static int sum_n(const int *a, int n)      /* N == 4，但调用方传了 8 */
{
    int s = 0;
    for (int i = 0; i < n; i++) {
        s += a[i];                          /* ← 第 34 行 */
    }
    return s;
}
```

| 四件事 | 在这个 demo 上怎么用 |
|--------|---------------------|
| ① 停下来 | 在 34 行下断点，每次进入循环都停一次 |
| ② 往前挪 | `next` 逐次看 `i` 从 0 涨到 7 |
| ③ 看状态 | `frame variable` 同时给出 `a`、`n`、`s`、`i` |
| ④ 看来源 | `bt` 告诉你它从 `main` 的哪一行来（区分 `n=4` 那次还是 `n=8` 那次） |

**第 ④ 件事在这里是决定性的**：`sum_n` 被调用了两次，只有看调用栈才知道当前这次是
`n=4` 的正确调用，还是 `n=8` 的越界调用。✅ 实测输出见 [3.3](./3.3-Mac实测lldb.md)。

### 花式变体其实都是①的组合

| 听起来高级的功能 | 本质 |
|------------------|------|
| **条件断点**（只在 `n==8` 时停） | ① + 一个表达式判断 |
| **数据断点 / 观察点**（某变量被改写时停） | ① + 硬件 facilities / 内存页保护 |
| **命中次数断点**（第 100 次才停） | ① + 一个计数器 |
| **单步 / 步入 / 步出** | ② 的三种粒度 |
| **监视窗口表达式求值** | ③ 的扩展 |
| **反汇编窗口** | ③ 但看的是汇编而非源码 |

认出这一点，你就不会因为某个 IDE 没有某按钮而焦虑 —— **它只是换个地方组合同样四件事**。

### 一个容易被忽略的前提：这些能力依赖调试信息

调试器能显示变量名和源码行，靠的不是二进制本身，而是
[2.5 节](../02-msvc-compiler-and-options/2.5-调试信息三种容器.md)讲的 DWARF / PDB：

```text
DW_AT_name  ("sum_n")     DW_AT_decl_line (30)
  DW_AT_name  ("a")       ← 参数名靠这里
  DW_AT_name  ("n")
  DW_AT_name  ("s")
```

✅ 实测（`llvm-dwarfdump ptrbug.o`）。所以：

| 编译方式 | 变量窗能不能显示名字 |
|----------|---------------------|
| `clang -g` | ✅ 能（DWARF 在 `.o` / dSYM 里） |
| `clang`（无 `-g`） | ❌ 只能看汇编和地址 |
| `clang -O2 -g` | ⚠️ 部分变量会显示 `<optimized out>` |

## 与后续衔接

知道做哪四件事之后，就差具体的**拼写**了 —— [3.2 命令对照表](./3.2-命令对照表.md)。
