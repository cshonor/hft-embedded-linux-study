[package]
name = "thread-pool-demo"
version = "0.1.0"
edition = "2021"
description = "The Book ch20: 线程池 + 优雅停机（Arc<Mutex<Receiver>> / Drop 发停机信号）"
publish = false

# 纯 std，无外部依赖 —— 本章的目的就是用标准库把并发骨架讲透。
[dependencies]
