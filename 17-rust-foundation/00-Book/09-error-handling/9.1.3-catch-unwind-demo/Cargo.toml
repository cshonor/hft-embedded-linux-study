[package]
name = "catch_unwind_demo"
version = "0.1.0"
edition = "2021"

[dependencies]

# release 默认本 demo 用 abort，对比 catch 失效
[profile.release]
panic = "abort"

# 自定义：release 优化 + unwind，临时调试 catch
[profile.release-unwind]
inherits = "release"
panic = "unwind"
debug = true
