# 10.3 生命周期与引用有效性

> **本章已拆分为专题笔记**；本文档为**索引 + 核心结论**，细节见下表。

对应 demo：`00-Book/10-generics-traits-lifetimes/10.3-lifetimes-demo/`  
所有权：[4.1-什么是所有权.md](../04-ownership/4.1-什么是所有权.md)

---

## 专题目录

| 文档 | 内容 |
|------|------|
| **本文** | 核心结论、最终三句、导读、记忆卡片 |
| [10.3.1 悬垂引用](./10.3.1-悬垂引用.md) | 定义、六类场景、机制 A～D |
| [10.3.2 同 `'a` 与红线](./10.3.2-同a约束与红线.md) | **`use_both` / `longest`、速查表**（重点） |
| [10.3.3 生命周期基础](./10.3.3-生命周期基础.md) | 变量寿命 vs `'a`、作用域、`'static str` |
| [10.3.4 longest 与 get_first](./10.3.4-longest与get_first.md) | 返回引用示例、场景 A/B |
| [10.3.5 显式与隐式](./10.3.5-显式与隐式生命周期.md) | 省略规则 elision |
| [10.3.6 结构体 / static / 泛型](./10.3.6-结构体-static与泛型.md) | 结构体持引用、组合 |
| [10.3.7 匿名生命周期 `'_`](./10.3.7-匿名生命周期与下划线.md) | **`'a` vs `'_`** · 何时写 · 何时必须命名 |

**建议阅读顺序**：10.3.3 → 10.3.7（`'_` 速通）→ 10.3.2 → 10.3.4 → 10.3.1（按需）→ 10.3.5 / 10.3.6

---

## 核心结论

生命周期标记（`'a`、`'static`）**不创建引用**，只约束：**引用在使用期间，指向的数据必须一直有效**。

> 入参、返回值都标 `'a` → 返回的引用**来自**带同一 `'a` 的入参，**不能比该组 `'a` 活得更久**。

---

## 最终三句（唯一结论，可直接复制）

> 与 **rustc / demo §0.8** 一致。详解 [10.3.2](./10.3.2-同a约束与红线.md)

```
【Rust 生命周期 · 同 'a 三条】

1. 无返回值函数
   入参一长一短 + 共用 'a → 永远不报错。
   编译器把 'a 收缩到最短作用域；该块内被借数据都活着。

2. 有返回值函数（返回 &'a T）
   返回值留在最短作用域内 → 不报错；
   返回值跑到最短作用域外 → 报错（悬垂）。

3. 'a 的本质
   'a = 引用的使用边界，不是「两变量必须同时 drop」。
   只要求：在 'a 范围内，所有被引用的数据都活着。
```

**canonical 实测（无返回值，永远 ✅）**：

```rust
fn use_both<'a>(a: &'a i32, b: &'a i32) {}

fn main() {
    let long_live = 100;
    {
        let short_live = 200;
        use_both(&long_live, &short_live);
    }
}
```

---

## 导读

生命周期**最常考**的是「函数返回引用」，但本质是**所有 `&` 的存活区间约束系统**。

| # | 问题域 | 专题 |
|---|--------|------|
| 1 | 嵌套 `{}` 跨块借用 | [10.3.1](./10.3.1-悬垂引用.md) |
| 2 | 多入参 `'a` / `'b` 分组 | [10.3.2](./10.3.2-同a约束与红线.md) |
| 3 | 结构体持引用 | [10.3.6](./10.3.6-结构体-static与泛型.md) |
| 4 | 借用重叠 | [4.2 引用与借用](../04-ownership/4.2-引用与借用.md) |
| 5 | `'static` | [10.3.3](./10.3.3-生命周期基础.md)、[10.3.6](./10.3.6-结构体-static与泛型.md) |
| 6 | 省略规则 | [10.3.5](./10.3.5-显式与隐式生命周期.md) |
| 7 | `get_first` / `longest` | [10.3.4](./10.3.4-longest与get_first.md) |

**考点 vs 工程**：面试多考「返回引用 + 多参数 `'a`」；工程上还防跨块悬垂、结构体持引用等。

---

## 记忆卡片

| 要点 | 一句话 |
|------|--------|
| `'a` | 时长分组；**区间内**被借数据须都存活 |
| 红线 | [10.3.2](./10.3.2-同a约束与红线.md) |
| 无返回值 | 内层一长一短 + 同 `'a` → **✅** |
| 有返回值 | `res` 跨红线 → **❌** |
| 最终三句 | 见上文可复制块 |
| 悬垂 | [10.3.1](./10.3.1-悬垂引用.md) |
| 显式/隐式 | [10.3.5](./10.3.5-显式与隐式生命周期.md) |
| 结构体 | [10.3.6](./10.3.6-结构体-static与泛型.md) |
