# Effective Rust — 目录结构

> **约定**：一个 Item = 一个目录；笔记与相关代码放在同一目录下，便于日后扩展。

## 布局

```
01-ER/
├── ER-本书目录.md          # 全书索引
├── ER-拓展索引.md          # §6 拓展汇总
├── 目录结构.md             # 本文件
├── Chapter-01-Types/
│   ├── Item-03-option-result-transforms/
│   │   ├── README.md       # 导航 hub（状态、对照、专题索引）
│   │   ├── 01-core-concepts.md
│   │   └── demo/
│   └── Item-01-express-data-structures/
│       ├── README.md
│       └── demo/           # 可选
├── …
├── Cargo.toml              # workspace 根（members 指向各 Item/*/demo）
├── rust-toolchain.toml
├── ER-demos/               # MSRV / clippy 文档与配置
│   ├── WORKSPACE.md
│   └── README.md
└── scripts/
```

## 命名

| 路径 | 含义 |
|------|------|
| `Item-NN-slug/README.md` | **导航 hub**：状态、Book 对照、一句话、专题索引 |
| `Item-NN-slug/*.md` | **专项笔记**：每个文件只讲一块，与 README 同级 |
| `Item-NN-slug/demo/` | 主 demo crate（多数 Item） |
| `Item-NN-slug/demo-*/` | 额外 demo（如 Item 4 的 `demo-core-error`、Item 35 的 `demo-bindgen` / `demo-sys-workspace`） |

## 笔记拆分约定

长 Item 拆成多个 **与 README 同级** 的 `.md` 文件，`README.md` 只做目录：

```
Item-02-express-common-behavior/
├── README.md              # hub（~50 行）
├── 01-core-concepts.md
├── 02-logic-flow.md
├── 03-key-takeaways.md
├── 04-examples.md
├── 05-pitfalls.md
```

Item 01 按知识点拆得更细（`01-fundamental-types` … `07-pitfalls`）；其余 Item 统一用上面 6 个文件名。

- 历史拆分脚本（已弃用 topics/ 子目录）：`scripts/split-item-topics.py`
- 扁平化迁移（topics → Item 根目录）：`scripts/flatten-item-topics.py`

## 新增 Item 内容时

1. 在对应 `Item-NN-slug/README.md` 维护 hub；正文放进同级专项 `.md` 文件。
2. 若有代码，在同级建 `demo/`（或 `demo-foo/`），并在 [`01-ER/Cargo.toml`](./Cargo.toml) 的 `members` 里加入路径。
3. 在 [ER-拓展索引.md](./ER-拓展索引.md) 与 Item 的 §6 中更新链接。

## 运行 demo

```bash
cd 01-ER
cargo test --workspace
```

独立 workspace（Item 24、Item 35 sys）在各自 `demo/` 或 `demo-sys-workspace/` 目录内运行，见各 README。
