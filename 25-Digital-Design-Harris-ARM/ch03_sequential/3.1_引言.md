# 3.1 引言

> **Core Concept:** 组合无记忆；时序有状态；同步用统一时钟切拍  
> **Link Target:** [2.9 组合延迟/毛刺](../ch02_combinational/2.9_时序.md) · [3.2 FF](./3.2_锁存器和触发器.md) · [3.3 同步](./3.3_同步逻辑设计.md)  
> **档位:** 浅读

## 状态

- [x] 已读（口述巩固）
- [x] 已写要点
- [x] 组合 vs 时序；同步 vs 异步分界

## 笔记

### 两大基础电路

| | **组合逻辑** | **时序逻辑** |
|--|--------------|--------------|
| 存储 | **无** 触发器/锁存器 | **有**（记忆单元） |
| 输出决定 | **仅当前输入** | **当前输入 + 过去保存的状态** |
| 并行 | 天然多路同时算 | 可在时钟节拍内多路同时算（受控） |
| 缺陷 / 能力 | 有 tp、毛刺；**存不住**；输入一变结果就变 | 能锁存稳定结果，可规模化搭系统 |

组合细节（延迟、毛刺）→ [2.9](../ch02_combinational/2.9_时序.md)。  
「立刻决定」是 **逻辑关系**；物理上仍要等传输延迟才稳定。

时序再分：

| | **同步时序** | **异步时序** |
|--|--------------|--------------|
| 时钟 | **全体 FF 共用同一 CLK**，边沿统一更新 | 无统一时钟，信号互相触发 |
| 工业 | CPU / MCU **主流** | 难分析、易不稳；通用处理器基本不用大规模异步 |

### 本章主线

```
存储单元（锁存/触发器）     → 3.2
  → 同步电路规范             → 3.3
  → 有限状态机 FSM           → 3.4
  → setup/hold · 亚稳态      → 3.5
  → 可控并行                 → 3.6
```

现代处理器的寄存器、流水线锁存、外设 FSM、同步约束，都建在本章之上。
