# 9.1 驯服垃圾回收 (GC) 🔴

> STW 暂停全部应用线程对 HFT 是毁灭性的 — 选低停顿 GC，或让 GC 无事可做

← 上一章：[chapter-08 C++](../chapter-08-超低延迟核心引擎开发/README.md) · 下一节：[9.2 JVM 预热与分层编译](./9.2-JVM预热与分层编译.md)

---

GC 清理/整理堆时可能 **Stop-The-World (STW)** — **暂停全部应用线程** — 对 HFT **毁灭性**。

## 选型

| GC | HFT 适用性 |
|----|------------|
| **CMS** | 并发标记 — 老年代 · 已逐步淘汰 |
| **G1** | 可预测停顿目标 · 常用基线 |
| **Shenandoah / ZGC** | **超低 STW** · 大堆友好 |
| **Epsilon** | **只分配不回收** — 内存耗尽即退出 · **极端最低延迟**（生命周期完全自控时） |

**原则：** 选 **并发/低停顿** GC；或 **Epsilon + 预分配/池化** 使堆 **永不触发回收**。

## 零对象创建 (Zero Object Creation)

| 热点路径 | 禁止 |
|----------|------|
| **`new` 短生命周期对象** | 分配在 **Eden** → 频繁 **Minor GC** |
| **Autoboxing** | `int` ↔ `Integer` — 隐式 **装箱对象** |
| **String 拼接** | 每次 `+` 产生新 `String` |

## 对象池与基础类型

| 实践 | 说明 |
|------|------|
| **对象池** | 启动预分配 · 运行时 **复用** |
| **Primitive types** | `int` / `long` / `double` — **更小 · cache 友好** |
| **避免包装类** | `Integer` / `Double` 在热路径 **禁用** |
| **堆外 (Off-heap)** | DirectByteBuffer / Chronicle — GC **不可见** 的区域（进阶） |

→ C++ 对照：[chapter-08 §3 动态内存与异常](../chapter-08-超低延迟核心引擎开发/8.3-动态内存与异常.md)

---

← 下一节：[9.2 JVM 预热与分层编译](./9.2-JVM预热与分层编译.md) · 返回：[本章索引](./README.md)
