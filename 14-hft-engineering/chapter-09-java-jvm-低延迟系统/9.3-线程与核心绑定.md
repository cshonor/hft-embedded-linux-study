# 9.3 高性能 Java 线程与核心绑定

> 复用线程、任务分池、绑核同 NUMA — 与 C++ 同一套 OS 哲学

← 上一节：[9.2 JVM 预热与分层编译](./9.2-JVM预热与分层编译.md) · 下一节：[9.4 Disruptor 与环形缓冲](./9.4-Disruptor与环形缓冲.md)

---

## 线程池

| 原则 | 说明 |
|------|------|
| **复用线程** | 避免频繁 `new Thread` — **内存 + 切换** 开销 |
| **任务分离** | **I/O / 日志 / 报表** 与 **μs 核心路径** 不同池 |
| **少即是多** | 过多线程 → **上下文切换** 恶化 tail latency |

## 线程亲和性 (Thread Affinity)

| 层 | 手段 |
|----|------|
| **OS** | `isolcpus` + `taskset` — [Ch5 §2](../chapter-05-操作系统内核极致调优/5.2-CPU隔离与核心绑定.md) |
| **Java** | **`OpenHFT/Java-Thread-Affinity`** — 代码层 **绑核** |
| **Spinning** | 热点线程 **忙等** 队列 — 与 C++ Bypass **同哲学** |

## NUMA

共享内存通信的线程 **绑定同一 NUMA 节点** — 避免 **跨节点远程内存** 访问。

→ [chapter-04 §1 NUMA](../chapter-04-硬件选型与服务器配置/4.1-NUMA超线程与缓存金字塔.md)

---

← 下一节：[9.4 Disruptor 与环形缓冲](./9.4-Disruptor与环形缓冲.md) · 返回：[本章索引](./README.md)
