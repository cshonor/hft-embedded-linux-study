# 9.5 性能测量与日志

> JVM 会扭曲手写基准 — 用 JMH；格式化与 I/O 永不进热路径

← 上一节：[9.4 Disruptor 与环形缓冲](./9.4-Disruptor与环形缓冲.md) · 下一节：[9.6 Java 在 HFT 中的分工](./9.6-Java在HFT中的分工.md)

---

## JMH 微基准

Java 微基准 **极难** — JVM **DCE（死代码消除）**、**内联**、**GC** 会 **扭曲** 手写 `System.nanoTime()` 循环。

| 工具 | 说明 |
|------|------|
| **JMH (Java Microbenchmark Harness)** | OpenJDK 官方 · **`@Benchmark` + `@Fork` + warmup** |
| **原则** | **Blackhole** 消费结果 · 多 **fork** · 报告 **percentile** |

```java
@BenchmarkMode(Mode.AverageTime)
@OutputTimeUnit(TimeUnit.NANOSECONDS)
@Warmup(iterations = 5)
@Measurement(iterations = 10)
public void ringPublish(RingState s) {
    s.ring.publish(s.event);
}
```

→ C++ 对照：[chapter-10 §5 rdtsc / T2T](../chapter-10-延迟测量与基准压测/10.5-精确测量与基准压测.md)

## 无锁异步日志

| 问题 | 对策 |
|------|------|
| **磁盘 I/O 慢** | **Disruptor 环** → 专用 **Logger 线程** |
| **String 分配** | 热点 **二进制 blob** · 后台格式化 |
| **log4j zeroGC** 等 | **零 GC** 日志框架 — 预分配 appenders |

**原则：** 与 C++ 相同 — [Ch10 §4 异步日志](../chapter-10-延迟测量与基准压测/10.4-异步日志.md) — **格式化永不进热路径**。

---

← 下一节：[9.6 Java 在 HFT 中的分工](./9.6-Java在HFT中的分工.md) · 返回：[本章索引](./README.md)
