# 5.1 上下文切换的代价

> 切换不只是 μs 级调度——更致命的是破坏 L1/L2 工作集

← [本章索引](./README.md) · 下一节：[5.2 CPU 隔离与核心绑定](./5.2-CPU隔离与核心绑定.md)

---

**Context Switch：** OS **保存** 当前线程/进程状态，**恢复** 另一线程运行。

| 开销 | HFT 视角 |
|------|----------|
| **保存 PCB** | 寄存器、栈指针等 — CPU 周期 |
| **Cache Invalidation** | 新线程 **冷缓存** |
| **TLB 刷新** | 页表映射缓存失效 → **内存 walk** |
| **恢复后 Cache Miss** | 从 **RAM（~60ns 级）** 重拉代码/数据 — [chapter-04 缓存金字塔](../chapter-04-hardware-selection-server-config/4.1-NUMA超线程与缓存金字塔.md) L1 **~0.5ns** 对比 |

**对 HFT：** 切换不仅是 **μs 级调度** — 更致命的是 **破坏 L1/L2 工作集**，原线程 **恢复延迟暴增**。

**触发源：** 公平调度 · **I/O 阻塞 syscall** · **锁等待** · **IRQ** — 热点路径应 **逐一剔除**。
