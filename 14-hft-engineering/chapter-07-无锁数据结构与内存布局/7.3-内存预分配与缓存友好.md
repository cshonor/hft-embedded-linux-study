# 7.3 内存预分配与缓存友好 🔴

> 热点数据常驻 L1/L2；malloc 出现在热路径 = 延迟赌博

← 上一节：[7.2 无锁 FIFO 队列](./7.2-无锁FIFO队列.md) · 下一节：[7.4 共享内存 IPC](./7.4-共享内存IPC.md)

---

## 延迟数量级

| 访问 | 约 |
|------|-----|
| **L1** | ~0.5 ns |
| **RAM** | ~60 ns |

**目标：** 热点数据 **常驻 L1/L2**。

## 空间与时间局部性

| 原则 | 实践 |
|------|------|
| **Spatial** | **`std::vector` / 数组** > `std::list` 指针追逐 |
| **Temporal** | 同一 order/book 结构 **反复访问** |
| **False sharing** | `read_idx` / `write_idx` **不同 cache line**（`alignas(64)`） |

## 消除不可预测分支

| 避免 | 原因 |
|------|------|
| **虚函数** | vtable · **阻碍 inline** · 分支预测失败 → **流水线清空** |
| **深 if-else** | misprediction 代价 |

**替代：** **CRTP / 模板** — [chapter-08 §2 消除运行时决策](../chapter-08-超低延迟核心引擎开发/8.2-消除运行时决策.md) · [chapter-04 §3 静态链接](../chapter-04-硬件选型与服务器配置/4.3-编译器与链接.md)

## 禁止热点动态分配

| `malloc`/`new` 问题 | |
|---------------------|---|
| 堆 **遍历找块** | 延迟不确定 |
| **Heap fragmentation** | 长期运行 **破坏 locality** |

**终极方案：**

| 策略 | 说明 |
|------|------|
| **栈缓冲** | 小对象、生命周期短 |
| **Memory Pool** | 启动时 **一大块连续内存** · 自定义 **free-list / index stack** |
| **对象复用** | Order/Event **placement new** 回收到池 |

```cpp
// 示意：启动时
pool = mmap_huge(N * sizeof(MarketEvent));
free_stack = {0..N-1};

MarketEvent* acquire() {
    int i = free_stack.pop();
    return &pool[i];
}
```

→ [chapter-05 Huge Pages](../chapter-05-操作系统内核极致调优/5.5-HugePages与TLB.md)

---

← 下一节：[7.4 共享内存 IPC](./7.4-共享内存IPC.md) · 返回：[本章索引](./README.md)
