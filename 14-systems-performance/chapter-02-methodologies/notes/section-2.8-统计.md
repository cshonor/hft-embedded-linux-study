## 2.8 统计

> **铁律：** 盯 **P99/P999**、**σ**、**异常值** — **别被 mean 骗了**。
> ← [2.7.2 容量 SLO](./section-2.7.2-容量规划三步法.md) · → [2.9 监控](./section-2.9-监控.md) · [Ch1.6](../../chapter-01-intro/notes/section-1.6-延迟指标与读法.md)

Gregg 2.8：延迟 **很少正态** — mean 做 SLA = **自我安慰**。

---

### 2.8.1 量化性能收益

**观测法（不干预）：**

```
观测：应用请求 10 ms，其中磁盘 I/O 9 ms
建议：内存缓存，DRAM ~10 μs
收益：10 ms → 1.01 ms = ~9x
```

**实验法（改了再量）：**

```
改前：事务延迟均值 10 ms
改后：事务延迟均值 2 ms
收益：5x
```

延迟是最佳量化指标 — **可直接跨组件比较**（IOPS 不行）。注意只测 **同步路径** 上的延迟；异步后台 I/O（如刷盘）不算。

---

### 2.8.2 平均值

| 类型 | 公式 | 适用场景 |
|------|------|----------|
| **算术平均** Mean | Σx / n | 最常用，但隐藏分布 |
| **几何平均** | ⁿ√(x₁×…×xₙ) | 多层协议栈逐层改进的 **乘积效应** |
| **调和平均** | n / Σ(1/xᵢ) | **速率平均**：50 MB/s 跑 100MB + 10 MB/s 跑 700MB → 11.1 MB/s |

**时间平均的陷阱：** CPU 从不「在 50%」— 是某区间内利用了 50%。5 分钟均值可能掩盖 **秒级 100% 尖刺**（Gregg 亲历案例：客户 CPU 饱和导致调度延迟，监控显示 ≤80% 因 5 分钟均值）。

**衰减平均 Decayed Average：** `uptime` 的 load average 就是衰减平均 — 近期权重更高，抑制短期波动。详见 [Ch6.6.1](../../chapter-06-cpus/)。

---

### 2.8.3 标准差 · 分位数 · 中位数

| 指标 | 含义 | HFT 用途 |
|------|------|----------|
| **σ 标准差** | 偏离均值的程度 | 抖动量化；σ 大 = 不稳定 |
| **P99/P999** | 99%/99.9% 请求的上限 | **SLA 核心** |
| **中位数 P50** | 数据中间值 | 看 **主体** 在哪；不受 outlier 影响 |

---

### 2.8.4 变异系数 CoV

**CoV = σ / mean** — 用单值表达离散程度。

σ=50 单独无意义；σ=50 + mean=200 → CoV=25%，说明有中等波动。

**z 值：** 某值偏离均值多少个 σ。

---

### 2.8.5 多峰分布

> *有人淹死在平均深度六英寸的河里。* — W. I. E. Gates

**系统性能常双峰：** cache hit 快峰 + cache miss 慢峰；GC 前快峰 + GC 时慢峰。

| 场景 | 快峰 | 慢峰 |
|------|------|------|
| Cache | L1 hit ~ns | miss → μs |
| **GC 语言** | ~几十 μs | **GC → ms** |
| 快慢路径 | 5 μs | 慢查 500 μs |

**做法：** histogram 看几峰；热力图看周期；热路径 **C++/Rust + 对象池** 消第二峰。

**mean 在双峰分布上 seriously misleading** — 可能恰好在两峰之间的谷底，不代表任何真实状态。

---

### 2.8.6 异常值

磁盘 I/O 多数 0-10 ms，偶发 >1000 ms — 这种 outlier **mean 几乎不动**，但对 HFT 致命。

- **mean** 被 outlier 微移；**median** 几乎不动
- **σ 和 P99** 有机会发现 outlier，但取决于频率
- **唯一可靠方法：** 看完整分布（histogram）+ 最大值告警

**HFT：** P999 不是「噪声」— 是 **P&L**。不删 outlier；**P999/Max 告警** + [2.5 trace](./section-2.5-性能分析方法论.md)。

---

### 实盘：tail 不是小数点 — 交易所硬规则 + 抢单经济学

**国内期货**等：**会员网关 → 撮合** 有 **超时丢弃** 硬限（常见 **~500 ms** 量级）。**专线** 固定占 **几十 μs～1 ms**。

```
总延迟 ≈ 专线(固定) + 引擎(P99/P999 盯这里) + 交易所侧
```

| 段 | 量级 | 说明 |
|----|------|------|
| 专线 | ~50 μs–1 ms | 减不掉 |
| 引擎 mean | 50 μs | 「看起来好」 |
| 引擎 **P999** | **2 ms** | mean **看不见** 的 1% |
| 交易所硬限 | ~500 ms | 超限 → **丢弃** |

1. **硬上限** — tail 叠加（雪崩、GC、重传）吃 **安全余量**
2. **抢单（μs～ms）** — P999 2 ms + 专线 1 ms → **错过最优档**，滑点吃光利润

| 量级感 | 含义 |
|--------|------|
| 每 **1 ms** | 单日收益 **可掉 10%+** |
| **1% 长尾** | **盈利 → 亏损** |
| mean 50 μs + P99 2 ms | **1% 坏单拖垮全天 P&L** |

---

### 统计指标优先级

| 优先级 | 指标 | 用途 |
|--------|------|------|
| **P0** | P99、P999、Max | SLA、告警 |
| **P1** | σ、IQR | 抖动；双峰时配合 histogram |
| **P2** | Mean | 仅 [2.6 排队论](./section-2.6.1-排队论概览与Kendall记号.md) 粗估 |
| **P3** | Outlier 列表 | 单笔 trace |

---

### 检查单

- [ ] SLA 用 **P99/P999**，不用 mean 签收
- [ ] **P99 与 mean 同屏** 告警
- [ ] histogram：**是否双峰**
- [ ] 不删 outlier；P999 可追溯


### 常见陷阱

1. 只报 mean 不报分位数——HFT 的 P99 可能是 mean 的 10 倍，mean 正常不代表没问题
2. σ 大但不查原因——标准差大说明有抖动，可能是调度/锁/cache 问题，不是「正常波动」
3. 采样率不够——每秒采一个点可能完全错过微秒级尖刺，HFT 需要高频采样或事件驱动

<details>
<summary>自测题（点击展开）</summary>

1. 几何平均和调和平均分别适合什么场景？
   <details><summary>答</summary>几何平均适合多层协议栈改进的乘积效应；调和平均适合速率平均（如不同速度跑不同距离）</details>
2. HFT 为什么不能用 mean 代替 P99？
   <details><summary>答</summary>一次 P99 尖刺就可能导致错单，mean 被大量正常样本稀释，看不出问题</details>
3. 多峰分布下 mean 为什么 misleading？
   <details><summary>答</summary>mean 可能落在两峰之间的谷底，不代表任何真实状态——需要 histogram 看完整分布</details>

</details>


---

← [2.7.2](./section-2.7.2-容量规划三步法.md) · [2.9 监控](./section-2.9-监控.md) · [本章导读](../README.md)
