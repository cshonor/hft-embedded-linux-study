# 第 4 章 · 上下文相关分析 · §4 语法制导翻译（工程实践）

← [本章目录](./README.md) · 上一节：[03-attribute-grammars.md](./03-attribute-grammars.md) · 下一节：---

鉴于严格**属性文法**的性能与工程瓶颈，工业编译器普遍采用 **Ad Hoc Syntax-Directed Translation（特定语法制导翻译）** — 在语法分析过程中**嵌入代码**传递语义信息。

---

## 动作嵌入（Actions）

| 做法 | 说明 |
|------|------|
| **时机** | 识别**句柄**、**归约**或构造子树时 |
| **内容** | 执行 C/代码片段 — 建符号表、查类型、生成 IR 片段 |
| **优势** | 无需保留整棵分析树；按需计算 |

**自顶向下**：在递归下降函数里直接写语义逻辑（**CI jlox**）。  
**自底向上**：在 YACC/Bison 产生式旁写 `{ … }` 动作。

---

## YACC / Bison 栈上值传递

```yacc
expr : expr '+' term  { $$ = makeAdd($1, $3); }
     | term           { $$ = $1; }
     ;
```

| 伪变量 | 含义 |
|--------|------|
| **`$1`, `$2`, …** | 产生式右部各符号的**语义值**（栈上） |
| **`$$`** | 左部非终结符的**结果值**（归约后推回栈顶） |

极高效率 — 与 LR **移入-归约栈**一体。

→ [ch3 §4 LR 表](../chapter03_parsers/04-lr-automation.md)

---

## 最常见应用：构建 AST

| 步骤 | 说明 |
|------|------|
| **丢弃语法糖节点** | 去掉纯为 parsing 存在的层级 |
| **保留语义节点** | 表达式、语句、声明 |
| **紧凑结构** | 供 ch5 **IR lowering** 与 ch8+ **优化** |

→ [CI jlox ch5 AST](../../../01_Crafting-Interpreters/part02_jlox/chapter05_representing-code/README.md)

---

## 其他 SDT 用途

| 用途 | 例子 |
|------|------|
| **变量声明** | 登记符号表、分配存储类 |
| **作用域** | 进入/退出块时 push/pop 环境 |
| **类型检查** | 归约 `expr + expr` 时检查 `$1.type` 与 `$3.type` |

**jlox Resolver**（ch11）≈ 单独一遍 SDT，在解释前解析绑定 — 与「parser 内嵌 action」分工不同，目的一致。

---

## 本章收束

| 层 | 内容 |
|----|------|
| **问题** | CFG 无法表达的上下文规则 |
| **理论** | 类型系统 + 属性文法 |
| **工程** | SDT 嵌入 Action · YACC `$`/`$$` · AST + 符号表 |

> 编译器开始**真正理解**代码含义 — 为 **IR** 与优化扫清**语义障碍**。

---

## Part I 前端完结 → Part II

```text
ch2 Token → ch3 结构 → ch4 语义(AST/类型) → ch5 IR …
```

→ [本书目录 ch5+](../../本书目录.md)
