## 5.1 优化编译器的能力和局限性

> **Ch5 §5.1** · [章导读](../README.md) · 上节 — · 下节 [§5.2 →](./section-5.2-表示程序性能.md)

---

**编译器常能做：**

- 常量折叠、死代码消除、内联、强度削减（`*8` → `<<3`）
- 寄存器分配、循环不变量外提、公共子表达式消除
- 自动向量化、循环展开（`-O3` / `-funroll-loops`）

**编译器不敢/不能做（除非你帮它）：**

| 障碍 | 例子 |
|------|------|
| **指针别名** | `*x` 与 `*y` 是否同一地址未知 → 不敢乱重排 store |
| **全局可见副作用** | 函数可能改内存 → 不敢删「看似多余」调用 |
| **跨编译单元** | 无 LTO 时不知道 `inline` 候选 |
| **违反严格别名** | `int*` 与 `float*` 互转 — UB |

**帮编译器：**

```c
restrict  // C99：承诺指针不重叠
__restrict__
#pragma GCC ivdep   // 谨慎：声称无依赖
static inline + 同文件
-fno-strict-aliasing  // 妥协，非首选
```

**HFT：** 热路径 `.cpp` **单文件或 LTO**；`restrict` 用于明确 buffer；避免热循环里调用 **未知副作用** 的函数。  
Release 优化等级：`-O0`/`-Og` 学调，**`-O3` 热路径上线**（勿与 `-Ofast` 混用不审浮点）— 等级表 → [Ch3 §3.2.1](../../chapter-03-machine-level-programs/notes/section-3.2.1-机器级代码与编译链路.md)。

---

### 口述巩固 · 自测

1. （待口述补）本节核心一句话？

---

← — · [本章导读](../README.md) · [§5.2 →](./section-5.2-表示程序性能.md)
