# 30.2 客户程序设计范式

> [30.3 测试客户](30.3_TCP_Test_Client.md) · [Ch 5 客户](../../1_BasicFoundation/Chapter05_TCP_Client_Server_Demo/study.md)

---

## 核心主旨

为公平对比各服务器架构，需统一的**高并发测试客户**设计范式。

---

## 典型模式

| 要素 | 说明 |
|------|------|
| 连接数 | 同时发起**数千** TCP 连接 |
| 协议 | 发固定长度请求 → 收等量回射 → 关闭 |
| 指标 | 首连建立到末连断开的**总耗时** → 吞吐 |

与 Ch 5 回射客户类似，但强调**并发连接规模**与可重复压测。

---

## 个人学习总结

TCP 客户端设计模式：基本模式=socket→connect→while(read stdin→send→recv→write stdout)→close。变种：select 同时监听 stdin 和 socket、非阻塞 connect 实现超时控制。

**一句话**：TCP 客户端=connect 后读写循环，进阶=select 多路复用+非阻塞 connect 超时。

> 易考点：客户端 recv 需处理 EOF（对端关闭）和 EINTR（信号中断）两种特殊情况。
