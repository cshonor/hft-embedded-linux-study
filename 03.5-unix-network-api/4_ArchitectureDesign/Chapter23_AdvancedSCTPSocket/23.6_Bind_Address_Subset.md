# 23.6 捆绑地址子集

> [Ch 9.3 sctp_bindx](../Chapter09_BasicSCTPSocket/9.3_Sctp_Bindx_Func.md) · [Ch 2.5 SCTP 多宿](../../1_BasicFoundation/Chapter02_TCP_UDP_SCTP/2.5_SCTP_Protocol.md)

---

## 问题

`bind` **通配**（如 `INADDR_ANY` / `in6addr_any`）时，SCTP 可能把主机**所有活动网卡 IP** 都作为多宿地址通告给对端。

---

## 需求

安全或路由策略：某些 IP（如内网 DB 专用）**不应**暴露给公网关联。

---

## 解决方案

**`sctp_bindx`** 显式传入**允许公开的 IP 数组** — 控制多宿拓扑的**可见子集**。

---

## 个人学习总结

地址子集绑定：`sctp_bindx` 可在关联建立后动态增删本地地址（SCTP_BINDX_ADD_ADDR/REM_ADDR），内核通过 ASCONF 消息通知对端。实现运行时多宿地址变更。

**一句话**：bindx ADD/REM=运行时动态加减本地地址，ASCONF 消息自动同步给对端。

> 易考点：动态增地址需对端支持 ASCONF 扩展（SCTP 协商时确认），否则 ADD 被忽略。
