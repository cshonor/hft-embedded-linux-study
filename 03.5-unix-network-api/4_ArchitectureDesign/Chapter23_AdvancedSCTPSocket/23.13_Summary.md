# 23.13 小结

> [study.md](study.md)

---

## 章节核心提炼

### 1. 微操与资源

**`SCTP_AUTOCLOSE`** + **`sctp_peeloff`** → 一到多服务器降僵死关联、长事务剥离，减内存泄漏风险。

### 2. 健壮性的代价

须处理 **Notifications**、**部分递送（MSG_EOR）** 状态机、多宿路径事件。

### 3. 面向未来

SCTP 在电信等领域仍重要；公网受 NAT 限制；思想影响 **QUIC**。

### 4. Ch 9 → 10 → 23 链

```text
基础 API → 回射与队头阻塞实验 → 生产级选项与架构
```

---

## 个人学习总结

高级 SCTP 总结：autoclose（空闲清理）、部分交付（大消息分批）、通知（事件驱动）、无序数据（实时优先）、地址子集绑定（动态多宿）、heartbeat（路径检测）、peeloff（关联拆分）、定时器调优。全方位控制关联行为。

**一句话**：高级 SCTP=把 TCP 做不到的事情做精细——多流调度、路径冗余、事件通知、消息级控制。

> HFT 关联：SCTP 的高级特性几乎为低延迟交易量身定做，但部署门槛（NAT/防火墙）限制了实际采用。
