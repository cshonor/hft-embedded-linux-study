# 26.9 Web 客户与同时连接（续）

> [26.6](../26.6_Web_Client_Multi_Connect.md) · [26.7](../26.7_Mutex_Lock.md) · [26.8](../26.8_Condition_Variable.md)

---

## 融合架构

**Mutex + 条件变量** 完善并发 Web 客户端（生产者-消费者式槽位控制）。

### 主线程

```text
pthread_mutex_lock
while (active >= MAX)
    pthread_cond_wait(&cond, &mutex)
active++
pthread_create(...)   /* 新下载线程 */
pthread_mutex_unlock
```

### 子线程（下载结束）

```text
pthread_mutex_lock
active--
pthread_cond_signal(&cond)   /* 唤醒可能等待的主线程 */
pthread_mutex_unlock
pthread_exit(NULL)
```

---

## 重点结论

主线程在槽位满时 **零 CPU 空转** 等待；子线程释放槽位即 **signal** — 经典并发 Web 客户模式。

---

## 个人学习总结

（待填）
