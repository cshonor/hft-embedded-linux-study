# 26.8 条件变量

> [26.7 Mutex](26.7_Mutex_Lock.md) · [26.9](26.9_Web_Client_Connect_Supplement.md)

---

## 核心主旨

活跃连接已满时，主线程应**休眠等待**槽位，而非 Mutex 自旋轮询。

**条件变量（`pthread_cond_t`）必须与 Mutex 配合。**

---

## pthread_cond_wait

```c
int pthread_cond_wait(pthread_cond_t *cond, pthread_mutex_t *mutex);
```

**原子逻辑**（调用前已持有 `mutex`）：

1. 线程睡眠  
2. **自动解锁** `mutex`  
3. 被唤醒后返回前 **自动重新加锁** `mutex`

---

## 虚假唤醒：必须用 while

```c
pthread_mutex_lock(&mutex);
while (active_threads >= MAX_THREADS) {
    pthread_cond_wait(&cond, &mutex);
}
active_threads++;
pthread_mutex_unlock(&mutex);
```

**禁止用 `if`** 只等一次。

---

## 唤醒

| API | 作用 |
|-----|------|
| **`pthread_cond_signal`** | 唤醒**一个**等待线程 |
| **`pthread_cond_broadcast`** | 唤醒**全部**等待线程 |

---

## 个人学习总结

`pthread_cond_t` — 条件变量：配合 mutex 实现"等待条件成立"。`pthread_cond_wait(&cond, &mutex)` 原子地解锁+睡眠，被唤醒后自动加锁。

**铁律**：cond_wait 必须在 while 循环中——防止**虚假唤醒**（spurious wakeup）。

**一句话**：mutex 保护数据，cond 通知"数据变了"——wait 时解锁让生产者能修改。

> 经典模式：生产者 lock→修改→signal→unlock；消费者 lock→while(!ok) wait→处理→unlock。
