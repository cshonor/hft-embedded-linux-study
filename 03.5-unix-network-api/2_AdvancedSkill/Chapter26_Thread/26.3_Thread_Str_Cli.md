# 26.3 使用线程的 str_cli 函数

> [Ch 6 str_cli](../../1_BasicFoundation/Chapter06_IO_Select_Poll/6.4_Str_Cli_Revised.md) · [Ch 6.6 shutdown](../../1_BasicFoundation/Chapter06_IO_Select_Poll/6.6_Shutdown_Function.md)

---

## 架构设计

用**多线程**替代 **`select`** 改造回射客户端。

| 线程 | 职责 |
|------|------|
| **主线程** | stdin → **`write`** 到套接字 |
| **子线程** | 套接字 **`read`** → stdout |

---

## 终止边界（核心难点）

```text
主线程：stdin EOF → shutdown(sockfd, SHUT_WR) 发 FIN → return 结束
         （尚有子线程时进程不退出，main 可 pthread_join）

子线程：read 得 EOF（对端 FIN）→ 应结束
         若子线程调用 exit(0) → 整个进程退出
```

须明确 **谁 join 谁、谁 detach、何时 shutdown**，避免半关闭与进程过早 `exit`。

---

## 个人学习总结

（待填）
