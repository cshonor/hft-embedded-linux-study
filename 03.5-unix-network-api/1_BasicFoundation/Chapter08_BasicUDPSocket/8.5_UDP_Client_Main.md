# 8.5 UDP 回射客户程序：main 函数

> [8.6 dg_cli](../8.6_UDP_Client_Dg_Cli.md)

---

## 核心主旨

- 校验命令行：**服务器 IP**  
- **`inet_pton`** → 填入 `sockaddr_in`（网络字节序）  
- **`socket`** → 将 `sockfd` 与地址交给 **`dg_cli`**

与 TCP 客户 main 类似，但**无 `Connect`**（初版）。

---

## 个人学习总结

（待填）
