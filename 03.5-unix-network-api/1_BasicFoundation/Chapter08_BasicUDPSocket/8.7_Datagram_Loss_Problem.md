# 8.7 数据报的丢失

> [8.12 connect 修订](8.12_Dg_Cli_Revised.md) · Ch 6 超时：[6.5](../Chapter06_IO_Select_Poll/6.5_Batch_Input_Process.md) · SO_RCVTIMEO：Ch 7.5

---

## 核心主旨

初版 UDP 回射在广域网上**极度脆弱**：UDP **无可靠性**。

---

## 通信陷阱

| 情况 | 客户行为 |
|------|----------|
| 请求丢包 | **`recvfrom` 永久阻塞** |
| 应答丢包 | 同上 |
| 无 TCP 定时器 | **无从得知**是否丢失 |

**单边盲区** — 不像 TCP 有重传。

---

## 重点结论

**绝不能无限期阻塞在 `recvfrom` 上。**

须加应用层超时：

| 手段 | 章节 |
|------|------|
| **`alarm` + 信号** | 经典 |
| **`select`/`poll` 超时** | Ch 6 |
| **`SO_RCVTIMEO`** | Ch 7.5 |

高可靠 UDP 还须自建 **ACK + 重传** 状态机（TFTP 等）。

---

## 个人学习总结

UDP 不可靠——数据报可能丢失（网络拥塞/接收缓冲满）。dg_cli 的 recvfrom 无超时 → 丢包则永久阻塞。

**解法**：alarm 超时、select 超时、SO_RCVTIMEO。

**一句话**：UDP 应用层必须自己实现超时+重传——TCP 帮你做的 UDP 都没有。
