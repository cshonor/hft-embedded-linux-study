# 5.10 wait 和 waitpid 函数

> [5.9 SIGCHLD](../5.9_SIGCHLD_Process.md)

---

## 核心主旨与关键论据

父进程必须通过 **`wait` / `waitpid`** 取走子进程**终止状态**，子进程才从僵尸变为完全释放。并发网关的 SIGCHLD 处理函数里，**只调用一次 wait 是致命错误** —— 必须用 **`while` + `waitpid(-1, ..., WNOHANG)`** 清空所有已退出子进程。

---

## 核心知识点

### `wait`

```c
pid_t wait(int *statloc);
```

- **阻塞**直到**任意一个**子进程终止  
- 返回终止子进程的 **PID**，状态写入 `statloc`  
- 无子进程时行为依实现（错误）

### `waitpid`

```c
pid_t waitpid(pid_t pid, int *statloc, int options);
```

| 能力 | 说明 |
|------|------|
| **指定 pid** | 等待特定子进程 |
| **`WNOHANG`** | 若无已终止子进程，**立即返回 0**，不阻塞 |
| **组合** | SIGCHLD handler 内循环非阻塞收割 |

### 标准 SIGCHLD 处理模板（铁律）

```c
void sig_chld(int signo)
{
    pid_t pid;
    int   stat;

    while ( (pid = waitpid(-1, &stat, WNOHANG)) > 0)
        printf("child %d terminated\n", pid);
    return;
}
```

> 生产环境 handler 内 **printf 仍不推荐**（非异步信号安全）；学习版可保留，考试记 **while+WNOHANG** 逻辑。

---

## 逻辑脉络：信号不排队陷阱

**场景**：5 个客户几乎同时断开 → 5 个子 exit → 可能产生 **5 次 SIGCHLD**，但 Unix **不对 SIGCHLD 排队**。

| 错误实现 | 后果 |
|----------|------|
| handler 里只 **`wait()` 一次** | 只回收 1 个子，**其余 4 个僵尸** |
| 父从不注册 SIGCHLD | **每个**子进程都僵尸 |

| 正确实现 | 行为 |
|----------|------|
| **`while (waitpid(-1, &stat, WNOHANG) > 0)`** | 一次信号处理收割**所有**已就绪子进程，直到返回 0 |

---

## 易错细节与重点结论

**考试默写**：`while (waitpid(-1, &stat, WNOHANG) > 0);`

| 易混 | 纠正 |
|------|------|
| wait 与 waitpid 在 handler 里阻塞 | handler 应用 **WNOHANG**，避免长时间占着信号 |
| 父进程 fork 前 wait | 应在 **SIGCHLD 处理函数**或专门收割逻辑里 wait |
| 子进程 double fork 成孤儿 | 进阶技巧（daemon），本书 Ch 13 |

---

> 💡 **后续拓展留白**  
> - `WIFEXITED` / `WIFSIGNALED` 解析 stat  
> - 高并发 prefork 父进程批量 wait  

---

## 个人学习总结

（待填）
