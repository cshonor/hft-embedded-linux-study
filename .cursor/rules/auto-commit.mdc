---
description: 文档与笔记修改后自动 git commit（hft 仓库）
alwaysApply: true
---

# 自动提交规则

在本仓库中完成 **文档 / 笔记 / README / OUTLINE** 等改动后，**必须**在同一轮对话内自动 `git commit`，**无需用户再次说「提交」**。

代码改动默认同样提交；若改动面大或含未跑测试的逻辑，可先说明再 commit。

> 用户确认：**「以后文档都自动 commit」** — 本规则优先于「等用户开口再 commit」的泛化习惯。

## 提交流程

每次修改完成后，按顺序执行：

1. `git status` — 确认变更范围
2. `git diff` — 理解改动内容（含 staged / unstaged）
3. `git add` — 仅添加与本次任务相关的文件；不要提交 `.env`、密钥等敏感文件
4. `git commit` — 用 HEREDOC 写 1–2 句 commit message，说明 **why** 而非罗列文件名
5. `git status` — 确认提交成功、工作区干净

## 约束

- **只 commit，不 push** — 除非用户明确要求 push
- **不要** 修改 git config
- **不要** 使用 `--no-verify`、 amend、force push 等破坏性操作
- 若 pre-commit hook 失败：修复问题后**新建 commit**，不要 amend
- 若无实际变更（nothing to commit），跳过提交
- commit message 使用英文，与仓库现有风格一致

## Commit message 示例

```
Add 7 English book note folders with HFT chapter scaffolds

Scaffold per-book directories aligned with READING-LIST curated chapters.
```

```
Update README with English book index table

Link each of the 7 HFT reference books to their note folders.
```
