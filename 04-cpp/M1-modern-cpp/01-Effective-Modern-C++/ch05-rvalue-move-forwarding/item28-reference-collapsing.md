# Item 28：理解引用折叠

> 第 5 章 · Item 28 · 上一节：[Item 27 替代方案](item27-alternatives.md)

## 这节讲什么

引用折叠是万能引用与完美转发的底层机制。四条规则的核心：**只要有左值引用参与，折叠结果是左值引用**。

---

## 四条规则

| 组合 | 结果 |
|------|------|
| `&` + `&` | `&` |
| `&` + `&&` | `&` |
| `&&` + `&` | `&` |
| `&&` + `&&` | `&&` |

**口诀：左值引用是"毒"——碰一下就变左值引用。只有两个右值引用才折叠为右值引用。**

折叠发生在四个语境：
1. 模板推导
2. `auto` 推导
3. `typedef` / `using`
4. `decltype`

---

## 为什么万能引用能绑左值

```cpp
template<class T> void f(T&& x);

Widget w;
f(w);    // 左值实参：T 推为 Widget&，T&& = Widget& && → 折叠为 Widget&
f(Widget{});  // 右值实参：T 推为 Widget，T&& = Widget&&
```

左值实参时 `T` 推为 `Widget&`，`T&&` = `Widget& &&`，折叠为 `Widget&`——这就是万能引用能绑左值的原理。

---

## 新手要点（和 C 的区别）

- **C 没有引用**，更没有引用折叠。这是 C++ 类型系统独有的机制。
- **不需要背规则**：记住一句——"左值引用碰一下就传染"。两个 `&&` 才能保持 `&&`。

---

## HFT 关联

- **理解万能引用**：读 muduo/HFT 框架的 `template<class T> void setCallback(T&& f)` 时，理解引用折叠才能正确推断 `T` 的推导结果。

---

## 自测题

1. 引用折叠的四条规则是什么？
2. "有左值参与就折叠为左值"如何解释万能引用接左值时 `T` 变 `T&`？
3. 引用折叠发生在哪四个语境？
4. `T&&` + `T&` 折叠成什么？

<details>
<summary>参考答案</summary>

1. 四条规则：`& &` → `&`；`& &&` → `&`；`&& &` → `&`；`&& &&` → `&&`。口诀是"左值引用会传染"——只要组合里出现一个 `&`，结果就是左值引用；只有两个都是 `&&` 时才保持右值引用。（注意 C++ 不允许直接写"引用的引用"，折叠只在模板/别名/`decltype` 等间接产生引用的引用的语境中发生。）

2. 万能引用 `T&&` 接左值时，模板推导的特殊规则把 `T` 推成实参类型的**左值引用**（`Widget&`），于是形参类型变成 `T&&` = `Widget& &&`；按折叠规则，一旦有 `&` 参与结果就是 `&`，所以形参最终是 `Widget&`（左值引用），可以绑定左值实参。接右值时 `T` 推为 `Widget`（不带引用），`T&&` = `Widget&&`，无需折叠、保持右值引用。二者结合起来就解释了"同一个 `T&&` 形参既能接左值也能接右值"。

3. 四个语境：**模板实例化/类型推导**（`T&&` 中 `T` 被推成引用类型时）、**`auto` 推导**（`auto&& x = expr;` 中 `auto` 推成 `T&` 时的折叠）、**`typedef` / `using` 别名**（别名展开后产生的引用的引用）、**`decltype`**（`decltype` 产出引用类型再与 `&`/`&&` 组合时）。这四个也是 C++ 中唯一允许"引用的引用"出现的地方。

4. 折叠成 `T&`（左值引用）。按规则 `&& &` → `&`（以及 `& &&` → `&`），只要有一个 `&` 参与，结果就是左值引用；顺序不影响结果。只有 `T&& &&`（两个都是右值引用）才折叠为 `T&&`。

</details>

---

## 参考与延伸

- 下一节：[Item 29 移动不存在/廉价](item29-move-not-exist.md)
- 回到：[第 5 章](README.md)
