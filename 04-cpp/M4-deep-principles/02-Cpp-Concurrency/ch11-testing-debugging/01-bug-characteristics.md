# 11.1 并发 bug 的特征

> 第 11 章 多线程应用的测试和除错 · 上一章：[10.5 与传统手写并行的对比](../ch10-parallel-algorithms/05-vs-manual.md) · 下一节：[11.2 数据竞争的检测](02-data-race.md)

## 这节讲什么

并发 bug 是最难抓的——不可复现、时序依赖、Heisenbug（加 print 就消失）。本节讲并发 bug 的四大特征、为什么它们比串行 bug 难调试、以及应对策略的总体思路。

---

## 核心规则（代码+表格）

### 并发 bug 的四大特征

| 特征 | 说明 | 例子 |
|------|------|------|
| **不可复现** | 跑 1000 次才出现一次 | 压力测试 8 小时才崩溃一次 |
| **时序依赖** | 只在特定线程交错下触发 | 线程 A 在 B 的两步之间插入操作 |
| **环境敏感** | 换机器/编译器/负载就消失 | 开发机不复现，生产机频发 |
| **Heisenbug** | 加 print 就不复现 | 加日志改变了时序，bug 消失 |

### 为什么并发 bug 难调试

```cpp
// 串行 bug：确定性
int sum = 0;
for (int i = 0; i < 100; ++i) sum += i;
// 如果 sum 算错，每次都错，加 print 不影响结果

// 并发 bug：非确定性
std::atomic<int> sum{0};
std::vector<std::thread> threads;
for (int t = 0; t < 4; ++t)
    threads.emplace_back([&]{
        for (int i = 0; i < 100; ++i)
            sum.fetch_add(1, std::memory_order_relaxed);
    });
// sum 最终值可能不是 400（如果用非原子 int 就是数据竞争）
// 但跑 1000 次可能只有 1 次不是 400
```

### Heisenbug 原理

```cpp
// 有 bug 的代码
int x = 0;
std::thread t1([&]{ x = 1; /* print("t1 done"); */ });
std::thread t2([&]{ std::cout << x; });
t1.join(); t2.join();
// 不加 print：t2 可能读到 0（t1 还没写）
// 加 print("t1 done")：print 耗时 → t1 先完成 → t2 总是读到 1 → bug 消失
```

`print`/`log` 有 I/O 开销（微秒级），改变了线程交错时序——这就是 Heisenbug。并发调试中，**加 print 是最后手段**。

### 应对策略总览

| 策略 | 说明 | 工具 |
|------|------|------|
| 静态分析 | 编译期检查潜在竞争 | Clang Static Analyzer, Coverity |
| 动态检测 | 运行时检测竞争 | TSan, Helgrind |
| 压力测试 | 高并发压测暴露问题 | 自定义压测脚本 |
| 形式化验证 | 数学证明无竞争 | 模型检查（极少用） |
| 代码审查 | 人工审查并发模式 | Code Review |

---

## 新手要点（和 C 的区别）

- **C 程序员对并发 bug 可能不熟悉**：C 程序员如果主要写单线程代码，没遇到过 Heisenbug。转型多线程 C++ 时，要建立"并发 bug 是非确定性的"这个认知。
- **Heisenbug 是并发编程的噩梦**：C 程序员习惯"加 printf 调试"——在并发中这可能让 bug 消失。并发调试要用专用工具（TSan），而非传统的 print 大法。
- **"我的机器上不复现"不等于没 bug**：C 程序员可能觉得"我测了没问题"——但并发 bug 可能只在特定核数、负载、调度下触发。开发机 4 核不复现，不等于生产机 16 核不复现。
- **静态分析更重要**：C 程序员可能不重视静态分析——但在并发中，编译期检查能发现很多潜在竞争。Clang 的 Thread Safety Analysis（注解式）能在编译期发现锁序问题。

---

## HFT 关联

- **HFT 系统的并发 bug 代价极高**：一条数据竞争可能导致错误下单——金融损失可能是百万级。HFT 对并发正确性的要求是"零容忍"。
- **HFT 的 Heisenbug 更隐蔽**：HFT 系统在高负载下才触发竞争——开发环境低负载时一切正常。必须在压力测试环境（模拟生产负载）下测试。
- **TSan 是 HFT 的必备工具**：HFT 系统开发阶段必须用 TSan 跑测试——10-20 倍减速但能抓到肉眼找不到的竞争。生产环境不用 TSan（性能），但 CI 流水线要用。
- **"加日志"在 HFT 中也改变时序**：HFT 系统加日志可能把纳秒级竞争"修复"——因为日志耗时改变了线程交错。HFT 调试要用硬件性能计数器（如 perf）而非 print。

---

## 自测题

1. 并发 bug 的四大特征是什么？
2. 什么是 Heisenbug？为什么加 print 会让 bug 消失？
3. 为什么"我的机器上不复现"不等于没并发 bug？
4. 调试并发 bug 应该用什么工具，而不是 print？
5. HFT 系统为什么对并发正确性"零容忍"？

---

## 参考与延伸

- 下一节：[11.2 数据竞争的检测](02-data-race.md)
- 上一章：[10.5 与传统手写并行的对比](../ch10-parallel-algorithms/05-vs-manual.md)
- 回到：[第 11 章](README.md)
