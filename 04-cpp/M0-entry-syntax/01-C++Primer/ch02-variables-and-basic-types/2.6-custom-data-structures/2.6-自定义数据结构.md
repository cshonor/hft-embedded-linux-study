# 2.6 自定义数据结构

## 本节讲什么

作为类的初步体验，以《C++ Primer》第五版经典示例 **`Sales_data`** 为主线：三成员 `struct`、C++11 **类内初始值**、教材完整版（构造 / 成员函数 / 友元）、**`.h + .cpp` 分离编译**与**头文件保护**。

## 要点

- **`Sales_data`**：`bookNo` + `units_sold` + `revenue`；末尾 **`;`** 不能漏
- **类内初始值**（C++11）：只用 `=` 或 `{}`，**不能用 `()`**；构造列表优先
- **聚合（Aggregate）**：满足条件的「纯数据」类/结构体，可用 `{}` 按成员顺序直接初始化；入门版 `Sales_data` 即典型例子
- **教材完整版**：加构造 / `combine` / `avg_price` / 友元 `add` `read` `print` 后不再是聚合
- **友元前置声明**：函数原型在 struct 前；**类的前置声明**分普通（不完整类型）与**有源**（.cpp 有实现且会链入）
- **`.h` 声明、`.cpp` 实现**；头文件保护 `#ifndef` 或 `#pragma once`

## 子节

- [2.6.1 Sales_data 入门与聚合](./2.6.1-Sales_data入门与聚合.md)
- [2.6.2 类内初始值](./2.6.2-类内初始值.md)
- [2.6.3 Sales_data 完整版](./2.6.3-Sales_data完整版.md)
- [2.6.4 友元与前置声明](./2.6.4-友元与前置声明.md)
- [2.6.5 头文件分离与保护](./2.6.5-头文件分离与保护.md)

---

## 代码自测

**题目 1：** C++ 的 `struct` 和 `class` 有什么区别？
```cpp
struct S { int x; };  // 默认访问？
class C { int x; };   // 默认访问？
```

<details>
<summary>参考答案</summary>

`struct` 默认 public 访问，`class` 默认 private 访问——这是唯一的语法区别。其余完全等价（都可以有构造/析构、继承、虚函数等）。通常 struct 用于纯数据聚合，class 用于有封装行为的类型。C 的 struct 只能包含数据，C++ 的 struct 是 class 的变体。

</details>
