# 14.4 输入输出运算符

## 本节讲什么

`>>` 和 `<<` 是 C++ 里唯一一对「**同一个符号、两套完全不同的语义**」的运算符：

```cpp
int a = 8 >> 1;      // ① 内置右移：位运算，4
std::cin >> x;       // ② 提取运算符：从流里读入数据，塞进 x
```

第①种和 C 一模一样；第②种是**重载出来的函数**。本节讲清楚三件事：

1. 编译器是怎么区分这两种身份的（不是"看符号"，是看**操作数类型的重载决议**）；
2. `cin >> x` 实际调用的到底是成员函数还是自由函数（答案和很多教程说的不一样）；
3. 一次 `>>` 在底层走了几层，以及为什么 HFT 热路径不能用它。

> 前置：[14.1 运算符重载](./14.1-运算符重载.md)（成员 vs 非成员）、[8.1 IO 类体系](../ch08-iostream-library/8.1-IO类体系.md)、[1.2 初识输入输出](../ch01-getting-started/1.2-初识输入输出.md)。

## 一句话区分：看左操作数的类型

**判定规则不是"看符号长什么样"，而是看操作数里有没有类类型。**

对任意 `a >> b`，编译器按 [over.match.oper] 组候选集：

| 情况 | 候选集 | 结果 |
|------|--------|------|
| `a`、`b` 都是内置算术/枚举类型 | 只有**内置候选**（整型提升后的各种 `L operator>>(L, R)` 组合） | 位右移 |
| 至少一个是类类型/枚举 | ① `a.operator>>(b)` 成员候选 ② 非成员 `operator>>(a,b)`（普通查找 + **ADL**） ③ 内置候选 | 走重载决议，选最优可行函数 |

所以：

- `8 >> 1`：`int` 和 `int`，没有类类型 → 只有内置候选 → **位运算**，编译后就是一条 `sar` 指令；
- `cin >> x`：`cin` 是 `std::istream`（类类型）→ 进入重载决议 → 在 `istream` 的成员里找到 `operator>>(int&)`。

> ⚠️ 常见误解：以为"是 IO 还是位运算"看符号在不在 `cin` 旁边。**真正决定的是左操作数的类型**。
> 写 `my_class >> 2`（左操作数是自定义类）照样会去查 `my_class::operator>>`，查不到才报错。

### 词法层面的证据：`>>` 曾经真的会打架

这两层身份的冲突不只是"语义上容易混淆"——C++03 时代它在**词法分析阶段**就是一个真 bug 源：

```cpp
std::vector<std::vector<int> >  v;   // C++03 必须写 "> >"，中间加空格
std::vector<std::vector<int>>   v;   // C++03 编译错误；C++11 起合法
```

原因：词法用**最大匹配**（maximal munch），`>>` 是**一个 token**，不会被自动拆成两个 `>`。
C++11 在 [temp.names] 加了一条特判：**`>>` 用于关闭两个嵌套模板参数列表时，视为两个 `>`**。

→ 这是"`>>` 有两层身份"最硬的证据：如果不是真的冲突过，标准根本不需要打这个补丁。

## `cin >> x` 调的到底是谁？——不是 `operator>>(cin, x)`

很多教程（包括本仓库早期表述）写：`cin >> x` 等价于 `operator>>(cin, x)`。
**对内置类型，这句话是错的。** 精确答案分三类（签名以 cppreference/标准为准）：

| 右操作数类型 | 选中的函数 | 身份 | 声明位置 |
|-------------|-----------|------|---------|
| `short/int/long/long long` + unsigned 版、`float/double/long double`、`bool`、`void*` | `std::istream::operator>>(T&)` | **成员函数** | `<istream>` |
| `char` / `signed char` / `unsigned char` | `operator>>(istream&, CharT&)` | **非成员自由函数** | `<istream>` |
| `char*`（C++20 前）/ `char (&)[N]`（C++20 起） | 非成员 | **非成员自由函数** | `<istream>` |
| `std::string` | `operator>>(istream&, string&)` | **非成员自由函数** | `<string>` |
| 自定义类型 `T` | 你自己写的 `operator>>(istream&, T&)` | **非成员自由函数**（必须） | 你的头文件 |
| 操纵符，如 `std::ws` | `operator>>(istream& (*)(istream&))` | 成员函数（第 17 号重载） | `<istream>` |
| `std::streambuf*` | `operator>>(basic_streambuf*)` | 成员函数（第 18 号） | `<istream>` |

**为什么会分成两半？** 一条设计约束：

> 左操作数是 `std::istream`（你已经关上源码的标准类）→ 只能用**成员**；
> 左操作数是 `istream` 但你没法给它加成员（char/string/自定义类型都是后加的）→ 只能用**非成员**。

所以 `cin >> x`（x 是 `int`）的真身是 `cin.operator>>(x)`，写成自由函数形式反而编不过：

```cpp
int x;
std::cin >> x;                    // ✅ 成员版：std::istream::operator>>(int&)
// operator>>(std::cin, x);       // ❌ 编译错误：标准库根本没有 operator>>(istream&, int&)
```

> 验证方法（取成员函数指针，能取到就证明它是成员）：
> ```cpp
> std::istream& (std::istream::*pmf)(int&) = &std::istream::operator>>;  // ✅ 能编译
> ```
> 反过来，你能给 `std::string` 写 `operator>>(std::cin, s)` 显式调用——因为那个版本**确实**是非成员。
>
> ⚠️ **实测状态**：本仓库当前环境**没有可用的 C++ 编译器**（WSL 被安全策略拦截，本机 clang 无 libc 头文件、
> 无 MSVC），本节示例代码**未实编译**。上面所有签名与行为结论取自标准条款与 cppreference
> （`basic_istream::operator>>`、`operator>>(std::basic_istream)`、`operator<<,>>(std::basic_string)`、
> `basic_ios::operator bool`），不是实测输出。

### 三个容易讲错的细节（cppreference 逐条核对）

1. **第 17 号操纵符重载的返回值不是 `*this`，而是 `func(*this)`**
   （1–16、18 都返回 `*this`，只有 17 直接返回操纵符的返回值；效果上等价，因为操纵符自己就返回流引用）。
2. **`short` / `int` 不是直接抽 `short` / `int`**：内部先当 `long` 抽出来，再做范围检查
   （LWG 118 / LWG 661）。越界时写 `numeric_limits<T>::min()` / `max()` 并置 `failbit`。
3. **`operator>>(istream&, char&)` 有特例**：抽到流的**最后一个**字符时**不置 `eofbit`**
   （与 `scanf("%c")` 行为一致），但抽最后一个 `int` 会置 `eofbit`。

## 为什么返回 `istream&`：链式 + 状态

```cpp
std::cin >> a >> b;        // 等价于 (cin.operator>>(a)).operator>>(b)
if (std::cin >> x) { }     // 流直接当条件用
while (std::cin >> x) { }  // 惯用输入循环
```

返回**引用**（不是值、不是 void）同时服务两件事：

| 需求 | 靠什么实现 |
|------|-----------|
| 链式调用 `>> a >> b` | 返回 `*this` 的引用，下一次 `>>` 的左操作数还是同一个流 |
| `if (cin >> x)` 能判断成败 | 流到 bool 的转换：`explicit operator bool()`，返回 `!fail()` |

`operator bool` 的演化（**版本断崖**，C++ 程序员最爱踩）：

| 标准 | 形态 | 出处 |
|------|------|------|
| C++98 原始 | `operator void*()` | 空指针 = 失败 |
| C++98 + LWG 468 | 「unspecified boolean type」（Safe Bool 惯用法） | 修 safe bool 问题 |
| **C++11 起（LWG 1094）** | `explicit operator bool() const` | 现在的安全写法 |

> `explicit` 是关键：允许 `if (cin)`，但禁止 `int n = cin;` 这类意外的整型转换。
> 这也是 C++ 面试常考的「为什么 C++98 不能直接用 `operator bool`」——答：会静默转成 int。

## 一次 `>>` 在底层走了几层

```
std::cin >> price        （price 是 double）
  │
  ├─ ① istream::operator>>(double&)             ← 重载决议选中（成员）
  │     ├─ ② sentry 构造：检查 good() → 若 tie() 了就 flush 对方 → 若 skipws 则跳过前导空白
  │     ├─ ③ std::num_get<char>::get(loc, ...)  ← locale facet，做真正的"字符→数字"解析
  │     │     └─ ④ 逐字符 sgetc()/sbumpc()      ← 经 basic_streambuf（虚调用）
  │     │           └─ ⑤ filebuf::underflow()   ← 缓冲空了才 refill
  │     │                 └─ ⑥ read(2)          ← 真正的系统调用
  │     ├─ 解析失败 → setstate(failbit)，value = 0
  │     └─ ⑦ 返回 *this
```

- **第 ② 步 sentry 是每次格式化输入都要付的固定成本**：状态检查 + tie 刷新 + 跳空白。
- **第 ③ 步 num_get 是 locale 依赖的 facet**——虚派发，且意味着解析行为随全局 locale 变化（千分位、小数点都是 facet 决定的）。
- **第 ⑥ 步不是每次都发生**：只有 streambuf 缓冲耗尽才 refill（所以 `strace` 里 `read` 次数远少于 `>>` 次数）。

### 失败语义（写代码时必知）

| 输入情况 | 写入 `value` 的值 | 流状态 |
|---------|-----------------|-------|
| 类型不匹配（期望 int 来了字母） | **0** | `failbit` |
| 有符号整型溢出（太大） | `numeric_limits<T>::max()` | `failbit` |
| 有符号整型溢出（太小） | `numeric_limits<T>::min()` | `failbit` |
| 无符号整型溢出（任一侧） | `numeric_limits<T>::max()` | `failbit` |
| `std::string` 版一个字符都没读到 | **先 `str.erase()` 清空**，无字符则置位 | `failbit` |

> 「失败时把值置 0」不是从 C++98 就有的：原始标准写的是"value 不变"，
> 后来 **LWG 696** 改成置 0 / min / max。写跨平台老代码时要留意。
> 失败后**不 `clear()` 就继续读，后续所有 `>>` 会静默失败**——详见 [8.2 管理条件状态](../ch08-iostream-library/8.2-管理条件状态.md)。

## 自定义类型的 `operator>>`：规范写法

必须为**非成员**（左操作数是 `istream`，你不能给标准库加成员）。两种写法：

### 写法 A：委托给标准版本（推荐，自动获得 sentry 与 failbit 语义）

```cpp
#include <iomanip>   // std::setw
#include <iostream>
#include <string>

// HFT 场景：一条行情快照的文本记录，格式 "AAPL 187.32 100"
struct Tick {
    char   symbol[8];     // 定长字段（HFT 里常见，避免动态分配）
    double price;
    int    qty;
};

std::istream& operator>>(std::istream& is, Tick& t) {
    // ⚠️ char 数组版本（C++20 前）不知道数组有多长 —— 必须显式 setw，否则就是缓冲区溢出
    is >> std::setw(sizeof t.symbol) >> t.symbol;
    is >> t.price >> t.qty;
    return is;                       // 返回流引用，支持链式 + while (is >> t)
}

std::ostream& operator<<(std::ostream& os, const Tick& t) {
    return os << t.symbol << ' ' << t.price << ' ' << t.qty;
}

int main() {
    Tick t;
    // while (std::cin >> t) { std::cout << t << '\n'; }
    return 0;
}
```

> C++20 起数组版本改成 `CharT (&s)[N]`，**编译器知道 N**，上面那个 `setw` 就不需要了
> （但 `char*` 指针版同时被移除）。这正是 C++20 修这个坑的方式。

### 写法 B：自己管 sentry（需要自定义解析逻辑时）

```cpp
std::istream& operator>>(std::istream& is, Tick& t) {
    std::istream::sentry s(is);   // ① 跳空白 + 检查状态 + flush tie
    if (!s) return is;            // ② 约定：失败就原样返回，不碰 t

    std::string sym;
    double px; int q;
    is >> sym >> px >> q;
    if (is.fail() || px <= 0) {           // ③ 业务校验也算失败
        is.setstate(std::ios::failbit);   //    必须置位，否则 while (is >> t) 死循环
        return is;
    }
    std::snprintf(t.symbol, sizeof t.symbol, "%s", sym.c_str());
    t.price = px; t.qty = q;
    return is;
}
```

**三条铁律**：
1. 返回 `istream&`（不是 `void`、不是值）；
2. 失败时**自己置 `failbit`**——这是调用方 `while (is >> t)` 唯一的退出依据；
3. 放在与类型**同一个命名空间**里，靠 **ADL** 被找到（不要往 `namespace std` 里塞）。

## 和 C 的区别

| 维度 | C（`scanf`） | C++（`>>`） |
|------|-------------|------------|
| 怎么知道要读什么类型 | 运行期解析**格式串** `"%d %lf"` | 编译期**重载决议**（`istream::operator>>(double&)`） |
| 写错类型的后果 | `%d` 配 `double` → 静默 UB | 编译期选中正确重载，不可能选错 |
| 忘 `&` | `scanf("%d", v)` → 崩溃 | `>>` 的参数声明就是 `T&`，不存在的错误类别 |
| 成败判断 | 返回成功项数（要自己比对 `== 3`） | `if (is >> a >> b)`（靠 `explicit operator bool`） |
| 可扩展到自定义类型 | ❌ 只能另写函数 | ✅ 重载 `operator>>` |
| 局部性/locale | `setlocale` 影响 `LC_NUMERIC` | 每个流自带 locale（`imbue()`） |
| 缓冲 | `FILE*` 自带 | `istream` → `streambuf` → 可换（`rdbuf()`） |

## HFT / 嵌入式关联

### 热路径为什么不用 `>>`

一次 `>>` 的固定开销构成（对照上面分层图）：

| 层 | 开销来源 |
|----|---------|
| sentry | 状态检查 + `tie()` 的 flush（cin 默认 tie 到 cout！） |
| locale facet | 虚派发 `num_get::get`，解析行为随 locale 变化 |
| 逐字符取数 | 每个字符一次 `sgetc()` **虚调用** |
| 格式化状态 | `flags()` / `width()` / `precision()` 每轮参与 |

两条立刻能做、且只解决部分问题的优化：

```cpp
std::ios::sync_with_stdio(false);   // 关掉与 C stdio 的同步（1.2 已提）
std::cin.tie(nullptr);              // 解掉 cin→cout 的 tie，省掉每次读之前的 flush
```

> 注意：这两条**只去掉同步和 tie**，facet 与逐字符虚调用一点没少。
> 真正的做法是**别用格式化输入**：
> - C++17 `std::from_chars`：无 locale、无分配、无异常，返回 `errc`；
> - 定长二进制协议：`read()` 直接读结构体 / 自研 SIMD 解析（见 `14-hft-engineering`）；
> - 冷路径（配置加载、回放、日志、调试输出）用 `>>` 完全没问题——**先分清热冷，再谈优化**。

**量测方法**（不要凭感觉下结论）：
```bash
# ① read(2) 次数 —— 验证 streambuf 缓冲确实在起作用
strace -e trace=read -c ./a.out < market.txt
# ② 采样看热点是不是落在 num_get / underflow 上
perf record -g ./a.out < market.txt && perf report | grep -i 'num_get\|underflow\|operator>>'
```

### 嵌入式视角

- `#include <iostream>` 会引入 **静态初始化**（`std::ios_base::Init` 保护 cout/cin 的构造顺序），
  在看重启动时间 / 固件体积的场景是实打实的成本；
- locale 的全局状态在无 MMU / 资源受限环境是负担，很多嵌入式工具链的 libstdc++ **裁掉了完整 locale**；
- 结论与 HFT 一致：**控制面用 `>>`，数据面用定长解析**。

## 常见错误

1. **把 `operator>>` 写成成员函数**
   ```cpp
   struct Tick { std::istream& operator>>(std::istream&); };  // ❌
   ```
   成员版的左操作数是 `*this`（Tick），调用变成 `t >> cin`，语义反了。**IO 运算符必须非成员。**
2. **以为 `cin >> x` 等于 `operator>>(cin, x)`** —— 对 `int`/`double` 等内置类型是**成员调用**，
   写成自由函数形式直接编译不过（见前文表格）。
3. **忘了 `>>` 遇空白即停**：`cin >> name` 只读一个词，整行要用 `std::getline`
   （见 [1.2 初识输入输出](../ch01-getting-started/1.2-初识输入输出.md)）。
4. **失败后不 `clear()`**：failbit 一旦置位，后面所有 `>>` 静默失败，
   典型症状是"循环只读了一次就退出"（见 [8.2 管理条件状态](../ch08-iostream-library/8.2-管理条件状态.md)）。
5. **自定义 `operator>>` 不置 failbit**：`while (is >> t)` 变成死循环。
6. **`char buf[8]; is >> buf;`（C++20 前）没加 `setw`** —— 输入超长就是缓冲区溢出。
   改用 `std::string`，或 `is >> std::setw(sizeof buf) >> buf`。
7. **右值当目标**：`cin >> (a + b)` 编译错误——`>>` 的右操作数是 `T&`（非 const 左值引用）。

## 自测题

**1（选择）** `int x; std::cin >> x;` 选中的函数是：
- A. `std::operator>>(std::istream&, int&)`
- B. `std::istream::operator>>(int&)`
- C. 内置右移运算符
- D. 由编译器运行时决定

<details><summary>答案</summary>

**B。** `int` 走的是 `basic_istream` 的**成员**重载。标准库**没有** `operator>>(istream&, int&)` 这个自由函数
（你可以写 `std::istream& (std::istream::*p)(int&) = &std::istream::operator>>;` 验证它确实是成员）。
非成员版本只有 `char&` / `char[N]` / `std::string&` / rvalue 流这几类。
C 错：左操作数是类类型，进入重载决议不是内置运算符。D 错：重载决议在**编译期**完成。

</details>

**2（判断）** 下面两行在 C++03 里都能编译：
```cpp
std::vector<std::vector<int>>  a;   // ①
std::vector<std::vector<int> > b;   // ②
```

<details><summary>答案</summary>

**只有 ② 能过。** C++03 词法用最大匹配，`>>` 是单个 token，① 会被解析成「右移」而报错。
C++11 在 [temp.names] 加了特判，`>>` 在关闭两个嵌套模板参数列表时视为两个 `>`，① 才合法。
→ 这是「`>>` 两层身份真的冲突过」的直接证据，不只是教学上的比喻。

</details>

**3（踩坑）** 输入 `abc` 后，这段代码的 `x` 是多少？
```cpp
int x = 42;
std::cin >> x;      // 用户输入 abc
std::cout << x;
```

<details><summary>答案</summary>

**`0`**（不是保持 42）。类型不匹配 → 置 `failbit`，并把 **0** 写入 `value`（LWG 696 起的规定）。
有符号整型溢出则写 `numeric_limits<T>::max()` / `min()`。
另外：**failbit 不清除，后续所有读取都会静默失败**，必须 `cin.clear()` + `cin.ignore(...)`。

</details>

**4（机制）** 为什么 `operator>>` 必须返回 `istream&` 而不是 `void`？给出两个理由。

<details><summary>答案</summary>

1. **链式调用**：`cin >> a >> b` = `(cin.operator>>(a)).operator>>(b)`，返回引用才能把同一个流接下去；
2. **状态判断**：`if (cin >> x)` / `while (cin >> x)` 依赖流 → `bool` 的转换
   （C++11 起是 `explicit operator bool()`，返回 `!fail()`）。
返回 `void` 两个都做不到；返回**值**（非引用）会拷贝流对象（而且 `basic_istream` 不可拷贝）。

</details>

**5（应用）** 给 `struct Price { int64_t ticks; };` 写 `operator>>`，要求：输入 `18732` 表示 187.32
（即 `ticks = 18732`，隐含两位小数）；非法输入（负数、超范围、非数字）必须让 `while (is >> p)` 正常退出。

<details><summary>答案</summary>

```cpp
#include <cstdint>
#include <istream>
#include <charconv>     // C++17 from_chars（热路径推荐）
#include <string>

struct Price { std::int64_t ticks; };

std::istream& operator>>(std::istream& is, Price& p) {
    std::string tok;
    if (!(is >> tok)) return is;            // sentry + 跳空白 + 失败直接返回

    std::int64_t v = 0;
    const char* first = tok.data();
    const char* last  = tok.data() + tok.size();
    auto [ptr, ec] = std::from_chars(first, last, v);   // 无 locale、无分配、无异常
    if (ec != std::errc{} || ptr != last || v < 0) {    // 解析失败 / 有残留字符 / 负数
        is.setstate(std::ios::failbit);                 // ⭐ 必须置位，否则 while 死循环
        return is;
    }
    p.ticks = v;
    return is;                                          // ⭐ 返回流引用
}
```

三个必考点：① 非成员；② 失败时 `setstate(failbit)`；③ 返回 `istream&`。
用 `from_chars` 而非 `stoll` 的理由：`stoll` 会抛异常、受 locale 影响；`from_chars` 是热路径首选。

</details>

## 延伸

- ← [14.1 运算符重载](./14.1-运算符重载.md)（成员 vs 非成员的基本规则）
- ← [8.1 IO 类体系](../ch08-iostream-library/8.1-IO类体系.md) / [8.2 管理条件状态](../ch08-iostream-library/8.2-管理条件状态.md)（failbit 与恢复）
- ← [8.4 string 流](../ch08-iostream-library/8.4-string流.md)（`istringstream` 拆字段）
- ← [1.2 初识输入输出](../ch01-getting-started/1.2-初识输入输出.md)（`>>` 的第一次出现）
- C++ Primer §14.2 输入和输出运算符；cppreference `basic_istream::operator>>`、`operator>>(std::basic_istream)`、`basic_ios::operator bool`
- 缺陷报告：LWG 696（失败置 0/极值）、LWG 118/661（short/int 走 long）、LWG 468/1094（`operator bool` 演化）
