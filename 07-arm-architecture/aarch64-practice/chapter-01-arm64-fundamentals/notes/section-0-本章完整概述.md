# Ch1 完整总结 · ARM64 体系基础知识

> **《ARM64体系结构编程与实践》** · 奔跑吧Linux社区 · **精读**  
> [章导读](../README.md) · [OUTLINE](../../OUTLINE.md) · [全书总结](../../BOOK-SUMMARY.md) · [AArch64 命名](../../AARCH64-NAMING.md)

---

## 本章定位

建立 **ARMv8-A / AArch64** 整体框架：**不写代码**，全是概念；后面所有章都依赖本章。

| | |
|---|---|
| **实验** | 无板级实验；记牢 EL、寄存器、PSTATE、A/R/M 产品线 |
| **复用** | 第 11 章异常处理会大量用本章术语 — 模糊处不要跳过 |

---

## 1.1 ARM 架构历史与产品线

### ARMv1～ARMv7-A（32 位时代）

ARMv7 三条产品线：

| 产品线 | 角色 | MMU | 典型 |
|--------|------|-----|------|
| **Cortex-A** | 应用处理器，跑 Linux | 有 | 手机 SoC、树莓派应用核 |
| **Cortex-R** | 硬实时 | 无 | 汽车刹车、工业安全 |
| **Cortex-M** | MCU | 无 | STM32、裸机/FreeRTOS |

### ARMv8-A 重大变革

两套执行状态：

| 状态 | 含义 |
|------|------|
| **AArch64** | 64 位；**A64** 指令；64 位 VA / 通用寄存器 |
| **AArch32** | 兼容老 **A32 / T32** |

> **Cortex-M / R 不会升到 AArch64**，仍是 32 位。只有 **Cortex-A** 才有 AArch64。  
> 细节见 [AARCH64-NAMING.md](../../AARCH64-NAMING.md)。

### ARMv9-A（Pi5 · Cortex-A76）

- 在 v8 上增强：SVE2、CCA、安全扩展  
- 部分 v9 核 **砍掉 AArch32，只留 AArch64**（A76 / 树莓派 5：**不能跑老 32 位 ARM 用户程序那条路径**）

**勿混淆：**

| 名字 | 是什么 |
|------|--------|
| **ARMv8-A / v9-A** | 架构规范（说明书） |
| **Cortex-A72 / A76** | 按说明书实现的 **具体 CPU 核** |

> 误区：不要把 A76「当成 ARMv8」——它是 **ARMv9** 核，兼容 v8 功能。Pi4=A72(v8)，Pi5=A76(v9)，**概念框架本章通用**。

---

## 1.2 四个异常等级 EL ⭐（本章最重要）

权限：**EL0 < EL1 < EL2 < EL3**

| 等级 | 角色 | 对应软件 |
|------|------|----------|
| **EL0** | 用户态 | Linux 应用 |
| **EL1** | 内核态 | Linux 内核 |
| **EL2** | 虚拟化 | Hypervisor |
| **EL3** | 安全监控 | Secure Monitor（TrustZone 世界切换） |

关键点：

1. 发生异常 → **升到更高 EL**（不能「异常降级」）；返回用 **`ERET`** 退回原等级。  
2. 普通嵌入式 Linux（Pi 默认）：应用 **EL0**、内核 **EL1**；EL2/EL3 多由固件（如 TF-A）处理完，Linux 日常不用。  
3. 相对 ARMv7-A 的 **7 种工作模式**：AArch64 **抛弃 mode，改用 4 个 EL** — 架构级大变。

---

## 1.3 AArch64 寄存器组

### 通用寄存器 X0–X30 / XZR

| 名字 | 含义 |
|------|------|
| **Xn** | 完整 64 位 |
| **Wn** | 同一寄存器的 **低 32 位**；写 Wn → **高 32 位清零** |
| **X0–X7** | 参数 / 返回值（AAPCS64） |
| **X29** | FP 帧指针 |
| **X30** | LR（返回地址） |
| **XZR** | 零寄存器，只读恒 0（比较、清零） |

> 不再有 ARMv7 那套 **R0–R15** 命名。

### 特殊寄存器

| 寄存器 | 作用 |
|--------|------|
| **SP_ELx** | 每 EL 独立栈指针（用户栈 / 内核栈物理分离） |
| **ELR_ELx** | 异常链接寄存器：异常发生时的 **PC** |
| **SPSR_ELx** | 异常发生时的 **PSTATE** |

> 异常时：**硬件**自动保存 PC→ELR、PSTATE→SPSR；**X0–X30 硬件不自动保存，软件必须手动压栈**。

### PSTATE（拆成多个域）

> **NZCV 专篇：** [NZCV.md](../../NZCV.md)

**NZCV（条件码）**

| 位 | 含义 |
|----|------|
| N | Negative |
| Z | Zero |
| C | Carry |
| V | Overflow（有符号） |

**DAIF（中断屏蔽）**

| 位 | 含义 |
|----|------|
| D | 调试异常屏蔽 |
| A | 异步 SError 屏蔽 |
| **I** | **IRQ 屏蔽**（内核「关中断」常改这一位） |
| F | FIQ 屏蔽 |

其它：PAN、UAO — 控制内核能否直接访问用户空间内存。

---

## 1.4 Cortex-A72 简述（Pi4B）

- 乱序超标量、多核  
- I-Cache / D-Cache / L2；内置 **MMU**、**GIC**；支持 TrustZone  

树莓派 5 是 **A76 / ARMv9**：微架构不同，**本章 EL / 寄存器 / PSTATE 概念完全兼容**。

---

## 1.5 必背小结

1. 只有 **Cortex-A** 有 AArch64；M/R 没有。  
2. **EL0–EL3** 替代 v7 的 7 种模式。  
3. X=64 位，W=低 32 且写清高位；XZR≡0。  
4. PSTATE：**NZCV** + **DAIF**（I=关 IRQ）。  
5. 异常：硬件存 ELR/SPSR；**通用寄存器软件保存**。  
6. **`ERET`**：异常返回，切回原 EL。

---

## 1.6 书中思考题（自测）

1. AArch64 有几个异常等级？Linux 用户程序、内核分别在哪一级？  
2. X0 与 W0 区别？写 W0，高 32 位会保留吗？  
3. XZR 是什么？  
4. ELR_EL1、SPSR_EL1 作用？  
5. PSTATE 的 I 位作用？  
6. AArch32 / AArch64，Cortex-M 支持吗？

**参考答案（自检）：**

1. **4** 个；用户 **EL0**，内核 **EL1**。  
2. X0=64 位整寄存器；W0=低 32；写 W0 → **高 32 清零，不保留**。  
3. **零寄存器**，读永远为 0。  
4. ELR_EL1=进 EL1 异常时的 **返回 PC**；SPSR_EL1=当时的 **PSTATE**。  
5. **屏蔽 IRQ**（关普通中断）。  
6. **不支持** AArch64（也不以「跑 AArch64」为目标）；M 仍是 32 位世界。

---

## 学习建议

- 本章 **只记概念**，不急着写汇编。  
- 下一章 [Ch2 实验环境 · Pi5 适配](../../chapter-02-raspberry-pi-lab/notes/section-0-Pi5适配与实验路线.md)；EL 与寄存器在 Ch11 会再练一遍。

下一章若继续：可按同样格式输出 **第 2 章** 完整总结。
