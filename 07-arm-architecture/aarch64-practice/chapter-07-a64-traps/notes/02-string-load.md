# 7.2 字符串加载陷阱

> 来源：§7.2 · 精读 · [章总览](section-0-本章完整概述.md)

## 本节讲什么

加载字符串常量到寄存器的陷阱——小端模式下的字节顺序与直觉相反，容易混淆。

## 核心要点

### 小端模式下的字符串存储

```
字符串 "ABCD" 的 ASCII 值：
  'A' = 0x41
  'B' = 0x42
  'C' = 0x43
  'D' = 0x44

小端模式内存布局（低地址 → 高地址）：
  地址+0: 0x41 ('A')  ← 低位
  地址+1: 0x42 ('B')
  地址+2: 0x43 ('C')
  地址+3: 0x44 ('D')  ← 高位

加载到 32 位寄存器：
  W0 = 0x44434241   (D 在最高位, A 在最低位)

直觉以为：0x41424344 (A在高位) → 错！
实际是：  0x44434241 (D在高位) → 小端反转
```

### LDR ='string' 伪指令

```asm
; 加载 4 字节字符串到 32 位寄存器
LDR W0, ='ABCD'     ; W0 = 0x44434241（小端）

; 加载 8 字节字符串到 64 位寄存器
LDR X0, ='ABCDEFGH' ; X0 = 0x4847464544434241

; 加载 2 字节
LDR W0, ='AB'       ; W0 = 0x00004241

; 加载 1 字节
MOV W0, #'A'        ; W0 = 0x41（字符字面量）
```

### 端序对比

```
大端模式（Big-Endian）：
  内存: [0x41, 0x42, 0x43, 0x44]  (A B C D 顺序)
  寄存器: 0x41424344              (A 在高位)
  → 直觉一致

小端模式（Little-Endian, AArch64 默认）：
  内存: [0x41, 0x42, 0x43, 0x44]  (A B C D 顺序)
  寄存器: 0x44434241              (D 在高位)
  → 与直觉相反！
```

### 字符串比较技巧

```asm
; 快速比较 4 字节字符串是否为 "FIXT"
; "FIXT" 小端: F=0x46, I=0x49, X=0x58, T=0x54
; 小端加载后 W1 = 0x54584946

LDR W1, ='FIXT'     ; W1 = 0x54584946（比较常量）
LDR W0, [X2]        ; 从内存读取 4 字节
CMP W0, W1           ; 一次比较 4 个字符
B.EQ match

; 8 字节比较
LDR X1, ='ITCH0001'  ; 8 字节常量
LDR X0, [X2]
CMP X0, X1
B.EQ match
```

### 字符串长度限制

```
单条 LDR 最多加载 8 字节：
  LDR X0, ='12345678'     ; 8 字符 ✓
  LDR X0, ='123456789'    ; 9 字符 ✗ → 汇编报错

超过 8 字节的字符串需要：
  - 多条 LDR 分别加载
  - 或用 .ascii/.asciz 定义在数据段，用 ADRP+ADD 获取地址
```

## 与 C 的对照

```c
// C 中字符串常量的内存布局
const char *s = "ABCD";
// 小端: s[0]='A'(0x41), s[1]='B'(0x42), s[2]='C'(0x43), s[3]='D'(0x44)

// 将 4 字节转为 uint32_t（小端）
uint32_t val = *(uint32_t *)"ABCD";
// 小端: val = 0x44434241

// 快速比较
if (*(uint32_t *)buf == *(uint32_t *)"FIXT") { ... }
// 编译器可能用 LDR + CMP 实现
```

## 常见错误

1. **以为 LDR ='ABCD' 得到 0x41424344**：小端模式下实际是 0x44434241，字符顺序反转。
2. **字符串超过 8 字节**：LDR X0 最多 8 字节，超出汇编报错。
3. **混淆字符字面量和字符串**：`MOV W0, #'A'` 是单字符（0x41），`LDR W0, ='A'` 也是 0x41 但通过文字池加载。

## HFT 关联

字符串加载在协议标记识别中有用：
- 快速比较 4/8 字节的协议标记（如 "FIXT", "ITCH"）
- 用 LDR 加载常量字符串后与数据比较 → 一条指令完成 8 字节对比
- 但需注意端序——小端模式下字符顺序反转
- 更推荐用 C 代码 `memcmp`，编译器自动处理端序

```asm
; HFT：快速协议标记识别（FIX 协议）
; 检查数据是否以 "8=FIX" 开头
LDR W1, =0x3D584638    ; "8=FIX" 的小端编码（手动计算）
; 或者
LDR W1, ='8=FI'        ; 前 4 字节
LDR W0, [X2]            ; 读取数据前 4 字节
CMP W0, W1
B.EQ is_fix_protocol
```

## 自测题

1. `ldr x0, ='AB'` 执行后（小端模式），x0 的值是什么？
<details><summary>答案</summary>
'A'=0x41, 'B'=0x42。小端存储：内存 [0x41, 0x42, 0, 0, 0, 0, 0, 0]。加载到 x0 = 0x0000000000004241。注意 B 在高位。
</details>

2. 如何快速检查 4 字节字符串是否为 "FIXT"？
<details><summary>答案</summary>
```asm
ldr x1, ='FIXT'      ; 加载常量（小端：0x54584946）
ldr w0, [x2]         ; 读数据（4字节）
cmp w0, w1
b.eq match
```
用 32 位比较一次检查 4 个字符。注意小端模式下 'F' 在低位。
</details>

3. 为什么字符串超过 8 字节不能单条 LDR 加载？
<details><summary>答案</summary>
LDR 最多加载 64 位（8 字节）。超过 8 字节的字符串需要多条 LDR 或用循环加载。更实际的做法是用 C 代码 `strcmp/memcmp`，让编译器生成高效的比较代码。
</details>

4. `LDR W0, ='ABCD'` 和 `MOV W0, #'A'` 的区别？
<details><summary>答案</summary>
- `LDR W0, ='ABCD'`：加载 4 字节字符串 "ABCD"，通过文字池。W0 = 0x44434241（小端）。汇编器生成 .word 0x44434241 在文字池中，然后 LDR 读取。
- `MOV W0, #'A'`：加载单字符 'A'（0x41），不需要文字池。W0 = 0x00000041。字符字面量 #'A' 直接编码在指令中。
</details>

5. 在大端模式下 `LDR W0, ='ABCD'` 的结果与 小端有何不同？
<details><summary>答案</summary>
大端模式下，内存中 [0x41, 0x42, 0x43, 0x44]（A 在低地址=高位），加载到 W0 = 0x41424344（A 在最高位）。这与小端的 0x44434241 正好相反。因此跨端序比较字符串时，不能直接用 LDR+CMP，需要确保两端使用相同端序。AArch64 默认小端，但可以通过 SCTLR.EE 位切换。
</details>

## 参考与延伸

- 原书 §7.2
- [3.5 LDR 伪指令](../../chapter-03-a64-load-store/notes/section-0-本章完整概述.md)
- [3.1 内存铁律（大小端）](../../chapter-03-a64-load-store/notes/section-0-本章完整概述.md)
