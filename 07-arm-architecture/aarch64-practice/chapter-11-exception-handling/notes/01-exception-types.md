# §11.1 异常类型

> **来源：** [Ch11 完整概述](section-0-本章完整概述.md) · [章导读](../README.md)

## 本节讲什么

ARMv8 的异常分为同步异常和异步异常两大类。本节列举所有异常类型及其触发原因，区分同步（当前指令触发，PC 精确）与异步（与当前指令无关，中断随时到来）。

**通俗类比：** 你正在写代码（CPU 跑用户程序 EL0），突然收到敲门声（异常事件），停下手上工作去门口处理事情（跳异常处理函数），处理完毕回来继续写代码（ERET 返回）。x86 叫中断/陷阱/故障，ARMv8 统一叫 Exception。

## 核心要点

| 类型 | 触发原因 | 同步/异步 |
|------|----------|-----------|
| **SVC** | `SVC #0` 指令（系统调用） | 同步 |
| **IRQ** | 外部硬件中断 | 异步 |
| **FIQ** | 快速中断（高优先级 IRQ） | 异步 |
| **SError** | System Error（异步外部错误） | 异步 |
| **同步异常** | 页错误、未定义指令、对齐错误、SP 对齐错误 | 同步 |

> **同步** = 当前指令触发的，PC 可确定（ELR 精确）
> **异步** = 与当前指令无关，中断随时到来

### 同步 vs 异步的关键区别

- 同步异常的 ELR 指向**触发指令本身**（如 SVC 指令的地址）
- 异步异常的 ELR 指向**下一条将要执行的指令**（中断打断时的 PC）
- 这影响异常返回后从哪里继续执行

### 异常发生时硬件自动完成的动作

异常从低 EL 进入更高特权 EL 时，CPU 硬件**在一条指令周期内**自动完成以下操作，软件无需写代码：

| 步骤 | 硬件动作 | 说明 |
|------|----------|------|
| 1. 保存 PC | 当前 PC → `ELR_ELx` | 同步异常存触发指令地址；异步异常存下一条指令地址 |
| 2. 保存 PSTATE | 当前 PSTATE → `SPSR_ELx` | 保存当前异常等级、中断屏蔽状态、条件标志等 |
| 3. 更新 PSTATE.M | 切换到目标异常等级 | 如 EL0→EL1 时 PSTATE.M 变为 0b0101 |
| 4. 屏蔽中断 | PSTATE.{D,A,I,F} 视类型置 1 | 进入异常处理时自动关中断，防止嵌套 |
| 5. 选 SP 寄存器 | 切换到目标 EL 的 SP | 若目标 EL 用 SP_ELx，则自动切换 |
| 6. 跳转向量表 | PC → `VBAR_ELx + offset` | 根据异常类型和当前状态计算偏移量 |

处理完毕后，软件执行 **ERET** 指令返回：
- 从 `ELR_ELx` 恢复 PC
- 从 `SPSR_ELx` 恢复 PSTATE，回到原来异常等级继续执行

> ⚠️ ERET 只能从高 EL 回到低 EL，不能平级或往更高 EL 跳。

### 异常目标 EL 路由规则

CPU 硬件根据异常类型 + 当前安全状态，决定异常最终去往哪个 EL：

| 触发方式 | 指令/事件 | 目标 EL | 说明 |
|----------|-----------|---------|------|
| SVC | `SVC #imm` | EL1 | 用户态系统调用，EL0→EL1 |
| HVC | `HVC #imm` | EL2 | Hypervisor 调用，EL0/EL1→EL2 |
| SMC | `SMC #imm` | EL3 | TrustZone 安全监控调用，EL0/EL1/EL2→EL3 |
| IRQ/FIQ | 外部中断信号 | EL1 或 EL2 | 由 `SCR_EL3.IRQ/FIQ` 路由配置决定 |
| SError | 异步外部错误 | EL1 或 EL3 | 由 `SCR_EL3.EA` 路由配置决定 |
| 同步异常 | 缺页/未定义指令/对齐 | 当前 EL 或更高 | 取决于异常类型和路由配置 |

> 如果 EL2 不存在（无虚拟化的 SoC），HVC 指令会 trap 到 EL3（若 EL3 配置了路由）或产生未定义指令异常。实际 SoC 上 EL3 几乎总是存在的（TrustZone 需要）。

### 异常 (Exception) vs 中断 (Interrupt)

这是 ARM 新手最常混淆的概念：

| 概念 | 定义 | 包含关系 |
|------|------|----------|
| **异常 (Exception)** | 广义，所有打断 CPU 正常执行流的事件 | 包含同步 + 异步 + SError |
| **中断 (Interrupt)** | 仅指异步异常中的 IRQ/FIQ | 是异常的**子集** |

关键区分：**SVC 系统调用是异常，但不是中断。** x86 里 `INT 0x80` 叫"软中断"，但 ARMv8 术语里"中断"专指异步的 IRQ/FIQ。

### 完整示例：Linux 用户态 read() 系统调用

```
用户程序 (EL0)                      内核 (EL1)
    │                                   │
    │  1. 执行 SVC #0                   │
    │──────────同步异常──────────────→ │
    │  2. 硬件自动:                      │
    │     PC→ELR_EL1, PSTATE→SPSR_EL1   │
    │     切到 EL1, 屏蔽中断              │
    │     PC→VBAR_EL1+offset            │
    │                                   │  3. 内核异常向量入口
    │                                   │     → el0_sync handler
    │                                   │     → 读 ESR_EL1 判断原因
    │                                   │     → 调用 sys_read()
    │                                   │  4. 执行 ERET
    │←─────────返回─────────────────────│
    │  硬件自动:                         │
    │  ELR_EL1→PC, SPSR_EL1→PSTATE      │
    │  切回 EL0, 恢复中断状态             │
    │  用户程序继续运行                   │
```

## HFT 关联

HFT 系统中，各类异常对延迟的影响差异巨大：

| 异常类型 | HFT 影响 | 开销量级 | 应对策略 |
|----------|----------|----------|----------|
| **SVC 系统调用** | EL0→EL1 陷入 + 返回 | 1-3μs | busy-polling 替代 read/epoll，共享内存替代 IPC |
| **IRQ 中断** | 随机打断交易路径 | 100-500ns | 绑核隔离中断（irqbalance + isolcpus），或裸金属禁用 IRQ |
| **页错误** | TLB miss + 缺页处理 | 1-10μs | 启动时预 fault 所有页面（mlock + 触摸每页） |
| **SError** | 不可预测、不可恢复 | 致命 | 硬件级 ECC 内存 + 总线错误检测 |

核心原则：HFT 系统要区分"可预测的慢"（如系统调用，可通过设计避免）和"不可预测的慢"（如异步 SError，只能靠硬件保障）。裸金属 HFT 方案直接在 EL1 运行交易逻辑，彻底消除 SVC 开销；用户态方案则通过 io_uring 或 busy-polling 最小化系统调用次数。

## 自测题

1. **SVC 指令触发的是哪种异常？ELR 保存的地址是什么？**

<details>
<summary>答案</summary>

SVC 触发**同步异常**。ELR 保存的是 **SVC 指令本身的地址**（同步异常的 ELR 指向触发指令）。返回后可以重新执行或跳过。
</details>

2. **IRQ 和 SError 的共同点是什么？它们的 ELR 指向哪里？**

<details>
<summary>答案</summary>

共同点：都是**异步异常**，与当前执行的指令无关。ELR 指向**下一条将要执行的指令**（被中断时的 PC）。IRQ 来自外部硬件，SError 来自系统总线错误。
</details>

3. **页错误（Page Fault）属于哪类异常？为什么？**

<details>
<summary>答案</summary>

**同步异常**。页错误由具体的访存指令触发（如 `LDR X0, [X1]` 访问未映射页面），CPU 在执行该指令时检测到错误，PC 可以精确定位到触发指令。因此是同步的，ELR 指向触发指令。
</details>

4. **异常发生时，硬件自动做了哪些操作？软件需要写代码参与吗？**

<details>
<summary>答案</summary>

硬件自动完成 6 步，软件无需参与：
1. PC → `ELR_ELx`（保存返回地址）
2. PSTATE → `SPSR_ELx`（保存当前状态）
3. 更新 PSTATE.M（切换到目标 EL）
4. 屏蔽中断（PSTATE.{D,A,I,F} 视类型置 1）
5. 切换 SP 寄存器到目标 EL 的 SP
6. PC → `VBAR_ELx + offset`（跳转向量表入口）

软件只需在处理完后执行 ERET，硬件自动从 ELR/SPSR 恢复。
</details>

5. **SVC 是中断吗？为什么？**

<details>
<summary>答案</summary>

**不是中断。** SVC 是**同步异常**——由当前指令直接触发，PC 可精确定位。ARMv8 术语中"中断"专指异步异常（IRQ/FIQ），与当前指令无关。SVC 属于异常的范畴，但不属于中断。x86 里 `INT 0x80` 叫"软中断"是 x86 习惯，ARMv8 不这样称呼。
</details>

6. **如果一个 SoC 没有 EL2（无虚拟化），执行 HVC 指令会发生什么？**

<details>
<summary>答案</summary>

两种情况：
- 如果 EL3 存在且配置了路由，HVC 会 trap 到 EL3
- 如果 EL3 不存在或未配置路由，HVC 产生**未定义指令异常**（同步异常）

实际 SoC 上 EL3 几乎总是存在的（TrustZone 需要 EL3），所以 HVC 通常会 trap 到 EL3 而非产生未定义指令异常。
</details>

## 参考与延伸

- [§11.2 异常等级切换](02-el-switch.md) — 异常如何在不同 EL 间切换
- [§11.5 异常综合征](05-esr.md) — 如何通过 ESR 判断同步异常的具体原因
- [Ch12 中断处理](../../chapter-12-interrupt-handling/notes/section-0-本章完整概述.md) — IRQ 异步异常的完整处理流程
