# 01 · minimal-c-project —— 第一个 CMake 工程

> **状态：⬜ 待跑通**（纪律：跑通之后才写正文；正文将是 `1.X-*.md` 节笔记）

- **目标**：把 `STM32-/labs/00-toolchain-clang` 的 Makefile 工程迁成 CMakeLists.txt，
  产物与 Make 版逐字节一致（`.elf` / `.bin` 可对比 `llvm-size`）
- **交付物**：迁移后的 CMakeLists.txt + Make 版对照 + 笔记正文（讲 target / 属性 / 现代 CMake 原则）
- **对应书**：《CMake构建实战》前几章；BOOK-MAP 见模块 `_refs/`
- **待实测输出**
- **坑点**：待记
