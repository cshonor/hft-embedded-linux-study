# P1 — CPU 模拟器

> 用 Logisim / Verilog 搭一个 8-bit CPU，把"程序在硬件上怎么跑"从黑盒变白盒。

## 项目目标

在硬件层面建立 CPU 的工作模型——ALU 怎么算、寄存器怎么存、控制器怎么按时节拍驱动指令周期。不追求商业级，追求**自己讲得通每根信号为什么这么连**。

## 交付物

- [ ] ALU（加减与/或/移位 + 标志位 Z/C/N/V）
- [ ] 寄存器堆（≥4 个通用寄存器 + PC + IR）
- [ ] 指令译码器（自定义指令集，含 R/I/J 三类）
- [ ] FSM 控制器（取指→译码→执行→写回，多周期）
- [ ] 内存接口（指令 + 数据存储）
- [ ] 至少 3 个汇编程序跑通：加法累加、斐波那契、内存拷贝

## 覆盖模块

| 模块 | 用到什么 |
|------|----------|
| [`00` digital-logic-cpu](../../00-digital-logic-cpu/) | 组合/时序逻辑、寄存器、FSM、CPU 数据通路 |

## 前置

无（Phase1 第一个项目）。

## 学习目标

- setup/hold 时间、时钟域、寄存器锁存的物理含义
- 控制信号如何由 FSM 按节拍产生
- 指令集 = 数据通路 + 控制信号的编码约定
- 为后续 `02` CSAPP Ch4（Y86 流水线）建立硬件直觉

## 里程碑

1. **M1** Logisim 搭出 ALU + 寄存器堆，手动单步能算
2. **M2** 定义指令集，写出译码器真值表
3. **M3** FSM 控制器，多周期跑通第一条指令
4. **M4** 跑通 3 个测试程序，能解释每个周期的信号

## 参考模块

- [00-digital-logic-cpu/](../../00-digital-logic-cpu/) — 组合/时序/CPU 词汇、CSAPP↔数字逻辑学习路线

## 工具

- Logisim（图形化，快速验证）或 Verilog（iverilator + GTKWave，更接近真实）
- 深度约束：组合/时序取黑盒语义，门级/Verilog 不主攻——能讲通功能即可
